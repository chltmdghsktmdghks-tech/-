# 음악 낙서장 — 네이티브 오디오 엔진 (이전 작업)

기존 앱: `~/AI Company/music_doodle` (Flutter + WebView + `assets/app.html`)
이 프로젝트: `~/AI Company/music_doodle_engine` — 소리를 네이티브로 옮기는 작업

**기존 앱은 손대지 않는다.** 스파이크가 실패하거나 이전이 길어져도 그쪽은 언제든 출시 가능해야 한다.

---

## 왜 옮기는가

찢어짐만이 이유가 아니다. 순서대로:

1. **샘플** — 실제 드럼/악기 샘플을 쓰려면 파일을 따로 두고 읽어야 한다.
   HTML 한 파일에 base64로 박으면 5~10MB가 되고 앱 시작이 눈에 띄게 느려진다.
2. **서버** — 로그인·클라우드 저장·공유를 하려면 인증 토큰을 WebView와 Flutter가 주고받아야 한다.
   그 경계가 계속 문제를 만든다. Flutter 쪽에 다 있으면 그냥 되는 일이다.
3. **찢어짐** — 위 둘에 비하면 작은 이유. 다만 지금 당장 사용자를 괴롭히는 건 이것이다.

웹 버전에서 찢어진 구조적 이유:
- 음마다 오실레이터·필터·게인 **노드를 만든다** (훅 구간에서 초당 264개까지 측정됨)
- `setTimeout` 으로 앞으로 몇백 ms 를 **미리 예약**한다
- 보급형 폰에서 타이머가 밀리면 예약이 과거로 가고, 밀린 음이 한꺼번에 쏟아진다 → 찢어짐
- 스케줄러 여유를 120ms→350ms 로 늘리고 `latencyHint:'balanced'` 로 버퍼를 키워도 **완전히는 안 없어졌다**

---

## 지금 구조 (스파이크)

```
flutter_pcm_sound  ──"샘플 더 줘"──▶  Engine.render(frames)
   (Android=Oboe)                        │
                                         ├─ 스텝 시퀀서: 몇 샘플 뒤에 다음 16분음표인가
                                         └─ Voice 48개: 각자 다음 샘플 값을 만든다
                                            → 합산 → 소프트 리미터 → Int16
```

**노드가 없다.** 오디오 장치가 부를 때 그 구간을 계산해 채운다. 밀릴 수가 없다.
화면이 아무리 바빠도 오디오는 자기 속도로 흘러간다.

### 파일
- `lib/engine.dart` — `Voice`(엔벨로프+파형+필터), `Engine`(시퀀서+믹스), `AudioHost`(패키지 연결)
- `lib/main.dart` — 시험 화면. **화면 부하 스위치**가 있다(매 프레임 도형 600개).
  웹 버전에서 정확히 이 상황이 찢어짐을 만들었으므로, 여기서 멀쩡하면 이전이 옳다는 뜻.

### 스파이크가 내는 소리
실제 앱의 **트랩 훅과 같은 밀도**: 140 BPM · 킥 · 스네어 · **16분 하이햇** · 808 · 패드 3음 · 벨 · 16분 플럭.

---

## 빌드 함정 (반드시 기억)

`flutter_pcm_sound` 는 `compileSdk 33` 으로 고정돼 있는데 최신 androidx 가 34 이상을 요구한다.
`android/build.gradle.kts` 에서 하위 모듈 전부를 34 / minSdk 23 으로 올려야 빌드된다.

> **순서가 중요하다.** `subprojects { afterEvaluate { ... } }` 를
> `subprojects { project.evaluationDependsOn(":app") }` **보다 먼저** 둬야 한다.
> 뒤에 두면 이미 평가가 끝나서 `Cannot run Project.afterEvaluate` 오류가 난다.

`android/app/build.gradle.kts` 의 `minSdk = 23` 도 함께 필요하다.

---

## 이전 계획 (스파이크 통과 후)

각 단계마다 **들을 수 있는 결과물**이 나오게 쪼갠다.

| 단계 | 내용 | 세션 |
|---|---|---|
| 0 | ✅ 스파이크 — 네이티브 오디오가 안 찢어지는지 확인 | 1 |
| 1 | 신디사이저 이식 — 배음표·몸통공명·ADSR·RING (웹의 `pitched()` 대응) | 4~6 |
| 2 | 드럼 5키트 × 10악기 (웹의 `vKick`~`vRide` 대응) | 2~3 |
| 3 | 믹서·이펙트 — EQ/컴프/리버브/딜레이/사이드체인, 곡별 믹스 | 3~4 |
| 4 | 곡 데이터 이식 — `MIDI_LIB`/`SONG_FORMS`/`GENRE_MIX` 를 Dart 로 | 2 |
| 5 | UI — 씬/타임라인/믹서/편집기를 Flutter 위젯으로 | 6~8 |
| 6 | 저장·WAV 내보내기·곡 만들기 질문 | 2~3 |

**총 20~27세션.** 한 세션에 3~5개 항목이 적당하다(토큰 한도).

### 이식할 때 웹 코드에서 가져올 것
`~/AI Company/music_doodle/assets/app.html` 에 다음이 **검증된 상태로** 들어 있다. 그대로 옮기면 된다.
- `HARM` 배음표 · `ADDITIVE` 배음별 감쇠 · `BODY_RES` 몸통공명 · `KEYTRACK`
- `SUSLV` 서스테인 레벨 · `RING` 울림 길이(비율 1.00 = 그린 길이만큼)
- `DRUM_KITS` 5키트 × 10악기 전 파라미터
- `MIDI_LIB` + `LONG_LIB` + `LONG_LIB2` 패턴 전부
- `SONG_FORMS` 5곡 · `GENRE_MIX` 곡별 믹스(hpf 음역정리 포함)
- 그쪽 `HANDOFF.md` 6-A~6-L 에 **왜 그 값인지와 함정**이 다 적혀 있다

---

## 0단계 결과 — ✅ 통과 (2026-08-11, 갤럭시 A17 실기)

| 조건 | 결과 |
|---|---|
| 악기 64종 · 화면부하 2배 | **오디오 여유 87%** |
| 버퍼 바닥 | **0회** |
| 음 부족 | **0회** |

실제 곡은 7트랙이므로 여기의 9분의 1 부하다. **이전이 옳다는 게 실기로 확인됐다.**
웹 버전이 초당 노드 264개에서 찢어졌던 것과 같은 폰에서 자릿수가 다른 여유다.

---

## 1단계 — 신디사이저 이식 ✅ (2026-08-11 청감 확인 완료)

악기 33종 · 지연 43ms · 부하 시험 전 항목 통과. 사용자 확인 "이제 쓸 만 한 거 같아".
아래는 그 과정에서 알아낸 것들 — **다시 만질 때 여기부터 읽을 것.**

### 새 파일

| 파일 | 내용 |
|---|---|
| `lib/dsp.dart` | 웨이브테이블 · 바이쿼드 · 엔벨로프 · 잡음 — Web Audio 가 해주던 것들 |
| `lib/instruments.dart` | 웹의 검증된 표를 **그대로** 옮긴 것 (INSTRUMENTS/HARM/ADDITIVE/BODY_RES/RING/SUSLV/SPACE/…) |
| `lib/synth.dart` | `SynthNote` — 웹 `pitched()` 의 이식 |
| `lib/engine.dart` | 음 배분 · 스테레오 합산 · 예약 재생 · 진단 |
| `lib/main.dart` | 시험 화면 — 악기 33종 · 건반 · 음길이 · 부하시험 |

### 옮기면서 새로 만들어야 했던 것 (웹에선 공짜였던 것)

1. **밴드리밋 웨이브테이블** — 톱니를 `2*phase-1` 로 만들면 높은 음에서 에일리어싱이 난다.
   22050Hz 를 넘는 배음이 낮은 음으로 접혀 들어와 쇳소리가 섞인다. 브라우저는 알아서 막아 준다.
   → 옥타브(11단)마다 배음 수를 달리한 표를 만들어 음높이에 맞는 걸 고른다.
   → 만들 때 `sin()` 을 수백만 번 부르면 시작이 느려지므로, 한 주기 길이의 사인표를 두고
     n배음은 인덱스를 n배로 건너뛴다. 배음을 1번부터 누적하며 각 옥타브 한계에서 스냅샷을 뜬다.

2. **Web Audio 의 Q 는 데시벨이다** ← 이거 놓치면 소리가 통째로 달라진다.
   웹 코드의 `lp.Q.value=0.7` 은 Q=0.7 이 아니라 `10^(0.7/20)=1.08` 이다.
   `moogbass` 의 `q:4.5` 는 1.68. 변환을 빼먹으면 필터가 웹보다 훨씬 날카로워진다.
   (peaking 의 Q 는 그냥 선형이다. lowpass/highpass 만 dB)

3. **필터 계수 갱신 주기** — 컷오프가 움직이는 악기(뜯는 악기의 밝기 감소, 워블 LFO)는
   매 샘플 계수를 다시 구하면 비싸다. 32샘플(0.7ms)마다만 갱신한다. 귀로는 구분 안 된다.

4. **스테레오** — 스파이크는 모노였다. 유니즌 폭(`SPACE.w`)이 들리려면 스테레오여야 한다.
   부분음마다 등파워 패닝으로 L/R 에 넣고, 로우패스·몸통공명을 채널마다 하나씩 돌린다.

### 웹의 별난 동작을 그대로 옮긴 것 (고치지 않았다)

가산합성(`ADDITIVE`) 악기는 **몸통공명이 있으면** 필터·앰프엔벨로프를 타고,
**없으면** 부분음이 출력으로 직행한다 (웹 `pg.connect(BR?lp:d)`).
그래서 피아노·기타·마림바·하프는 필터를 타고, 일렉피아노·벨·플럭은 안 탄다.
일관성은 없지만 **이게 검증된 소리**라 그대로 뒀다. 바꾸려면 청감 비교부터 해야 한다.

### 지연(레이턴시) 잡기 — 원인이 세 겹이었다

건반을 눌러도 늦게 소리가 났다. 세 개가 겹쳐 있었다.

**① 표본화 주파수가 장치와 달랐다 (44100 → 48000)**
`adb shell dumpsys media.audio_flinger` 로 확인: A17 의 출력 스레드는 전부 48000Hz.
44100 을 밀면 안드로이드가 매 샘플 리샘플링을 하고, **그 순간 저지연 경로 자격을 잃는다.**

**② 플러그인이 AudioTrack 버퍼를 `getMinBufferSize()` 로 잡았다 ← 제일 컸다**
그 값은 저지연용이 아니라 **일반(딥 버퍼) 경로의 최소값**이다. A17 실측 6156프레임 = **138ms**.
→ `PROPERTY_OUTPUT_FRAMES_PER_BUFFER`(장치 버스트, A17=256) × 4 로 잡고
  `PERFORMANCE_MODE_LOW_LATENCY` 를 요청하도록 고쳤다. 거부되면 원래 방식으로 되돌아간다.

**③ 우리 큐가 컸다** — `feedThreshold` 6000프레임(×2 = 272ms). 768 로 낮췄다(화면에서 조절 가능).

결과 (`dumpsys` 실측):

| | 전 | 후 |
|---|---|---|
| Flags | `0x000` | **`0x001` (FAST)** |
| FrmCnt | 6156 | 1024 |
| Latency | **138ms** | **31ms** |

> 확인 명령 (앱을 켜 둔 채로):
> `adb -s RFKL505TRAZ shell dumpsys media.audio_flinger | grep -E "^ +F[0-9]+ +[0-9]+ +yes"`
> 필드 9=Flags, 26=Underruns, 35=Latency(ms). 언더런이 **계속 늘면** 버퍼가 너무 작은 것이다
> (시작 직후 한 번 쌓이는 건 정상 — 먹이기 전이라 그렇다).

플러그인은 `packages/flutter_pcm_sound` 로 복사해 고쳐 쓴다. pubspec 의 `path:` 의존.
**패키지를 올리면 이 수정이 날아간다.** 올릴 일이 생기면 위 두 군데를 다시 넣어야 한다.

#### ✅ 해결 — 오디오를 백그라운드 아이솔레이트로 옮겼다 (`lib/audio_isolate.dart`)

원인은 화면 캡처로 확정됐다: **오디오 여유 98% · 리미터 감쇠 -1.5dB 인데 버퍼 바닥 538회.**
CPU 도 음량도 아니었다. 소리를 *만드는* 게 아니라 *넘기는* 길이 막힌 것이다.

`flutter_pcm_sound` 의 feed 콜백은 **루트(UI) 아이솔레이트로** 온다.
그 아이솔레이트는 위젯도 다시 그린다 → 건반을 누르면 setState → 재빌드 → 그동안 feed 가 밀린다.
**웹(WebView)에서 도망쳐 나온 것과 같은 종류의 문제**가 작게 남아 있었다.

→ 콜백을 아예 안 쓴다. 백그라운드 아이솔레이트가 **스스로 시계를 보고** 모자란 만큼 채운다.
`BackgroundIsolateBinaryMessenger.ensureInitialized(rootIsolateToken)` 가 전제.
(플러그인 콜백은 여전히 루트로만 가므로 받을 수 없다. 그래서 잔량 조회 메서드를 플러그인에 추가했다)

**옮기면서 밟은 함정 세 개 — 다시 만지면 또 밟는다:**

1. **목표 버퍼량은 '총량'이어야 한다.** 처음엔 512프레임을 목표로 잡았는데,
   AudioTrack 자체가 1024프레임을 원한다. 총량이 그보다 작으면 AudioTrack 이 늘 굶는다.
   → 목표 2048프레임(43ms). 그 안에 AudioTrack 몫 1024가 들어 있다.
2. **잔량 조회를 `await` 하면 안 된다.** 이 왕복이 10ms 넘게 걸리는 순간이 있고,
   그동안 급식이 멈춘다. 실측에서 1.5초마다 언더런이 뭉텅이로 늘었다. 던져 놓고 나중에 반영한다.
3. **드리프트 보정은 한 번에 조금씩.** 확 맞추면 그 순간 급식량이 튀어서 버퍼가 출렁인다
   (지연이 19~31ms 사이를 오갔다). 한 번에 ±64프레임까지만 움직인다.

**최종 실측 — 160음 쏟아붓기 + 화면부하 2배 (A17):**

| | 값 |
|---|---|
| 버퍼 바닥 | **0회** |
| 음 부족 | **0회** |
| 하드클립 | **0회** |
| 최대 동시음 | 29 |
| 오디오 여유 | 98% |
| 지연 | 43ms (총) |
| 화면 FPS | 60 (최저 58) |

#### 참고 — UI 스레드에서 급식하던 시절의 바닥

더 줄여 봤다: 장치 버퍼 512프레임(21ms) + 큐 384. 저지연 플래그는 잡혔는데
**언더런이 초당 1000개 넘게 계속 늘었다.** 구조 때문이다:

```
AudioTrack 이 비면 → 쓰기 스레드가 큐에서 꺼내 채움
                     └ 그 큐는 Dart 의 feed 콜백이 채움
                       └ **그 콜백은 UI 스레드를 거친다**
```

화면이 한 번 버벅이면 그대로 소리가 끊긴다. 버퍼가 그 지연을 흡수해 줘야 한다.
게다가 임계값을 384 로 낮추면 콜백이 초당 125번이 되어 채널 왕복 자체가 병목이 된다.

실측 하한: **장치 버퍼 1024프레임(31ms) + 큐 768(32ms)**. 언더런 0 유지.
**더 내리려면 엔진을 백그라운드 아이솔레이트로 옮겨야 한다** (`BackgroundIsolateBinaryMessenger`).
그러면 UI 버벅임과 오디오가 완전히 분리된다. → 다음 작업 1순위.

> 참고: FAST 플래그(`0x001`)는 장치의 fast track 여유에 따라 붙었다 말았다 한다.
> 붙지 않아도 latency 31ms 는 그대로였다. 중요한 건 버퍼 크기지 플래그가 아니다.

### 지직거림 — 리미터가 범인이었다

"온음표로 10음 이상 치면 지직거린다" 의 원인.

예전 코드는 샘플마다 `s / (1 + (|s| - 0.85))` 로 **파형을 그 자리에서 구부렸다.**
이건 리미팅이 아니라 **왜곡(distortion)** 이다. 음이 몇 개만 겹쳐도 합이 1을 넘고,
넘는 순간마다 파형이 꺾이면서 없던 배음이 생긴다 — 그게 지직거리는 소리다.

→ 제대로 된 리미터로 바꿨다. '지금 얼마나 줄여야 하는가'(`_gr`)를 따로 추적해서
그 값을 **부드럽게** 곱한다. 내려갈 땐 1ms, 올라올 땐 120ms.
파형은 그대로 두고 크기만 움직이므로 왜곡이 안 생긴다.
앞에 `masterGain = 0.55` 로 겹칠 여유를 미리 비워 뒀다(나중에 믹서가 이 자리를 대신한다).

**그런데 어택만 빠르게 해서는 못 막는다** — 그 다음 실측에서 드러났다.
피크 1.48 인데 감쇠는 -1.9dB 뿐이었고 **하드클립 152회**가 났다.
이유: 목표 게인을 **순간값 하나**로 정했기 때문. 뾰족한 피크는 몇 샘플 만에 지나가 버려서
게인이 조금 내려가다 만다. 그런데 그 피크는 룩어헤드만큼 뒤에 출구에 도착하고,
그때는 게인이 이미 복구돼 있다.

→ '최근 최대값'을 들고 있게 고쳤다(엔벨로프 팔로워: 올라갈 땐 즉시, 내려갈 땐 120ms).
감쇠 -1.9dB → **-3.7dB**, **하드클립 152회 → 0회**.

세 겹이 다 필요했다: **룩어헤드(4ms) + 엔벨로프 팔로워 + 부드러운 게인**.
하나라도 빠지면 지직거린다.

화면에 **리미터 감쇠 dB** 와 **하드클립 횟수**를 띄웠다. 이 숫자로 원인을 가른다:
- 하드클립 > 0 → 리미터가 못 따라잡음. 룩어헤드를 늘리거나 `masterGain` 을 내린다
- 감쇠가 크고(-10dB 이하) 지직거림 → 음량 문제. `masterGain` 을 더 내린다
- 감쇠도 클립도 작은데 지직거림 → 오디오가 못 따라간 것. 버퍼/부하 문제

같이 고친 것: 바이쿼드 계수 갱신 간격 32 → 16샘플.
계수가 갑자기 바뀌면 상태와 안 맞아 '탁' 소리가 나는데, 음이 겹칠수록 티가 난다.

### 서스테인 — 웹에서 못 고쳤던 진짜 원인

"온음표를 그려도 금방 끊긴다"를 웹에서 `RING` 비율을 1.00 으로 올려 고치려 했는데 체감이 거의 안 바뀌었다.
원인은 비율이 아니라 **곡선 모양**이었다.

웹은 어택 뒤 곧바로 `exponentialRampToValueAtTime(0.0001, t+rel)` 로 **0까지** 떨어뜨린다.
지수 곡선은 앞쪽에서 대부분을 잃는다 — 3초짜리 음도 1초면 -25dB, 1.5초면 -37dB 라 사실상 안 들린다.
`rel` 을 3초로 늘려도 "3초 동안 들리는" 게 아니라 "3초에 걸쳐 0이 되는" 것뿐이었다.

→ 두 토막으로 나눴다 (`buildRing`):
```
body : 음표 길이 동안 peak → peak*0.20 으로 완만하게   (끝까지 들린다)
rel  : 그 뒤 RING[0] 초 동안 정리                      (실제 피아노의 댐퍼)
```
가산합성 배음도 같은 모양. 배음마다 `body = 음표길이 × 감쇠배율` 이라
1배음은 음표 길이만큼 살고 높은 배음은 먼저 죽는다(원래 의도 유지).

끝에 남는 크기는 `kRingTail = 0.20` (synth.dart). 줄이면 웹처럼 빨리 사라진다.
**이건 웹에서 옮긴 게 아니라 새로 고친 것이다.** 웹 버전을 계속 쓸 거면 거기에도 같은 수정이 필요하다.

### 아직 안 옮긴 것
- 드럼 (2단계) · 믹서/이펙트 (3단계) · 곡 데이터 (4단계)
- `vocal` 보컬찹 — 포먼트 밴드패스 3개. 별도 경로라 뒤로 미룸
- 휴머나이즈의 타이밍 흔들기(`hTime`) — 시퀀서가 생기는 4단계에서

### 확인할 것 (폰)
1. **전체 훑기** — 33종이 차례로. 웹 앱과 같은 악기를 번갈아 들어 비교
2. **음 길이 `온음표`** 로 두고 피아노/기타/벨 — 그만큼 울리는가 (웹에서 계속 지적됐던 부분)
3. **아르페지오** — 글라이드(앞 음에서 미끄러짐)가 들리는가
4. **음 쏟아붓기 + 화면부하 2배** — 버퍼 바닥 0 유지되는가

---

## 2단계 — 드럼 이식 (2026-08-12, 코드는 완성 · **폰 실측 미검증**)

⚠️ **이 세션은 폰(RFKL505TRAZ)이 연결되지 않아 소리를 들어보지 못했다.**
`adb devices` 에 기기가 안 잡혔다(오프라인 에뮬레이터만 보임). 아래는 전부 코드 리뷰 +
폰 없이 돌리는 수치 스모크 테스트(`tool/drum_smoke.dart`)로만 확인한 상태다.
**다음 세션은 반드시 `adb devices` 로 폰 연결부터 확인하고, 아래 "다음에 폰에서 확인할 것"부터 시작할 것.**

### 옮긴 것
타악기 10종(킥·스네어·하이햇·탐·크래시·라이드·림샷·박수·쉐이커·카우벨) × 키트 5종
(어쿠스틱·808·909·로파이·록) — 웹 `DRUM_KITS` 전체 데이터와 `vKick`~`vCowbell` 신호 흐름을
값 하나 안 바꾸고 그대로 옮겼다.

| 파일 | 내용 |
|---|---|
| `lib/drums.dart` (신규) | `DRUM_KITS` 데이터 5키트 + `DrumVoice`(타격 하나의 신스) |
| `lib/dsp.dart` | `Env.start({from})` 추가 · `satWave()`(킥 드라이브) · `metalWave()`(하이햇/크래시/라이드 금속) |
| `lib/engine.dart` | 드럼 전용 보이스 풀(`kDrumPolyphony=24`) · `drumOn()` · `scheduleDrum()` — 기존 피치 있는 음(`_queue`)과 같은 샘플-정확 예약 큐를 공유 |
| `lib/audio_isolate.dart` | `AudioClient.drumOn/drumBatch` · `AudioStats` 에 드럼 지표 3개 추가 |
| `lib/main.dart` | 키트 선택 칩 · 타악기 10개 패드 · "드럼 훅 부하 시험"(140BPM·16분 하이햇·8마디 — 0단계 스파이크와 같은 밀도) |
| `tool/drum_smoke.dart` (신규) | 폰 없이 돌리는 수치 확인 — `dart run tool/drum_smoke.dart` |

### 왜 SynthNote 를 재사용하지 않았나 — 레이어 3종 구조
피치 있는 음은 "배음을 쌓아 하나의 음"이지만, 드럼 타격 하나는 짧은 레이어 몇 개가
동시에 울리고 끝난다. 그래서 `DrumVoice` 를 따로 만들고, 레이어를 3종으로 나눴다:

- **Tone** — 오실레이터 1개. 피치가 시간에 따라 떨어질 수 있다(킥·탐·림의 '때리는' 느낌).
  필터를 걸 수도 있는데, **엔벨로프 뒤에** 건다(탐의 쉘 공명 — 웹 `o→g→pk→dest` 순서 그대로).
- **Noise** — 잡음이 필터(최대 3단)를 거친 **뒤** 엔벨로프를 곱한다(웹 `noise→filters→gain`).
  스네어만 밴드패스 중심주파수가 시간에 따라 움직인다(열렸다 닫히는 느낌).
- **Cluster** — 오실레이터 여러 개(금속 클러스터 6개 또는 카우벨 2개)를 합쳐 **엔벨로프 앞**
  필터 1단을 거친다(웹 `osc들→highpass/bandpass→gain`).

즉 **톤은 필터가 엔벨로프 뒤, 노이즈/클러스터는 필터가 엔벨로프 앞** — 웹 코드를 함수별로
하나하나 따라가며 확인한 순서라 헷갈리기 쉽다. 다시 만질 때 이 순서부터 의심할 것.

### 밟은 함정

1. **`Env` 를 주파수 램프로도 재사용했다.** 킥·탐·림은 피치가 시간에 따라 떨어지는데,
   기존 `Env` 는 "무음(0.0001)에서 시작해서 무음으로 끝나는" 음량 전용이었다.
   `start()` 에 `{double from = kSilent}` 를 추가해서, 시작값을 임의로 줄 수 있게 했다
   (`freqEnv.start(from: K.f)` 처럼). **`Env.next()` 는 구간이 다 끝나면 값을 0 으로 돌려준다**
   (음량은 그래야 맞으니까) — 그래서 주파수 램프는 마지막 구간 뒤에 반드시
   `e.add(3.0, 마지막값)` 같은 긴 '유지' 구간을 붙여야 한다. 안 붙이면 피치 스윕이 끝나는
   순간 오실레이터가 조용히 죽는다(들어보면 "픽" 하고 끊기는 소리로 들릴 것).
2. **새추레이션·금속 클러스터는 타격마다 만들지 않는다.** 웹은 `WaveShaper`/오실레이터를
   그때그때 만들었지만, 킥의 드라이브 파형도 하이햇의 금속 클러스터도 **배음 구조가 고정**이다.
   `satWave(amt)` 와 `metalWave()` 를 `WaveSet.fromHarmonics()` 로 한 번만 굽고 캐싱했다
   (1단계에서 쓴 것과 같은 인프라 재사용 — 옥타브별 에일리어싱 방지 표까지 공짜로 딸려온다).
3. **스네어의 이동 밴드패스는 16샘플마다만 계수를 다시 구한다** (`_kModStride`, synth.dart 와
   같은 값). 매 샘플 다시 구하면 저사양 폰에서 비용이 늘고, 그렇다고 너무 뜸하면 필터가
   덜컥거린다. `Env.next()` 자체는 매 샘플 불러 진행시키되(그래야 타이밍이 안 어긋난다),
   바이쿼드 계수 갱신만 간격을 둔다.
4. **드럼은 아직 팬(pan)이 없다.** 웹의 `SPACE.drum` 은 `{pan:0}` 이라 전부 가운데다 —
   믹서(3단계)에서 트랙 단위로 좌우를 배치하므로 지금은 굳이 넣지 않았다.
5. **드럼 폴리포니는 24개로 따로 잡았다** (`kDrumPolyphony`, 피치 있는 음의 96개와 별도 풀).
   16분 하이햇을 최대 밀도로 쏟아부어도(부하 시험 스크립트 기준) 동시에 6개를 넘지 않아서
   여유가 크다 — 모자라면 `drumStarved` 지표가 올라간다.

### 스모크 테스트 (폰 없이 돌린 것 — 소리 자체는 확인 못 함)

```
cd "$HOME/AI Company/music_doodle_engine" && dart run tool/drum_smoke.dart
```
결과: 10종×5키트 50개 조합 전부 예외 없이 렌더됨 · 140BPM 16분 하이햇 8마디 + 무작위
피치 음 80개를 동시에 돌려도 **드럼 음부족 0 · 음부족 0 · 하드클립 0 · 버퍼 바닥 0 ·
리미터 후 피크 0.96**(1.0 안 넘음, 정상). 드럼 동시 최대 6개, 피치 있는 음 동시 최대 13개.
→ 숫자로는 건강하다. **하지만 이건 '안 터진다'만 확인한 것이지, 킥이 킥처럼 들리는지는
전혀 확인 못 했다.**

### 빌드 상태
`flutter analyze` 오류 0 (packages/ 안 info 경고만, 무시 대상).
`flutter build apk --release` 성공 — `build/app/outputs/flutter-apk/app-release.apk` (44.6MB).
**폰 미연결로 설치·실행은 못 했다.** `~/AI Company/music_doodle/공유/` 로 복사도 안 했다
(그건 폰이 잡혔을 때 하는 단계라).

### 2026-08-12 재확인 세션 — 여전히 폰 미연결
`adb devices` 재시도(daemon 재시작 포함)해도 `RFKL505TRAZ` 안 잡힘.
`emulator-5562 offline` 만 보임 — 이건 우리 폰이 아니라 정체불명 오프라인 에뮬레이터라
무시해도 된다(계속 나타나면 `adb -s emulator-5562 emu kill` 로 정리 고려).
코드 변경 없음 — `flutter analyze` 재확인(오류 0), `flutter build apk --release` 재확인(성공,
44.6MB)만 하고 **새 단계(3단계 믹서)는 시작하지 않았다.** 이유: 지시서상 2단계는 폰 청감
검증 전까지 "미완료"로 취급해야 하고, 검증 안 된 채로 다음 단계를 쌓으면 문제가 어디서
생겼는지 못 가른다. **다음 세션도 반드시 `adb devices` 확인부터.** 폰이 잡히면 위
"다음에 폰에서 확인할 것" 1~5번을 그대로 따르면 된다(코드는 이미 준비 완료 상태).

### 다음에 폰에서 확인할 것 (반드시 이것부터)
1. `~/Library/Android/sdk/platform-tools/adb devices` 에 `RFKL505TRAZ` 가 `device` 로 잡히는지 먼저 확인.
2. 잡히면: 이 코드는 이미 빌드까지 끝났으니 `flutter build apk --release` 다시 돌리거나
   기존 APK를 그대로 설치 → `공유/오디오엔진시험.apk` 로 복사 → `adb install -r` → 실행.
3. **사용자가 귀로 들어야 할 것**:
   - 새 "드럼 키트" 섹션에서 5키트 × 10종 패드를 눌러 웹 앱과 번갈아 들어 비교
     (같은 킥이 808/909/록에서 확실히 다르게 들려야 한다 — 이게 이번에 옮긴 핵심)
   - "드럼 훅 부하 시험" 버튼 — 140BPM 16분 하이햇이 화면부하 2배에서도 안 찢어지는지
     (0단계 스파이크가 확인했던 것과 같은 밀도를 드럼으로 재현한 것)
   - 스네어를 여러 번 쳐서 '탁' 소리가 기계적으로 반복되지 않는지 (밴드패스가 실제로 움직이는가)
4. `dumpsys media.audio_flinger` 로 언더런 0 유지되는지, 화면 지표(버퍼 바닥·드럼 음부족·
   하드클립·오디오 여유)가 A17 기준값 근처인지 확인.
5. 문제없으면 이 섹션의 "⚠️ 폰 실측 미검증" 을 실측값으로 덮어쓸 것. 문제 있으면 위
   "밟은 함정" 항목부터 의심.

---

## 3단계 — 마스터 버스 1/N: HPF/LPF·3밴드 EQ·컴프레서 (2026-08-12, **폰 실측 미검증**)

⚠️ **이 세션은 사용자 지시로 폰 검증을 건너뛰고 코드만 이어서 진행했다** ("폰 검증하지말고
계속 작업해"). 2단계(드럼)의 "⚠️ 폰 실측 미검증" 은 아직도 그대로다 — 이번 세션도 폰이
안 잡혀서(에뮬레이터만 보임) 못 했고, 지시에 따라 검증 없이 다음 덩어리로 넘어갔다.
**다음 폰 세션은 2단계와 3단계 1/N 을 한꺼번에 확인해야 한다.**

### 왜 "마스터 버스"부터인가
`믹서·이펙트`(3단계) 전체는 트랙별 EQ/센드/사이드체인까지 포함하는 큰 작업인데,
**트랙이라는 개념 자체가 아직 없다**(곡 데이터 이식은 4단계). 그래서 지금 걸 수 있는 것부터
갖췄다 — 모든 소리가 마지막에 한 번 거치는 **마스터 버스**. 트랙이 생기면 그 위에 트랙별
FX를 추가하는 순서다.

### 옮긴 것 / 새로 만든 것

| 파일 | 내용 |
|---|---|
| `lib/mixer.dart` (신규) | `MasterBus` — HPF/LPF · 3밴드 EQ(저/중/고) · 컴프레서 |
| `lib/dsp.dart` | `Biquad` 에 `lowShelf()`/`highShelf()` 추가 (RBJ 쿡북, 웹의 `lowshelf`/`highshelf`와 같은 식) |
| `lib/engine.dart` | `masterGain` 적용 **뒤** · 룩어헤드 리미터 **앞**에 `masterBus.process()` 를 끼움 |
| `lib/audio_isolate.dart` | `_cMaster` 명령 · `AudioStats.compGrDb` · `AudioClient` 에 EQ/HPF/컴프 거울 필드 + `syncMaster()` |
| `lib/main.dart` | "마스터" 섹션 — EQ 슬라이더 3개·하이패스·컴프 on/off+threshold·컴프 감쇠 표시 |
| `tool/mixer_smoke.dart` (신규) | 폰 없이 돌리는 수치 확인 — `dart run tool/mixer_smoke.dart` |

### 컴프레서는 옮긴 게 아니라 새로 설계했다 — 이유
Web Audio 의 `DynamicsCompressor` 내부 알고리즘(정확한 검출·니 곡선)은 **비공개**라
그대로 옮길 수 없다. 대신 표준 소프트니 공식(Giannoulis/Massberg/Reiss,
"Digital Dynamic Range Compressor Design—A Tutorial and Analysis")으로 새로 만들고,
**파라미터 이름·기본값은 웹 `MASTER.comp*` 와 그대로 맞췄다**(threshold/ratio/attack/release/knee).
검출은 L/R 중 큰 쪽의 순간 피크를 dB로 본 뒤(스테레오 링크), 게인 자체를 어택/릴리즈로
스무딩한다(리미터처럼 파형이 아니라 게인만 부드럽게 움직인다 — synth 리미터와 같은 원칙).

### 3밴드 EQ는 20밴드를 근사한 것 — 정확히 옮긴 게 아니다
웹의 3밴드 모드는 `MST_EQ3` 로 20밴드를 그룹 분배한다(저 7개·중 7개·고 6개 평균).
20밴드 자체가 아직 없어서(UI도 3단계 뒤쪽 작업), 지금은 **저역 쉘프 150Hz · 중역 피킹
1kHz(Q=0.8) · 고역 쉘프 4kHz** 세 필터로 대표했다. 청감상 방향은 맞지만 **정확한 대역
분배는 20밴드를 옮길 때 다시 봐야 한다.**

### 게인 구조 — 손대지 않았다
웹은 `MIX_HEADROOM`(트랙 합계 −6dBFS) + `MASTER_MAKEUP`(+4.9dB) 구조인데, 트랙 페이더가
아직 없어서 지금은 기존 `masterGain=0.55`(고정 헤드룸) 그대로 뒀다. 트랙이 생기면(4~5단계)
그때 웹과 같은 헤드룸/메이크업 구조로 다시 손볼 것.

### 기본값은 반드시 투명해야 한다 — 검증 방법
전부 손대지 않으면 1·2단계에서 검증된 소리가 **그대로** 나가야 한다(EQ 0dB·컴프 꺼짐일 때
RBJ 계수가 항등필터로 접히는 성질을 이용). `mixer_smoke.dart` 1번이 이걸 확인한다.
`drum_smoke.dart` 를 다시 돌려 2단계와 **완전히 같은 수치**(피크 0.9594…·드럼최대동시 6·
하드클립 0)가 나오는 것도 확인했다 — 마스터 버스가 끼어들어도 회귀가 없다는 뜻.

### 스모크 테스트 결과 (폰 없이 돌린 것)
```
cd "$HOME/AI Company/music_doodle_engine" && dart run tool/mixer_smoke.dart
```
1) 기본값 — 피크 0.967·하드클립 0·컴프감쇠 0.00dB (투명 확인)
2) EQ(-12dB 저/고, HPF 200Hz) — RMS 0.3063→0.1988(-35.1%) (필터가 실제로 걸린다는 확인)
3) 컴프(threshold=-30dB, ratio=8) — 최대 감쇠 -25.4dB, 하드클립 0 (컴프가 실제로 작동)

**숫자로는 배선이 맞다는 것만 확인했다. EQ를 걸었을 때 정말 '저음이 얇아진 소리'로
들리는지, 컴프가 '자연스럽게 눌리는' 느낌인지는 귀로만 알 수 있다 — 폰에서 반드시 확인.**

### 아직 안 옮긴 것 (3단계 다음 덩어리)
- 마스터 리버브(`REV_KINDS` 홀/플레이트/룸) · 딜레이(`DLY_KINDS`) · 코러스(`CHO_KINDS`)
  → Web Audio `ConvolverNode` 는 OS/하드웨어가 FFT 컨벌루션을 최적화해 주지만, Dart 에서
  직접 구현하려면 파티션 FFT 컨벌루션이 필요하다(순진한 시간영역 컨벌루션은 임펄스 길이
  2.6초 = 샘플당 12만 번 곱셈이라 실시간이 불가능하다). **대신 콤+올패스(프리버브류) 알고리즘
  리버브로 대체할 계획** — 다음 세션에 진행하고 여기에 이유를 적을 것.
- 20밴드 EQ 모드 · EQ/컴프 순서 바꾸기(`MASTER.order`)
- 트랙별 FX(트랙마다 리버브/딜레이 send, 사이드체인 덕킹) — 트랙 구조가 생긴 뒤(4단계)
- 곡별 믹스(`GENRE_MIX`, hpf 로 음역 정리) — 마찬가지로 트랙/곡 구조가 필요

### 빌드 상태
`flutter analyze` 오류 0 (packages/·tool/ 안 info 경고만, 무시 대상).
`flutter build apk --release` 성공 — `build/app/outputs/flutter-apk/app-release.apk` (45.6MB).
**폰 미연결로 설치·실행·청감 확인은 못 했다.**

### 다음 세션이 반드시 할 것
1. `adb devices` 로 `RFKL505TRAZ` 확인 → 잡히면 설치 후 **2단계(드럼)와 3단계(마스터
   EQ/컴프)를 한 세션에서 같이** 청감 확인 (둘 다 미검증 상태로 쌓여 있다).
2. 마스터 섹션 슬라이더를 움직이며: EQ 저/고를 세게 걸었을 때 소리가 실제로 그렇게
   들리는지, 컴프 threshold 를 내렸을 때 "눌리는" 느낌이 자연스러운지(펌핑 안 하는지),
   메이크업 게인을 올렸을 때 음량만 회복되고 눌리는 느낌 자체는 그대로인지(아래 참고).
3. `dumpsys media.audio_flinger` 로 언더런 0 유지 확인 — 바이쿼드 10개(HPF/LPF/EQ 3밴드
   ×스테레오)가 늘었으니 오디오 여유가 A17 기준값(98%)에서 크게 안 떨어지는지 볼 것.
4. 문제없으면 이 섹션의 "⚠️ 폰 실측 미검증" 을 실측값으로 덮어쓸 것.

### 추가 — 컴프레서 메이크업 게인 (2026-08-12, 같은 세션 이어서)
`MasterBus.compMakeupDb` 필드 자체는 처음부터 있었는데(웹 `MASTER.compGain` 대응),
아이솔레이트 프로토콜(`syncMaster()`)이 **항상 0.0 을 하드코딩해서 보내고 있었고**
UI 에도 슬라이더가 없어서 손이 안 닿는 상태였다 — 사용자가 지적해서 마저 연결했다.

- `AudioClient.compMakeupDb` 거울 필드 추가, `syncMaster()` 가 실제 값을 보내도록 수정
- `main.dart` 컴프 threshold 슬라이더 밑에 "메이크업 게인"(0~+18dB) 슬라이더 추가
- 적용 위치는 그대로: **검출기(게인 리덕션 계산) 뒤**, 압축된 신호에 상수로 곱한다
  (`mixer.dart` `process()` — `g = 10^(compGrDb/20) * makeup`). 메이크업을 올려도
  `compGrDb`(감쇠 곡선 자체)는 변하면 안 된다 — 변하면 검출기 앞에 잘못 낀 것.

**스모크 테스트로 검증** (`mixer_smoke.dart` 4번, 새로 추가):
컴프(threshold=-30dB·ratio=8)에 메이크업 +12dB 를 걸어 RMS 0.0267→0.0990(**+271%**),
같은 시험의 최대 감쇠는 -25.4dB→-25.5dB(0.1dB 차이 — 오차 범위, 검출기는 안 바뀜),
하드클립 0 유지. `drum_smoke.dart` 재실행도 2단계와 동일 수치 — 회귀 없음.
`flutter build apk --release` 성공(45.6MB). **폰 미검증은 위와 동일하게 남아 있다.**

---

## 3단계 2/N — 마스터 리버브 (2026-08-12, 자동 세션·**폰 실측 미검증**)

⚠️ **이 세션도 자동 실행(스케줄)이라 폰(RFKL505TRAZ)이 안 잡혔다** (`adb devices` 재확인·
daemon 재시작까지 해봤지만 여전히 목록이 비었거나 정체불명 오프라인 에뮬레이터만 보임).
**청감으로는 전혀 확인 못 한 상태 — 다음 폰 세션은 2단계·3단계(1/N)·3단계(2/N, 이번 것)
셋을 한꺼번에 확인해야 한다.**

### 왜 리버브부터인가
지난 세션(3단계 1/N)의 "아직 안 옮긴 것"에 "다음 세션에 진행"이라고 못 박아 둔 항목이다.
웹의 `DLY_KINDS`(딜레이)·`CHO_KINDS`(코러스)는 **트랙 단위 이펙트**라서(web `FX_DEFAULT`,
트랙마다 send) 트랙 개념이 생기는 4단계(곡 데이터 이식) 전에는 걸 자리가 없다.
반면 마스터 리버브(`MASTER.rev`/`revKind`)는 웹에서도 **마스터 버스에 직접** 달려 있어서
지금 걸 수 있다 — "한 번에 한 덩어리만" 원칙에 맞춰 이것만 했다.

### 옮긴 것 / 새로 만든 것

| 파일 | 내용 |
|---|---|
| `lib/mixer.dart` | `MasterReverb`(신규) — 콤필터 4개(병렬) + 올패스 2개(직렬), 스테레오. `ReverbKind`(dur/preSec/toneHz) · `_Comb`/`_Allpass`/`_PreDelay`. `MasterBus.revWet`/`revKind` 필드, `process()` 끝에 가산(send) 방식으로 낀다 |
| `lib/audio_isolate.dart` | `_cMaster` 페이로드에 `revWet`/`revKind` 2개 추가(맨 뒤에 붙여서 기존 순서 안 깼다) · `AudioClient` 거울 필드 · `syncMaster()` |
| `lib/main.dart` | 마스터 섹션에 "리버브 종류"(홀/플레이트/룸 세그먼트) · "리버브 양"(0~40% 슬라이더, 웹 UI 범위 그대로) 추가 |
| `tool/mixer_smoke.dart` | 5번 시험 추가 — 배선 확인(RMS 증가) + 꼬리 확인(입력이 끊긴 뒤에도 소리가 남는지) |

### 컨볼버가 아니라 콤+올패스로 새로 설계한 이유
웹은 `ConvolverNode` + 잡음 임펄스(`REV_KINDS`, `makeRevImpulse` — 지수감쇠 잡음 + 초기반사
스파이크 몇 개)를 썼다. 브라우저 컨볼버는 OS/하드웨어가 FFT 컨벌루션으로 최적화해 주지만,
Dart 로 그대로 옮기면 **순진한 시간영역 컨벌루션**이 된다 — 홀 임펄스 2.6초 = 초당 48000
샘플마다 12만 번 곱셈이라 저사양 폰 실시간 오디오 콜백 안에서는 불가능하다(이미 2단계
HANDOFF 에 이 판단이 적혀 있었다).

→ 대신 표준 **Schroeder 구조**(콤필터 4개 병렬 + 올패스 2개 직렬 — 컴프레서를 새로 설계했던
것과 같은 원칙: "웹이 못 옮기는 알고리즘이면, 파라미터의 *의미*만 맞춰서 새로 만든다")로 했다.
`dur`(RT60 성격의 감쇠시간) · `pre`(프리딜레이) · `tone`(밝기→댐핑)는 웹 `REV_KINDS` 값을
**그대로 재사용**해 홀/플레이트/룸 세 캐릭터의 방향을 맞췄다. `decay`·`er`(초기반사 타이밍)는
잡음 임펄스 전용이라 콤/올패스 구조엔 대응이 없어 가져오지 않았다.

**아직 귀로 확인 못 한 것 — 다음 세션 최우선**: 댐핑 매핑(`damp = 1 - tone/12000`)과
콤 피드백(`fb = 10^(-3·delay/dur)`) 공식은 표준 DSP 이론으로 유도했지만, "홀이 정말
넓고 부드럽게 들리는지" "룸이 정말 짧고 또렷한지"는 순전히 귀로만 알 수 있다.
디자인 자체가 틀렸을 수도 있다(콤 지연 4개는 최소 구성이라 자잘한 금속성 울림이
들릴 가능성 — 들리면 콤 개수를 6~8개로 늘리는 게 표준 해법).

### 웹과 다르게 설계한 것 — 의도적
- **믹스 방식**: 웹은 크로스페이드가 아니라 **가산(send) 방식**이다
  (`revIn→conv→revTone→revWet→dest` 가 드라이 경로와 별도로 목적지에 합쳐진다,
  `n.revWet.gain.value=MASTER.rev`). 그대로 따라서 `l += rl*revWet` 식으로 더했다 —
  크로스페이드였다면 드라이 신호 자체가 줄어들어야 하는데 웹은 그러지 않는다.
- **웹의 마스터 시트엔 `revSize` 슬라이더가 없다**(그건 트랙 이펙트 쪽 파라미터,
  `FX_DEFAULT.revSize`). 마스터는 "양"(0~0.4)과 "종류"만 노출한다 — 그래서 우리도
  `revSize` 없이 종류(dur/pre/tone)만으로 캐릭터를 정했다.
- **꺼져 있으면 탱크 자체가 안 돈다** (`revWet>0` 일 때만 `_reverb.process()` 호출) —
  컴프레서의 `compWet>0` 조건과 같은 절약. 기본값(웹과 같이 rev=0)일 때 비용 0·출력이
  이전 단계와 **완전히 같다**는 걸 스모크 테스트로 확인했다(아래).

### 스모크 테스트 결과 (폰 없이 돌린 것)
```
cd "$HOME/AI Company/music_doodle_engine" && dart run tool/mixer_smoke.dart
```
1~4번은 3단계 1/N 과 동일 수치(리버브가 꺼져 있으면 회귀 없음 — 위 "절약" 설계가 맞다는 뜻).
5번(신규): 리버브 켬(hall·wet=1.0, 스모크는 배선 확인이 목적이라 웹 UI 상한 0.4보다 세게 걸었다)
— RMS 0.3046→0.3193(+4.8%)·하드클립 0. 훅이 끝난 뒤 1초 구간(입력이 새로 안 들어오는
구간)의 RMS 도 꺼짐 0.232→켬 0.318 로 뚜렷이 늘었다 — **탱크가 실제로 잔향을 만들어
내고 있다는 확인**(배선만 된 게 아니라 감쇠 모델도 동작한다는 뜻).
`drum_smoke.dart` 도 2단계와 완전히 같은 수치 — 회귀 없음.

**숫자로는 "탱크가 돌고 있다"만 확인한 것이지, 그게 진짜 홀/플레이트/룸처럼
들리는지는 전혀 확인 못 했다.** 폰에서 반드시 들어볼 것.

### 아직 안 옮긴 것 (3단계 다음 덩어리)
- 20밴드 EQ 모드 · EQ/컴프/리버브 순서 바꾸기(`MASTER.order`)
- 트랙별 FX(트랙마다 리버브/딜레이 send, 코러스, 사이드체인 덕킹) — 트랙 구조가 생긴 뒤(4단계)
- 곡별 믹스(`GENRE_MIX`, hpf 로 음역 정리) — 마찬가지로 트랙/곡 구조가 필요
- 리버브 `revSize`(트랙 쪽 파라미터, 공간 크기) — 마스터엔 웹도 없어서 스킵했다.
  트랙 이펙트를 옮길 때(4단계 이후) 콤 지연 길이를 revSize 로 스케일하는 방식으로 추가하면 된다

### 빌드 상태
`flutter analyze` 오류 0 (packages/·tool/ 안 info 경고만, 무시 대상).
`flutter build apk --release` 성공 — `build/app/outputs/flutter-apk/app-release.apk` (45.6MB).
**폰 미연결로 설치·실행·청감 확인은 못 했다.** `공유/` 로 복사도 안 했다(폰이 잡혔을 때 할 단계).

### 다음 세션이 반드시 할 것
1. `adb devices` 로 `RFKL505TRAZ` 확인 → 잡히면 이 APK 설치 후
   **2단계(드럼)·3단계 1/N(EQ/컴프)·3단계 2/N(리버브) 셋을 한 세션에서 같이** 청감 확인
   (전부 미검증 상태로 쌓여 있다 — 순서대로 하나씩이 아니라 한꺼번에 훑는 게 효율적이다).
2. 리버브 섹션에서: 종류(홀/플레이트/룸)를 바꿔가며 실제로 캐릭터가 다르게 들리는지,
   양을 0→40% 로 올릴 때 자연스럽게 공간감이 붙는지(금속성 울림·삐- 소리 없이),
   무음 구간에서 꼬리가 부자연스럽게 뚝 끊기지 않는지(현재 구현은 wet=0 이 되는 순간
   탱크 자체가 멈추므로, 리버브 켜둔 채 갑자기 0으로 내리면 즉시 끊긴다 — 이게 거슬리면
   다음 세션에서 wet 값 자체를 부드럽게 램프하도록 고칠 것).
3. `dumpsys media.audio_flinger` 로 언더런 0 유지 확인 — 콤 4+올패스 2, 스테레오라
   바이쿼드가 아니라도 계산량이 늘었으니 오디오 여유가 A17 기준값(98%)에서 크게 안
   떨어지는지 볼 것.
4. 문제없으면 이 섹션의 "⚠️ 폰 실측 미검증" 을 실측값으로 덮어쓸 것. 캐릭터가 이상하게
   들리면(예: 다 비슷하게 들린다, 삐- 소리가 난다) 위 "아직 귀로 확인 못 한 것" 부터 의심.

---

## 3단계 3/N — 20밴드 그래픽 EQ · EQ/컴프 순서 전환 (2026-08-12, 자동 세션·**폰 실측 미검증**)

⚠️ **이번에도 자동 실행(스케줄)이라 폰(RFKL505TRAZ)이 안 잡혔다.** `adb kill-server`+
`start-server` 로 데몬을 재시작해도, `system_profiler SPUSBDataType` 으로 USB 목록을 봐도
Samsung/Android 기기 자체가 물리적으로 안 잡혀 있었다(정체불명 `emulator-5562 offline` 만
계속 보임 — 무시 대상). **2단계·3단계 1/N·2/N 에 이어 이번 것도 청감 미검증 상태로 쌓였다.
다음 폰 세션은 넷을 한꺼번에 확인해야 한다.**

### 왜 이 덩어리인가
지난 세션(3단계 2/N)의 "아직 안 옮긴 것"에 남아 있던 마스터 버스 전용 항목 중 트랙 구조가
필요 없는 것 둘 — **20밴드 그래픽 EQ**와 **EQ/컴프 순서 전환**(`MASTER.order`) — 을 했다.
트랙별 FX·곡별 믹스는 여전히 4단계(곡 데이터 이식) 이후로 남겨 뒀다.

### 옮긴 것 — 3밴드 근사를 진짜 20밴드로 교체
지난 세션까지는 3밴드 모드를 저(150Hz 로우쉘프)·중(1kHz 피킹 Q0.8)·고(4kHz 하이쉘프)
필터 3개로 **근사**했다(HANDOFF 3단계 1/N 에 "정확히 옮긴 게 아니다"라고 적어 뒀던 부분).
이번에 웹처럼 **실제 20밴드 체인**(`MST_EQ_FREQ` 20개 주파수, 각 피킹 Q=2.2·양끝은
쉘프)을 만들고, 3밴드 모드는 그 20밴드에 **그룹으로 값을 분배**하도록 바꿨다
(`MST_EQ3.lo/mid/hi` 그대로 — 인덱스 0~6/7~13/14~19). 즉 3밴드 모드도 이제 근사가 아니라
20밴드 체인을 통과하는 것뿐이다 — 웹의 `applyMaster()` 로직과 완전히 같은 구조.

| 파일 | 내용 |
|---|---|
| `lib/mixer.dart` | `kMstEqFreq`(20개 주파수)·`kMstEq3Lo/Mid/Hi`(그룹 인덱스) 상수 추가. `MasterBus` 의 필터 3개(`_eqLoL/_eqMidL/_eqHiL` 등)를 `List<Biquad> _eqL/_eqR`(20개) 로 교체. `eqMode`('3'\|'20')·`eq20`(20개 dB 배열)·`eqFirst`(EQ/컴프 순서) 필드 추가. `process()` 를 `_processEq()`/`_processComp()` 로 쪼개고 `eqFirst` 에 따라 순서를 바꿔 호출(HPF/LPF 는 항상 EQ 쪽에 묶여 함께 움직인다 — 웹 `wireMasterOrder()` 와 같은 이유) |
| `lib/audio_isolate.dart` | `AudioClient` 에 `eqMode`/`eq20`/`eqFirst` 거울 필드. `syncMaster()` 페이로드 맨 뒤(인덱스 15~17)에 추가(기존 순서 안 깼다) |
| `lib/main.dart` | "EQ 모드"(3밴드/20밴드) 전환 버튼. 20밴드일 때 세로 페이더 20개(`RotatedBox` 로 가로 `Slider` 를 세움) — 주파수 라벨은 압축 표기(`1k`,`16k`). "EQ/컴프 순서" 전환 버튼(EQ→컴프 / 컴프→EQ). 3밴드 라벨을 실제 대역폭에 맞게 수정(예: "저역 150Hz"→"저역 25~100Hz" — 예전 라벨은 근사 필터의 중심 주파수였지 실제로 옮긴 그룹 범위가 아니었다) |
| `tool/mixer_smoke.dart` | 6번(20밴드 배선 확인 — 밴드 1개 vs 3밴드 그룹 7개 비교), 7번(순서 전환 시 결과가 달라지는지) 추가 |

### EQ/컴프 순서 — HPF/LPF 가 어느 편에 붙는지가 함정
웹 `wireMasterOrder()` 를 보면 `eqIn`이 `n.eqLo`(=하이패스 `mHp`)부터 시작한다. 즉
"EQ 블록"은 EQ 밴드만이 아니라 **하이패스·로우패스까지 포함한 덩어리**이고, 순서를
바꾸면 이 덩어리 전체가 컴프 앞/뒤로 옮겨간다. `_processEq()` 에 HPF/LPF 를 20밴드와
같이 넣어서 이 구조를 그대로 따랐다 — 안 그러면 "컴프→EQ" 를 골랐을 때 하이패스만
빠져나가 웹과 다른 신호 경로가 된다.

### 스모크 테스트로 확인한 것
```
cd "$HOME/AI Company/music_doodle_engine" && dart run tool/mixer_smoke.dart
```
1~5번은 이전 단계와 **거의 같은 방향**(1번은 완전히 동일 — 기본값 투명성 유지, 회귀 없음).
2번(EQ -12dB 저/고 + HPF)은 RMS 감소폭이 -35.1%(예전 3필터 근사) → **-39.1%**(진짜
20밴드)로 **바뀌었다** — 이건 회귀가 아니라 근사를 정확한 구조로 고친 결과다(예상된 변화,
HANDOFF 3단계 1/N 에 "정확한 대역 분배는 20밴드를 옮길 때 다시 봐야 한다"고 적어 뒀던 부분).
6번(신규): 20밴드에서 밴드 1개(50Hz, -12dB)만 걸었을 때 RMS 변화(0.3063→0.2705)가
3밴드 그룹 전체(7개 밴드 -12dB, 0.3063→0.1802)보다 작다 — 그룹 분배가 올바른 방향이라는 확인.
7번(신규): EQ→컴프 대 컴프→EQ 순서를 바꾸면 RMS 가 0.0748 대 0.1032 로 뚜렷이 다르다
— 순서 전환이 실제로 신호 경로를 바꾼다는 확인(컴프가 EQ 앞에 있으면 부스트되지 않은
신호를 검출하므로 덜 눌린다).
`drum_smoke.dart` 재실행도 2단계와 **완전히 같은 수치**(피크 0.9594…·드럼최대동시 6·
하드클립 0) — 마스터 버스가 더 복잡해졌어도 회귀 없음.

**숫자로는 배선과 그룹 분배 방향이 맞다는 것만 확인했다. 20밴드 각 밴드를 손으로 움직였을
때 정말 그 주파수 대역이 변하는 소리로 들리는지, 3밴드→20밴드 전환 시 소리가 부자연스럽게
튀지 않는지는 전혀 확인 못 했다 — 폰에서 반드시 들어볼 것.**

### 아직 안 옮긴 것
- 트랙별 FX(트랙마다 리버브/딜레이 send, 코러스, 사이드체인 덕킹) — 트랙 구조가 생긴 뒤(4단계)
- 곡별 믹스(`GENRE_MIX`, hpf 로 음역 정리) — 마찬가지로 트랙/곡 구조가 필요
- 리버브 `revSize`(마스터엔 웹도 없음, 트랙 이펙트 쪽에서 다룰 것)
- 20밴드 UI 의 세로 페이더가 손가락으로 정밀 조작하기 편한지는 미확인(터치 UI 인체공학은
  5단계 본 UI 작업에서 다시 볼 것 — 지금은 시험 화면 수준)

### 빌드 상태
`flutter analyze` 오류 0 (packages/·tool/ 안 info 경고만, 무시 대상. lib/ 안 새 info 3개는
이번 세션에 고쳤다 — `for` 문 중괄호 누락).
`flutter build apk --release` 성공 — `build/app/outputs/flutter-apk/app-release.apk` (45.7MB).
**폰 미연결로 설치·실행·청감 확인은 못 했다.** `공유/` 로 복사도 안 함(폰이 잡혔을 때 할 단계).

### 다음 세션이 반드시 할 것
1. `adb devices` 로 `RFKL505TRAZ` 확인 → 잡히면 이 APK 설치 후 **2단계·3단계 1/N·2/N·3/N
   (이번 것) 넷을 한 세션에서 같이** 청감 확인.
2. 20밴드 모드에서 낮은 주파수 밴드(25~63Hz)와 높은 주파수 밴드(8k~16kHz)를 각각 세게
   걸어 실제로 그 대역이 바뀌는 소리로 들리는지, 3밴드↔20밴드 전환 시 슬라이더 값이
   안 보이는 곳(다른 모드)에서 남아 있다가 다시 전환했을 때 이상하게 겹쳐 들리지 않는지.
3. EQ/컴프 순서를 바꿔가며 실제로 소리 차이가 나는지(펌핑 느낌이 달라지는지).
4. `dumpsys media.audio_flinger` 로 언더런 0 유지 확인 — 바이쿼드가 3개→20개로 늘었으니
   오디오 여유가 A17 기준값에서 눈에 띄게 떨어지는지 반드시 볼 것(이번 세션은 폰이 없어
   CPU 실측을 전혀 못 했다 — 계산량 증가가 실제로 문제인지는 순전히 폰에서만 알 수 있다).
5. 문제없으면 이 섹션의 "⚠️ 폰 실측 미검증" 을 실측값으로 덮어쓸 것.

---

## 4단계 1/N — 곡 데이터 이식: 드럼 패턴 라이브러리 (2026-08-12, **폰 실측 미검증**)

⚠️ **사용자가 "다음단계진행"이라고 지시해서 폰 검증 없이 다음 단계로 넘어갔다.** 이 시점에
`adb devices`·USB 목록 둘 다 확인했지만 여전히 `RFKL505TRAZ` 가 안 잡혀 있었다(정체불명
`emulator-5562 offline` 만 보임). **2단계·3단계 1/N·2/N·3/N 에 이어 5개 덩어리가 청감
미검증으로 쌓여 있다 — 다음 폰 세션은 반드시 다섯 개를 전부 훑어야 한다.**

### 왜 이 덩어리인가 — 3단계를 마치고 4단계로
3단계(마스터 버스)의 남은 항목(트랙별 FX·곡별 믹스)은 트랙 개념이 있어야 걸 수 있는데,
트랙은 4단계(곡 데이터 이식)에서 생긴다. 그래서 3단계를 완전히 마무리짓기보다 4단계로
넘어가는 게 순서상 맞다. 4단계 자체도 커서(HANDOFF 맨 위 "이전 계획"에 "곡 데이터 이식
2세션" 이라고 낙관적으로 적어 뒀는데 실제로는 데이터 양이 상당하다), **의존성이 없는
것부터** 쪼갰다 — 드럼 패턴은 스텝 번호만으로 되어 있어 스케일·키·코드 시스템이 전혀
필요 없다. 베이스/코드/멜로디는 `SCALE`/`KEY`/`degRow`/`CHORDS` 가 함께 있어야 음높이를
셈할 수 있어서 다음 덩어리로 미뤘다.

### 옮긴 것
웹 `MIDI_LIB.drum`(48개) + `LONG_LIB.drum`(14개, 트랩/프로그레시브 하우스 롱폼) +
`LONG_LIB2.drum`(10개, 팝/재즈/엠비언트 롱폼) = **드럼 패턴 72개 전부**, 그리고 그걸
스텝 목록으로 풀어내는 `buildDrum()`+`tileNotes()` 해석 로직.

| 파일 | 내용 |
|---|---|
| `lib/patterns.dart` (신규) | `DrumPatternDef`(패턴 하나) · `kDrumPatterns`(72개, 웹 그대로) · `buildDrumPattern()`(압축 표기→(lane,step,vel) 목록, 타일링 포함) · `findDrumPattern()` |
| `lib/main.dart` | "드럼 패턴 재생" 칩 11개 — 장르별 대표 패턴을 웹 `GENRE_PRESETS` 의 bpm·키트 그대로 매칭해서 눌러보게 함(예: 로파이=Lofi Chorus·78bpm·lofi키트) |
| `tool/pattern_smoke.dart` (신규) | 폰 없이 돌리는 수치 확인 6가지 — `dart run tool/pattern_smoke.dart` |

### 손으로 안 옮기고 파이썬으로 파싱해서 옮긴 이유
72개 패턴 중엔 64자리 스텝 배열(`h:[0,2,3,4,...,62]` 같은)이 여럿이다. 사람이 손으로
옮기면 숫자 하나가 밀리거나 빠지기 쉽고, 그런 실수는 **귀로 알아채기 어렵다**(리듬이
"약간 이상하다" 정도로만 들리고 원인을 못 찾는다). 그래서 `app.html` 을 정규식으로 파싱해
JSON 으로 바꾼 뒤 Dart 리터럴로 기계적으로 찍어냈다 — 스모크 테스트 1번(개수 72)과 5번
(전 패턴 스텝 범위 검사)이 이 변환이 안전했다는 걸 구조적으로 확인해 준다.

### 타일링(`tileNotes`) — 상당수 패턴이 실제로 반복된다
처음엔 "웹 예제들을 보니 `bars`==`src`라 타일링이 거의 no-op이겠지" 라고 짐작했는데
틀렸다. `House 4/4`(bars:4, src:1) 처럼 **1마디 정의를 4번 반복**해서 4마디를 채우는
패턴이 17개나 있다(`Boom Bap`·`Trap Hats`·`Backbeat 8`·`Shuffle Hat`·`Four Drop` 등,
전부 `src<bars`). 웹 `tileNotes()` 를 얼렁뚱땅 안 옮기고 그대로(`reps=max(1,
floor(bars/src))`, 반복마다 `r*src*SPB` 만큼 스텝을 밀기) 옮겨 둔 게 맞았다 — 스모크
테스트 2번(`House 4/4` 킥이 정확히 16개, 마지막 킥이 60스텝)이 이걸 확인한다.

### 스모크 테스트 결과 (폰 없이 돌린 것)
```
cd "$HOME/AI Company/music_doodle_engine" && dart run tool/pattern_smoke.dart
```
1) 패턴 개수 72(빠뜨린 것 없음) 2) `House 4/4` 타일링 40타격(킥 16개, 마지막 60스텝 —
반복 로직 확인) 3) `Fill In`(타일링 없음) 8타격 그대로 4) 하이햇 벨로시티 공식(`s%4==0?2:1`)
전 패턴 통과 5) 72개 패턴 전부 스텝 범위(0~`bars*16-1`)·lane 이름 검사 통과 6) 대표
패턴 5개(로파이~엠비언트)를 이어 붙여 37초 렌더 — 피크 0.963·하드클립 0·버퍼바닥 0.
`drum_smoke.dart`·`mixer_smoke.dart` 도 재실행해 이전 단계와 동일 수치 확인 — 회귀 없음.

**숫자로는 "웹의 스텝 데이터를 안 틀리고 옮겼다"만 확인한 것이다. 패턴 하나하나가 실제로
그 장르답게 들리는지(예: House Chorus 가 정말 하우스 킥 패턴처럼 들리는지, Trap Hook 의
16분 하이햇이 밀도 있게 들리는지)는 전혀 확인 못 했다 — 폰에서 반드시 들어볼 것.**

### 아직 안 옮긴 것 (4단계 다음 덩어리)
- 베이스/코드/멜로디 패턴(`MIDI_LIB.bass/chord/melody` + `LONG_LIB`/`LONG_LIB2` 대응분) —
  `SCALE`/`KEY`/`degRow`/`CHORDS`/`CHORD_TYPES` 스케일·키·코드 시스템을 먼저(또는 같이)
  옮겨야 음높이를 셈할 수 있다. 다음 세션 최우선.
- `SONG_FORMS`(6장르 인트로~아웃트로 섹션 구성) · `FORM_ORDER` · `GENRE_MIX`(트랙별
  볼륨/팬/EQ/리버브 send) — 트랙 버스(현재 마스터 버스만 있고 트랙별 버스가 없다)가
  있어야 `GENRE_MIX` 를 걸 자리가 생긴다.
- 실제 시퀀서(스텝을 따라가며 다음에 뭘 재생할지 결정하는 루프) — 지금은 패턴 하나를
  버튼 눌러 한 번 예약해 보는 시험 화면 수준이지, 곡을 처음부터 끝까지 이어 재생하는
  게 아니다. 5단계(UI) 작업과 맞물릴 가능성이 크다.

### 빌드 상태
`flutter analyze` 오류 0 (packages/·tool/ 안 info 경고만, 무시 대상. `lib/` 는 전부 깨끗함).
`flutter build apk --release` 성공 — `build/app/outputs/flutter-apk/app-release.apk` (45.8MB).
**폰 미연결로 설치·실행·청감 확인은 못 했다.** `공유/` 로 복사도 안 함.

### 다음 세션이 반드시 할 것
1. `adb devices` 로 `RFKL505TRAZ` 확인 → 잡히면 이 APK 설치 후 **2단계·3단계(1~3/N)·
   4단계 1/N(이번 것) 다섯 덩어리를 한 세션에서 같이** 청감 확인. 특히 "드럼 패턴 재생"
   칩 11개를 장르별로 눌러 웹 앱의 같은 패턴과 번갈아 들어 비교할 것.
2. 문제없으면 이 섹션의 "⚠️ 폰 실측 미검증" 을 실측값으로 덮어쓸 것.
3. 그다음 작업은 베이스/코드/멜로디 패턴 이식(스케일·키·코드 시스템 포함) — 위 "아직 안
   옮긴 것" 1번.

---

## 4단계 2/N — 스케일·조·코드 시스템 + 베이스/코드/멜로디 패턴 (2026-08-13, 자동 세션·**폰 실측 미검증**)

⚠️ **이번에도 자동 실행(스케줄)이라 폰(RFKL505TRAZ)이 안 잡혔다.** `adb kill-server`+
`start-server` 로 데몬을 재시작하고 `system_profiler SPUSBDataType` 으로 USB 목록도 봤지만
Samsung/Android 기기가 물리적으로 안 잡혀 있었다(`emulator-5562 offline` 만 보임 — 무시 대상).
**2단계·3단계(1~3/N)·4단계(1/N·2/N, 이번 것) 여섯 덩어리가 청감 미검증으로 쌓였다.
다음 폰 세션은 여섯 개를 전부 훑어야 한다.**

### 왜 이 덩어리인가
지난 세션(4단계 1/N)이 "다음 세션 최우선"으로 못 박아 둔 항목 — 베이스/코드/멜로디는
음높이를 셈하려면 스케일·조·코드 시스템(`SCALE`/`KEY`/`CHORDS`/`degRow`)이 먼저 있어야 해서
드럼 다음으로 미뤄 뒀던 덩어리다. 이번에 그 전제조건과 실제 패턴 데이터를 함께 옮겼다
("한 번에 한 덩어리"로 보기엔 크지만, 이론 시스템만 옮기고 패턴을 안 옮기면 들어볼 수 있는
게 하나도 없고, 패턴만 옮기고 이론이 없으면 애초에 음높이를 못 구해서 — 이 둘은 쪼갤 수
없는 한 쌍이었다).

### 옮긴 것

| 파일 | 내용 |
|---|---|
| `lib/theory.dart` (신규) | 웹 `SCALE`/`KEY`/`CHORD_TYPES`/`TENSIONS`/`TRIAD_DEGS`/`chordQual()`/`chordFreqsOf()`/`applyKey()`(다이아토닉 7화음 계산 부분) 그대로. `MusicKey`(root+mode) · `ChordSpec` · `diatonicChords(key)` · `degreeFreq(도수,type,key)` |
| `lib/patterns.dart` | `NotePatternDef`(name/bars/src/notes) · `_tileNoteList()`(드럼과 같은 `tileNotes` 로직 재사용) · `MelodicHit`/`buildRowsPattern()`(베이스·멜로디, 글라이드 포함) · `ChordHit`/`buildChordPattern()`(코드, 타입 오버라이드 포함) · `findBassPattern`/`findChordPattern`/`findMelodyPattern`. 데이터: `kBassPatterns`(41) · `kChordPatterns`(52) · `kMelodyPatterns`(48) — 웹 `MIDI_LIB`+`LONG_LIB`+`LONG_LIB2` 의 bass/chord/melody 전부 |
| `lib/main.dart` | "화성 패턴 재생" 섹션 — 장르 11개, 드럼+베이스+코드+멜로디를 한 번에 예약해서 "그 장르로 들리는가" 확인 (`kTestSongPatterns`, `_playSong()`) |
| `tool/theory_smoke.dart` (신규) | 폰 없이 돌리는 수치 확인 8가지 — `dart run tool/theory_smoke.dart` |

### 왜 웹의 `ROWS`/`BASS_F`/`LEAD_F` 배열을 안 옮겼나
웹은 피아노롤 UI가 있어서 "화면에 보이는 줄"이 곧 스케일 음이고, 그 줄 번호로 미리 계산해
둔 주파수 표(`BASS_F`/`LEAD_F`, 15개)를 찾는 구조다. 우리는 아직 피아노롤이 없다(5단계에서
생김) — 그래서 "그 줄에 실제로 무슨 소리가 나는가"만 있으면 되고, [degreeFreq] 가 그 자리에서
직접 계산한다(로직은 웹 `applyKey()` 의 non-PRO 분기와 완전히 같다, 미리 표를 안 만들 뿐).
프로 모드(크로매틱 37줄)는 옮기지 않았다 — 마찬가지로 피아노롤이 생길 때 같이 볼 것.

### 글라이드 — `prevRowFreq()` 를 패턴 하나 안에서 재현
베이스 808 패턴(`Trap 808 V` 등)의 절반은 글라이드다(다섯째 값 `1`). 웹은 "같은 클립 안에서
바로 앞에 울린 음"을 찾는데(`prevRowFreq`, 모든 줄을 합쳐서 step 이 더 작은 것 중 가장 가까운
것), 우리는 아직 클립·타임라인이 없어서(5단계) **패턴 하나(타일링 적용 후)를 그 자체로 하나의
악절**로 보고 같은 규칙을 적용했다(`buildRowsPattern` 안에서 전체 노트를 step 순으로 봤을 때
가장 가까운 앞 노트). 실제 시퀀서가 생기면(4단계 남은 항목) 클립 경계 처리를 다시 봐야 한다.

### 코드 타입 오버라이드 — 재즈 진행의 7화음
다이아토닉 3화음만으로는 재즈가 안 된다. 패턴 데이터의 코드 노트 다섯째 값(`'m7b5'`,`'dom7'`
등)이 있으면 그 도수의 **근음은 유지한 채 타입만** 바꾼다(`buildChordPattern`, 웹
`{r:c.spec.r,t:ty,x:[],b:null}` 그대로). `Jazz Keys H`(ii°7–V7–im7–VImaj7 진행)로 스모크
테스트에서 확인 — 4음 화음이 실제로 나온다.

### `arp4`/`block4` 헬퍼 — 함수가 아니라 결과값을 옮겼다
웹은 64개 아르페지오 음을 손으로 안 적으려고 두 헬퍼 함수(`arp4`: 코드톤을 오르내리는 16분음표
패턴으로, `block4`: 코드 번호 목록을 마디별 롱톤/스플릿 노트로)를 LONG_LIB2 에서만 썼다.
Dart 로 함수 자체를 옮기지 않고, **파이썬으로 같은 로직을 재구현해 호출한 뒤 결과값을
리터럴로 찍어냈다** — 정적 데이터라 굳이 런타임에 재계산할 이유가 없고(값은 같다, 계산
시점만 다르다), 실수 없이 옮기려면 어차피 파이썬 재구현이 필요했다(드럼 패턴 이식 때와
같은 이유). `Pop Arp`(MIDI_LIB, 손으로 쓴 것)와 `Pop Arp P`(LONG_LIB2, `arp4()` 로 만든 것)를
나란히 손으로 검산해서 두 방식이 같은 패턴 모양을 낸다는 걸 확인했다.

### 스모크 테스트 결과 (폰 없이 돌린 것)
```
cd "$HOME/AI Company/music_doodle_engine" && dart run tool/theory_smoke.dart
```
1) 패턴 개수 — bass 41(25+7+9) · chord 52(25+12+15) · melody 48(25+9+14), 전부 웹 원본과
같은 개수(파싱 누락 없음 — 각 라이브러리 항목을 수작업으로도 재검산했다).
2) C단조 다이아토닉 7화음 — 0:min(i,Cm) 2:dim(ii°) 3:maj(III,E♭) 5:min(iv,Fm) 7:min(v,Gm)
8:maj(VI,A♭) 10:maj(VII,B♭) — 웹 LONG_LIB 주석("0=i(Cm) 2=III(E♭) 3=iv(Fm) 4=v(Gm)
5=VI(A♭) 6=VII(B♭)")과 근음·성질이 정확히 맞는다.
3) 기준 주파수 — 베이스 도수0=32.70Hz(표준 C1과 일치) · 코드/멜로디 도수0=261.63Hz(표준
C4와 일치) — `BASS_BASE=24`/기준 `60` 계산이 실제 음이름과 맞다는 확인.
4) 타일링(`Long Root`, bars=4,src=2) — 2번 반복, steps=[0,16,32,48] 정확.
5) 글라이드(`Trap 808 V` step16) — 글라이드 시작 주파수가 정확히 "그 앞 노트(step11,
도수0)"의 주파수와 일치 — prevRowFreq 로직 재현이 맞다는 확인.
6) 코드 타입 오버라이드(`Jazz Keys H` 첫 화음) — 4음(m7b5) 확인.
7) 전 패턴(141개) 스텝 범위 검사 통과.
8) 트랩 훅(베이스+코드+멜로디+드럼 동시, 4마디) 렌더 — 피크 0.963·하드클립 0·버퍼바닥 0·
음부족 0·드럼부족 0. `drum_smoke.dart`/`mixer_smoke.dart`/`pattern_smoke.dart` 재실행도
이전 단계와 완전히 같은 수치 — 회귀 없음.

**숫자로는 화성 이론과 데이터 파싱이 정확하다는 것만 확인했다. 코드 진행이 실제로
"그 장르답게" 들리는지(로파이 워크베이스가 로파이 같은지, 재즈 7화음이 재즈처럼 들리는지,
글라이드가 808 슬라이드처럼 들리는지)는 전혀 확인 못 했다 — 폰에서 반드시 들어볼 것.**

### 아직 안 옮긴 것 (4단계 다음 덩어리)
- `SONG_FORMS`(6장르 인트로~아웃트로 섹션 구성) · `FORM_ORDER` · `GENRE_MIX`(트랙별
  볼륨/팬/EQ/리버브 send) — 트랙 버스(현재 마스터 버스만 있음)가 있어야 `GENRE_MIX` 를
  걸 자리가 생긴다.
- 실제 시퀀서(스텝을 따라가며 다음에 뭘 재생할지 결정하는 루프, 클립·타임라인 개념) —
  지금은 패턴 하나를 버튼 눌러 한 번 예약해 보는 시험 화면 수준. 글라이드의 "클립 경계"
  처리(위 참고)도 이때 다시 봐야 한다.
- 프로 모드(크로매틱 37줄) — 피아노롤 UI가 생길 때(5단계) 같이.
- `vocal` 보컬찹 — 1단계에서부터 미룬 항목, 여전히 안 옮김.

### 빌드 상태
`flutter analyze` 오류 0 (packages/·tool/ 안 info 경고만, 무시 대상. `lib/` 새 파일
`theory.dart` 도 깨끗함).
`flutter build apk --release` 성공 — `build/app/outputs/flutter-apk/app-release.apk` (46.0MB).
**폰 미연결로 설치·실행·청감 확인은 못 했다.** `공유/` 로 복사도 안 함.

### 다음 세션이 반드시 할 것
1. `adb devices` 로 `RFKL505TRAZ` 확인 → 잡히면 이 APK 설치 후 **2단계·3단계(1~3/N)·
   4단계(1/N·2/N) 여섯 덩어리를 한 세션에서 같이** 청감 확인.
2. "화성 패턴 재생" 칩 11개를 장르별로 눌러 웹 앱의 같은 조합(예: 로파이 코러스 =
   베이스 Lofi Walk C + 코드 Lofi Keys C + 멜로디 Lofi Hook + 드럼 Lofi Chorus)과
   번갈아 들어 비교할 것. 특히 트랩(글라이드)·재즈(7화음)에서 차이가 크게 날 수 있다.
3. `dumpsys media.audio_flinger` 로 언더런 0 유지 확인 — 화성 패턴은 베이스+코드(최대
   4음)+멜로디가 동시에 겹쳐 폴리포니가 늘어난다.
4. 문제없으면 이 섹션의 "⚠️ 폰 실측 미검증" 을 실측값으로 덮어쓸 것.
5. 그다음 작업은 위 "아직 안 옮긴 것" 중 시퀀서/클립 구조 — `SONG_FORMS` 를 걸려면
   먼저 필요하다.

---

## 4단계 3/N — 시퀀서/클립 구조: SONG_FORMS(배열형 6곡) + FORM_ORDER + 필인 (2026-08-13, 자동 세션·**폰 실측 미검증**)

⚠️ **이번에도 자동 실행(스케줄)이라 폰(RFKL505TRAZ)이 안 잡혔다.** `adb kill-server`+
`start-server` 로 데몬을 재시작하고 `system_profiler SPUSBDataType` 으로 USB 목록도 봤지만
Samsung/Android 기기가 물리적으로 안 잡혀 있었다(`emulator-5562 offline` 만 보임 — 무시 대상).
**2단계·3단계(1~3/N)·4단계(1/N·2/N·3/N, 이번 것) 일곱 덩어리가 청감 미검증으로 쌓였다.
다음 폰 세션은 일곱 개를 전부 훑어야 한다 — 특히 이번 것(전체 곡 재생)부터 우선 확인할 것,
가장 최근이고 가장 상위 구조라 문제가 있으면 하위 단계보다 먼저 드러난다.**

### 왜 이 덩어리인가
지난 세션(4단계 2/N)이 "다음 작업"으로 못 박아 둔 항목. `SONG_FORMS`(인트로~아웃트로 섹션
구성)를 걸려면 "섹션을 순서대로 이어붙이고 그 안에서 패턴을 반복 배치하는" 뼈대가 먼저
있어야 하는데, 지금까지는 패턴 하나(또는 드럼+베이스+코드+멜로디 한 조)를 버튼 눌러
**한 번** 예약해 보는 시험 화면 수준이었다(`_playSong`). 이번에 그 뼈대(웹 `applySongForm()`
의 타이밍 계산 부분)를 옮기고, 그걸로 실제 곡 하나를 처음부터 끝까지 이어 들을 수 있게 했다.

**객체형 롱폼 6곡(trap/proghouse/pop/jazz/ambient)은 옮기지 않았다** — 그쪽은 같은 타입
트랙을 여러 개 두는 슬롯 구조(예: 트랩은 패드+브라스가 둘 다 chord)와 곡 자체의
`tracks`/`order`를 쓰는데, 지금은 "트랙"이라는 개념 자체가 없다(마스터 버스 하나뿐).
배열형 6곡(lofi/house/hiphop/citypop/ballad/rock)은 타입 하나당 트랙 하나(드럼/베이스/
코드/멜로디)라 기존 4파트 구조를 그대로 확장할 수 있어서 먼저 했다 — "한 번에 한 덩어리".

### 옮긴 것 / 새로 만든 것

| 파일 | 내용 |
|---|---|
| `lib/song.dart` (신규) | `SongSection`(섹션 하나) · `kSongForms`(6곡 배열형, 웹 `SONG_FORMS` 그대로) · `kFormOrder`(`[0,1,2,3,1,2,4]`) · `kPlainDrum`(필인→민짜 매핑, 웹 `PLAIN_DRUM` 9개 중 배열형 6곡에 해당하는 6개만) · `buildSong()` — 곡 하나를 처음부터 끝까지 편 `SongSchedule`(드럼/베이스/코드/멜로디, 전부 시간(초) 계산 완료 상태) |
| `lib/main.dart` | "전체 곡 재생" 섹션(장르 6개 칩) · `_playFullSong()` — `buildSong()` 결과를 `host.drumBatch()`/`host.batch()` 로 통째로 예약 |
| `tool/song_smoke.dart` (신규) | 폰 없이 돌리는 수치 확인 4가지 — `dart run tool/song_smoke.dart` |

### 이중 타일링 — 패턴 자체의 반복과 섹션 안 반복은 다른 층이다
패턴(`DrumPatternDef`/`NotePatternDef`)은 이미 제 `bars`/`src` 로 한 번 타일링된다
(4단계 1/N·2/N에서 옮긴 `buildDrumPattern`/`buildRowsPattern`/`buildChordPattern`).
그런데 그 결과를 SONG_FORMS 섹션(예: 8마디 '코러스')에 넣으면, 패턴 자신의 길이(`pattern.bars`,
예: 4마디)만큼 **또 반복**된다(`Lofi Chorus` bars=4 → 8마디 코러스에 2번). 웹
`applySongForm()` 의 `for(let b=cur;b+base.bars<=end;b+=base.bars)` 를 그대로 옮겼다 —
"타일링의 타일링"이라 헷갈리기 쉬우니 다시 만질 때 이 구조부터 의심할 것.

### 필인은 곡 전체에서 '가장 마지막 등장' 한 번만 — 뒤에서 덮어쓰는 트릭
`Lofi Chorus` 같은 필인 있는 패턴은 코러스 섹션(8마디, 패턴 4마디×2반복) 안에서도 반복되고,
코러스 섹션 자체도 `FORM_ORDER` 안에서 두 번(위치 2, 5) 나온다 — 즉 곡 하나에 총 4번 등장.
매번 필인이 돌면 지겨워지므로, 웹처럼 **곡 전체를 훑어 각 패턴명이 마지막으로 등장하는
마디 번호 하나만 미리 구해 두고**(`fillAt`, 순서대로 훑으며 뒤에서 덮어쓰므로 최종값이
자동으로 "가장 마지막"이 된다), 실제로 스케줄을 만들 때 그 마디에서만 필인 버전
(`kPlainDrum` 의 키), 나머지는 민짜(값)를 쓴다. `song_smoke.dart` 2번이 이걸 정확히
확인한다 — lofi 곡에서 1차 코러스 섹션(2반복)은 둘 다 민짜, 2차 코러스 섹션(2반복)은
앞은 민짜·뒤(진짜 마지막 등장)만 필인.

### GENRE_MIX(장르별 볼륨/팬/EQ/리버브 send)는 옮기지 않았다 — 이유는 3단계와 동일
트랙 버스가 없어서(마스터 버스 하나로 전부 나감) 걸 자리가 없다. 그래서 지금은 모든 곡이
드럼/베이스/코드/멜로디를 있는 그대로 섞어 마스터 버스 하나만 거쳐 나간다 — 웹의 "로파이는
드럼을 뒤로 빼고 코드를 앞으로" 같은 장르별 밸런스가 전혀 반영 안 된 상태. 트랙 구조가
생기면(다음다음 덩어리 예상) 그때 옮길 것.

### 스모크 테스트 결과 (폰 없이 돌린 것)
```
cd "$HOME/AI Company/music_doodle_engine" && dart run tool/song_smoke.dart
```
1) 6곡 전부 예외 없이 빌드, 마디 합 전부 44(4+8+8+4+8+8+4, `FORM_ORDER` 대로) — 곡 길이는
   bpm에 따라 76.5초(록)~155.3초(발라드).
2) 필인 로직 — lofi 곡에서 `Lofi Chorus`(민짜 92타격) vs `Lofi Chorus Plain`(77타격) 구간별
   타격 수: 1차 코러스 구간(2반복)=184(=92×2, 전부 민짜) · 2차 코러스 구간(2반복)=169
   (=92+77, 뒤 반복만 필인) — 기대와 정확히 일치.
3) house 드럼 타격 668개 전부 [0, 85.2초) 안, 음수·역행 시간 없음(섹션 경계 계산 확인).
4) house(4파트 전부 있는 곡) 렌더 87.2초 — 피크 0.959·하드클립 0·버퍼바닥 0·음부족 0·
   드럼부족 0·최대동시음 12·최대동시드럼 4. 실제 곡 길이·밀도로 렌더해도 여유가 크다.
`drum_smoke.dart`/`mixer_smoke.dart`/`pattern_smoke.dart`/`theory_smoke.dart` 재실행도
전부 이전 단계와 동일 수치 — 회귀 없음.

**숫자로는 섹션 이어붙이기·타일링·필인 로직이 정확하다는 것만 확인했다. 실제로 들었을 때
"곡처럼" 들리는지(인트로에서 코러스로 넘어갈 때 자연스러운지, 코러스가 반복될 때 필인
있는 버전이 정말 마지막에만 나와서 신선하게 들리는지, 브레이크 구간에서 악기가 빠지는 게
휑하게 느껴지지 않는지)는 전혀 확인 못 했다 — 폰에서 반드시 들어볼 것.**

### 아직 안 옮긴 것
- 객체형 롱폼 6곡(trap/proghouse/pop/jazz/ambient) — 슬롯별 트랙 편성(`tracks`)과 곡 자체
  `order`가 필요. 트랙 구조를 먼저 일반화해야 한다(같은 타입 트랙 여러 개 허용).
- `GENRE_MIX`(장르별 볼륨/팬/EQ/리버브 send) — 트랙 버스가 있어야 걸 자리가 생긴다.
- 조(key)/템포 바꾸기 UI — 지금은 `_playFullSong` 이 항상 C단조 기본값(`MusicKey()`)만 쓴다.
- 재생 중 정지/일시정지, 진행 표시(지금 몇 마디인지) — "버튼 누르면 끝까지 한 번에 예약"
  수준이라 도중에 멈추는 기능이 없다(`host?.batch`/`drumBatch` 만 있고 취소 API는
  `engine.clearSchedule()`가 이미 있으니 정지 버튼 자체는 어렵지 않다 — 다음에 UI만 달면 됨).
- `EXTRA_TRACKS`("꽉 채워서" 옵션으로 악기를 더 얹는 것) — 롱폼 곡 전용이라 트랙 구조와 함께.

### 빌드 상태
`flutter analyze lib/` 오류·경고 0 (`song.dart` 의 불필요한 `!` 하나는 이번 세션에 고쳤다 —
`flat == null || fillAt[..] == b` 로 이미 `useFill=false` 분기에서 `flat` 이 non-null 로
좁혀지는 걸 분석기가 알아채서 `!` 가 군더더기였다). `packages/`·`tool/` 안 info 경고만 남음
(무시 대상, 전부 `avoid_print`/`avoid_relative_lib_imports` 류).
`flutter build apk --release` 성공 — `build/app/outputs/flutter-apk/app-release.apk` (46.0MB).
**폰 미연결로 설치·실행·청감 확인은 못 했다.** `공유/` 로 복사도 안 함.

### 다음 세션이 반드시 할 것 (4단계 3/N 시점 기록 — 4단계 4/N 이후로는 아래 새 섹션 참고)
1. `adb devices` 로 `RFKL505TRAZ` 확인 → 잡히면 이 APK 설치 후 **2단계·3단계(1~3/N)·
   4단계(1/N·2/N·3/N) 일곱 덩어리를 한 세션에서 같이** 청감 확인. 시간이 부족하면
   "전체 곡 재생"(이번 것)부터 — 가장 상위 구조라 하위 단계 문제도 여기서 같이 드러난다.
2. "전체 곡 재생" 칩 6개(로파이/하우스/힙합/시티팝/발라드/록)를 눌러 인트로→벌스→코러스→
   브레이크→벌스→코러스→아웃트로가 실제로 곡처럼 들리는지, 섹션 전환이 뚝뚝 끊기지 않는지,
   두 번째 코러스에서 필인이 들리고 첫 번째 코러스는 밋밋한 게 맞는지(의도된 차이).
3. `dumpsys media.audio_flinger` 로 언더런 0 유지 확인 — 곡이 1~2분 넘게 이어지므로 짧은
   시험보다 오래 도는 조건에서도 버퍼가 안정적인지 볼 것(스모크 테스트는 렌더만 확인했지
   실제 저지연 재생 경로에서 긴 시간 도는 건 폰에서만 확인 가능).
4. 문제없으면 이 섹션의 "⚠️ 폰 실측 미검증" 을 실측값으로 덮어쓸 것.
5. 그다음 작업은 위 "아직 안 옮긴 것" 중 트랙 구조 일반화(같은 타입 트랙 여러 개 허용) —
   객체형 롱폼 곡과 `GENRE_MIX` 둘 다 이게 있어야 진행할 수 있다.

---

## 4단계 4/N — 트랙 버스(TrackMix) + GENRE_MIX(배열형 6곡 몫) (2026-08-13, 자동 세션·**폰 실측 미검증**)

⚠️ **이번에도 자동 실행(스케줄)이라 폰(RFKL505TRAZ)이 안 잡혔다.** `adb kill-server`+
`start-server` 로 데몬을 재시작해도 여전히 `emulator-5562 offline`(무시 대상)만 보이고
실제 기기는 물리적으로 안 잡혀 있었다. **2단계·3단계(1~3/N)·4단계(1/N·2/N·3/N·4/N, 이번 것)
여덟 덩어리가 청감 미검증으로 쌓였다. 다음 폰 세션은 여덟 개를 전부 훑어야 한다 — 시간이
부족하면 "전체 곡 재생"(GENRE_MIX 가 새로 걸린 상태)부터.**

### 왜 이 덩어리인가
지난 세션(4단계 3/N)의 "그다음 작업"이 "트랙 구조 일반화"였다. 다만 **완전 일반화**(같은
타입 트랙 여러 개, 객체형 롱폼용)는 크고, 그 전에 걸 수 있는 더 작고 확실한 덩어리가 있었다
— **배열형 6곡**(lofi/house/hiphop/citypop/ballad/rock)은 타입 하나당 트랙 하나(드럼/베이스/
코드/멜로디)라 "타입 하나 = 버스 하나" 구조로 바로 맞는다. `GENRE_MIX` 도 이 6개 항목은
정확히 그 4개 슬롯만 쓴다. 그래서 이번엔 "트랙 버스 4개(드럼/베이스/코드/멜로디) + 그 위에
배열형 6곡 GENRE_MIX" 까지만 하고, 객체형 롱폼(같은 타입 트랙 여러 개)은 다음으로 미뤘다.

### 옮긴 것 / 새로 만든 것

| 파일 | 내용 |
|---|---|
| `lib/synth.dart` | `SynthNote.part`(int, kPartBass=0/kPartChord=1/kPartMelody=2) 필드 추가. `noteOn()` 에 `part` 파라미터(기본 melody) — 신호 생성 로직과는 무관, engine.dart 라우팅 전용 메타데이터 |
| `lib/mixer.dart` | `TrackMix`(신규) — 볼륨·팬·3밴드 EQ(150Hz 쉘프/1kHz 피킹Q0.8/4kHz 쉘프)·HPF/LPF·리버브 send. `TrackMixSet`(신규) — drum/bass/chord/melody 4개 묶음. `MasterBus.process()` 에 `sendL`/`sendR` 옵션 파라미터 추가(트랙 send 가 마스터 리버브와 같은 탱크를 공유 — 탱크가 선형이라 `l*revWet+sendL` 한 번에 태워도 수학적으로 등가) |
| `lib/engine.dart` | `noteOn`/`schedule`/`_Sched` 에 `part` 필드 추가. `trackMix`(TrackMixSet) 필드 추가. `render()` 내부 샘플 루프를 **파트별로 따로 합산 → TrackMix 통과 → 합산 → 마스터**로 재구성(예전엔 전부 한 번에 합산했다) |
| `lib/genre_mix.dart` (신규) | `PartMixSpec`/`GenreMixSpec` + `kGenreMix`(웹 GENRE_MIX 의 배열형 6곡 몫, 값 그대로) + `applyGenreMixTo()`/`resetGenreMix()` |
| `lib/audio_isolate.dart` | `_cGenreMix` 명령 · `AudioClient.setGenreMix(genre)` · `batch()` 항목에 8번째(선택) `part` 필드 — 없으면 melody(기존 호출부 안 건드려도 됨) |
| `lib/main.dart` | `_playSong`/`_playFullSong` 의 bass/chord/melody 노트에 `part` 태그. `_playFullSong` 이 재생 직전 `host.setGenreMix(genre)` 호출(배열형 6곡만 실제로 걸림). "믹스 초기화" 버튼 추가(마스터 EQ/컴프처럼 명시적으로 되돌리기 전까진 유지되는 설계) |
| `tool/genremix_smoke.dart` (신규) | 폰 없이 돌리는 수치 확인 5가지 |

### 트랙 버스가 진짜 "투명"한지 — Engine/SynthNote 를 거치면 검증이 오염된다
`TrackMix.process()` 는 순수 신호로는 1e-13 수준까지 투명하다고 단위 시험으로 확인했다(아래).
그런데 **처음엔 이걸 Engine 두 개(노트를 파트별로 나눠 보낸 것 vs 전부 melody 로 보낸 것)로
비교하려다 "최대 차이 16348 LSB"(50% 가까이!)라는 가짜 실패를 봤다.** 원인 추적:
`SynthNote.noteOn()` 의 사람다움(`_hCent()`, synth.dart 33번줄)이 **전역 공유 `Random`**
(`_rng = Random(20260811)`, 프로세스 시작 시 한 번 시드)을 쓴다. 같은 프로세스 안에서 엔진을
두 개 만들어 순서대로 노트를 재생하면, 두 번째 엔진의 노트는 **첫 번째 엔진이 이미 뽑아간
난수 뒤부터** 뽑아가서 피치 미세 흔들림이 달라지고, 그게 필터/리미터를 거치며 증폭돼 완전히
다른 파형이 나온다 — **라우팅과 전혀 무관한, 원래부터 있던 구조**(dart run 이 매번 새
프로세스라 "그 스크립트 하나"는 항상 결정론적이지만, 한 스크립트 안에서 엔진을 여러 개
만들면 서로 영향을 준다). 그래서 `genremix_smoke.dart` 는:
- **투명성/팬 방향**(exact-match 가 필요한 것) → `TrackMix.process()` 를 직접 부르는
  순수 신호 단위 시험으로만 확인(1)/1-b)).
- **GENRE_MIX 가 실제로 걸리는지**(방향만 확인하면 되는 것) → 진짜 Engine 을 쓰되
  ">"/"<=1e-5" 같은 방향성만 본다(2)/3)). 다음에 이 파일을 만질 때, 또는 다른 스모크에서
  "엔진 두 개 만들어 정확히 비교"하는 코드를 보면 이 함정부터 의심할 것.

### 스모크 테스트로 확인한 것
```
cd "$HOME/AI Company/music_doodle_engine" && dart run tool/genremix_smoke.dart
```
1) 투명(단위) — 신호를 4버스로 나눠 처리+합산 vs 1버스로 합쳐 처리 → 최대 차이 8e-13
   (선형 시스템 중첩 원리가 실제로 성립 — 라우팅 구조 변경이 신호를 안 바꾼다는 확인).
1-b) 팬 방향 — pan=-0.14 는 왼쪽이, pan=0.30 은 오른쪽이 실제로 더 크다.
2) 베이스 RMS — lofi(vol=1.05) 걸면 0.1834→0.2285 로 커진다(배선 확인).
3) 리버브 send — lofi(rev=0.24) 걸고 멜로디 음이 끊긴 뒤 무음 구간에 RMS 0.026 잔향이 남는다
   (마스터 자신의 revWet=0 인 채로 — send 단독 경로가 실제로 탱크를 돌린다는 확인).
4) 모르는 장르('trap') — 예외 없이 조용히 무시, 트랙 버스 값 그대로.

**숫자로는 배선(라우팅·팬 방향·볼륨 배율·리버브 send)이 맞다는 것만 확인했다. lofi/house/
hiphop/citypop/ballad/rock 각각이 실제로 "그 장르답게 자리 잡은 소리"로 들리는지(드럼이
뒤로 빠지고 코드가 앞에 나오는지, 팬이 자연스러운지, EQ 로 대역이 정리됐는지)는 전혀
확인 못 했다 — 폰에서 반드시 들어볼 것.**

### 회귀 확인 — 피크가 미세하게 움직였다(회귀 아님, 원인 확인함)
`drum_smoke`/`mixer_smoke`/`pattern_smoke`/`theory_smoke` 는 이전 단계와 **완전히 같은
수치**를 낸다(TrackMix 가 안 끼는 경로이거나, 파트 하나만 쓰는 경로라 영향이 없다).
그런데 `song_smoke.dart` 4번(house 전체곡)만 **피크 0.959→0.963** 로 움직였다. 원인을
직접 확인했다 — engine.dart 의 render() 루프를 예전 방식(파트 분리 없이 한 번에 합산)으로
**임시로 되돌려서 같은 렌더를 돌려 보니 정확히 0.959 가 나왔다.** 즉 TrackMix 의 기본
HPF(20Hz)/LPF(20000Hz)/EQ(0dB) 필터 체인이 신호 경로에 새로 끼면서 생기는 미세한 이동이지,
로직 버그가 아니다. **이건 새로운 현상이 아니다** — 3단계에서 마스터 버스(MasterBus)가
처음 생겼을 때도 똑같이 피크가(약 0.967→0.965) 움직였고, 그게 그 이후 모든 스모크의 새
기준값이 됐다. 트랙 버스도 같은 종류의(투명하지만 완전한 항등은 아닌) 필터를 신호 경로에
새로 추가하는 것이므로, 같은 크기(1% 미만)의 이동은 **기대되는 정상 범위**로 본다.
하드클립·버퍟바닥·음부족·드럼부족은 전부 0 그대로다 — 기능적 회귀는 없다.

이번 세션 재실행 기준값(다음에 이 스모크들을 돌릴 때 비교 기준):
- `drum_smoke` 2) 피크(리미터 후)=0.9613 드럼최대동시=6 하드클립=0
- `mixer_smoke` 1) 피크=0.965 하드클립=0 컴프감쇠=0.00dB
- `pattern_smoke` 6) 피크=0.965 하드클립=0
- `theory_smoke` 8) 피크=0.962 하드클립=0
- `song_smoke` 4) 피크=0.963 하드클립=0 (이전 0.959 — 위 이유로 이동, 회귀 아님)

### 아직 안 옮긴 것
- **트랙 구조 완전 일반화**(같은 타입 트랙 여러 개 — 예: 트랩의 패드+브라스가 둘 다 chord) —
  객체형 롱폼 5곡(trap/proghouse/pop/jazz/ambient)과 `GENRE_MIX` 의 나머지 슬롯(b808/pad/
  brass/bell/plk 등)이 이걸 요구한다. 지금 구조("타입당 버스 하나")로는 못 담는다.
- 트랙 버스의 HPF/LPF 는 GENRE_MIX 의 배열형 6곡 항목엔 없어서(객체형 전용 필드) 안 옮김 —
  기본값(20Hz/20000Hz, 투명) 그대로.
- 시퀀서 UI(정지/일시정지/진행 표시), 조/템포 바꾸기 UI — 4단계 3/N 부터 미룸.
- "믹스 초기화" 는 수동 버튼이다(마스터 슬라이더와 같은 설계 — 자동으로 안 꺼진다).
  `_playSong`(11개 장르 시험, 배열형 밖의 장르도 포함)은 매번 자동으로 초기화하게 해 뒀지만,
  드럼 단독 시험(`_playPattern`)·건반 자유연주·부하시험 등은 안 건드렸다 — 직전에
  전체곡을 재생했다면 그 장르의 드럼 버스 EQ/팬이 남아 있을 수 있다(사소하지만 다음에
  손대게 되면 참고).

### 빌드 상태
`flutter analyze`(lib/) 오류·경고 0. `flutter analyze`(전체) 는 `packages/`·`tool/` 안
info 64개만(전부 `avoid_print`/`avoid_relative_lib_imports`, 기존부터 무시 대상).
`flutter build apk --release` 성공 — `build/app/outputs/flutter-apk/app-release.apk`
(46.0MB). **폰 미연결로 설치·실행·청감 확인은 못 했다.** `공유/` 로 복사도 안 함.

### 다음 세션이 반드시 할 것 (4단계 4/N 시점 기록 — 4단계 5/N 이후로는 아래 새 섹션 참고)
1. `adb devices` 로 `RFKL505TRAZ` 확인 → 잡히면 이 APK 설치 후 **여덟 덩어리(2단계·3단계
   1~3/N·4단계 1~4/N)를 한 세션에서 같이** 청감 확인. 시간이 부족하면 "전체 곡 재생" 부터
   — GENRE_MIX 가 새로 걸린 상태라 가장 최근 변화이자 가장 상위 구조다.
2. "전체 곡 재생" 6개 칩을 눌러: 로파이는 드럼이 코드보다 뒤로 빠지고 코드/멜로디가
   왼쪽/오른쪽으로 자연스럽게 벌어지는지, 하우스는 드럼이 앞으로 나오고 하이가 살아있는지,
   힙합은 808 베이스가 확실히 두꺼운지 — GENRE_MIX 표(genre_mix.dart)의 vol/pan/eq 방향과
   귀로 들리는 인상이 맞는지 대조할 것.
3. "믹스 초기화" 버튼을 눌렀을 때 정말 이전(4단계 3/N 상태, 장르 밸런스 없음)처럼 밋밋하게
   돌아가는지 확인.
4. `dumpsys media.audio_flinger` 로 언더런 0 유지 확인 — 트랙 버스마다 바이쿼드 5개씩
   (hpf+lpf+eq3, 스테레오) × 4트랙 = 40개가 늘었으니 오디오 여유가 A17 기준값에서 크게
   떨어지지 않는지 볼 것(이번 세션은 폰이 없어 CPU 실측을 전혀 못 했다).
5. 문제없으면 이 섹션의 "⚠️ 폰 실측 미검증" 을 실측값으로 덮어쓸 것.
6. 그다음 작업은 트랙 구조 완전 일반화(같은 타입 트랙 여러 개) — 객체형 롱폼 5곡과
   GENRE_MIX 나머지 슬롯 둘 다 이게 있어야 진행할 수 있다.

---

## 4단계 5/N — 트랙 구조 완전 일반화 + 객체형 롱폼 첫 곡(트랩) (2026-08-13, 자동 세션·**폰 실측 미검증**)

⚠️ **이번에도 자동 실행(스케줄)이라 폰(RFKL505TRAZ)이 안 잡혔다.** `adb kill-server`+
`start-server` 재시작 후에도 `system_profiler SPUSBDataType` 에 Samsung/Android 기기가
물리적으로 안 잡혀 있었다(`emulator-5562 offline` 만 보임 — 무시 대상). **2단계·3단계
(1~3/N)·4단계(1~5/N, 이번 것) 아홉 덩어리가 청감 미검증으로 쌓였다. 다음 폰 세션은
"트랩 전체 곡"부터 우선 확인할 것** — 가장 최근이고 가장 상위 구조(6트랙 동시)라 문제가
있으면 하위 단계보다 먼저 드러난다.

### 왜 이 덩어리인가
지난 세션(4단계 4/N)이 못 박아 둔 다음 작업. 트랙 버스가 "타입당 하나"(bass/chord/melody
고정 3개)에서 **곡마다 다른 개수·이름의 슬롯**으로 일반화돼야 객체형 롱폼(트랩 등 —
패드·브라스가 둘 다 chord 타입인데 버스는 따로)을 걸 수 있다. 일반화만 하고 실제로 쓰는
곳이 없으면 검증이 안 되므로, **같은 세션에 트랩 하나를 끝까지 연결**했다 — 데이터(드럼
패턴·베이스/코드/멜로디 패턴)는 4단계 1/N·2/N 에서 이미 다 옮겨져 있어서(웹 LONG_LIB),
이번엔 그걸 엮는 뼈대(SONG_FORMS 객체형·트랙 편성·GENRE_MIX 객체형 몫)만 있으면 됐다.

### 옮긴 것 / 새로 만든 것

| 파일 | 내용 |
|---|---|
| `lib/mixer.dart` | `TrackMixSet` 을 고정 4필드(drum/bass/chord/melody)에서 `drum`(계속 고정 — 모든 곡이 드럼 타입 트랙을 정확히 하나만 둠) + `List<String> slotNames`/`List<TrackMix> slots`(멀로딕 버스 N개)로 바꿨다. `configure(names)`(편성 전환 — 이름이 같으면 no-op), `bus(name)`/`indexOf(name)` 추가. `bass`/`chord`/`melody` 게터는 하위호환으로 남김(기본 편성 인덱스 0/1/2) |
| `lib/engine.dart` | `render()` 의 파트별 합산을 고정 6변수(bL/cL/mL/dL...) 스위치문에서 **슬롯 개수만큼의 배열**(`_accL`/`_accR`, 인스턴스 필드로 재사용 — 핫패스 할당 방지)로 바꿨다. 덧셈 순서를 예전과 정확히 같게 유지(슬롯 순서대로 합산 뒤 드럼) — 그래서 기본 3슬롯일 때 부동소수점 결과가 스모크 테스트 기준값과 **한 비트도 안 달라졌다**(아래). `n.part` 범위를 벗어나면 마지막 슬롯으로 클램프(편성 전환 도중 남은 음 방어) |
| `lib/genre_mix.dart` | `GenreMixSpec` 을 고정 4필드에서 `Map<String,PartMixSpec>`(슬롯 이름 기반)으로 바꿨다. `PartMixSpec` 에 `hpf`/`lpf`(nullable — 배열형 6곡엔 없어서 건드리지 않음) 추가. `melodicSlotsFor(genre)`(드럼 뺀 슬롯 이름 목록, `Map` 삽입 순서 그대로) 신규. `kGenreMix` 에 웹 GENRE_MIX 의 `trap` 항목 추가(배열형 6곡 값은 안 바꿈) |
| `lib/song.dart` | `ObjectSongTrack`(slot/type/voice) · `ObjectSongSection`(parts 가 슬롯맵) · `ObjectSongForm`(tracks/sections/order) · `kObjectSongForms['trap']`(웹 SONG_FORMS 트랩 그대로) · `buildObjectSong()`(`buildSong()` 과 같은 로직 — 섹션 이어붙이기·패턴 자기반복·마지막 등장에만 필인 — 슬롯 맵 위에서) 신규. `kPlainDrum` 에 `'Trap Hook':'Trap Hook Plain'` 추가 |
| `lib/audio_isolate.dart` | `_cGenreMix` 핸들러가 값 적용 **전에** `engine.trackMix.configure(melodicSlotsFor(genre))` 로 편성부터 맞추도록 수정(배열형 6곡은 no-op) |
| `lib/main.dart` | `kFullSongsObject`(지금은 트랩 하나) · `_playObjectSong()`(슬롯별 인덱스·악기를 계산해 `host.batch()`) · "완성곡" UI 섹션(핑크 칩) 추가 |
| `tool/objectsong_smoke.dart` (신규) | 폰 없이 돌리는 수치 확인 4가지 |
| `tool/genremix_smoke.dart` | 4번 시험이 `'trap'` 을 "모르는 장르"로 썼는데 이번에 트랩이 진짜 장르가 돼서 전제가 깨졌다 — `'nonexistent-genre'` 로 바꿨다(값 자체는 그대로, 이름만 교체) |

### 왜 드럼은 일반화하지 않았나
객체형 롱폼(trap/proghouse/pop/jazz/ambient) 전부 `tracks` 에 드럼 타입 트랙이 **정확히
하나**다(`{slot:'drum',type:'drum',...}`). 여러 개를 두는 경우가 아예 없으므로, 드럼은
계속 `TrackMixSet.drum` 고정 필드 하나로 남겨 뒀다 — 일반화가 필요 없는 축까지 괜히
건드리면 코드만 복잡해지고 얻는 게 없다.

### 렌더 핫패스 — 배열 크기를 매 샘플이 아니라 매 `render()` 호출에서만 확인한다
슬롯 개수(`nSlots`)는 곡마다 다르므로 합산 버퍼(`_accL`/`_accR`)를 고정 크기로 둘 수 없다.
그렇다고 매 샘플 새로 할당하면(예전엔 로컬 변수 6개라 공짜였던 것) GC 압박이 생긴다.
그래서 `Engine` 의 인스턴스 필드로 8슬롯짜리를 미리 잡아 두고, `render(frames)` 진입 시
(매 오디오 콜백당 한 번, 샘플마다가 아니다) `trackMix.slots.length` 가 그보다 크면만 다시
할당한다 — 지금까지 가장 큰 편성(트랩 5개)도 여유 안에 들어온다. 매 샘플 반복문은 그
버퍼를 0으로 리셋하는 것뿐(nSlots 가 작아서 비용이 예전 "로컬 변수 6개 초기화"와 같은
자릿수다).

### 덧셈 순서를 지킨 이유 — 스모크 테스트 기준값이 한 비트도 안 흔들려야 했다
예전 코드는 `(bl+cl+ml+dl)*masterGain` 처럼 고정 순서로 더했다. 부동소수점 덧셈은
순서를 바꾸면 결과가 미세하게 달라질 수 있어서(결합법칙이 정확히 성립하지 않는다),
슬롯을 배열로 일반화하면서도 **슬롯 순서대로 합산 → 드럼을 마지막에 더함** 이라는 순서를
그대로 지켰다. 그 결과 기본 3슬롯(bass/chord/melody) 구성에서 `drum_smoke`/`mixer_smoke`/
`pattern_smoke`/`theory_smoke`/`song_smoke` 전부 4단계 4/N 이 남긴 기준값과 **완전히
동일한 숫자**가 나왔다(피크 0.9613.../0.965/0.965/0.962/0.963 — 소수점 하나도 안 틀림).
회귀가 없다는 걸 근사치가 아니라 정확한 일치로 확인한 것.

### 슬롯 라우팅 검증 — RMS 비교 대신 음소거 이분법을 쓴 이유
처음엔 "GENRE_MIX 를 걸면 vol 배율만큼 RMS 가 변하는가"로 라우팅을 확인하려 했는데,
트랩 `bell` 슬롯은 vol=0.90(줄어야 함)인데 rev=0.24(리버브 send)가 커서 2초·음 4개 창
기준 RMS 가 오히려 **늘었다**(0.1267→0.1859) — 라우팅 버그가 아니라 리버브 탱크가
보태는 에너지가 vol 10% 감소보다 컸을 뿐이다(리버브 send 자체는 이미 4단계 4/N
`genremix_smoke.dart` 3번이 따로 검증함). rev 를 0 으로 고정해도 EQ/팬의 잔여 효과로
정확히 90% 가 안 나와 여전히 애매했다. → **음소거 이분법**으로 바꿨다: b808+bell 두
슬롯에 동시에 음을 걸어 두고, bell 버스 vol 을 0 으로 죽이면 b808 단독 렌더와 **정확히
같아야** 하고(1e-6 이내 — bell 음이 다른 슬롯으로 새면 안 된다), 다시 켜면 **뚜렷이
커야** 한다(bell 음이 엉뚱한 곳에 버려지지 않고 진짜 나온다). 이진 판정이라 EQ/pan/rev
같은 부수 효과에 흔들리지 않는다. 이 시험을 만들다가 또 하나 함정을 밟았다 — 두 렌더가
스케줄하는 음 개수가 다르면(4개 vs 8개) 전역 공유 RNG(휴머나이즈, synth.dart `_hCent()`)
소비량이 갈려서 "같아야 할" 음도 미세하게 달라진다(4단계 4/N 이 이미 밟았던 함정과 같은
종류). `Human.setLevel(0)` 으로 휴머나이즈를 꺼서 결정론적으로 만들고 시험 끝에 되돌렸다.

### 스모크 테스트 결과
```
cd "$HOME/AI Company/music_doodle_engine" && dart run tool/objectsong_smoke.dart
```
1) 'trap' 빌드 — 마디=120(웹 주석 "120마디≈3분26초"와 일치) 길이=205.7s. 슬롯별 타격:
   드럼=2040 b808=200 pad=112 brass=148 bell=438 plk=768 — 전부 0개가 아님(전 슬롯이
   실제로 쓰인다는 확인).
2) 편성/값 — `melodicSlotsFor('trap')` = `[b808, pad, brass, bell, plk]`(GENRE_MIX 삽입
   순서 그대로). `configure()` + `applyGenreMixTo()` 뒤 b808 vol=1.12·hpf=22·lpf=2500,
   brass pan=0.3, 드럼 버스 hpf=30 — 전부 표(genre_mix.dart) 값과 정확히 일치.
3) 라우팅(음소거 이분법) — b808단독=0.1459, bell무음=0.1459(**b808단독과 완전히 같음** —
   bell 음이 안 샌다), bell켜짐=0.1908(뚜렷이 커짐 — bell 음이 실제로 나온다).
4) 트랩 전체곡(드럼+5멀로딕 슬롯, 6트랙 동시) 렌더 — 길이=207.7s 피크=0.969 하드클립=0
   버퍼바닥=0 음부족=0 드럼부족=0 최대동시음=22 최대동시드럼=7. 5트랙(house)보다 트랙이
   하나 더 늘었는데도 여유가 있다.
`drum_smoke`/`mixer_smoke`/`pattern_smoke`/`theory_smoke`/`song_smoke` 재실행 — 전부
4단계 4/N 과 **완전히 동일한 수치**(위 "덧셈 순서" 항목 참고) — 회귀 없음.

**숫자로는 편성·라우팅·GENRE_MIX 배선이 정확하다는 것만 확인했다. 트랩이 실제로
"그 장르답게" 들리는지(808 이 두껍고 하이햇이 촘촘한지, 패드는 뒤로 빠지고 브라스 힛이
악센트로 튀는지, 벨 리드가 훅에서 도드라지는지) 전혀 확인 못 했다 — 폰에서 반드시
들어볼 것.**

### 아직 안 옮긴 것
- **proghouse/pop/jazz/ambient**(객체형 롱폼 나머지 4곡) — 뼈대(`ObjectSongForm`/
  `buildObjectSong`/`TrackMixSet.configure`)는 이제 범용이니, 각 곡의 `kObjectSongForms`
  데이터 + `kGenreMix` 항목만 추가하면 된다(트랙 편성·섹션·순서는 이미 웹 소스에서 뽑아둔
  게 이 세션 조사 중에 있다 — HANDOFF 맨 위 리서치 참고 없이 `~/AI Company/music_doodle/
  assets/app.html` 의 `SONG_FORMS`(7764줄~)/`GENRE_MIX`(7952줄~)에서 다시 뽑으면 됨).
- `EXTRA_TRACKS`("꽉 채워서" 옵션) — 롱폼 곡 전용, 트랙 구조 위에 얹는 것이라 나중에.
- 시퀀서 UI(정지/일시정지/진행 표시), 조/템포 바꾸기 UI — 계속 미룸.
- "믹스 초기화" 버튼은 여전히 배열형 6곡 편성(`['bass','chord','melody']`)으로만
  되돌린다 — 객체형 롱폼을 재생한 직후 눌러도 정상 동작한다(`_cGenreMix` 의 else 분기가
  `configure(const ['bass','chord','melody'])` 를 먼저 부른 뒤 리셋하므로), 다만 트랩을
  재생한 직후 "화성 패턴 재생"(`_playSong`, 배열형 밖 장르도 포함)을 눌러도 그 함수가
  스스로 `setGenreMix(null)` 을 부르므로 마찬가지로 안전하다.

### 빌드 상태
`flutter analyze lib/` 오류·경고 0. `flutter analyze lib/ tool/` 전체는 `packages/`
안 info + `avoid_print`/`avoid_relative_lib_imports`(전부 기존부터 무시 대상)만.
`flutter build apk --release` 성공 — `build/app/outputs/flutter-apk/app-release.apk`
(46.0MB). **폰 미연결로 설치·실행·청감 확인은 못 했다.** `공유/` 로 복사도 안 함.

### 다음 세션이 반드시 할 것
1. `adb devices` 로 `RFKL505TRAZ` 확인 → 잡히면 이 APK 설치 후 **아홉 덩어리를 한
   세션에서 같이** 청감 확인. 시간이 부족하면 "완성곡" 섹션의 "▶ 트랩 전체곡"부터 —
   가장 최근 변화이자 가장 상위 구조(6트랙 동시 재생)라 문제가 있으면 여기서 먼저 드러난다.
2. 트랩을 들으며: 808 베이스가 확실히 두껍고 좁게(hpf22/lpf2500) 들리는지, 패드가 뒤로
   빠지고(pan -0.22) 브라스 힛이 오른쪽에서 악센트로 튀는지(pan 0.30), 벨 리드가 훅
   섹션에서 도드라지는지, 인트로→벌스→프리훅→훅→(반복)→브리지→훅→아웃트로 전환이
   자연스러운지(3분 26초짜리 곡이라 배열형 6곡보다 훨씬 길다 — 끝까지 들어볼 것).
3. `dumpsys media.audio_flinger` 로 언더런 0 유지 확인 — 트랙 버스가 4개(배열형)에서
   6개(트랩: 드럼+5멀로딕)로 늘었고 곡도 3배 이상 길다. 오디오 여유가 A17 기준값에서
   떨어지지 않는지 볼 것(이번 세션은 폰이 없어 CPU 실측을 전혀 못 했다).
4. 문제없으면 이 섹션의 "⚠️ 폰 실측 미검증" 을 실측값으로 덮어쓸 것.
5. 그다음 작업은 proghouse/pop/jazz/ambient 4곡 추가 — "아직 안 옮긴 것" 참고, 뼈대는
   이미 있으니 데이터만 옮기면 된다(한 세션에 1~2곡씩, "한 번에 한 덩어리").

---

## 폰 실측 세션 (2026-08-13 저녁, 사용자 동석) — 아홉 덩어리 첫 실기 검증

드디어 폰(RFKL505TRAZ)이 연결된 상태로 돌렸다. **2단계~4단계 5/N 이 실기에서 처음 돌아갔다.**

### ⚠️ 이 세션 초반에 낸 사고 — 반드시 읽을 것

`HANDOFF.md` 를 **읽지 않고** "2단계 드럼을 이제 시작한다"고 착각해서 `lib/drums.dart` 를
새로 써 버렸다(이미 8/12 에 완성돼 있던 파일). `engine.dart` 에도 중복 배선을 넣었다.

복구 방법 — **같은 일이 또 나면 이렇게 하면 된다**:
세션 기록 `~/.claude/projects/-Users-choeseunghwan-AI-Company-infinity-trader/*.jsonl` 에
Write/Edit 의 **전체 내용이 그대로 남아 있다.** 덮어쓴 시각을 기준으로 그 이전의 마지막
Write 를 찾고, 그 뒤의 Edit 들을 시각 순으로 다시 적용하면 원본이 복원된다.
복원 검증은 스모크 테스트 수치 대조로 했다(전 항목 소수점까지 일치 → 소리가 같다는 증거).

**교훈: 작업 시작 전 `ls lib/` 와 `HANDOFF.md` 확인은 선택이 아니다.**

### 실측 결과 — 트랩 전체곡 (6트랙 동시, 3분 26초)

| 지표 | 값 |
|---|---|
| 버퍼 바닥 (앱 자체) | **0회** |
| 하드클립 | **0회** |
| 음 부족 · 드럼 음 부족 | **0회 · 0회** |
| 동시음 / 동시 드럼 | 최대 22 / 최대 7 (스모크 테스트 예측과 정확히 일치) |
| 리미터 감쇠 | -2.9dB (최대 -6.9dB) · 리미터 전 피크 2.11 |
| 오디오 여유 | 71% (한산한 구간) → **53%** (가장 빽빽한 구간) · **최악 0%** |
| 지연 | 31ms · FAST 플래그 유지 |

**앱 자체 지표는 전부 깨끗하다.** 다만 아래 문제를 찾았다.

### 찾은 문제 ① 예약 큐가 O(n) 이었다 — 고쳤다 (절반 해결)

`dumpsys media.audio_flinger` 의 언더런 카운터가 **조작을 전혀 안 해도** 계속 늘었다
(60초에 3166회 = 초당 53). 앱 자체 `버퍼 바닥` 은 0인데도. 평균 CPU 는 여유 71% 였다.
→ 평균이 아니라 **순간 스파이크** 문제(`최악 여유 0%`).

원인: `render()` 의 청크 루프가 매 구간마다 예약 큐 **전체**를 세 번 훑었다
(가장 이른 예약 찾기 · `at` 일괄 감산 · `removeAt` 중간 삭제). 트랩은 예약이 3700개
(드럼 2040 + 멜로딕 1666)라 초당 수십만 번 돌았다.

수정: `at` 을 **절대 시각**(`_now` = 지금까지 렌더한 총 샘플 수)으로 바꾸고 큐를 시각 순
정렬. 다음 예약은 맨 앞 하나만 보면 되고(O(1)), 발사는 인덱스만 민다(삭제 비용 0).

결과: **초당 53회 → 23회.** 절반으로 줄었지만 **0 은 아니다.**

> **함정**: 이렇게 바꾸면 *같은 시각* 예약의 발사 순서가 바뀐다. 예전엔 큐를 뒤에서부터
> 훑어서 "나중에 넣은 것부터" 쳤다. 순서가 바뀌면 전역 RNG(휴머나이즈)를 소비하는 차례가
> 달라져 렌더 결과가 미세하게 달라진다. `_seq`(일련번호) 를 정렬 2차 키로 넣어 순서를
> 못 박았다 — Dart 의 `List.sort` 는 안정 정렬을 보장하지 않으므로 이게 없으면
> **같은 곡인데 실행할 때마다 결과가 달라진다.**

### 스모크 테스트 기준값 재설정 (2026-08-13)

발사 순서가 "나중 것 먼저" → "넣은 순서대로" 로 바뀌면서 두 개가 미세하게 움직였다.
버그가 아니라 순서 변경에 따른 휴머나이즈 난수 배정 차이다(하드클립 0·버퍼바닥 0 유지).
**앞으로는 이 값과 대조할 것:**

| 시험 | 이전 | **새 기준값** |
|---|---|---|
| drum | 0.9613… | **0.9593505859375** |
| pattern | 0.965 | **0.962** |
| mixer / theory / song / objectsong | 0.965 / 0.962 / 0.963 / 0.969 | **변화 없음** |

### 찾은 문제 ② 아직 안 고침 — 남은 언더런 (초당 ~23회)

트랩 재생 중 `dumpsys` 언더런이 여전히 는다. 앱 자체 `버퍼 바닥` 은 0이라
**둘 중 하나가 실제를 못 보고 있다**(앱 쪽 추정은 1.5초마다 보정하는 근사값이다).

다음 세션이 볼 것, 의심 순서대로:
1. **매 `render()` 호출마다 `Int16List(frames*2)` 를 새로 만든다** → 초당 ~48번 할당.
   오디오 아이솔레이트에서 GC 가 돌면 그게 곧 스파이크다. 버퍼를 재사용하면 되는데,
   `FlutterPcmSound.feed` 가 메서드 채널로 **복사해 보내는지** 먼저 확인할 것
   (복사 전에 재사용하면 소리가 깨진다).
2. **믹서 체인 비용** — 트랩은 트랙 버스 6개 + 마스터(EQ·컴프·리버브)를 매 샘플 돈다.
   가장 빽빽한 구간에서 여유가 53% 까지 떨어졌다. 1·2단계 때(98%)와 자릿수가 다르다.
3. `최악 여유 0%` 가 한산할 때도 뜬다 — 어디서 한 번 크게 튀는지 특정할 것.

### 사용자가 아직 귀로 확인 안 한 것
이 세션은 수치 검증까지만 했다. 트랩을 재생해 두긴 했으나 **청감 피드백은 못 받았다.**
다음에 반드시: 808 두께(hpf22/lpf2500), 패드 뒤로 빠짐(pan -0.22), 브라스 오른쪽 악센트
(pan 0.30), 벨 리드가 훅에서 도드라지는지, 드럼 5키트가 서로 다르게 들리는지.

### 남은 언더런 추적 (같은 세션 이어서) — 두 개 고쳤고, 아직 0은 아니다

`dumpsys` 언더런을 줄이려고 네 가지를 시도했다. **측정 방법에 함정이 있었으니 아래를 먼저 읽을 것.**

| 시도 | 결과 |
|---|---|
| ① 예약 큐 O(n) 제거 (절대 시각) | 초당 53 → 23회 |
| ② 폴링 3ms→8ms, 덩어리 256→1024 | **초당 92회 — 더 나빠져서 되돌림** |
| ③ 버퍼 목표 2048 → 3072 | 초당 9.4회 |
| ④ SynthNote 의 Env 재사용(할당 제거) | 초당 15.6회 |

**②가 왜 실패했나** — "채널 왕복을 줄이면 낫겠지"는 틀린 가설이었다.
`Future.delayed` 는 정확하지 않고, 폴링 사이에 렌더가 끼면 실제 간격이 더 벌어진다.
그동안 버퍼는 계속 빠지므로 **파이는 깊이**가 커진다.
채널 왕복 횟수보다 버퍼가 얼마나 깊이 파이느냐가 훨씬 중요하다. 코드 주석에도 남겼다.

**③↔④ 는 오차 범위다 (측정 방법 함정)**
90초 창으로 쟀는데, 트랩은 구간마다 밀도가 완전히 다르다(인트로 vs 훅).
재생 시작 시점이 조금만 달라도 창이 다른 구간을 본다. 9.4 와 15.6 은 구분할 수 없는 값이다.
**앞으로 A/B 를 할 거면 곡 전체(207초)를 창으로 잡거나, 밀도가 일정한 부하(음 쏟아붓기)로 잴 것.**
곡 전체로 재보려 했으나 그때 사용자가 폰을 만지고 있어서(스크롤 위치가 바뀌어 탭이 빗나감)
재생 없이 유휴를 잰 셈이 됐다 — **무효**. 다음 세션이 다시 재야 한다.

**그래도 ①과 ④는 남길 가치가 있다** — 측정과 무관하게 구조적으로 옳다.
①은 O(n)→O(1), ④는 음 하나당 객체 30여 개 할당 제거(가산합성 6 + 디테일 2, 각각 리스트 3개).
둘 다 스모크 테스트 수치를 바꾸지 않는다(순수 성능 개선).

**지금 기본값**: 버퍼 목표 3072프레임 = 지연 64ms(예전 43ms). 폴링 3ms.
지연을 다시 줄이려면 언더런 원인을 끝까지 잡은 뒤에 할 것.

### 다음 세션이 할 것 (우선순위 순)
1. **곡 전체(207초) 창으로 언더런 재측정** — 폰을 만지지 않는 상태에서.
   `adb shell input tap` 전에 반드시 화면 캡처로 버튼 위치를 확인할 것(스크롤 위치가 매번 다르다).
2. 여전히 남으면: 앱 자체 `버퍼 바닥`(0) 과 `dumpsys`(늘어남)가 왜 다른지부터.
   앱 쪽은 1.5초마다 보정하는 **추정값**이라 짧은 dip 을 놓칠 수 있다 — 그쪽을 먼저 정확하게 만들 것.
3. 그다음이 proghouse/pop/jazz/ambient 4곡.

### 청감 확인 현황 (2026-08-13 저녁, 사용자 직접 확인)

| 덩어리 | 상태 |
|---|---|
| 1단계 신디사이저 | ✅ "이제 쓸 만 한 거 같아" (8/11) |
| 2단계 드럼 + 4단계 트랩 전체곡 | ✅ "지금 괜찮아" — 6트랙 동시 재생 청감 통과 |
| 3단계 **EQ** (20밴드) | ✅ "이큐 잘 작동돼" — 슬라이더를 움직이면 소리가 실제로 바뀐다 |
| 3단계 컴프레서 · 리버브 · 하이패스 | ⬜ **아직 청감 미확인** |
| 4단계 GENRE_MIX 세부 (808 두께·패드 pan·브라스 악센트) | ⬜ 아직 세부 청감 미확인 |

→ 아홉 덩어리 중 **큰 줄기는 통과**했다. 남은 청감 확인은 3단계의 컴프/리버브/하이패스와
GENRE_MIX 의 슬롯별 세부(팬·EQ가 곡 안에서 의도대로 들리는가)뿐이다.
다음 세션이 사용자에게 물을 것은 **"컴프 켜면 눌리는 느낌이 나는가", "리버브 올리면 공간이
생기는가", "하이패스 올리면 저음이 빠지는가"** 이 셋이다(EQ 는 이미 확인됨, 다시 묻지 말 것).

---

## 4단계 6/N — 프로그하우스 · 팝 이식 (2026-08-13 심야, 사용자 동석)

### 옮긴 것
| 파일 | 내용 |
|---|---|
| `lib/song.dart` | `kObjectSongForms` 에 `'proghouse'`(112마디) · `'pop'`(96마디) 추가 |
| `lib/genre_mix.dart` | `kGenreMix` 에 두 곡의 슬롯별 믹스 추가 (웹 GENRE_MIX 값 그대로) |
| `lib/main.dart` | `kFullSongsObject` 를 4튜플 → **5튜플(조 추가)**. `_playObjectSong` 에 `mode` 인자 |
| `tool/objectsong_smoke.dart` | `smokeNewSongs()` — 마디 수·슬롯 사용 여부·GENRE_MIX 슬롯 이름 대조 |

**패턴은 하나도 새로 안 만들었다** — `Prog *`/`Pop *` 전부 4단계 1/N·2/N 에서 이미 옮겨져 있었다.
이번엔 뼈대(트랙 편성·섹션·order·믹스)만 얹으면 됐다. 다음 두 곡(jazz/ambient)도 같다.

### 밟을 뻔한 함정 — 팝은 **장조**다
웹 `SONG_FORMS` 는 곡 메타에 `key:{mode:'major'}` 를 따로 들고 있다(트랩·프로그하우스는 없음=단조).
`kFullSongsObject` 가 조를 안 들고 있어서 그대로 두면 **팝이 단조로 재생된다** — 코드가
I–V–vi–IV 가 아니라 i–v–VI–iv 로 나와서 완전히 다른 곡이 된다. 튜플에 조를 넣고
`buildObjectSong(key: MusicKey(mode: mode))` 로 넘기게 고쳤다.
**jazz/ambient 도 웹에 `key` 가 명시돼 있으니(둘 다 minor) 옮길 때 확인할 것.**

### 스모크 결과
```
5) 'proghouse' — 마디=112 길이=210.0s 슬롯별=drum=1640 bass=264 pad=88 stab=160 arp=1280 lead=280
5) 'pop'       — 마디=96  길이=221.5s 슬롯별=drum=1008 bass=280 piano=176 gtr=152 voc=258 plk=896 str=72
```
웹 주석("112마디≈3분30초", "96마디=3분41초")과 길이가 맞는다. 전 슬롯이 0이 아니다
(= 편성한 트랙이 곡에서 실제로 다 쓰인다).

시험이 하나 잡아 준 것: **팝의 `str` 슬롯은 GENRE_MIX 에 없어서 투명하게 나간다.**
웹도 마찬가지다(웹 `pop.str` 은 SONG_FORMS 쪽 EXTRA_TRACKS 용 값이라 GENRE_MIX 항목이 아니다).
버그가 아니라 원본 그대로지만, 스트링이 믹스에서 안 다뤄진다는 건 알고 있어야 한다.

기존 5개 스모크 재실행 — mixer 0.965 · pattern 0.962 · theory 0.962 · song 0.963 · trap 0.969, **회귀 없음**.

### 언더런 — 곡 전체(207초) 창으로 제대로 잰 첫 숫자
**6018회 / 207초 = 초당 29회** (전체 샘플의 0.06%). 폰을 만지지 않고 잰 값이다.
지금까지의 90초 창 숫자(23/9.4/15.6)는 구간 밀도가 달라 서로 비교 불가였다 —
**앞으로는 이 207초 값(29/초)을 기준으로 비교할 것.**

### 다음 세션이 할 것
1. **jazz · ambient 이식** (마지막 두 곡). jazz 는 **7화음**을 쓰므로 코드 패턴의
   다섯째 값을 확인할 것(웹 주석 참고). 둘 다 `key` minor 명시돼 있다.
2. 언더런 29/초 원인 — 앱 자체 `버퍼 바닥`(0)과 `dumpsys`(늘어남)가 왜 다른지부터.
   앱 쪽은 1.5초마다 보정하는 추정값이라 짧은 dip 을 놓친다. 그쪽을 먼저 정확하게 만들 것.
3. `EXTRA_TRACKS`("꽉 채워서") · 시퀀서 UI · 5단계(UI 이식, 6~8세션).

---

## 4단계 7/N — 재즈 · 엠비언트 이식 = **곡 데이터 완료(5곡)** (2026-08-13 심야)

| 곡 | 마디 | 길이 | 트랙 | 조 |
|---|---|---|---|---|
| 트랩 「새벽 네 시」 | 120 | 205.7s | 6 | 단조 |
| 프로그하우스 「밤을 건너」 | 112 | 210.0s | 6 | 단조 |
| 팝 「여름이 오면」 | 96 | 221.5s | 7 | **장조** |
| 재즈 「새벽 두 시」 | 96 | 198.6s | 6 | 단조 |
| 엠비언트 「긴 겨울」 | 64 | 213.3s | 7 | 단조 |

전부 웹 주석의 길이와 일치. 전 슬롯이 0이 아님(편성한 트랙이 곡에서 실제로 다 쓰인다).

### 이번에 고친 구조적 가정 — **드럼 슬롯 이름을 박아 두면 안 된다**
엠비언트만 드럼 트랙의 슬롯 이름이 `'drum'` 이 아니라 **`'perc'`** 다
(웹 `SONG_FORMS.ambient` 는 `{slot:'perc', type:'drum'}`, `GENRE_MIX.ambient` 에도
`drum` 키가 아예 없고 `perc` 로 들어 있다).

세 군데가 `'drum'` 을 이름으로 박아 두고 있었다:
1. `genre_mix.dart` `melodicSlotsFor` — `k != 'drum'`
2. `genre_mix.dart` `applyGenreMixTo` — `slot == 'drum' ? mixes.drum : ...`
3. `song.dart` `buildObjectSong` 의 필인 스캔 — `sec.parts['drum']`

그대로 뒀으면 **엠비언트 퍼커션이 멀로딕 버스로 새고 드럼 버스는 투명하게 남았을 것**이다
(소리는 나니까 눈치채기 어렵고, 믹스만 조용히 틀린다).
→ `GenreMixSpec.drumSlot`(기본 `'drum'`) 을 추가해 곡마다 지정하게 했고,
`buildObjectSong` 은 이름이 아니라 **`t.type == 'drum'`** 으로 찾게 고쳤다.
스모크에 라우팅 확인을 넣었다(엠비언트 드럼 버스 vol==0.34 이고, `perc` 가 멀로딕 버스로
**중복 생성되지 않는지**).

### 원본 그대로라 안 고친 것
`pop.str` · `ambient.str2` 는 GENRE_MIX 에 항목이 없어 **투명하게** 나간다.
웹도 같다(그 값들은 SONG_FORMS 쪽 EXTRA_TRACKS 용이지 GENRE_MIX 항목이 아니다).
스모크가 매번 "주의:" 로 알려 준다.

### 남은 것
- `EXTRA_TRACKS`("꽉 채워서" 옵션) — 롱폼 곡 위에 트랙을 더 얹는 기능
- 시퀀서 UI(정지/일시정지/진행 표시), 조·템포 바꾸기 UI
- **5단계 UI 이식(6~8세션, 가장 큼)** · 6단계 저장/내보내기
- 언더런 초당 29회(207초 창 기준) 원인 — 앱 자체 `버퍼 바닥`(0)과 `dumpsys` 불일치부터

### 사용자 청감 확인 필요 (이번 추가분)
- **재즈**: 7화음이 재즈답게 들리는가(m7♭5–V7–im7–VImaj7). 브러시 드럼이 가장 작고
  업라이트 베이스가 앞에 나오는가. 아웃트로에 드럼이 빠지고 색소폰으로 끝나는가.
- **엠비언트**: 리버브가 깊게(0.3~0.5) 걸려 공간이 크게 들리는가. '눈' 섹션에서만
  퍼커션이 들어오는가. 코드가 두 개뿐이라 화성이 아니라 질감으로 끌고 가는 느낌인가.

---

## 4단계 8/N — 언더런 원인 규명 (2026-08-14 새벽, A17 실측)

계기를 세 개 넣어서 **원인을 특정했다**(`audio_isolate.dart` → `AudioStats`):
`급식 지체`(루프 한 바퀴 최대 시간 + 20ms 초과 횟수) · `큐 최소 잔량`.

### 실측 (트랩 전체곡 100초)
| 지표 | 값 |
|---|---|
| 급식 지체 | **최대 36ms · 20ms 초과 124회** |
| 큐 최소 잔량 | **0프레임** |
| 오디오 여유 | 76% (최악 0%) |
| 앱 자체 `버퍼 바닥` | 0회 ← **틀린 값이다** |

### 결론 — 급식 루프가 상시 밀린다
`Future.delayed(3ms)` 를 노렸는데 **초당 1.2회꼴로 20~36ms 씩 밀린다.**
36ms 밀리면 그동안 48000×0.036 = **1728프레임**이 빠진다. 목표 3072 에서 그만큼 파이고,
실제로 **큐가 0 까지 비었다**(= AudioTrack 쓰기 스레드가 굶었다 = `dumpsys` 언더런).

곡 시작 때 한 번(예약 3700개 처리)이 아니라 **상시**라는 게 핵심이다 —
124회는 한 번의 스파이크로 설명되지 않는다.

**근본 원인: Dart 아이솔레이트는 실시간 우선순위를 못 받는다.** 일반 스레드라
안드로이드가 다른 일에 CPU를 주면 그냥 밀린다. GC·타이머 해상도도 겹친다.
아이솔레이트로 옮겨서 UI 스레드 의존은 끊었지만(538회→0회), **스레드 우선순위 문제는 남았다.**

### 앱 자체 `버퍼 바닥`은 못 믿는다
`fed - played + driftFix` 추정값이라 1.5초마다만 보정한다. 짧은 dip 을 통째로 놓친다.
실제로 큐가 0 이 됐는데도 0회로 나왔다. **`큐 최소 잔량`(실측 조회)을 대신 볼 것.**

### 다음 세션이 할 것 — 상황에 따라 버퍼를 바꾼다
버퍼를 4096(85ms)으로 올리면 36ms 지체를 흡수하고도 남는다. 문제는 건반 지연이다.
그런데 **곡 재생에는 85ms 지연이 아무 문제가 안 된다**(시작이 85ms 늦을 뿐이다).
건반 연주에만 문제다. → 실제 DAW 도 그렇게 한다:

1. **건반/패드를 누를 때** = 작은 버퍼(지금의 3072 또는 더 작게)
2. **곡을 재생할 때** = 큰 버퍼(4096~6144)로 자동 전환

`AudioClient.setAhead()` 가 이미 있으니 `_playObjectSong`/`_playFullSong` 진입 때 올리고,
건반을 누르면 내리면 된다. **전환 시점에 큐를 비우지 말 것** — 목표만 바꾸면 자연히 채워진다.

검증: 트랩 전체곡 207초 창에서 `dumpsys` 언더런 **0** 과 `큐 최소 잔량 > 500` 이 목표.
(현재 기준값: 언더런 초당 29회 · 큐 최소 0)

그래도 안 잡히면 그때는 **오디오 생성 자체를 네이티브(C/C++ 또는 Kotlin)로** 내려야 한다 —
Dart 아이솔레이트로는 실시간 우선순위를 얻을 방법이 없다. 다만 그건 마지막 수단이다.

---

## 4단계 9/N — 상황별 버퍼 전환 (2026-08-14 새벽) — 언더런 **29/초 → 7.5/초**

### 한 것
곡을 재생할 때와 건반을 칠 때 버퍼 목표를 다르게 쓴다 (`AudioClient.setSongMode`).

| 상황 | 버퍼 | 지연 | 이유 |
|---|---|---|---|
| 건반·드럼패드 | 3072 (화면에서 선택) | 64ms | 손가락에 붙어야 한다 |
| 곡·패턴 재생 | **6144** | 128ms | 시작이 128ms 늦을 뿐, 안 끊기는 게 훨씬 중요 |

`kAheadSong = 6144` 은 **실측으로 정한 수**다: 급식 루프가 상시 20~36ms 밀리는데(초당 1.2회)
36ms 면 1728프레임이 빠진다. 거기에 AudioTrack 몫(1024)과 지체가 겹칠 여유를 얹었다.
실제 DAW 도 이렇게 한다(연주 모드 ↔ 믹스다운 모드).

호출부는 `main.dart` 의 재생 함수 전부(`_playObjectSong`/`_playFullSong`/`_playSong`/
`_playPattern`/`_chord`/`_arp`/`_sweep`/`_flood`/`_drumFlood`)에 `setSongMode(true)`,
건반(`_play`)·드럼패드(`_drumHit`)에 `setSongMode(false)`.

**같이 고친 것 — 목표를 올릴 때 한 번에 다 만들면 안 된다.**
`need` 상한을 `ahead*2` 에서 **1536프레임**으로 낮췄다. 목표가 3072→6144 로 뛰는 순간
차액을 한 번에 렌더하면 그 렌더가 길어져서 **그것 자체가 지체**가 된다(고치려던 문제를
그 자리에서 만드는 셈). 여러 바퀴에 걸쳐 조금씩 채운다.

### 실측 (트랩 전체곡, 200초 창, 폰 무조작)
| | 전 | 후 |
|---|---|---|
| `dumpsys` 언더런 | 6018 / 207s = **29/초** | 1502 / 200s = **7.5/초** |

**4배 줄었다. 0 은 아니다.** 전체 샘플의 0.016% 수준.

> 화면 계기(`급식 지체`·`큐 최소 잔량`)는 측정 직후 폰 화면이 잠겨서 못 읽었다.
> 다음 세션이 그 두 값을 확인할 것 — 큐 최소 잔량이 500 이상이면 목표 달성이다.

### 다음 세션이 할 것
1. **화면 계기 확인** — `급식 지체`(20ms 초과 횟수)와 `큐 최소 잔량`. 곡 재생 중에 읽을 것.
   큐 최소가 아직 0 근처면 `kAheadSong` 을 8192(170ms)까지 올려 본다(곡 재생엔 무해하다).
2. 그래도 안 잡히면 **오디오 생성을 네이티브(C/C++ 또는 Kotlin)로** — Dart 아이솔레이트는
   실시간 우선순위를 못 받는다는 게 이번에 확인된 구조적 한계다. 마지막 수단.
3. 5단계 UI 이식(6~8세션, 가장 큼) · `EXTRA_TRACKS` · 시퀀서 UI.

### 청감 미확인 (쌓여 있는 것)
- 재즈 · 엠비언트 전체곡 (이번에 추가)
- 3단계 컴프레서 · 리버브 · 하이패스 (EQ 는 확인됨)
- GENRE_MIX 슬롯별 세부(팬·EQ가 곡 안에서 의도대로 들리는가)

---

## 4단계 10/N — **언더런 0 달성** (2026-08-14 새벽, A17 실측)

### 마지막 두 조각
큐를 두껍게 해도(6144) 언더런이 초당 7.5회 남았다. 그런데 **큐 최소 잔량은 1898프레임**
— 큐는 안 비었는데 AudioTrack 이 빈 것이다. 즉 남은 원인은 Dart 쪽이 아니라
**플러그인의 쓰기 스레드(Java)** 였다. 두 가지를 고쳤다(`packages/flutter_pcm_sound`):

1. **쓰기 스레드 우선순위** `THREAD_PRIORITY_AUDIO`(-16) → **`URGENT_AUDIO`(-19)**.
   이 스레드가 늦게 깨면 큐에 데이터가 있어도 AudioTrack 이 빈다.
2. **AudioTrack 버퍼** 버스트 4개(1024프레임) → **8개(2048프레임)**.
   **총 지연은 안 늘어난다** — `ahead` 는 큐 + 장치를 합친 값이라, 장치 버퍼가 커지면
   같은 총량 중 더 많은 몫이 장치 안에 있을 뿐이다. 대신 쓰기 스레드가 늦게 깨도
   버틸 여유가 두 배가 된다.

### 최종 실측 (트랩 전체곡 200초, 폰 무조작)
| | 값 |
|---|---|
| **`dumpsys` 언더런** | **0회** (증가 0) |
| 큐 최소 잔량 | 1974프레임 (안 빔) |
| 하드클립 · 음부족 · 드럼부족 | 0 · 0 · 0 |
| 최대 동시음 / 드럼 | 22 / 7 (스모크 예측과 일치 = 곡이 끝까지 재생됨) |
| 오디오 여유 | 93% |
| 급식 지체 | 최대 54ms · 20ms 초과 **614회** ← **여전하다** |

### 여기서 배운 것 — 지체는 없앨 수 없다. 흡수하는 것이다
급식 지체는 오히려 늘었는데(124회 → 614회) 언더런은 0 이 됐다.
**Dart 아이솔레이트는 실시간 우선순위를 못 받는다** — 이건 고칠 수 없는 구조적 사실이다.
대신 버퍼 두 겹(Dart 큐 6144 + AudioTrack 2048)이 그 지체를 전부 흡수한다.
지체 자체를 쫓지 말고 **흡수 용량**을 보는 게 맞다.

### 전체 경과
| 단계 | 언더런 |
|---|---|
| UI 스레드에서 급식 | 초당 53회 (+ 버퍼 바닥 538회) |
| 백그라운드 아이솔레이트 | 초당 29회 |
| + 예약 큐 O(n) 제거 · Env 재사용 | 초당 29회(같은 창 기준) |
| + 상황별 버퍼(곡 6144) | 초당 7.5회 |
| **+ 쓰기 스레드 URGENT · 장치 버퍼 2배** | **0회** |

### 지연은 어떻게 됐나
- 곡 재생: 6144프레임 = 128ms (문제 없음 — 시작만 늦다)
- 건반: 3072프레임 = 64ms (화면에서 조절 가능)
- 장치 버퍼가 2048로 커지면서 FAST 플래그(0x001)는 빠졌다. 저지연 경로를 못 타는데,
  **총 지연은 `ahead` 가 정하므로 건반 지연은 그대로다.** 더 짧게 하고 싶으면
  건반 모드에서만 장치 버퍼를 줄이는 방법이 있으나, setup 시점에 정해지는 값이라
  재생 중 못 바꾼다 — 필요해지면 그때 다루자.

### 다음
1. **5단계 UI 이식** (6~8세션, 가장 큼) — 이제 소리 쪽은 다 됐다.
2. `EXTRA_TRACKS`("꽉 채워서") · 시퀀서 UI(정지/일시정지/진행 표시) · 조·템포 UI
3. 청감 미확인: 재즈 · 엠비언트 전체곡 / 컴프 · 리버브 · 하이패스 / GENRE_MIX 세부

---

## 4단계 11/N — 20밴드 EQ 조작이 느리던 것 (2026-08-14, 사용자 제보)

### 원인은 EQ 계산이 아니라 **화면 재빌드**였다
`mixer.dart` `_updateFilters()` 는 이미 **값이 바뀐 밴드만** 다시 만든다
(`if (g[i] == _lastEq20[i]) continue;`). 오디오 쪽은 문제가 없었다.

진짜 원인: 슬라이더 `onChanged` 가 부모(HomePage)의 `setState` 를 불렀다.
그러면 슬라이더를 끄는 **내내(초당 60회) 화면 전체가 다시 그려진다** —
악기 칩 33개 + 드럼 패턴 버튼 11개 + 화성 버튼 11개 + 곡 버튼 11개 +
20밴드 슬라이더 20개 + 마스터 슬라이더들 + 건반 26키.

→ `_Eq20Band` 를 **StatefulWidget** 으로 분리해 자기 값을 스스로 들게 했다.
드래그 중엔 **그 슬라이더 하나만** 다시 그린다. 값은 `onChanged` 로 흘려보내
`host.eq20[i]` 에 쓰고 오디오 아이솔레이트로 보낸다(이 경로는 그대로다).

`didUpdateWidget` 으로 바깥 변경(예: '믹스 초기화')은 따라가되, 드래그 중에는
`initial` 이 안 바뀌므로 되감기지 않는다.

### 같은 병이 남아 있는 곳 (아직 안 고침)
`_HomePageState` 는 **지표 스트림(250ms)과 FPS 카운터(400ms)에서도 `setState` 를 부른다** —
가만히 둬도 초당 6~7회 화면 전체를 다시 그린다. 지금은 사용자가 "나머진 괜찮다"고 해서
안 건드렸지만, **5단계 UI 를 만들 때는 처음부터 이렇게 짜면 안 된다**:
자주 바뀌는 부분(지표·재생 위치·레벨미터)은 반드시 자기 위젯으로 떼어 낼 것.
안 그러면 타임라인·믹서처럼 위젯이 수백 개인 화면에서 그대로 터진다.

### 확인
`flutter analyze lib/` 오류 0 · 빌드·설치 완료. **체감은 사용자 확인 필요**
(20밴드 모드에서 슬라이더를 끌어 보고 따라오는지).

---

## 4단계 12/N — 재즈·엠비언트가 "구리다" 진단 (2026-08-14, 사용자 제보)

사용자: **컴프·리버브·하이패스 ✅ 괜찮다** (3단계 믹서 전 항목 청감 확인 완료).
**재즈·엠비언트 전체곡은 "곡이 너무 구리다".**

### 이식 버그는 아니다 — 세 가지를 확인했다
1. **7화음이 실제로 반영된다** — 패턴 데이터에 5번째 값(`'m7b5'`/`'dom7'`/`'min7'`/`'maj7'`/
   `'min9'`)이 있고 `patterns.dart:277` 이 그걸 읽어 `ChordSpec.type` 으로 넣는다.
2. **화성 충돌 없음** — `tool/harmony_check.dart`(신규)로 다섯 곡 전부 검사.
   긴 음(4분 이상)이 화음과 반음으로 부딪히는 경우: trap 1 · proghouse 0 · pop 0 ·
   **jazz 0** · ambient 0. (trap 마디58 벨 G 는 따로 볼 것)
3. 길이·슬롯 사용은 이미 스모크로 확인됨.

> **검사 도구를 만들 때 두 번 틀렸다 — 다음 사람은 그대로 밟지 말 것:**
> ① `log2` 를 직접 테일러 근사로 짰다가 피치클래스가 통째로 어긋났다. `math.log(x)/math.ln2` 를 쓸 것.
> ② 워킹 베이스의 **반음 접근음을 전부 오류로 셌다**(116개). 그건 재즈 문법이지 실수가 아니다.
> 베이스는 **첫 박만** 엄격하게 봐야 한다(웹에서도 똑같이 밟았던 함정).

### 진짜 원인 ① 재즈에 **스윙이 없었다** — 고쳤다
`swing` 이 **웹 원본에도 없다**(패턴 이름 'Ride Swing' 하나뿐, 타이밍 처리는 없음).
즉 재즈를 **정박으로** 치고 있었다. 화성이 아무리 맞아도 그러면 재즈로 안 들린다.

→ `song.dart` 에 `swingStep()` + `kSwing` 추가. 한 박(16분 4스텝) 안에서 **뒷 8분(스텝 2)만**
뒤로 민다. `jazz: 0.62`(재즈 셔플). 16분(스텝 1·3)은 안 건드린다 — 건드리면 비틀거린다.
마디 시작(`barSteps`)에는 안 적용한다 — 그러면 박자가 통째로 밀린다.
검증: 스텝 0→0.00 · 1→1.00 · **2→2.48** · 3→3.00 · 4→4.00 · **6→6.48**. 트랩 길이 불변(205.7s).

**이건 이식이 아니라 새로 넣은 것이다.** 웹 버전을 계속 쓸 거면 거기에도 같은 게 필요하다.

### 진짜 원인 ② 엠비언트는 **너무 비어 있다** — 아직 안 고쳤다
3분 33초에 멜로디성 음이 **156개뿐**이다(재즈 708, 트랩 1402). 패드 32 · 스트링 24 ·
서브 28 · 벨 96 · 하프 160 · 퍼커션 28. 곡 설계 자체가 "화성이 아니라 질감"이라 의도된
희소함이긴 한데, **신스 패드가 시간에 따라 변하지 않으면 그냥 비어 들린다.**
웹에서도 같았을 것이다(데이터가 같으므로).

고치려면 셋 중 하나이고, **어느 쪽인지는 사용자가 정해야 한다**:
- (a) 편곡을 더 채운다(패드 코드 변화·벨 프레이즈 추가) — 곡 데이터 작업
- (b) 패드 음색이 시간에 따라 움직이게 한다(필터 LFO·느린 스윕) — 신스 작업
- (c) 리버브/딜레이를 더 깊게 — 믹스 작업

### 다음
- 재즈: 스윙 넣은 버전 **청감 확인 필요**(설치 완료)
- 엠비언트: 위 (a)/(b)/(c) 중 방향 정하기
- 5단계 UI 이식

---

## 4단계 13/N — 엠비언트 (b) 필터 움직임 + (c) 리버브 심화 (2026-08-14, 사용자 지시)

### (b) 트랙 버스에 느린 로우패스 움직임 — `TrackMix.lfoHz` / `lfoDepth`
**음마다가 아니라 버스에 걸었다.** 이유: 음마다 걸려면 `noteOn`→배치 메시지→`SynthNote`
까지 전부 인자를 늘려야 하는데, 패드 전체가 같이 숨쉬는 게 목적이라 버스 하나면 충분하다.
실제 믹스에서도 이건 버스에 거는 처리다.

컷오프는 **64샘플(1.3ms)마다만** 다시 잡는다 — 이렇게 느린 움직임에는 그 간격으로 충분하고,
매 샘플 계수를 구하면 비싸다.

**`lpfFreq` 를 같이 내려야 한다.** 20000(기본, 열림)에서는 아무리 흔들어도 안 들린다.
→ pad `lpf 2400 · 0.071Hz(14초 주기) · ±45%`, str `lpf 3200 · 0.050Hz(20초) · ±34%`.
주기를 서로 다르게 둔 건 의도적이다 — 같으면 둘이 같이 숨쉬어서 맥놀이처럼 뭉쳐 들린다.

### (c) 리버브 심화
pad 0.44→**0.62** · str 0.48→**0.66** · bell 0.42→**0.58** · harp 0.40→**0.56** · perc 0.32→**0.44**.
**서브(sub)는 0.05 그대로** — 저음에 리버브를 먹이면 곡 전체가 흐려진다.

### 새 나가지 않게 한 것
`resetGenreMix()` 가 `lfoHz`/`lfoDepth` 도 0 으로 되돌린다. 안 그러면 엠비언트를 한 번 튼 뒤
**트랩·팝의 패드까지 계속 움직인다**(장르를 바꿔도 버스 객체는 재사용되므로).
`tool/ambient_check.dart` 2번이 그걸 검사한다.

### 검증 — 여기서 또 측정을 틀렸다
처음엔 "RMS 변동폭이 커지는가"로 봤는데 **켬 0.00857 < 끔 0.01002** 로 나와서
"안 움직인다"는 결론이 나왔다. 틀린 측정이었다 — 패드의 **음량 엔벨로프**가 RMS 변동을
지배해서 필터 효과가 묻힌다.
→ **켬/끔을 창별로 나눈 비율**로 바꿨다. 나누면 엔벨로프가 약분되고 필터 효과만 남는다.
결과: 창별 비율 0.865~0.973, **변동 10.8%** — 실제로 움직인다.
(엠비언트에 맞게 '은은하게' 움직이는 정도다. 더 극적으로 하려면 `lfoDepth` 를 올린다)

> **교훈: 무언가 "안 바뀐다"고 결론 내리기 전에 측정이 그걸 볼 수 있는지부터 의심할 것.**
> 이 세션에서만 두 번 밟았다(워킹 베이스 오탐, 그리고 이것).

### 확인
`flutter analyze lib/` 오류 0 · 기존 스모크 전부 통과(회귀 없음) · 빌드·설치 완료.
**엠비언트 청감 확인 필요** — 패드가 천천히 열렸다 닫히는지, 공간이 더 넓어졌는지.

---

## 5단계 1/N — 믹서 (2026-08-15)

웹 `renderMixer()`(app.html 6444줄~) 이식. 구성: 트랙 스트립 → ＋악기 → **라이브** → **마스터**.

### 가로 모드를 통째로 다시 짰다 (사용자: "재정신으로 만든거니?")
1차는 세로·가로 모두 "세로 페이더 스트립"이었다. **가로에서 완전히 망가져 있었다:**
- 이 폰 가로 화면은 높이가 **384dp**(앱바·상태바 빼면 300dp). 세로 페이더에 남는 높이가
  사실상 0 이라 **손잡이 동그라미만 찍히고 트랙이 안 보였다** — 볼륨을 못 끄는 믹서였다.
- 폭 832dp 를 106dp 스트립으로만 써서 **오른쪽 절반이 통째로 비어 있었다.**

→ **가로 = 채널당 한 줄(가로 페이더)** 로 재작성. `[색막대][이름/음색][M][S][볼륨][좌우]
[잔향][저음][고음][삭제]` 가 폭을 전부 쓴다. 한 화면에 4~5채널.
→ **세로 = 스트립 유지**(그게 믹서의 모양이다). 대신 페이더 길이를 `(높이-398).clamp(110,260)`
으로 **못 박고**(예전엔 `Flexible` 이라 남는 높이 400dp 를 전부 먹었다), 남는 높이는
좌우·잔향·저음·고음 **네 개가 `Expanded` 로 나눠 갖는다**. 한 군데 큰 구멍 대신 고르게 넓어진다.

> 라벨·값을 양끝 정렬(`spaceBetween`)로 두면 안 된다 — 값(100%)이 **옆 칸 라벨(좌우)** 바로
> 옆에 붙어서 어느 슬라이더 값인지 헷갈린다. 라벨+값을 붙여서 왼쪽에 두는 게 맞다.

### 라이브 채널 — 엔진에 **전용 버스**를 새로 팠다
화면만 만들면 안 됐다. 건반 음은 `kPartMelody` 로 나가고 있어서, **멜로디 트랙 페이더를
내리면 내가 치는 소리까지 같이 작아졌다.** 웹도 라이브 채널을 따로 뒀다.

- `synth.dart` `kPartLive = -2` (슬롯 인덱스가 아니라 표시값이라 음수)
- `mixer.dart` `TrackMixSet.live` — **`configure()` 도 `resetGenreMix()` 도 안 건드린다.**
  곡을 틀어 편성이 바뀌어도 손으로 맞춘 건반 볼륨은 남아야 한다.
- `engine.dart` 렌더 루프에서 `part == kPartLive` 면 별도 합산 → 슬롯 뒤·드럼 앞에 더한다.
  건반을 안 치면 합이 **정확히 0** 이라 기존 곡 렌더값이 한 비트도 안 바뀐다(스모크 유지).
- `AudioClient.noteOn()` 의 `part` 기본값이 `kPartLive`. 화음·아르페지오도 8번째 칸에 붙였다.
  (훑기·부하시험은 진단용이라 멜로디 그대로 뒀다)
- 딜레이는 **안 넣었다** — 엔진에 딜레이가 아직 없다(리버브만). 노브만 달면 만져도 아무
  일이 안 일어난다.

### 마스터 페이더
`Engine.userGain`(신규) × `masterGain`(고정 헤드룸 0.55). 1.0 이면 0.55×1.0 = 0.55 라
값이 한 비트도 안 바뀐다. `_cMasterVol = 11`. 표시는 웹과 같이 dB.
세로에서는 **오른쪽 붙박이**, 가로에서는 **아래 붙박이** — "맨 옆에 마스터"(사용자 지시).

### 상태 (`project.dart`)
`LiveChannel`(voice/vol/rev) · `MasterChannel`(vol) 추가. 둘 다 `ChangeNotifier` 라
스트립 하나만 다시 그린다. `main.dart` 의 `voice` 는 `live.voice` 로 가는 통로가 됐다
(건반 악기와 믹서의 라이브 악기가 **같은 값**이다).

### 검증 — `tool/live_bus_check.dart` (신규)
1) 라이브 페이더 0 → RMS 0.000000 · 2) **멜로디 페이더 0 이어도 라이브는 100.0%** ·
3) 라이브 0 이어도 곡 소리 그대로 · 4) `configure()` 뒤에도 live.vol 유지 · 5) 마스터 0.5 → 50.0%

> **또 측정에서 한 번 속았다.** 2)가 처음엔 93.9% 로 나와 "실패"였다. 라우팅이 아니라
> `noteOn()` 의 사람다움(humanization)이 **전역 공유 RNG** 를 써서 엔진마다 파형이 달라진
> 탓이었다. `Human.setLevel(0)` 으로 끄니 정확히 100.0%/50.0%.
> `genremix_smoke.dart` 는 이걸 몰라서 "방향만 본다"로 우회해 뒀다 — 거기도 같은 수를 쓸 수 있다.

기존 스모크 전부 통과(회귀 없음): objectsong(트랩 207.7s 피크 0.969) · genremix · mixer · song.
`flutter analyze lib/` 오류 0 · 빌드·설치 완료 · **폰에서 세로/가로 둘 다 눈으로 확인**.

### 남은 것
- 라이브 페이더가 실제로 건반 소리를 줄이는지 **청감 확인 필요**(계산으로는 확인됨)
- 화면: 씬 · 타임라인 · 편집기 · 라이브 · 쇼 (타임라인·편집기가 제일 크다)
- 트랙 이름 바꾸기 · 음색 바꾸기 · 순서 이동(◀▶) — 웹에는 있고 여기엔 아직 없다
- 마스터 아날라이저(웹의 실시간 주파수 막대) — 지표 스트림처럼 자기 위젯으로 떼서 만들 것

---

## 5단계 2/N — 씬 시퀀서: **프로젝트 트랙이 실제로 연주된다** (2026-08-15)

### 왜 이걸 먼저 했나
믹서까지 만들어 놓고 보니 **프로젝트 트랙은 소리와 아무 연결이 없었다.** 곡 재생은
`song.dart` 에 박힌 데모곡만 돌았고, 트랙의 음색·패턴은 화면 글씨였다. 이 상태로 씬·
타임라인·편집기를 그리면 **전부 죽은 화면**이 된다. 그래서 연결부터 만들었다.

  트랙 목록 → 타입별 패턴(patterns.dart) → 음/타격 → 엔진 예약(batch/drumBatch)

### 트랙마다 자기 버스 (`configureBuses`, `_cSlots = 12`)
드럼 아닌 트랙은 **버스 이름 = 트랙 id**. 그래야 멜로디 트랙을 두 개 만들어도 페이더가
따로 논다(전에는 타입이 곧 버스라 같은 타입끼리 한 버스를 썼다).
드럼은 엔진에 버스가 하나뿐(`TrackMixSet.drum`)이라 드럼 트랙 여럿은 그 하나를 같이 쓴다.

> **버스 이름 규칙은 `SceneSequencer.busOf` 한 곳에서만 정한다.** 믹서와 시퀀서가 각자
> 정하면 어긋나는 순간 페이더가 엉뚱한 트랙을 만진다.

> **`configure()` 뒤에는 반드시 `pushMix()`.** configure 는 버스를 새 객체(투명)로 다시
> 만들기 때문에, 안 부르면 ▶를 누를 때마다 믹서 설정이 초기화된 것처럼 들린다.
> `play()` 순서: `setGenreMix(null)` → `configureBuses` → `pushMix` → 예약.

**데모곡을 틀면 그 곡이 `setGenreMix()` 로 편성을 덮어쓴다.** 그래서 `main.dart` 에
`_syncBuses()` 를 두고 (a) 부팅 (b) 트랙 추가·삭제 (c) 씬 화면에서 돌아올 때 다시 잡는다.

### 지금은 '예약형' 재생이다
▶를 누르면 `reps`(×4/×8/×16) 번 반복할 분량을 **한꺼번에** 예약 큐에 넣는다(데모곡과 같은 길).
재생 중에 패턴을 바꾸면 이미 예약된 것은 안 바뀐다 — 다시 ▶를 눌러야 한다.
진짜 무한 루프(다음 마디부터 반영)는 UI 가 재생 위치를 알아야 해서 다음 덩어리로 미뤘다.

루프 길이 = 가장 긴 패턴. 짧은 패턴은 그 안에서 자기 길이만큼 반복한다(2마디+4마디 = 두 번).

### 같이 살아난 것
- **음색·키트가 진짜로 들린다.** `Track.voice` 는 이제 연주에 쓰이는 값이다.
- 드럼은 음색표가 아니라 **키트**를 쓴다 → `Track.kit` 을 따로 뒀다. `voice` 에 키트를
  넣으면 트랙 이름 자동짓기가 '어쿠스틱' 이 돼 버린다.
- 트랙 이름 바꾸기(씬 화면에서 이름 탭).
- `Track` 기본 이름이 드럼일 때 'drum' 으로 찍히던 것 수정(`kTrackTypeLabel` 로 대체).

### 화면 (`lib/ui/scene_view.dart`)
위: ▶재생/■정지 · 템포 · 조(12키, 옆으로 넘김) · 장/단조 · 반복.
아래: 트랙 한 줄 = 이름(탭=이름 바꾸기) · 음색/키트(탭=고르기) · 패턴(탭=고르기) · 쉬기.

> 1차엔 장/단조·반복 칩을 조 12개와 **같은 가로 목록**에 넣었더니 화면 밖으로 밀려서
> 안 보였다. 조만 넘기고 나머지는 안 밀리는 줄에 따로 뒀다.

> `FilledButton` 은 `foregroundColor` 를 안 잡으면 테마 기본색(보라)이라 어두운 바탕에서
> 거의 안 읽힌다. 시험 화면의 `_btn`(화음·아르페지오·전체 훑기)도 같은 병이라 같이 고쳤다.

### 검증
`flutter test test/scene_check_test.dart` — 7항목 전부 통과:
네 트랙 연주(타격 154 · 버스별 음 0:22 1:42 2:16) · 버스 배정 · 2마디 패턴이 4마디 루프를
채움(16→64) · 템포 두 배=길이 절반 · 뮤트 제외 · **실제 렌더 RMS 0.1820 하드클립 0
버퍼바닥 0** · 조 바꾸면 5.00반음 이동.

> **이 검사만 `dart run` 이 아니라 `flutter test` 다.** `project.dart` 가 `ChangeNotifier`
> (package:flutter)를 쓰기 때문에 순수 Dart VM 으로는 안 돌아간다(dart:ui 를 못 찾는다).
> 엔진·패턴·믹서만 건드리는 검사는 계속 `tool/` 에 두고 `dart run` 으로 돌린다.

**폰 실측(A17):** ▶ 누르면 「4마디 × 8번 · 음 320개 · 타격 616개 · 83초」, 재생 30초 동안
`dumpsys media.audio_flinger` 우리 트랙 **Underruns=0**. 기존 스모크 전부 통과(회귀 없음).

### 남은 것
- 무한 루프 재생 + 재생 위치 표시(마디/박) — 그게 있어야 "재생 중 패턴 교체"가 된다
- 씬 여러 개(지금은 하나) · 클립 · 타임라인 · 편집기
- 트랙 순서 이동(◀▶) — 웹에 있고 아직 없다
- 드럼 트랙 여러 개는 버스를 같이 쓴다(엔진에 드럼 버스가 하나뿐)

---

## 5단계 3/N — 무한 반복 + 재생 위치 (2026-08-15)

### 루프는 **오디오 아이솔레이트**가 돈다 (`LoopState`)
UI 타이머로 다음 판을 예약하면 시계가 어긋나 판마다 조금씩 밀리고, 그 밀림이 쌓인다.
그래서 한 판 분량의 프로그램만 넘겨 주고(`setLoop`), **엔진 시계(`Engine.nowFrames`)**
기준으로 아이솔레이트가 알아서 다음 판을 예약한다(`topUp`).

예약은 렌더보다 **앞서** 들어가야 한다(`guard = ahead + 0.25초`). 렌더가 지나간 자리에
넣으면 그 판은 통째로 안 들린다.

> **아이솔레이트 안에 있던 코드를 `LoopState` 클래스로 뺐다.** 그래야 시험이 **실물**을
> 돌린다. 베낀 코드를 시험하면 통과해도 실물이 맞는지는 모른다.

### 돌고 있는 중에 갈아 끼우기
`setLoop(..., restart: false)` 는 **박자를 안 건드리고 내용만** 바꾼다 → 다음 판부터
새 패턴·새 음색이 나온다(DAW 에서 클립 바꾸는 느낌). 씬 화면은 패턴·음색·키트를 고르거나
템포·조를 바꾸면 이걸 부른다. 템포/조 슬라이더는 **250ms 디바운스** — 안 걸면 드래그하는
내내 씬을 새로 만들어 보낸다.

### 재생 위치
`AudioStats` 에 `looping` · `loopPos`(0~1) · `loopCount` 추가.
**귀에 들리는 지점** 기준이다(`nowFrames - buffered`) — 만들어 둔 앞부분을 빼야 화면과
소리가 맞는다. 예약이 앞질러 가 있어 `head < startAt` 인 구간이 생기는데, 그때는 한 판
뒤로 돌려서 센다(`posOf`).

화면(`_PlayHead`)은 **자기 위젯**이고, 250ms 지표 사이를 화면 주사율로 이어서 그린다
(그냥 그리면 초당 4번 툭툭 끊긴다). 트랙 줄은 이것 때문에 다시 안 그려진다.

### 검증 — `flutter test test/loop_check_test.dart`
1) 20판 · 판당 오차 최대 4프레임 · **누적 오차 0프레임(0.00ms)**
2) 렌더 덩어리를 64~4096 으로 흔들어도 누적 2프레임
3) 재생 중 교체해도 킥 간격 유지(오차 2프레임) 4) 위치 0→1 · 되감기 5) 정지

> **또 측정에서 틀렸다(이번이 세 번째다).** 처음엔 "샘플값이 문턱을 넘는 순간"을 타격으로
> 셌더니 20판에 **408개**가 잡혔다. 킥은 저음이라 한 번 치는 동안 파형이 0을 수십 번
> 지나간다. → 짧은 창의 최대값으로 **포락선**을 만들고 0.2초 쉬는 방식으로 고쳤다.
>
> 그리고 판당 오차가 0이 안 나온다고 실패로 보면 안 된다 — `drums.dart` 는 **씨앗 없는
> 난수**로 타격마다 레이어 간격을 흔든다(소리의 개성). 그래서 문턱을 넘는 지점이 몇 샘플씩
> 다르다. **밀림이 진짜라면 누적 오차가 판 수에 비례해 커진다** — 그걸 봐야 한다.

**폰 실측(A17):** 「한 판 4마디 · 10.4초 · 음 40개 · 타격 77개 · 무한 반복」,
**2분 넘게 재생 · Underruns=0**, 재생 중 드럼 패턴 교체(Lofi Chorus→Boom Bap, 타격 77→52)
해도 안 끊기고 위치도 계속 돌았다.

### 남은 것
- 씬 여러 개 · 클립 · 타임라인 · 편집기
- 트랙 순서 이동(◀▶)
- 마디 수를 트랙마다 다르게(지금은 가장 긴 패턴에 맞춰 짧은 것이 반복)

---

## 5단계 4/N — 씬 (장르 한 벌 갈아입기) (2026-08-15)

### 웹의 격자를 그대로 옮기지 않았다
웹 씬 뷰는 **트랙×씬 격자**에 클립을 놓고 씬을 발사한다. 이 폰 세로 화면(384dp)에서
트랙 4 × 씬 4 격자를 그리면 칸이 40dp 도 안 나온다 — 이름도 못 읽는다.
→ **씬은 칩으로 골라 타고, "지금 씬"만 트랙 목록으로 편다.** 개념은 같고 배치만 다르다.

### 씬이 들고 있는 것: 클립 + **템포 + 드럼 키트**
웹과 다른 점이다. 장르가 통째로 바뀔 때 템포와 키트가 같이 안 바뀌면 그 장르로 안 들린다
(로파이 78 · lofi 킷 → 하우스 124 · 909 킷). 그래서 `Scene {name, bpm, kit, clips}`.

`clips` 는 **트랙 id → 패턴 이름**. 값이 없으면 그 씬에서 그 트랙은 쉰다.

> **패턴을 고르면 트랙과 씬 양쪽에 쓴다**(`Project.setClip`). 트랙에만 쓰면 씬을 갔다
> 오는 순간 방금 고른 게 사라진다.
> **트랙을 추가하면 모든 씬에** 기본 패턴으로 넣는다. 안 그러면 새 트랙이 다른 씬에서만
> 조용해서 고장처럼 보인다.

### 빈 씬으로 시작하지 않는다
웹은 빈 씬으로 시작하지만 여기서는 **씬마다 장르 프리셋을 채워 둔다**(로파이·하우스·힙합·
시티팝). 빈 씬을 누르면 아무 소리도 안 나서 고장 같고, 무엇보다 이 앱에서 씬이 재미있는
이유가 **탭 한 번에 밴드가 장르를 갈아입는 것**이라서다. `＋` 는 다음 프리셋을 가져오고,
11개를 다 쓰면 지금 씬을 복제한다. 칩을 길게 누르면 이름 바꾸기·복제·삭제.

프리셋 표(`kTestSongPatterns`)는 시험 화면 안에 있던 걸 **`lib/presets.dart` 로 옮겼다** —
씬도 같은 표를 쓴다. 두 군데 두면 한쪽만 고치는 날이 온다.

### 템포가 바뀔 때 판 길이
`set(restart:false)` 는 `frames` 만 바꾸고 `nextAt` 은 안 건드린다. 그래서 **이미 예약된
판은 옛 길이로 끝까지 가고, 그 경계에서 새 길이가 시작된다** — 빈 구간도, 겹침도 없다.

### 검증
`flutter test` — 씬 시퀀서 11항목 + 루프 5항목 전부 통과. 새로 넣은 것:
8) 씬 전환(패턴 한 벌 + 키트 + 124BPM) · 9) 클립이 그 씬에 남는가(갔다 와도) ·
10) 새 트랙이 모든 씬에 · 11) 삭제해도 마지막 하나는 남는가.

**폰 실측(A17):** 재생 중에 「하우스」 칩을 누르니 안 끊기고 다음 판부터
92→**124BPM** · 키트 로파이→**909** · 네 패턴 전부 House 로 바뀌었다(한 판 10.4초→7.7초).

### 남은 것
- 클립 · 타임라인 · 편집기 (씬을 시간축에 늘어놓는 것)
- 트랙 순서 이동(◀▶)
- 씬마다 조(key)도 다르게 할지 — 지금은 조만 전역이다

---

## 5단계 5/N — 곡(타임라인): 씬을 늘어놓아 곡 만들기 (2026-08-15)

### 구간 = (어느 씬, 몇 **판**)
길이 단위를 마디가 아니라 **판(loop)** 으로 잡았다. 씬마다 패턴 길이가 다른데(2마디짜리도
4마디짜리도 있다) 마디로 자르면 패턴이 중간에 잘려 어색해진다. 판이면 프레이즈가 온전하다.

### 재생은 **루프 장치를 그대로 재활용**한다
곡 전체를 **한 판**으로 보고 `setLoop(notes, drums, 곡길이)` 에 넣는다. 그러면
"끝나고 처음부터 다시"까지 공짜로 얻는다(아이솔레이트가 엔진 시계로 이어 붙인다).
새 재생 경로를 따로 만들지 않았다 — 이미 밀림 0으로 검증된 길을 그대로 쓴 것이다.

### `build()` 를 씬에서도 만들 수 있게 열었다
`build(p, tr, {reps, from, bpm, at})`:
- `from` — **트랙에 실린 값 대신 그 씬의 클립**으로 만든다.
  **곡을 만들 때 트랙을 건드리면 안 된다** — 화면의 '지금 씬'이 곡 재생 때문에 바뀌면
  사용자가 방금 고르던 게 사라진다. 그래서 트랙을 안 거치고 씬에서 바로 읽는다.
- `bpm`·`at` — 구간마다 템포가 다르므로 **시각을 초로 누적**한다(마디로 세면 경계에서 어긋난다).

### 검증
`flutter test` — 씬 15항목 + 루프 5항목 통과. 새로 넣은 것:
12) 곡 길이 = 구간 길이 합(86.5초 · 음 352 · 타격 562) · 13) 구간이 시각 순서대로 ·
14) **곡을 만들어도 지금 씬(트랙에 실린 값)은 안 바뀐다** · 15) 씬을 지우면 그 씬을 쓰던
구간이 정리되고 남은 구간의 씬 번호가 당겨진다.

**폰 실측(A17):** 로파이2판→하우스2판→힙합2판→로파이2판 = 1:27.
재생 위치를 10초 간격으로 6번 찍어 0:10 → 0:42 → 1:03 으로 정확히 진행하는 것 확인,
구간 경계(0:25/0:40/1:02)에서 다음 칸으로 넘어갔다. **Underruns=0.**

> **한 번은 "위치가 멈췄다"고 오진할 뻔했다.** 65초 간격으로 두 번 찍었더니 같은 화면이
> 나왔는데, 다시 10초 간격으로 여러 번 찍어 보니 멀쩡히 움직였다. 앞의 것은 화면 캡처가
> 같은 프레임을 준 것이었다. **한 번 찍고 "멈췄다"고 판단하지 말 것** — 이 프로젝트에서
> 측정을 잘못한 게 이번이 네 번째다.

### 공용으로 뺀 것
`lib/ui/play_head.dart` `LoopPosBuilder` — 0.25초마다 오는 위치를 화면 주사율로 이어
그리는 부분. 씬 화면(마디/박)과 곡 화면(구간/시각)이 같은 걸 쓴다.

### 남은 것
- 편집기(패턴 안의 음을 직접 찍는 것) — 웹의 가장 큰 화면
- 라이브 · 쇼 화면
- 트랙 순서 이동(◀▶) · 씬마다 조(key) 다르게

---

## 5단계 6/N — 편집기: 음을 직접 찍는다 (2026-08-15)

### 라이브러리 패턴은 **못 고친다** → 들어오는 순간 복사
`patterns.dart` 의 표는 `const` 상수다(그래야 어디서 틀어도 같은 소리가 난다).
편집기에 들어오면 `Project.makeEditable(track)` 이 **내 패턴으로 복사**하고 트랙의 클립을
그 복사본으로 갈아 끼운다. 원본은 그대로 남는다 — 다른 씬이 쓰고 있을 수 있다.

**담는 그릇을 라이브러리와 같은 타입(`DrumPatternDef`/`NotePatternDef`)으로 잡았다.**
그래서 만드는 쪽(`buildDrumPattern`·`buildRowsPattern`·`buildChordPattern`)을 한 줄도
안 고쳤다. 찾을 때만 **내 패턴을 먼저** 본다(`Project.findDrum`/`findNote`/`barsOf`).

### 격자
가로 = 스텝(16분음표, 한 마디 16칸) · 세로 = 드럼이면 레인 10개, 아니면 **도수 0~14**
(`theory.dart degreeFreq` 가 0~14 로 자르는 것과 같은 범위 = 2옥타브+1). 아래가 낮은 음.
박(4칸)·마디(16칸)마다 칸 밝기를 달리했다 — 안 그러면 어디가 어딘지 못 찾는다.

같은 스텝에 도수를 여러 개 찍을 수 있다(화음). 기본 길이는 코드 7스텝, 나머지 2스텝.

> **격자와 재생 위치 세로줄은 같은 스크롤 안에 둬야 한다.** 처음엔 세로줄을 격자 밖에
> 뒀다가, 옆으로 밀면 엉뚱한 칸을 가리키는 상태가 됐다(4마디 = 64칸이라 반드시 민다).

### 찍으면서 듣는다
편집기 안에도 ▶가 있고, 한 칸 찍을 때마다 `refreshLoop` 로 **다음 판부터** 반영된다.
눈으로 찍고 귀로 확인하는 게 이 화면의 전부다.

### 검증
`flutter test` — 씬 19항목 + 루프 5항목 통과. 새로 넣은 것:
16) 편집 = 내 패턴으로 복사(원본 레인 6개 그대로) · 17) 고치면 실제 타격 수가 바뀜(77→16) ·
18) 짧은 내 패턴(1마디)이 4마디 루프를 채움(16타격) · 19) 목록에 내 패턴이 맨 앞.

**폰 실측(A17):** 드럼 편집기에 Lofi Chorus 가 그대로 그려지고(킥·스네어·하이햇·림·쉐이커),
베이스 편집기는 도수 0~14 격자에 Lofi Walk C 가 찍혀 있다. **재생 중에 칸 3개를 찍어도
안 끊기고 Underruns=0.**

### 남은 것
- 음 길이 조절(지금은 고정 2스텝) · 세기(vel) · 글라이드
- 라이브 · 쇼 화면
- 트랙 순서 이동(◀▶) · 저장/불러오기(지금은 앱을 끄면 다 사라진다)

---

## 5단계 7/N — 곡을 **송폼**으로 되돌림 (2026-08-15, 사용자 지적)

사용자: **"곡 설정 수대로 흐르지도 않고 다음번 숫자곡도 나오지 않아. 곡마다 송폼별로
흘러가야지 장르가 왜 달라지니."**

### 무엇이 틀렸나 — 설계가 틀렸다
5단계 4~5/N 에서 **씬 = 장르**로 잡고 곡을 로파이→하우스→힙합→로파이로 이어 붙였다.
그건 곡이 아니라 **짜깁기**다. 곡은 스타일 하나 안에서 **구간**(인트로·벌스·코러스·
브레이크·아웃트로)이 흘러야 한다.

**필요한 데이터는 이미 옮겨져 있었다** — `song.dart` 의 `kSongForms`(장르별 섹션 구성 +
섹션별 마디 수)와 `kFormOrder`(=[0,1,2,3,1,2,4] 인트로→벌스→코러스→브레이크→벌스→코러스
→아웃트로). 4단계에서 데모곡 재생용으로 옮겨 놓고, 5단계에서 안 쓰고 새로 지어냈다.
**이미 있는 것을 먼저 찾을 것.**

### 고친 것
- `Project.genre` + `setGenre(key)` — 씬을 그 스타일의 섹션으로 다시 채우고, 얼개도
  `kFormOrder` 로 다시 잡는다. 판 수는 **섹션 마디 수 ÷ 그 구간의 판 길이**(벌스 8마디 ÷
  4마디 = 2판). 결과: 로파이 총 44마디 = `tool/song_smoke.dart` 의 데모곡 길이와 정확히 일치.
- **템포·키트는 곡 전체에 하나.** 구간마다 안 바뀐다.
- 곡 화면에 **스타일 줄**(로파이 78 · 하우스 124 · 힙합 88 · 시티팝 106 · 발라드 68 · 록 138).
- 씬 추가는 이제 **지금 구간 복제**(다른 장르를 붙이는 건 말이 안 된다).
- 처음 보이는 구간을 **코러스**로 — 인트로는 드럼·베이스가 쉬는 구간이라 씬 화면에서
  ▶를 누르면 "왜 이렇게 허전하지"가 된다.

### "설정 수대로 안 흐른다" 의 진짜 원인
곡을 고쳤을 때 `setLoop(restart: false)` 를 썼다. 씬(한 판 10초)에서는 '다음 판부터'가
자연스럽지만 **곡은 한 바퀴가 2분**이다. 판 수를 바꿔도 2분 동안 옛 구성이 계속 흐른다.
→ 곡 화면에서는 **고치면 처음부터 다시** 튼다. 바꾼 게 바로 들린다.

### 검증
`flutter test` — 23항목 통과. 새로 넣은 것:
11-b) **곡 전체가 한 스타일**(템포 {78} · 키트 {lofi}) ·
11-c) 얼개가 송폼대로(인트로→벌스→코러스→브레이크→벌스→코러스→아웃트로 · 총 44마디) ·
11-d) 스타일 교체 시 구간 이름은 그대로 내용·템포만 바뀜(Lofi Chorus→House Chorus · 124BPM).

> 1~7번 검사가 갑자기 실패했는데 **검사가 낡은 것이었다** — 기본 구간이 코러스가 아니라
> 인트로였던 시점에 "네 트랙이 다 소리를 낸다"를 기대했다. 인트로는 드럼·베이스가 쉰다.

**렌더 실측(구간별 RMS):** 인트로 0.031 → 벌스 0.179 → 코러스 0.188 → 브레이크 0.074 →
벌스 0.179 → 코러스 0.188 → 아웃트로 0.031. **곡의 셈여림이 실제로 그려진다.**

**폰 실측(A17):** 전체 2:15. 재생 중 코러스를 2판→3판으로 올리니 **곧바로** 2:28 · 음
397→437 · 타격 588→665 로 바뀌고 처음부터 다시 흘렀다. 스타일을 힙합으로 바꾸니 전 구간이
88BPM · 2:00 으로 바뀌었다. **Underruns=0.**

### 남은 것
- 스타일을 바꾸면 손으로 고친 판 수는 초기화된다(섹션 마디 수가 스타일마다 달라서 다시 계산).
  지킬 방법이 필요하면 그때 논의.
- 객체형 롱폼(트랩·프로그하우스·팝·재즈·엠비언트)은 아직 스타일 목록에 없다 —
  `kObjectSongForms` 는 구조가 달라서 따로 이식해야 한다.
- 저장/불러오기 · 라이브 · 쇼 화면

---

## 5단계 8/N — 저장·불러오기 (2026-08-16)

편집기까지 만들어 놓고 저장이 없으면 만들 이유가 없다. 앱을 끄면 트랙·구간·곡·내가 찍은
패턴이 전부 사라지고 있었다.

### 버튼이 아니라 **자동 저장**
폰 앱에서 "저장 눌렀나?"를 신경 쓰게 하면 안 된다. 무언가 바뀌면 **0.8초 조용해질 때
한 번** 파일에 쓴다(슬라이더를 끄는 동안 초당 60번 쓰지 않으려고 모은다).
앱을 켜면 자동으로 되살린다 — **오디오를 켜기 전에** 불러와야 편성(`configureBuses`)을
되살린 트랙으로 잡는다.

`getApplicationDocumentsDirectory()/project.json` 하나. 사람이 읽을 수 있는 JSON 이라
나중에 내보내기/가져오기로 이어 붙이기 쉽다. `path_provider` 를 새로 넣었다(첫 외부 의존).

곡 화면 오른쪽 위 ⟳ = **처음부터 새로**(저장된 것도 지운다).

### 폰에서 잡은 진짜 버그 — 얼개는 저장이 안 됐다
스타일은 저장되는데 **구간 판 수는 앱을 껐다 켜면 되돌아갔다.**
원인: `Arrangement`(얼개)는 `Project` 가 아니라 **자기가 알리는 객체**인데 저장소가
거기 안 붙어 있었다. 화면은 `Listenable.merge([song, project, transport])` 로 둘 다
듣고 있어서 **눈에는 멀쩡히 바뀌어 보였다** — 그래서 화면만 보고는 못 잡는다.

> 교훈: `ChangeNotifier` 를 여러 개로 쪼개 놓으면(그게 성능상 옳다) **저장·동기화 쪽은
> 그 목록을 하나도 빠짐없이 다시 확인해야 한다.** 트랙도 같은 이유로 하나하나 건다
> (믹서 페이더는 `Track` 만 알린다).

### 검증
`flutter test` — 22항목 통과. 새로 넣은 것:
20) 저장→불러오기 **JSON 왕복이 글자까지 같은가** ·
21) **되살린 곡이 같은 소리를 내는가**(음 828 · 타격 448 · 89.0초 일치 — 글자만 같고
소리가 다르면 소용없다) · 22) 처음부터 새로.

**폰 실측(A17):** 스타일을 로파이로, 코러스를 4판(0:49)으로 바꾼 뒤 **강제 종료**(am
force-stop) → 다시 켜니 **로파이 78 · 코러스 4판 · 전체 2:40** 그대로 살아 있었다.

### 남은 것
- 프로젝트 여러 개(지금은 하나) · 내보내기/가져오기
- 객체형 롱폼 5장르(트랩·프로그하우스·팝·재즈·엠비언트)를 스타일 목록에
- 음 길이·세기 조절 · 라이브 · 쇼 화면

---

## 5단계 9/N — 곡 내보내기(WAV) + 공유 (2026-08-16, "수익성 측면에서 개선")

### 왜 이게 수익성 항목인가
앱 안에만 있는 음악은 **아무도 못 듣는다 = 아무도 이 앱을 모른다.** 만든 사람이 제일
먼저 하고 싶은 건 "들려주기"고, 그게 그대로 유입 경로다(카톡으로 받은 사람이 "이거 뭐로
만들었어?"). 나중에 유료화를 건다면 **워터마크 없는 내보내기 / 길이 제한 해제**가 가장
자연스러운 자리이기도 하다 — 만들기는 다 열어 두고, 꺼낼 때 값을 받는 구조.

### 오프라인 렌더 (`lib/export.dart`)
재생은 초당 48000샘플을 **제때** 만들어야 하지만, 파일로 뽑을 때는 **최대한 빨리** 만들면
된다. 같은 `Engine` 을 쉬지 않고 돌린다(시험에서 하던 그 방식 그대로).
- **전용 아이솔레이트**에서 돈다. UI 에서 돌리면 화면이 그 시간 내내 얼고, 오디오
  아이솔레이트에서 돌리면 재생이 끊긴다. 그래서 렌더용으로 하나 더 띄운다.
- 믹서 값(`SceneSequencer.mixSnapshot`)을 그대로 실어 보낸다 — **들리던 것과 같은 소리**로
  나와야 한다. 재생과 같은 규칙(`pushMix`)에서 뽑는다.
- 끝에 **꼬리 2초**를 더 렌더한다. 안 그러면 마지막 음·리버브가 잘린다.
- WAV(RIFF 44바이트 + PCM16). MP3/AAC 는 인코더(네이티브 의존)가 필요해서 뒤로 미뤘다.

### 진행률 — 이건 만들고 나서 폰에서 보고 고친 것
처음엔 `Isolate.run` + 동그라미만 돌렸다. 폰에서 2분 40초짜리가 **약 30초** 걸리는데,
그동안 숫자가 없으니 **멈춘 것처럼 보였다.** → `Isolate.spawn` + 포트로 중간 보고를
받아 **퍼센트로 채워지는 원**을 보여 준다(실측: 10초에 68%, 30초에 100%).
`Isolate.run` 은 돌려주는 값만 받을 수 있어서 진행률을 못 받는다.

### 검증 — `test/export_check_test.dart`
1) WAV 헤더 규격(48000Hz·2채널·16비트·RIFF 길이 필드 일치) · 2) 길이 = 곡 + 꼬리 2초 ·
3) **진짜 소리가 들어 있는가**(RMS 0.1467 · 피크 0.967 — 무음 파일 뽑아 놓고 성공했다고
하는 게 최악이다) · 4) **믹서가 반영되는가**(전부 뮤트 → 피크 0.0000) · 5) 클리핑 0개.
맥에서 38.9초 렌더에 1.03초(≈37배속).

**폰 실측(A17):** 2:40 곡 → 약 30초 만에 `음악낙서장_로파이.wav` 생성, **공유 시트에
카카오톡·Gmail·Drive·퀵셰어가 그대로 뜬다.**

### 다음에 수익화를 붙인다면 (지금 구조에서 자연스러운 순서)
1. **프로젝트 여러 개** — 무료 1~2곡, 유료 무제한. 저장 구조(`project.json` 하나)를
   폴더로 늘리면 된다.
2. **스타일 팩** — 지금 6개(배열형). 트랩·프로그하우스·팝·재즈·엠비언트는 `kObjectSongForms`
   에 이미 데이터가 있다(구조가 달라 이식 필요). 스타일 추가가 곧 상품이 된다.
3. **내보내기 품질** — 무료 WAV/길이 제한, 유료 MP3·풀길이.
2번이 콘텐츠라 만들기 제일 쉽고, 1번이 붙잡아 두는 힘이 제일 크다.

### 남은 것
- 프로젝트 여러 개 · 스타일 5종 추가 · 음 길이·세기 조절 · 라이브 · 쇼 화면

---

## 5단계 10/N — 곡 여러 개 (2026-08-16)

곡이 하나뿐이면 새로 만드는 순간 앞 곡이 사라진다. 그러면 사람은 **두 번째 곡을 시작하지
않는다**("망칠까 봐"). 목록이 있어야 마음 놓고 새로 만들고, 복제해서 실험한다.
유료화를 붙인다면 무료 N곡 / 유료 무제한의 그릇이기도 하다.

### 파일 구조
```
documents/
  index.json      {current:"s3", songs:[{id,name,genre,updated}]}
  songs/s1.json   곡 하나 = 파일 하나
```
목록을 따로 두는 이유: 곡이 20개여도 **목록 화면은 index 하나만 읽으면 된다.**
곡 파일을 전부 열어 이름을 꺼내면 곡이 늘수록 느려진다.

`Store` 에 `overrideDir` 를 열어 뒀다 — 시험에서 **임시 폴더로 진짜 파일을 쓰고 읽는다**
(path_provider 는 폰에서만 되지만, 저장 로직은 진짜 파일로 검사해야 의미가 있다).

### 시험이 잡은 것 두 개 — 둘 다 **데이터가 날아가는** 버그였다
1. **시작할 때 빈 프로젝트가 저장된 곡을 덮어썼다.** `open()` 은 "열기 전에 지금 곡을
   저장" 하는데, 앱 시작 시점의 프로젝트는 아직 **빈 기본값**이다. 그걸 저장해 버려서
   되살리려던 파일을 지웠다. → `open(id, saveCurrent: false)` 로 시작 때만 끈다.
   **앱 업데이트 한 번에 사용자 곡이 통째로 날아갈 뻔했다.**
2. **옮겨온 곡의 이름이 첫 자동 저장에 덮였다.** 곡 하나 시절 파일에는 이름 칸이 없어서
   목록에만 '내 곡'을 넣었는데, 자동 저장이 파일에서 읽은 기본 이름('새 곡')으로 목록을
   갱신했다. → **파일 안에 이름을 박아 넣는다.** (폰에서 눈으로도 확인됨)

### 옛 저장 파일 옮기기
5단계 8/N 의 `project.json` 하나짜리를 첫 곡으로 옮긴다. 원본은 **지우지 않고**
`project.json.bak` 으로 남긴다.

### 검증
`flutter test test/store_check_test.dart` — 6항목 통과:
첫 실행 · 새 곡(앞 곡과 안 섞임) · **곡을 오가도 각자 내용 유지**(스타일 house·볼륨 0.33
그대로) · 복제/이름/삭제(마지막 하나는 못 지움) · 이름 없던 옛 파일 · 옛 파일 옮기기.

**폰 실측(A17):** 기존 곡이 「내 곡」으로 옮겨졌고, 새 곡을 만든 뒤 둘을 오가며 확인 —
**A곡 2:40 / B곡 2:15** 로 각자 유지된다. 시험 화면 버튼에 `내 곡 (2개) — 새 곡 2` 처럼
지금 열린 곡이 보인다.

### 남은 것
- 스타일 5종 추가(트랩·프로그하우스·팝·재즈·엠비언트 — `kObjectSongForms` 이식)
- 음 길이·세기(vel)·글라이드 · 라이브 · 쇼 화면
- 곡 목록에서 길이·마지막 재생 시점 보여주기(지금은 스타일·수정 시각만)

---

## 5단계 11/N — 스타일 11종 (객체형 5곡 추가) (2026-08-16)

트랩 · 프로그하우스 · 팝 · 재즈 · 엠비언트를 스타일 목록에 넣었다. 이제 **11종**.

### 배열형과 객체형은 다르다 — 편성 자체가 바뀐다
- **배열형**(로파이~록, `kSongForms`): 드럼/베이스/코드/멜로디 **4트랙 고정**.
  트랙은 그대로 두고 클립만 갈아 끼운다(믹서에서 맞춘 값이 살아남는다).
- **객체형**(트랩~엠비언트, `kObjectSongForms`): **곡마다 편성이 다르다.**
  트랩 = 드럼·808 베이스·패드·브라스·벨·플럭(6). 팝 = 7트랙. 엠비언트는 **드럼 슬롯이
  'perc'** 다. 4트랙으로는 그 곡을 연주할 수 없으므로 **트랙 목록 자체를 새로 만든다.**

슬롯 이름(b808·plk·vib…)은 그대로 두면 못 읽으니 `kSlotLabel` 로 한글 이름을 붙였다
(808 베이스·플럭·비브라폰…).

### 장르 믹스를 트랙 초기값으로 얹었다
`kGenreMix` 의 슬롯별 볼륨·좌우·잔향·EQ 를 새 트랙의 초기값으로 넣는다. 안 얹으면
**트랩 808 이 패드와 같은 크기로 나와서 그 장르로 안 들린다.**
(씬 재생은 `setGenreMix(null)` 로 장르 몫을 걷어내므로, 트랙 값으로 갖고 있어야 한다)

### 조(mode)도 스타일이 들고 있다 — **팝만 장조**
안 맞추면 완전히 다른 곡이 된다. `kSongGenres` 에 5번째 칸으로 넣고 재생·전환 때 같이 건다.

### 시험이 잡은 것 둘
1. **객체형 → 배열형으로 돌아올 때 트랙이 안 돌아왔다.** 트랩(6트랙)에서 로파이로 가면
   벨·플럭이 남아 엉뚱한 트랙이 붙었다. → 객체형으로 편성을 만들었으면 그 사실을
   기억해 뒀다가(`_formTracksGenre`) 배열형으로 갈 때 4트랙으로 되돌린다.
2. **기본으로 보이는 구간이 '벌스'로 바뀌어 있었다.** "파트가 제일 많은 구간"으로 골랐더니
   로파이는 벌스와 코러스가 동점이라 앞의 벌스가 뽑혔다. → **이름으로 먼저**
   (코러스·훅·드롭) 찾고, 없을 때만 파트 수로 고른다.

### 검증
`flutter test` — 25항목 통과. 새로 넣은 6항목(스타일 5종 + 되돌리기):
trap 6트랙 206초 · proghouse 6트랙 210초 · pop 7트랙 222초 · jazz 6트랙 199초 ·
ambient 7트랙 213초. 각 곡의 **멀로딕 트랙이 하나도 빠짐없이 소리를 낸다**(5/5, 6/6).

**폰 실측(A17):** 재즈로 바꾸니 편성이 드럼·베이스·피아노·비브라폰·색소폰·기타로 바뀌고,
얼개가 **인트로→헤드→B→솔로→헤드→B→헤드2→아웃트로**(재즈 형식 그대로) · 3:19 ·
음 1804 · 타격 952. 25초 재생 **Underruns=0.**

### 남은 것
- 음 길이·세기(vel)·글라이드 조절 · 라이브 · 쇼 화면
- 곡 목록에 길이 보여주기 · 스타일 바꿀 때 손으로 고친 판 수 지키기

---

## 5단계 12/N — 음 길이·세기·글라이드 (편집기) (2026-08-16)

편집기에서 **음 하나를 골라** 길이·세기·글라이드를 고칠 수 있다.

### 왜 필요했나
칸을 눌러 켜고/끄는 게 전부였다 → 편집기로 찍은 음은 전부 **2칸·보통·직진**이다.
라이브러리 패턴은 그 셋으로 표정을 만든다(같은 음도 짧게/여리게/미끄러져 들어오면
완전히 다르게 들린다). 그래서 내가 찍은 판만 유독 밋밋했다.

- 빈 칸 누르기 → 찍고 **바로 그 음이 골라진다**(아래 바가 그 음을 가리킨다)
- 음 누르기 → 고르기 / 고른 걸 다시 누르기 → 지우기
- 아래 바: 길이 −/+ · 세기(여리게·보통·세게) · 글라이드 · 한 칸 옮기기 ◀▶ · 지우기
- **길이가 눈에 보인다** — 4칸짜리는 4칸을 차지하는 막대다(예전엔 전부 한 칸이라
  길이가 있다는 것도 몰랐다). 세기는 막대 밝기, 글라이드는 막대 왼쪽 표시.

### 드럼에도 세기를 넣었다 — 저장 형식이 늘었다
웹 형식은 레인별 **스텝 목록**뿐이라 세기를 담을 곳이 없었다 →
`DrumPatternDef.vels`(레인별, 스텝 목록과 **자리를 맞춘** 세기 목록)를 덧붙였다.
- 라이브러리 72개는 전부 `vels == null` — **웹과 같은 소리를 유지해야 한다.**
  없으면 `defaultDrumVel(lane, step)`(하이햇 박머리 2/사이 1 …)로 계산한다.
- 나란한 목록은 **순서가 어긋나면 조용히 망가진다**(킥 세기가 스네어에 붙는 식).
  그래서 정렬은 `DrumOps.toDef` 한 곳에서만 한다.
- 저장 파일에는 만진 패턴만 `vels` 칸이 생긴다(옛 파일도 그대로 읽힌다).

이걸로 **고스트 노트**(여린 스네어)와 악센트가 가능해졌다 — 드럼이 사람처럼 들리는
가장 큰 요인이다.

### 계산을 화면에서 떼어냈다 — `lib/edit_ops.dart`
`NoteOps`(음 목록) · `DrumOps`(드럼 스텝·세기)는 순수 함수다. 화면 안에 두면 시험이
못 찌른다 — 그런데 여기는 **눈이 속기 쉬운 자리**다(막대는 길어졌는데 소리는 그대로일
수 있다). 그래서 시험이 데이터가 아니라 **실제 재생 이벤트**까지 따라간다.

**코드 트랙은 5번째 칸이 글라이드가 아니라 코드 이름**(`min7`·`dom7`…)이다.
그래서 모든 함수가 `isChord` 를 받는다. 여기가 깨지면 재즈 코드가 전부 평범한
3화음으로 바뀌는데 **소리로는 알아채기 어렵다.**

### 코드 트랙은 7줄로 줄였다
15줄로 그리면 8번째 줄이 1번째와 **똑같은 소리**를 낸다(`buildChordPattern` 이 도수를
7로 나눈 나머지만 쓴다). 왜 다른 줄인데 같은 소리냐는 질문만 남는다.
도수 이름도 `1↑`·`2↑` 처럼 옥타브를 표시한다(0~14 라는 숫자보다 읽힌다).

### 위젯 시험이 잡은 것 둘 — 둘 다 **눈으로는 못 봤을** 버그
1. **격자 칸이 30px 이 아니라 32px 씩 갔다.** 칸 30 + 여백 1+1 = 32 인데 재생줄과
   음막대는 30 씩 갔다 → 뒤로 갈수록 어긋나고, 마지막 4칸은 화면 밖으로 밀려
   **눌리지도 않았다.** 예전 편집기부터 있던 버그다(넘침 경고를 폰에서 못 봤다).
2. **같은 화면에 다른 트랙을 실으면 앞 트랙 패턴을 고쳤다.** Flutter 는 같은 자리의
   같은 종류 위젯을 재사용해서 `initState` 가 다시 안 불린다 → `didUpdateWidget` 에서
   패턴 이름을 다시 잡는다.

### 검증
`flutter test` — 6개 파일 전부 통과.
- `editor_check_test.dart` 8항목: 길이 2칸 0.326초 → 6칸 0.978초 · 세기 2→3 ·
  글라이드 0 → **261.6Hz(앞 음 높이)** · 옮기기(막힌 자리는 안 감) ·
  **코드 타입 m7b5 보존** · 드럼 세기 저장·불러오기 왕복 ·
  라이브러리 기본 세기 그대로(하이햇 24타) · 마디 줄이면 꼬리 잘림
- `editor_ui_test.dart` 12항목(화면을 직접 누른다): 찍기·고르기·길이·세기·글라이드·
  옮기기·다시 눌러 지우기 · 드럼은 세기만 · 트랙 바꾸면 그 트랙 패턴 ·
  **세로(400×800)·가로(800×400) 둘 다 안 넘침**

APK 빌드 완료(51.9MB). **폰이 연결돼 있지 않아 실측은 다음 차례로 미뤘다** —
확인할 것: 아래 바가 360dp 폭에서 안 잘리는지(글라이드 칸까지), 여린 스네어가
실제로 여리게 들리는지.

### 남은 것
- 폰 실측(위) · 라이브 화면 · 쇼 화면
- 곡 목록에 길이 보여주기 · 스타일 바꿀 때 손으로 고친 판 수 지키기

---

## 5단계 13/N — 라이브 화면 (2026-08-16)

**내 곡 위에 얹어 친다.** 씬 루프를 돌려 놓고 그 위에서 패드를 두드린다.

### 이 화면의 약속은 하나 — 틀린 음이 안 나온다
패드는 반음 건반이 아니라 **지금 조의 음계**다(`degreeFreq` — 곡을 만들 때 쓰는 바로
그 함수). 아래줄 도수 1~7, 윗줄은 한 옥타브 위. 피아노 건반을 주면 초보는 반드시 옆
반음을 짚고, **한 번 어긋난 소리를 들으면 그 뒤로 안 누른다.** 그게 이 앱에서 제일 흔한
이탈 지점이다. 계이름(C4·D#5)을 같이 보여 줘서 연주하는 사이에 배운다.

- **단음 / 화음 / 아르페지오** — 화음은 그 도수의 **다이아토닉 3화음**이라 틀릴 수가 없다
- 음색 24종 · 길이(짧게·보통·길게) · 옥타브 −1~+2
- 라이브 볼륨·잔향을 여기서 바로 잡는다(치면서 조절하는 값이라 믹서까지 안 간다)
- **씬 칩을 누르면 돌면서 바뀐다**(다음 판부터). 인트로 → 코러스로 넘기며 치는 게 라이브다

### 소리는 전부 라이브 버스로
`kPartLive` 로만 나간다 → 믹서의 라이브 페이더·잔향이 **이 소리만** 잡는다.
여기가 틀리면 곡 트랙 버스에 얹혀서, 라이브를 줄이면 반주까지 같이 줄어든다.

### 반응 속도 — `setSongMode(false)`
곡 재생은 128ms 앞질러 만들지만(안 밀리는 게 중요) 손가락에 붙어야 하는 화면에서는
그게 그대로 늦음이다. 라이브에서는 사용자 설정값(기본 64ms)으로 되돌린다.
루프는 엔진 시계 기준 `guard = ahead + 0.25초` 로 채우므로 64ms 로 줄여도 판이 안 빈다.
**폰에서 루프 재생 + 연타 + 씬 전환까지 하고도 Underruns=0.**

### 계산은 `lib/live_ops.dart` 로 뺐다
`LivePads.events()` 가 패드 → 엔진 이벤트를 만든다. 화면 안에 두면 시험이 못 찌르는데,
"틀린 음이 안 나온다"는 **귀로는 확인이 안 된다**(살짝 어긋난 음도 "그런 곡인가" 싶다).

### 검증
`flutter test test/live_check_test.dart` — 8항목 통과:
C단조 14패드 **전부 음계 안**(음계 0,2,3,5,7,8,10) · F장조로 바꿔도 전부 음계 안(으뜸음 F) ·
윗줄 = 정확히 2.000배 · 옥타브 ±(523.3 / 130.8Hz) · 화음 7도수 전부 다이아토닉과 일치 ·
**이벤트 112개 전부 kPartLive** · 아르페지오 4음 0.00→0.33초 · 계이름.

**폰 실측(A17):** 라이브 열기 → 반주 재생 → 패드 연타 → 화음 모드 → 씬(B→솔로) 전환.
**Underruns=0** (두 번 측정). 계이름 C4~A#4 / C5~A#5 로 위아래 줄이 구분된다.

### 폰에서 눈으로 잡은 것
**두 줄의 글자가 똑같았다**(1 C / 1 C). 음이름만 쓰면 옥타브가 안 보인다 → 옥타브 번호를
붙였다(C4·C5). 위아래가 뭐가 다른지 화면만 봐선 모르면 윗줄은 안 쓰게 된다.

### 남은 것
- 쇼 화면 · 곡 목록에 길이 보여주기 · 스타일 바꿀 때 손으로 고친 판 수 지키기
- 라이브 녹음(친 걸 트랙으로 남기기) — 이게 되면 라이브가 '만들기'로 이어진다

---

## 5단계 14/N — 라이브 녹음 (2026-08-16)

**친 걸 트랙으로 남긴다.** 라이브 화면에서 녹음 → 패드 두드리기 → 다시 눌러 끝내면
그 자리에서 「라이브」 트랙이 되고 다음 판부터 반주와 같이 난다.

### 패드가 음계라서 녹음이 쉬워졌다
무슨 음인지 **이미 알고 있다**(도수 번호). 음높이를 알아낼 필요가 없고, 결과가
편집기와 **똑같은 형식**([도수,스텝,길이,세기])으로 바로 나온다 →
녹음한 걸 편집기에서 이어서 고칠 수 있다. 라이브가 '만들기'로 이어지는 자리다.

### 박자는 자동으로 맞춘다 (안 맞출 수가 없다)
패턴은 16분음표 칸 단위라 **칸 사이에 음을 둘 곳이 없다.** 초보에게는 이게 기능이다 —
조금 어긋나게 쳐도 판에 딱 붙는다.

### 늦게 잡히는 문제 — 안 되돌리면 판 전체가 한 칸씩 밀린다
귀에 닿는 소리는 엔진이 만든 것보다 늦다(앞질러 만든 양 + 장치 버퍼). "제때 쳤다"고
느낀 순간의 위치는 실제보다 그만큼 뒤다 → `latencySec`(= `aheadFrames/48000`, 기본 64ms)
만큼 되돌린 뒤 반올림한다. **시험이 이걸 직접 잡는다**: 되돌리면 16번 칸, 안 되돌리면
17번 칸. (귀로 더 맞춰야 하면 이 값만 손보면 된다)

### 무엇이 담기나
- 단음 = 1음 · **화음 = 3음**(도수 d·d+2·d+4 — `diatonicChords` 와 같은 자리) ·
  아르페지오 = 친 시간차 그대로 4음(각자 제 칸에 떨어진다)
- 길이는 화면의 짧게/보통/길게를 따라간다(1·2·4칸) — **들은 길이와 남는 길이가 다르면
  "내가 친 게 아닌데" 가 된다**
- 옥타브를 올려 뒀으면 그만큼 올려 담는다. 패턴이 담는 도수는 0~14뿐이라 넘치면
  한 옥타브 내린다(잘라내면 눌린 음과 **다른 음**이 남는다 — 계이름을 지키는 쪽을 골랐다)

### 담기는 곳 — 「라이브」 트랙, 지금 씬에만
`Project.ensureLiveTrack()`. `addTrack` 은 **모든 씬에** 기본 패턴을 채우는데
(트랙만 만들고 다른 씬에 가면 조용해서 고장처럼 보이니까), 라이브는 그러면 안 된다 —
인트로에서 친 게 코러스에서도 되풀이되면 곡이 아니라 사고다. 이름으로 찾아 **다시 쓴다**
(칠 때마다 트랙이 늘면 열 번 치고 트랙이 열 개다). 같은 씬에서 다시 치면 덮어쓴다.

### `LoopClock` — 위치를 아무 때나 물어볼 수 있게
녹음은 손가락이 닿는 **그 순간** 몇 박인지 알아야 한다. 그건 build 안이 아니라 콜백
안이다. `play_head.dart` 의 이어 맞추기(0.25초마다 오는 값 + 보간)를 `LoopClock` 으로
꺼내고 `LoopPosBuilder` 도 그걸 쓰게 했다 — 같은 계산이 두 벌 있으면 한쪽만 고친다.

### 검증
`flutter test` — 9개 파일 전부 통과.
- `live_rec_test.dart` 8항목: 박 맞춤(4.2→4, 7.6→8) · **늦음 되돌리기(16 vs 17번 칸)** ·
  겹쳐 녹음(5번 쳐도 1음) · 판 넘어가면 감김(1.02→1번 칸) ·
  **담은 게 실제로 소리가 난다**(시퀀서까지 돌려 8음·bell) · 다른 씬은 조용 ·
  트랙은 하나 · 저장 왕복
- `live_ui_test.dart` 7항목(화면을 직접 누른다): 패드가 조의 음계 · 그냥 치면 안 담김 ·
  녹음 중 '담기 3' · **끝내면 트랙 생성(음 3개)** · 음색 따라감 · 다시 녹음=덮어쓰기 ·
  가로·세로 안 넘침

APK 빌드 완료. **폰이 중간에 빠져서 녹음은 아직 실측 못 했다** — 다음 차례에
확인할 것: (1) 친 게 박에 맞게 담기는지(늦음 보정값이 A17 에서 맞는지) ·
(2) 담긴 뒤 반주와 같이 나는지 · (3) 녹음 중 Underruns.

### 남은 것
- 위 실측 · 쇼 화면 · 곡 목록에 길이 보여주기
- 스타일 바꿀 때 손으로 고친 판 수 지키기

---

## 5단계 15/N — 스타일 바꿔도 내 판 지키기 · 곡 목록에 길이 (2026-08-16)

### ① 스타일 칩을 눌러도 손으로 고친 판이 안 사라진다
스타일 칩은 **구경하려고 눌러 본다.** 그때마다 손으로 찍은 판이 조용히 사라지면
사용자는 나중에야 알게 되고 되돌릴 방법도 없다. 앱을 지우는 이유는 대개 이런 것이지
기능이 모자라서가 아니다.

- **배열형 → 배열형**(로파이↔하우스↔록…): 씬을 새로 만들기 전에 내 판을
  (구간번호:트랙종류)로 적어 뒀다가 **같은 자리에 다시 실어 준다**. 라이브러리 판은
  새 스타일 것으로 갈린다(그건 사용자가 만든 게 아니다).
- **객체형**(트랩·재즈…처럼 편성이 통째로 바뀜): 실을 자리가 없다. 아무 트랙에나
  얹으면 그 곡에서 중요한 파트를 밀어낸다 → **안 싣고 개수만 알린다.**
  판 자체는 안 지운다(`userNote`/`userDrum` 에 그대로, 편집기 목록 **맨 위**에 뜬다).
- 어느 쪽이든 화면이 한 줄로 말해 준다("손으로 고친 판 3개는 그대로 뒀습니다" /
  "편성이 바뀌어 2개는 빠졌습니다 — 편집기 목록 맨 위에 있습니다").
  **말 안 하면 스타일 바꿀 때마다 사용자가 직접 확인해야 한다.**

`Project.keptMine` / `parkedMine` 이 그 개수다.

### ② 곡 목록에 길이가 보인다
`SceneSequencer.songSeconds(p, tr)` — **음을 만들지 않고 셈으로만** 낸다.
목록 한 줄 그리자고 `buildSong`(2~3분짜리 음 수천 개)을 돌리면 곡이 스무 개일 때
목록이 멈춘다. 길이는 저장할 때 index.json 에 적어 둔다(`SongMeta.sec`) — 목록은
곡 파일을 안 열고도 길이를 안다.

`build` 와 **같은 규칙**으로 판 길이를 잡아야 한다(뮤트한 트랙은 안 센다) — 안 그러면
목록에 적힌 길이와 실제 재생 길이가 다르다. 시험이 그걸 직접 맞춰 본다.

### 검증
`flutter test` — 11개 파일 전부 통과.
- `genre_keep_test.dart` 8항목: 내 판 만들기 → 하우스로 바꿔도 **클립 그대로 · 지킨 판 1개** ·
  나머지는 새 스타일로(House Off C) · **지킨 판이 실제로 소리 남**(킥 8타) ·
  트랩(객체형)은 개수만 알리고 판은 보관 · 편집기 목록 맨 위 · 되돌아와도 안 지워짐 · 저장 왕복
- `song_len_test.dart` 4항목: 셈 = 실제(6종 전부 **차이 0.0초** — lofi 135.4 · trap 205.7 ·
  jazz 198.6 · ambient 213.3) · 뮤트해도 같은 규칙 · 목록에 저장 · 다시 켜도 남음

APK 빌드 완료. **폰 실측은 아직** — 사용자가 폰을 쓰는 중이라 자동 조작을 멈췄다
(라이브 녹음 실측 3가지도 같이 밀려 있다).

### 남은 것
- 폰 실측: 라이브 녹음(박 맞음·반주와 같이 남·Underruns) · 스타일 칩 안내 한 줄 · 곡 목록 길이
- 쇼 화면

---

## 5단계 16/N — 쇼 화면 (2026-08-16)

**남에게 보여 주는 화면.** 만든 곡을 틀고 폰을 세워 두는 자리다.
그래서 다른 화면과 반대로 만들었다 — 조작은 숨기고(누르면 3초 나온다), 글씨는 크게,
배경은 검게, 상태바·내비게이션 바까지 없앤다(`immersiveSticky`).

보이는 것: 지금 **구간 이름**(코러스…)이 제일 크게 · 곡 이름 · 0:42 / 2:15 ·
구간 띠(지금 구간이 켜진다) · 악기마다 기둥(울릴 때 솟는다) · 드럼 4개(타격마다 점멸).

### 소리를 '분석'하지 않는다 — **악보를 갖고 있으니까**
보통 이런 화면은 출력 소리를 분석해 만든다. 이 앱은 언제 무슨 악기가 울리는지 이미 안다:
- **정확하다** — 분석은 늘 몇십 ms 늦고, 큰 소리에 작은 소리가 묻힌다
- **싸다** — 프레임마다 하는 일이 이진 탐색 한 번(3분짜리 곡의 음 수천 개여도 같다)
- **악기별로 나뉜다** — 베이스가 울릴 때 베이스 기둥만 솟는다

`ShowScore.from()` 이 곡을 한 번 만들어 **시각표로 접는다**(음 자체는 안 들고 있는다 —
화면에 필요한 건 시각뿐이다). `pulse(t)` 는 바로 앞 타격을 이진 탐색으로 찾아
`(1-dt/decay)^2.2` 로 잦아든다(직선이면 미는 느낌이 난다).

### 곁다리로 고친 것 — 곡 화면이 프레임마다 곡 전체를 만들고 있었다
구간 (시작,길이) 계산이 `song_view._spans()` 안에 있었는데, 그게 구간마다 `build` 를
돌렸다 → **화면을 다시 그릴 때마다 3분짜리 곡의 음 수천 개를 새로 만든 셈.**
`SceneSequencer.songSpans()`(셈만 한다)로 옮기고 곡 화면·쇼 화면·저장이 같이 쓴다.

### 검증
`flutter test` — 13개 파일 전부 통과.
- `show_check_test.dart` 8항목: 곡 길이 = 재생 길이 · **소리 내는 악기 3/3 전부 움직임** ·
  타격 시각에 1.00 → 0.15초 뒤 0.22 → 0.31초 뒤 0.00 · **시작 전엔 0** ·
  악기별 분리(12.31초에 베이스 1.00 / 멜로디 0.00) · 구간 이름
  (인트로→벌스→코러스→브레이크→벌스→코러스→아웃트로) · **구간 경계 12.3초에서 0→1** ·
  드럼 레인 분리(킥 76타 · 하이햇 224타)
- 화면 4항목: 구간 이름 5/5 보임 · 악기 이름 3/3 보임 · 곡 이름과 시간 · 가로·세로 안 넘침

APK 빌드 완료. **폰 실측은 밀려 있다**(사용자가 폰 사용 중).

### 남은 것
- 폰 실측 한 번에: 라이브 녹음(박·반주와 같이 남·Underruns) · 스타일 칩 안내 ·
  곡 목록 길이 · **쇼 화면(기둥이 박에 맞게 튀는지 · 전체 화면 되는지)**
- 다음 후보: 쇼 화면에 캐릭터(웹의 오리지널 밴드) · 곡 목록 정렬/검색 · 첫 실행 안내

---

## 5단계 17/N — 폰 실측(밀린 것 4가지) + 실측이 잡은 것 2가지 (2026-08-16)

갤럭시 A17(SM-A175N)에서 12~16/N 을 한 번에 확인했다. **전부 통과.**

### ① 라이브 녹음
반주 재생 → 녹음 → 패드 8번(1·3·5·7도 아래줄 → 윗줄) → 담기.
- 버튼에 **「담기 8」**, 끝내니 **「8음을 「라이브」 트랙에 담았습니다」**
- 씬 화면에 **「라이브」 트랙 생성**(멜로디 · 음색 피아노 · 패턴 「라이브 솔로」 —
  씬 이름을 따라간다)
- 편집기로 열어 보니 **계단 모양**으로 정확히 담겼다: 누른 순서대로 도수가 올라가고,
  길이는 전부 2칸(보통), 간격이 고르다(0.4~0.5초 간격 탭 → 3칸 간격)
- **Underruns=0** (녹음 중·재생 중)

### ② 스타일 칩 안내
새 곡에서 드럼 판을 손으로 고친 뒤 로파이 → 하우스:
**「손으로 고친 판 1개는 그대로 뒀습니다」** 가 뜨고, 씬 화면에서 드럼은
**「드럼 내 패턴」 그대로**, 베이스는 **House Off C**, 키트는 **909** 로 갈렸다.

### ③ 곡 목록 길이 · ④ 쇼 화면
목록에 `재즈 116BPM · 3:19` 로 나온다. 쇼는 **전체 화면**(상태바·내비 바 없음),
1:14 지점에서 구간이 **솔로**로 켜지고 베이스·피아노·기타 기둥이 솟고 **킥이 터진다**.
프레임 6장의 밝은 픽셀 비율이 24%~60% 로 출렁 — 소리에 맞춰 움직이는 게 숫자로도 보인다.
**Underruns=0.**

### 실측이 잡은 것 2가지 (고침)
1. **쇼 화면 조작이 멈춰 있을 때도 3초 뒤 사라졌다.** ▶ 버튼이 없어져서
   화면을 한 번 눌러야 다시 나오는데, 그걸 모르면 아무것도 못 한다(내가 실제로 막혔다).
   → **재생 중일 때만** 숨긴다(4초). 멈춰 있으면 계속 보인다.
   같은 이유로 멈춰 있으면 기둥도 **전부 쉬게** 했다(멈춘 순간이 얼어붙어 있으면 고장처럼 보인다).
2. **옛 곡은 목록에 길이가 영영 안 나왔다.** 길이는 저장할 때 적히는데, 기존 곡은
   고쳐서 저장하기 전까지 칸이 빈다. → 켤 때 **한 번만** 곡 파일을 열어 채운다
   (`Store._fillMissingLengths`). 폰에서 「새 곡 · 로파이 78BPM · **2:40**」 로 채워졌다.

### 검증
`flutter test` — 13개 파일 전부 통과(길이 시험에 '옛 목록 채우기' 1항목 추가).

### 폰에 남은 시험 흔적 (사용자 확인 필요)
- **새 곡 2(재즈)** 의 「솔로」 씬에 내가 녹음한 **「라이브」 트랙(8음)** 이 붙어 있다
- **새 곡 3** 은 내가 만든 시험용 곡이다(하우스 · 드럼 내 패턴 1개)
→ 둘 다 지워도 되지만 **사용자 확인 없이는 안 지운다**. 트랙은 씬 화면의 ⊖,
   곡은 곡 목록의 ⋮ → 삭제.

### 남은 것
- 쇼 화면에 캐릭터(웹의 오리지널 밴드) · 곡 목록 정렬/검색 · 첫 실행 안내

---

## 5단계 18/N — 쇼 화면의 밴드 (2026-08-16)

무대 위에서 **사람들이 연주한다.** 막대 그래프와 같은 숫자(`ShowScore` 의 0~1)를
사람 모양으로 바꾼 것 — 오른쪽 위 아이콘으로 밴드 ↔ 막대를 오간다(기본은 밴드).

### 그림은 전부 코드로 그린다 (`ui/show_band.dart`, `CustomPainter`)
이미지 파일을 하나도 안 쓴다. 이유가 셋:
1. **저작권** — 남의 그림이 섞일 여지가 없다(출시가 걸려 있다)
2. 앱 크기 — 캐릭터 몇 십 장이면 몇 MB, 코드는 몇 KB
3. **소리에 맞춰 움직여야 한다** — 팔 각도·몸 기울기가 매 프레임 바뀐다.
   그림판이면 프레임마다 다른 장이 필요하지만, 코드는 값 하나만 바꾸면 된다.

### '연주하는 것처럼' 보이게 하는 세 가지
사람은 **박자에 맞는 움직임**을 아주 잘 알아본다. 그래서 셋만 지켰다:
- 소리가 나면 **무릎이 굽었다 펴진다**(사람은 이걸 리듬으로 읽는다)
- 팔이 악기 쪽으로 움직인다 — 건반은 위아래, 기타/베이스는 스트로크,
  관악기는 벨이 들리며 몸이 젖혀진다
- 소리 크기만큼 **빛난다**(어두운 무대라 빛이 곧 소리다)

드러머는 뒤쪽 **단(riser) 위**에 있고 킥·스네어·하이햇·크래시가 각각 따로 반응한다
(스틱 두 개가 스네어/하이햇에 맞춰 내려친다). 무대에는 조명 기둥 셋 · 바닥선 · 그림자.

### 폰에서 눈으로 잡은 것
**드러머가 가운데 연주자와 겹쳤다.** 화면 높이의 13% 만 띄웠더니 앞줄 사람 키
(100 남짓)와 부딪혔다 → **34% 로 올리고 단을 깔았다.** 조명도 너무 진해서 옅게(0.16→0.09).

### 검증
`flutter test` — 14개 파일 통과. 쇼 시험에 4항목 추가:
밴드가 기본 · 막대로 전환하면 악기 이름 3/3 과 드럼 줄이 글자로 보임 · 다시 밴드로 ·
**밴드도 가로·세로 안 넘침**.

**폰 실측(A17):** 하우스 곡 재생 중 — 베이스(기타 걸침)·패드(건반)·리드(관악기)가
각자 자리에 서고 드러머는 뒤 단 위. 프레임 간 바뀐 픽셀 **3.5% / 10.2%** —
멈춘 그림이 아니라 소리에 맞춰 움직인다는 뜻이다.

### 남은 것
- 곡 목록 정렬/검색 · 첫 실행 안내 · 밴드에 관객·조명 색 바꾸기(스타일별)

---

## 5단계 19/N — 첫 화면 (2026-08-16)

**앱을 켜면 나오던 게 엔진 시험 화면이었다** — 악기 33종 칩, 건반 지연 설정, 부하 시험,
20밴드 EQ. 만드는 동안에는 그게 맞았지만, **음악을 만들러 온 사람에게 계기판을 보여 주는
셈**이다. 뭘 눌러야 할지 모르면 그대로 나간다. 출시를 생각하면 여기가 제일 큰 구멍이었다.

### 새 첫 화면에 두는 것은 두 가지뿐
1. **지금 곡** — 이름 · 스타일 · 길이 · 곡 개수. 다시 켰을 때 "내가 만들던 거"가 바로
   보여야 이어서 하게 된다. 누르면 곡 목록.
2. **할 일 넷** — 만들기(씬) · 곡으로 잇기 · 라이브 · 쇼.
   **버튼마다 한 줄 설명**을 붙였다("악기를 고르고 한 판을 만든다" / "반주 위에 직접
   얹어 친다 — 틀린 음이 안 나온다"). **이름만 있으면 처음 온 사람은 못 고른다.**
   그 아래 순서 안내 한 줄: "처음이면 만들기 → 곡으로 잇기 순서로".

믹서는 오른쪽 위 아이콘, **엔진 시험 화면은 맨 아래 작은 글씨**로 내려보냈다
(엔진을 직접 두드려 볼 일은 계속 있다 — 없애지 않고 자리만 옮겼다).

### 구조
`main.dart` 의 옛 `build()` 는 `_lab(context)` 가 됐고, `build()` 는 `HomeView` 만
돌려준다. **시험 화면 코드는 한 줄도 안 고쳤다** — 자리만 바뀌었다.
`HomeView` 는 오디오를 직접 안 만진다(곡을 열고 나서 할 일은 `onSongOpened` 로 위임).

### 검증
`flutter test` — 15개 파일 통과. `home_ui_test.dart` 7항목:
지금 곡(이름·스타일) · 할 일 4/4 · **한 줄 설명** · 만들기로 이동 · 라이브로 이동 ·
시험 화면은 눌러야만 열림 · 가로·세로 안 넘침.
(`pumpAndSettle` 은 못 쓴다 — 재생 위치 표시가 계속 도는 애니메이션이라 영영 안 멈춘다)

**폰 실측(A17):** 첫 화면이 「음악 낙서장 / 지금 곡 새 곡 3 · 하우스 124BPM · 1:25 ·
곡 3개 / 만들기 · 곡으로 잇기 · 라이브 · 쇼」 로 나온다. 쇼로 들어갔다 뒤로 나와도 그대로.

### 남은 것
- 곡 목록 정렬/검색 · 스타일별 조명 색 · 관객
- (출시 관련은 **사용자가 말할 때까지 꺼내지 않는다** — 2026-08-16 지시)

---

## 5단계 20/N — 만들기 화면 UI 손질 (2026-08-16)

만들기(씬) 화면의 **주인공은 트랙**인데 위쪽 설정이 화면을 절반 가까이 먹고 있었다.

### 조(調) 두 줄 → 한 칸
12키 칩 한 줄 + 단조/장조 한 줄 = 두 줄을 통째로 쓰고 있었다. 그런데 **초보가 제일 안
만지는 값**이다(조가 뭔지 모른다). `조 C 단조 ▾` 한 칸으로 접고, 누르면 바닥 시트에서
12키 + 장/단조를 고르게 했다. 시트에는 설명도 한 줄 넣었다 —
"바꾸면 모든 악기가 같이 옮겨 갑니다 — 곡은 그대로입니다" · "단조(어둡다)/장조(밝다)".

폰에서 **트랙 4개 + ＋악기 줄이 전부 한 화면에** 들어온다(예전엔 스크롤해야 했다).

### 시험이 잡은 것 — 가로에서 장/단조를 누를 수 없었다
시트를 `isScrollControlled` 없이 열면 **가로 화면에서 아래쪽이 잘린다.** 잘린 부분은
보이지도 눌리지도 않는다(시험이 `hitTestable=0` 으로 잡았다). → 스크롤 가능한 시트로.

### 멈춰 있을 때 마디 표시가 뭔지 알 수 없었다
'멈춤' + 빈 칸 넷. 이게 뭔지 알 방법이 없다 → **'한 판 4마디' + 칸마다 번호(1·2·3·4)**.
돌기 시작하면 번호 대신 채워지는 막대가 된다.

### 검증
`flutter test` — 16개 파일 통과. `scene_ui_test.dart` 7항목:
조는 접혀 있다(펼쳐진 키 0개) · 트랙 4/4 보임 · 펼치면 12키 · 고른 조 반영(F 장조) ·
접힌 칸에 표시 · 멈춤 상태 마디 표시 · 가로·세로 안 넘침.

---

## 5단계 21/N — 곡 화면 UI 손질 (2026-08-16)

### 구간 순서를 **끌어서** 바꾼다
줄마다 ↑↓ 버튼이 있었다. 구간이 7~10개면 한 칸씩 눌러 옮겨야 한다(맨 위 구간을
맨 아래로 보내려면 아홉 번). `ReorderableListView` 로 바꾸고 ↑↓ 자리에 **손잡이**를 뒀다.

**키를 자리 기반(`'$i-...'`)으로 두면 안 된다** — 끌어서 옮길 때 키까지 같이 바뀌어
Flutter 가 "같은 줄이 남아 있다"고 착각한다(순서가 안 바뀐다). `ObjectKey(section)` 으로
바꿨다. 콜백도 `onReorder`(빼기 전 기준이라 직접 -1 해야 함) 대신 **`onReorderItem`**
(이미 보정된 자리)을 쓴다 — 한 칸씩 밀리는 흔한 버그를 아예 안 만든다.

### 판 수 — 숫자를 누르면 1·2·4·8·16
−/+ 만 있으면 8판까지 일곱 번 눌러야 한다. 숫자에 밑줄을 긋고 누르면 목록이 뜬다.

### ＋구간 붙이기를 아래 고정
목록 맨 끝에 있어서 구간이 많아지면 **스크롤해야 닿았다.** 목록 밖으로 빼서 늘 보인다.

### 검증
`flutter test` — 17개 파일 통과. `song_ui_test.dart` 7항목:
손잡이 7개 · **끌면 곡 데이터가 바뀐다**(구간 **객체**로 비교 — 씬 번호는 겹쳐서
순서 변화를 못 본다) · 나머지 구간 순서 유지 · 판 수 목록 5개 · 고르면 반영(8판) ·
붙이기 줄이 스크롤 없이 보임 · 가로·세로 안 넘침.

**폰 실측(A17):** 인트로를 끌어 3번째로 옮겼고(벌스·코러스·인트로·브레이크…),
판 수 숫자를 누르니 1·2·4·8·16 목록이 떴다. 7구간 + 붙이기 줄이 **한 화면에** 들어온다.

---

## 5단계 22/N — 편집기 격자 손질 (2026-08-16)

4마디(64칸)를 30px 칸으로 그리면 **끝까지 보려면 다섯 번 밀어야 한다.** 셋을 넣었다.

### ① 마디 자
격자 위에 마디 구분선과 번호(1·2·3·4). **지금 몇 마디를 보고 있는지**가 안 보이면
길을 잃는다(칸만 64개 늘어서 있으면 어디가 어딘지 모른다).

### ② 마디 점프
위쪽에 마디 번호 칸. 누르면 그 마디로 미끄러진다(`ScrollController.animateTo`).

### ③ 촘촘 보기 (30px ↔ 18px)
칸 너비를 상수에서 **상태값**으로 바꿨다(`_cell`). 18px 보다 더 줄이면 손끝보다 칸이
작아져서 못 찍는다 — 그래서 두 단계만.
격자 줄·칸 위젯이 `cell` 을 받아 그린다(음 막대·재생줄도 같은 값을 쓴다 —
**한 군데라도 옛 상수를 쓰면 뒤로 갈수록 어긋난다**. 5단계 12/N 에서 겪었다).

### 검증
`flutter test` — 17개 파일 통과. 편집기 화면 시험에 3항목 추가:
마디 자·점프 줄 있음 · **촘촘 보기 1022 → 638px** · 되돌리면 1022px.

**폰 실측은 못 했다** — 폰 화면이 꺼져(잠금) 있어 자동 조작이 안 먹었다.
다음에 켜져 있을 때 확인할 것: 마디 자 글씨 크기 · 점프가 원하는 마디에 서는지 ·
촘촘 보기에서 칸을 손가락으로 찍을 수 있는지(18px = 실기기 약 6.5mm).

---

## 5단계 23/N — 패턴 고르기 (묶어서 보여 주기) (2026-08-16)

패턴 시트가 **이름 칩 한 벽**이었다. 드럼만 72개다 — 처음 온 사람에게
'Lofi Chorus' 와 'Boom Verse' 중 뭘 고르라는 건지 알 방법이 없다.

### 이 곡의 스타일 것부터
패턴 이름은 전부 스타일 머리말로 시작한다('Lofi …', 'Trap …'). 그 표를
`presets.dart kStylePrefixes` 로 두고(힙합은 Boom·808·Dusty·Hip·Gfunk 처럼 여럿),
시트를 세 묶음으로 나눴다:

1. **내가 고친 판** — 늘 맨 위
2. **이 곡 스타일**(로파이면 로파이 8개) — 기본으로 이것만 보인다
3. 「전부 보기」를 누르면 **기본**(Shuffle·Backbeat 처럼 스타일에 안 묶인 것) 과
   **다른 스타일** 이 펼쳐진다

72개 → **8개**. 대개 이 안에서 고른다.

### 검증
`flutter test` — 17개 파일 통과. 씬 화면 시험에 3항목 추가:
**이 스타일 것만 먼저**(로파이 8개 · 하우스 0개) · 전부 보기로 펼치면 기본·다른 스타일
묶음이 나옴 · 골랐을 때 트랙에 실림(House Chorus).

폰 실측은 다음 차례(통화 중이라 조작 안 함).

---

## 5단계 24/N — 믹서 '한눈에' 보기 (2026-08-16)

세로 화면에서 스트립(세로 페이더)은 100dp 씩 먹어 **세 채널밖에 안 보인다.**
재즈·팝처럼 6~7트랙인 곡은 밸런스를 맞추려고 계속 좌우로 밀어야 했다.

- 위에 **보기 줄**을 뒀다: `한눈에`(한 채널 = 한 줄) ↔ `자세히`(스트립).
  줄 모양은 원래 가로 화면용으로 만든 것인데, 세로에서도 그게 필요했다.
- **처음 값은 트랙 수로 정한다** — 5개가 넘으면 줄 모양. 그때부터 스트립이 안 맞는다.
- 마스터는 두 보기 모두 붙박이(스트립은 오른쪽 끝, 줄은 맨 아래).

### 시험이 잡은 것 — 줄 모양이 세로에서 42px 넘쳤다
줄 모양은 **800dp 가로 기준**으로 만든 것이라, 400dp 세로에서 칸을 5~6등분하면
'볼륨 100%' 가 그대로 넘쳤다(라벨만 줄여도 부족). → `FittedBox` 로 **글자가 작아지게**
했다. 잘리는 것보다 작아지는 게 낫다.

### 검증
`flutter test` — 18개 파일 통과. `mixer_ui_test.dart` 6항목:
4트랙은 스트립 기본 · 줄 모양에서 4/4 전부 보임 · **6트랙 재즈는 줄 모양이 기본**(6/6) ·
마스터 붙박이 · 가로는 원래대로 · 가로·세로 안 넘침.

---

## 5단계 25/N — 라이브 악기 고르기 + UI 손질 폰 실측 (2026-08-16)

### 라이브 악기도 묶어서
음색 24종이 한 줄로 늘어서 있었다(원하는 악기까지 계속 밀어야 하고, 그 줄이 패드
자리를 먹는다). **한 칸(`피아노 ▾`)으로 접고** 시트에서 계열별로 고른다:
건반 · 뜯고 치는 · 관악기 · 현악기 · 신스 · 베이스(`instruments.dart kVoiceFamily`).
사람은 "기타 비슷한 거"를 찾지 'nylon' 을 찾지 않는다. 줄 하나가 패드로 갔다.

### 폰 실측(A17) — UI 손질 네 가지 전부 확인
1. **라이브 악기 시트** — 6묶음으로 펼쳐진다(피아노가 켜진 채).
2. **패턴 시트** — 「드럼 패턴 / 하우스 스타일 패턴입니다」 + **내가 고친 판 1개** +
   **하우스 5개**(House Chorus Plain·Verse·Chorus·Break·4/4). 72개 → 6개.
3. **편집기** — 마디 점프 줄(1·2)과 마디 자가 보이고, 촘촘 보기를 누르니
   **한 마디 반이 한 화면에** 들어온다(칸은 여전히 찍을 수 있는 크기).
4. **믹서** — 「트랙 4개 · 한눈에 | 자세히」. 한눈에로 바꾸니 4트랙 + 라이브 +
   ＋악기가 **한 화면에** 들어온다. 자세히는 스트립 + 마스터 붙박이 그대로.

**실수 하나:** 라이브 화면에서 좌표를 잘못 짚어 **라이브 볼륨 슬라이더를 건드렸다**
(10% 로 내려갔다). 바로 되돌려 98%(기본값 100% 근처)로 맞춰 뒀다.

### 검증
`flutter test` — 18개 파일 통과. 라이브 화면 시험에 3항목 추가:
음색이 한 칸으로 접힘 · 계열 6묶음 · 고르면 바뀜(색소폰).

---

## 5단계 26/N — 믹서 전면 손질 (사용자 지적) (2026-08-16)

> "믹서가 쓰기 너무 어렵고 파라미터들이 너무 작아"

맞는 지적이었다. 한 줄에 슬라이더를 **다섯 개**(볼륨·좌우·잔향·저음·고음) 늘어놨는데,
400dp 화면에서 하나가 60dp 남짓이다 — **잡히지도, 원하는 값에 세우지도 못한다.**
스트립도 100dp 폭에 작은 노브 넷이 들어 있었다.

### 원칙 하나로 다시 짰다: **한 화면에 큰 것 하나**
- **줄(한눈에)**: 이름 · M/S · 톱니 + **볼륨 슬라이더 하나**(손잡이 반지름 11).
  믹서에서 열에 아홉은 볼륨만 만진다.
- **스트립(자세히)**: 작은 노브 넷을 빼고 **페이더를 260 → 최대 420px** 로 늘렸다.
  M/S 는 그대로(믹싱 중 제일 자주 누른다).
- **채널 시트**(줄의 톱니 · 스트립의 「좌우·잔향·EQ」 · 이름 누르기): 볼륨·좌우·잔향·
  저음·고음이 **화면 폭을 다 쓰는 슬라이더**(손잡이 반지름 12, 높이 40px)로 하나씩.
  값이 오른쪽에 크게 뜨고, **↺ 되돌리기**를 붙였다(가운데를 손으로 맞추기는 어렵다).
- 라이브 줄도 같은 모양 — 볼륨·잔향 둘 다 큼직하게(만지는 값이 둘뿐이라 다 넣을 수 있다).

### 폰에서 눈으로 잡은 것 둘
1. **M/S 버튼이 글자만 해졌다.** `_Toggle` 은 폭을 안 정하면 글자 크기로 줄어든다
   (예전엔 `Expanded` 안에 있어서 안 드러났다) → 34~38dp 로 못 박았다.
2. **스트립에서 M/S 가 아예 사라졌다** — 노브를 들어낼 때 같이 잘려 나갔다.
   시험에 `M/S 6개씩` 검사를 넣었다.

### 검증
`flutter test` — 18개 파일 통과. 믹서 시험 11항목:
스트립/줄 기본값 · 6트랙이면 줄 · 마스터 붙박이 · **스트립에 M/S** ·
**줄에는 볼륨만**(좌우·잔향 없음) · 줄 슬라이더 높이 30px · **채널 시트 큰 슬라이더 5/5** ·
시트 슬라이더 높이 40px · 가로 그대로 · 안 넘침.

**폰 실측(A17):** 스트립 페이더가 화면 대부분을 차지하고, 「좌우·잔향·EQ」를 누르면
시트가 큰 슬라이더 다섯 개로 열린다(드럼 · 볼륨 100% ↺ · 좌우 가운데 ↺ · 잔향 4% ·
저음 0 ↺ · 고음 0 ↺ · 이 트랙 지우기).

---

## 5단계 27/N — 곡 목록 (정렬 · 이름 바꾸기) (2026-08-16)

- **정렬**: 최근 · 이름 · 길이 (곡이 3개 넘을 때만 칩이 뜬다). 보기만 정렬하고
  **저장 파일의 순서는 안 건드린다.**
- **이름 바꾸기를 밖으로 꺼냈다** — ⋮ 안에 있으면 있는 줄도 모른다.
  곡 이름이 전부 '새 곡 2' 로 남는 이유가 그거다. 줄마다 연필 아이콘.
- ListTile 을 `Material(transparency)` 로 감쌌다 — 색 있는 상자 안에 그냥 두면
  **눌린 표시가 가려져서 눌러도 반응 없어 보인다**(Flutter 경고로 알려 준다).

### 위젯 시험에서 배운 것 — 파일을 쓰는 시험은 `runAsync` 안에서
위젯 시험은 **가짜 시계** 위에서 돈다. 진짜 파일 I/O 를 그냥 `await` 하면 영영 안
끝난다(시험이 통째로 멈췄다). `tester.runAsync(() async { … })` 안에서 해야 한다.
또 첫 곡의 기본 이름 '새 곡' 은 머리말의 **「새 곡」 버튼과 글자가 같아** 화면에서
곡 이름을 찾을 때 겹친다 — 시험에서는 다른 이름으로 바꿔 두고 본다.

### 검증
`flutter test` — 19개 파일 통과. `songs_ui_test.dart` 6항목:
곡 셋 · 정렬 칩 3개 · **이름순**(가나다→마바사→하하하) · **길이순**(206s→135s→135s) ·
줄마다 이름 바꾸기 버튼 · 눌러서 실제로 바뀜.

---

## 5단계 28/N — '없음' 트랙에서 빈 판 그리기 · 편집기 손질 (2026-08-17)

폰에서 「음 찍기」를 눌렀는데 **아무 일도 안 났다.** 거기서 시작해 세 가지를 고쳤다.

### 1) 패턴이 '없음' 이면 편집기가 안 열렸다
`onPressed: track.pattern == null ? null : ...` — 버튼이 꺼져 있었다. 그런데
`IconButton` 의 꺼진 색(white38)과 켜진 색(white54)이 거의 같아 **눈으로 구별이 안 된다.**
사용자에게는 그냥 고장이다.

빈 판에서 직접 그리는 것도 하나의 만드는 방법이므로 **항상 열리게** 했다.
`makeEditable` 이 이미 null 을 받아 빈 2마디를 만들어 준다.
씬 전체가 '없음' 이면 **무음 안내**를 띄운다(재생해도 소리가 안 나는 걸 그 자리에서 알려 준다).

### 2) 숨어 있던 버그 — 알림이 통째로 버려지고 있었다
`makeEditable` 은 편집기의 `initState` 에서 불린다. 즉 **화면을 짓는 도중**이다.
거기서 `notifyListeners()` 를 하면 Flutter 가 막는다:

```
setState() or markNeedsBuild() called during build.
```

이 예외는 **그 알림을 받을 위젯을 그냥 건너뛴다** — 뒤에 있던 씬 화면이 안 고쳐진다.
디버그에서 빨간 예외 3개가 뜨는데도 릴리즈에서는 조용히 틀린 화면이 남는다.

고친 방법: 값은 지금 바꾸고 **알림만 다음 차례로 미룬다**(`scheduleMicrotask`).
`Track.setPatternQuiet` / `Track.ping` 을 새로 뒀다.
미루면 그 사이에 대상이 사라질 수 있다(스타일을 바꾸면 트랙을 통째로 갈아 낀다)
→ `Track`·`Project` 에 `dispose` 표시(`_dead`)를 두고 살아 있을 때만 알린다.
`genre_keep_test` 가 `A Track was used after being disposed` 로 이걸 잡았다.

### 3) 편집기 — 작아서 못 쓰던 것들
- **줄 이름이 가로로 밀면 사라졌다.** 2마디로 점프하면 어느 줄이 킥인지 알 수 없다.
  이름 칸을 **가로 스크롤 밖으로** 뺐다. 세로는 두 목록을 짝지어 움직인다
  (`_vsLabel`/`_vsGrid` + `_syncLabels`). 이름도 10.5 → 11.5pt, 기준 줄은 굵게.
- **위쪽 바를 두 줄로.** 한 줄에 여섯 개를 넣었더니 폰에서 「신스 베이스 내 …」,
  「빈 칸=찍기 · 음=고…」 처럼 **다 잘렸다.** 위: 재생·이름·지우기 / 아래: 길이·보기.
- 돋보기 아이콘에 **글자**를 붙였다(「촘촘 보기」/「넓게 보기」). 아이콘만으로는 못 맞힌다.
- 마디 점프 칸 30×28 → **42×38**(실기기 약 6.5×5.8mm), `_Small` 32 → **38**.
- 마디 자 글씨 9.5 → 11.5pt.
- **빈 판 안내** 한 줄: 「칸을 누르면 음이 찍힙니다. 위로 갈수록 높은 음.」
- 아래 안내가 드럼에서도 '길이·글라이드' 라고 했다 → 드럼은 「세기」만.

### 검증
`flutter test` — **21개 파일 통과.** 새 시험 둘:
- `scene_ui_test.dart` 「씬이 통째로 비었을 때」 4항목: 무음 안내 · 빈 판도 편집기가 열림 ·
  빈 내 패턴이 생김(음 0개) · 하나 실리면 안내가 사라짐.
- `editor_ui_test.dart` 「빈 판 편집기」 3항목 + 「12-b) 줄 이름은 안 밀린다」:
  마디 칸 42×38 · 이름 온전 · 돋보기 글자 · 점프해도 이름 x 좌표 그대로(21 → 21).

**폰 실측(A17):** 드럼 편집기에서 2마디로 점프해도 킥·스네어·하이햇이 그대로 남는다.
세로로 밀어도 이름 칸이 같이 따라온다. 위쪽 바 두 줄 다 안 잘림. APK 설치 완료.

---

## 5단계 29/N — 곡 화면 머리 · 쇼 화면 밴드 (2026-08-17)

### 곡 화면 — 머리를 두 줄로
「곡 재생 · 전체 1:51 + 설명 · 내보내기 · 반복」 넷을 한 줄에 넣었더니
가운데 설명이 **두 줄로 깨져** 나왔다(「구간을 늘어놓고 ▶ 를 / 누르세요」 — 폰에서 확인).
편집기와 같은 처방:
- 위: **곡 재생(폭을 다 씀, 46px)** + 반복. 제일 많이 누르는 둘이다.
- 아래: 전체 길이 + 음/타격 개수(한 줄로 못 박음) + 내보내기.
- 글씨도 같이 키웠다(설명 10.5 → 12, 내보내기 12.5 → 13.5).

`song_ui_test` 에 「6-b) 위쪽 바 두 줄」 추가 — 400px 화면에서 재생 버튼이
**294px**(절반 넘음)이고 설명이 `maxLines: 1` 인지 본다.

### 쇼 화면 — 밴드가 작고, 드러머만 이름이 없었다
- **크기**: `scale` 을 폭으로만 정하고 1.0 에서 잘랐더니 폰 세로 화면에서
  사람이 작고 무대가 텅 비었다. **폭과 높이 둘 다** 보게 했다
  (`min(slot/96, h/360).clamp(0.55, 1.9)`) → 폰에서 약 1.4배.
- **드러머 이름표**: 앞줄 셋에는 이름이 있는데 드러머만 없었다. 단(riser) 아래에 붙였다.
  이름 그리는 코드를 `_label` 하나로 합쳤다(앞줄·드러머가 같은 모양).
- **스타일별 무대 조명**: 조명 기둥 셋의 색이 파랑·보라·주황 고정이었다.
  `kStageLights` 로 스타일 11개마다 다르게 — 로파이는 따뜻하게, 하우스는 차갑게,
  재즈는 술집 조명, 록은 빨강+흰빛. **곡이 바뀌면 화면 분위기도 바뀐다.**

### 검증
`flutter test` — **22개 파일 통과.** 새 시험 「무대 조명 색」 3항목:
스타일 11개 전부 색이 있다(빠지면 조용히 기본색으로 나온다) · 기둥은 셋씩 ·
11개 조합이 **서로 다르다**(다 같으면 넣으나 마나).

**폰 실측(A17):** 곡 화면 머리 두 줄 다 안 잘림. 쇼 화면에서 밴드가 커지고
「드럼」 이름표가 보이며, 로파이 조명(주황·보라·청록)이 들어왔다. APK 설치 완료.

---

## 5단계 30/N — 쇼: 서스테인 동안 계속 빛나기 (사용자 요청) (2026-08-17)

> "쇼모드에서 서스테인중이면 계속 빛나게하자"

맞는 지적이다. `ShowScore` 는 **음의 시작 시각만** 들고 있었다. 그래서 4초를 끄는
패드도 0.3초 만에 꺼졌다 — 소리는 나는데 화면은 조용한 상태다.

### 악보에 '끝'을 같이 담았다
음 이벤트는 `[voice, freq, durSec, vel, soft, glide, delaySec, part]` 다.
`delaySec`(시작)만 쓰던 것을 `(시작, 시작+durSec)` 쌍으로 모은다.

`trackEnds[i]` 에 넣는 값은 그 음의 끝이 **아니라** `0~i 번째 중 가장 늦게 끝나는
시각`이다. 이유: 패드가 4초 울리는 동안 짧은 음이 겹쳐 시작하면 이진 탐색은
**짧은 음**을 집는다. 그 음의 끝만 보면 아직 울리는 패드를 놓친다.

### 밝기는 세 토막
- **때림** — 시작 직후 0.3초 동안 1 에서 가파르게(²·²) 떨어진다
- **서스테인** — 음이 안 끝났으면 `kSustain = 0.45` 아래로 안 내려간다
- **릴리스** — 끝나고 `kRelease = 0.22초` 동안 스러진다(뚝 끊으면 깜빡여 보인다)

### 빛과 움직임을 갈랐다
`BandMember` 에 `hit`(서스테인 없는 타점)을 더했다.
- `level`(서스테인 포함) → **빛**: 후광·몸 색
- `hit`(타점만) → **움직임**: 무릎 굽히기·건반/기타/관악기 팔

안 갈랐으면 패드가 4초 내내 무릎을 굽히고 서 있는다. 굳은 것처럼 보인다.

### 곁다리 — 편성이 여섯이면 무대가 텅 비었다
사람 크기를 폭으로 정하니 6트랙이면 0.68배로 작아지는데, 드러머는 화면 높이의
고정 비율(0.34)로 띄워서 **사이가 텅 빈다**(폰에서 확인).
`y = floorY - max(h*0.20, 128*scale)` — 사람 키에 맞춰 띄우되 겹칠 만큼은 안 내린다.
칸 기준도 96 → 88 로 줄여 사람이 조금 커졌다.

### 검증
`flutter test` — **23개 파일 통과.** 새 시험 「서스테인·릴리스」 5항목.
진짜 곡은 음이 바로 이어져 릴리스 구간이 잘 안 나온다 → **손으로 만든 악보**
(2초에 시작, 3초 눌림)로 본다: 치기 전 0 · 치는 순간 1 ·
**누르는 3초 내내 0.45 아래로 안 내려감**(0.05초 간격 60번 전수 검사) ·
가운데는 정확히 서스테인 바닥 · 끝나면 잦아들어 0.22초 뒤 0.

**폰 실측(A17):** 재생 중 베이스·피아노·비브라폰이 **계속 후광을 두르고 있다**
(예전에는 깜빡였다). 드러머가 앞줄 가까이 내려와 밴드가 한 덩어리로 보인다.

---

## 5단계 31/N — 「이 트랙 지우기」가 손에 안 닿았다 (2026-08-17)

시험 찌꺼기(라이브 트랙)를 지우려고 폰에서 「이 트랙 지우기」를 **두 번 눌렀는데
아무 일도 안 났다.** 시트조차 안 닫혔다.

### 고장인지부터 가렸다 — 시험으로
눈으로 더 눌러 보는 대신 위젯 시험을 하나 썼다: 시트를 열고 그 버튼을 눌러
트랙 수가 줄어드는가. **통과했다**(6 → 5). 즉 코드는 멀쩡하고 **손가락이 문제**다.

그 버튼은 작은 글자 버튼(TextButton, 12.5pt)이 시트 **오른쪽 아래 구석**에 붙어
있었다. `SafeArea` 안이긴 하지만 안드로이드 내비게이션 바 바로 위 몇 dp 지점이라,
손가락 크기로 누르면 내비게이션 바를 먼저 문다.

### 고친 것
- 지우기를 **폭을 다 쓰는 46px 버튼**으로(368×46px). 아래 여백 18 → 26.
- **한 번 묻는다.** 트랙을 지우면 그 안에 찍은 음도 같이 사라지고 되돌리기가 없다.
  「'라이브' 트랙을 지울까요? / 이 트랙에 찍은 음도 같이 사라집니다. 되돌릴 수 없어요.」
  → 취소 · 지우기.

### 검증
`flutter test` — 23개 파일 통과. 믹서 시험에 4항목 추가:
지우기 버튼 크기(368×46) · **지우기 전에 묻는다**(아직 안 지워짐) ·
취소하면 그대로 · 「지우기」를 누르면 지워지고 **채널 시트도 같이 닫힌다**.

### 곁다리 — 시험 찌꺼기 정리(사용자 승인)
- 「새 곡 2」의 **라이브 트랙 삭제 완료**(7 → 6트랙). 폰에서 확인.
- 시험용 곡 「새 곡 3」은 **이미 없었다** — 곡 목록에 「새 곡 2」·「나무」 둘뿐이다.
- 믹서에 남아 있는 「라이브」 줄은 트랙이 아니라 **라이브 채널**(붙박이)이다.

### 다음 (사용자가 고른 순서)
1. **라이브 화면** — 아직 UI 손질을 안 한 유일한 주요 화면
2. 처음 켠 사람 안내
3. 소리 자체(음색·믹스) — 청감 확인 필요
4. 쇼 화면 연출 더

---

## 5단계 32/N — 라이브 화면 (2026-08-17)

UI 손질이 안 된 마지막 주요 화면. 폰에서 열자마자 둘이 눈에 띄었다.

### 1) 옥타브가 화면 밖에 있었다
`_ControlRow` 가 한 줄이었다: 음색 · 단음/화음/아르페지오 · 짧게/보통/길게 ·
옥타브 −/값/+ — 393dp 폭에 553dp 어치다. 처음 보이는 건 「…짧게 보통」 까지고
**「길게」도 잘려 있다.** 옥타브는 라이브에서 없으면 안 되는 값인데 **있는 줄도 모른다.**

두 줄로 갈랐다:
- 위: 음색 ▾ · 단음 · 화음 · 아르페지오 (325dp — 안 밀린다)
- 아래: 짧게 · 보통 · 길게 · ▼ **옥타브 기본** ▲

「−/+」만 두면 무엇의 −인지 모른다 → **지금 값을 가운데**에 놓고, 기본이 아니면
청록으로 물들여 눈에 띄게 했다.

### 2) 슬라이더 둘이 이름표 없이 아이콘만 달고 있었다
🔊 와 `blur_on`(⬤) 아이콘에 슬라이더를 한 줄에 반씩. 두 문제가 겹쳤다:
- `blur_on` 이 '잔향'으로 읽힐 리 없다 — **무엇을 만지는 값인지 모른다**
- 슬라이더가 100dp 남짓 — **원하는 값에 못 세운다**(믹서에서 이미 치른 값이다)

믹서와 같은 처방: **한 줄에 하나씩, 이름과 값을 붙여서.**
`_LiveSlider`(높이 36, 손잡이 반지름 10) — 「볼륨 100%」 「울림 18%」.
믹서 시트만큼(40+) 키우면 패드 자리를 먹으므로 36 으로 줄였다.
'잔향'이 아니라 **'울림'** 으로 적었다(믹서 시트에도 「잔향 (울림)」으로 되어 있다).

### 확인해 보니 이미 되어 있던 것
녹음을 누르면 **반주가 자동으로 켜진다**(`_toggleRec` → `_togglePlay`).
안 틀고 녹음하면 박자 기준이 없다.

### 검증
`flutter test` — 23개 파일 통과. 라이브 시험에 3항목 추가:
**옥타브가 화면 안에**(오른쪽 끝 306px / 화면 400) · 길이 셋 다 보인다(3/3) ·
볼륨·울림에 이름과 값이 붙어 있다.

**폰 실측(A17):** ▲ 를 누르니 「옥타브 +1」이 청록으로 바뀌고 패드가 C5/C6 로 올라갔다.
▼ 로 되돌려 놓았다. 패드는 그대로 크다(두 줄 × 7칸).

---

## 5단계 33/N — 처음 켠 사람 안내 (2026-08-17)

첫 화면에 버튼이 넷 있고 설명도 한 줄씩 붙어 있다. 그런데도 **소리를 내려면
무엇을 눌러야 하는지**는 어디에도 안 적혀 있다. 만들기에 들어가도 패턴이 '없음'
이면 재생해도 조용하다(28/N 에서 안내를 넣었지만, 그 화면까지 가야 보인다).

그래서 안내를 '이 화면은 무엇이다'가 아니라 **'무엇을 누르면 소리가 난다'**로 적었다.

### 무엇을 넣었나
첫 실행에 한 번 뜨는 시트 — 네 단계 + 막혔을 때 한 줄.
1. **만들기** — 악기 줄마다 「패턴」을 눌러 고른다. 고르는 순간 그 악기가 들어온다.
2. **곡으로 잇기** — 인트로·벌스·코러스로 늘어놓으면 한 곡. 「내보내기」로 파일.
3. **라이브** — 패드가 그 곡의 조에 맞춰져 있어 아무 데나 눌러도 틀린 음이 안 난다.
4. **쇼** — 밴드 화면.
+ 「소리가 안 나면 — 「만들기」에서 패턴이 전부 「없음」인지 보세요.」

### 두 가지를 지켜야 한다
- 처음에는 **저절로** 뜬다. 찾아 들어가야 하면 아무도 안 본다.
- 한 번 보면 **다시 안 뜬다**. 대신 첫 화면 「사용법」 으로 언제든 다시 연다.

`Store.guideSeen` 을 `index.json` 에 같이 적는다(값 하나뿐이라 파일을 따로 안 둔다).
밖을 눌러 닫아도 본 것으로 친다(`.then(onDone)`).

`HomeView` 를 `StatefulWidget` 으로 바꿨다. 띄우는 시점이 두 군데다:
- `initState` → **`addPostFrameCallback`**. 그 자리에서 열면 Navigator 가 아직 없다.
- `didUpdateWidget` → 저장소는 앱이 켜진 **뒤에** 읽힌다. 그때 `guideSeen` 을 알게 된다.
`_guideShown` 으로 한 실행에 한 번만 뜨게 막는다.

### 검증
`flutter test` — **24개 파일 통과.** 새 시험 `guide_ui_test.dart` 8항목:
첫 실행에 저절로 뜬다 · 네 단계 + 누를 것이 적혀 있다 · 무음 대처 ·
「시작하기」 누르면 닫히고 본 것으로 적힌다 · **다시 켜도 안 뜬다** ·
「사용법」으로 다시 열린다 · **껐다 켜도 `index.json` 에 남는다**(Store 를 새로 만들어 확인) ·
가로에서도 안 넘친다.

**폰 실측 못 했다** — 이 항목을 만드는 도중 폰 연결이 끊겼다(`adb devices` 비어 있음).
다음에 연결되면 볼 것: 첫 실행에 시트가 뜨는지 · 「시작하기」 뒤 다시 켜도 안 뜨는지 ·
첫 화면 「사용법」 버튼 자리. **APK 는 빌드만 되어 있고 설치 안 됨.**

---

## 5단계 34/N — 스타일마다 소리 크기가 달랐다 (2026-08-17)

귀로는 못 잡는 문제다. 스타일마다 GENRE_MIX 값이 따로라서 어떤 곡은 크고 어떤 곡은
작은데, **눈으로는 안 보이고 귀로는 기억이 안 난다.** 그래서 쟀다.

### 재는 방법에서 두 번 틀렸다
1. **앞 20초만 쟀다** → 빠른 스타일은 20초 안에 코러스가 들어오고 느린 스타일은
   인트로만 들어온다. '재즈가 트랩보다 14.8dB 작다'는 결론을 낼 뻔했다.
2. **코러스 씬만 쟀다**(빠르니까) → 객체형 스타일(트랩·재즈 등)은 **씬 클립과 곡의
   구간이 다르다.** 트랩 피크가 0.96 → 0.70 으로 떨어졌다. 같은 소리가 아니다.

결국 **곡 전체를 렌더**하고, 그 안에서 **제일 시끄러운 3초**를 견준다
(사람이 "이 곡 크다"를 정하는 건 전체 평균이 아니라 제일 큰 대목이다).

### 잰 값 (보정 전)
```
트랩 −7.7 · 힙합 −9.4 · 팝 −11.5 · 발라드 −12.2 · 엠비언트 −12.4 · 록 −12.6
시티팝 −13.3 · 프로그하우스 −13.5 · 로파이 −13.7 · 하우스 −14.2 · 재즈 −17.9
```
**10.2dB(체감 두 배쯤).** 곡을 바꿀 때마다 볼륨을 만져야 한다 = 고장이다.

### 고친 것 — `kStyleGain` (genre_mix.dart)
목표 −13dBFS. 다만 **완전히 평평하게는 안 맞춘다** — 재즈가 성긴 건 원래 그런
음악이고, 억지로 끌어올리면 리미터에 눌려 납작해진다. 보정 폭을 **−5 ~ +3.5dB** 로 묶었다.

값이 가는 길은 **사용자 마스터 페이더와 따로**다(섞으면 믹서에 뜨는 숫자와 실제
소리가 어긋난다):
- `Engine.styleGain` — `masterGain * userGain * styleGain`
- 아이솔레이트 명령 `_cStyleGain(14)` · `AudioClient.setStyleGain`
- `main.dart _syncBuses()` 에서 밀어 준다 — `setGenre` 가 `notifyListeners` 를
  부르므로 스타일이 바뀌면 자동으로 따라간다
- 내보내기도 같은 값(`song_view` → `masterVol: styleGain(genre)`) — 파일이
  **들리던 것과 같은 크기**여야 한다

### 결과 (보정 후)
```
트랩 −10.0 · 힙합 −11.2 · 팝 −12.1 · 발라드 −12.6 · 록 −12.6 · 엠비언트 −13.0
시티팝 −13.2 · 프로그하우스 −13.1 · 로파이 −13.3 · 하우스 −13.7 · 재즈 −15.2
```
**10.2dB → 5.2dB.** 클리핑 0. (리미터가 있어서 −5dB 를 넣어도 RMS 는 −2.3dB 만
줄어든다 — 그래서 트랩·재즈가 아직 양 끝에 있다.)

### `test/mix_meter.dart` — 이름이 `_test.dart` 가 아니다
11곡을 통째로 렌더하느라 **10분 넘게** 걸린다. `flutter test` 는 `_test.dart` 만
자동으로 집어가므로 평소 시험(24개, 6초)에는 안 낀다.
GENRE_MIX 나 kStyleGain 을 건드렸을 때만 손으로 돌린다:
```
flutter test test/mix_meter.dart
```

### 아직 안 된 것
- **청감 확인** — 숫자로는 붙였지만 실제로 들어 봐야 한다. 특히 재즈(+3.5dB)가
  납작해지지 않았는지, 트랩(−5dB)이 힘이 빠지지 않았는지.
- **폰 실측·설치 못 함** — 33/N 부터 폰 연결이 끊긴 상태다(`adb devices` 비어 있음).
  APK 는 빌드만 되어 있다.

---

## 5단계 35/N — 쇼 화면 연출: 관객 · 구간 전환 (2026-08-17)

### 폰 없이 그림을 확인하는 법을 만들었다 — `test/show_snap.dart`
폰 연결이 끊긴 채로 그림 작업을 해야 했다. `CustomPainter` 를 **화면 없이** 캔버스에
그려 PNG 로 떨어뜨린다(`PictureRecorder` → `toImage` → PNG). 나오는 곳 `/tmp/mdshow/`.
```
flutter test test/show_snap.dart
```
4장을 뽑는다: 로파이 4트랙 · 하우스 7트랙 · 구간전환 섬광 · 멈춤.
**이 그림이 진짜 버그를 둘 잡았다**(아래). `_test.dart` 가 아니라 평소 시험엔 안 낀다.
주의: 시험 환경엔 한글 폰트가 없어서 이름표가 □□ 로 나온다 — 폰에서는 멀쩡하다.

### 잡은 것 1 — 드러머가 앞줄 사람을 덮었다
편성이 셋뿐이면 사람이 커지는데(scale 1.5), 드러머 띄우는 높이는 `128 * scale` 이라
따라가지 못했다. **킥 드럼이 가운데 사람 머리를 덮었다.**
숫자를 근거로 다시 잡았다: 앞줄 사람은 발끝에서 머리까지 약 `110 * scale`,
드럼 세트는 기준점에서 아래로 약 `58 * scale` → 최소 `168 * scale` 을 띄워야 한다.
여유를 붙여 **185**.

### 잡은 것 2 — 관객이 도넛으로 보였다
관객 실루엣을 거의 검게(0xFF05070B) 칠했는데 **그 자리 배경이 이미 새까맣다.**
검은 데에 검은 걸 그리면 아무것도 안 보인다 — 테두리만 남아 도넛처럼 보였다.

> 실루엣은 어두워서 보이는 게 아니라 **밝은 배경 앞에 있어서** 보인다.

그래서 먼저 **무대 빛이 새어 나오는 띠**를 깔고 그 위에 새까만 사람을 앉혔다.
띠 위쪽은 투명에서 시작한다 — 바로 밝게 하면 가로줄 하나가 그어진 것처럼 보인다.
두 줄(뒷줄 11명 작게·흐리게 / 앞줄 8명 크게·진하게)로 겹쳐 '무리'로 보이게 했다.
킥에 맞춰 들썩이되 **사람마다 어긋나게**(다 같이 뛰면 기계 같다).
무작위는 안 쓴다 — 매 프레임 다시 그리므로 랜덤이면 관객이 순간이동한다. 사인파로 흩는다.

### 구간 전환 섬광
인트로 → 벌스 → 코러스가 **귀로만 넘어가면 화면은 계속 같아 보인다.**
`ShowScore.sectionPulse(t, decay)` 를 더했다(구간 시작에서 1, 0.7초에 걸쳐 0).
쇼 화면은 이 값으로 (1) 무대 전체를 살짝 환하게, (2) 조명 기둥을 세게 만든다.

### 검증
`flutter test` — **25개 파일 통과.** `show_check_test` 에 「구간 전환 섬광」 5항목:
넘어가는 순간 1 · 절반쯤에서 0.5 · 지나면 0 · **구간 한가운데는 0**(계속 번쩍이면
눈이 아프다) · 7구간이면 7번 다 켜진다.

**폰 실측·설치 못 함** — 33/N 부터 계속 끊긴 상태(`adb devices` 비어 있음).
APK 는 33·34·35/N 까지 들어간 것으로 빌드되어 있다. 연결되면 한 번에 설치·확인:
처음 안내 시트 · 스타일 보정 청감 · 관객과 구간 섬광.

---

## 5단계 36/N — 폰 없이 화면 보기 · 패턴 이름 한국어 (2026-08-17)

### `test/screen_snap.dart` — 화면을 PNG 로 뽑는다
폰이 계속 끊겨 있어서 만들었다. `RepaintBoundary` 를 씌우고 `toImage` 로 떨어뜨린다.
나오는 곳 `/tmp/mdscreen/`. 다섯 장: 첫화면 · 만들기 · 곡으로잇기 · 라이브 · 믹서.
```
flutter test test/screen_snap.dart
```
**한글 폰트를 실어야 한다** — 시험 환경에는 폰트가 하나도 없어 글자가 전부 □□ 로
나온다(35/N 밴드 그림에서 처음 겪었다). 맥의 `AppleGothic.ttf` 를 `FontLoader` 로
기본 폰트 이름('Roboto')에 실어 쓴다. 대신 **아이콘이 □ 로 나온다**(아이콘 폰트를
덮어써서) — 글자를 보려고 만든 도구라 그대로 둔다. 앱에는 영향 없다.

### 그림이 잡은 것 1 — 음색 칸이 좁아 「신스 ⋯」
트랙 이름 칸이 104dp 고정이라 음색 칸이 90dp 밖에 안 됐다. 이름 칸 **92** 로 줄이고
음색:패턴 비율을 3:4 → **4:5** 로 바꿨다.

### 그림이 잡은 것 2 — 패턴 이름만 영어였다
화면이 전부 한국어인데 패턴 칸만 「Lofi Walk C」 「House Off V」 였다.
**악보를 몰라도 쓰라고 만든 앱에서 제일 많이 누르는 칸**이 그랬다.

`lib/pattern_names.dart` 를 새로 뒀다. **이름 자체는 안 바꾼다** — 이름이 곧 키라서
씬의 클립·저장 파일·`kStylePrefixes` 가 전부 그 문자열로 묶여 있다. 바꾸면 예전에
저장한 곡이 자기 패턴을 못 찾는다. 그래서 **보여 줄 때만** 옮긴다.

낱말 표 하나로 213개를 덮는다(`<스타일> <구실> <자리>` 꼴이라 가능하다).
- `patternLabel` — 시트·편집기용 긴 꼴: `Lofi Walk C` → 「로파이 워킹 · 코러스용」
- `patternShort` — **트랙 줄용 짧은 꼴**: → 「워킹 코러스」.
  한 곡은 스타일 하나다 — 네 줄에 「로파이」 가 네 번 나올 이유가 없다. 이걸 안 하면
  긴 이름이 칸에서 잘린다(그림으로 확인하고 넣었다).
- 모르는 낱말은 **그대로 둔다.** 억지로 옮기다 틀린 말을 만드는 것보다 낫다.
- 코드 진행(`Cm-Ab-Eb-Bb`)은 영어가 아니라 **악보 기호**다 — 안 건드린다.
- 낱말로 쪼개면 어색한 몇 개는 통째로 적어 둔다(`Call & Answer` → 「묻고 답하기」).

### 검증
`flutter test` — **26개 파일 통과.** 새 시험 `pattern_names_test.dart` 6항목:
213개 다 모았다 · **영어가 안 남는다**(코드 기호 제외) · 옮긴 이름이 서로 안 겹친다 ·
읽어서 말이 된다(다섯 개 눈으로) · **짧은 이름이 8자 이하**(칸에서 안 잘리게) ·
모르는 이름은 그대로.

`scene_ui_test` 도 같이 고쳤다. 짧은 이름 「코러스」 는 **씬 칩과 글자가 같아서**
이름으로 찾으면 둘이 잡힌다 → 「패턴」 라벨로 칸을 집는다.

**폰 실측·설치 못 함** — 33/N 부터 끊긴 상태. APK 는 36/N 까지 빌드되어 있다.

---

## 5단계 37/N — 가로 화면 (그림으로 잡았다) (2026-08-17)

`screen_snap.dart` 에 **가로(800×400)** 도 뽑게 했다. 지금까지 시험은 「안 넘쳤다」
까지만 봤는데 — **안 넘쳐도 못 쓸 수 있다.** 셋 다 같은 병이었다:
*세로용으로 두 줄·세 줄로 나눈 것이 가로에서 그대로 남아 높이를 다 먹는다.*
가로는 **폭이 남고 높이가 모자라다.** 세로와 반대다.

### 라이브 — 패드가 화면의 1/3
설정이 4줄(씬·연주방식·길이옥타브·볼륨울림)이라 정작 두드릴 패드가 110px 였다.
가로에서는 연주방식+길이+옥타브를 **한 줄로**(800dp 에 641dp 어치라 다 들어간다),
볼륨·울림도 **한 줄에 둘로**. 패드 110 → **272px**(화면의 68%).

### 만들기 — 트랙이 두 줄만 보였다
위쪽 설정(재생·템포·조·안내)이 400px 중 300px 을 먹었다. **만들기 화면의 주인공은
트랙이다.** 가로에서는 조 칩을 재생 줄로 올려 붙여 한 줄로 만들었다.

### 첫 화면 — 넷 중 둘 반만 보였다
큰 버튼 넷이 세로로 쌓여 있었다. 가로에서는 **두 개씩 두 줄**(2×2) — 스크롤 없이
네 개와 「사용법」 까지 다 들어온다.

### 판정 기준
`MediaQuery.of(context).size.height < 520` 하나로 통일했다.
폭이 아니라 **높이**로 보는 게 맞다 — 문제가 '높이가 모자라다' 이기 때문이다.
(태블릿 세로처럼 폭도 높이도 넉넉한 경우를 가로 취급하면 안 된다.)

### 검증
`flutter test` — **26개 파일 통과.** 가로 항목 5개 추가:
- 첫 화면: 「만들기」와 「곡으로 잇기」가 **같은 높이에 나란히**(y212) · 넷 다 화면 안
- 만들기: 조 칩이 재생과 **같은 줄**(둘 다 y31)
- 라이브: 볼륨·울림이 **같은 줄**(둘 다 y119) · 설정이 y128 에서 끝나 **패드 272px**

좌표로 검사하는 이유: 「안 넘침」은 통과하는데 정작 못 쓰는 배치를 이 방법으로만 잡는다.

---

## 5단계 38/N — 가로 믹서 (2026-08-17)

37/N 에서 첫화면·만들기·라이브를 고쳤는데 **믹서가 남아 있었다.**
줄 하나가 **168px** 이라 400px 화면에 트랙 세 줄이 들어가고, 마스터가 라이브 줄을
덮고 있었다(그림으로 확인).

원인은 같다 — 세로용으로 **이름·M/S/톱니** 한 줄, **볼륨** 한 줄로 나눈 것이
가로에서 그대로 남았다. 폭은 1600px 이 남는데 높이를 두 배로 쓴다.

### 고친 것
`Flex(direction: wide ? horizontal : vertical)` 하나로 두 줄 ↔ 한 줄을 가른다.
- 이름·버튼 묶음은 **고정 280dp**(`_wrapHead`), 볼륨은 남는 폭 전부(`_wrapVol`)
- 왼쪽 색 띠도 62 → 34 로(줄이 낮아졌으니)
- 라이브 줄도 같은 규칙 — 이름·악기·볼륨·잔향이 **한 줄**에 들어간다

줄 168 → **112px**. 4트랙 + 라이브 + 마스터가 **스크롤 없이** 다 보인다.

### `wide` 를 위젯 안에서 재면 안 됐다
`_TrackRow` 는 가로 화면뿐 아니라 **세로의 '한눈에' 보기**도 쓴다. 위젯 안에서
`MediaQuery` 를 보면 세로 400dp 에서도 한 줄로 붙어 버린다(거기선 두 줄이 맞다).
→ 부르는 쪽(`_buildLandscape`)이 `c.maxWidth >= c.maxHeight` 로 정해서 넘긴다.

### 검증
`flutter test` — **26개 파일 통과.** 믹서 가로 2항목:
이름과 **첫 줄 슬라이더가 같은 높이**(둘 다 y31) · **마스터 아래끝 y361**(화면 400 안).

시험에서 값 글자('100%')로 줄을 집으려다 틀렸다 — 재즈 프리셋은 트랙마다 볼륨이
달라서 첫 '100%' 가 **셋째 줄**이었다(y312). 슬라이더로 집으니 맞았다.

---

## 5단계 39/N — 편집기 (세로·가로) (2026-08-17)

`screen_snap.dart` 에 **편집기**를 추가했더니(그동안 안 뽑고 있었다) 셋이 나왔다.

### 1) 격자 아래가 화면의 40% 나 비었다
줄 높이를 `_kRow = 28` 로 못 박아 뒀는데 **드럼은 레인이 10개뿐**이다.
10×28 = 280px 인데 화면은 800px 이라 아래가 통째로 비었다.

`rowHeightFor(h, rows) = (h/rows).clamp(28, 46)` — 자리가 남으면 칸을 키운다.
**큰 칸이 곧 누르기 쉬운 칸이다**(사용자가 계속 지적해 온 그 문제다).
드럼 28 → **46px**. 칸도 줄 높이를 꽉 채우게 `CrossAxisAlignment.stretch` 로 늘렸다.

### 2) 가로에서 레인 10개 중 2.5개만 보였다
위쪽 바 두 줄이 400px 중 300px 을 먹었다. 다른 화면과 같은 처방 — 가로면 한 줄로
(재생 · 이름 · 길이 · 촘촘보기 · 지우기가 800dp 에 다 들어간다). 2.5 → **6줄**.

### 3) 아래 바가 안내 한 줄인데 86dp 를 먹었다
아무것도 안 골랐을 때는 「찍은 칸을 누르면 …」 한 줄뿐인데 높이가 86 고정이었다.
가로에서 그 86dp 가 격자 두 줄이다. 비었을 때 **42dp** 로 줄였다.

### 시험 도우미가 틀렸던 것
`_cell()` 이 격자 좌표를 잴 때 `ListView 높이 / 줄 수` 로 줄 높이를 구했다.
줄 높이에 **상한(46)** 이 생기면서 격자가 뷰포트를 다 안 채우게 되자,
이 계산이 실제보다 크게 나왔다(57 vs 46). `rowHeightFor` 를 시험에서도 그대로 부르게
고쳤다 — **화면과 시험이 같은 식을 쓰게** 하는 게 핵심이다.

### 검증
`flutter test` — **26개 파일 통과.** 편집기 3항목 추가:
가로에서 재생·촘촘보기가 **같은 높이**(둘 다 y29) · 자리가 남으면 줄이 **46px** 로 커짐 ·
빈 아래 바의 안내 글 높이 17px.

---

## 5단계 40/N — 폰 실측 (33~39/N 밀린 것 한 번에) (2026-08-17)

### 폰이 안 잡히던 것
`adb devices` 가 비어 있었다(33/N 부터 일곱 덩어리 동안). 케이블 문제가 아니라
**adb 서버가 죽어 있었다** — `adb kill-server` 후 다시 띄우니 바로 붙었다.
다음에 또 안 잡히면 이것부터:
```
~/Library/Android/sdk/platform-tools/adb kill-server && ~/Library/Android/sdk/platform-tools/adb devices -l
```

### 확인된 것
- **33/N 첫 실행 안내** — 앱을 켜니 시트가 떴다. 「시작하기」 뒤 **앱을 완전히 껐다
  켜도 다시 안 뜬다**(`index.json` 의 `guideSeen` 이 살아 있다). 「사용법」 버튼 제자리.
- **35/N 쇼** — 관객 실루엣이 무대 빛 앞에 제대로 보인다. 드러머 이름표 · 힙합 조명.
  재생 중 **베이스·피아노·비브라폰이 계속 빛나고**(서스테인) 색소폰·기타는 조용하다.
- **34/N 소리** — 재생 20초 동안 **Underruns 0**. 스타일 보정이 오디오 경로를 건드렸는데
  끊김 없다. **다만 청감 확인은 아직 사장님 몫이다.**
- **36/N 패턴 이름** — 「붐 코러스」 「808 코러스」 「먼지낀 건반 코러스」 로 나온다.
- **39/N 편집기** — 드럼 레인 10개가 **한 화면에 다 들어오고** 칸이 46px 로 커졌다.

### 폰에서 새로 잡은 것 둘
1. **「시작하기」 글자가 보라색이었다.** `FilledButton` 에 `foregroundColor` 를 안 잡으면
   테마 기본색이 나온다 — 청록 바탕에서 안 읽힌다. `scene_view` 에 이미 같은 주석이
   있었는데 새 버튼에서 되풀이했다. 흰색으로 못 박았다.
2. **긴 이름이 여전히 잘렸다** — 「먼지낀 건반 코…」 「업라이트 …」.
   시험은 '짧은 이름 8자 이하'로 통과했지만 **실제 칸에는 6자쯤 들어간다.**
   칸을 더 넓히는 건 한계가 있어서(줄이 하나뿐인 화면이 아니다) `_Field` 를
   **두 줄까지** 쓰게 했다(11.5pt · 줄간격 1.15). 이제 전부 다 보인다.

### 남은 것
- **청감 확인** — 스타일 보정(34/N)이 재즈를 납작하게, 트랩을 힘없게 만들지 않았는지.
  하우스·트랩·재즈를 차례로 틀어 보고 **볼륨을 만지고 싶어지는지**가 판정 기준이다.

---

## 5단계 41/N — 쇼: 연주자를 사람으로 (사용자 요청) (2026-08-17)

> "쇼 모드 플레이어들 더 디테일하게 — 실제 연주자들 움직임, 패션, 같은 악기라도
>  플레이어를 조금씩 다르게" / "다리 더 자연스럽게" /
> "베이스·기타는 손을 계속 움직이는 게 아니라 음의 높낮이에 따라 넥·헤드 쪽으로
>  노트가 나오는 순간에 움직이도록" / "드러머도 너무 이상해"

### 1) 사람마다 다르게 — `_Persona`
같은 모양 셋이 나란히 서 있으면 밴드가 아니라 **아이콘 세 개**다.
그렇다고 무작위로 흩으면 `paint` 가 초당 60번 불리므로 **매 프레임 다른 사람**이 된다.
→ **이름에서 뽑은 씨앗**으로 정한다. 같은 트랙은 언제나 같은 사람이다.

여섯 가지가 다르다: 키 · 몸집 · 머리(짧은머리·긴머리·상투·캡·비니) ·
옷(티셔츠·재킷·후드·민소매) · 다리 벌린 정도 · **리듬 타는 위상**.
위상이 제일 중요하다 — 다 같이 움직이면 사람이 아니라 기계로 보인다.

### 2) 악기는 **음색**이 정한다 (`kBandInstrument`)
「기타」라고 이름 짓고 나일론 기타를 골랐는데 화면에서 **색소폰을 불고 있었다**
(트랙 종류가 melody 라서). 폰에서 잡았다. 이제 음색으로 고른다.

`kVoiceFamily`(instruments.dart)를 그대로 못 쓴다 — 그건 **소리 내는 방식** 기준이고
화면은 **자세** 기준이다. 마림바·벨·하프는 '뜯고 치는' 이지만 자세는 건반이다
(마림바 연주자가 기타를 메고 있었다 — 이것도 폰에서 잡았다).

### 3) 손이 음을 따라간다
기타리스트 왼손이 사인파로 **계속 흔들리고 있었다.** 그건 연주가 아니라 떨림이다.
`ShowScore.trackPitch` 를 더했다 — 음마다 그 트랙 안에서의 높이(0~1)를 미리 접어 둔다
(옥타브는 곱셈으로 올라가므로 **로그**로 잰다. 사람 귀와 같은 자다).
`pitchAt(t)` 는 **음이 바뀔 때만** 값이 바뀌므로, 손도 그때만 자리를 옮긴다.
높은 음일수록 헤드 쪽으로 올라간다.

### 4) 다리에 무릎이 생겼다
엉덩이에서 발까지 직선 하나였다 — 무릎을 굽혀도 막대가 짧아졌다 길어질 뿐이다.
허벅지 → 무릎 → 정강이로 꺾고(칠 때 무릎이 앞으로), 발은 작은 타원.
한쪽 발은 박자에 맞춰 앞꿈치를 든다(발장단).

### 5) 드러머 — 앉아서 친다
킥 드럼을 **몸통 자리**에 그려서 사람이 드럼 위에 얹힌 것처럼 보였다.
실제 드럼은 드러머가 뒤에 앉고 세트가 앞을 가린다 → 그리는 순서를 그렇게 바꿨다:
스탠드 → 심벌 → **드러머(앉은 다리·몸·팔)** → 탐 → 스네어 → 하이햇 → 킥.
스틱은 **치는 북을 겨눈다**(안 칠 때는 들려 있다). 발은 킥에 맞춰 페달을 밟는다.

### 6) 팔·악기에 어두운 테두리
팔이 몸통과 같은 색이라 **묻혀서 안 보였다**(건반 연주자는 팔이 없어 보였다).
테두리를 먼저 깔고 그 위에 색을 얹는다. 건반도 허리 높이까지 내리고 넓혔다 —
가슴 높이에 두면 팔이 짧아서 몸통 뒤에 숨는다.

### 검증
`flutter test` — **27개 파일 통과.** 「연주자 악기」 8항목:
음색이 악기를 정한다(나일론 → 기타) · 마림바는 건반 자세 · 모르는 음색은 트랙 종류로 ·
**음마다 높이가 있다**(92개) · 0~1 로 펴진다 · **음 사이에는 안 움직인다**(차이 0).

`test/show_snap.dart` 에 「크게」(악기별 두 명) 와 「같은악기 다섯」 을 더했다 —
개성 코드가 죽으면 다섯이 똑같이 보여서 바로 드러난다.

**폰 실측(A17):** 베이스=기타 · 피아노/비브라폰=건반 · 색소폰=관악기 · 기타=기타.
다섯이 머리·옷·자세가 다 다르다. 드러머가 세트 뒤에 앉아 스틱으로 친다.

---

## 5단계 42/N — 실제 연주 자세로 (사용자 요청) (2026-08-17)

> "실제 악기 플레이어들이 연주하는 모습이 어떤지 분석하고 개선해 / 드럼은 앉아서
>  치니까 하체가 안 보여도 되잖아 / 드럼은 보통 왼손과 오른손이 교차되어 연주해"

### 드럼 — 크로스 그립
오른손잡이 드러머의 실제 배치를 따랐다. **우리는 정면에서 보므로 좌우가 뒤집힌다:**
- 하이햇은 드러머 **왼쪽** → 화면 **오른쪽**
- 스네어는 몸 바로 앞
- 라이드는 드러머 오른쪽 → 화면 왼쪽 / 크래시는 화면 오른쪽 위

그래서 **드러머의 오른손이 화면 왼쪽 어깨에서 나와 오른쪽 하이햇까지 건너간다.**
그게 크로스 그립이다. 드럼을 쳐 본 사람이면 팔이 안 겹친 그림을 보고 바로
"저건 드럼 치는 게 아니다" 라고 안다.

**한 번 반대로 그렸다** — 팔 둘이 나란히 오른쪽만 가리켜서 교차가 안 보였다.
좌우가 뒤집힌다는 걸 코드에 주석으로 박아 뒀다.
그리는 순서도 중요하다: **스네어 치는 손이 아래**, 하이햇 치는 손이 그 위.

**하체는 안 그린다.** 앉아 있으면 킥 드럼과 스네어에 완전히 가린다 —
그려 봐야 통 뒤에서 삐져나온 막대로만 보인다.

### 건반 — 손이 좌우로 옮겨 다닌다
실제 건반 연주는 **어깨가 거의 안 움직이고 손이 좌우로 간다.** 높은 음일수록 오른쪽.
`pitch` 로 두 손의 가운데를 정하고, 한 옥타브쯤 벌려 잡는다.
두 손은 여전히 **엇갈려** 내려간다(같이 누르면 인형이다).

### 색소폰 — 벨이 아래를 향한다
위로 들면 트럼펫이다. 색소폰은 몸 앞에 **거의 세로로**(60°) 매달리고 마우스피스만
입에 온다. 큰 음을 낼 때 잠깐 들어 올린다.
0.6rad(35°)로 그렸더니 수평으로 보여 트럼펫과 구별이 안 됐다 → 1.05rad 로.
관 길이도 34 로 늘리고 두 손을 관 위아래에 얹었다.
`sax`·`clarinet` 만 아래로, `trumpet`·`flute`·`brass` 는 앞·위 그대로.

### 검증
`flutter test` — **27개 파일 통과.**
**폰 실측(A17):** 드러머가 세트 뒤에 앉아 **팔을 교차해서** 친다(하체 없음).
색소폰이 아래로 매달린다. 건반 두 명이 서로 다른 자리를 짚는다.

---

## 5단계 43/N — 편집기 되돌리기 (2026-08-17)

### 왜 지금인가
편집기의 「지우기」는 **확인도 없이 판 전체를 날린다.** 위쪽 바 오른쪽 끝의 작은
아이콘이라 잘못 누르기도 쉽다. 그런데 이 앱에는 되돌리기가 **어디에도 없었다** —
방금 30분 찍은 걸 날리면 기억으로 복원해야 한다.

믹서의 트랙 지우기(31/N)는 확인 창으로 막았지만, 편집기는 그것만으로는 부족하다.
음 하나를 잘못 지우는 일이 훨씬 잦기 때문이다. 그래서 **되돌리기**를 넣었다.

### 어떻게
고치기 **직전**의 판을 복사해서 쌓는다(`_snapshot`). 얕은 복사면 안 된다 —
같은 목록을 가리키면 되돌려도 그대로다.
판 하나는 커야 음 100개 남짓이라 **20벌을 들고 있어도 부담이 없다.**

고치는 길이 열두 군데다(찍기·지우기·길이·세기·글라이드·옮기기·드럼 세기·
드럼 옮기기·드럼 지우기·마디 수·비우기) — **전부** 앞에 스냅샷을 건다.
하나라도 빠뜨리면 "어떤 건 되돌아가고 어떤 건 안 되는" 더 나쁜 상태가 된다.

버튼은 위쪽 바에 ↺ 로. 되돌릴 게 없으면 흐려진다.

### 검증
`flutter test` — **27개 파일 통과.** 편집기 시험 4항목 추가:
음을 더 찍는다(1 → 4개) · 「지우기」로 0개 · **되돌리면 4개가 그대로 살아난다** ·
한 걸음씩 더 되돌리면 2개.

**폰 실측(A17):** 드럼 편집기에서 탐을 하나 찍고 ↺ 를 누르니 원래대로 돌아왔고,
되돌릴 게 없어지자 버튼이 다시 흐려졌다.

---

## 5단계 44/N — 편집 제스처 · 「쉬기」 확인 (사용자 요청) (2026-08-17)

> "씬에서 악기 지우기 누르면 바로 없어지잖아 그것도 확인창으로 막고 / 노트를 꾹 눌러
>  옆으로 = 길이, 위아래 = 벨로시티 / 빈 칸을 누른 채 옆으로 끌면 쫘르륵 깔리게"

### 꾹 눌러 끌기 — 왜 '꾹' 인가
격자는 **가로로 스크롤**된다. 그냥 끌면 화면이 밀린다. 길게 누르면 스크롤과 안 싸운다
(누르고 있는 동안은 스크롤이 안 잡는다). 사용자가 말한 "꾹눌러서" 가 정확히 맞는 방법이었다.

- 있는 음 꾹 + **좌우** → 길이(칸 하나당 한 스텝)
- 있는 음 꾹 + **위아래** → 세기(20px 당 한 단계, 위가 세게)
- 빈 칸 꾹 + **아무 방향** → 지나가는 칸마다 **쫘르륵** 깔린다

되돌리기(43/N)와 맞물린다: **끄는 동안은 스냅샷을 한 번만** 쌓는다.
안 그러면 다섯 칸 깐 걸 되돌리는 데 다섯 번 눌러야 한다.

제스처는 **알려 주지 않으면 아무도 못 찾는다** → 아래 바(아무것도 안 골랐을 때
비어 있는 자리)에 「꾹 눌러 옆 = 길이 · 위아래 = 세기 · 빈 칸을 끌면 쫘르륵」 을 적었다.

### 「쉬기」 확인
⊖ 는 사실 삭제가 아니라 「이 트랙 쉬기」인데, 확인도 없이 바로 빠지고 **무엇이
빠졌는지도 안 알려 줬다.** 되돌릴 수는 있지만(패턴을 다시 고르면 된다) 무엇이 실려
있었는지 기억하고 있어야 한다 — 그게 사실상 되돌리기 없음이다.
이제 「'드럼' 을 쉬게 할까요? / 지금 실린 「코러스」 가 빠집니다」 라고 **이름을 대고** 묻는다.

### 검증
`flutter test` — 27개 파일 통과. 편집기 4항목 · 씬 3항목 추가.
끌면 5개가 깔린다 · **끈 건 한 번에 되돌아간다** · 옆으로 끌면 2칸→5칸 ·
위로 끌면 세기 2→3 · 쉬기 전에 묻는다 · **무엇이 빠지는지 이름을 댄다** · 취소하면 그대로.

---

## 5단계 45/N — 인서트 플러그인 뼈대 (사용자 요청) (2026-08-17)

> "믹서에 밴드 플러그인들을 넣자 기타·베이스 앰프, 여러 종류의 딜레이, 오버드라이브,
>  디스토션 / 옵토컴프, FET컴프, VCA컴프, 리버브 3종, 딜레이 3종, 그래픽 EQ, 리미터 /
>  로직의 플러그인 방식처럼" · "앰프는 클린·크런치·하이게인 (펜더·복스·마샬 성향)"

물어본 것: **어디부터** / **인서트 몇 칸**.
답: **인서트 뼈대 + 로직식 UI 부터**, 칸은 **제한 없이**.

### 열넷을 손으로 그리지 않는다 — 플러그인이 자기 손잡이를 설명한다
`FxParam(key, label, min, max, def, unit, choices)` 를 플러그인마다 들고 있고
(`kFxCatalog`), 화면은 그 표를 읽어 손잡이를 만든다.
**새 플러그인을 더해도 UI 코드는 안 는다.** 열넷을 각각 그리면 화면 열네 개를
따로 관리하게 된다 — 그 길로 가면 끝이 없다.

### 소리는 샘플 하나씩
`step(l, r)` → `outL`/`outR`. 배열을 안 만든다 — 초당 48000번 × 트랙 수 × 인서트 수라
할당이 생기면 그 자리에서 끊긴다(엔진의 기존 방식과 같다).

### 지금 만든 셋
- **앰프** — 클린(펜더)/크런치(복스)/하이게인(마샬). 셋을 가르는 건 게인 양이 아니라
  *어디가 도드라지고 어떻게 뭉개지는가* 다: 펜더는 중역이 파이고(−4dB@500), 복스는
  중역이 솟고(+6dB@2k), 마샬은 프리앰프를 **두 번** 통과시킨다.
  게인 단 **앞에서 저역을 깎는다**(90/110/150Hz) — 안 깎으면 하이게인이 뭉갠다.
  진공관은 위아래를 **다르게** 뭉갠다(비대칭) — 대칭으로 자르면 홀수 배음만 나와
  트랜지스터처럼 딱딱해진다.
  **캐비닛 흉내가 없으면 전부 지직거리는 소리로만 들린다** — 진짜 앰프 소리의 절반은
  스피커가 고역을 잘라 주는 데서 온다. 로우패스 + 존재감 봉우리를 마지막에 뒀다.
- **딜레이** — 시간/반복/어둡게/섞기/핑퐁. 되돌아올수록 어두워진다(테이프처럼).
- **컴프** — 옵토/FET/VCA. 어택·릴리스·무릎이 다르다(진짜 기기의 차이가 거기 있다).

### 채널 스트립에 자리
`TrackMix.inserts` — **EQ 뒤, 팬·페이더 앞**(로직과 같은 자리).
**순서가 곧 소리다** — 컴프 뒤 드라이브와 드라이브 뒤 컴프는 완전히 다른 소리다.
그래서 목록을 그대로 쓴다(정렬하지 않는다).

### 검증 — `test/fx_check_test.dart` 10항목
이펙트는 **틀려도 그럴듯하게 들린다.** "뭔가 달라지긴 했네" 로 넘어가면 게인만
바뀌고 왜곡은 안 걸린 걸 못 잡는다. 그래서 숫자로 본다:
```
앰프 배음 비율   클린 2.4% < 크런치 13.8% < 하이게인 26.4%
게인은 볼륨이 아니다  0.1 → 0.9 에서 크기 0.53배
딜레이 100ms      정확히 4800샘플 · 반복 0.7 에서 9번
섞기 0            원음과 한 비트도 안 다름
컴프              작은 소리 1.00배 · 큰 소리 0.19배
컴프 반응 속도    FET 5 < VCA 84 < 옵토 418 샘플
채널에 꽂힌다      0.504 → 0.608 · **끄면 0.504006510 그대로**
```

### 다음 (이 위에 얹기만 하면 된다)
1. 아이솔레이트 명령(인서트 꽂기·순서·파라미터) + 저장
2. 로직식 UI — 채널마다 인서트 랙, 칸을 누르면 그 플러그인 창
3. 나머지 11개: 리버브 3종 · 딜레이 3종(테이프/디지털/핑퐁) · 그래픽 EQ · 리미터 ·
   베이스 앰프 · 디스토션
4. **악기 소리 자체**(사용자 요청) — 기타·베이스·드럼·건반의 어택/디케이/서스테인/릴리스

---

## 5단계 46/N — 로직식 인서트 (배선 · 저장 · 화면) (2026-08-17)

45/N 에서 만든 DSP 를 **실제로 꽂을 수 있게** 이었다. 네 군데를 다 이어야 한다 —
하나라도 빠지면 "되는 것 같은데 안 되는" 상태가 된다.

### 1) 아이솔레이트 배선
명령 셋을 더했다:
- `_cInserts(15)` — 그 버스의 인서트를 **통째로** 갈아 끼운다(순서가 바뀔 수 있으니).
  **손잡이 값도 같이** 보낸다 — 저장해 둔 곡을 열 때 값이 기본값으로 튀면 안 된다.
- `_cFxParam(16)` — 손잡이 하나. 끄는 동안 초당 수십 번 오므로 제일 가벼운 길.
- `_cFxOn(17)` — 켜고 끄기.

`_cBus` 가 쓰던 버스 찾기 삼항 연산을 `_busOf()` 로 빼서 넷이 같이 쓴다.

### 2) 저장
`FxSlot` 을 껍데기(type, on)에서 **type + on + 손잡이 값**으로 바꾸고 `Track.toJson`
에 `fx` 로 넣었다. 옛 저장 파일에는 `fx` 가 없다 → 빈 목록으로 읽힌다(안 깨진다).

`kFxOrder`(웹에서 옮겨온 `['eq','cho','comp','rev','dly']`)는 **엔진이 읽은 적이 없는
죽은 뼈대**였다. 트랙을 만들 때 그걸로 채우던 것을 없애고 빈 목록으로 시작한다.

### 3) 재생·내보내기 양쪽
- `pushMix` 가 인서트도 같이 보낸다 — **편성을 다시 잡으면 버스가 새 객체라 비어 있다.**
  안 보내면 곡을 다시 틀 때마다 꽂아 둔 게 사라진다.
- `ExportJob.inserts` + `insertSnapshot` — **내보낸 파일이 들리던 것과 같아야 한다.**
  빼먹으면 앰프를 걸어 놓고 뽑았는데 깨끗한 소리가 나온다. '나중에' 로 미루기 쉬운 자리다.

### 4) 화면 — `lib/ui/fx_rack.dart`
로직과 같은 규칙:
- 칸을 누르면 **그 플러그인 창**이 열린다
- 전원 버튼으로 끈다(**빼는 게 아니라 지나가게만** — 켜고 끄며 비교하는 게 믹싱이다)
- 손잡이를 잡고 **끌어서 순서**를 바꾼다
- 칸에 지금 설정이 한 줄 요약으로 보인다(창을 안 열어도 「크런치」인지 안다)

**플러그인 창은 손으로 안 그린다.** `FxParam` 설명을 읽어 만든다 —
고르는 값(성향·방식)은 칩, 나머지는 큰 슬라이더 + 값 + ↺ 되돌리기.
열넷을 다 넣어도 이 파일은 안 커진다.

### 앰프에 톤 스택을 붙였다 (사용자 요청)
> "모든 플러그인들은 필요한 파라미터들 모두 조작할 수 있게" · "캐비닛 시뮬은 기본으로"

「톤」 하나였던 것을 실제 앰프 패널대로 **베이스·미들·트레블·프레즌스**로 나눴다.
미들의 중심 주파수는 성향마다 다르다(펜더 400 · 복스 700 · 마샬 650Hz).
**캐비닛은 손잡이가 없다** — 끄는 스위치 없이 신호가 반드시 지나간다.
성향마다 캡이 다르다(펜더 5.2k · 복스 4.6k · 마샬 4.0kHz — 마샬이 제일 어둡다).

### 그림으로 잡은 것
「성향」 칩 셋이 **한 줄에 하나씩 늘어졌다.** `Container(alignment:)` 를 주면 최대 폭으로
늘어난다 — **이 프로젝트에서 세 번째 겪는 함정이다**(씬 칩 · 믹서 M/S 에 이어).
`Row(mainAxisSize: min)` 으로 고쳤다.

### 검증
`flutter test` — **28개 파일 통과.** `fx_check_test` 에 4항목 추가:
꽂힌다(amp → delay) · **순서를 바꾼다**(delay → amp) ·
**저장·되살리기**(2칸 · 게인 0.8 · 딜레이 꺼짐까지 그대로) · 내보내기에도 실린다.

`screen_snap.dart` 에 인서트 랙 · 고르기 · 플러그인 창 세 장을 더했다.

**폰 실측 못 함** — 이 항목 도중 케이블이 다시 빠졌다. APK 는 빌드되어 있다.

### 남은 것
- 나머지 11개: 리버브 3종 · 딜레이 3종(테이프/디지털/핑퐁) · 그래픽 EQ · 리미터 ·
  베이스 앰프 · 디스토션 — **`kFxCatalog` 에 한 줄씩 더하면 화면은 저절로 생긴다**
- 마스터에도 인서트(지금은 트랙만)
- 악기 소리 자체(어택/디케이/서스테인/릴리스)

## 5단계 47/N — 나머지 플러그인 + 마스터 인서트 (사용자 요청) (2026-08-20)

> "3종류의 리버브, 3종류의 딜레이, 그래픽 이큐, 리미터 … 믹싱 **마스터링**할 수 있게"

46/N 에서 뼈대를 세웠으니 여기서는 **목록을 채우고 마스터에 통로를 냈다.**

### 채운 것 — 8종으로 열넷을 덮는다
플러그인마다 클래스를 따로 만들면 열넷이 열넷이 된다. 성향이 다른 것끼리는
**「모드」 손잡이 하나로 묶었다** — 안이 다른 회로면 묶고, 겉만 다르면 안 묶는다.

| 플러그인 | 안에 든 것 | 묶은 방법 |
|---|---|---|
| 앰프 | 클린(펜더)·크런치(복스)·하이게인(마샬) | 성향마다 프리HP·보이싱·미들 주파수·캡이 다르다 |
| 베이스앰프 | — | 250Hz 로 갈라 **고역만** 일그러뜨린다(저역이 뭉개지면 베이스가 아니다) |
| 드라이브 | 오버드라이브·디스토션·퍼즈 | tanh / 하드니 / tanh×8 |
| 딜레이 | 디지털·테이프·핑퐁 | 테이프는 되돌아올수록 어두워진다(damp +0.3) |
| 컴프 | 옵토·FET·VCA | 어택 30ms / 0.4ms / 6ms, 무릎 12 / 3 / 6dB |
| 리버브 | 룸·플레이트·홀 | 콤 필터 길이 배율 |
| 그래픽EQ | 63~8k 8밴드 | 피킹 8개 (Q 1.4) |
| 리미터 | — | 어택 0 · 천장/드라이브 dB |

숫자로 확인한 것: 리버브 꼬리 **룸 0.96초 · 플레이트 1.85 · 홀 2.75**,
그래픽EQ 1k 칸을 올리면 1k 는 3.98배인데 125Hz 는 1.02배(옆 칸을 안 건드린다),
리미터 천장 -6dB 로 잡으면 피크가 정확히 0.501, 드라이브 왜곡 9% < 17% < 19%,
베이스앰프 60Hz 0.2% 인데 800Hz 는 14.5%.

### 마스터 인서트 — 트랙과 **같은 통로**로
마스터용 코드를 따로 쓰면 화면·저장·내보내기가 전부 두 벌이 된다. 그래서
`FxChainOwner` 라는 통로를 만들고 `Track` 과 `MasterChannel` 둘 다 그걸 따르게 했다.
`FxRack` 은 이제 '트랙' 을 모르고 '주인' 만 안다 — 화면 코드가 한 벌로 끝난다.

거는 자리는 **마스터 EQ 뒤, 안전 리미터 앞**이다. 엔진의 look-ahead 리미터는
그대로 둔다 — 그건 도구가 아니라 **찢어짐 방지 그물**이라 마스터링과 별개다.

내보내기(WAV)에도 마스터 인서트를 실었다. **들리던 것과 파일이 달라지면 안 된다.**

### 검증
`flutter analyze lib` 깨끗 · `flutter test` **28개 파일 통과**.
`fx_check_test` 는 20항목(플러그인) + 8항목(인서트·마스터) = 검사 스물여덟.
마스터 항목: 꽂힌다 · 트랙과 같은 통로 · 저장·되살리기(FET 성향까지) · 엔진 자리.

### 남은 것
- **악기 소리 자체** — 어택/디케이/서스테인/릴리스 (사용자 요청, 아직 손 못 댐)
- 폰 실측 41~47/N (adb 가 다시 끊겼다)

## 5단계 48/N — 악기 엔벨로프 (사용자 요청) (2026-08-20)

> "기타, 베이스, 드럼, 건반 악기들도 더 실제와 가깝게 개선해
>  서스테인, 어택, 디케이, 릴리즈 등등도 신경쓰고"

이번엔 **음정 있는 악기**(건반·기타·베이스·활·입김). 드럼은 파일이 따로라 다음 덩어리.

### 어택이 전부 5ms 였다
`atk = (voice=='bell'||voice=='guitar') ? 0.002 : 0.005` — 33종 중 둘 빼고 전부 같았다.
**이게 "다 신스 같다"의 제일 큰 원인이다.** 실제로는 활을 긋는 스트링이 소리가 서는 데
90ms 넘게 걸리고, 피크로 긁는 기타는 2ms 도 안 걸린다. 그 차이가 악기를 구별하게 만든다.

`ATK` 표를 만들었다 — [보통 세기 어택, 세기 민감도]. 잰 값:
**기타 1.8ms < 피아노 3.9 < 색소폰 26.2 < 스트링 140.6ms.**

**세기에 따라서도 달라진다.** 해머·피크가 세게 밀수록 현이 빨리 튄다 —
피아노 여리게 6.5ms · 세게 2.7ms. 여태는 세기가 음량만 바꿨다.

**어택을 늘리면 음이 밀린다** — 유지 구간에서 늘어난 만큼 빼서 총 길이를 지켰다.
안 그러면 스트링을 90ms 로 바꾼 순간 곡 전체가 뒤로 밀린다.

### 배음도 같은 어택으로 서야 한다
가산합성 악기(피아노·기타·하프…)는 배음마다 자기 엔벨로프를 갖는데
거기 어택이 `0.004` 로 **박혀** 있었다. 기타를 1.8ms 로 잡아도 실제로 들리는 건 4ms 다.
거기에도 같은 값을 넣었다.

### 높은 음이 빨리 죽어야 한다
피아노 제일 낮은 음은 20초 넘게 울리는데 제일 높은 음은 1초도 안 간다 — 현이 짧고
얇을수록 에너지를 빨리 잃기 때문이다. 여태는 음높이와 상관없이 똑같이 울렸다.
`RINGKEY` 지수를 넣었다: **피아노 저음 1.09초 · 고음 0.31초 / 기타 6번줄 1.10 · 1번줄 0.37초.**

### 세기 = 음량이 아니라 음색
피아노에서 세기 차이는 음량보다 **음색**으로 먼저 들린다. 배음 세기가 고정이라
살살 친 것과 세게 친 것이 크기만 달랐다. 위 배음일수록 세기를 크게 타게 했다
(900Hz 위 비중 **여리게 10.8% → 세게 14.9%**). 뜯는 악기의 필터 스윕도 세기를 탄다.

### 찾은 버그 — **세기가 두 번 곱해지고 있었다**
피아노·기타·나일론·업라이트·마림바·하프는 배음 쪽에 세기(V)가 이미 들어 있는데
앰프 엔벨로프에도 또 들어갔다. 그래서 살살 친 음이 3.3배가 아니라 **11배**
(−21dB — 사실상 안 들림) 작게 나왔다. 피아노·기타 반주가 유난히 묻히던 이유.
세게 친 음(V=1)은 지금 소리 그대로 두고 여린 음만 제자리로 돌렸다.

숫자로도 나왔다 — **재즈가 −15.2 → −14.2dB.** 스타일 편차 5.2 → **4.2dB**(34/N 이후 최소).
나머지 10개 스타일은 0.3dB 안에서만 움직였고 클리핑은 여전히 0.

### 그 밖에
- **오르간 키 클릭** — 해먼드는 건반 누르는 순간 접점이 '틱' 한다. 이게 없으면
  배음을 아무리 맞춰도 오르간이 아니라 사인 패드다. (첫 5ms 고역 31% → 52%)
- 기타/나일론의 피크 소리를 갈랐다(나일론은 손톱이라 훨씬 무디다 — 같은 코드면 구별이 안 된다)
- 베이스에 지판 짚는 '딱' 추가(세게 뜯을 때만)
- 피아노 해머 소리가 세기를 탄다(살살 짚었는데 해머가 쿵 하면 제일 먼저 가짜로 들린다)

### 검증
`flutter analyze lib` 깨끗 · `flutter test` **29개 파일 통과**.
새 `env_check_test.dart` 10항목 — 어택 순서·세기별 어택·세기별 밝기·음정별 울림·
**총 길이 안 밀림**·키 클릭·표 누락. `mix_meter.dart` 11스타일 재측정.

### 남은 것
- **드럼** — 다음 덩어리(어택·피치 스윕·세기별 음색, 그리고 같은 타격이 반복되는 문제)
- 폰 실측 41~48/N

## 5단계 49/N — 드럼 (사용자 요청) (2026-08-20)

48/N 이 음정 있는 악기였고, 이번이 드럼이다.

### 제일 큰 문제 — **같은 타격이 매번 똑같았다**
16비트 하이햇을 치면 열여섯 번이 **바이트 단위로 동일한 파형**이었다.
사람이 치면 그럴 수가 없다. 이게 '머신건' 이라 불리는, 프로그램 드럼의 대표적인 티다.

두 겹으로 고쳤다:
1. **잡음 씨앗을 타격마다 바꾼다.** 하이햇·셰이커는 소리의 거의 전부가 잡음이라,
   음량·밝기를 아무리 흔들어도 잡음이 같으면 파형이 같다. 여기가 핵심이었다.
2. **음정·음량·밝기·길이를 매번 조금씩 흔든다**(`_vary`). 폭은 타악기마다 다르다 —
   반복이 많은 하이햇·셰이커가 제일 크고, 카우벨처럼 음정이 뚜렷한 건 작다.

세기는 `Human.level` 을 따른다. **0 이면 완전히 똑같이 나온다** — 안 그러면
같은 곡을 내보낼 때마다 파일이 달라진다(드럼 난수 씨앗도 이참에 박아 뒀다).
잰 값: 하이햇 열 번의 파형 차이 **32.6%**, 흔들림 0 에서는 14400샘플 전부 일치.

### 세기가 음량만 바꾸고 있었다
`VG[vel]` 로 크기만 곱했다. 그건 볼륨 노브지 연주가 아니다. 실제로는:

- **킥** — 세게 밟으면 비터 소리('딱')가 음량보다 먼저 늘고, 음정이 더 높이 튀었다
  떨어진다. 첫 12ms 고역 비중 **5.6‰ → 13.8‰**
- **스네어** — 고스트 노트는 헤드가 제대로 안 울려서 **줄(와이어) 소리 위주**다.
  세게 치면 몸통 저역과 크랙이 같이 커진다. 크랙 밝기 **73% → 92.5%**
  (전체로 재면 안 된다 — 세게 칠 때 몸통 저역도 커져서 오히려 어두워 보인다)
- **하이햇** — 살짝 스치면 심벌의 높은 모드가 안 깨어난다. 세기로 **열리는
  로우패스**를 넣었다(하이패스 모서리만 움직여선 잡음이 넓어 밝기가 안 변한다).
  13kHz 위 **37.8% → 89.7%**
- **탐** — 세게 치면 헤드가 더 눌려 더 높은 데서 떨어진다. **64% → 77%**
- **크래시** — 에너지가 많이 들어갈수록 오래 흔들린다. **0.72초 → 0.95초**
- **라이드** — 세게 치면 컵(벨) 쪽이 살아난다

세기 2 에서는 예전 값과 같게 맞췄다 — 이미 귀로 맞춰 둔 소리를 흔들지 않으려고.

### 하이햇 초킹
하이햇은 **하나뿐인 악기**다. 닫으면 열려 있던 소리가 멎어야 하는데, 여태는
열린 하이햇이 다음 박까지 겹쳐 울렸다(드럼이 아니라 심벌 뭉치가 된다).
새 하이햇이 나면 울고 있던 하이햇을 12ms 에 걸쳐 훑어 내린다(뚝 끊으면 '틱' 한다).
잰 값: 닫은 뒤 RMS 0.0455 → **0.00001**.

### 검증
`flutter analyze lib` 깨끗 · `flutter test` **30개 파일 통과**.
새 `drum_check_test.dart` — 흔들림 2 + 세기별 음색 6 + 초킹 1 + 10종 소리남 = 19항목.
`mix_meter.dart` 재측정: 크기 변화 **최대 0.2dB**, 클리핑 0, 스타일 편차 4.3dB.

### 남은 것
- 폰 실측 41~49/N

## 5단계 50/N — 악기 그림 (사용자 요청) (2026-08-20)

> "밴드악기들 더 실사적으로 만들고" · "드럼 키트의 킥이 전반적으로 너무 킥같지 않아
>  킥처럼 재설계해"

### 이번에 배운 것 — **기하보다 색이 먼저다**
킥을 고칠 때 처음엔 모양만 손봤다(테를 두껍게, 포트홀을 뚫고, 다리를 달고).
그림으로 뽑아 보니 **여전히 초록 공**이었다. 원인은 모양이 아니라 색이었다:
통·가죽·쇠붙이가 전부 밴드 색 하나였다. 그러니 무엇을 그려 넣어도 한 덩어리다.

색을 셋으로 갈랐다:
- **셸(통)** — 밴드 색
- **가죽(헤드)** — 크림색(`0xFFF2EDDF`). 북에서 제일 밝은 면이다.
- **쇠붙이** — 크롬(밝은 회백)

이 한 가지로 킥·탐·스네어가 한꺼번에 살아났다. 악기와 연주자가 같은 색인 문제는
관악기에서도 똑같이 나왔다 — 관에 **테두리**를 두르니 몸통에서 떨어져 나왔다
(팔에 테두리를 두른 것과 같은 이유다).

### 킥 드럼 — 정면에서 본 베이스 드럼
큰 원 하나면 공이다. 킥으로 보이게 하는 건 **쇠붙이**다:
- **후프** — 얇은 선이 아니라 5px 짜리 '띠'. 가죽 가장자리를 물고 있다
- **러그 여덟 개** — 후프를 조이는 조임쇠. **이게 핵심이다.** 러그가 없으면
  무엇을 해도 드럼으로 안 읽힌다
- **포트홀** — 앞 가죽에 뚫린 구멍. 가운데가 아니라 **비켜서** 뚫는다
- **스퍼 두 개 · 페달** — 바닥을 짚고, 밟으면 페달 앞이 내려간다

세로 기준을 **단(무대) 윗면** 하나로 통일했다(`deck`). 킥이 여기 얹히고
스탠드 다리도 여기까지 내려온다 — 예전엔 킥이 단을 뚫고 있었다.

### 나머지 세트
- **탐** — 킥 반지름에서 자리를 계산해 **킥 위 테두리에 얹는다**.
  예전엔 킥 안에 파묻혀 전부 한 덩어리였다. 크기도 키웠다(실제로 22인치 킥에
  12·14인치 탐이라 그렇게 작지 않다)
- **스네어** — **킥보다 앞**에 그린다(실제로도 킥 옆에 나와 있다)
- **하이햇** — 접시 **두 장** + 클러치 봉. 벌어진 틈이 보여야 하이햇이다
- **팔·스틱** — 통을 다 그린 **뒤**에. 스틱 끝이 가죽 위에 보여야 치는 걸로 읽힌다

### 기타 · 베이스
**허리가 있어야 기타로 읽힌다.** 둥근 네모는 그냥 판때기다. 위·아래 두 덩이(볼)를
겹치고 사이를 좁힌 윤곽(`_guitarBody`)으로 바꿨다. 여기에 줄·프렛·포지션 마크·
튜너까지 얹었다.

기타와 베이스를 **한눈에** 가르는 단서 둘을 넣었다:
넥 길이(54 대 46 — 실제 34인치 대 25.5인치)와 줄 수(4줄 굵게 / 6줄).
사운드홀 대신 픽업 두 개, 튜너는 한쪽에 4개(실제 배치), 오른손은 스트로크가 아니라
손가락 뜯기라 폭이 절반 이하.

### 건반 · 관악기
- **건반** — 검은 건반을 고르게 박으면 줄무늬다. 실제처럼 **두 개·세 개**로 묶고
  흰 건반 경계선을 그었다. 스탠드도 X 다리로.
- **색소폰 / 트럼펫** — 예전엔 둘 다 '삼각형 + 동그라미' 라 각도만 달랐다.
  색소폰은 **허리 아래까지 내려가는 구부러진 관** + 아래 U 굽이 + 위를 보는 나팔,
  트럼펫은 **곧은 관** + 밸브 세 개 + 나팔로 갈랐다.
  나팔은 다각형으로 그리면 '손에 쥔 상자'가 된다 — **굵기를 키운 선분**을 잇는 쪽이
  훨씬 안정적으로 나팔이 된다.

### 검증
`flutter analyze lib` 깨끗 · `flutter test` **30개 파일 통과**.
`show_snap.dart` 에 **드럼 크게(쉼/칠때)** 두 장을 더했다 — 쇠붙이까지 보이는 크기라
이번 작업은 전부 이 그림으로 판단했다. (폰 없이 그림을 눈으로 따지는 유일한 방법)

### 남은 것
- 폰 실측 41~50/N

## 5단계 51/N — 폰 실측 41~50/N + 빈 판 알림 (2026-08-20)

열 덩어리째 밀려 있던 폰 확인을 몰아서 했다(갤럭시 A17 · SM-A175N).
adb 로 앱을 띄우고 화면을 하나씩 넘기며 스크린샷과 logcat 을 봤다.

### 확인한 것 — 전부 정상
| 화면 | 결과 |
|---|---|
| 첫 화면 | 지금 곡 카드 · 네 갈래 버튼 · 사용법 |
| 씬(만들기) | 6트랙 · 조/템포/구간 · 머리글 「음 42개 · 타격 59개」 |
| 노트 편집기 | 4마디 · 세기가 색 진하기로 · 아래 제스처 안내 |
| 믹서 | 6트랙 + 라이브 + MASTER · 「마스터링」 버튼 |
| **마스터링 시트** | 「곡 전체에 걸린다 — 맨 마지막에 지나가는 자리」 · 인서트 꽂기 |
| **플러그인 고르기** | 8종이 묶음별로(밴드·공간·EQ·다이내믹) |
| **컴프 창** | 방식(옵토/FET/VCA) · 문턱 −18dB · 비율 4:1 · 어택 · 릴리스 · 보정 |
| 곡 화면 | 구간 7개 · 스타일 줄 · 내보내기 |
| 라이브 | 두 옥타브 건반 · 음색/화음/아르페지오 · 녹음 |
| **쇼 화면** | 50/N 악기 그림이 실기에서 그대로 |

**쇼 화면 60fps 고정** — logcat `fps=60.00~60.15`, 프레임 최대 18.2ms.
악기 그림을 그만큼 복잡하게 만들었는데도 안 떨어졌다.
예외·오버플로 **0건**(앱을 죽일 때 나는 teardown 경고만 남는다).

### 찾은 문제 — **빈 판이 조용히 안 들린다**
폰의 저장된 곡에서 드럼이 통째로 안 났다. 그런데 씬 화면 트랙 줄은 멀쩡해 보였다 —
「드럼 내 패턴 3」이라는 **이름이 붙어 있으니까.** 머리글의 「타격 0개」를 보고서야 알았다.

코드 버그는 아니었다. 패턴 없는 트랙에서 「음 찍기」를 누르면 `makeEditable` 이
**빈 2마디**를 만들어 주는 게 설계다(빈 판에서 직접 그리는 것도 만드는 방법이니까).
문제는 그 뒤로 **그 트랙이 무음이라는 걸 알 방법이 없다**는 것이다.

`Project.isSilent(track)` 을 만들고, 씬 트랙 줄의 패턴 칸을 노랗게 테두리 치고
값 앞에 **「비어 있음 ·」** 을 붙였다. 앞에 두는 이유: 뒤에 붙이면 이름이 길 때 잘려서
정작 알아야 할 말이 안 보인다.

라벨을 「패턴 · 비어 있음」으로 늘렸다가 **칸이 10px 넘쳤다**(시험이 잡았다) —
라벨은 한 줄로 못 박고 색만 바꿨다.

### 실수 하나
좌표를 눈대중으로 찍다가 뒤로가기가 앱을 빠져나가 **시계 앱이 열렸다.**
알람 소리 설정을 묻는 창이 떴는데 **아무것도 누르지 않고 뒤로가기로 닫았다**(설정 변경 없음).
그 뒤로는 홈 화면을 다시 찍어 좌표를 재서 진행했다.

### 검증
`flutter analyze lib` 깨끗 · `flutter test` **30개 파일 통과**.
`scene_check_test` 에 4항목 추가(패턴 없음 · 빈 복사본 · 하나 찍으면 소리남 · 새 곡은 전부 소리남).

### 남은 것
- **청감 확인** — 48·49/N 이 소리를 많이 바꿨다. 특히 (1) 피아노·기타 반주가
  이제 들리는지(세기 두 번 곱하던 버그), (2) 하이햇 16비트가 기계음에서 벗어났는지.

## 5단계 52/N — 폰 오디오 실측 (48~50/N 이후) (2026-08-20)

48·49/N 에서 DSP 를 꽤 늘렸다(배음마다 어택, 타격마다 잡음 씨앗, 하이햇에 로우패스
한 단 추가, 울림 길이 증가). **폰에서 소리가 끊기는지**는 안 재 봤으므로 쟀다.

### 잰 방법
곡 화면에서 **6트랙 곡 전체를 60초** 틀고(음 392개 · 타격 476개),
엔진 시험 화면의 지표를 읽었다. 화면부하 「보통」(그림도 같이 그리는 상태).

### 결과 — 끊김 0
| 지표 | 값 | 뜻 |
|---|---|---|
| **버퍼 바닥** | **0회** | 언더런 없음 |
| **음 부족** | **0회** | 목소리 모자람 없음 |
| **드럼 음 부족** | **0회** | 드럼 풀 모자람 없음 |
| **하드클립** | **0회** | 찢어짐 없음 |
| 울리는 음 | 최대 **40** | 동시 40음 |
| 울리는 드럼 | 최대 **11** | |
| 오디오 여유 | **66%** | 실시간의 1/3만 쓴다 |
| 화면 FPS | **60** | 그림도 안 떨어짐 |
| 리미터 감쇠 | 0.0dB | 리미터가 일할 일이 없었다 |

**늘린 DSP 값을 폰이 감당한다.** 여유 66% 면 아직 배 가까이 남는다.

### 지켜볼 것 — 급식 지체
`급식 지체 최대 53ms · 20ms초과 **630회**` · `큐 최소 잔량 1336프레임(≈28ms)`.
급식 루프(3ms 주기)가 상시로 늦는다. **소리는 안 끊겼다** — 버퍼가 깊어서(5860프레임
≈122ms) 늦은 만큼을 먹고 버텼다. 다만 큐가 28ms 까지 얇아진 적이 있으니 여유가 무한하진 않다.

단, 이 값은 **재는 행위가 오염시킨다** — 재는 동안 adb 로 화면을 계속 캡처하고
UI 트리를 덤프했다. 그것만으로도 UI 아이솔레이트가 바쁘다. 48/N 이전 기준값이 없어서
**"48~50/N 때문에 늘었다"고는 말할 수 없다.** 다음에 DSP 를 더 늘리기 전에
같은 방법으로 한 번 재 두고 비교할 것.

### adb 로 폰을 볼 때 알아 둘 것 (다음 사람이 헤매지 않게)
이번에 두 번 크게 헤맸다:

1. **`screencap` 은 굳은 화면을 준다.** 입력이 한동안 없으면 마지막 프레임을 그대로
   돌려준다(Impeller/Vulkan). 앱은 멀쩡히 60fps 로 돌고 있는데 스크린샷만 안 바뀐다 —
   "앱이 얼었다"고 30분 헤맸다. **캡처 직전에 아주 작은 스와이프를 넣으면** 새로 그린다:
   `input swipe 540 1200 540 1195 60` → 바로 `screencap`.
2. **엔진 시험 화면에서는 `input tap` 이 위젯에 안 닿는다.** 스와이프(스크롤)는 되는데
   탭만 안 먹는다. 다른 화면(첫 화면·씬·믹서·곡)에서는 잘 된다. 그래서 이 화면에서
   무언가를 눌러야 하면 **다른 화면에서 실행하고 지표만 보러 오는 쪽**이 빠르다
   (지표는 엔진 전역 누적이라 화면을 옮겨도 남는다).
3. **좌표는 눈대중으로 찍지 말 것.** 뒤로가기가 앱을 빠져나가 엉뚱한 앱이 열린다
   (51/N 에서 시계 앱이 열렸다). 캡처 → 좌표 계산 → 탭 순서를 지킬 것.
   `sips -Z 800` 로 줄인 그림에서 잰 좌표는 `× 2.925`(1080×2340 기준).

### 남은 것
- **청감 확인** — 48·49/N 이 소리를 많이 바꿨다(피아노·기타 반주 크기, 하이햇 반복).
- DSP 를 더 늘리기 전 급식 지체 기준값 잡아 두기

## 5단계 53/N — 멜로디 작·편곡 · 하이햇 · 반주 크기 · 드러머 (사용자 요청) (2026-08-21)

> "피아노 기타 안들려 / 하이햇 개선 요망 / 왜 드럼플레이어가 드럼을 안치고 팔을
>  어정쩡하게 들고있어 / 멜로디들이 왜 다 아르페지오야 작곡 공부해와서 좋은
>  멜로디들로 다시 작,편곡해 코드톤만 쓰지말라고"

네 가지를 한 덩어리로 처리했다.

### ① 멜로디 — **정말 아르페지오였다**
느낌이 아니라 숫자로 확인했다. 아르페지오와 멜로디를 가르는 셈 세 가지:
**코드톤 비율**(100%면 화음을 풀어 놓은 것) · **도약 비율**(아르페지오는 3도씩 뛴다) ·
**길이 종류**(다 같으면 리듬이 없다).

| 패턴 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 힙합 벌스 `Hip Riff V` | 코드톤 **92%** · 도약 **91%** | 64% · 20% |
| 발라드 코러스 `Ballad Line C` | **100% · 100%** | 64% · 38% |
| 하우스 벌스 `House Pluck V` | 75% · **93%** · 길이 **1종** | 71% · 23% · 4종 |
| 시티팝 코러스 `City Hook C` | 75% · 73% | 60% · 37% · 6종 |
| 록 코러스 `Rock Riff C` | 83% · 82% | 71% · 56% |
| 그 외 | `Lofi Sub` `House Lead C` `City Hook V` `City Hook` `Simple Hook` `Gfunk Riff` `Amb Bell A` |

기준값은 **이 프로젝트 안에서 이미 잘 된 것들**에서 가져왔다 — 재즈 색소폰(58%/44%),
팝 보컬(54%/50%), 프로그 리드(50%/29%). 롱폼 5곡은 원래 제대로 된 선율이었고,
**배열형 6곡(로파이·하우스·힙합·시티팝·발라드·록)만 아르페지오였다.**

다시 쓸 때 지킨 것: 모티프를 내고 반복·변형해 답한다 · 순차진행을 뼈대로 하고
도약은 표현할 때만(그리고 도약한 뒤에는 걸어서 되돌아온다) · 경과음·보조음을
약박에 넣는다 · 길이를 섞고 쉼표를 쓴다 · **제일 높은 음은 곡에서 딱 한 번**,
프레이즈의 3분의 2 지점에 · 마지막은 계단으로 내려와 안정음에 앉는다.

**안 고친 것**: `Arp Up`·`Prog Arp`·`Pop Arp`·`Trap Pluck H`·`Amb Harp` 등.
이름부터 도형이거나, `plk`/`arp`/`harp` **배경 텍스처 슬롯**(볼륨 0.46~0.52 ·
하이패스 420~450Hz)이라 16분 아르페지오가 제 역할이다. 멜로디는 그 위의
`lead`/`voc`/`sax`/`bell` 이 맡는다 — 프로그하우스가 원래 그렇게 만든 음악이다.

### ② 피아노·기타가 안 들리던 이유
트랙을 **하나씩만 남기고 렌더해** 크기를 쟀다(`test/track_meter.dart`, 새 도구).

재즈 전체 −15.1dB 기준: 드럼 −1.2 · 베이스 −3.5 · 색소폰 −5.0 은 정상인데
**피아노 −14.9 · 기타 −28.3 · 비브라폰 −32.4** 였다.

원인: 코드 트랙은 `sequencer.dart` 에서 **무조건 `soft: true`** 가 붙고,
`soft` 는 음량을 **0.55배(−5.2dB)** 로 깎는다. 코드 패턴에는 이미 낮은 세기가
들어 있는데 그 위에 또 깎으니 반주가 묻혔다. **0.82(−1.7dB)** 로 바꿨다 →
피아노 −14.9 → **−11.5**, 시티팝 패드 −13.9 → **−10.4**.

**아직 남은 것**: 재즈 기타·비브라폰은 −28dB 다. 이건 믹스가 아니라 **편곡** 문제다 —
기타는 66초, 비브라폰은 50초가 지나야 처음 나오고 곡 전체에서 76음뿐이다
(피아노는 712음). 크기를 올릴 게 아니라 **언제 들어오는지**를 손봐야 한다.
(재는 창을 45초로 잘랐다가 둘 다 '무음'으로 나와서 한참 헤맸다 — 전체를 렌더해야 한다.)

### ③ 하이햇 — 잡음이 열 배였다
어쿠스틱 킷에서 잡음 층 0.40 대 금속 층 **0.039** — 열 배 차이라, 하이햇이 아니라
'치익' 하는 잡음이었다. 셋을 고쳤다:
- **금속을 여섯 배로**(0.13 → 0.78 계수). 금속 층을 빼 보면 이제 어쿠스틱 **80%** ·
  909 **96%** 가 사라진다(예전엔 사실상 안 들렸다)
- **스틱이 때리는 '칙'** 을 앞에 넣었다 — 2.5~5kHz 짜리 3ms 앞머리. 몸통 잡음은
  1.5ms 늦춰서 뒤에 오게 했다(스틱이 닿고 **그다음에** 접시가 운다).
  2.5~5kHz 에서 앞 5ms 가 뒤보다 **7배**
- **열린 하이햇**(세기 3)을 1.8배 → 3.2배로. 33ms 대 105ms — 이제 열린 소리가 난다

'금속처럼 들리는가'를 재려고 **스펙트럼 평탄도와 자기상관을 둘 다 써 봤는데 못 잡는다.**
금속 클러스터는 일부러 배음이 안 맞는 사각파 여섯 개라 위쪽이 잡음처럼 촘촘하고
주기도 아주 길다. 결국 **빼 보고 얼마나 줄어드는지**로 바꿨다 — 그게 제일 정직하다.

### ④ 드러머가 팔을 어정쩡하게 들고 있던 이유
스틱 높이를 **타격 세기**로 정하고 있었다. 세기는 0.22초면 0으로 떨어지는데
8분음표 간격은 88BPM 에서 0.34초다 — **그 사이 내내 팔이 허공에 멈춰 있었다.**

실제 스틱은 친 자리에서 튀어 올랐다가 **다음 박에 맞춰 내려온다.** 그래서
`ShowScore.strokePhase()` 로 **다음 타격까지의 진행도**를 재서 높이를 정한다:
0=방금 침(헤드에 닿음) · 0.45=리바운드 꼭대기 · 1.00=다시 내려와 치기 직전 ·
−1=칠 게 없음(허공에 들지 않고 북 가까이서 쉰다). 크래시가 울리면 그쪽으로 팔이 간다.

### 검증
`flutter analyze lib` 깨끗 · `flutter test` **31개 파일 통과**.
- 새 `melody_check_test.dart` — 아르페지오 판별 · **11개 스타일 전부** 첫 멜로디 검사 · 음역
- `drum_check_test` 에 하이햇 3항목(금속 기여도 · 스틱 어택 · 열린 하이햇)
- 새 `track_meter.dart`(도구) — 트랙 하나만 남기고 렌더해 크기 재기
- `mix_meter.dart` 재측정: 스타일 편차 **4.4dB** · 클리핑 0 (멜로디를 다 바꾸고도 안 흔들림)
- `show_snap.dart` 에 스틱 위상 네 장(0 · 0.45 · 0.9 · 쉼)

`scene_check_test` 가 멜로디 음 개수 64를 박아 놔서 깨졌다 — 패턴에서 세어 계산하도록 고쳤다.

### 남은 것
- **재즈 기타·비브라폰 편곡** — 크기가 아니라 등장 시점 문제(위 ② 참고)
- 청감 확인 — 이번엔 바뀐 게 많다(멜로디 12개 · 하이햇 · 반주 크기)

## Phase 1 — 기반 안정화 (개선 계획 3장) (2026-08-22)

전체 개선 계획(Phase 1~7)을 받았다. **Phase 1 = 기반 안정화**만 했다.
계획의 원칙대로 기존 구조를 재작성하지 않고, **깨졌는지 알 수 있게** 만드는 데 집중했다.

### 먼저 조사한 것
`engine.dart`(485줄) 렌더 경로 · `audio_isolate.dart`(739줄) 급식 루프 ·
`export.dart` 렌더 경로 · `store.dart`/`project.dart` 저장 포맷 · 기존 시험 27개.

확인한 사실:
- 재생과 내보내기는 **같은 `Engine.render()`** 를 쓴다(각각 다른 인스턴스).
- 저장 포맷에 `'v': 1` 이 **이미 있다**(project.dart:934). 마이그레이션 준비 완료 상태.
- 자동 저장도 **이미 있다** — `store.dart` 의 800ms 디바운스(`_debounce`).
- 성능 계측도 **이미 있다** — 엔진 시험 화면의 `AudioStats`
  (여유·언더런·큐 잔량·급식 지체·보이스 최대·FPS). Phase 1 에서 새로 만들 필요 없었다.

### 찾은 버그 — **박수(clap)가 결정적이지 않았다**
`drums.dart` 의 `clap()` 만 흔들림 설정과 무관하게 난수를 썼다:
```dart
final off = i * k.spread * (1 + _rnd2() * 0.12);   // 조건 없음
```
`Human.level == 0` 으로 흔들림을 꺼도 박수 자리가 매번 달라졌다.
결과: **같은 곡을 두 번 내보내면 파일이 다르다**(트랩·하우스·팝이 박수를 쓴다).
그리고 이것 때문에 회귀 시험 자체가 불가능했다 — 기준값이 매번 바뀌니까.
`_varyOn` 을 보게 고쳤다.

### 새로 만든 안전망
**`test/engine_guard_test.dart`** — 19항목. 음색이 아니라 **엔진이 안 깨지는지**를 본다.

| 영역 | 확인한 것 | 결과 |
|---|---|---|
| 결정성 | 같은 입력 → 같은 소리 | 192000샘플 전부 일치 |
| 결정성 | 타악기 10종 각각 | 10종 통과 |
| 위생 | NaN · 무한대 | 0개 |
| 위생 | 하드클립 · DC 치우침 | 0개 · −0.00004 |
| **회귀** | 기준 곡 지문(피크·RMS·대역 5개) | 골든 고정 |
| 스케줄러 | 예약 200개 소진 · 동시 64음 · 예약 비우기 | 누락 0 |
| 스케줄러 | 정지 후 목소리 반납 · 꼬리 감쇠 | 8음 → 0음 · 2초 뒤 0 |
| 보이스 | 폴리포니 400 요청(한도 96) | 안 깨짐 · 굶음 304회 세어짐 |
| 보이스 | 드럼 200 요청(한도 24) | 안 깨짐 |
| 보이스 | **악기 33종 전수** 저역·고역 | 전부 정상 |
| **재생=내보내기** | 같은 재료를 두 경로로 | **최대 차이 0.0 — 완전 일치** |

기준 곡 지문(2026-08-22): 피크 0.9635 · RMS 0.1745 · DC −0.00004 ·
대역 [44.1, 50.2, 3.6, 1.0, 1.0]% · 체크섬 742858024.

> 이 시험이 깨졌다고 무조건 버그는 아니다. 음색을 일부러 고치면 당연히 바뀐다.
> **바뀌었다는 사실을 놓치지 않게** 하는 것이 이 시험의 일이다. 의도한 변경이면
> 골든 값을 갱신하고 **왜 바꿨는지 HANDOFF 에 적는다.**

**`test/save_roundtrip_test.dart`** — 13항목. 한 곡 안의 **모든 값**이 왕복에서 살아남는지.
믹서 6값 · 뮤트/솔로 · 음색 · 인서트 종류·순서·켜짐·손잡이 값 · 내가 찍은 드럼 자리와 세기 ·
내가 찍은 음 4요소 · 곡 구성 · **두 번 왕복해도 바이트 단위 동일**(2042자) ·
모르는 필드와 미래 버전(v99)에도 안 죽음.

`toJson` 에만 적고 `fromJson` 에서 빠뜨리는 실수는 **아무 소리 없이** 사용자 작업을
지운다 — 그래서 항목별로 못 박았다.

### 결과
```
Phase        1 (기반 안정화)
Completed    결정성 · 회귀 골든 · 스케줄러 · 보이스 · 재생=내보내기 · 저장 왕복
Changed      lib/drums.dart (clap 결정성 1줄)
Added        test/engine_guard_test.dart (19항목)
             test/save_roundtrip_test.dart (13항목)
Tests        flutter test — 33개 파일 전부 통과
Analyze      lib 깨끗
Known issues 급식 지체 20ms 초과가 상시로 잡힌다(52/N) — 소리는 안 끊긴다.
             기준값이 없어 원인 규명 못 함. Phase 3 전에 잡아 둘 것.
             재즈 기타·비브라폰 등장 시점 문제(53/N) — Phase 4 편곡 과제.
```

### Phase 2 로 넘어가기 전에
계획 17장대로 **현재 상태에서 프로젝트를 다시 확인**할 것.
특히 Phase 2(UX 정리)는 `home_view`·`scene_view`·`mixer_view` 를 건드리는데,
이 셋이 1246~1893줄이라 **11장(구조 개선)을 같이 봐야 한다.**

## Phase 1 후속 — 급식 지체 파고들기 (2026-08-23)

52/N 에서 `급식 지체 20ms 초과 630회` 가 잡혔는데 원인을 못 짚고 넘어갔다.
Phase 2 로 가기 전에 이것부터 잡았다.

### 고친 것 ① — 급식 경로가 초당 188KB 씩 쓰레기를 만들고 있었다
`engine.render(n)` 은 **부를 때마다 `Int16List` 를 새로 만든다.** 급식 루프는
이걸 3ms 마다 부른다 — 1024프레임이면 4KB, 초당 47번 = **188KB/초**.
그 청소(GC)가 곧 급식 지체다.

- `Engine.renderInto(out, frames)` 추가 — **할당이 없다.** `render()` 는 겉껍질로 남겨
  내보내기·시험은 그대로 쓴다.
- 급식 루프는 버퍼 **한 개**를 만들어 돌려쓴다.
- 걸림돌: 플러그인 `feed` 가 `bytes.buffer.asUint8List()` 라 **뷰를 무시하고 밑에 깔린
  버퍼 전체를 보냈다.** 앞부분만 담은 뷰를 넘기면 뒤쪽 쓰레기까지 재생된다.
  `packages/flutter_pcm_sound` 는 우리가 고쳐 쓰는 복사본이라 여기서 offset/length 를
  지키게 고쳤다.

측정(`test/render_bench.dart`, 새 도구 · 오디오 20초 · 청크 1024):
**958ms → 860ms (10.2% 단축)** · 쓰레기 188KB/초 → 0 · **두 방식 결과 완전히 동일.**

### 되돌린 것 ② — 믹서 레코드는 범인이 아니었다
`TrackMix.process` 가 `(double, double)` 을 돌려준다. 샘플마다 슬롯 수만큼 불리니
**초당 24만 개** — `Fx.step` 이 outL/outR 필드를 쓰는 것과 같은 이유로 느릴 줄 알았다.

바꿔서 재 봤더니 **860ms → 890ms 로 오히려 3.5% 느렸다**(3회 반복, 노이즈 아님).
Dart 가 이 레코드는 이미 잘 풀어내고 있다. **되돌렸고**, 같은 실수를 반복하지 않게
`mixer.dart` 에 그 사실을 주석으로 남겼다.

> 이번 건이 이 계획 14장("측정 → 판단")이 왜 있는지 보여 준다.
> 원칙("핫 패스에 할당 금지")이 맞아 보여도 **재 보기 전에는 모른다.**

### 고친 것 ③ — 지체를 셋으로 나눠 잰다
`maxPollGapMs` 하나만 보면 "늦었다"는 것만 알지 **왜** 늦었는지를 모른다.
630회를 놓고도 원인을 못 짚은 이유가 이것이다. 그래서 나눴다:

```
급식 지체        최대 Xms · 20ms초과 N회
  ├ 소리 만들기   렌더에 쓴 시간      (우리 잘못)
  ├ 다음 판 예약   스케줄링에 쓴 시간   (한 판이 클수록 튄다)
  └ 안 돌던 시간   나머지             (타이머 정밀도 · GC · 시스템)
```
엔진 시험 화면에 그대로 나온다. `AudioStats` 에 `maxRenderMs`/`maxSchedMs` 추가.

### 폰 실측 — **630회 → 1회**
케이블이 다시 붙어서 같은 조건(6트랙 곡 60초 · 화면부하 끔)으로 쟀다.

| 지표 | 52/N (고치기 전) | 지금 |
|---|---|---|
| **급식 지체 20ms 초과** | **630회** | **1회** |
| 급식 지체 최대 | 53ms | 20ms |
| 큐 최소 잔량 | 1336프레임(28ms) | **3423프레임(71ms)** |
| 버퍼 바닥 · 음 부족 · 하드클립 | 0 · 0 · 0 | 0 · 0 · 0 |
| 오디오 여유 | 66% | 64% |

**버퍼 재사용이 범인이 맞았다.** 초당 188KB 쓰레기를 없애니 상시 지체가 사라졌다.

남은 최대 20ms 의 정체도 계기가 알려 줬다 — **소리 만들기 16.2ms · 예약 0.2ms ·
안 돌던 시간 3.9ms.** 거의 전부가 렌더 한 번이다. 한 번에 채우는 양을
1536 → 768프레임으로 줄여 봤지만 그 봉우리는 안 줄었다(15.7 → 16.2ms).
**즉 이건 '많이 만들어서'가 아니라 '음을 많이 켜서'다** — 구간이 바뀌는 순간
그 렌더 안에서 수십 개의 `noteOn` 이 한꺼번에 일어난다(엔벨로프·필터를 그때 만든다).
곡을 트는 동안 딱 한 번이고 버퍼가 71ms 를 들고 있어서 소리에는 영향이 없다.

캡 축소는 그래도 남겼다 — **큐 최소 잔량이 1420 → 3423프레임으로 두 배 넘게 두꺼워졌다.**
급한 문제는 아니지만 여유는 여유다.

다음에 이 봉우리까지 없애고 싶으면 볼 자리: `SynthNote.noteOn` 이 음을 켤 때 하는 일
(엔벨로프 구성·바이쿼드 계수 계산)을 미리 만들어 두거나 여러 렌더에 나눠 켜는 것.
**지금은 필요 없다** — 20ms 초과가 1회다.

### 예전에 못 했던 것 위 셋 중 ①은 맥 벤치로 확인했지만
**630회가 실제로 줄었는지는 폰에서 봐야 안다.** 폰이 붙으면:
`엔진 시험 화면 → 지표 초기화 → 곡 전체 재생 60초 → 세 줄이 각각 몇 ms인지` 확인할 것.
- '안 돌던 시간'이 크면 → 타이머/GC. `_kPollMs` 나 버퍼 목표(ahead)를 손볼 자리.
- '소리 만들기'가 크면 → 한 번에 채우는 양(지금 최대 1536프레임)을 줄일 자리.
- '다음 판 예약'이 크면 → `SceneLoop.topUp` 의 `maxPerTick` 을 줄일 자리.

### 결과
```
Changed   lib/engine.dart (renderInto) · lib/audio_isolate.dart (버퍼 재사용 + 계기)
          lib/main.dart (지표 3줄) · packages/flutter_pcm_sound (뷰 존중)
          lib/mixer.dart (되돌린 이유 주석)
Added     test/render_bench.dart (측정 도구)
Tests     flutter test — 33개 파일 전부 통과 (골든 회귀 포함 = 소리 안 변함)
Perf      급식 경로 10.2% 단축 · 쓰레기 188KB/초 제거
Pending   폰 실측 (케이블)
```

## Phase 2 (1) — 간단히 보기 / 전부 보기 (개선 계획 4-3) (2026-08-23)

계획 4-3 의 요구는 **"기능을 제거하는 것이 아니라 정보 노출량을 줄인다"** 이다.
그래서 화면·코드를 지우지 않고 **진입점만 감춘다.** 믹서도 편집기도 그대로 있다.

### 감추는 것
| 화면 | 간단히 보기에서 감춤 | 이유 |
|---|---|---|
| 첫 화면 | 믹서 아이콘 | 페이더·EQ·인서트를 처음부터 내밀면 도움이 아니라 벽이다 |
| 첫 화면 | 엔진 시험 화면 | 개발용이다 |
| 만들기 | 조 고르기(12키 × 장/단조) | 음악을 아는 사람의 손잡이. 스타일을 고르면 조도 따라온다 |
| 만들기 | 음 편집기(연필) | 음을 하나씩 찍는 건 한 단계 깊은 일 |

**감추지 않은 것**: 음색·패턴 칩(핵심 조작), 재생, 씬 고르기, 곡으로 잇기, 라이브, 쇼.
이것만으로도 곡이 된다 — 그게 이 앱의 주장이다.

### 기본값 — 쓰던 사람에게서 뺏지 않는다
- 처음 쓰는 사람(`guideSeen == false`) → **간단히**
- 이미 쓰던 사람(`guideSeen == true`) → **전부 보기**

기능을 갑자기 뺏으면 그건 개선이 아니라 고장이다. `index.json` 에 `simple` 로 남는다.

### 세로·가로를 나란히 보고 잡은 것
토글을 처음엔 첫 화면 **아래쪽**에 뒀다. 세로에서는 멀쩡했는데
**가로(800×400)에서 화면 밖으로 밀렸다** — 간단히 보기로 들어간 사람이
되돌아올 길이 안 보인다. `ListView` 라 밀면 나오긴 하지만, 처음 쓰는 사람은
거기 뭐가 있는지를 모른다.

**머리줄로 올렸다**(제목 오른쪽). 두 방향·두 모드 모두 항상 보인다.

그림 도구도 같이 고쳤다 — `screen_snap.dart` 의 `show()` 가 같은 위젯 트리를
재사용해서 **앞에서 연 대화상자가 다음 그림에 그대로 남아 있었다**(가로 만들기 그림이
믹서 시트에 덮여 나왔다). `MaterialApp` 에 이름별 키를 줘서 매번 새로 짓게 했다.

### 검증
**`test/simple_mode_test.dart`** (새 파일) — 16항목. **세로·가로 각각** 8항목:
- 간단히면 믹서·시험화면·조·연필이 **없다**
- **되돌아올 버튼이 보인다** (가로에서 밀렸던 그 자리)
- 전부 보기로 돌리면 **다시 나온다** — 감춘 것이지 없앤 것이 아니다
- 음색·패턴은 **그대로** — 감출 것이 아니다
- 넘침 없음

"안 보인다"와 "없다"를 구별한다 — 가로에서 `엔진 시험 화면` 은 접혀 있어서
그냥 찾으면 없다고 나온다. **밀어서 닿는지**로 확인한다.

`screen_snap.dart` 에 네 장 추가: 첫화면·만들기 × 간단히 × 세로/가로.

### 결과
```
Phase      2 (1/N) — 간단히 보기
Changed    lib/store.dart (simpleMode) · lib/ui/home_view.dart · lib/ui/scene_view.dart
           test/screen_snap.dart (트리 재사용 버그 + 그림 4장)
Added      test/simple_mode_test.dart (16항목 · 세로 가로 각각)
Tests      flutter test — 34개 파일 전부 통과
Analyze    lib 깨끗
Pending    폰 실측 — 폰이 잠겨 있다(deviceLocked=1). 잠금 해제는 사용자만 할 수 있다.
```

### Phase 2 남은 것 (계획 4장)
- 4-1 첫 실행 경험 · 4-2 Create Flow (장르 고르면 바로 소리)
- 4-4 결과 중심 UX (Energy / Density / Groove 손잡이)

## Phase 2 (2) — 첫 흐름: 고르면 바로 소리 (개선 계획 4-1·4-2) (2026-08-23)

계획이 요구하는 첫 흐름은 한 줄이다:
`앱 실행 → 장르 선택 → **기본 음악이 즉시 재생** → 사용자가 바로 조작`

### 예전에는 이랬다
앱 실행 → **개념 설명 다이얼로그**(만들기·곡·라이브·쇼 네 가지를 한 화면에)
→ 닫기 → 첫 화면 → 만들기 → **재생 버튼을 찾아 누름** → 그제서야 소리.

계획이 경계하는 바로 그것이다 — "첫 실행에서 Mixer·FX·Scene·Track·Pattern·Scale·Chord 를
한꺼번에 설명하지 마십시오". 읽을거리를 먼저 내밀면 대부분은 닫고 나가고,
**나가면 아무 소리도 안 난다.**

그리고 더 큰 구멍이 있었다 — **스타일(장르)이 곡 화면에만 있었다.**
만들기 화면 안에서는 장르를 바꿀 방법이 아예 없었다.

### 지금은 이렇다
앱 실행 → **「무슨 음악을 만들까요」** (장르 11종) → 고르는 순간
장르·템포·조·편성이 잡히고 만들기 화면으로 들어가며 **바로 재생**.

- 새 파일 `lib/ui/create_sheet.dart` — 시트와, 그림 도구가 쓸 수 있게 뺀 `CreateSheetBody`
- 장르마다 **한 줄 느낌** — 「느긋하고 먼지 낀 — 공부할 때 틀어 두는」처럼.
  장르 이름만 보고는 뭐가 다른지 모른다. 음악 용어 대신 **들리는 느낌**으로 적었다.
- 카드 색은 **쇼 화면 무대 조명 색**(`kStageLights`)과 같다 — 고른 것이 이어진 느낌
- 만들기 화면 맨 위에 **스타일 칩** 추가. 누르면 같은 시트가 열리고,
  고르면 **그 자리에서 소리가 바뀐다**(닫고 나서 재생을 또 누르게 하지 않는다)
- `SceneView(autoPlay: true)` — 첫 프레임 뒤에 켠다(build 중에 걸면 `setState` 가 씹힌다)

### 계획과 다르게 한 것 하나
계획 4-2 의 Create 양식은 `Genre / BPM / Mood` 세 가지다. **장르 하나만 물었다.**
소리를 듣기 전에 손잡이를 셋 물어보면 그건 설문지다. 장르마다 검증된 템포·조·편성이
이미 있고(`kSongGenres` + `setGenre`), 템포는 **들으면서** 바꾸는 게 맞다 —
만들기 화면 맨 위에 슬라이더가 이미 있다. Mood/Energy 는 계획 4-4 에서 다룬다.

### 안내는 없애지 않았다
「사용법」 버튼으로 언제든 열린다. **순서를 바꾼 것이지 지운 것이 아니다.**
`guide_ui_test.dart` 도 새 의도로 고쳤다 — 첫 실행에 뜨는 것은 장르 고르기이고,
안내는 「사용법」 으로 열리며 내용(네 단계·무음 대처)은 그대로여야 한다.

### 잡은 것
- `markGuideSeen()`(파일 쓰기)을 **기다리다가** 시트가 안 떴다.
  값 자체는 그 줄에서 바로 켜지므로 기다릴 필요가 없다.
- 그림 도구에서 제목 글자가 네모로 나왔다 — `Material` 조상이 없어서였다.
  `Scaffold` 로 감싸니 정상. (앱에서는 시트가 Material 안이라 원래 문제없다)
- 시험에서 시트를 다시 띄울 때 **앞서 밀어 둔 스크롤 위치가 남아** 맨 위 카드를
  못 눌렀다. `MaterialApp` 에 `UniqueKey()` 를 줘서 매번 새 트리로.

### 검증
**`test/create_flow_test.dart`** (새 파일) — 14항목, **세로·가로 각각 7항목**:
- 장르 11종이 다 있다(접힌 것은 훑으면서 모아 확인)
- **전문 용어가 없다** — 믹서·FX·씬·트랙·패턴·스케일·코드 중 하나도 안 나온다
- 고르면 값이 넘어온다 · 장르·템포·조가 같이 바뀐다 · 편성이 채워지고 전부 소리난다
- 자동 재생 화면이 선다 · 만들기에 스타일 칩이 있다

`screen_snap.dart` 에 「13_시작하기」 세로·가로 두 장 추가.
`flutter test` — **35개 파일 전부 통과.** `flutter analyze lib` 깨끗.

### 남은 것
- **폰 실측** — 폰이 잠겨 있다(`deviceLocked=1`). 특히 **고르는 순간 진짜로 소리가 나는지**는
  폰에서만 확인된다(시험 환경엔 오디오가 없다).
- 계획 4-4 결과 중심 손잡이(Energy / Density / Groove)

## Phase 2 (3) — 느낌 손잡이 (개선 계획 4-4) (2026-08-23)

> Energy `Calm ─ Hype` · Density `Simple ─ Busy` · Groove `Straight ─ Swing`
> "처음부터 완벽한 자동화까지 구현할 필요는 없습니다. 우선 **구조를 확장 가능하게**."

### 구조 — 한 곳에서 끝난다
`lib/feel.dart` 하나다. **엔진도 신스도 패턴 데이터도 안 건드린다.**
`SceneSequencer.build()` 가 만들어 낸 (음 목록, 타격 목록)을 **맨 마지막에 한 번** 주무른다.

이 자리를 고른 이유: **재생과 내보내기가 같은 목록을 쓴다.** 여기서 바꾸면
들은 것과 파일이 저절로 같다(Phase 1 에서 둘이 샘플 단위로 같다는 걸 못 박아 뒀다).
손잡이를 더 붙이려면 `applyFeel` 에 한 줄씩 더하면 된다 — 그게 계획이 말한 '확장 가능한 구조'다.

### 세 손잡이가 실제로 하는 일
| 손잡이 | 하는 일 | 잰 값 |
|---|---|---|
| **기운** | 세기(벨로시티)를 밀고 당긴다 | 차분 8 ← 보통 16 → 신남 24 (음 8개 합) |
| **빽빽함**(왼쪽) | 약박부터 덜어 낸다. 16분 뒷자리 → 8분 뒷자리 순 | 음 8→4개 · 타격 14→10개 |
| **빽빽함**(오른쪽) | 하이햇을 16분으로 쪼갠다 (끼운 것은 **약하게**) | 하이햇 8→16개 |
| **그루브** | 뒷박을 뒤로 민다. 최대 셋잇단(2:1) | 8분 뒷박 250ms → **333ms** |

**골격은 안 건드린다** — 아무리 단순하게 해도 킥·스네어는 남는다(4/4 · 2/2 확인).
빼면 곡이 아니라 정적이 된다.

**가운데(0.5)가 지금까지의 소리다.** 기본값에서는 목록이 **글자 하나 안 바뀐다**(시험 1번).
그래야 예전 곡이 그대로 들린다.

### 화면 — 숫자를 안 보여 준다
`0.73` 은 아무 뜻도 없다. **「신남」**이라고 적는다. 슬라이더 아래에 양 끝 말
(차분↔신남 · 단순↔빽빽 · 반듯↔스윙)을 두고, 지금 상태는 색깔 글자로.
만지는 즉시 반영된다 — 닫아야 들리면 무슨 소리인지 모른 채 만지게 된다.

만들기 화면 맨 위에 **「느낌」 칩**. 기본값이 아니면 칩에 지금 상태가 뜬다(예: 「신남 ▾」).
**간단히 보기에서도 남긴다** — 음악 용어를 몰라도 만질 수 있는 것이라
오히려 여기가 제일 어울린다.

### 세로·가로를 나란히 보고 잡은 것
스타일·느낌 칩을 위쪽 줄에 넣었는데, **세로에서 아래 줄에도 이미 있어서 두 번 나왔다.**
덤으로 템포 슬라이더가 찌그러졌다. 가로일 때만 위쪽 줄에 붙이게 고쳤다
(조 칩이 원래 그렇게 되어 있었는데 새 칩만 빠뜨렸다).

시험의 '넘침 없음'이 이걸 잡았지만 **무엇이 잘못됐는지는 그림이 알려 줬다** —
`takeException()` 은 "예외가 있었다"까지만 말한다.

### 검증
**`test/feel_check_test.dart`** (새 파일) — 13항목. 목록을 직접 세어 본다:
가운데면 그대로 · 세기가 1~3 을 안 벗어남 · 골격은 남음 · 끼운 하이햇은 약함 ·
앞박은 제자리 · 뒷박만 셋잇단으로 · 시간 순서 유지 · 저장 왕복 · 옛 파일 안전.

`simple_mode_test` 에 「스타일·느낌은 간단히 보기에서도 남는다」 추가(세로·가로).
`screen_snap.dart` 에 「14_느낌」 세로·가로.
`flutter test` — **36개 파일 전부 통과.** `flutter analyze lib` 깨끗.

### 결과
```
Phase      2 (3/3) — 느낌 손잡이
Changed    lib/sequencer.dart(한 줄) · lib/project.dart(저장) · lib/ui/scene_view.dart(칩)
Added      lib/feel.dart · lib/ui/feel_sheet.dart · test/feel_check_test.dart
Tests      36개 파일 전부 통과
Pending    폰 실측 — 폰이 잠겨 있다. 특히 **스윙이 실제로 스윙으로 들리는지**는
           귀로만 판정된다.
```

### Phase 2 를 마치며
계획 4장의 네 항목을 다 지났다:
4-1 첫 실행 · 4-2 Create Flow · 4-3 Simple/Advanced · 4-4 결과 중심 UX.

**다음(Phase 3, 음악 퀄리티)으로 가기 전에** 계획 17장대로 현재 상태를 다시 확인할 것.
특히 Phase 3 은 악기 음색을 만지는데, 48~53/N 에서 이미 상당 부분 했다 —
**남은 것이 무엇인지부터 추려야** 같은 일을 두 번 안 한다.

## Phase 3 (1) — 대표 악기 품질 · 믹스 안 존재감 (개선 계획 5장) (2026-08-23)

계획 17장대로 **먼저 남은 것을 추렸다.** 5-2 의 다섯 기준 중:

| 기준 | 상태 |
|---|---|
| 1. 기본 음색 | 웹에서 검증된 배음표를 그대로 쓴다 |
| 2. 세기 반응 | ✓ `env_check_test` · `drum_check_test` 가 이미 본다 (48·49/N) |
| 3. **음역별 자연스러움** | ✗ 없었다 |
| 4. **연속 연주** | ✗ 없었다 |
| 5. **믹스 안 존재감** | ✗ 없었다 |

계획이 못 박은 우선순위는 5번이다 — **"단독으로 좋은 소리보다 Mix 안에서 좋은 소리"**.
그래서 재는 것부터 만들었다: **`test/instrument_quality_test.dart`**.

### 잰 결과 — 피아노가 다른 악기보다 **23.5dB** 작았다
```
베이스 -12.2 · 서브베이스 -14.9 · 리드 -21.3 · 패드 -24.5
플럭 -25.1 · 일렉피아노 -26.1 · 피아노 -35.7      ← 킥보다 22.3dB 작다
```
혼자 들으면 멀쩡한데 곡에 넣으면 사라진다. 사용자가 "피아노 기타 안들려" 라고
한 그 지점이고, 53/N 에서 `soft` 를 손봐 3.4dB 올린 것으로는 부족했던 이유다.

원인은 **가산합성 경로에서 크기가 두 번 곱해지는 것**이었다(48/N 에서 세기 V 를
잡았는데 두 개가 더 있었다):
- `I.peak` 이 두 번 — 0.52 짜리 피아노는 0.27 이 된다(**−11dB**)
- `comp` 가 **만들지도 않은 오실레이터** 수로 나눈다. 유니즌·디튠은 가산합성에서
  아예 안 만들어진다(배음이 그 자리를 대신한다). 그런데도 √4 로 나눴다(**−6dB**)

고친 방법: **앰프 엔벨로프는 크기가 아니라 모양이다.** 크기는 전부 배음 쪽에 있으므로
엔벨로프 꼭대기를 1.0 으로 뒀다.

결과:
- 피아노 −35.7 → **−27.8dB** · 중역 악기 5종 편차 **6.6dB**
- 재즈 믹스에서 **기타 −43.4 → −34.0dB(+9.4)** · 비브라폰 −44.1 → −38.3(+5.8)
- 스타일 편차 4.4 → **3.9dB**(지금까지 최소) · 재즈 −14.4 → −13.0dB · 클리핑 0

### 드럼 좌우 자리 (계획 5-4 의 Hat Stereo)
드럼이 **전부 가운데**에서 났다. 실제 세트는 한 점에서 안 난다 — 전부 가운데면
심벌과 스네어가 같은 자리에서 겹쳐 뭉친다.

앞에서 본 기준으로 자리를 잡았다(**쇼 화면 밴드 그림과 같은 좌우**):
하이햇 오른쪽(+0.34) · 라이드 왼쪽(−0.34) · 탐 왼쪽 · 셰이커 왼쪽 · 크래시 오른쪽 ·
**킥·스네어는 가운데**(골격이라 치우치면 곡이 기운다).

등파워로 벌린다 — 팬만 바꿔서 곡 크기가 달라지면 안 된다.
하이햇·셰이커는 **자리도 조금씩 흔든다**(되풀이가 많아서 — 정확히 같은 점에서
나면 그게 또 하나의 '머신건'이다).

### 골든이 제 일을 했다
드럼을 벌리자 기준 곡 피크가 **0.9635 → 0.9474** 로 움직였고 시험이 잡았다.
한쪽에 몰리던 것이 나뉜 것이라 RMS·대역은 그대로다(총 힘은 안 변한다).
**의도한 변경이므로 기준값을 갱신하고 이유를 시험 파일에 적었다** — 갱신 이력까지.
이게 Phase 1 에서 이 시험을 만든 이유다.

### 아직 못 잡은 것
- **재즈 비브라폰 −24.7dB** — 이건 크기가 아니라 **편곡**이다(50초에 등장, 384음).
  계획 Phase 4(음악 자동화)에서 볼 자리.
- **일렉 피아노는 안 올라갔다** — 몸통 공명이 없어서(`BODY_RES` 에 없다) 원래
  앰프 엔벨로프를 안 타는 경로다. 즉 두 번 곱해지던 문제가 없었다.
  재즈 피아노가 일렉 피아노라 전체 대비 −12.9dB 다. 이건 결함이 아니라
  **믹스 균형** 문제라 Phase 4 Auto Mix 에서 다룬다.

### 검증
새 `instrument_quality_test.dart` — 대표 악기 7종 × (음역별 · 8연타 · 존재감) 17항목.
`drum_check_test` 에 좌우 자리 3항목.
`flutter test` — **37개 파일 전부 통과.** `flutter analyze lib` 깨끗.
`mix_meter` 재측정 — 편차 3.9dB · 클리핑 0.

### 남은 것
- 계획 5-3 의 Stereo behavior / Unison / Resonance 검증
- 5-4 Percussion 장르별 존재감
- **폰 실측** — 폰이 잠겨 있다. 좌우가 실제로 벌어져 들리는지는 폰에서만 안다.

## Phase 3 (2) — 스테레오 · 비용 · 폰 실측 (2026-08-23)

계획 5-3 이 남긴 것 중 **폰에 제일 직결되는 것**부터 봤다.

### 모노 합산 — 고칠 게 없었다
**이 앱의 목적지는 폰이다.** 폰 스피커는 사실상 모노라, 좌우로 벌린 소리가 합쳐질 때
서로 지우면 그 악기는 폰에서 안 들린다. 헤드폰으로는 멀쩡한데 폰에서만 사라지는 것이라
**귀로 잡기 제일 어려운 종류**다. 아무도 안 쟀길래 쟀다.

33종 전부 재 보니 **제일 큰 손실이 패드 0.99dB** — 넓은 패드가 1dB 잃는 건 정상이다.
저역 5종도 전부 중앙. **지금 설계가 이미 모노에 안전하다**는 게 결론이다.
고칠 것이 없으므로 **안 고쳤다.** 대신 **다시 깨지지 않게 시험으로 못 박았다**(기준 1.5dB).

### 비용 — 재 보니 문제가 아니었다
계획 5-3 이 "CPU 사용량을 함께 측정하십시오" 라고 했는데 악기별로는 안 재 봤다.

8음을 억지로 쌓아 보면 **피아노가 10.5%로 다음 악기(바이올린 5.5%)의 두 배**,
제일 싼 것의 7.6배다. 폰은 약 8배 느리니 84% — 위험해 보인다.

그런데 **실제 곡으로 재면 다르다**:
| 스타일 | 맥 | 폰 추정 | 최대 동시음 |
|---|---|---|---|
| 재즈 | 4.5% | 36% | 14 |
| 트랩 | 3.8% | 30% | 21 |
| 프로그하우스 | 3.1% | 25% | 13 |
| 발라드·로파이 | 2.4% | 19% | 10 |

폰 실측(오디오 여유 64% = 36% 사용)과 **정확히 맞는다.** 실제 곡은 8음짜리 피아노
화음을 쌓지 않는다. **재 보기 전에 고쳤으면 헛수고였다** — 이번에도 측정이 먼저였다.

빽빽한 재즈 대목을 흉내 낸 시험을 안전망으로 남겼다(한도 15%, 지금 5.8%).
세 배 여유를 둬서 **진짜 회귀만** 잡는다.

### 폰 실측 — 밀렸던 것을 한 번에
폰 잠금이 풀려서 Phase 2·3 을 통째로 확인했다. `pm clear` 로 **새로 깐 상태**부터.

| 확인 | 결과 |
|---|---|
| 첫 실행에 장르 시트 | ✓ 뜬다 |
| 장르 누르면 즉시 재생 | ✓ 힙합 88BPM · 정지 버튼 · 재생 위치 움직임 |
| 간단히 보기 기본 | **✗ → 고침** (아래) |
| 간단히 보기가 감추는가 | ✓ 조·연필 없음 · 스타일/느낌만 |
| 토글 → 전부 보기 | ✓ 믹서가 나타나고 열린다 |
| 쇼 화면 | ✓ 새 드럼 킷·밴드 정상 |
| 곡 2분 연속 재생 | ✓ 멈춤·깨짐 없음 |
| 급식 지체 | **20ms 초과 0회** · 언더런 0 · 하드클립 0 |

### 폰에서만 잡힌 버그 — 새로 깔면 간단히 보기가 안 켜졌다
`simpleMode` 는 `start()` 에서 `index.json` 을 읽어 정하는데,
**파일이 아예 없는 첫 설치**는 그 분기를 안 탄다. 필드 기본값(`false`)이 그대로 남아
처음 쓰는 사람에게 믹서·편집기가 다 보였다 — 정확히 안 하려던 것이다.

시험은 통과하고 있었다. `Store` 를 만들어 `simpleMode = true` 로 **직접 세팅**해서
확인했기 때문이다. **"만들자마자의 기본값"을 아무도 안 봤다.**
필드 기본값을 `true` 로 바꾸고, 그 한 줄을 보는 시험을 추가했다.

> 시험이 다 통과해도 **폰에 새로 깔아 보는 것과는 다르다.**
> 이번 것은 시험 환경에서는 구조상 안 나오는 종류였다.

### 검증
`instrument_quality_test` 에 3항목 추가(모노 합산 · 저역 중앙 · 빽빽한 곡 비용).
`simple_mode_test` 에 「새로 깐 상태의 기본값」 추가.
`flutter test` — **37개 파일 전부 통과.** `flutter analyze lib` 깨끗.

### Phase 3 남은 것
- 5-4 Percussion 장르별 존재감
- 5-3 Unison / Resonance 는 모노 합산 시험이 간접적으로 덮는다(둘 다 스테레오·위상 문제다)
- **청감** — 드럼 좌우가 실제로 벌어져 들리는지, 가산합성 악기가 커진 것이 과하지 않은지

## Phase 3 (3) — 스윙이 안 들리던 이유 (사용자 지적) (2026-08-23)

> "1. 폰 스피커가 모노라서 모르겠음 / 2. 피아노 기타 들리긴함 / 3. 스윙 모르겠음"

1번은 판정이 불가한 게 맞다 — 폰 스피커는 모노라 좌우가 안 들린다(이어폰·블루투스에서만).
2번은 됐다는 뜻으로 받았다. **3번이 진짜 문제였다.**

### 걸리긴 걸렸는데 **몇 개 안 걸렸다**
슬라이더를 끝까지 올렸을 때 실제로 밀린 타격 수를 세어 봤다:
`로파이 616개 중 88개(14%)` · `재즈 448개 중 0개(0%)`.

**8분 자리만 밀고 있었다.** 그런데 로파이·힙합·트랩은 **16분 기반**이라 8분 자리에
있는 타격이 몇 개 안 된다. 재즈는 라이드가 16분(0·3·4·7…)이라 아예 하나도 안 밀렸다.
그래서 만져도 아무 일이 안 일어난 것처럼 들렸다.

### 고친 방법 — **판이 쓰는 격자에 건다**
16분 자리에 뭐가 있으면 16분 셔플, 8분만 쓰면 8분 스윙.
어느 쪽이 더 많이 걸리는지 **세어서** 고른다.

여기서 한 번 헛짚었다 — 드럼과 음정 악기를 **합쳐서** 셌더니 재즈가 뒤집혔다
(라이드는 16분인데 멜로디가 8분이라 8분이 이겼고, 라이드가 하나도 안 밀렸다).
**그루브는 드럼이 정한다.** 드럼으로 격자를 뽑고 음정 악기는 거기 맞춘다 —
악기마다 격자가 다르면 그건 스윙이 아니라 어긋난 것이다.

| 스타일 | 고치기 전 | 고친 뒤 | 최대 밀림 |
|---|---|---|---|
| 로파이 | 14% | **55%** | 64ms |
| 힙합 | 14% | **39%** | 114ms |
| 트랩 | — | **34%** | 36ms |
| 재즈 | **0%** | **57%** | 43ms |
| 발라드 | 2% | **40%** | 147ms |

### 칩에 뭘 만졌는지 안 떴다
그루브만 만졌는데 칩에는 「보통」(기운)이 떴다 — 뭘 만졌는지 알 수가 없다.
**제일 많이 움직인 손잡이**를 보여 주게 고쳤다(그루브만 만지면 「많이 스윙」).

### 검증
`feel_check_test` 에 3항목 추가 — 16분 판은 16분 셔플 · 8분 판은 8분 스윙 · 칩 표시.
`flutter test` — **37개 파일 전부 통과.** 폰에 설치 완료.

### 남은 것
- **청감** — 이제 들리는지. 로파이·재즈에서 그루브를 끝까지 올려 보면 제일 확실하다.
- 스테레오는 **이어폰이나 블루투스 스피커**로만 판정된다.

## Phase 4 (1) — 필(Fill) (개선 계획 6-2) (2026-08-23)

### 먼저 안 한 것 — 음색별 크기 정규화
Auto Mix(계획 6-5)로 들어가기 전에, 사용자가 겪은 "안 들려"의 뿌리인
**음색을 바꾸면 트랙 크기가 달라지는 문제**를 다시 쟀다. Phase 3 수정 뒤 상태:

| 역할 | 벌어짐 |
|---|---|
| 베이스 6종 | 3.7dB |
| 코드/패드 10종 | 6.2dB |
| 멜로디 17종 | **11.2dB** (플루트 −19.4 ↔ 스탭 −30.6) |

멜로디의 11.2dB 를 정규화할까 하다가 **안 했다.** 대부분이 **지속음 대 감쇠음**
차이다 — 0.4초 RMS 로 재면 계속 소리 내는 플루트가 튕기고 마는 하프보다 당연히 크다.
같게 맞추면 하프의 어택이 과해진다. **재 보고 안 고친** 두 번째 경우다
(첫 번째는 믹서 레코드, 세 번째는 피아노 CPU).

### 한 것 — 필
지금 문제: **2분짜리 곡이 똑같은 4마디의 되풀이**다. 아무리 음색이 좋아도
그러면 '루프'로 들린다. 사람이 만든 음악은 4·8마디마다 매듭을 짓는다.

`lib/fill.dart` — `applyFeel` 과 같은 자리(시퀀서가 만든 목록의 마지막)에서 끝낸다.
엔진·패턴 데이터는 안 건드리고, 재생과 내보내기가 같은 목록을 쓰므로 둘이 저절로 같다.

하는 일 셋:
1. **필 자리를 갈아엎는다** — 킥만 남기고 지운다(킥은 발이라 필 중에도 계속 밟는다)
2. 스네어→탐으로 16분을 깔며 **세기를 올린다**(1·2·2·3). 탐은 음정이 내려간다
3. 다음 마디 첫 박에 **크래시** — 매듭이 지어졌다는 신호

손잡이 하나(「매듭」)가 **빈도와 길이를 같이** 정한다 — 사람은 「가끔/보통/자주」로
생각하지 「8마디마다 4스텝」으로 생각하지 않는다.
없음 / 가끔(8마디마다) / 보통(4마디마다 한 박) / 자주(4마디마다 반 마디).

### 여기서 배운 것 — 패턴이 이미 자기 필을 갖고 있었다
처음엔 잔가지(하이햇·라이드)만 걷어내려 했다. 그런데 창 안을 찍어 보니
**스네어 3연타가 이미 들어 있었다** — 로파이 코러스는 마지막 마디에 자기 필이 있다.
그 위에 또 깔면 타격이 겹쳐 지저분해진다. 그래서 **갈아엎는 쪽**으로 바꿨다.

측정으로 안 봤으면 "필을 넣었는데 왜 지저분하지"로 한참 헤맸을 자리다.

### 검증
`feel_check_test` 에 6항목 추가 — 크래시로 매듭 · 필 자리 갈아엎기 · **킥은 남음** ·
세기 상승 · 하이햇 걷힘 · **끄면 한 글자도 안 바뀜**.

`scene_check_test` 의 「짧은 패턴이 루프를 채움」이 깨졌다 — 필이 마지막 마디를
갈아엎어 타격 수가 바뀌어서다. 그 시험은 **타일링**을 재는 것이므로 필을 꺼 뒀다.
(안 끄면 필을 손볼 때마다 엉뚱하게 깨진다.)

`flutter test` — **37개 파일 전부 통과.**
`mix_meter` — 스타일 편차 **3.5dB**(또 줄었다) · 클리핑 0. 폰에 설치 완료.

### 남은 것
- **청감** — 필이 자연스러운지. 4마디마다 매듭이 지어지는 게 들리는지.
- 계획 6-1 Pattern Variation · 6-3 Build/Break/Drop · 6-5 Auto Mix

## Phase 4 (2) — Auto Mix: 킥↔베이스 비켜 주기 (계획 6-5) (2026-08-23)

계획 6-5 가 제일 먼저 꼽은 충돌이 **Kick↔Bass** 다. 둘 다 저역을 쓰니 겹치면
킥이 뭉개지거나 베이스가 안 들린다. 표준 처리는 **킥이 칠 때 나머지를 잠깐 낮추는 것**
(사이드체인) — 하우스·프로그하우스에서는 그게 곧 그 장르의 소리(펌핑)이기도 하다.

### 장르 값으로 뒀다
```
프로그하우스 0.50 · 하우스 0.42 · 트랩 0.32 · 팝 0.18 · 엠비언트 0.18
힙합 0.14 · 로파이 0.10 · 시티팝 0.08 · 록 0 · 발라드 0 · 재즈 0
```
**재즈·발라드·록에 펌핑을 걸면 그건 개선이 아니라 고장이다.** 사람이 친 드럼은
비켜 주지 않는다. 데이터로 두면 장르를 더할 때 코드를 안 고친다(계획 7-1).

거는 자리는 **드럼을 더하기 직전** — "드럼 빼고 전부"가 눌리고 킥은 그대로 뚫고 나온다.
되돌아오는 시간 160ms(더 빠르면 딸꾹질, 느리면 늘 눌린 채).

### 재생 경로에 안 닿을 뻔했다
씬·곡 재생은 `setGenreMix(null)` 로 **장르 몫을 일부러 걷어낸다**(사용자가 만진
믹서 값을 살리려고). 거기에 비켜 주기를 얹었으면 같이 지워졌다.
**슬롯별 믹스 값이 아니라 그 장르의 성격**이므로 전용 명령(`setDuck`)으로 따로 보낸다.
내보내기(`ExportJob.duck`)에도 실었다 — 안 실으면 파일만 안 비켜 준다.

### 재는 법을 세 번 틀렸다
크기 비율(dB)로 재려다 계속 실패했다:
1. 킥 앞뒤를 견줬다 → 베이스 자체의 감쇠와 섞였다
2. 두 렌더의 dB 차이를 봤다 → **킥이 창을 덮었다**(킥만 −5.6dB, 멜로디를 더해도 −5.4dB)
3. 500Hz 하이패스 → 킥의 '딱'(2.8kHz)이 통과했다

원인은 산수였다 — **킥이 멜로디보다 7배 커서, 멜로디를 통째로 지워도 전체 RMS 는
1dB 밖에 안 움직인다.** 결국 **두 렌더를 빼서** 봤다. 드럼은 안 눌리니 두 렌더에서
똑같고, 남는 차이가 곧 눌린 멜로디다. 헷갈릴 여지가 없다.

잰 값(0.45로 걸었을 때): 킥 직후 멜로디의 **47%** 가 깎이고, 0.4초 뒤 **8%** 로 회복.
안 걸면 차이 정확히 0.

> 「그럴듯한 숫자가 나올 때까지 재는 법을 바꾸는 것」과
> 「재는 법이 틀렸다는 걸 알아내는 것」은 다르다. 여기서는 **킥만 따로 재 보고**
> 창을 덮는다는 사실을 확인한 뒤에 방법을 바꿨다.

### 검증
`instrument_quality_test` 에 5항목 — 안 걸면 0 · 걸면 눌림 · 되돌아옴 ·
**어쿠스틱 장르는 안 걺** · 전기 장르는 걺.
`flutter test` — **37개 파일 전부 통과.** `mix_meter` 편차 3.5dB · 클리핑 0.

### 남은 것
- **폰 설치·청감** — 폰이 또 빠졌다(케이블). 하우스·프로그하우스에서 펌핑이 들리는지,
  재즈에서 안 들리는지가 판정 기준이다.
- 계획 6-1 Pattern Variation · 6-3 Build/Break/Drop

## Phase 4 (3) — 변형(Pattern Variation) (계획 6-1) (2026-08-23)

계획 6-1 의 요구는 「각 Pattern 에 Original / Variation 1·2·3 개념을 구조화하고,
사용자가 노트를 만드는 게 아니라 **기존 Pattern 을 기반으로 자연스러운 변형을 생성**」이다.
필(6-2)이 마디의 **끝**을 표시했다면, 이건 마디의 **본문**이 매번 같은 문제다.

### 원칙 하나 — 작곡된 것은 안 바꾸고, 되풀이된 것만 바꾼다
4마디 패턴은 4마디로 작곡된 것이다. 그 안을 손대면 원곡이 망가진다.
반면 2마디 패턴이 4마디 판을 채우려 **두 번 도는 것**은 작곡이 아니라 되풀이다.
그 두 번째 바퀴가 변형을 받는다. 필에서 배운 것과 같다 —
「패턴이 이미 자기 필을 갖고 있었다」, 있는 것 위에 덧칠하지 않는다.

`lib/variation.dart` 는 **순수 함수**다. 난수를 안 쓰고 변형 번호(0·1·2·3)만으로
결정된다 — 씨앗을 관리할 필요조차 없다. 0번은 **받은 것을 그대로** 돌려준다.

거는 자리는 필·느낌과 다르다. 둘은 다 만들어진 목록을 마지막에 주무르는데,
변형은 **만드는 중에** 걸어야 한다 — 완성된 목록에서는 어느 타격이 몇 바퀴째
것인지 알 수 없기 때문이다.

### 수(手) — 파트마다 성격이 다르다
```
드럼   ① 잔가지 솎기  ② 고스트 스네어  ③ 킥 밀기      ④ 악센트 옮기기
베이스 ① 끝음 옥타브  ② 약박 덜기      ③ 레가토       ④ 여리게
멜로디 ① 약박 덜기    ② 끝음 옥타브    ③ 레가토       ④ 여리게
코드   ① 윗음 빼기    ② 레가토         ③ 여리게
```
**코드에서는 타격을 안 뺀다.** 코드 하나가 곧 그 마디의 화성이라, 빼면 진행이
사라진다(Am–F 에서 F 를 빼면 그냥 Am 두 마디다). 두께와 세기만 만진다.

킥·스네어·박수는 **골격**이라 개수도 세기도 안 건드린다. 멜로디의 강박도 안 준다.

### 못 하면 다음 수를 둔다
하우스 패턴에는 스네어가 없다(박수를 쓴다). 고스트 스네어를 걸면 **아무 일도
안 일어난다** — 번호는 올라갔는데 소리는 그대로다. 그래서 각 수는 「할 게 없으면
null」을 돌려주고, 그러면 다음 수로 넘어간다.

시험이 이걸 잡아냈다. 72개 패턴 × 3번을 전부 돌려 보니 **발라드 인트로와
프로그 브레이크가 세 수 모두 실패**했다 — 킥도 스네어도 없고 셰이커가 4분마다만
쳐서 뺄 것도 더할 것도 없었다. 그래서 두 가지를 더했다:
1. 솎기의 「약박」을 두 단계로 — 16분 뒷자리가 없으면 8분 뒷자리까지 본다
2. **악센트 옮기기**(마지막 수) — 뺄 것도 더할 것도 없으면 세기의 무늬를 바꾼다

### 씬 루프가 0% 였다
처음 재 보니 **11개 장르 중 10개가 씬에서 0%** 였다. 씬 루프는 한 바퀴만 만들어
무한 반복하므로(`reps: 1`), 변형이 들어갈 자리가 없다 — 늘 첫 바퀴다.
곡에서만 5~44% 바뀌었다.

씬 화면은 사용자가 제일 오래 듣는 곳이고 「변화」 손잡이도 거기 있다.
**아무 일도 안 하는 손잡이는 없느니만 못하다.**

그래서 씬 루프를 **변형 한 주기만큼**(2 또는 4바퀴) 한 판으로 돌린다.
변형을 끄면 주기가 1이라 예전과 완전히 같다.

여기서 걸리는 게 하나 있었다 — `LoopClock` 은 자유 시계가 아니라 0.25초마다
`loopPos` 로 **다시 맞춘다**. 판이 4배 길어지면 재생 머리가 격자 위에서 튄다.
그래서 `setLoop` 에 **눈금 길이**(`unitSec`)를 따로 준다. 판은 4바퀴, 눈금은
한 바퀴 — `posOf` 가 눈금 안의 위치를 돌려준다. 안 주면 눈금=판이라 예전 그대로다.
화면 격자와 라이브 녹음이 쓰는 `loopBars`/`loopSec` 은 **한 바퀴 값 그대로** 둔다.

### 두 번째 벌스가 첫 번째와 똑같았다
구간마다 `build` 를 새로 부르므로 변형 번호가 0부터 다시 시작했다 —
로파이·시티팝은 벌스가 두 번 나오는데 **한 음도 안 달랐다.**
`build` 에 `barsBefore`(앞 구간까지 지나간 마디 수)를 넘겨 곡 전체로 잇는다.

### 잰 값
자리(리듬·음높이·길이)가 바뀐 비율 — 세기는 뺐다.
```
              1바퀴  2바퀴  3바퀴   곡 전체
프로그하우스    22%   50%   12%     58%
하우스          21%   73%   32%     35%
팝              23%   88%   16%     60%
로파이          25%   38%   25%     30%
재즈            21%   81%   17%     46%
```
처음엔 「세기까지 포함」으로 재서 110%·131% 같은 수가 나왔다 — 세기를 한 단
낮추는 수가 **모든 음을 다르게** 만들어서다. 자리와 세기를 갈라 재고 나서야
2바퀴가 사실상 세기만 바뀐다는 걸 알았다. 그래서 세기 수를 **마지막 수로 밀고**
레가토(이어 붙이기)를 넣었다. 자리 2% → 50~88% 로 올랐다.

레가토는 **한 마디를 넘겨서까지 늘이지 않는다** — 엠비언트처럼 음 사이가 먼
곡에서 몇 초짜리 음이 겹겹이 쌓인다.

### 손잡이
「변화」 하나 — **없음 / 가끔(두 바퀴마다) / 자주(네 바퀴마다)**.
말과 동작이 하나씩 짝이다. 슬라이더는 움직이는데 하는 일이 같으면 제일 나쁜
손잡이라, 「보통」 같은 중간 말을 일부러 안 만들었다(그 자리에 다른 동작이 없다).

손잡이가 다섯이 되면서 **가로에서 「변화」가 화면 밖으로 밀렸다.** 스크롤은
되지만 스크롤바 없는 시트에서 화면 밖은 곧 없는 것이다 → 누우면 **두 칸**으로 놓는다.

### 검증
- `variation_check_test` (새로 씀) — 16항목. 0번은 원본 그대로 · 번호마다 반드시
  달라짐(드럼 72 · 멜로디/베이스 89 · 코드 52개 패턴 × 3번) · 골격 안 줄어듦 ·
  코드 타격 수·자리·근음 그대로 · 레가토가 다음 음을 안 넘음 · 결정적 ·
  씬 바퀴마다 다름 · **끄면 바퀴가 전부 같음** · 곡의 되풀이 구간이 처음과 다름
- `feel_ui_test` (새로 씀) — 세로·가로 안 넘침 · 다섯 손잡이 다 보임 ·
  「처음으로」에 닿음 · 슬라이더가 값을 바꿈
- `save_roundtrip_test` — **느낌 다섯 항목이 왕복에서 빠져 있었다**(변화만이 아니라
  기운·빽빽함·그루브·매듭 전부). 채웠다.
- `scene_check_test`·`genre_keep_test` — 타격 수를 세는 시험 셋이 깨졌다.
  전부 **타일링·판 지키기를 재는 시험**이라 변형을 꺼 뒀다(필과 같은 처방).
- `flutter test` — **39개 파일 전부 통과.** `flutter analyze lib` 깨끗.
- `mix_meter` 편차 3.5dB · 클리핑 0. `buildSong` 0.85 → 0.9ms(2400음 기준).
- 골든(`engine_guard_test`)은 안 건드렸다 — 기준 곡은 엔진에 직접 예약하므로
  시퀀서를 안 지난다.

### 폰 확인 (2026-08-23 21:5x)
설치·실행 확인. 60fps 유지 · 앱 오류 로그 0 · `AudioTrack state:started` 48kHz 스테레오.
느낌 시트에 「변화 — 자주」가 세로에서 스크롤 없이 다 들어간다.
재생 머리가 4마디 격자 안에서 정상으로 움직인다(「2마디 3박」) — **눈금(`unitSec`)이
제대로 걸렸다는 증거**다. 안 걸렸으면 판이 4배라 머리가 격자를 기어갔을 것이다.

여기서 하나 더 나왔다 — 씬 화면 상태줄이 「한 판 4마디 · 타격 245개」로 찍혔다.
245는 **네 바퀴 몫**이다. 바퀴 수로 나눠서 보여 주고, 여러 바퀴면
「4바퀴마다 변화」를 덧붙인다. 폰에 띄워 보지 않았으면 못 봤을 자리다.

### 남은 것
- **청감** — 판정 기준 셋:
  ① 하우스·프로그하우스에서 펌핑(비켜 주기)이 들리는지, 재즈에서 안 들리는지
  ② 4마디마다 매듭(필)이 지어지는지
  ③ **같은 4마디가 바퀴마다 달라지는지** — 특히 2바퀴째(레가토)가 어색하지 않은지
- 계획 6-3 Build/Break/Drop

## Phase 3(2)·4(4) — 사용자 지적 네 가지 (2026-08-23)

폰으로 들어 보고 온 지적:
```
1. 드럼 킥 스네어가 너무 구려
2. 스윙도 느낌이 하나도 안 나
3. 필인은 송폼이 넘어갈 때나 송폼이 많이 반복되면 넣어야지
4. (변형은) 모르겠어
```
넷 다 **맞는 지적**이었고, 넷 다 원인이 달랐다.

### 1. 킥·스네어 — 「구리다」를 숫자로 바꿨다
「구리다」는 고칠 데를 안 알려준다. `drum_meter` 를 새로 써서 잤다:
```
             길이(-20dB)   타점    ~60  60~150  150~400  ...  폰에서 들리는 몫
킥 acoustic      94ms     1.3ms    2%    36%     48%          62%
킥 rock          75ms     1.3ms
스네어 acoustic  47ms     6.4ms
```
**킥이 75~142ms 밖에 안 울렸다.** 그건 「둥」이 아니라 「톡」이다. 원인도 나왔다 —
어쿠스틱·록 킥은 `sub` 가 **0** 이라 울려 줄 저음 층이 아예 없었다.
스네어는 34~60ms, 타점 5.9~7.7ms. 짧고 흐리다.

고친 뒤: **킥 176~405ms · 스네어 95~130ms · 스네어 타점 1.4ms.**

한 번 지나쳤다가 되돌린 것 — 처음엔 킥을 600ms 까지 늘렸는데 **폰에서 들리는 몫이
62% → 43%** 로 떨어졌다. 저음을 키우면 폰 스피커에서는 오히려 안 들린다.
길이는 300ms 대로 되돌리고 대신 `drive`(포화)를 올려 배음으로 존재감을 냈다.

### 스네어 타점 — 가설을 두 번 틀렸다
「밴드패스가 높은 데서 시작해 늦게 내려온다」고 보고 스윕을 두 단으로 나눴다 →
**안 변했다.** 더 빠르게(1.5ms) 해 봐도 안 변했다.

그래서 **층을 갈라서 쟀다**:
```
전체   타점 6.6ms      잡음만 2.0ms      몸통만 6.5ms
```
범인은 몸통이었다. 몸통 음 둘(188·332Hz)이 **144Hz 차로 맞물려 6.9ms 마다 겹치는데**,
첫 겹침은 어택 램프에 깎이고 **두 번째 겹침이 최고점**이 된다. 유지 구간(8ms)을
없애 처음부터 감쇠시키니 1.4ms 가 됐다. 얇아진 몸통은 크기를 올려 되찾았다.

> 가설을 세 번 세워 두 번 틀렸다. **층을 가르는 데 5분이면 됐을 것을**
> 필터를 두 번 만지느라 돌아갔다. 안 되면 만지지 말고 갈라 볼 것.

### 자가 또 틀렸다 (네 번째)
킥을 길게 고치자 `loop_check_test` 가 「누적 오차 20초」로 터졌다. 루프는 멀쩡했다 —
타격을 「조용하다가 소리 남」으로 찾고 있었는데 **킥이 길어져 타격 사이가 조용하지
않아서** 두 번째부터 통째로 안 잡혔다. 「갑자기 커짐」으로 바꾸니 **누적 오차 3프레임
(0.06ms)**. 이 프로젝트에서 자가 틀린 게 네 번째다.

### 2. 스윙 — 한 격자로는 안 됐다
재 보니 **재즈 멜로디가 92음 중 0음 밀렸다.** 격자를 드럼에서 세어 골랐는데,
재즈는 라이드가 16분이라 16분 격자가 뽑히고 → 8분 위의 멜로디는 전부 '앞칸'.
반대로 8분 격자를 고르면 로파이 16분 하이햇이 죽는다. **고칠 때마다 반대쪽이 깨졌다.**

추측으로 될 일이 아니었다. `kGenreSwingGrid` 로 뺐다 — 8분 스윙(재즈·발라드·시티팝·
팝·록·힙합·엠비언트) / 16분 셔플(로파이·트랩·하우스·프로그하우스).
힙합은 이론상 16분 셔플이지만 **붐뱁 하이햇이 8분이라** 16분으로 밀면 20% 밖에
안 움직인다 → 8로 뒀다. 격자는 「이론상 무슨 셔플인가」가 아니라 **그 패턴이 어디에
있는가**로 정해야 들린다.

계단식(홀수 칸만 민다)도 버렸다. **시간을 늘였다 줄이는 연속 워프**로 바꿨다 —
두 칸을 묶어 앞칸을 늘이고 뒷칸을 그만큼 줄인다. 격자에 안 맞는 음(필·글라이드)도
같이 밀려서 프레이즈가 통째로 스윙한다. 쌍의 길이는 안 변하므로 박이 안 밀린다.

세기도 넣었다 — **뒷칸 잔가지는 한 단 여리게.** 「따-단」의 '단'이 같은 크기면
그냥 늦게 친 것으로 들린다. 손잡이 곡선도 `groove^0.65` 로 눕혀 가운데에서 이미 스윙이다.

결과: 밀린 타격 34~59%(전 장르), **재즈 멜로디 0/92 → 36/92.**

### 3. 필 — 매듭이 너무 잦았다
「4마디마다」는 기계다. 지금은 **구간이 끝나는 자리**에 넣고, 구간이 길 때만 안쪽에도 넣는다:
```
가끔 → 구간 끝에만    보통 → 구간 끝 + 8마디마다    자주 → 구간 끝 + 4마디마다
```
그리고 **정작 송폼이 넘어가는 자리에만 크래시가 없었다.** 매듭의 크래시는 「필 다음
마디 첫 박」인데, 구간 끝 필의 다음 마디는 다음 구간이라 이 구간 안에 넣을 자리가
없었기 때문이다. 부르는 쪽이 「앞 구간이 필로 끝났다」를 아니까(`crashOnStart`)
다음 구간 첫 박에 얹는다. 확인: 넘어가는 자리 크래시 6/6·9/9 (아웃트로 뒤는 없음 — 맞다).

### 4. 변형 — 조심스러워서 안 들렸다
「약박 하나 걸러 하나」·「고스트 하나」·「킥 하나」. 4마디 안에서 타격 몇 개 바뀌는 건
안 걸린다. **한 바퀴의 성격을 통째로 바꾸는 것**으로 갈았다:
```
드럼   성기게(16분 하이햇 → 8분) ↔ 촘촘하게(8분 → 16분) ↔ 고스트+밀기 ↔ 악센트
멜로디 약박 덜기 ↔ **통째로 옥타브 위** ↔ 레가토 ↔ 끝음 ↔ 여리게
```
「두 번째 벌스는 한 옥타브 위로」는 실제 편곡의 상투 수단이고, 음이 그대로라 틀릴 수가
없는데 귀에는 제일 크게 걸린다. 자리 기준 바퀴별 차이 12~89% / 42~106% / 7~98%
(전에는 10~25% / 19~88% / 4~102%).

### 검증
- `drum_meter`·`swing_meter` 새로 씀(수동 도구). `vary_meter` 는 길이(`len`)까지 보게 고침
- `feel_check_test` +6항목 — 재즈 8분 멜로디가 밀린다 · 로파이는 안 밀린다 ·
  쌍의 시작은 제자리 · 뒷칸이 여려진다 · 가끔=구간 끝에만 · 자주=안쪽에도
- `variation_check_test` +1 — 잔가지 밀도가 바퀴마다 20% 넘게 바뀐다
- `loop_check_test` — 타격 찾는 자를 「갑자기 커짐」으로 고침
- 골든 갱신: 피크 0.9474→0.9416 · RMS 0.1736→**0.1816**(더 오래 울리니 당연)
- `flutter test` **39개 파일 전부 통과** · `analyze lib` 깨끗
- `mix_meter` 편차 3.2dB · 클리핑 0 · CPU 맥 6.0%(폰 추정 48%)
- 최대 동시 드럼 5~7 (한도 24) — 킥을 3배 늘렸어도 여유

### 남은 것
- **폰 설치** — APK 는 나왔는데(22:33) 폰이 또 빠졌다(케이블). 꽂으면 바로 올린다.
- **청감** — 킥·스네어가 「둥」·「탁」으로 들리는지, 스윙이 스윙으로 들리는지,
  매듭이 알맞은 자리에 오는지, 바퀴마다 달라지는 게 이제 들리는지
- 계획 6-3 Build/Break/Drop

## Phase 4 (5) — 구간 성격 Build / Break / Drop (계획 6-3) (2026-08-23)

계획 6-3 이 요구한 Intro / Build / Drop / Break / Outro.

### 먼저 찾은 것 — 이름은 이미 다 있었다
구간 이름을 훑어보니 **필요한 게 전부 있었다**:
```
프로그하우스  인트로 그루브 브레이크다운 빌드업 드롭 그루브 브레이크2 빌드업 드롭 아웃트로
하우스       인트로 벌스 코러스 브레이크 벌스 코러스 아웃트로
엠비언트      여명 안개 눈 정적 해질녘
```
없던 것은 **이름이 소리를 바꾸는 길**이었다. 빌드업이 벌스와 똑같이 들리면
그건 빌드업이 아니라 이름표다. 그래서 새 데이터를 만들지 않고 `roleOf(이름)` 하나만 붙였다.
모르는 이름은 전부 `normal` 로 떨어져 **한 글자도 안 바뀐다**(내가 지은 씬 이름도 안전하다).

### 어디까지 했는가 — 못 하는 것을 적어 둔다
계획은 필터 열림 · 리버브 변화 · 라이저도 적었다. 그건 **구간 안에서 값이 움직이는
자동화**가 필요한데 지금 엔진에는 그 길이 없다(버스 값은 재생 시작 때 한 번 보낸다).
없는 길을 내려면 실시간 오디오 경로를 건드려야 하고 그건 §15 가 막은 자리다.
**세기 곡선과 밀도만** 썼다 — 이 둘로도 「차오른다 / 확 비었다 / 꽉 찼다」는 다 들린다.

```
빌드업  세기가 구간 전체에 걸쳐 오른다 · 뒤로 갈수록 잔가지가 촘촘 · 마지막 마디는 발이 멎는다
드롭    골격과 음이 한 단 위
브레이크 잔가지를 걷고 한 단 아래 — 골격은 남긴다
인트로  잔가지 절반 · 잔가지와 음만 한 단 아래(골격은 그대로)
아웃트로 뒤로 갈수록 잦아든다
```
**골격(킥·스네어·박수)은 안 지운다.** 딱 하나 예외가 빌드업의 마지막 마디인데,
실수가 아니라 **드롭을 세게 만들려고 일부러 비우는 자리**다. 2마디짜리 짧은 구간에서는
안 한다(절반이 발 없이 가면 곤란하다).

필 **뒤**에 건다 — 필은 「킥은 발이라 계속 밟는다」가 규칙이라 마지막 마디에 킥을
남겨 두는데, 빌드업에서는 그 발이 멎어야 하기 때문이다.

### 재 보고 알게 된 것 둘

**① 드롭은 드럼을 못 키운다.** 전부 한 단 올렸더니 평균 세기가 2.95/3 이 됐다 —
세기 단이 셋뿐이라 거의 모두 3 이고 하이햇의 강약 무늬가 통째로 사라진다.
꽉 찬 게 아니라 **납작해지는 것**이다. 골격만 올리게 바꿨더니 이번엔 **아무 일도
안 일어났다** — 라이브러리 패턴의 킥·스네어는 애초에 세기 3(최대)이라 올릴 데가 없다.

이건 고칠 결함이 아니라 이 시스템의 사실이다: **제일 큰 데를 더 키울 수는 없다.
나머지를 낮춰야 한다.** 드롭의 대비는 앞의 빌드업(발이 멎는다)과 낮춰 놓은
인트로·브레이크·아웃트로에서 나온다. 드롭 자체에서 달라지는 건 **멜로디·베이스**다
(계획이 적은 "Main melody" 가 그것이다).

**② 브레이크는 평균 세기로 재면 오히려 세다.** 여린 하이햇을 통째로 걷어내니
남은 킥·스네어만으로 평균이 올라간다. 실제로 그렇게 나와서 시험이 한 번 깨졌다.
귀가 듣는 것은 평균이 아니라 **초당 총량**이다 — 그걸로 재니 제대로 나온다.

### 잰 값
```
프로그하우스 인트로 1.75 | 그루브 1.96 | 브레이크다운 밀도0.9 | 빌드업 2.1→3.0 | 드롭 2.49
하우스      인트로 1.62 | 벌스 2.03 | 코러스 2.51 | 브레이크 밀도1.6 | 아웃트로 1.8→1.1
트랩       프리훅 1.4→2.1 | 훅 2.58 | 아웃트로 1.9→1.1
```
빌드업은 **평균으로는 안 보인다**(앞을 내리고 뒤를 올리니 상쇄된다) — 앞뒤 반씩
갈라 봐야 차오르는 게 보인다. 계량 도구를 그렇게 고쳤다.

### 검증
- `arrange_check_test` (새로 씀) — 15항목. 이름→성격 17개 · **보통 구간은 한 글자도
  안 바뀜** · 빌드업 뒤가 세다/촘촘하다/발이 멎는다/짧으면 안 뺀다 ·
  드롭은 골격만(잔가지 강약이 살아 있다) · 브레이크는 잔가지만 걷고 골격은 남는다 ·
  아웃트로는 잦아든다 · 인트로는 골격 세기를 안 건드린다 ·
  **성격을 걸면 안 걸었을 때보다 그쪽으로 간다**(11개 장르)
- 마지막 항목은 **구간끼리 안 견준다** — 발라드는 코러스 패턴이 벌스보다 드럼이
  성긴데 그건 패턴 데이터 몫이지(Phase 5) 편곡이 못 한 게 아니다.
  `buildSongPlain`(성격 안 걺)과 견주는 쪽으로 바꿨다.
- `editor_check_test` — 씬 이름이 코러스라 세기가 한 단 올라 깨졌다.
  그 시험은 **편집기 값이 재생까지 가는가**를 재므로 성격을 꺼 뒀다(필·변형과 같은 처방).
- `arrange_meter` 새로 씀(수동 도구)
- `flutter test` **40개 파일 전부 통과** · `analyze lib` 깨끗
- `mix_meter` 편차 3.4dB · 클리핑 0

### 남은 것
- **폰 설치·청감** — 인트로가 얇게 들어오는지, 빌드업이 차오르는지,
  드롭이 터지는지, 브레이크가 비는지, 아웃트로가 잦아드는지
- Phase 4 는 이걸로 6-1·6-2·6-3·6-5 를 다 지났다. 남은 건 **6-4 Auto Harmony**.

## Phase 4 (6) — 어울리는 코드 고르기 Auto Harmony (계획 6-4) (2026-08-23)

계획 6-4: 「현재 Key + 현재 Melody → 추천 Chord. 사용자가 음악 이론을 몰라도
잘 어울리는 화성을 얻는 것이 목표.」 **Phase 4 의 마지막 항목**이다.

### 왜 쉬운 문제였나
이 프로젝트는 멜로디도 코드도 **도수**로 저장한다(멜로디 = 스케일 도수 0~14,
코드 = 다이아토닉 번호 0~6). 주파수를 거칠 일이 없다 — 조를 바꾸면 둘 다 같이
옮겨 가므로 **어느 조에서 풀든 답이 같다.** 조는 「어느 번호가 dim 인가」에만 쓴다.

### 기준선을 두 번 틀렸다 — 그리고 그게 전부였다
처음엔 자동이 **89% 맞고** 사람이 쓴 코드는 63% 밖에 안 맞는다고 나왔다.
「자동이 사람보다 낫다」는 결론이 나올 뻔했는데, **사람과 같은 코드를 고른 비율이
34%** 였다. 이게 이상했다 — 사람보다 나은데 사람과 다르다?

기준이 틀렸다. 멜로디·코드 짝을 **패턴 이름 앞부분**으로 묶고 있었다
(「Lofi Hook」→ 「Lofi」로 시작하는 첫 코드 패턴). 그러면 **벌스 멜로디에 코러스 코드**가
붙는다. 실제 씬에서 같이 울리는 짝으로 다시 잡으니:
```
                        틀린 짝    바른 짝
사람이 쓴 코드가 맞는 정도  63%   →   76%
자동이 맞는 정도           89%   →   89%
사람과 같은 코드           34%   →   60%
```
**기준이 틀리면 그 뒤 숫자는 전부 뜻이 없다.** 「사람이 63%밖에 안 맞는다」가
이상하다고 느낀 게 유일한 단서였다.

### 마디별로 고르면 안 된다 — 그런데 흐름은 거의 도움이 안 됐다
사람이 쓰는 진행은 마디별 최적이 아니라 흐름이다(5도 아래로, 한 음 위로, 3도 아래로).
그래서 **한 줄 전체를 한 번에** 고르게 바꿨다(동적 계획법 + 뿌리 움직임 점수).

기대는 컸는데 잰 값은 이렇다:
```
흐름 가중치  0    1    2    3    5    8
맞는 정도   89%  89%  88%  87%  84%  78%
사람과 같음 59%  60%  56%  51%  46%  37%
```
**흐름을 넣어 얻은 건 1점이다.** 더 세게 걸면 오히려 나빠진다.
기본값 1.0 으로 두되 「크게 이겼다」고 적지 않는다 — 잰 대로 적는다.
(DP 구조 자체는 남긴다. 이게 있어야 「쉬는 자리는 코드를 안 바꾼다」 같은 규칙을 넣을 수 있다.)

### 시험이 잡은 것 — 쉬는 자리에서 코드가 혼자 돌아다녔다
멜로디가 없는 마디는 맞고 틀리고가 없으니 점수가 전부 0 이고, 그러면 흐름 점수만
남아서 **「움직이면 좋다」만 보고 0→3→6→2 로 돌아다녔다.** 바꿀 이유가 없으면
그대로 두는 게 맞다 → 빈 자리는 앞 코드를 이어 간다.

### 어디에 붙였나
편집기의 **코드 판에서만** 「멜로디에 맞추기」가 나온다. 멜로디 판·드럼 판에는 없다
(§15 「기능이 있다는 이유만으로 UI에 모두 노출」). 씬에 멜로디가 없으면 흐려진다.
판 전체를 갈아엎는 일이라 **되돌리기가 받쳐 준다**.

### 검증
- `harmony_check_test` (새로 씀) — 11항목. 조를 옮겨도 같은 번호 · 뻔한 멜로디엔 뻔한
  코드 · **사람이 쓴 것보다 못하지 않다(76%→89%)** · 사람과 같은 코드 60%(아무렇게나면 14%) ·
  dim 은 안 고른다 · 끝은 으뜸화음 · 쉬는 자리는 이어 간다 · 결정적 ·
  나온 줄을 코드 패턴으로 바로 쓸 수 있다
- `harmony_ui_test` (새로 씀) — 멜로디 판엔 없다 · 코드 판엔 있다 · 누르면 채운다 ·
  **채운 코드가 멜로디와 96% 맞는다** · 되돌리기로 원래대로 · 세로·가로 안 넘침
- `harmony_meter` (수동 도구) — 짝을 씬에서 잡고 흐름 가중치를 쓸어 본다
- `screen_snap` 에 코드 편집 화면 추가(세로·가로). 가로는 위쪽이 한 줄로 접혀
  **일곱 번째 버튼이 잘리기 쉬운 자리**라 눈으로 확인했다 — 안 잘린다.
- `flutter test` **42개 파일 전부 통과** · `analyze lib` 깨끗

### 남은 것
- **폰 설치·청감** — 폰이 아직 안 붙었다. APK 는 나와 있다.
- **Phase 4 완료.** 6-1 변형 · 6-2 필 · 6-3 Build/Break/Drop · 6-4 Auto Harmony ·
  6-5 Auto Mix 를 다 지났다. §17 대로 다음은 **Phase 5(콘텐츠)** 인데,
  들어가기 전에 사용자 청감 확인을 받는 게 맞다 — Phase 3·4 에서 고친 게 많다.

## Phase 4 완료 보고 (§17) (2026-08-24)

```
Phase          4 — 음악 자동화
Completed      6-1 변형 · 6-2 필 · 6-3 Build/Break/Drop · 6-4 Auto Harmony · 6-5 Auto Mix
Changed files  variation.dart · fill.dart · arrange.dart · harmony.dart (전부 새 파일)
               feel.dart · sequencer.dart · engine.dart · audio_isolate.dart
               genre_mix.dart · drums.dart · export.dart · project.dart
               ui/feel_sheet.dart · ui/scene_view.dart · ui/editor_view.dart
Tests added    variation_check · arrange_check · harmony_check · harmony_ui · feel_ui
               (도구) vary_meter · arrange_meter · harmony_meter · drum_meter · swing_meter
Tests passed   43개 파일 전부
Performance    맥 6.0% · 폰 추정 48% · 최대 동시 음 18 / 드럼 5~7(한도 24)
               클리핑 0 · 스타일 편차 4.4dB
Known issues   폰 청감 확인이 밀려 있다(케이블). 재즈 비브라폰 -24.7dB(편곡 문제),
               일렉 피아노 -12.9dB(믹스 균형) 은 Phase 3 부터 남아 있다.
```

## Phase 5 (1) — 장르를 데이터로 (계획 7-1) + 드릴·R&B (2026-08-24)

### 먼저 센 것 — 장르 하나에 8개 표
장르를 더하려면 **6개 파일의 8개 표**를 고쳐야 했다:
```
presets.dart    kSongGenres(키·이름·템포·키트·조) · kGenrePresets(대표 패턴 4개)
genre_mix.dart  kGenreDuck · kGenreSwingGrid · kGenreMix
song.dart       kSongForms 또는 kObjectSongForms
create_sheet    kGenreFeel(한 줄 설명)
show_band       kStageLights(무대 조명)
```
하나만 빠뜨려도 **조용히 반쪽짜리 장르**가 된다 — 소리는 나는데 펌핑이 없거나,
스윙이 안 걸리거나, 무대가 파랗기만 하거나, 고르는 화면에 설명이 비어 있다.
**오류도 안 난다.** 그게 제일 나쁘다.

### 얇은 값은 한 표로, 두꺼운 둘은 시험으로
`lib/genres.dart` 에 `GenreDef` 하나로 모았다 — 키·이름·템포·키트·조·설명·비켜주기·
스윙격자·조명·대표패턴 넷. 기존 표 다섯은 **거기서 파생**시켰다(이름·타입 그대로라
부르는 쪽은 한 줄도 안 고쳤다).

구간 구성(`kSongForms`)과 믹스 편성(`kGenreMix`)은 덩치가 크고 성격이 달라 제자리에 뒀다.
대신 `genre_check_test` 가 **모든 장르가 그 둘에도 있는지** 지킨다.
표를 합치는 게 목적이 아니라 **빠뜨림을 못 하게 막는 것**이 목적이므로,
그 목적은 시험으로도 똑같이 이룬다.

거기에 「대표 패턴이 라이브러리에 실제로 있는가」 「곡이 실제로 30초 넘게 울리는가」
「내보낸 적 있는 키가 안 사라졌는가」까지 넣었다 — 마지막 것은 **옛 곡이 안 열리는 일**을
막는다(키는 저장 파일에 남는다).

### 구조를 실제로 시험했다 — 드릴·R&B
계획 7 의 최소 목록에서 빠진 둘이다. 넣어 보니 **엔진 코드는 한 줄도 안 고쳤다.**
데이터만 더했다: 패턴 22개 · 곡 구성 2개 · 믹스 편성 2개 · `GenreDef` 2줄.

```
드릴  142 BPM · 스네어가 **3박에만** 온다(이 장르의 표식) · 미끄러지는 808 ·
      마디 끝 하이햇 롤 · 어두운 벨. i–VI / VI–VII / i
R&B   92 BPM · **7화음**(min7·maj7·min9)이 이 장르의 소리 · 뒤로 눕는 킥 ·
      고스트 섞인 16분 하이햇 · 목소리 리드
```

### 시험 셋이 새 장르를 잡았다
1. `pattern_names_test` — 「Drill Intro → Drill 인트로」. 이름이 **반만 한글**이었다.
   낱말 표와 스타일 목록에 넣어야 짧은 꼴에서 스타일이 떨어져 나간다.
   R&B 는 패턴 이름에서만 「알앤비」로 쓴다 — 목록 한 줄만 알파벳이면 그게 더 걸린다.
2. `melody_check_test` — 「Drill Plk H(길이 1종)」. 코드톤을 같은 길이로 훑는 줄,
   **사용자가 「멜로디들이 왜 다 아르페지오야」라고 한 그것**을 내가 또 썼다.
   길이를 섞고 2도로 지나가게 고쳤다.
3. `mix_meter` — 드릴이 곡 중 제일 컸다(-8.1dB · 편차 3.4 → 4.6dB).

### 페이더로는 안 됐다 — 리미터가 도로 밀어 올린다
드릴 페이더를 15% 내렸는데 **0.2dB 밖에 안 움직였다**(-8.1 → -8.3).
마스터 리미터가 그만큼을 되돌린다. 이 편차는 페이더가 아니라 **밀도**가 만든 것이다
(드릴은 원래 빽빽하고 눌린 장르다). 값은 낮춰 뒀다 — 크기는 그대로여도
**리미터가 덜 물어서** 다이내믹이 덜 뭉갠다. 「고쳤다」가 아니라 「이래서 이렇다」로 적어 둔다.

### 검증
- `genre_check_test` (새로 씀) — 10항목 × **13개 장르**
- `flutter test` **43개 파일 전부 통과** · `analyze lib` 깨끗
- `mix_meter` 13개 클리핑 0 · 편차 4.4dB (계량기가 11개만 세던 것도 고쳤다)
- 고르는 화면은 이미 「장르 13종이 다 있다 — 전부 닿는다」를 세로·가로로 보고 있었다
  (같은 검사를 하나 더 붙였다가 중복이라 뺐다 — 있는 걸 먼저 봤어야 했다)

### 남은 것
- **폰 설치·청감** — 폰이 아직 안 붙었다.
- 계획 7-2 콘텐츠를 **팩 단위**로 묶기(결제는 만들지 말 것).

## Phase 5 (2) — 콘텐츠 팩 (계획 7-2) (2026-08-24)

계획 7-2: 「나중에 Pack 으로 확장할 수 있어야 합니다. 하지만 Phase 5에서는
**결제 기능을 먼저 만들지 마십시오.** 먼저 콘텐츠를 독립적인 패키지 단위로 관리할 수
있게 만드는 것이 우선입니다.」

그래서 `lib/packs.dart` 에는 값도, 잠금도, 무료/유료 구분도 없다. **묶음과 이름뿐**이다.

### 나눌 수 있는 상태인지부터 쟀다
팩을 만드는 뜻은 하나다 — **하나를 들어내도 나머지가 멀쩡할 것.** 그게 안 되면
「팩」이라는 이름만 붙인 목록이다. 그래서 먼저 셌다:
```
장르가 쓰는 패턴 168개 · 둘 이상이 같이 쓰는 것 0개
라이브러리 240개 중 어느 곡도 안 쓰는 것 72개  ← 공용(편집기에서 직접 고르는 것)
```
**겹침이 0이었다.** 콘텐츠가 이미 깨끗하게 갈라져 있었다 — 팩은 그 사실을 이름 붙인 것뿐이고,
시험이 그 상태를 지킨다.

### 묶는 기준은 「무엇을 만들고 싶은가」
음악 이론도 연도도 아니다. 처음 여는 사람이 자기 자리를 찾을 수 있는 말이어야 한다 —
**고르는 화면의 소제목이 곧 이것**이기 때문이다.
```
느긋하게(3) 처음이라면 여기서       로파이 · 팝 · 발라드
비트(3)    랩을 얹거나 고개를 끄덕이거나  힙합 · 트랩 · 드릴
춤(2)      네 박에 쿵쿵              하우스 · 프로그하우스
도시(2)    부드럽고 반짝이는          시티팝 · R&B
연주(2)    사람이 치는 소리           록 · 재즈
공기(1)    깔아 두는                 엠비언트
```

### 구조가 바로 쓰인다
장르가 13개가 되면서 고르는 화면이 **한 덩어리로는 어디쯤인지 감이 안 왔다.**
팩으로 묶어 소제목을 붙였다(세로 1열 · 가로 3열, 둘 다 눈으로 확인).
결제와는 아무 상관 없이 **지금 당장** 읽기가 나아진다 —
구조만 만들어 두고 안 쓰는 것보다 낫다.

### 시험이 잡은 것 — 내가 만든 것
격자 하나였던 목록이 팩마다 격자를 갖게 되자 `create_flow_test` 가 두 번 멈췄다:
1. **「어느 격자를 밀라는 것이냐」** — 미는 것은 바깥 목록이지 격자가 아니다
2. 하우스가 세 번째 묶음으로 내려가 **화면 밖인 데다 아직 만들어지지도 않았다**
   (목록이 게을러서). `ensureVisible` 로는 안 되고 `scrollUntilVisible` 이어야 한다

둘 다 **화면 구조를 바꾸면 따라오는 것**이라 시험 쪽을 고쳤다.

### 검증
- `pack_check_test` (새로 씀) — 8항목. 빠진 장르 0 · 두 팩에 든 장르 0 ·
  **팩끼리 패턴 겹침 0(168개)** · **팩을 하나씩 빼 봐도 남은 곡이 다 울린다** ·
  이름·id 검사
- `flutter test` **44개 파일 전부 통과** · `analyze lib` 깨끗

### 남은 것
- **폰 설치·청감** — 폰이 아직 안 붙었다.
- 계획 7 preamble 이 적은 묶음 중 아직 없는 것: **FX 프리셋 · Live Behavior**.
  FX 는 장르마다 인서트를 미리 꽂아 두는 것인데(로파이=포화+필터, 드릴=벨에 리버브),
  **사용자가 꽂아 둔 것을 덮어쓰면 안 되므로** 「장르가 꽂은 것」 표시가 필요하다
  (저장 형식이 한 칸 는다). 청감 확인 뒤에 손대는 게 맞다 — Phase 3·4 에서 소리를
  많이 바꿔 놓고 아직 한 번도 못 들었다.

## Phase 6 (1) — 쇼가 곡의 짜임을 보여 준다 (계획 8) (2026-08-24)

계획 8: 「음악 이벤트에 따라 시각 요소가 반응하도록 합니다.
Drop → Band 활성화·Stage energy 증가 / Break → Drummer 감소·조명 변화 / Build → 움직임 증가.
**현재 구현을 최대한 활용하고, 새 그래픽 엔진을 불필요하게 도입하지 마십시오.**」

### 소리는 안 건드렸다
Phase 3·4 에서 소리를 많이 바꿔 놓고 아직 한 번도 못 들었다. 여기서 또 소리를 바꾸면
무엇이 좋아지고 무엇이 나빠졌는지 갈라낼 수 없다. 계획 8 은 **화면** 얘기라 지금 하기 좋다.

### 새로 만든 게 거의 없다
6-3 에서 만든 `roleOf(구간 이름)` 이 그대로 쓰인다. 쇼 화면은 이미 지금 구간 이름을
알고 있었고(`sectionNames[si]`), 무대도 이미 그리고 있었다. 이은 것은 한 줄짜리 값
하나 — `ShowScore.energyAt(t)`(0~1) 뿐이다.
```
드롭 1.0 · 보통 0.55 · 인트로 0.30 · 브레이크 0.15
빌드업 0.35 → 0.95 (구간 안에서 차오른다)
아웃트로 0.45 → 0 (잦아든다)
```

**음량으로 안 재는 이유**: 리미터가 눌러 놔서 구간끼리 크기 차이가 작다
(Phase 4 에서 잰 값 — 드롭 페이더를 15% 내려도 0.2dB 밖에 안 움직였다).
화면은 곡의 **짜임**을 보여 줘야 하고, 그건 구간 이름이 이미 알고 있다.

### 눈으로 보고, 그다음 쟀다
빛기둥 셋을 밝기·폭·흔들림 세 가지로 달라지게 했다. 그림 두 장(브레이크·드롭)을
나란히 놓으니 차이는 보이는데 **얼마나** 인지는 몰라서 밝기를 쟀다:
```
          전체    위쪽    관객쪽
브레이크  21.48   13.79   17.80
드롭      26.72   21.30   17.80   ← 관객 쪽이 그대로다
```
**위쪽만 1.54배, 관객 쪽은 하나도 안 변했다.** 빛기둥만 만졌으니 당연한데,
그림만 봐서는 「밝아졌네」로 넘어갔을 자리다. 무대 앞 빛(glow)도 같이 따라가게 고쳤다:
```
브레이크  21.05   13.79   17.80
드롭      27.25   21.30   22.27   ← 이제 아래도 따라온다
```

### 드러머는 이미 줄어들고 있었다
계획의 「Break → Drummer 감소」는 **손댈 게 없었다.** 연주자 움직임은 실제 타격에서
오는데, 브레이크는 편곡이 잔가지를 걷어내므로(계획 6-3) 드러머가 저절로 덜 친다.
없는 일을 만들지 않았다.

### 검증
- `show_check_test` +3항목 — **13개 스타일 전부** 드롭>보통>브레이크 ·
  빌드업이 구간 안에서 차오름 · 아웃트로가 잦아듦 · 0~1 을 안 벗어남 ·
  곡 밖(음수·끝 너머)에서도 안 죽음
- `show_snap` 에 「무대에너지 브레이크·보통·드롭」 세 장 — **나란히 놓고 본다.**
  하나씩 보면 「좀 밝네」로 끝나고 차이가 있는지 없는지 알 수 없다.
- `flutter test` **44개 파일 전부 통과** · `analyze lib` 깨끗

### 폰 확인 (2026-08-24 23:00)
드디어 폰이 붙어서 **Phase 3~6 을 통째로 올렸다.** 60fps · 앱 오류 0 ·
`AudioTrack state:started` 48kHz 스테레오.
```
· 고르는 화면      팩 소제목(느긋하게/비트/춤)과 함께 13개가 제대로 나온다
· 드릴             142BPM · 5트랙(드럼·808·패드·벨·플럭) · 구간 6개 · 3분 23초
· 씬 상태줄        「한 판 4마디 · 6.8초 · 음 46개 · 타격 92개 · **4바퀴마다 변화**」
· 쇼 화면          브레이크 조명 5.82 → 훅(드롭) 14.65 — **2.5배**
```
브레이크에서는 빛기둥이 좁고 어둡고, 훅에서는 넓고 밝고 무대 앞까지 환하다.
맥에서 잰 1.5배보다 폰에서 더 크게 벌어졌다(곡이 실제로 구간을 오가기 때문이다).

**아직 못 한 것은 청감뿐이다** — 킥·스네어가 「둥/탁」으로 들리는지, 스윙이 스윙으로
들리는지, 필이 알맞은 자리에 오는지, 바퀴마다 달라지는 게 들리는지는 사람이 들어야 안다.

### 남은 것
- **청감** — 위 넷.
- 계획 8-1 Share(음악 + 아트워크 + 곡 정보 + 비주얼라이저)
- 계획 7 preamble 의 **FX 프리셋 · Live Behavior** — 청감 확인 뒤에.

## Phase 6 (2) — 내보낼 때 표지와 곡 정보를 같이 (계획 8-1) (2026-08-25)

계획 8-1: 「WAV Export 뿐만 아니라 **Music + Artwork + Track information + Visualizer**
구조로 확장할 수 있도록 설계하십시오. 최종적으로는 짧은 Music Video 형태로 공유할 수
있는 기반을 준비합니다.」

### 지금 무엇이 나가고 있었나
WAV 파일 하나. 받은 사람 화면에는 **회색 네모에 파일 이름**뿐이다 —
무슨 곡인지, 뭘로 만든 건지 아무것도 안 보인다.

영상까지 가려면 프레임을 찍어 인코딩해야 하고 그건 dependency 가 는다(§15).
**표지 한 장은 `dart:ui` 만으로 되고**, 영상으로 갈 때 그 프레임의 바탕이 그대로 이것이다.
그래서 표지·정보를 먼저 했다.

### 아무 그림이나 그리지 않는다
표지는 **그 곡의 것**이어야 한다. 세 가지를 곡에서 가져온다:
```
색    그 스타일의 무대 조명(kStageLights)
짜임  구간을 길이 비율대로 늘어놓은 띠 — 인트로는 낮고 짧게, 드롭은 높고 길게
글    곡 이름 · 스타일 · 템포 · 조 · 길이 · 구간 이름
```
띠의 높이는 **쇼 화면의 무대 에너지와 같은 규칙**을 쓴다(계획 8) — 두 곳이 다른 말을
하면 같은 곡이 아닌 것처럼 보인다. 색만으로 나누지 않고 높이도 나누는 이유는
흑백으로 뽑히는 자리(썸네일·인쇄)에서 전부 같아지기 때문이다.

### 시험 환경에서는 글씨가 안 보였다
`CustomPainter` 는 위젯 트리 밖에서 그려서 화면의 기본 글꼴을 안 물려받는다 —
한글이 전부 두부(□)로 나와 **글자가 넘치는지 줄이 깨지는지 볼 수가 없었다.**
`CoverPainter` 에 글꼴 이름을 받는 칸을 두고, 그림 도구가 AppleGothic 을 실어 넘긴다.
(앱에서는 비워 둔다 — 기기 기본 글꼴이 한글을 알아서 낸다.)

그러고 나서 보니 **긴 이름이 두 줄이 되면 다음 줄과 붙었다.** 줄마다 y 를 못 박아
둬서다. 그린 높이를 돌려받아 다음 줄이 그 아래에서 시작하게 고쳤다.

### 표지가 실패해도 곡은 나간다
`try/catch` 로 감쌌다. 그림 때문에 음악을 못 보내면 본말전도다.

### 검증
- `cover_check_test` (새로 씀) — 4항목. 진짜 PNG(머리글·크기) ·
  **13개 장르가 서로 다른 표지**(화소 3% 문턱) · **구간이 다르면 표지도 다름**(5.5%) ·
  이름 없음·모르는 장르·0초 구간에도 안 죽음
- `cover_snap` (도구) — 로파이·드릴·재즈·R&B + 아주 긴 이름. **눈으로 본다.**
- `flutter test` **45개 파일 전부 통과** · `analyze lib` 깨끗
- 폰: 새 빌드 설치·실행 확인(60fps · 오류 0)

### 공유는 사용자가 눌러 봐야 한다
내보내기를 끝까지 눌러 보지 않았다 — 공유 시트가 뜨면 그때부터는 **밖으로 나가는 길**이라
내가 임의로 진행할 자리가 아니다. 파이프라인(렌더 → 파일 → 시트)은 시험이 덮고 있고,
실제 공유 화면에서 표지가 어떻게 보이는지는 눌러 보면 바로 나온다.

### 남은 것
- **청감** — 킥·스네어 · 스윙 · 필 자리 · 바퀴마다 변화. 폰에 최신 빌드가 올라가 있다.
- 계획 8-1 의 나머지: **Visualizer → 짧은 영상**. 프레임 인코딩이 필요해
  dependency 판단이 먼저다(§15).
- 계획 7 preamble 의 **FX 프리셋 · Live Behavior**.

## P0 재점검 — 곡을 끝까지 돌려 본다 (§19 P0 · 계획 3-1·3-6) (2026-08-25)

§17: 「다음 Phase 로 넘어가기 전에 현재 상태를 기준으로 **다시 전체 프로젝트를 확인**하십시오.」
P1 은 다 지났고 P2 도 장르·팩까지 끝났다. 그래서 맨 위(P0)로 돌아왔다.

### 왜 지금인가 — 4초만 보고 있었다
골든 시험은 **4초**만 렌더한다. Phase 3~6 에서 얹은 것이 많다 —
변형(판이 4배 길어짐) · 구간 성격 · 3배 길어진 킥 · 장르 13개.
**그 전부를 얹고 곡을 끝까지 돌려 본 적이 없었다.**

짧게 보면 못 잡는 것들: 목소리가 새는 것 · 뒤로 갈수록 커지거나 작아지는 것 ·
DC 가 서서히 밀리는 것 · **특정 구간에서만 넘치는 것**(드롭은 3분짜리 곡의 2분 30초에 온다).

### 잰 값 — 13곡 전부, 꼬리까지 (35분 분량)
```
장르        길이   렌더    실시간%  피크  클립  음부족  최대음/드럼  끝난뒤
lofi        135초  3435ms    2.5% 0.965     0      0  10/5      0
rock         77초  2711ms    3.5% 0.970     0      0  11/10     0
trap        206초  8867ms    4.3% 0.970     0      0  24/11     0
pop         222초 10115ms    4.6% 0.971     0      0  27/7      0
drill       203초  6694ms    3.3% 0.973     0      0  12/9      0
rnb         240초  8097ms    3.4% 0.967     0      0  20/8      0
ambient     213초  9312ms    4.4% 0.961     0      0  28/4      0
```
- NaN·무한대 0 · 하드클립 0
- **끝나고 3초 뒤 울리는 음 0개** — 안 꺼지는 음이 없다(3분을 돌려야 쌓이는 종류다)
- 목소리 부족 0 (한도 96/24 · 최대 28/11 — 여유 71%)
- DC 끝 15% 구간 전부 |0.004| 안
- 맥 렌더 2.3~4.6% (폰 추정 18~37%)

### 하나 걸렸다 — R&B 만 5분 13초
다른 곡은 1분 17초~3분 42초인데 **R&B 혼자 5분 13초**였다.
원인은 내 실수다 — 트랩의 얼개(벌스 16마디)를 그대로 베꼈는데, 트랩은 140BPM 이라
16마디가 27초지만 R&B 는 92BPM 이라 **42초**다. 42초짜리 벌스는 어느 장르에서도 길다.
**마디 수는 템포를 보고 정해야 한다.**

벌스·코러스를 12마디로 줄이고 얼개에서 프리 하나를 뺐다(브레이크 뒤 마지막 코러스는
남겼다 — 그게 이 얼개의 매듭이다) → **4분 00초.** 끝까지 렌더해 보지 않았으면
못 봤을 자리다.

### 시험이 느려졌다 — 알고 그렇게 뒀다
`flutter test` 가 13초 → **1분 24초**가 됐다(`long_run_test` 가 35분 분량을 렌더한다).
줄일 수는 있지만 **줄이면 이 시험의 뜻이 없어진다** — 목소리가 새는지는 3분을 돌려야
보이고, 장르마다 다르므로 몇 개만 봐서도 안 된다. 커밋 전에 한 번 도는 값으로는 괜찮다.

### 검증
- `long_run_test` (새로 씀) — 8항목 × 13곡 전곡
- `flutter test` **46개 파일 전부 통과** · `analyze lib` 깨끗
- `mix_meter` 13개 클리핑 0 · 편차 4.4dB

## Phase 5 (3) — 장르별 인서트(FX 프리셋) (계획 7) (2026-08-25)

계획 7: 「각 장르는 단순히 패턴만 다르게 하지 마십시오. 장르마다 Patterns · Instruments ·
**Sound Character** · Genre Mix · **FX Presets** · Scene Presets · Live Behavior 를 묶으십시오.」

### 믹스가 이미 하는 것은 안 한다
`kGenreMix` 가 볼륨·좌우·EQ·리버브 센드를 장르마다 잡아 준다. 여기서 또 리버브를 걸면
**두 번 걸린다.** 인서트는 믹스가 **못 하는 것**만 맡는다 — 포화(드라이브·앰프) ·
다이내믹(컴프) · 시간(딜레이).
```
로파이  베이스 앰프(둔탁) + 건반 옅은 드라이브(먼지)
힙합    드럼 컴프(붙잡기) + 베이스 앰프
록      멜로디 **기타 앰프**(크런치) + 베이스 앰프    ← 이 장르는 이게 전부다
트랩·드릴 드럼 컴프 + 벨 핑퐁 딜레이 / 808 베이스 앰프
하우스·프로그 드럼 컴프 + 코드/패드 딜레이
R&B    건반 옅은 드라이브(일렉피아노 온기) + 보컬 테이프 딜레이
엠비언트 벨·하프 긴 딜레이
재즈    **없음**
```
재즈를 비워 둔 건 빠뜨린 게 아니다. **마이크 앞에서 친 소리**가 그 장르다 —
앰프도 딜레이도 걸면 그게 아니게 된다. 「장르마다 뭔가 하나씩」은 이유가 아니다.

### 제일 위험한 것부터 막았다
이 기능의 위험은 하나다: **공들여 꽂아 둔 인서트가 스타일을 바꾸는 순간 조용히
날아가는 것.** 오류도 안 나고 되돌릴 수도 없다.

`Track.fxAuto` 를 뒀다 — 지금 체인이 「장르가 꽂아 준 것」인가. 사용자가 인서트를
하나라도 만지면(`addFx`·`removeFx`·`moveFx`·`setFxParam`·`setFxOn`) false 가 되고,
그다음부터 장르를 바꿔도 그 트랙은 안 건드린다. 옛 파일은 false 로 읽는다 —
그 체인은 사용자가 꽂은 것이기 때문이다.

### 자가 또 틀렸다 (다섯 번째)
인서트를 넣고 `mix_meter` 를 돌렸는데 **숫자가 하나도 안 움직였다.**
`ExportJob` 에 `inserts` 와 `duck` 을 안 넘기고 있었다 — 자가 **실제로 나가는 소리와
다른 신호**를 재고 있었던 것이다. 비켜 주기(계획 6-5)를 넣은 뒤로 줄곧 그랬다.

고치고 다시 재니 진짜 그림이 나왔다:
```
        고치기 전   자를 고친 뒤   컴프를 다시 잡은 뒤
트랩 피크  0.961      0.655         0.949
힙합 피크  0.965      0.741         0.962
스타일 편차 4.4dB     6.0dB         4.2dB
```
**내 컴프가 드럼을 짓뭉개고 있었다** — 문턱 -14~-16dB · 비율 4~6:1 인데 보정이 2dB뿐.
비율만큼 깎아 놓고 되돌려 주지 않은 것이다. 문턱을 -7~-9dB 로 올리고 비율을 2.5~3.5 로
낮춰 **피크 몇 dB 만 붙잡는** 값으로 바꿨다. 컴프는 붙잡는 것이지 짓뭉개는 것이 아니다.

> 자를 고치지 않았으면 「인서트를 넣었는데 아무 일도 안 일어나네」로 끝났을 것이다.
> **숫자가 안 움직이는 것도 증거다.**

### CPU
인서트가 +20~34% 를 쓴다. 맥 5.1~5.9%(폰 추정 41~47%) — 여태 잰 폰 여유 안이지만
전보다 눈에 띄게 올랐다. 자동 시험이 이걸 못 보고 있어서(엔진에 직접 예약하는 길이라
버스 인서트를 안 지난다) `long_run_test` 에 **제일 무거운 한 곡을 인서트까지 켜고**
재는 항목을 넣었다(13곡 다 하면 시험이 3분이 된다).

### 검증
- `genre_fx_test` (새로 씀) — 10항목. 표의 인서트·손잡이가 다 있고 범위 안 ·
  표대로 꽂힘 · **재즈는 비어 있음** · **내가 꽂은 것은 스타일을 바꿔도 그대로**(값까지) ·
  안 만진 트랙은 갈림 · 갈아탈수록 안 쌓임 · `fxAuto` 저장 왕복 · 옛 파일은 내 것으로
- `long_run_test` +2 — 인서트까지 켜도 실시간 12% 안(5.8%) · 인서트가 넘치게 안 만듦
- `mix_meter` — **자를 고쳤다**(인서트·비켜 주기를 넘긴다). 13개 클리핑 0 · 편차 4.2dB
- `kSlotLabel` 에 'keys' 추가 — R&B 트랙 이름이 「keys」로 날것이 찍히고 있었다
- `flutter test` **47개 파일 전부 통과** · `analyze lib` 깨끗

### 남은 것
- **청감** — 이제 들을 것이 하나 더 늘었다(록에서 기타 앰프, 로파이 먼지, 드럼 컴프).
- 계획 7 preamble 의 **Live Behavior** · 계획 8-1 의 **짧은 영상**(dependency 판단 먼저).

## Phase 5 (4) — 라이브도 장르를 따른다 (계획 7 「Live Behavior」) (2026-08-25)

### 어떤 장르든 피아노였다
라이브 패드는 여태 **피아노 · 한 음 · 보통 길이** 고정이었다.
트랩을 만들다 라이브를 열면 피아노가 나왔다. 그 장르로 만들고 있었는데 말이다.
```
로파이 일렉피아노·한음      하우스 스탭·화음·짧게    힙합 일렉피아노·한음
시티팝 일렉피아노·화음      발라드 피아노·화음·길게   록 기타·한음
트랩·드릴 벨·한음·짧게      프로그하우스 플럭·펼쳐서·짧게
팝 피아노·화음             재즈 피아노·한음          R&B 일렉피아노·화음
엠비언트 패드·화음·길게
```

### 음계는 안 건드린다
장르마다 음을 빼면(펜타토닉 같은 것) 「**아무 패드나 눌러도 맞는 음이 나온다**」는
이 화면의 약속이 흔들린다 — 첫 화면이 「틀린 음이 안 나온다」고 적어 둔 그것이다.
바꾸는 건 **음색 · 주법 · 길이**뿐이다.

### 여기도 사용자 것은 안 덮는다
음색은 `LiveChannel.voiceAuto` — 사용자가 한 번이라도 고르면 그다음부터 장르가
안 건드린다(인서트와 같은 규칙). 주법·길이는 이 화면 안에서만 사는 값이라
열 때마다 새로 잡는다(예전에도 열 때마다 초기화됐으니 나빠지는 것은 없다).

### 알림을 일부러 안 보낸다
`suggestVoice` 는 값만 바꾸고 `notifyListeners()` 를 **안 부른다.** 이건 화면이 열릴 때
(`initState`) 불리는데, 빌드 도중에 알림을 보내면 그 값을 듣고 있는 화면이
「빌드 중 재빌드」로 터진다. 지금 듣는 쪽은 자동 저장뿐이라 안 터지지만 **하나만 더
붙어도** 터진다. 안 보내도 되는 이유: 값은 이미 바뀌었고 이 화면은 다음 빌드에서 읽는다.
저장이 안 되는 것도 문제가 아니다 — 장르가 정해 주는 기본값이라 다음에 또 얹힌다.

### 검증
- `live_behavior_test` (새로 씀) — 8항목. 장르마다 쓸 수 있는 값(음색 7종) ·
  트랩→벨 · 록→기타 · 엠비언트→패드 · **내가 고른 음색은 그대로** ·
  **패드 14개가 다 음계 안**(조를 옮겨도)
- `flutter test` **48개 파일 전부 통과 · 29초**

## 시험이 흔들렸다 — 시계로 문턱을 잡은 탓

`long_run_test` 의 「맥 렌더가 실시간의 12% 안」이 **혼자 돌리면 4.6%, 다 같이 돌리면
45.6%** 로 나왔다. `flutter test` 가 시험들을 동시에 돌리기 때문이다.
그렇게 흔들리는 시험은 결국 아무도 안 본다 — **없는 것보다 나쁘다.**

상대값(제일 무거운 곡 ÷ 제일 가벼운 곡)으로 바꿔 봤다. **그것도 흔들렸다 — 182배.**
한 시험 안에서 순차로 재는데 그중 일부 구간만 굶으면 비율이 통째로 무너진다.

결론: **병렬 시험 안에서 시계로 성능을 재는 방법은 없다.** 어떤 형태든 흔들리고,
흔들리는 시험은 결국 아무도 안 본다 — 없는 것보다 나쁘다.

그래서 `long_run_test` 는 **맞고 틀림만** 본다(NaN·클립·새는 목소리·DC).
그건 몇 번을 돌려도 같은 답이 나온다. 성능은 **조용한 기계에서 일부러 돌리는 도구**로
옮겼다 — `render_bench` 에 「곡 전체 · 인서트 포함」을 붙였다(그 파일은 `_test.dart` 가
아니라 자동으로 안 돌아간다).
```
장르        길이   인서트없이 → 켜고        실시간%(맥)
trap        206초  9425ms → 11807ms (+25%)   5.7%
proghouse   210초  8421ms → 11586ms (+38%)   5.5%
rock         77초  2963ms →  3863ms (+30%)   5.0%
ambient     213초 11619ms → 14027ms (+21%)   6.6%
lofi        135초  3541ms →  4699ms (+33%)   3.5%
```

> 두 번 고쳐 보고 나서야 **자리가 틀렸다**는 걸 알았다. 문턱을 어떻게 잡느냐가 아니라
> **여기서 잴 일이 아니었다.**

### 덧 — 「17분」과 「908초」는 내가 만든 것이다
성능 문턱을 걷어낸 뒤에도 시험이 17분, 트랩·엠비언트가 각각 **908초**로 찍혔다.
잠깐 코드 회귀를 의심했는데, 그 둘만 따로 돌려 보니 **8.7초 · 9.4초**였다.

원인은 나였다 — 전체 시험 두 벌과 벤치를 **동시에** 돌려 놓고 그 위에서 또 쟀다.
`TaskStop` 으로 껍데기 쉘을 죽여도 `flutter_tester` 는 살아남는다(끝나고 두 개가
0% 로 떠 있었다).

같은 값이 두 번(908초·908초) 나온 게 단서였다. **길이가 다른 두 곡이 똑같은 시간에
끝난다면 그건 그 곡들의 성질이 아니다.** 느린 게 아니라 둘 다 같은 것을 기다린 것이다.
무거운 것은 하나씩 돌릴 것.

## 작곡 손보기 — 코드 진행과 멜로디 (2026-08-26)

사용자 지적: 「작곡 좀 잘해라. **멜로디랑 코드 진행이 너무너무 구려**」.
맞는 말이다 — 엔진·믹스·이펙트를 아무리 다듬어도 **음이 구리면 구린 곡**이다.
Phase 3~6 이 전부 그 위에 얹는 일이었다.

### 재 보니 원인이 둘이었다
`music_meter` 를 새로 써서 13개 장르의 코드 진행과 48개 멜로디를 전부 뽑았다.

**① 13개 중 7개가 벌스에서 코드 두 개만 왔다 갔다 했다.**
```
house · hiphop · drill · rnb · ambient   i–VI
rock · proghouse                          i–VII
```
게다가 인트로 = 벌스 = 아웃트로가 같은 진행이었다.

**② 13개 중 10개가 민짜 3화음만 썼다.**
7음·9음을 쓰는 건 재즈·R&B·엠비언트뿐이었다 — **그 셋이 그나마 곡 같은 소리가 나는
이유가 그것**이었다. i–VI 삼화음 반복은 「MIDI 데모」 소리가 난다.

### 고친 것 — 진행
모든 장르에 **네 코드 벌스**를 주고, **코러스가 i 로 착지**하게 했다.
```
house    벌스 i–VI–III–VII   코러스 iv–VI–VII–i
hiphop   벌스 i–iv–VII–VI    코러스 iv–VII–VI–i
citypop  벌스 VI–VII–III–iv  코러스 iv–VII–III–i
drill    벌스 i–i–VI–VII     훅   iv–VI–VII–i   ← 최면은 살리되 두 마디 뒤에 움직인다
rnb      벌스 i–VI–iv–VII    코러스 iv–VII–VI–i
rock     벌스 i–VII–VI–VII                      ← 파워코드는 민짜가 맞다
ambient  i–VI–III–VII
```

### 고친 것 — 화성 색
장르마다 색을 정했다. **믹스가 못 하는 일**이라 여기서만 할 수 있다.
```
로파이   min7 · dom7 · maj7 · m7b5 · min9   (이 장르는 7화음이 전부다)
힙합     min9 · min7 · dom7 · maj7          (먼지 낀 로즈)
시티팝   maj7 · dom7 · min9                 (반짝임의 정체)
발라드   min7 · dom7 · maj7 · min9
프로그   min7 · maj7 · add9 · sus2          (sus 가 이 장르의 패드다)
팝       add9 · maj7 · min7, V 는 민짜로 힘
트랩·드릴 i·v 에만 min7, VII 는 민짜로 어둡게
록       민짜 유지 — 파워코드가 곧 그 장르다
```

### 시험이 내 실수를 바로 잡았다
`harmony_clash_test` 를 새로 써서 「한 씬의 코드 트랙끼리·베이스와 코드가 맞는가」를
봤더니 **내가 방금 만든 것 셋**이 걸렸다:
```
house/코러스   베이스가 4마디 전부 어긋남  ← 코드를 바꿔 놓고 베이스를 안 바꿨다
ambient/안개   패드↔스트링 1마디 충돌
rnb/프리       베이스 2마디 어긋남
proghouse/그루브 패드는 i–VI, 스탭은 i–VI–III–VII  ← 3마디째에 i 와 III 가 동시에
```
**코드를 바꾸면 베이스도 같이 바꿔야 한다.** 소리로는 「뭔가 탁하다」로만 들리고
어디가 문제인지 알 수 없는 종류다. 전용 베이스가 없던 R&B 프리에는 하나 만들어 줬다.

### 고친 것 — 멜로디
48개를 재 보니 「구리다」의 실체가 나왔다: **모티프 되풀이 0%.**
한 번 나온 말이 다시 안 나오면 아무것도 기억에 안 남는다.
```
                    되풀이   여백
Lofi Hook     0% → 45%    28% → 38%
Lofi Sub      0% → 83%
RnB Line V    0% → 70%
Amb Bell B    0% → 71%
Drill Bell I  0% → 67%
```
다시 쓴 방식은 하나다 — **한 마디 모티프를 세우고, 그것을 다른 높이로 되풀이하고,
3마디째에 꼭대기를 만들고, 4마디는 풀어 주고 쉰다.**

여백도 봤다. **다섯 패턴이 판을 한 칸도 안 남기고 채우고 있었다**
(Rock Riff V · City Hook · Pop Voc V · Pop Voc B · Prog Lead D/B · Wide Leaps).
노래하는 줄인데 숨을 안 쉰다. 프레이즈 끝마다 한 칸씩 비웠다.

### 내가 또 밟은 함정
드릴 인트로를 다시 쓰면서 **코드톤만 짚는 아르페지오(100%)로 만들었다** —
사용자가 예전에 지적한 바로 그것이다. 시험이 잡았다. VII 을 지나가게 고쳤다.

### 검증
- `music_meter`(수동, 새로 씀) — 코드 진행 · 멜로디 생김새(음수·음역·도약·되풀이·여백·길이 종류)
- `harmony_clash_test`(새로 씀) — 코드 트랙끼리 12쌍 · 베이스와 코드 41개 씬
- `melody_check_test` +2 — **되풀이되는 모티프**(20% 넘음) · **숨 쉴 자리**(여백 3% 넘음)
- `flutter test` **49개 파일 전부 통과 · 31초**

## 작곡 손보기 2 — 멜로디가 코드와 안 맞았다 · 코드가 뛰어다녔다 (2026-08-27)

앞 세션에서 **코드 진행**과 **멜로디 생김새**를 손봤다. 그런데 그 둘을 **같이** 본 적이
한 번도 없었다. `harmony_clash_test` 는 코드↔코드, 코드↔베이스만 봤다.
정작 제일 크게 들리는 줄 — 멜로디 — 은 아무도 검사하지 않았다.

### 재고 나서 알게 된 것 — 같은 실수가 열여섯 번
`test/clash_meter.dart`(새 도구)로 13개 장르 59개 줄, 음 1294개를 재 봤다.
도수 공간이라 조와 무관하게 **반음 단위로 정확히** 나온다.

실수는 딱 세 가지였고, 전부 **같은 원인**이었다 — 멜로디를 「조」 기준으로만 쓰고
그 순간 밑에서 울리는 「코드」를 안 봤다:
```
조의 5음(G) 을 iv(F–Ab–C–Eb) 위에서 길게 끌기   → Ab 와 반음      8곳
조의 b3음(Eb) 을 VII(Bb–D–F–Ab) 위에서 길게 끌기 → D 와 반음      6곳
조의 b6음(Ab) 을 i·v 위에서 길게 끌기            → G 와 반음      2곳
```
소리로는 「탁하다·뭔가 틀렸다」로만 들린다. 어느 음이 문제인지는 안 들린다.

```
                긴음충돌   끝음해결
고치기 전         2.9%     46/59
고친 뒤           0.0%     54/59
```
「긴음충돌」 = 점8분(3칸) 넘게 끄는 음이 코드톤과 반음. **16분·8분으로 지나가는 반음은
안 센다** — 그것까지 막으면 멜로디가 밋밋해진다.

### 구조가 만든 실수 — 2마디 리프가 4마디 진행 위에 있었다
힙합 `Hip Riff V` 는 2마디인데 코드는 4마디였다. 그래서 **같은 음이 한 바퀴에는 맞고
다음 바퀴에는 틀렸다.** 어느 한 음을 고치면 다른 바퀴가 깨진다.
2마디를 4마디로 늘려, 3·4마디는 같은 리듬을 VII·VI 에 맞춰 옮겨 놓았다(시퀀스).
덤으로 「2마디가 그대로 두 번」 이던 지루함도 없어졌다.

### 시티팝 아웃트로 — 벌스 훅에 코러스 코드가 깔려 있었다
`song.dart` 의 배선 실수다. 코드는 `City Maj C`(코러스), 멜로디는 `City Hook V`(벌스).
인트로는 짝이 맞아 6%인데 아웃트로만 13%였다. 코러스 훅으로 맞췄다 —
곡이 제 훅으로 끝난다.

### 일부러 안 고친 것
- **록** — 파워코드가 움직여도 리프는 펜타토닉 자리를 지킨다. 그게 록 리프다.
  코드톤 38%, 끝음 안 해결 — 숫자는 나쁘지만 **그게 맞다.**
- **엠비언트 하프** — 코드 위에 흩뿌리는 배음이라 해결하지 않는 게 맞다.
- 두 가지는 `melody_clash_test` 의 `_endFree` 에 이름을 적어 뒀다. 예외는 **적어 두는 것**이지
  잣대를 무르게 하는 게 아니다.

### 코드가 마디마다 완전4도씩 뛰어다녔다
`chordFreqsOf` 는 어떤 코드든 **근음 자리**로만 쌓았다. i–iv–VI–VII 를 치면
밑음이 60→65→68→70 으로 기어오르다가 다음 바퀴에 60 으로 뚝 떨어진다.
```
코드가 바뀔 때마다 밑음이 움직인 거리 (평균, 87개 줄)
  고치기 전   5.3반음   ← 완전4도. 사람은 이렇게 안 친다
  고친 뒤     2.5반음
```
사람은 건반에서 손을 한자리에 두고 **자리바꿈(inversion)** 으로 잇는다.
`theory.dart` 에 `voiceLead()` 를 넣고 `buildChordPattern` 이 쓰게 했다.

- **코드톤 집합은 안 건드린다** — 옥타브만 옮긴다. 화성은 그대로고 움직임만 준다.
- 근음이 맨 밑에 없어도 된다 — **베이스 트랙이 근음을 따로 짚는다.**
- 밑음은 55~72(G3~C5) 안에 둔다. 밑이면 베이스와 엉키고 위면 멜로디 자리다.
- 아래쪽에서 음이 뭉치면(반음 붙은 자리) 벌점을 준다 — 자리바꿈이 만드는 탁함.
- 클립은 되풀이되므로 **끝 코드 → 첫 코드**도 이어야 한다. 그래서 두 바퀴 돈다.
  난수가 없어 두 바퀴는 항상 같은 답을 낸다(결정성 유지).
- 덤: 코드가 60~70 에서 56~68 로 내려앉아 **멜로디(60~84)와 겹치는 자리가 줄었다.**

### 시험이 또 내 실수를 잡았다
발라드 코러스에서 Eb 를 F 로 옮겼더니 `melody_check_test` 가 **모티프 되풀이 23%→15%**
로 떨어진 것을 잡았다. 그 Eb 가 「2도씩 오르는」 되풀이의 한 마디였다.
도수는 되돌리고 **길이만 8분으로** 줄였다 — 스치는 음은 부딪치지 않는다.
덤으로 꼭대기 G 앞에 숨 쉴 자리가 생겨 봉우리가 더 세게 온다.

> 화성만 보고 고치면 선율이 깨지고, 선율만 보고 고치면 화성이 깨진다.
> **둘 다 재고 있어야 어느 쪽이 깨졌는지 안다.**

### 자리바꿈이 딸려 온 것 — 변형 「얇게」가 장단을 지울 뻔했다
변형 ①「얇게」(`_openChord`)는 코드의 맨 윗음을 뺀다. 근음 자리만 쓰던 때는 맨 윗음이
언제나 5음·7음이라 안전했다. 자리바꿈이 들어오니 **둘째 자리바꿈(5음–근음–3음)** 에서
맨 윗음이 3음이 된다 — 빼면 5음+근음, **텅 빈 4도**만 남고 장단이 사라진다.
남은 두 음이 4·5도면 가운데를 대신 빼게 했다. 맨 밑음은 그대로 남으므로
`variation_check_test` 의 「변형은 화성을 안 잃는다」 약속도 지켜진다.

> 한 곳을 고치면 그 값을 **가정하고 있던 다른 곳**이 조용히 틀어진다.
> 여기서는 「맨 윗음은 3음이 아니다」 라는, 아무도 적어 두지 않은 가정이었다.

### 새로 만든 것
- `test/melody_clash_test.dart` — 2항목. **길게 끄는 음이 코드와 반음으로 안 부딪친다** ·
  **프레이즈 끝음이 코드톤으로 내려앉는다**(일부러 안 푸는 3개 제외)
- `test/clash_meter.dart`(도구) — 장르·씬별 반음충돌·긴음충돌·코드톤·끝음해결 +
  문턱 넘은 줄은 **어느 마디 몇 칸인지까지** 찍는다
- `test/voicing_meter.dart`(도구) — 코드가 바뀔 때 밑음·전체가 몇 반음 움직이는지

### 검증
```
flutter test            50개 파일 전부 통과 · 32초
flutter analyze lib     깨끗
clash_meter             긴음충돌 0.0% · 끝음해결 54/59 (음 1294개 · 59개 줄)
voicing_meter           밑음이동 5.3 → 2.5반음 (87개 줄)
mix_meter               13곡 클리핑 0 · 스타일 편차 4.2dB
long_run(13곡)          NaN·클립·새는 음·DC 전부 0 · 최대 동시 음 30(한도 96)
```
멜로디는 60~84, 코드는 이제 56~68 에 앉는다 — **겹치는 자리가 줄어 탁함도 준다.**

### 남은 것
- **청감** — 새 진행·새 멜로디·**자리바꿈**. 폰에 최신 빌드가 올라가 있다.
  자리바꿈은 「코드가 덜 뛴다」로 들려야 정상이다. 만약 「밋밋해졌다」면
  `kVoiceLow`/`kVoiceHigh`(theory.dart) 를 넓히거나 벌점을 줄이면 된다.
- 계획 8-1 의 나머지: **Visualizer → 짧은 영상**. 프레임 인코딩이 필요해
  dependency 판단이 먼저다(§15).
- 재즈 비브라폰 −24.7dB(편곡) · 일렉 피아노 −12.9dB(믹스 균형) — Phase 3 부터 밀림.

## 작곡 손보기 3 — 두 악기가 같은 음을 치고 있었다 · 베이스가 코드를 안 따라갔다 (2026-08-29)

### Phase 3 부터 못 풀던 것의 정답 — 「믹스 문제」가 아니었다
`재즈 비브라폰 −24.7dB` 은 세 세션째 「믹스 균형 문제」로 미뤄져 있었다.
아니었다. 새로 만든 `stack_meter` 로 재 보니:

```
장르        트랙쌍                       같은 음 비율
재즈        Jazz Keys ↔ Jazz Vib          100%   ← 완전히 같은 음, 같은 옥타브
트랩        Trap Pad P ↔ Trap Brass P     100%
팝          Pop Keys C ↔ Pop Str C        100%   ← 두 트랙이 **같은 패턴 이름**
R&B        RnB Keys ↔ RnB Pad            100%   ← 두 트랙이 **같은 패턴 이름**
엠비언트     Amb Pad ↔ Amb Str              94%
프로그하우스   Prog Pad ↔ Prog Stab           57%
```

**비브라폰이 피아노와 똑같은 음을 치고 있었다.** 두 악기가 같은 진행을 같은 옥타브에서
치면 한쪽은 페이더를 올려도 안 들린다 — 마스킹이다. **편곡으로만 고쳐진다.**

세 곳은 아예 **두 트랙에 같은 패턴 이름**이 꽂혀 있었다(팝 코러스·브리지의 스트링,
R&B 프리·코러스의 패드, 엠비언트 해질녘의 스트링2). 배선 실수다.

### 고친 것 — 코드 줄에 「층」을 준다
`voiceLead` 에 `oct` 를 붙이고, 코드 패턴 한 줄의 **6번째 값**으로 층을 적게 했다.
```
재즈    피아노 55~67  ·  비브라폰 67~79   (겹침 0반음)
트랩    패드   56~74  ·  브라스   70~80
R&B    건반   56~74  ·  패드     68~86
팝      피아노 59~76  ·  기타 71~81 · 스트링 71~88
```
`같은 음` 100% → **0%.** 위층을 맡는 줄에는 리듬도 새로 줬다 — 재즈 비브라폰은
피아노가 쉬는 자리(6·14칸)에서 답하고, R&B·팝의 패드/스트링은 마디를 통째로 끄는
후광 노릇만 한다. 리듬까지 같으면 층만 올려 봐야 여전히 한 덩어리로 들린다.

### 시험이 못 보던 자리 — **펴 놓고 안 봤다**
`harmony_clash_test` 는 패턴의 **원본 노트**만 견줬다. 그래서
**2마디 베이스가 4마디 진행 밑에 깔린 자리**를 통째로 못 봤다 — 3·4마디는
아무도 검사한 적이 없다. 펴 놓고 다시 재니 **열세 곳**이 나왔다:

```
하우스/벌스   코드 i VI III VII  ↔ 베이스 i VI i VI      (2마디 베이스)
힙합/벌스     코드 i iv VII VI   ↔ 베이스 i i VI VI      ← 코드만 바꾸고 베이스를 안 바꿨다
힙합/코러스   코드 iv VII VI i   ↔ 베이스 iv VII i i
시티팝/벌스   코드 VI VII III iv ↔ 베이스 VI VII III III
록/벌스      코드 i VII VI VII  ↔ 베이스 i VII i VII    (2마디 베이스)
드릴/벌스     코드 i i VI VII    ↔ 베이스 i iv i iv      (2마디 베이스)
드릴/훅      코드 iv VI VII i   ↔ 베이스 i VI VII iv     ← 1마디와 4마디가 뒤바뀌어 있었다
```
**베이스가 코드와 다른 근음을 짚는 것은 제일 크게 틀리는 소리다.** 전부 고쳤다.
2마디 베이스는 4마디로 늘렸다(하우스·록·드릴).

> 앞 세션에 「코드를 바꾸면 베이스도 같이 바꿔야 한다」를 적어 놓고도 힙합 벌스를
> 놓쳤다. 적어 두는 것만으로는 안 잡힌다 — **재는 것이 잡는다.**

엠비언트 서브만 예외로 뒀다. 두 마디를 통째로 끄는 **페달 톤**이고, 코드가 그 위를
지나가는 것이 그 장르의 소리다.

### 멜로디 검사도 넓혔다
위층 줄이 생겨 멜로디와 같은 자리에 오게 됐으므로, `melody_clash_test` 가
**코드 트랙 전부**를 본다. 다만 **울리는 화성 전체를 하나로** 본다 —
패드가 min9 를 치는데 스트링이 min7 이면 9음은 패드의 코드톤이지 실수가 아니다.
(처음엔 줄마다 따로 봤더니 그런 것 다섯 개가 걸렸다. 잣대가 틀린 것이었다.)

### 재 보고 안 고친 것 — 드럼 그루브
`groove_meter`(새 도구)로 49개 드럼 패턴의 마디 변화·백비트·하이햇 격자·세기를 쟀다.
- 팝·발라드 백비트 0~17% → **틀린 게 아니라 하프타임**이다(스네어가 3박). 요즘 팝이 그렇다.
- 킥·스네어 세기가 전부 3(최대) → 데이터는 밋밋하지만 `Human` 흔들림과 변형
  (`_ghostPush`/`_accent`)이 출력에서 갈라 준다. `drum_check_test` 가 이미 그것을 지킨다.
- 대부분 「1마디 되풀이」 → 변형·필이 그 위에서 판을 바꾼다.

숫자가 나쁘다고 다 결함은 아니다. **장르가 그런 것**과 **실수**를 가르는 것이 이 일이다.

### 재는 자를 바꿨다 — **RMS 는 「들리는가」를 못 잰다**
비브라폰을 한 옥타브 올렸더니 `track_meter` 숫자가 **내려갔다**(−16.5 → −20.0dB).
소리는 더 잘 들리는데 숫자는 나빠진 것이다. **RMS 는 저역이 좌우한다** —
높은 악기는 RMS 가 작아도 아무도 안 쓰는 대역에 있어서 잘 들린다.

숫자를 믿고 게인을 올렸으면 곡을 망칠 뻔했다. 그래서 재는 것을 바꿨다 —
**마스킹 여유**:
```
1) 그 트랙만 렌더  → 5개 대역 중 어디가 이 악기의 자리인가
2) 그 트랙만 뺀 렌더 → 그 대역을 나머지가 얼마나 채우고 있는가
3) 둘의 차          = 제 자리에서 얼마나 튀어나오는가
```
크게 음수면 **나머지에 덮여** 페이더를 올려도 안 들린다. 그게 진짜 「안 들린다」다.

> 숫자가 나빠졌다고 되돌리기 전에 **그 숫자가 무엇을 재는지**부터 본다.

### 재 보고 안 고친 것 둘
- **멜로디 트랙끼리** — 두 줄이 겹치는 순간의 같은 음 비율이 0~29%. 정상이다
  (트랩 벨↔플럭 27% · 프로그 아르페지오↔리드 10~29% · 팝 보컬↔플럭 18%).
  코드 줄과 달리 멜로디는 처음부터 서로 다른 선을 그리고 있었다.
- **곡 흐름** — `arc_meter`(새 도구)로 구간별 촘촘함을 쟀다. 13개 장르 전부
  제일 조용한 구간 대비 제일 큰 구간이 **2.3~5.8배**. 인트로→벌스→코러스→아웃트로가
  숫자로도 올라갔다 내려온다. 코러스·훅·드롭은 `roleOf` 가 이미 `drop`(에너지 1.0)
  으로 잡고 있어 음 수가 같아도 세기가 올라간다.
- **악기 33종 크기** — `voice_meter`(새 도구). 베이스류를 빼면 폭 11.5dB
  (제일 작은 아날로그 패드 −30.9 · 제일 큰 플루트 −19.4). 음색을 바꾸면 크기가
  튄다는 뜻이라 신경 쓰이지만, **정규화는 안 했다** — 장르별 `PartMixSpec` 게인이
  그 위에서 이미 맞춰져 있어서, 음색 쪽을 건드리면 13개 장르의 균형을 전부 다시
  잡아야 한다. 청감 없이 할 일이 아니다. 숫자만 남겨 둔다.

### 예외를 하나 지웠다
엠비언트 해질녘의 스트링 둘은 층을 올려도 여전히 81% 같은 음이었다(둘 다 마디를
통째로 끄니 층만으로는 안 갈라진다). 두 번째 줄에 **sus2·add9·sus4** 를 줬다 —
같은 도수를 다른 색으로 잡으니 **81% → 27%.**
```
Amb Str A   C5 Eb5 G5 Bb5  |  Ab4 C5 Eb5 G5  |  Eb5 G5 Bb5 D6  |  C5 F5 Bb5
Amb Str2 A  G5 C6 D6       |  Ab5 C6 Eb6 Bb6 |  F5 Bb5 Eb6     |  F5 Bb5 Eb6
```
**예외로 적어 두기 전에 고칠 수 있는지부터 본다.** 남은 예외는 팝 코러스의
기타↔스트링 하나뿐이다 — 하나는 8분으로 긁고 하나는 마디를 끄니 서로를 안 지운다.

### 편집기에도 층을 알려 줬다
코드 패턴에 6번째 칸이 생겼으니, 편집기에서 **새로 찍는 음**도 그 줄의 층을 따라가야
한다. 안 그러면 방금 찍은 음만 한 옥타브 아래로 떨어진다. `EditOps.add` 가
`chordOct(list)` 로 줄의 층을 읽어 붙인다. (길이·세기 편집은 원래 줄을 복사하므로
이미 층이 보존됐다. 저장 포맷도 노트 배열을 그대로 담아 왕복에 살아남는다.)

### 새로 만든 것
- `test/stack_check_test.dart` — 코드 악기 둘이 같은 음을 치지 않는다(60% 문턱).
  일부러 포개는 두 쌍(팝 기타↔스트링 · 엠비언트 스트링 둘)은 이름을 적어 뒀다.
- `test/stack_meter.dart` · `test/bass_meter.dart` · `test/groove_meter.dart`
  · `test/arc_meter.dart` · `test/voice_meter.dart` (도구)
- `test/track_meter.dart` 전면 재작성 — RMS 대신 **마스킹 여유**
- `harmony_clash_test` 전면 재작성 — **씬 길이만큼 펴 놓고** 마디마다 견준다

### 「소리로 다시 확인하기」 — `chroma_check_test` (새 시험)
여태 화성 시험은 **전부 패턴 데이터**만 봤다. 데이터가 맞아도 그 뒤가 틀리면
(자리바꿈이 엉뚱한 음을 고르거나, 코드 타입 표가 어긋나거나, 조 계산이 밀리거나)
**아무도 모른다.** 그래서 고리를 닫았다:

```
씬의 코드 트랙만 렌더 → 마디마다 12음 에너지(크로마) → 제일 센 음들이
그 마디에 적어 놓은 코드톤인가
```
고에젤(Goertzel)로 12음 × 4옥타브를 직접 재므로 FFT 라이브러리가 필요 없다(§15).

**결과: 52마디 전부 100%** — 적어 놓은 코드톤이 실제 소리에서도 제일 세다.
자리바꿈이 화성을 안 흔든다는 것이 소리로 확인됐다.

시험이 진짜인지도 확인했다 — 기대 코드를 **일부러 한 칸 밀어** 돌렸더니
바로 실패했다(적은것 G,Bb,D,F · 들린것 Eb,F,Ab,C). 통과만 하는 시험은 시험이 아니다.

### 마스킹 여유 — 그리고 또 한 번 자를 잘못 댔다
새 `track_meter` 로 쟀더니 **재즈 기타가 −81dB, 즉 무음**으로 나왔다.
기타를 의심하기 전에 어디를 봤는지부터 봤다 — 기타는 곡의 **33~49%** 에서만 치는데
내가 고른 토막이 25%·50%·75% 였다. **세 토막이 전부 비껴갔다.**
지금은 **그 악기가 제일 크게 우는 5초 토막 셋**을 트랙 스스로 고르고, 곡 전체도
같은 토막에서 잰다. 다시 재니 기타는 −7.8dB — 멀쩡했다.

```
재즈   드럼 −1.3 · 베이스 +0.3 · 피아노 −5.8 · 비브라폰 −13.3 · 색소폰 −0.0 · 기타 −7.8
R&B   드럼 −6.3 · 베이스 +0.4 · 건반 −2.0 · 패드 −9.8 · 보컬 −0.6
팝     드럼 −1.7 · 베이스 +0.9 · 피아노 −11.2 · 기타 −17.5 · 보컬 −0.4 · 플럭 −13.6 · 스트링 −10.2
```
**게인은 안 건드렸다.** 「100% 마스킹」이라는 진짜 결함은 편곡으로 없앴고, 남은 값들은
**받쳐 주는 악기가 뒤에 있는 정상 범위**다(컴핑 비브라폰 −13, 텍스처 기타 −17).
여기서부터는 귀가 결정할 일이다 — 숫자만 남겨 둔다.

## 변형이 화성을 깨고 있었다 (2026-08-29 밤)

`melody_clash_test` 는 **패턴 데이터의 도수**만 봤다. 그래서 변형(`varyMelodic`/
`varyChord`)이 지나간 뒤를 못 봤다 — 곡을 트는 동안 **실제로 울리는 것**은 그쪽인데.
실제로 예약되는 것과 같은 것(주파수)을, **네 바퀴 전부**에 대해 보게 고쳤더니
다섯 곳이 나왔다. 전부 원본에는 없고 **변형이 만든 것**이었다.

### 잣대부터 틀렸다 — 음이름이 아니라 **간격**
처음엔 12음 자리(피치 클래스)로 봤다. 그러면 **G4↔Ab3 도 「반음」으로 잡힌다** —
그건 장7도라 하나도 안 부딪친다. 같은 자리에서 스치며 우는 **단2도(1반음)** 만이
문제다. 단9도(13반음)도 뺐다 — 한 옥타브 벌어지면 거칠지 않고 **색**이다.

> 잣대를 잘못 대면 멀쩡한 것을 고치게 된다. 이번 회차만 두 번째다
> (앞은 RMS 로 「들리는가」를 재려던 것).

### ① `_openChord` 가 멜로디의 발판을 빼고 있었다
```
시티팝 인트로  코드 Ab4 C5 Eb5 G5(VI maj7) · 멜로디 G4
변형1(_openChord) → 맨 윗음 G5 를 뺌 → 코드 Ab4 C5 Eb5
                 → 멜로디 G4 가 Ab4 와 **단2도**로 남는다
```
멜로디가 겹쳐 짚고 있던 바로 그 음을 빼 버린 것이다. 네 음 이상인 코드는
**속 성부(밑에서 셋째, 근음 자리에서는 대개 5음)** 를 빼게 고쳤다 —
바깥 두 음을 남기는 것이 건반에서 화음을 얇게 하는 법이다.
(3화음은 그대로 — 빈 4·5도만 남는 경우를 피하는 기존 규칙이 이미 있다.)

### ② `_legato` 가 스쳐 가는 음을 눌러앉혔다
```
록 벌스   1마디 12칸 Ab4 (len 2 → 4) ↔ 코드 C4 Eb4 G4 의 G4
발라드 코러스 3마디 14칸 Eb5 (len 2 → 4) ↔ 코드 D5
```
둘 다 원래는 **16분·8분으로 지나가던 음**이다. 지나갈 때는 아무 문제가 없다.
늘여서 눌러앉히니 단2도가 됐다. **레가토는 「끄는 음들을 이어 붙이는 것」이지
스치는 음을 늘이는 게 아니다** — 원래 3칸 넘게 끄는 음만 잇게 고쳤다.

### 변형은 그대로 세다
`vary_meter` — 곡 전체 차이 **30~56%**, 바퀴마다 자리·세기가 다 달라진다.
`variation_check_test` 18항목 전부 통과(번호마다 달라진다 · 근음 남는다 · 결정적이다).
**약하게 만들어서 고친 게 아니다.**

### 편집기 — 위층 줄임을 알려 준다
코드 판에 층이 생겼으니, 같은 격자인데 한 옥타브 위에서 소리 나는 줄은 그 사실이
보여야 한다. 처음엔 패턴 이름 옆에 붙였는데 **가로·세로 둘 다 잘렸다**(그림으로 확인).
마디 버튼 뒤로 옮겼다 — 거기가 비어 있다. **보여만 준다**(고치는 손잡이는 안 만들었다).

## Phase 5 (5) — 장르 둘을 더했다: 디스코 · 가스펠 (2026-08-29 밤)

계획 7-1 은 「새 장르를 추가할 때 **엔진 코드를 대량 수정하지 않도록**」 구조를 데이터
중심으로 바꾸라고 했다. 그 말이 맞았는지 확인하는 제일 정직한 방법은 **실제로 더해 보는
것**이다. 엔진 코드는 **한 줄도 안 고쳤다** — 표에 값을 적고 패턴을 썼을 뿐이다.

```
고친 파일   genres.dart(정의) · song.dart(구간) · genre_mix.dart(믹스)
           genre_fx.dart(인서트) · packs.dart(묶음) · pattern_names.dart(낱말)
           patterns.dart(패턴 40개)
안 고친 것  engine · sequencer · synth · drums · mixer · UI 전부
```

### 디스코 — 「거울볼 아래, 네 박에 쿵」
```
116 BPM · 단조 · 16분 스윙 · 비켜 주기 0.12
드럼   네 박에 킥 + 엇박 하이햇 + 16분 셰이커 (디스코의 정체가 이 셋이다)
베이스 근음↔옥타브를 오가는 8분
건반   16분으로 끊어 치는 일렉피아노 (아래층)
스트링 마디를 통째로 끄는 **위층** — 같은 진행을 다른 층에서
브라스 리프 한 줄
진행   벌스 i–iv–VII–III · 프리 iv–v–VI–VII · 코러스 VI–VII–v–i
```
팩은 「춤」(하우스 · 프로그하우스 · 디스코).

### 가스펠 — 「넓게 흔들리는, 오르간과 손뼉」
```
78 BPM · **장조** · 8분 셔플 · 비켜 주기 0 (가스펠에 펌핑을 걸면 그건 고장이다)
드럼   넓은 백비트 + 라이드 셔플 + 손뼉
피아노 앞뒤로 받치는 컴핑 (아래층)
오르간 마디를 끄는 **위층** — 살짝 물린 드라이브만 건다
진행   벌스 I–vi–ii–V · 프리 IV–iii–ii–V · 코러스 IV–V–vi–I · 브릿지 vi–ii–IV–Vsus4
```
장조는 팝에 이어 **둘째**다. 팩은 「연주」(록 · 재즈 · 가스펠).

### 새 장르는 예전 장르보다 **엄한 잣대**를 통과했다
예전 13개는 만들어진 뒤에 시험이 따라왔다. 이 둘은 처음부터 전부 통과해야 했다:
```
harmony_clash   코드끼리·베이스가 마디마다 맞는가        통과
melody_clash    길게 끄는 음이 단2도로 안 부딪치는가
                (변형 4바퀴 전부)                      통과
stack_check     코드 악기 둘이 같은 음을 안 치는가       통과 (건반 아래층·스트링 위층)
melody_check    모티프 되풀이 · 숨 쉴 자리 · 아르페지오 아님  통과
chroma_check    **적은 코드가 실제 소리로 나오는가**      315자리 100%
genre_check     8개 표에 빠짐없이 들었는가              15개 × 8항목
pack_check      팩끼리 패턴을 안 나눠 쓰는가            208개 · 겹침 0
```

### 이름 표가 하나 걸렸다
`pattern_names_test` 가 **짧은 이름이 8자를 넘는다**고 잡았다 —
「디스코 베이스 코러스」. `kStyleWords` 에 'Disco'·'Gospel' 을 안 넣어서
스타일 낱말이 안 떨어진 것이다. 넣으니 「베이스 코러스」가 됐다.
**표 하나를 빠뜨리면 조용히 반쪽짜리 장르가 된다**던 그 자리를 시험이 지켰다.

### 보컬이 무대에서 **관악기를 불고 있었다**
장르를 더하다 `show_band.dart` 의 `kBandInstrument` 표를 보니 **'vocal' 이 없었다.**
음색을 모르면 트랙 종류로 떨어지는데(`melody` → `horn`), 그래서 **팝·R&B·가스펠**
세 장르의 보컬이 색소폰을 불고 있었다. 소리는 사람 목소리인데.

`'voice': ['vocal']` 갈래를 만들고 `_mic()` 를 그렸다 — 마이크를 잡고, 크게 부를 때
입에 붙이며 빈 손을 든다. **악기를 안 든 것 자체가 이 사람의 표시**라 손에 아무것도
더 쥐여 주지 않았다. 그림으로 확인했다(`show_snap` 9_보컬_0 · 9_보컬_90).

시험 둘을 붙였다:
- `4-c) 노래는 마이크` — vocal → voice
- `4-d) 곡이 쓰는 음색이 표에 다 있다` — **21종 전수.** 표에서 빠지면 오류가 안 나고
  조용히 엉뚱한 자세로 그려진다. 그런 건 사람이 그림을 볼 때까지 안 잡힌다.

### 크기 다시 맞추기
자리바꿈·층·베이스 손질로 장르마다 크기가 어긋났고(프로그하우스 스탭이 위층으로
가면서 저역이 줄었다), 새로 들어온 넷은 아예 보정이 없었다.
```
        고치기 전            고친 뒤
편차     6.2dB              4.2dB
제일 큼  드릴 −8.3           가스펠 −9.7
제일 작음 하우스 −14.5        하우스 −13.9
클리핑   0                  0
```
`kStyleGain` 을 15개 전부 다시 잡았다. 값 뒤 주석에 **보정 전 값**을 적어 뒀다 —
다음에 또 어긋나면 그 숫자에서 다시 계산하면 된다.

## 표에서 빠진 것 셋 — 장르를 더하다 줄줄이 나왔다 (2026-08-29 밤)

장르 둘을 넣으면서 **표를 하나씩 짚어 보니**, 이미 있던 장르들도 빠진 자리가 있었다.
전부 **오류가 안 나는 종류**다. 그래서 오래 남아 있었다.

### ① 보컬 음색이 세 표에서 빠져 있었다
`vocal` 은 소리를 내는 진짜 음색인데(`instruments.dart` 의 게인·엔벨로프·공간 표에
다 들어 있다) **`VOICE_LABEL`·`ALL_VOICES`·`kVoiceFamily` 셋에서 빠져 있었다.**
```
화면이 전부 한국어인데 그 트랙만 「vocal」 로 나왔다
고르는 목록에 없어서 **한 번 바꾸면 되돌릴 수 없었다**
```
팝·R&B·가스펠 세 장르가 쓰는 음색이다. 「보컬」이라 이름 붙이고 목록·묶음에 넣었다.

### ② 필인 든 판 넷에 민짜 짝이 없었다
`kPlainDrum` 은 「같은 코러스가 여러 번 나올 때 마지막 한 번만 필인을 쓴다」는 표다.
배열형 6곡과 트랩만 들어 있고 **프로그·팝은 빠져 있었다** — 즉 그 둘은 끝마디
탐 굴림이 **코러스마다 돌고 있었다.** 새로 만든 디스코·가스펠도 같은 자리에 있었다.
넷 다 민짜 판을 만들어 표에 넣었다.

### ③ 크기 보정이 넷 빠져 있었다
`styleGain` 은 모르는 장르에 조용히 1.0 을 준다. 드릴·R&B·디스코·가스펠이
그래서 −8dB 까지 튀어 있었다(편차 6.2dB).

### 그래서 **표를 지키는 시험**을 넷 붙였다
```
genre_check 5-d  곡이 쓰는 음색이 이름·목록·묶음 표에 다 있다   21종 × 3개 표
genre_check 5-e  필인 든 드럼 판에 민짜 짝이 있다              59개 판
genre_check 5-f  크기 보정이 장르마다 있다                     15개
show_check  4-d  곡이 쓰는 음색이 밴드 그림 표에 다 있다        21종
genre_check 2-b  고르는 화면의 카드 색이 안 겹친다              15가지 색
```
> 「빠뜨리면 조용히 반쪽짜리가 된다」는 말을 HANDOFF 에 세 번 적어 놓고도
> 세 군데를 빠뜨렸다. **적어 두는 것으로는 안 막힌다. 세는 시험만 막는다.**

### 이름표가 또 하나 빠져 있었다 — 가스펠 오르간
믹스 값을 눈으로 훑다가 트랙 이름이 **「org」** 로 찍혀 있는 것을 봤다.
`kSlotLabel` 에 'org' 가 없어서 슬롯 키가 그대로 이름이 된 것이다 —
'vocal' 과 **똑같은 종류**의 빠짐이다. 「오르간」을 넣고
`5-h) 트랙 이름표가 다 있다`(22개) 로 못 박았다.

> 같은 종류의 구멍이 표를 옮겨 가며 계속 나온다. 하나 막을 때마다
> **그 표를 세는 시험**을 같이 만들지 않으면 다음 장르에서 또 난다.

### 록·하우스가 1분짜리였다
같은 얼개(44마디)를 쓰는데 **빠른 곡은 훨씬 짧게 끝난다.**
```
발라드 68BPM  2:35        하우스 124BPM  1:25
로파이 78BPM  2:15        록     137BPM  1:17   ← 같은 표, 절반 길이
```
1분 남짓이면 「곡」이 아니라 루프로 들린다. `kFormOrderBy` 를 만들어 그 둘만
브레이크–코러스를 한 번 더 지나게 했다 — **새 패턴은 안 만들었다.**
이미 있는 구간을 한 번 더 지날 뿐이고, 변형이 바퀴마다 다르게 만든다.
```
하우스 1:25 → 1:48        록 1:17 → 1:37
```
배열형 6곡이 1:37~2:35 한 묶음으로 모였다(객체형은 3:19~4:00 — 구간 수가 더 많아서다).

## 킥이 뚱뚱했다 (2026-08-30, 사용자 지적)

> 「킥이 전반적으로 너무 뚱뚱거려」

### 재 보니 길이였다
```
키트        길이     어택    ~60   60~150  150~400
acoustic   303ms   2.0ms    6%    40%     41%
k808       541ms   9.5ms   36%    42%     19%   ← 제일 김
k909       254ms   2.0ms    9%    44%     35%
lofi       405ms   2.1ms   10%    44%     35%
rock       176ms   1.8ms    6%    41%     40%
```
대역은 문제가 없었다 — 킥은 원래 60~150Hz 가 주인이다. **꼬리가 길었다.**
로파이 405ms · 808 541ms 면 다음 킥이 올 때까지 앞 킥이 안 죽는다. 그게 「뚱뚱」이다.
게다가 로파이·808 은 비터 딱 소리가 0.30 밖에 안 돼서 **윤곽이 없는 채로 울리기만** 했다.

### 세 가지를 같이 바꿨다
```
① 울림 길이   176~541ms → 138~379ms   (다섯 키트 전부 22~37% 짧게)
② 음정이 앉는 시간  0.26 → 0.19초      「둥—」 하고 끌리던 꼬리
③ 비터 딱 소리  0.30~0.85 → 0.40~0.88  길이를 줄인 만큼 윤곽을 세운다
```
길이만 줄이면 「톡」이 되고 만다 — 예전에 그래서 「구리다」는 말을 들었다.
**줄인 만큼 어택을 세우는 것**이 짝이다.

### 한 번 되돌렸다 — 909 가 95ms 로 떨어졌다
첫 시도에서 909 킥이 **95ms** 가 나왔다. 2026-08-23 에 「75~142ms 는 너무 짧다」고
늘렸던 바로 그 구간이다. `rel` 을 되돌려 174ms 로 맞췄다. 록도 129 → 138ms.

> 같은 값을 한 번은 늘리고 한 번은 줄였다. **되돌린 게 아니라 그 사이에서 자리를
> 찾은 것이다**(139~379ms). 골든 갱신 이력에 두 줄을 나란히 적어 뒀다 —
> 다음에 또 만질 때 한쪽만 보고 판단하면 같은 왕복을 반복한다.

### 골든이 제 일을 했다
`engine_guard_test` 가 바로 잡았다: RMS 0.1816 → 0.1896 · 1번 대역 45.4% → 47.8%.
**짧아졌는데 RMS 가 올랐다** — 딱 소리를 키운 몫과, 음정이 빨리 앉아 효율 좋은
저역에 머무는 시간이 늘어난 몫이다. 의도한 변경이라 값을 갱신하고 이유를 적었다.

### 킥을 조이니 시험 하나가 걸렸다 — **소리가 아니라 재는 법**
`loop_check_test 5) 정지` 가 「정지 뒤 타격 1개」로 떨어졌다.
`_onsets` 는 **앞 창이 없는 첫 창을 무조건 타격으로 읽는다**(prevEnv 가 0에서 시작).
정지 직후 버퍼의 첫 창에는 앞 판의 **꼬리**가 들어 있다 — 킥 길이가 바뀌면서
그 꼬리 크기가 문턱을 넘은 것이다. 새 타격이 난 게 아니다.

정지는 「지금 나던 소리를 자르는 것」이 아니라 「다음 것을 안 내는 것」이다.
앞 0.3초는 꼬리로 보고, 대신 **꼬리가 실제로 죽는지**(마지막 0.5초 최대 진폭 < 0.001)
를 같이 보게 했다 — 예전 검사에는 없던 항목이다. 걸린 김에 더 촘촘해졌다.

## 장조 곡이 단조로 열릴 수 있었다 (2026-08-30)

가스펠 씬 화면을 그림으로 뽑았더니 머리에 **「조 C 단조」** 가 찍혀 있었다.
가스펠은 장조다. 그림을 만든 쪽의 실수였지만(시험이 기본 `Transport` 를 넘겼다),
그걸 따라가다 **진짜 구멍**을 찾았다.

`Store.open` 이 조성·템포가 없는 파일을 열 때 **지금 열려 있던 곡의 값**을 물려받고
있었다:
```dart
..mode = j['mode'] as String? ?? transport.mode;   // ← 앞 곡의 조성
```
단조 곡을 보다가 조성 칸이 없는 옛 팝·가스펠 파일을 열면 **단조로 열린다.**
그러면 I–vi–ii–V 가 i–VI–ii–v 가 되어 **코드가 전부 딴 것**이 된다.
소리는 나므로 오류로는 절대 안 잡힌다.

**그 곡의 스타일 기본값**으로 떨어지게 고쳤다(`songGenreOf(project.genre)`).
`store_check_test 6)` 으로 못 박았고, 고치기 전 코드로 되돌려 **실제로 실패하는 것**
까지 확인했다(gospel · minor · 140BPM 으로 열렸다).

그림도 고쳤다 — 이제 그 장르의 템포·조성으로 찍는다.
**그림이 거짓말을 하면 그림을 보고 판단하는 일이 전부 틀어진다.**

## 느낌 손잡이가 두 장르에서 죽어 있었다 (2026-08-30)

`feel_check_test` 는 손잡이 하나하나가 **옳게 도는지**를 한 패턴으로 본다.
그건 「규칙이 맞는가」다. **「15개 장르 전부에서 손이 닿는가」는 아무도 안 봤다.**
새 판(`feel_matrix`, 손잡이 5 × 장르 15 = 75칸)을 만들어 재 봤다:

```
장르        신남   빽빽   스윙   필    변화
jazz         89%     0%    41%    2%    25%   ← 빽빽이 죽어 있다
ambient      86%     0%    19%    5%    23%   ← 여기도
lofi         48%     8%    46%    2%    14%
house        44%     8%    23%    2%    17%
```

### 왜 죽었나 — 「빽빽」이 **하이햇만** 쪼갰다
재즈는 하이햇이 없다. **라이드와 셰이커로 시간을 센다.** 엠비언트도 셰이커뿐이다.
그 둘에서는 손잡이를 끝까지 끌어도 **아무 일이 안 일어났다** — 오류도 없고,
화면도 멀쩡하고, 소리만 그대로였다.

그루브가 이미 쓰는 **「잔가지」 묶음**(hat·ride·shaker)을 여기서도 쓰게 했다.
다만 아무 줄이나 쪼개면 안 된다 — **간격을 보고 정한다**:
```
1칸(이미 촘촘)        건드리지 않는다
2·4칸(16분·8분)       그냥 쪼갠다        ← 하이햇·재즈 라이드
8·16칸(널찍한 줄)     손잡이를 **끝까지 밀었을 때만**
                     ← 엠비언트 셰이커의 여백은 그 장르의 소리다
```
```
        전 → 후
jazz    0% → 26%   (표에서 제일 크게 움직인다 — 라이드가 8분으로 쪼개진다)
ambient 0% →  4%   (끝까지 밀어야 반응한다 — 그게 이 장르에 맞다)
lofi    8% → 18%   하우스 8→19% · 시티팝 6→16% · 프로그 6→15%
```
하이햇만 보던 탓에 **여덟 장르가 덜 반응하고 있었다.**

### 「필」은 원래 작게 나온다
팝이 0.9% 로 걸렸는데 결함이 아니다 — 필은 **네 바퀴 중 한 마디**만 건드리고,
음이 많은 장르일수록 비율이 작다. 문턱을 손잡이별로 나눴다(필만 0.5%).
**숫자가 작다고 다 고장은 아니다. 무엇을 재는지 알고 문턱을 정해야 한다.**

`feel_reach_test` 로 못 박았다 — 75칸 중 하나라도 죽으면 실패한다.
새 장르를 넣을 때 제일 놓치기 쉬운 자리다.

## 셰이커·카우벨이 지금까지 **전부 무음**이었다 (2026-08-30)

킥을 고친 김에 드럼 **열 레인을 전수**로 재 봤다(그전엔 킥·스네어만 봤다).

```
── shaker ──
acoustic    0ms   0.0ms  0.00  ｜ 0% 0% 0% 0% 0% 0%
   ... 다섯 키트 전부, 카우벨도 마찬가지
```

### 이름이 두 벌이었다
```
patterns.dart (데이터 쪽)   'shaker'   'cowbell'
drums.dart    (소리 쪽)     'shake'    'cow'      ← trigger 스위치에 이 둘만
```
시퀀서는 데이터 쪽 이름으로 예약하는데 스위치에 그 case 가 없으니 **아무 일도
일어나지 않았다.** 오류도 안 나고, 다른 타악기는 멀쩡히 나니까 「그냥 안 들리는 층」
으로 남아 있었다.

**15곡 전체 타격 14775개 중 2361개(16%)가 소리가 안 났다.**
로파이 코러스는 4마디에 셰이커가 32타 있다 — 그 사각사각거리는 층이 통째로 없었다.
재즈 인트로·헤드·B, 엠비언트, 디스코, R&B, 팝 인트로, 프로그 드롭, 가스펠도 같다.

### 시험이 왜 못 봤나 — **자기 쪽 이름으로 자기를 확인했다**
`drum_check_test` 의 「소리 남」 검사가 `DRUM_ORDER`, 즉 **소리 쪽 이름**으로만 돌았다.
`9-b) 패턴이 쓰는 이름으로도 소리 남` 을 붙이고, 고치기 전 코드로 되돌려
**실제로 `shaker, cowbell` 로 실패하는 것**까지 확인했다.

이름을 한쪽으로 통일하지 않았다 — 데이터 쪽 이름이 **사용자가 편집기에서 만든 패턴에
저장돼 있다.** 바꾸면 그 패턴들이 이름을 잃는다. `kDrumAlias` 로 둘 다 받는다.

### 소리 크기는 거의 안 움직였다
타격이 16% 늘었는데 편차 4.5 → 4.3dB, 클리핑 0. 셰이커는 **작고 높다**(피크 0.26,
5kHz 위가 90%). 크기가 아니라 **결**을 더하는 층이라 그렇다.
15곡 전곡 스윕도 그대로 통과(최대 동시 드럼 4~10, 한도 24).

## 사용감 — 눈으로 보고 고친 것 넷 (2026-08-30)

### ① 라이브 패드가 **C단조를 D#·G#·A# 로** 적고 있었다
C단조는 **Eb·Ab·Bb** 다. 소리는 같지만 악보를 읽는 사람에게는 다른 음이다.
♯/♭ 은 조가 정한다 — `keyUsesSharps(root, mode)` 를 만들고, **단조는 나란한
장조**(+3반음)로 판단한다. G장조는 F♯, C단조는 E♭ 로 나온다(둘 다 시험에 있다).

조 이름표도 같이 고쳤다: 「D# 단조」라고 쓰는 사람은 없다 — **E♭단조**다.
장조는 또 다르다(C♯장조가 아니라 **D♭장조**). `keyLabel(root, mode)` 와
`kMinorKeyNames`/`kMajorKeyNames` 를 나눴고, 고르는 칩도 지금 조성에 맞춰 적는다.

### ② 곡 화면 스타일 줄에서 **내가 뭘 골랐는지 안 보였다**
장르가 15개가 되니 한 줄에 넷쯤 보이고 나머지는 오른쪽으로 흘러간다.
가스펠은 14번째다 — 화면을 열면 로파이만 보인다. 열 때·바뀔 때 고른 칩을
화면 가운데로 끌어오게 했다(`Scrollable.ensureVisible`).

### ③ 믹서 스트립이 100dp 붙박이라 **셋만 보였다**
「트랙 4개」라고 적혀 있는데 셋만 보이면 나머지가 있는 줄을 모른다.
너비를 화면에 맞춰(넷은 들어가게, 76dp 아래로는 안 내려감) 잡고,
다 안 들어가면 **오른쪽 끝을 흐려** 더 있다는 표시를 준다.
(7트랙짜리 팝은 기본이 「한눈에」 보기라 원래 다 보인다 — 스트립 보기만 해당된다.)

### ④ 그림이 거짓말을 하고 있었다
가스펠 씬 화면 그림에 「조 C 단조」가 찍혀 있었다 — 가스펠은 장조다.
시험이 기본 `Transport` 를 넘긴 탓이다. **그림을 보고 판단하는데 그림이 틀리면
그 판단이 전부 틀어진다.** 이제 그 장르의 템포·조성으로 찍는다.
(이걸 따라가다 `Store.open` 의 진짜 구멍을 찾았다 — 위 항목 참고.)

## 배선을 전수로 훑었다 — 둘이 더 나왔다 (2026-08-30)

「곡이 가리키는 것이 **실재하는가**」를 표별로 전수 확인했다.
패턴 이름 · 슬롯 이름 · 키트 이름 · 구간 순서 · 스타일 머리말.

### ① R&B 프리 구간에 **베이스가 없었다**
`RnB Bass P` 를 **코드 목록에 잘못 넣어 놨다**(2026-08-26 에 내가 넣은 것이다).
`findBassPattern` 이 못 찾으니 그 파트는 통째로 안 실렸다 — 오류는 안 나고,
**코드 패턴 고르는 목록에는 베이스 줄이 하나 섞여** 있었다.
베이스 목록으로 옮겼다. `chroma_check` 의 베이스 음이 104 → 108개로 늘었다
(소리로 확인된 셈이다).

### ② 네 장르가 **패턴 목록에서 제 것을 못 찾고 있었다**
`kStylePrefixes` 에 드릴·R&B·디스코·가스펠이 빠져 있었다. 그러면 둘이 망가진다:
```
① 그 장르 것이 목록 위로 안 올라온다        → 208개 중에서 눈으로 찾아야 한다
② patternIsBasic 이 「기본 패턴」으로 봐서   → 다른 모든 장르 목록에 섞여 나온다
```
②가 더 나쁘다 — 로파이를 만드는 사람의 드럼 목록에 「드릴 훅」이 끼어 있었다.

덤으로 **'Disco' 머리말이 시티팝 아래 있었다.** 디스코가 장르가 아니라 시티팝의
곁가지였던 시절의 흔적이다. 제 장르로 돌려줬다.

### 그래서 시험 둘
```
5-i) 곡이 가리키는 것이 다 실재한다   패턴·슬롯·키트·순서 전부
5-j) 곡이 쓰는 패턴이 제 스타일로 묶인다  15개 스타일
```
> 「표를 빠뜨리면 조용히 반쪽짜리가 된다」를 이번 회차에만 **여섯 번** 겪었다
> (음색 이름·슬롯 이름·믹스 편성·필인 짝·크기 보정·스타일 머리말).
> 표를 늘릴 때마다 **그 표를 세는 시험**을 같이 만드는 것 말고 다른 방법이 없다.

## 내보내기가 마스터 페이더를 빼먹고 있었다 (2026-08-30)

재생은 엔진이 **스타일 보정 × 사용자 페이더**를 따로 곱한다
(`Engine.styleGain` × `Engine.userGain`). 내보내기는 곱할 자리가
`ExportJob.masterVol` **하나뿐**인데 거기에 스타일 보정만 넣고 있었다.

```
믹서에서 마스터를 −6dB 로 내린다 → 들리는 소리는 작아진다
                              → 내보낸 파일은 **그대로**
```
마스터 **인서트**(마스터링)는 실리는데 **페이더만** 안 실렸다.
`SceneSequencer.exportMasterVol(project, master)` 로 이름을 붙여 곱하고,
`export_check_test 6)` 으로 못 박았다.

## 사용감 — 15개가 되니 「지금 뭘 골랐는지」가 안 보인다

곡 화면 스타일 줄에 이어, **만들기 시트**도 같은 문제였다. 팩이 6개라
가스펠(연주 팩)은 한참 아래인데, 「스타일 바꾸기」로 열면 맨 위만 보인다.
열 때 지금 것을 화면 안으로 끌어온다(`Scrollable.ensureVisible`).

## 없는 병을 두 시간 쫓았다 — 그리고 진짜 하나를 고쳤다

전체 시험이 갑자기 **16분 → 31분**이 됐다. 두 번 연속이라 회귀로 봤다.
범인 후보를 하나씩 재 봤다:
```
render_bench      정상 (2분)          → 엔진은 안 느려졌다
long_run 15곡     정상 (105초)        → 렌더도 안 느려졌다
씬 하나 렌더       707ms              → 씬 렌더도 정상
버스를 0 으로      **더 빠르다**(460 vs 736ms) → 디노멀 가설 기각
엔진 20·40·80개    개당 15ms 그대로     → 엔진이 새는 것도 아니다
장르 2·4·8·10개    8·14·30·40초        → **완벽히 선형**
장르 12개          9분 넘음            → ?
```
마지막 한 줄만 안 맞았다. **다시 돌렸더니 48초.** 전체 15개도 64초.

즉 그 두 번은 **이 기계가 이따금 멈추는 그것**이다(HANDOFF 「시험이 흔들렸다」
항목의 그 현상). 코드는 멀쩡했다.

> 「느리다」와 「멈춘다」는 다르다 — 전에 적어 놓고도 또 속았다.
> **선형인지 아닌지**를 재는 것이 이번에 답을 줬다. 2·4·8·10 이 8·14·30·40초로
> 곧게 늘어나는데 12만 9분이면, 그건 코드가 아니라 그 순간의 기계다.

### 그래도 하나는 진짜였다 — 크로마가 `cos` 를 수억 번 부르고 있었다
쫓다가 찾았다. `_goertzel` 이 해닝 창을 **샘플마다 계산**하고 있었고
(`0.5 - 0.5*cos(2πi/n)`), 재는 창이 음이 울리는 **내내**였다 —
엠비언트 서브는 한 음이 6초라 15만 샘플 × 37음이었다.
```
창을 0.5초로 자르고, 창 배열을 길이별로 캐시
→ 커버리지는 6배(52 → 315자리)인데 시간은 예전과 비슷(64초)
```
길게 잰다고 더 정확해지지 않는다 — 실제로 결과가 한 자리도 안 바뀌었다
(창을 0.75초로 넓혀 봐도 제일 나쁜 곳이 똑같이 75%였다).

## 「보컬」이 삼각파였다 — 일곱 표에 있고 정작 음색표에만 없었다

`vocal` 은 SUSLV·ATK·KEYTRACK·SPACE·VOICE_LABEL·ALL_VOICES·kVoiceFamily
**일곱 표**에 다 있었다. 그런데 `INSTRUMENTS` 에만 없었다.
없으면 오류가 아니라 `kInstDefault`(맹숭한 삼각파)로 조용히 떨어진다.
팝·R&B·가스펠의 **주선율**이 그것으로 울고 있었다.

크기로는 안 보인다(voice_meter 는 멀쩡하다고 한다). 배음을 재야 보인다:
```
보컬(고치기 전)   0 -118  -19 -122  -27 -124  -32   ← 짝수 배음이 없다 = 삼각파
색소폰           0  -12  -24  -13  -12  -12  -18   ← 배음이 다 있다
```
`test/timbre_meter.dart` 를 새로 만들어 잡았다. **크기와 음색은 다른 것이다.**

고친 내용: 성대 파형(HARM, 배음이 다 있고 고르게 준다) · **포먼트**
(BODY_RES 700·1220Hz — 몸통 공명과 원리가 같아 그 표를 그대로 썼다) ·
가수 비브라토(VIB) · 스쿱(BEND).
```
보컬(고친 뒤)    -1   -1    0   -4   -8  -13  -17   ← 700·1220 이 봉우리
```

### 유니즌은 일부러 0 이다
4cent 만 줘도 배음끼리 맥놀이가 생겨 3배음이 20dB 파인다
(`uni 4` → `-13 -30`, `uni 0` → `-1 0`). 혼자 부르는 목소리에 코러스가
걸려 있으면 그 순간 신스로 들린다. 흔들림은 Human 과 비브라토가 이미 만든다.

### 지키는 법
`genre_check_test 5-d` 가 **세 표**를 보고 있었는데 정작 소리를 정하는
`INSTRUMENTS` 를 안 봤다. 네 번째로 넣었다. 되돌려서 실패하는 것도 확인했다.
> 목록에 있는데 **정의가 없는 것**이 제일 안 보인다. 이름은 한국어로 잘 나온다.

## 지우면 되돌릴 수 있어야 한다 — 두 군데가 확인도 없었다

**곡 삭제**(`songs_view`): 「⋮」의 **「복제」 바로 아래**가 「삭제」였고,
한 번 누르면 확인도 없이 곡이 영영 사라졌다.
**씬 삭제**(`scene_view`): 씬 하나에 트랙 전부의 패턴이 들어 있고, 게다가
`onSceneRemoved` 가 **그 씬을 쓰던 곡 구간까지 같이 지운다.** 이것도 한 번에 갔다.

확인 창을 하나 더 띄우는 쪽이 아니라 **되돌리기**를 골랐다 —
확인 창은 제대로 누른 사람까지 매번 붙잡으면서, 정작 「누르고 나서 아차」는 못 막는다.
```
「나무」 을 지웠습니다                              [되돌리기]
「코러스」 을 지웠습니다 · 곡에서 2군데도 같이 빠졌어요  [되돌리기]
```
`remove` 가 **되돌릴 거리를 들고 나오게** 바꿨다(`RemovedSong`·`RemovedScene`):
파일 내용 · 목록에서의 자리 · 곡 구간표 · 열려 있었는지. 셋 중 하나라도 빠지면
되돌린 게 아니다. 씬 구간표는 **깊게** 복사한다 — `onSceneRemoved` 가 남은 것들의
번호를 그 자리에서 내리므로, 얕게 들고 있으면 되돌릴 때 이미 어긋나 있다.

트랙 삭제는 그대로 뒀다 — 이미 확인 창이 있고, `dispose()` 시점이 얽혀서
되돌리기로 바꾸면 얻는 것보다 깨질 위험이 크다.

### 위젯 시험에서 두 시간 헤맨 것: 탭이 파일 I/O 를 못 기다린다
「삭제」를 눌러도 곡이 3개 그대로였다. onTap 은 들어가는데
`await store.remove()` 가 **영영 안 끝났다** — 가짜 시계 위에서 만들어진
진짜 파일 I/O 는 `runAsync` 를 뒤에 아무리 붙여도 안 깨어난다.
```dart
await tester.runAsync(() async {          // ← 탭을 이 **안에서** 해야 한다
  await tester.tap(find.text('삭제'));
  await Future<void>.delayed(...);
});
```
같은 파일의 「이름 바꾸기」 시험이 통과하고 있어서 더 헷갈렸다 —
`rename` 은 목록을 **동기로** 먼저 고치고 파일은 나중에 쓴다. 시험이 본 건
동기 부분이었다(파일 쓰기는 그때도 안 끝났을 것이다).

## 씬 메뉴가 길게 누르기에만 있었다

이름 바꾸기·복제·삭제가 **길게 눌러야만** 나왔다. 곡 목록에서 이미 겪은 것과
같은 문제다 — 「있는 줄도 모르니 이름이 전부 "씬 2" 로 남는다」.
**지금 씬에만** ⋮ 를 붙였다. 하나만 나오니 안 어지럽고, 어느 씬에 대한
메뉴인지도 그 자리에서 보인다. 길게 누르기는 그대로 뒀다(익힌 사람이 있다).

## 스타일을 바꿀 때, 걱정되는 쪽을 입 다물고 있었다

```dart
keptMine > 0 ? '지켰습니다' : (parkedMine > 0 ? '빠졌습니다' : null)
```
**둘 다 0 이 아닐 수 있다** — 일부는 새 얼개에서 자리를 찾고 일부는 못 찾는다.
그런데 이 삼항은 지킨 쪽만 말하고 **빠진 쪽은 입을 다물었다.**
하필 사용자가 걱정하는 것은 빠진 쪽이다("내가 만든 게 날아갔나").
`genreChangeNote(kept, parked)` 로 화면에서 떼어 내고 네 경우를 다 시험한다.

## 편집기에서 재생 막대가 시간의 85% 를 화면 밖에 있었다

4마디 판은 **64칸 × 30px = 1920px** 인데 화면에 보이는 건 280px 남짓이다.
그래서 재생을 눌러도 **어디를 치고 있는지 안 보였다.** 지금 어디인지 모르는
편집기는 반쪽이다.

따라가게 했다. 규칙은 셋뿐이고 `followTarget()` 로 화면에서 떼어 놨다:
```
보이는 폭의 5~92% 안이면 안 건드린다   ← 가장자리 직전까지 놔둬야 매 프레임 안 민다
밀 때는 왼쪽 15% 자리로               ← 0% 에 놓으면 다음 순간 또 나간다
밀 거리가 1px 미만이면 안 민다
```

### 고치는 중에는 안 따라간다
음을 찍고 있는데 화면이 저 혼자 넘어가면 다음 손가락이 엉뚱한 칸에 떨어진다.
**손을 대면 5초 동안 가만히 있는다** — 찍기·끌기·마디 버튼·손으로 밀기·촘촘 보기.
손으로 민 것만 잡아야 하므로 `dragDetails != null` 로 거른다 —
우리가 `animateTo` 로 옮긴 것은 dragDetails 가 없다(안 그러면 스스로를 막는다).

### 덤: 촘촘 보기가 보던 자리를 잃고 있었다
칸 너비만 바꾸면 스크롤 위치는 픽셀이라 그대로 남는다. 3마디를 보다가
촘촘히 하면 **4마디가 보였다**(960px ÷ 18 = 53칸). 칸 수로 환산해 다시 놓는다.

## 라이브 「장르가 정해 주는 음색」이 통째로 죽어 있었다

`LiveChannel.voice` 의 setter 는 **「사용자가 골랐다」는 뜻**이라 `voiceAuto` 를
끈다. 그런데 저장 파일을 불러올 때 그 setter 를 그대로 썼다.
자동 저장이 있으니 **모든 곡이 한 번은 저장됐다 열린다** →
계획 7 의 「장르마다 라이브 기본 음색」이 첫 왕복에서 영영 죽었다. 오류는 안 난다.
```
저장 전   voice=piano auto=true
불러온 뒤 voice=piano auto=false   ← 여기
```
`restore(...)` 를 따로 뒀다 — **값을 넣는 것과 「내가 골랐다」고 적는 것은 다른 일이다.**
`auto` 도 파일에 남긴다. 옛 파일은 `auto` 가 없으니 **true 로 본다**(여태 죽어
있었으니 「고른 적 없다」가 맞을 확률이 높고, 한 번 고르면 그때 다시 꺼진다).

## 저장 파일 하나가 앱을 못 열게 만들 수 있었다

일부러 망가뜨려 놓고 일곱 가지를 열어 봤다. 여섯은 멀쩡했는데 하나가 터졌다:
```
모르는 조(mode) → Null check operator used on a null value  (kScale[mode]!)
```
마지막에 열던 곡을 자동으로 여니 **그 곡 하나로 앱이 안 뜬다.**
`scaleOf(mode)` 로 바꿨다(모르면 단조). 여는 쪽에서도 조·템포·루트를 검사한다 —
`"mode":"lydian","bpm":0,"root":999` 를 글자로 박아 넣고 열리는지까지 시험한다
(`bad_file_test`). NaN 은 `jsonEncode` 가 거부해서 코드로는 못 만든다 —
**글자로 고쳐야** 실제로 들어올 수 있는 것을 재현할 수 있다.

> 소리가 조금 다른 것과 아무것도 못 하는 것은 급이 다르다. 파일이 앱을 못 죽이게.

## voice_meter 의 「폭」이 없는 병을 만들 뻔했다

18.7dB 로 나와서 「악기끼리 크기가 벌어졌다」로 읽힌다. 아니었다 —
저역 악기는 **40번 음**으로, 나머지는 60번으로 잰다. 낮은 음이 에너지가 크다.
갈라서 찍게 고쳤다: **멜로디·화성 28종 11.5dB · 저역 6종 3.8dB.**

## 장르 믹스가 6개 스타일에서 통째로 죽어 있었다

`kGenreMix` 는 스타일마다 **볼륨·좌우·잔향·EQ** 를 잡아 준다 — 그게 「그 장르로
들리게」 하는 절반이다. 15줄이 적혀 있는데 **9줄만 쓰이고 6줄은 죽어 있었다.**

```
[house] 트랙: drum 1.00  bass 1.00  chord 1.00  melody 1.00   ← 전부 기본값
        표  : drum 1.10  bass 1.05  chord 0.78  melody 0.72
```

배열형 6종(로파이·하우스·힙합·시티팝·발라드·록)이 그랬다. `_seedObject` 는
믹스를 얹는데 `_seedArray` 는 **인서트만 얹고 믹스는 안 얹었다.**

죽은 이유가 한 겹 더 있다: 재생 경로가 **전부** `setGenreMix(null)` 을 부른다
(버스에 걸린 장르 몫을 걷어내야 사용자 믹서가 살아난다). 그러니 **트랙에 안
얹으면 어디에도 안 얹힌다.** 두 길 중 하나만 막힌 게 아니라 둘 다 막혀 있었다.

고친 뒤 크기 편차는 **4.2 → 4.0dB**(오히려 조금 좁아졌다) · 클리핑 0.

### 같이 넣은 것: `mixAuto` — 손으로 맞춘 페이더를 안 덮는다
믹스를 얹는 코드가 없었으니 「스타일 바꾸면 믹서가 날아간다」 문제도 없었다.
고치는 순간 생긴다. 인서트에는 이미 같은 규칙이 있었다(`fxAuto`) —
**「값을 넣는 것」과 「내가 만졌다고 적는 것」은 다른 일이다**(이번에 세 번째다:
`LiveChannel.restore` · `Arrangement.restore` · 이제 `Track.applyMix`).
```
세터(vol=/pan=/rev=/eqChanged())  → 사용자가 만졌다 → mixAuto = false
applyMix(...)                     → 장르가 얹는다   → 깃발 안 건드림
```
옛 파일은 `mixAuto` 가 없다 → **true 로 읽는다.** 그때는 믹스를 얹는 길 자체가
없었으니 지금 들어 있는 값은 사용자가 맞춘 게 아니라 그냥 기본값이다
(`fxAuto` 를 false 로 읽는 것과 반대인 이유: 인서트는 있으면 사용자가 꽂은 것이다).

### 죽어 있던 깃발이 단서였다
`spaceAuto` 라는 bool 이 있었다. pan 세터가 **끄기만 하고 아무도 안 읽었다.**
누군가 「손으로 만진 팬은 지켜야지」까지 생각하고 멈춘 자리다. 그 자리에
`mixAuto` 를 넣었다.

> **아무도 안 읽는 깃발은 못 끝낸 생각의 표시다.** 지우기 전에 왜 놨는지 본다.

### 지키는 법
`genre_check_test 5-f2` 가 **15개 스타일 전부** 표의 값이 실제 트랙까지 닿는지
본다(이름이 맞는지는 5-g 가 이미 봤다 — 이름이 맞아도 **얹는 코드가 없으면**
아무 일도 안 일어난다는 게 이번 교훈이다). 되돌려서 여섯 개가 정확히 이름을
불리며 실패하는 것도 확인했다.

## 「막 써 보기」 — 무작위로 2천 걸음 걸어 보니 셋이 나왔다

화면 시험은 한 화면의 한 흐름을 본다. 사람은 그렇게 안 쓴다 — 스타일을 구경하다
씬을 지우고, 트랙을 더하고, 판을 고치고, 되돌리고, 저장했다 다시 연다.
**그 사이사이에 어긋나는 것**이 오래 남는 고장이 된다.

`rough_use_test` 를 만들었다. 씨앗 고정 무작위(실패하면 **같은 순서로 다시 난다**)로
11가지 동작을 섞고, 매 걸음마다 일곱 가지를 확인한다:
없는 트랙을 가리키는 씬 · 없는 씬을 가리키는 구간 · 보는 씬이 범위 밖 ·
모르는 조 · 소리를 못 만듦 · **없는 판을 실음** · **왕복에서 글자 하나라도 달라짐**.

40벌 × 100걸음 = 4000걸음, 7초. 이걸로 셋을 잡았다.

### ① 씬을 복제하면 곡이 통째로 어긋났다
```
전: 곡 = 인트로 · 벌스 · 코러스 · 브레이크
후: 곡 = 인트로 · 인트로 복사 · 벌스 · 코러스   ← 브레이크가 곡에서 사라진다
```
씬을 끼우면 뒤쪽 번호가 하나씩 밀리는데 **곡 구간은 번호로 씬을 가리킨다.**
보던 씬 번호도 밀려서, 트랙엔 브레이크가 실려 있는데 씬 바는 코러스를 가리켰다.
지우는 쪽은 처음부터 `onSceneRemoved` 로 이걸 했다 — **끼우는 쪽만 짝이 없었다.**
`onSceneInserted` 를 만들어 짝을 맞췄다.

> 짝이 있어야 하는 것은 **한쪽만 있을 때가 제일 안 보인다.** 지우기는 눈에 띄어서
> 처음부터 고쳤고, 복제는 「그냥 하나 더 만드는 것」처럼 보여서 아무도 안 봤다.

### ② 새로 더한 드럼만 어쿠스틱이었다
록 곡에 드럼을 하나 더하면 어쿠스틱으로 들어와 나머지와 다른 소리가 났다.
그리고 **저장했다 열면 그제서야 록 키트로 바뀌었다**(`_loadScene` 이 씬 키트를
얹으므로). 「어제는 이 소리가 아니었는데」가 이런 데서 난다.

### ③ `currentScene` 이 그냥 열린 필드였다
`p.currentScene = 3` 이 가능한데, 그러면 **번호만 바뀌고 실린 판은 그대로**다.
앱은 늘 `launchScene` 을 써서 아직 사고가 없었지만, 열려 있는 한 언젠가 난다.
읽기 전용으로 닫았다.

## 장르가 적어 둔 필터 52군데가 어디에도 안 실리고 있었다

`kGenreMix` 는 슬롯마다 **고역통과·저역통과·느린 움직임**도 적어 뒀다 —
보컬 200Hz(저음을 걷어 베이스와 안 부딪히게) · 엠비언트 패드 LFO(숨쉬듯 움직이게).
**52군데**다. 그게 전부 죽어 있었다.

앞의 「장르 믹스가 6개 스타일에서 죽어 있었다」와 같은 뿌리인데 **한 겹 더 깊다**:
그건 배열형 6종만이었고, 이건 **15종 전부**다. 트랙에 담을 칸이 아예 없었다
(`Track` 에 vol·pan·rev·eq 는 있는데 hpf·lpf·lfo 는 없었다).

얼마나 차이 나는지 재 봤다(같은 곡을 필터 있이/없이 렌더해 차이의 RMS):
```
트랩      음악보다 6dB 아래   ← 확실히 들린다
가스펠            11dB 아래
팝·R&B            13dB 아래
엠비언트          12dB 아래
로파이            (필터 안 씀)
```
트랙에 네 칸을 더하고 `applyMix` · `pushMix` · `mixSnapshot` · 내보내기까지 이었다.
아이솔레이트 메시지는 **뒤에 덧붙였다** — 길이를 먼저 보므로 짧은 옛 메시지도 그대로 돈다.
크기 편차는 4.0dB 그대로, 클리핑 0.

### 재는 것부터 틀릴 뻔했다
처음 잰 결과가 「엠비언트 −120dB(차이 없음)」였다. 버스 이름이 **슬롯 이름이 아니라
트랙 id**(`busOf`)인데 슬롯 이름으로 버스를 찾고 있었다 — 드럼만 우연히 맞아서
드럼 필터만 재고 있었던 것이다. 슬롯 → 트랙 → 버스 이름으로 두 번 건너야 한다.

> **0 이 나오면 「없다」가 아니라 「내가 잘못 쟀다」부터 의심한다.**

### 지키는 법
`genre_check_test 5-f2` 가 필터까지 본다(볼륨만 보면 52군데가 죽어도 통과한다).
`export_check_test 7)` 은 **저역이 실제로 줄어드는지**로 본다 — 칸 개수만 세면
담기만 하고 안 쓰는 경우를 못 잡는다. 되돌려서 0.14798 → 0.14798(안 변함)로
실패하는 것도 확인했다.

## 시험이 또 늦었다 — 이번엔 1초 만에 알았다

전체 시험이 **16분**. 전에는 여기서 코드를 뒤졌다. 이번엔 `time` 한 줄로 끝났다:
```
160.17s user  12.10s system  17% cpu  16:16.45 total
```
**CPU 17%.** 우리 몫은 160초인데 벽시계가 976초면, 나머지 816초는 이 기계가
우리를 안 돌린 시간이다. 코드가 느려지면 user 가 늘지 벽시계만 늘지 않는다.

> 「느리다」와 「멈춘다」를 가르는 건 **CPU% 한 숫자**다. 다음엔 이것부터 본다.

## 쇼 화면이 30초 만에 꺼지고 있었다

쇼 화면은 파일 첫 줄부터 「곡을 틀어 놓고 폰을 세워 두는 자리」라고 적혀 있다.
그런데 안드로이드 기본 화면 꺼짐이 보통 30초다 — **3분짜리 곡의 첫 소절에서
화면이 검게 됐다.** 남에게 보여 주려고 만든 화면인데.

**패키지는 안 더했다**(§15). `MainActivity.kt` 가 창 플래그 하나를 세우는 게
전부라 채널 하나면 된다. 앱 전체에 켜면 배터리를 먹으니 **쇼 화면에 있는 동안만**
켜고 나갈 때 끈다. 시험은 가짜 채널을 달아 `[true] → [true, false]` 를 확인한다
(켜는 것만 보면 반쪽이다).

## 말이 두 가지였다

`create_sheet`(고르는 화면)의 카드 오른쪽에 **아무 설명 없는 숫자**가 있었다.
78 · 104 · 68… 그 화면은 「들리는 느낌」으로 말하기로 해 놓고 여기만 정체불명이었다.
「빠르기」를 작게 붙였다.

그리고 같은 것을 씬 화면은 **「템포」**라고 부르고 있었다.
한 손잡이에 두 이름이 붙으면 초보는 다른 것인 줄 안다. 「빠르기 92 BPM」으로 맞췄다
(값의 BPM 은 남긴다 — 아는 사람에게는 그게 기준점이다).
`scene_ui_test 12)` 가 「템포」가 다시 안 나오는지 지킨다.

## CPU% 가 답을 다시 확인해 줬다

같은 시험을 두 번 돌렸다:
```
16:16 total   17% cpu   user 160.17s   ← 기계가 멈춘 날
 1:15 total  225% cpu   user 158.92s   ← 멀쩡한 날
```
**user 시간은 똑같다.** 벽시계만 13배다. 코드가 느려지면 user 가 늘지 벽시계만
늘지 않는다 — 이 한 줄이면 1초 만에 갈린다.

## 잘못된 메시지 한 통이 소리를 영영 끌 수 있었다

오디오는 별도 아이솔레이트에서 돈다. `Isolate.spawn` 은 기본이
**`errorsAreFatal: true`** 라, 메시지 처리에서 예외가 하나 새면
**그 아이솔레이트가 통째로 죽는다** — 앱은 멀쩡히 살아 있는데 그때부터 소리만
안 난다. 오류 창도 안 뜬다. 사용자에게는 「갑자기 소리가 안 남」으로만 보인다.

메시지 규약은 계속 는다(이번 세션에만 `_cBus` 에 넷을 더했다). 그래서
**일부러 망가뜨린 열네 통**을 던져 봤다. 지금 코드로는 그중 **열둘이 아이솔레이트를
죽인다**(음성 검증으로 확인). 안 죽는 둘은 「모르는 번호」뿐이다 —
switch 가 그냥 지나가서.
```
빈 목록 · 칸이 모자람 · 형이 다름 · 버스 이름이 숫자 · 버스 값이 문자 ·
루프에 목록 아님 · 슬롯이 목록 아님 · 마스터가 짧음 · 인서트가 이상함 ·
앞당김이 문자 · 장르가 숫자 · null 만
```
처리기를 `handleAudioMessage()` 로 꺼내 **try/catch 로 감쌌다.** 한 통이 잘못
왔으면 그 한 통만 버린다. `isolate_msg_test` 가 열네 가지를 던지고, **그 뒤에도
멀쩡히 도는지**까지 본다(안 던지는 것만 보면 반쪽이다 — 죽었어도 안 던진다).

### 꺼내면서 걸린 것
진단 계수기 다섯(`maxGapMs`·`lateTicks`…)이 급식 루프의 **지역 변수**라 같이 못
따라온다. 통을 하나 만들어 묶는 건 급식 루프의 뜨거운 자리를 건드리는 일이라
(§15) **콜백 하나만 넘겼다** — `_cReset` 이 그걸 부른다. 인자 하나로 끝났다.

### 규약이 어긋났는지는 셀 수 있다
보내는 쪽 `_tx.send([_cXxx, ...])` 의 길이와 받는 쪽 `m[i]` 의 최대 index 를
맞춰 봤다. **19가지 메시지 전부 맞았다**(`_cMaster` 만 안 맞아 보였는데 그건
마지막 쉼표를 세어 버린 내 계산 실수였다). 지금은 맞다는 것을 확인해 뒀으니,
앞으로 어긋나도 **죽지는 않는다**.

## 조용히 실패하던 세 자리 — 소리·저장·소리 장치

이번 묶음은 하나의 규칙이다: **실패를 삼키지 않는다.**

### ① 저장 실패를 로그에만 찍고 있었다
```dart
} catch (e) { debugPrint('저장 실패: $e'); }   // 그게 전부였다
```
저장 공간이 꽉 찼거나 권한이 사라지면 그때부터 **아무것도 안 남는데**,
사용자는 **앱을 껐다 켠 뒤에야** 안다. 그때는 이미 늦었다.

쓰기를 `_write()` 한 곳으로 모으고 실패를 센다(`saveFails`·`lastSaveError`).
뿌리(`main.dart`)가 그걸 듣고 **어느 화면에 있든** 빨간 스낵바를 띄운다.
다시 써지면 스스로 0 으로 돌아가고 「다시 저장되고 있습니다」를 띄운다.
실패할 때마다 띄우지는 않는다 — 그건 알림이 아니라 방해다.
시험은 `index.json` 자리에 **폴더를 놔서** 쓰기를 막고 확인한다.

### ② 소리 장치를 못 열면 동그라미만 영영 돌았다
`AudioClient.start()` 에 try/catch 가 없었다. 여기서 터지면 화면은 멀쩡히
움직이는데 ▶를 눌러도 아무 일이 안 난다. 제목 옆 동그라미만 계속 돈다 —
**「앱이 고장 났다」로만 보이고 무엇이 문제인지 알 길이 없다.**
「소리 장치를 못 열었습니다 — 앱을 다시 켜 보세요」 한 줄로 바꿨다.

### ③ (앞 항목) 오디오 메시지 한 통이 소리를 영영 껐다
같은 규칙의 제일 심한 경우였다. 열넷 중 열둘이 아이솔레이트를 죽였다.

> 셋 다 **오류는 안 났다.** 로그에만 찍히거나, 동그라미만 돌거나, 그냥 조용해졌다.
> 「오류가 안 난다」가 곧 「괜찮다」가 아니다 — 이 프로젝트에서 제일 많이 나온 병이다.

## 가스펠·디스코의 코러스가 안 올라가고 있었다

숫자로 볼 수 있는 몇 안 되는 음악적 성질이 있다: **코러스가 벌스보다 커지는가.**
「코러스에 왔다」고 느껴지는 이유의 절반은 음이 올라가는 것이다 — 크기만 커지면
그냥 시끄럽다. `lift_meter` 를 만들어 15개 스타일을 다 쟀다.

```
가스펠   벌스 10.8/14 → 코러스 10.5/14   −0.3   ← 오히려 내려간다
디스코   벌스 10.8/14 → 코러스 11.6/14   +0.8
팝      벌스  4.6/7  → 코러스  7.0/13   +2.4 · +6 · +61음 · +1층
```
가스펠 보컬은 곡 내내 **C5~C6 한 옥타브 안**에만 있었다. 처음부터 끝까지 높은 데서
소리치는 꼴이고, 코러스에 와도 **더 갈 데가 없었다.** 디스코 브라스도 같았다.

둘 다 **벌스를 한 옥타브 내렸다**. 정확히 7도만 내리는 게 핵심이다 —
음이름이 같아서 코드와의 관계가 하나도 안 바뀐다.
```
가스펠  벌스 C4~C5 · 코러스 C5~C6   +6.8 · +7
디스코  벌스  4.3   · 코러스 11.6    +7.3 · +5
```

### 디스코는 그냥 내리면 부딪혔다
4마디 첫 긴 음(도수 4 = G)이 그 아래 Bb7 의 Ab 와 **반음**이 됐다.
위 옥타브였을 땐 단9도(13반음)라 색으로 들리던 것이 옆자리로 내려온 것이다.
그 한 음만 도수 5(Ab, Bb7 의 7도)로 옮겼다.
> **음역을 옮기면 화성이 따라 바뀐다.** 음이름이 같아도 **거리**가 달라진다.

### 커지는 길은 셋이다
`melody_check_test 5)` 는 **높아지거나 · 빽빽해지거나 · 층이 늘거나** 중
하나만 있으면 통과시킨다. 트랩의 훅은 벨을 안 올리고 **플럭 한 층을 얹어서** 커진다 —
도수만 보면 「안 커진다」로 잡히는데 그건 틀린 판정이다.

## 「모든 트랙이 정말 우는가」를 시험으로 못 박았다

이 프로젝트에서 제일 오래 안 잡힌 고장이 다 이 모양이었다 —
셰이커 2361타 무음 · 보컬이 삼각파 · 스트링이 편성에 없음. 셋 다 「소리는 나는 것
같은데」로 보여서 귀로도 잘 안 잡혔다.

`track_alive_test` 가 **트랙 하나만 남기고 렌더**해서 정말 우는지 본다.
15개 스타일 **76개 트랙** 전부 통과. 팝 기타의 볼륨을 0 으로 만들어
`pop/기타` 를 정확히 집어내는 것도 확인했다.

### 재는 창을 잘못 잡아 또 속을 뻔했다
「엠비언트 스트링2 −120dB(완전 무음)」이 나왔다. **아니었다** — 그 트랙은
213초짜리 곡의 **161초**에 처음 우는데 내가 앞 90초만 재고 있었다.
그리고 곡 전체로 나누면, 다섯 구간 중 둘에만 나오는 악기는 그것만으로 12dB 가
깎여 「안 들린다」로 보인다(엠비언트 하프 −32 → 제 구간만 재니 −29).
**그 트랙이 우는 동안만** 재야 맞다.

> 「0 이 나오면 내가 잘못 쟀는지부터 의심한다」를 적어 놓고 또 그랬다.
> 이번엔 적는 김에 **왜 틀렸는지의 모양**까지 적어 둔다 —
> ①창이 짧았다 ②안 우는 시간까지 나눴다. 다음엔 이 둘부터 본다.

## 박수가 스네어보다 13dB 아래였다

`drum_meter` 를 훑다가 걸렸다: 스네어 피크 0.89, 박수 0.19 — **−13.4dB**.
그런데 **박수만 뒷박을 치는 판이 여덟**이다:
```
House Chorus Plain · House Break · City Break · Clap Beat ·
Perc Layer · Prog Groove · Jazz B · Gospel Break
```
그 판들에서는 뒷박이 거의 안 들렸다. 오류가 나는 종류가 아니라 「좀 심심하네」로만
느껴지는 자리다.

2배로 올려 **−7.6dB** 로 맞췄다. 스네어 위에 겹치는 판(트랩·팝·디스코·가스펠)에서는
여전히 겹이지 주인공이 아니다. 크기 편차 4.0dB·클리핑 0 은 그대로다
(박수는 전체 에너지에서 작아서 총량은 안 움직인다 — 바뀌는 건 **들리느냐**다).
`drum_check_test 9-c` 가 **−10.5 ~ −3dB** 밖으로 나가면 잡는다. 위아래 둘 다 막는
이유: 너무 커지면 겹이 아니라 또 하나의 스네어가 된다.

## 계기판 둘이 거짓말을 하고 있었다

- `bass_meter` 의 **근음%**: 코드가 아예 없는 씬(하우스 아웃트로·힙합 브레이크)에서
  0% 로 찍혔다. 근음을 안 짚은 게 아니라 **짚을 코드가 없다.** 「—」로 바꿨다.
- `groove_meter` 의 **백비트%**: 스네어만 셌다. 하우스·프로그·디스코·가스펠은
  뒷박을 **박수**로 친다 — 「하우스 백비트 25%」를 보고 「뒷박이 없다」고 읽을 뻔했다.
  박수를 같이 센다(하우스 코러스 25% → 64%, 프로그 그루브 — → 100%).

> 계기판이 틀리면 **없는 병을 고치고 있는 병은 놓친다.** 고칠 때마다 왜 틀렸는지
> 한 줄 남긴다 — 다음에 그 숫자를 보는 사람은 나일 확률이 높다.

## 곡을 여러 개 놓고 막 써 봤다 — 2000걸음

`rough_use_test` 는 프로젝트 하나 안에서만 논다. 실제로는 곡을 만들고 · 복제하고 ·
지우고 · 되돌리고 · 오가면서 그 사이에 자동 저장이 돈다.
`store_rough_test` 가 그걸 흔든다. 매 걸음마다 다섯을 본다:
목록↔파일이 맞는가 · 열린 곡이 목록에 있는가 · 저장이 실패하고 있는가 ·
소리를 만들 수 있는가 · **다시 열었을 때 내용이 그대로인가.**
그리고 마지막에 **앱을 껐다 켠 것처럼** 다시 읽어 목록·열린 곡·모든 곡이 열리는지 ·
고아 파일이 없는지까지 본다.

지금은 깨끗하다(4800걸음까지 돌려 봤다). 「내용이 그대로인가」는 저장된 템포 복원을
지워 보고 **한 걸음 만에 잡히는 것**을 확인했다.

## 안내에 「느낌」이 없었다

악보를 모르는 사람이 음악을 바꾸는 **제일 쉬운 길**인데 사용법 어디에도 없었다.
있는 줄 모르면 없는 것이다. 「만든 것은 저절로 저장돼요」도 같이 넣었다 —
처음 쓰는 사람이 제일 먼저 궁금해하는 둘이다.

## 한 가지를 세 이름으로 부르고 있었다 — 울림·잔향·리버브

화면에 실제로 보이는 문자열 155가지를 뽑아 「같은 뜻 다른 말」을 찾아봤다.
```
라이브 화면   「울림」
믹서         「잔향」·「잔향 (울림)」·「좌우·잔향·EQ」
```
같은 손잡이(리버브 보내기)인데 화면마다 이름이 달랐다. 빠르기/템포와 같은 병이다.
전부 **「울림」**으로 맞췄다.

**「리버브」는 그대로 뒀다** — 그건 보내기가 아니라 **인서트 플러그인의 이름**이다.
다른 물건이니 다른 이름이 맞다. (「리버브 종류/양」은 엔진 시험 화면에만 있다.)

같이 나온 것: **지우기 / 삭제**. 뜻을 갈랐다 —
**지우기 = 비운다**(편집기의 판 비우기) · **삭제 = 없앤다**(곡·씬·트랙).
그래서 「이 트랙 지우기」 → **「이 트랙 삭제」**.
`mixer_ui_test 5-e2` 가 「잔향」이 다시 나오면 잡는다.

> 낱말은 코드처럼 리팩터링되지 않는다. **한 번에 하나씩 늘어난다** —
> 화면을 새로 만들 때마다 그 자리에서 가장 자연스러운 말을 고르기 때문이다.
> 가끔 전부 뽑아서 나란히 놓고 봐야 보인다(이번엔 155개를 뽑았다).

## 시험 출력에서 일부러 낸 오류 로그를 막았다

`isolate_msg_test` 는 일부러 메시지를 망가뜨리므로 「처리 실패」가 열넷씩 찍혔다 —
전체 시험 출력이 안 읽힌다. `debugPrint` 를 잠깐 갈아 끼워 담아 두고,
**로그에 남기는지까지 시험으로 확인한다**(5번). 삼키기만 하면 다음 사람이
왜 이상한지 알 길이 없다.

## 필이 그 장르에 없는 악기로 치고 있었다

`applyFill` 은 무조건 **스네어 → 탐**이었다. 그런데 엠비언트의 타악기 판
(`Amb Perc`)에는 **크래시·림·셰이커뿐**이다 — 곡 어디에도 스네어도 탐도 없는데
필에서만 나왔다. 느린 엠비언트에 16분 스네어 굴림이 들어간 셈이다.

```
엠비언트 타격(고치기 전): crash 5 · shaker 24 · rim 8 · snare 4 · tom 4
                (고친 뒤): crash 5 · shaker 24 · rim 8
```

**그 구간이 이미 쓰는 악기 중에서** 굴릴 것과 떨어뜨릴 것을 고른다
(`_fillLanes`). 칠 것이 하나도 없으면 필도 없다.
15개 스타일을 다시 재 보니 판에 없던 것이 더해지는 경우는 **크래시뿐**이다
— 그건 「매듭」 신호라 일부러 남긴다(로파이·힙합·재즈).

> 그 장르에 없는 악기가 매듭에서만 튀어나오면 **그건 매듭이 아니라 사고다.**

### 시험 픽스처가 현실과 달랐다
`feel_check_test` 의 필 픽스처는 **킥·하이햇뿐**이었다. 그래서 「스네어가 없는 판」이
됐고 필이 하이햇으로 굴러 시험이 넷 깨졌다. 픽스처에 뒷박 스네어·탐을 넣어
실제 드럼 판처럼 고쳤다. 그러면서 「필 자리」 판정도 고쳐야 했다 —
원래 있던 뒷박 스네어(12칸)와 필(12~15칸)이 겹치므로 **13칸부터**만 센다.

> 픽스처가 현실보다 단순하면 **시험은 통과하는데 앱은 틀린다.**
> 이번엔 반대로 나왔다 — 앱을 고쳤더니 시험이 깨졌고, 깨진 쪽이 픽스처였다.

## 글자를 키운 폰에서는 화면이 넘치고 있었다

안드로이드 「설정 → 디스플레이 → 글자 크기와 스타일」은 글자를 **1.3배**까지 키운다.
눈이 나쁜 사람은 대개 이걸 켜 놓고 산다. 그런데 이 앱은 **그 상태로 한 번도
그려 본 적이 없었다** — 시험 열둘이 전부 1.0배다.

`text_scale_test.dart` 로 14개 화면을 세로·가로 × 1.0·1.3배로 그려 봤다.
1.0배는 다 멀쩡한데 **1.3배 세로에서 네 화면이 넘쳤다**:

```
씬       칩 셋이 가로로 41px 넘침 · 트랙 줄(64) 안에서 두 줄이 넘침
곡       구간 줄(58) 안에서 「인트로 / 0:25 · 78BPM」 두 줄이 10px 넘침
믹서     「울림」+값이 100dp 칸을 2.9px 넘침
시작하기  장르 카드 비율(5.4)이 고정이라 설명 두 줄이 6px 넘침
```

폰에서는 노랑·검정 빗금이 그어지거나 글자가 그냥 잘린다.

**고친 방법 — 못 박은 크기를 글자 배율만큼 늘린다** (`lib/ui/text_scale.dart`).
안드로이드 14부터 배율이 곧지 않아서(큰 글자일수록 덜 키운다) **본문 크기(14)로 재야**
실제로 커지는 만큼 나온다. 끝없이 늘리면 화면에 아무것도 안 들어오니 1.6배에서 멈춘다.

가로 넘침은 크기로는 못 고친다. 칩 줄은 `Row` → **`Wrap`** 으로 바꿨다 —
`Flexible` 로 억지로 줄였더니 이번엔 칩 **안쪽**이 넘쳤다(1.0배에서도).
자리가 모자라면 다음 줄로 내려가는 게 맞다.

> **화면이 넘치는지는 눈이 아니라 예외로 잡을 수 있다.** 그래서 시험이 된다.
> 다만 한 프레임에 여러 군데가 넘치면 플러터가 「Multiple exceptions (10)」 한 줄로
> 뭉쳐서 **어디가 넘쳤는지를 감춘다.** `FlutterError.onError` 를 가로채 모아 보려다
> 시험이 멎어서, 한 군데씩 고치고 다시 돌리는 쪽으로 갔다.

시험 도구 하나가 낡아 있었다 — `screen_snap` 이 아직 「잔향 (울림)」을 찾고 있었다
(이름을 「울림」으로 맞춘 뒤로). `_test.dart` 가 아니라 `flutter test` 가 안 돌린다.

### 작은 폰(320dp)도 같이 봤더니 — 1.0배에서도 넘쳤다

이 앱이 네이티브로 옮겨진 이유가 **보급형 폰**이다. 그런데 시험은 전부 400dp 였다.
320dp 를 넣자마자 **글자를 안 키운 상태에서도** 「시작하기」가 넘쳤다.

까닭이 재미있다. 장르 카드는 `childAspectRatio` 로 크기를 정하고 있었다 —
그건 **「높이 = 폭 ÷ 비율」** 이라 **폭이 좁은 폰일수록 카드가 낮아진다.**
그런데 안에 들어갈 두 줄짜리 설명은 그대로다. 폭이 줄면 오히려 더 높아야 하는데
반대로 낮아진 것이다. `mainAxisExtent`(높이를 직접 준다)로 바꿨다.

> **비율로 크기를 정하면 좁은 화면에서 거꾸로 간다.** 글이 들어가는 칸은 높이를 준다.

같이 나온 것:
```
첫 화면  「음악 낙서장」+버튼들이 6.2px 넘침 → 제목이 줄어든다(버튼은 눌러야 한다)
쇼      「0:12 / 2:15」가 42px 넘침 → 줄임표 대신 FittedBox 로 **줄여서** 보여 준다
곡      「0:25 · 78BPM」에 maxLines 가 없어 좁은 폰에서 두 줄로 접혔다
```

## 트랙을 지우면 되돌릴 수 없었다

곡([Store.remove])도 씬([removeScene])도 되돌아오는데 **트랙만 안 됐다.**
그런데 트랙 하나에는 **모든 씬의 클립**이 달려 있다 — 한 번 누르면 곡 전체에서
그 악기가 사라진다. 이 앱에서 한 손짓으로 제일 많이 잃는 자리다.
여태 대화상자에 「되돌릴 수 없어요」라고 **적어 두는 것으로 때웠다.**

> 경고는 되돌리기의 대신이 아니다.

`RemovedTrack` 을 들고 나온다(트랙 · 자리 · 씬마다의 클립). 씬은 **번호가 아니라
객체로** 붙잡는다 — 되돌리기 전에 씬이 지워지거나 순서가 바뀌면 엉뚱한 씬에
클립이 돌아간다. `removeTrack` 에서 `dispose()` 를 뺐고, 알림이 사라질 때
`RemovedTrack.drop()` 이 치운다.

## 내보낸 파일이 전부 같은 이름이었다

`음악낙서장_팝.wav` — **장르만** 썼다. 팝으로 만든 곡 셋이 전부 같은 이름이고,
같은 이름끼리 덮어쓴다. 곡에 이름을 붙이는 자리를 만들어 놓고 정작 파일에는
안 쓰고 있었다. 이제 곡 이름을 쓴다(`exportStem`) — 파일 이름에 못 쓰는 글자
(`/ \ : * ? " < > |` · 제어문자 · 앞뒤 점)를 걷어 내고, 걷어 내니 남는 게 없으면
그때만 장르로 돌아간다.

### 긴 이름으로도 훑었다 — 「구간 붙이기」가 화면을 밀어냈다

곡·씬·트랙 이름은 **사용자가 적는다.** 「나무」로만 재 보고 넘어가면 실제로 긴 이름을
적은 사람 화면에서만 깨진다. 말도 안 되게 긴 이름을 넣고 다섯 화면을 다시 그렸다.

곡 화면 아래 **「＋구간 붙이기」** 줄이 걸렸다. 씬 이름 칩을 `Wrap` 으로 늘어놓는데,
이 줄은 **화면 아래에 못 박혀 있다** — 이름이 길거나 씬이 많으면 `Wrap` 이 끝없이
높아져서 위쪽 구간 목록을 밀어내고 화면을 넘긴다(가로에서 13px, 1.3배에서 43px).

씬을 복제해 열두 개쯤 만들면 **긴 이름이 아니어도** 같은 일이 난다.
칩 폭을 150 으로 묶고(잘려도 색과 자리로 안다), 줄 전체를 두어 줄에서 멈춰 굴린다.

> 아래에 못 박은 것은 **자라면 안 된다.** 자랄 수 있으면 천장을 줘야 한다.

`text_scale_test.dart` 는 이제 **19가지 × 6(세로·가로·작은폰 × 1.0·1.3배)** 를 그린다.

## 스타일 바꾸기 — 한 번에 제일 많이 바꾸면서 되돌릴 길이 없었다

`setGenre` 는 씬·트랙·클립·믹서·인서트·구간표를 통째로 간다(객체형 장르는
**편성까지**). 그런데 스타일 칩은 곡 화면에서 옆으로 흐르는 줄에 있다 —
스크롤하다 잘못 눌리기 쉬운 자리다.

트랙 하나를 지울 때는 확인 창을 띄우면서, **곡 전체가 갈리는 여기에는 아무것도
없었다.** 「손으로 고친 판 3개는 그대로 뒀습니다」라고 말은 해 주는데, 그 말은
**무엇이 일어났는지**를 알려 줄 뿐 **되돌려 주지는 않는다.**

`GenreUndo` — 새 길을 내지 않고 **이미 잘 도는 길**을 그대로 쓴다:
`project.toJson()` 으로 뜨고 `loadJson()` 으로 되돌린다. 곡을 저장하고 여는 바로
그 길이라, 여기서만 빠뜨리는 값이 생기지 않는다(템포·조는 `Transport` 것이라 따로).

알림도 바꿨다 — 예전엔 **고친 판이 있을 때만** 떴다. 이제 늘 뜬다:
「스타일을 하우스로 바꿨습니다 · 되돌리기」. 아무 말도 없이 곡이 바뀌는 것보다는
한 줄 뜨는 편이 낫다(같은 스타일을 다시 고르면 아무 일도 안 한다).

> 「무엇이 일어났는지 알려 주기」와 「되돌릴 수 있게 하기」는 다른 일이다.
> 앞의 것만 있으면 사용자는 **잃은 것을 정확히 알면서 못 되찾는다.**

## 누를 것이 손가락만 하지 않았다

폰에서 「두 번 눌렀는데 안 먹었다」는 대개 기능이 아니라 **크기** 문제다.
머티리얼은 48dp, 애플은 44pt 를 최소로 잡는다. 열두 화면의 누를 것을 전부 재 봤다.

```
씬 칩의 ⋮        18×15   ← 3mm 남짓. 이만한 것을 두 번 세 번 눌러야 하면 있으나 마나다
곡 「2판」        38×19   ← 눌러서 1·2·4·8 을 고르는 자리
믹서 「한눈에」    70×26
믹서 M / S       29×34   ← 뜻이 반대인 짝(안 들리게 / 이것만 들리게)이 붙어 있다
마스터 「0dB」    64×30
＋악기 넷        71×32
라이브 칩들      53×32   ← 연주 중에 누르는 것들이다
```

**고치는 방법이 두 가지다.** 보이는 것을 키우거나(칩·버튼), **보이는 건 그대로 두고
눌리는 자리만** 넓히거나(`HitTestBehavior.opaque` + 여백). 줄 높이가 이미 넉넉한
자리(곡의 −/+, 씬 칩의 ⋮)는 뒤쪽이 낫다 — 화면이 복잡해 보이지 않는다.

지금은 **세로 32 · 가로 28** 아래가 없다(`tap_size_test.dart` 가 지킨다).
가로 28 인 까닭은 편집기 격자 칸이 30 이라서다 — 16칸이 화면 폭을 나눠 가지니
그건 어쩔 수 없고, 대신 세로가 35~46 이라 손가락이 닿는다.

> 크기는 **재 보기 전에는 안 보인다.** 화면을 눈으로 보면 「작은 점 셋」은
> 작아 보이는 게 당연해서 흠으로 안 읽힌다. 숫자로 뽑아야 18×15 가 눈에 들어온다.

## 저장하다 앱이 죽으면 곡이 깨졌다

`writeAsString` 은 **먼저 파일을 비우고** 새 내용을 채운다. 그 사이에 앱이 죽으면
(안드로이드는 메모리가 모자라면 뒤에 있는 앱을 그냥 죽인다) 또는 사용자가 최근앱에서
밀어 버리면 **반만 쓰인 JSON** 이 남는다. 그 파일은 못 읽는다 — 곡이 통째로 날아간다.

이 앱은 만지는 족족 저장한다(`_debounce`). 그러니 「쓰는 중」인 시간이 길고,
하필 그때 죽을 확률도 그만큼 높다. 그리고 **`index.json` 이 깨지면 곡 전부가**
안 보인다 — 파일은 멀쩡한데 목록이 가리키질 못한다.

이제 임시 파일에 다 쓰고 `rename` 으로 제자리에 넣는다. 같은 저장소 안의 rename 은
**한 번에 일어난다**(중간이 없다) — 죽어도 남는 건 옛 파일 아니면 새 파일이고,
둘 다 온전하다.

> `bad_file_test` 는 「망가진 파일이 앱을 못 죽인다」를 지킨다.
> 이건 애초에 **망가진 파일이 생기지 않게** 한다. 둘 다 필요하다.

### 그러다 더 큰 것이 나왔다 — 실패 경고가 지워지고 있었다

시험을 쓰다 보니 「곡 저장이 실패했는데 `saveFails` 가 0」이었다.
저장 한 판은 파일을 **둘** 쓴다 — 곡 파일과 목록. 그런데 `_write` 가 **성공할 때마다**
`saveFails` 를 0 으로 되돌리고 있었다. 곡 저장이 실패해도 뒤이은 목록 저장이 성공하면
**그 자리에서 경고가 지워졌다.**

하필 제일 중요한 실패(곡이 안 남았다)가 안 보이고 덜 중요한 실패(목록)만 보였다.
이제 판이 끝날 때(`_writeIndex` 끝) **한 번만** 셈한다.

> 「알림을 만들었다」와 「알림이 뜬다」는 다른 일이다. 알림을 **꺼뜨리는 길**이
> 같은 코드 안에 있었다. 알림은 만든 뒤에 **일부러 실패시켜 봐야** 확인된다.

## 죽은 코드 — lib 안에서 선언 말고는 안 쓰이는 것

이름을 전부 뽑아 참조 수를 세 봤다(13가지). 진짜 죽은 것 넷:
```
project.dart      MixSettings · AppSettings   ← 웹에서 옮겨 오다 남은 클래스
project.dart      moveTrack                   ← 트랙 순서 바꾸기. 화면이 없다
audio_isolate.dart setQuality                 ← 엔진에 highQuality 스위치가 있는데
                                                 아무도 안 켜고 끈다(늘 high)
presets.dart      presetPatternFor
```
나머지(`harmonyFit`·`setLevel`·`glideOn`·`packOf`·`isKnownGenre`·`buildSongPlain`)는
**시험에서만** 쓴다 — 재는 도구라 그게 맞다.

**지금은 안 지웠다.** `setQuality` 는 더 약한 폰을 만났을 때 쓸 손잡이고,
`moveTrack` 은 화면만 붙이면 되는 것이라 지우면 다시 만들어야 한다.
다만 **「있으니까 화면에 내놓는다」는 안 한다**(§15) — 필요해질 때 붙인다.

## 앱을 뒤로 보내도 CPU 가 안 자고 있었다 (폰에서 잡았다)

APK 를 올리고 폰에서 확인하다 우연히 봤다:

```
앱을 켜고 홈으로 나간 뒤 2분
  PARTIAL_WAKE_LOCK 'AudioMix' ACQ=-2m4s  ws=WorkSource{10357 com.musicdoodle...}
```

**아무 소리도 안 나는데** 소리 장치가 열린 채라, 안드로이드가 우리 앱 몫으로
`AudioMix` 잠금을 잡고 있었다 — 화면을 끄고 다른 앱을 써도 CPU 가 안 잔다.
배터리가 닳는 앱은 그 이유만으로 지워진다.

까닭은 간단했다. **이 앱에는 생명주기 처리가 한 줄도 없었다**
(`WidgetsBindingObserver` 를 아무도 안 달았다). 시작할 때 장치를 열고 **끝까지 안 놓는다.**

고친 방법: 뒤로 가면 장치를 놓고(`_cSleep` → `FlutterPcmSound.release()`),
돌아오면 다시 연다(`_cWake` → `setup` + 미리 채우기 + `start`).
**소리가 나는 중이면 안 놓는다** — 듣다가 화면을 나가는 사람이 있다.

**깰 때 시계를 되돌리는 게 핵심이다.** 급식량은 「흐른 시간 × 48000 − 먹인 양」으로
재는데, 자는 동안에도 `Stopwatch` 는 흐른다. 그대로 두면 깨는 순간 「몇 분 치가
밀렸다」로 읽혀서 한꺼번에 쏟아붓는다. `clock`·`fed`·`driftFix`·`lastSync` 를 전부 0 으로.

### 폰에서 확인한 순서 (다시 재려면 이대로)
```
adb -s RFKL505TRAZ shell dumpsys power | grep -A3 "Wake Locks: size"
  앱 켠 채   → size=2  ('AudioMix' 잡힘)
  홈으로     → size=0  ← 고치기 전에는 계속 2 였다
  다시 켜면  → size=1  (장치 다시 열림)
  재생하면   → 「2마디 4박」로 재생 머리가 나아간다 · audio_flinger 에 48000Hz 트랙 활성
  정지+홈    → size=0
```

> **켜 놓고 나간 뒤를 아무도 안 봤다.** 화면 시험도, 소리 시험도 전부 「앱을 쓰는
> 동안」만 본다. 안 쓰는 동안 무엇을 붙잡고 있는지는 **폰에 물어봐야** 안다.

## 엔진 파일을 훑어 버그 열둘을 잡았다 (2026-09-04)

아직 안 읽은 lib 파일들(edit_ops·packs·presets / fx·genre_fx / mixer·genre_mix /
feel·dsp / synth·engine / song·sequencer / project)에 **찾는 에이전트 일곱**을 붙이고,
나온 후보를 **하나씩 다른 에이전트가 회의적으로 재검증**하게 했다. 후보 13건 중
12건이 실전 경로까지 확인됐고, 1건은 기각됐다(재즈 스윙 — `applyFeel` 이 이미 한다).

### ① 편집기를 여는 순간 반복 마디가 사라졌다 — 제일 큰 것

라이브러리 패턴은 `bars > src` 인 것이 많다(1마디를 두 번 돌려 2마디를 채운다).
재생할 때는 `buildDrumPattern`/`tileNoteList` 가 그 자리에서 펼친다.

그런데 `makeEditable`(라이브러리 판 → 내 판 복사)이 원본을 **그대로 복사해 놓고**
`src=bars` 라고만 적었다 — 「이미 펼쳐진 걸로 친다」는 뜻인데 실제로는 첫 마디만
있었다. 편집기를 **열기만 해도** 뒷마디가 조용해지고, 한 번이라도 저장하면
(편집기 저장도 늘 `src=bars`) 그 반복이 영영 사라졌다.

```
Boom Bap (2마디, 1마디 원본): 킥 0·7·10·16·23·26
  편집기를 열면                킥 0·7·10          ← 둘째 마디 통째로 사라짐
```

드럼 17개·음정 22개 패턴이 이 모양이다. 이제 **진짜 펼치고 나서** `src=bars` 를
적는다(`tileDrumHits` 를 새로 만들고 `tileNoteList` 를 공개했다 — 펼치는 코드가
한 곳에 있으면 이 어긋남이 다시 못 생긴다).

> 주석이 「이미 펼쳐진 길이」라고 말하는데 **펼치는 코드가 어디에도 없었다.**
> 주석은 약속이지 구현이 아니다.

### ② 스타일을 바꿔도 앞 장르의 필터가 남았다

드럼 버스는 `TrackMixSet.configure` 가 **절대 새로 안 만드는** 자리다(드럼은 항상
고정 버스 하나). 그런데 `_apply` 가 `s.hpf == null` 이면 값을 안 건드리고 넘어갔다 —
hpf 를 거는 장르(트랩 30Hz)를 듣다가 안 거는 장르(로파이)로 가면 그 30Hz 가
**그대로 남았다.** 같은 로파이인데 **방금 뭘 틀었느냐에 따라 다르게** 들렸다.

이제 null 이면 투명한 기본값(20 / 20000)으로 **되돌린다**.

> 「값이 없으면 넘어간다」와 「값이 없으면 기본값으로 돌린다」는 다른 일이다.
> 살아남는 객체에 얹을 때는 **항상 뒤엣것**이 맞다.

### ③ 카우벨은 느낌 손잡이를 하나도 못 받고 있었다

`feel.dart` 의 잔가지 목록이 `{'hat','ride','shaker','shake'}` 였다.
`'shake'` 는 **소리 낼 때만 쓰는 내부 별명**(`drums.dart` 의 `kDrumAlias`)이라
작곡 단계의 레인 이름과는 **한 번도 안 맞는 죽은 항목**이고, 정작 정식 이름
`'cowbell'` 이 빠져 있었다. 그래서 카우벨은 밀도를 올려도 안 쪼개지고 그루브를
켜도 뒷칸이 안 여려졌다 — 시티팝·디스코·라틴 판이 그 영향을 받는다.

같은 뜻의 목록이 `arrange.dart`·`variation.dart` 에는 **맞게** 적혀 있었다.
세 파일이 각자 들고 있으니 한 곳만 틀려도 아무도 모른다. `feel.dart` 안에서는
공유 상수 하나로 합쳤다.

### ④ 나머지 아홉

```
engine.dart  오른쪽 채널만 넘치면 하드클립을 안 셌다(왼쪽만 셌다)
             → 진단 숫자가 실제보다 적게 나온다. 계기가 거짓말하면 없는 병을 고친다
engine.dart  드럼 목소리가 바닥나면 **킥의 사이드체인도 걸리지 않았다**
             — 바로 위 주석이 「목소리를 못 얻어도 비켜 주는 것은 한다」인데
               그 줄이 `return` 뒤에 있었다. 붐비는 트랩 구간에서 실제로 난다
sequencer    베이스에서 만든 패턴이 코드·멜로디 고르기에도 떴다(한 표를 같이 쓴다).
             고르면 오류는 없고 소리만 틀렸다 → 만든 타입을 기억한다(`userNoteType`).
             옛 곡(타입 모름)은 예전처럼 다 통과시킨다
project      씬을 지울 때 **보던 씬보다 앞이면** 번호를 안 당겼다 → 지운 뒤
             엉뚱한 씬을 보고 있었다(`duplicateScene` 에는 반대쪽 짝이 있었다)
project      같은 타입 트랙이 둘이면(+악기로 베이스를 두 번 넣으면) 뒤엣것만
             남아서 → 스타일을 바꿔도 첫 베이스는 믹스가 안 갱신되고,
             씬에 남긴 사용자 패턴 하나가 조용히 사라졌다
dsp.dart     보급형 폰용으로 심벌 6개를 한 파형에 구울 때 **둘씩 같은 칸에 뭉갰다**
             (6→4칸, 살아남은 둘이 정확히 옥타브 — 「비조화라야 심벌」의 정반대).
             기본음을 1/5 로 낮춰 칸을 촘촘하게: 6/6칸, 오차 588→55센트
song.dart    드릴 훅의 마무리(하이햇 16분 뭉치)가 곡에서 **세 번 다** 돌았다.
             민짜 짝(`Drill Hook Plain`)을 만들어 넣었다
```

**R&B 코러스는 일부러 안 넣었다.** 에이전트가 같이 지목했는데, 마디별로 갈라 보니
네 마디가 한 글자도 안 다르다 — 뺄 필인이 아예 없다. 민짜를 만들면 원본과 똑같은
판이 하나 더 생길 뿐이다.

### 필인을 찾는 자도 같이 고쳤다

`genre_check_test` 5-e 가 「끝마디에 **탐·크래시**가 몰렸나」로 재고 있어서 드릴을
놓쳤다 — 드릴은 필인이 **하이햇**이고 탐이 아예 없다. 이제 레인을 안 가리고
**「끝마디가 이 판 자신의 되풀이 주기를 깨는가」** 로 잰다.

두 가지를 조심해야 했다:
- **레인마다 따로** 재야 한다. 한꺼번에 재면 구간 첫 박 크래시(0마디에만 있는
  문패)가 주기 계산을 망친다 — 그것 때문에 처음엔 드릴을 그대로 놓쳤다.
- **코러스·훅·드롭에서만** 재야 한다. 빌드업·프리는 끝으로 갈수록 촘촘해지는 게
  그 구간의 목적이라 같은 자로 재면 전부 걸린다(실제로 넷이 걸렸다).

고친 자가 진짜 잡는지 확인했다 — `kPlainDrum` 에서 드릴 줄을 빼면 실패하고,
넣으면 통과한다.

> **자를 고치지 않으면 같은 자리가 또 빈다.** 프로그·팝·디스코·가스펠이
> 2026-08-29 에 같은 이유로 빠져 있었고, 그때 만든 자가 드릴을 또 놓쳤다.

### 덤: 생명주기 고침에 구멍이 하나 더 있었다

APK 를 올리고 폰에서 확인하다 발견했다. **화면이 꺼진 채로 앱이 시작되면**
소리 장치를 안 놓는다:

```
화면 꺼진 상태에서 앱 시작 → 10초 뒤
  PARTIAL_WAKE_LOCK 'AudioMix'  ← 앞에 있지도 않은데 잡고 있다
```

까닭은 **순서**다. 소리 장치를 여는 데 시간이 걸리는데(아이솔레이트를 띄우고
버퍼를 채운다), 그 사이에 온 생명주기 알림은 `host` 가 아직 null 이라
`didChangeAppLifecycleState` 가 그냥 돌아간다 — 뒤늦게 장치가 열려도
**잠들라고 말해 줄 사람이 이미 지나갔다.** 화면이 꺼진 채 시작하면 `resumed` 가
아예 한 번도 안 와서 영영 그 상태다.

다 열고 나서 **지금 상태를 한 번 되묻는다**(`WidgetsBinding.instance.lifecycleState`).
`resumed 가 아니다` 가 아니라 **`paused`·`detached`·`hidden` 일 때만** 재운다 —
`inactive` 는 지나가는 상태(알림창을 내린 정도)라 곧 정해지고 그때 콜백이 온다.
성급히 재우면 **앞에 있는 앱이 잠깐 조용해진다.**

폰에서 확인: 화면 꺼진 채 시작 → `AudioMix` 없음 · 크래시 없음.

> 콜백을 달아 두는 것과 **그 콜백이 놓친 것을 따라잡는 것**은 다른 일이다.
> 늦게 준비되는 자원은 준비된 순간에 **한 번 되물어야** 한다.

## lib 46개를 전부 `dart format` 에 맞췄다 (2026-09-04)

지난번에 `lib/ui/mixer_view.dart` 하나만 실수로 포맷을 돌려서, 그 파일만 줄바꿈
모양이 다른 상태였다. 46개 중 41개가 `dart format` 과 달랐다. 사용자가 나머지도
맞추라고 해서 전부 맞췄다.

포맷을 돌리자 `flutter analyze` 가 없던 경고를 하나 냈다 —
`main.dart` 의 `if (mounted) setState(...)` 가 블록 없이 여러 줄이 됐다.
중괄호를 씌웠다. 이제 `flutter analyze lib` 는 **무결점**이고
`dart format --set-exit-if-changed lib/` 는 0개 변경이다.

시험 62개 파일 전부 통과(77초). 포맷은 공백만 바꾸지만 `dart format` 은
trailing comma 를 넣고 빼기도 하므로 시험으로 확인하는 것이 맞다.

> 앞으로 lib 를 고치면 `dart format lib/` 를 같이 돌려라. 한 파일만 다른 모양이면
> 다음 사람이 그 파일을 볼 때마다 「왜 여기만 다르지」를 다시 묻는다.

## 코드 자리바꿈이 9화음에서 통째로 죽어 있었다 (2026-09-05)

`voiceLead` 는 코드를 옥타브씩 옮겨 가며 **밑음을 위로 넘기는** 자리바꿈을 전부
시험해 보고 앞 코드와 제일 가까운 것을 고른다. 그 넘기는 줄이 이랬다:

```dart
v = [...v.sublist(1), v.first + 12];   // 밑음을 한 옥타브 올린다
```

**올린 뒤 다시 정렬을 안 했다.** 옥타브보다 좁은 코드는 밑음을 12 올리면 그것이
맨 위가 되니까 우연히 맞았다. 그런데 9화음은 원래 폭이 14다:

```
min9  [60, 63, 67, 70, 74]
넘기면 [63, 67, 70, 74, 72]   ← 72 가 맨 뒤에 있다. 정렬이 깨졌다.
```

깨진 배열로 두 가지 검사가 헛돈다:
- 「반음 붙었나」가 **음수 간격**(74→72 = −2)을 보고 `gap <= 1` 로 읽어 벌점 8을 준다
- `v.first` 가 더 이상 맨 밑음이 아니라 음역 벌점도 엉뚱한 음을 본다

그래서 **넓은 코드는 자리바꿈이 하나도 못 살아남았다.** 라이브러리 전체를 재 보니
9화음 720개가 **100% 근음 자리**였다. 정렬 한 번 더 하는 것으로 62% 로 내려가고
코드가 바뀔 때 손이 움직이는 거리가 1.32 → 1.21 반음이 됐다.

> 회전시킨 뒤 정렬을 안 한 것은 「대개 맞으니까」 안 보이는 종류다.
> 불변식(**낮은음 → 높은음**)을 깨 놓고 그것에 기대는 검사를 두면 조용히 틀린다.

### 그러자 층(oct)이 무의미하다는 게 드러났다

`stack_check_test` 가 깨졌다 — 프로그하우스 그루브의 패드(층 0)와 스탭(층 1)이
79% 같은 음이었다. 되돌려서 재 보니 **옛 코드에서도 57%** 였고 코드 넷 중 하나는
음까지 완전히 같았다. 내 고침이 만든 게 아니라 **원래 있던 구멍**이었다.

원인은 띠 폭이다. `kVoiceLow=55` / `kVoiceHigh=72` — 폭이 **17**인데 `oct` 는 이 띠를
12씩 올린다. 층 0 = 55~72, 층 1 = 67~84 → **67~72 가 겹친다.** 층을 나눈 의미가 없다.

띠를 딱 한 옥타브(55~66)로 좁혔다. 재 본 값:

```
low high  최대겹침  60%넘는쌍  평균움직임
 55   72       79%         1       1.31   ← 지금까지
 55   66       38%         0       1.25   ← 이렇게 고쳤다
 55   62       35%         0       1.30   (층 사이에 빈 구간 5반음 — 근거 없는 값)
```

> **띠를 옥타브 단위로 옮길 거면 띠 폭이 옥타브여야 한다.** 이건 취향이 아니라
> 산수다. 폭 17짜리를 12씩 옮기면 반드시 겹친다.

### 그러자 또 하나가 드러났다 — 단조의 sus2

`melody_clash_test` 가 엠비언트에서 걸렸다. `Amb Str2 A` 의 첫 코드가 `sus2` 인데,
**단조에서 sus2 의 2도는 b3 바로 반음 밑이다**(C단조면 D ↔ Eb). 벨 멜로디가 Eb 를
길게 끄는 자리와 정확히 겹쳤다. 옛 코드는 자리바꿈이 우연히 그 D 를 한 옥타브
위에 놓아 단9도(= 「색」)로 넘어가고 있었다 — **우연에 기대고 있었다.**

`sus4` 로 바꿨다. 2도가 없으니 그 충돌 자체가 안 생기고, 열린 소리라는 성격은 같다.
전 장르·전 씬·변형 4바퀴를 다시 세어 **반음 충돌 0건**이다.

새 시험 `test/voicing_check_test.dart` 를 뒀다 — 정렬·코드톤 보존·넓은 코드가
근음에 갇히지 않는가·근음만 쌓는 것보다 손이 덜 움직이는가. 정렬 한 줄을 되돌리면
3번이 「720개 중 720개(100%)」로 실패한다(확인함).

> 하나를 고치니 둘이 더 나왔다. 셋 다 **같은 자리**의 문제다 —
> 코드를 어디에 놓을 것인가. 자를 하나 고치면 그 자가 가리던 것들이 같이 나온다.

## 열려 있는 곡을 지우면 **옆 곡이 그 곡으로 덮였다** (2026-09-05)

UI 파일(lib/ui/*, main.dart)을 찾는 에이전트 일곱으로 훑었다. 후보 14건 중 하나가
적대적 재검증을 통과했는데, 그것이 **만든 걸 잃는** 종류였다.

```dart
if (wasCurrent) {
  currentId = songs.first.id;   // ← 먼저 다음 곡을 가리킨다
  await open(currentId!);       // ← open 은 기본으로 「지금 곡」을 먼저 저장한다
}
```

`open` 이 부르는 `saveNow` 는 `currentId` 를 읽는다. 그런데 그 값은 방금 **다음 곡**
으로 바뀌었고, 메모리에 들어 있는 프로젝트는 아직 **지운 곡**이다. 그래서

> 다음 곡 파일에 지운 곡을 쓰고, 목록의 이름·장르·길이까지 덮은 다음, 그것을 연다.

곡이 둘일 때 열린 쪽을 지우면 **남은 한 곡이 지운 곡으로 바뀌어 있었다.** 화면은
멀쩡해 보인다 — 방금 덮어쓴 파일을 도로 읽으니까. 되돌리기를 눌러도 살아나는 건
지운 곡뿐이라 **옆 곡은 어떤 방법으로도 복구되지 않았다.**

`open` 자신의 주석이 이미 이 위험을 적어 두고 있었다 — 「그걸 저장해 버리면 방금
되살리려던 곡 파일을 덮어쓴다(시험 5번에서 잡았다)」. 같은 함정에 두 번째로 빠진
것이고, 이번엔 부르는 쪽이었다.

이제 지운 곡의 파일은 이미 지웠으니 저장할 것이 없다 — `saveCurrent: false` 로 연다.
여는 데 실패하면 `currentId` 를 null 로 둔다(그래야 0.8초 뒤 자동 저장이 지운 곡을
엉뚱한 파일에 안 쓴다).

### 자가 둘 다 눈을 감고 있었다

**`store_check_test` 4-b** 는 「열린 곡을 지우고 되돌리기」를 정확히 밟는데,
지운 곡만 보고 **살아남은 곡을 안 봤다.** 개수·자리·파일유무·지운 곡 이름만 셌다.
`4-c2` 를 더했다 — 남은 곡의 이름·장르가 그대로인지, 파일 안까지 본다.
고침을 되돌리면 「남은 곡 지울곡/trap」으로 실패한다(확인함).

**`store_rough_test`** 퍼즈는 더 나빴다. 걸음마다

```
동작 → remember()  → mismatch()
```

순서였다. `remember()` 가 「지금 곡의 지금 값이 참값」이라고 등록하는 함수라,
**삭제가 옆 곡을 덮어써도 그 오염된 값이 곧바로 정답으로 박혔다.** 2000걸음을
걸어도 영원히 못 잡는 자였다.

이제 동작을 두 종류로 가른다:
- **곡을 바꾸는 동작**(새 곡·복제·열기·삭제·되돌리기) → `mismatch()` 를 **먼저**.
  바꿔 온 곡은 두고 간 그대로여야 한다. 그다음 `remember()`.
- **지금 곡을 고치는 동작**(스타일·볼륨·저장) → `remember()` 를 먼저. 새 값이 참값.

고친 자로 옛 코드를 돌리니 「스타일이 바뀌었다(lofi → pop)」로 바로 잡는다.

### 그 자가 곧바로 진짜를 하나 더 잡았다

되돌리기가 **마지막 저장 이후의 변경을 통째로 잃고 있었다.** `remove` 가 되돌릴
거리를 **파일에서** 읽는데, 지금 곡은 화면 쪽이 더 최신이다(자동 저장은 0.8초 뒤).
스타일을 바꾸고 바로 지웠다가 되돌리면 예전 스타일로 돌아왔다.

지울 곡이 지금 곡이면 **먼저 저장하고** 읽는다. 이 시점의 `currentId` 는 아직 지울
곡이라 제 파일에 제대로 들어간다.

> 자를 고치면 그 자가 가리던 것이 바로 나온다. 이번엔 고치자마자 2000걸음 안에서
> 다른 진짜가 걸렸다. **못 잡는 자는 없는 것보다 나쁘다** — 있다는 안심을 준다.

## UI 파일을 훑어 열셋을 잡았다 — 전부 진짜였다 (2026-09-05)

아직 안 훑은 화면 파일들(mixer_view·fx_rack / show_band·show_view·show_ops /
editor_view·edit_ops / scene_view·play_head / main / song_view·songs_view·store /
live_view·live_ops·home_view·feel_sheet — 12,000줄 넘는다)에 **찾는 에이전트 일곱**을
붙였다. 후보 14건이 나왔고, **하나씩 다른 에이전트가 반박을 시도**했다.

이번엔 **기각이 0건**이다. 열넷 전부 코드가 그 자리에 있었고, 도달하는 경로가 있었고,
막아 주는 것도 보정해 주는 것도 없었다. 다만 검증자가 **수정안 세 개는 반려**했다
(그대로 넣으면 다른 것이 깨진다) — 그 셋은 자리를 옮겨서 고쳤다. 아래 ⑤⑨⑫.

### ① 열려 있는 곡을 지우면 옆 곡이 그 곡으로 덮였다 (앞 항목 참고)

만든 걸 잃는 종류라 따로 적었다. 자 둘이 눈감고 있던 것까지 같이 고쳤고, 고친 자가
곧바로 **되돌리기가 마지막 저장 이후의 변경을 잃는** 진짜를 하나 더 잡았다.

### ② 곡 재생을 누를 때마다 사용자가 맞춰 둔 빠르기·조가 날아갔다

```dart
final g = songGenreOf(widget.project.genre);
widget.transport.bpm = g.$3;
widget.transport.mode = g.$5;      // ← 「곡 재생」 버튼 안
```

「곡 재생」은 **듣기**지 고치기가 아니다. `Transport` 의 setter 가 `notifyListeners`
를 부르니 0.8초 뒤 그 값이 **파일에 저장까지** 됐다. 조는 소리도 실제로 바뀐다.

이 두 줄이 **덮어 주던 진짜 구멍**이 따로 있었다 — `Store.newSong`/`resetCurrent` 가
`project.reset()` 으로 스타일만 갈아입히고 `Transport` 는 안 맞췄다. 팝(장조) 곡에서
새 곡을 만들면 로파이가 장조를 물려받았다. 뿌리를 막고 두 줄을 뺐다.

> 증상을 덮는 코드는 **덮는 동안 그 자리를 못 보게 한다.** 빼려면 뿌리를 먼저 막아야
> 한다 — 안 그러면 「고쳤더니 다른 게 터졌다」가 된다.

### ③ 곡을 바꿔 열면 마스터 볼륨·마스터링·라이브 버스가 안 실렸다

`Store.open()` 은 이 셋을 그 곡의 값으로 바꾸는데, **엔진에 밀어 주는 자리가
부팅 한 번과 믹서 페이더뿐**이었다. 마스터를 −6dB 로 해 둔 곡을 열어도 앞 곡 크기로
울리고, 앞 곡에 꽂은 리미터가 안 꽂은 곡에도 그대로 걸려 있었다.

`_syncSongChannels()` 를 만들어 **곡 여는 두 길**에서 부른다. `_syncBuses` 에 안 넣은
이유: 그쪽은 `project` 가 바뀔 때마다 도는데 `setInserts` 는 인서트를 **새로 만드는**
명령이라, 페이더를 만질 때마다 마스터 이펙트가 매번 초기화된다.

> 이 코드베이스는 같은 병을 이미 앓았다 — 8월 30일 「내보내기가 마스터 페이더를
> 빼먹고 있었다」. 그때는 내보내기 쪽만 고쳤다. **한 자리를 고칠 때 그 값이 흐르는
> 다른 길을 같이 세어 봐야 한다.**

### ④ 고르기만 해도 되돌리기 기록이 쌓였다

`_snapshot()` 이 갈래 **위**에 있어서, 이미 찍힌 칸을 눌러 고르기만 해도 판이 하나도
안 바뀐 기록이 쌓였다. 되돌리기를 눌러도 같은 판으로 돌아가 **아무 일도 안 일어난다.**
몇 번씩 헛도는 것은 사용자에게 고장으로 보인다. 고치는 갈래에서만 쌓는다.

### ⑤ 라이브 화음이 들리는 것보다 한 옥타브 낮았다 (C조 빼고 전부)

`diatonicChords` 는 근음을 `% 12` 로 접어 늘 C4 언저리에 쌓는다 — 곡의 코드 트랙은
그 자리가 맞다. 그런데 같은 패드의 나머지 셋(라벨 `midiOf`·단음 `freqOf`·녹음한 것의
재생 `degreeFreq`)은 **안 접힌** 자리를 쓴다. A단조에서 도수 2~6 이 한 옥타브 밑으로
울렸고, 그걸 녹음해 두면 재생은 **들었던 것보다 한 옥타브 위**로 나왔다.

시험 5번이 이걸 못 잡은 이유가 정확히 남을 만하다 — `chordFreqsOf(...)` 를
`chordFreqsOf(...)` 와 견주는 **동어반복**이었다. 무엇을 넣어도 통과한다.
새 5-b) 는 **24조 × 14패드**에서 화음 밑음 = 단음 = 녹음이 같은지 본다.

### ⑥ 빠르기 슬라이더가 씬 칩 한 번에 되돌아갔다

`_launch` 가 씬이 들고 있던 옛 템포를 다시 쓴다. 슬라이더는 `Transport` 에만 썼다 —
클립·키트에는 있는 `setKit` 같은 짝이 빠르기에만 없었다. `Project.setBpm` 을 만들어
씬에도 같이 남긴다.

### ⑦ 재생 중에 지금 씬을 지우면 지운 씬 소리가 계속 났다

씬을 **고르는** 쪽(`onLaunch`)은 `_refresh` 로 루프를 다시 보내는데 **지우는** 쪽만
빠져 있었다. 화면만 바뀌고 스피커는 그대로였다. 되돌리기에도 같이 붙였다.

### ⑧ 솔로를 켜도 다른 트랙이 안 흐려졌다

스트립은 저마다 `AnimatedBuilder(animation: track)` 으로 **자기 트랙만** 듣는다
(페이더를 끌 때마다 전체가 다시 그려지면 안 되니 그 판단은 맞다). 그런데 흐림 조건
`anySolo && !track.solo` 는 **남의 트랙** 값이다. 소리는 바뀌는데 화면은 그대로 —
「왜 갑자기 안 들리지」가 어디에도 안 나왔다.

`MixerView` 를 State 로 바꾸고 M/S 를 누르는 자리(`_pushAll`)에서만 한 번 다시 그린다.
페이더 경로(`_push`)는 손대지 않았다.

### ⑨ 녹음 중에 라이브 화면을 나가면 친 음이 말없이 사라졌다

「담기 N」이 떠 있어도 뒤로가기 한 번에 `LiveRecorder` 가 화면과 함께 사라졌다.
안내도 확인도 스낵바도 없었다.

검증자가 **제안된 수정(dispose 에서 담기)을 반려했다** — 두 군데가 깨진다.
`_keep` 이 `notifyListeners` 를 부르는데 `dispose` 는 트리가 잠긴 상태에서 돌아
「widget tree was locked」로 터지고, 게다가 조용히 담으면 같은 이름의 이전 판을
**무성으로 덮어쓴다.** 그래서 `PopScope` 로 **pop 을 한 번 붙잡는** 자리로 옮겼다 —
거기는 평범한 콜백이라 setState 도 스낵바도 합법이다.

> 「잃는 것을 막는 고침」이 **새로운 무성 손실을 만들 수 있다.** 반려가 옳았다.

### ⑩ 씬 루프가 돌고 있는 채로 쇼 화면에 들어가면 시간축이 통째로 어긋났다

씬 루프는 눈금 한 칸이 **한 바퀴**(수 초)인데 쇼 화면은 한 칸을 **곡 전체**로 읽는다.
밴드가 소리와 전혀 안 맞게 번쩍이고 시간 표시가 폭주한다. 들어올 때 곡 모드로 다시
걸어 준다.

### ⑪ 백비트 램프가 클랩을 못 봤다

화면의 「스네어」 램프와 드러머 왼손이 `drumTimes['snare']` 만 봤다.
`show_ops` 가 스네어·클랩·림을 `'backbeat'` 하나로 묶는다.

**다만 이 건은 찾은 쪽이 과장했다.** 「하우스·재즈·가스펠에서 곡 내내 죽어 있다」고
했는데, 재 보니 그 셋 다 스네어가 있다(하우스 24 · 재즈 26 · 가스펠 145).
**정말로 0인 것은 엠비언트 하나**다. 대신 하우스는 백비트 88타 중 **64타(클랩)를
통째로 놓치고** 있었으니 「손이 대부분 안 움직인다」는 여전히 맞다. 숫자를 재 보지
않았으면 과장을 그대로 옮겨 적었을 것이다.

### ⑫ 편집기 재생 막대가 소리의 절반 속도로 기어갔다

엔진이 주는 `pos`(0~1)의 기준자는 **씬 루프 한 바퀴**(그 씬에서 들리는 제일 긴 패턴)
인데, 격자는 **지금 고치는 패턴**이다. 2마디 드럼을 고치는데 씬에 4마디 패드가 있으면
`pos * steps` 는 절반 속도가 된다 — 격자를 한 바퀴 도는 동안 소리는 두 바퀴 돈다.
`headStep()` 으로 되접는다(top-level 순수 함수라 시험이 직접 부른다).

---

시험은 전부 **고침을 되돌리면 실패하는지 확인**했다. 새로 붙인 것:
`voicing_check_test`(새 파일) · `wiring_guard_test`(새 파일) ·
store 4-c2/4-c3 · live 5-b/16/16-b · editor 8-b/따라가기 3-b · mixer 4-c ·
show 5/5-b. `flutter test` **64개 파일 전부 통과**, `flutter analyze lib` 무결점.

> `wiring_guard_test` 는 **약한 자다.** `AudioClient` 가 아이솔레이트 포트를
> 라이브러리 안에 숨긴 구상 클래스라 가짜를 끼울 자리가 없어서, 원본에 그 호출이
> 남아 있는지만 본다. 순서나 값이 틀리면 못 잡는다. 대신 **지워지면** 잡는다.
> 그 자리(시험용 대역)가 생기면 이 파일은 지워도 된다 — 파일 머리에 적어 뒀다.

## 옛 웹 앱에서 가져오기 · 곡 모드 둘 · 라이브 플러그인 (2026-09-05)

사용자 요청 넷: ① 옛 판(`music_doodle/assets/app.html`, 8,593줄)에서 가져올 만한 것을
전부 ② 라이브에도 플러그인 ③ 잇기 ↔ 타임라인 모드 선택 + 연동 ④ 만들기·잇기·라이브
어디서든 믹서.

옛 판을 **찾는 에이전트 여섯**으로 갈래별(편집기·곡구성·라이브·소리·저장공유·사용감)로
훑어 후보 33건(값 큰 것 11건)을 뽑았다. 이번 판에 넣은 것과 그 까닭을 아래 적는다.

### ③ 곡 모드 둘 — **자료는 하나다**

「곡으로 잇기」와 「타임라인」을 위쪽 토글로 고른다. 중요한 것은 만든 방식이다:

> **두 벌을 들고 맞추지 않는다.** 구간표(`Arrangement.sections`) 하나뿐이고,
> 타임라인은 그 목록에서 폭을 셈으로 뽑아 그린다. 옮겨 담는 코드가 **없어서**
> 어긋날 수가 없다.

두 벌을 들고 동기화하는 길이 흔한데, 그건 반드시 한쪽만 고치는 날이 온다 —
이 저장소에서 그 종류를 벌써 여러 번 잡았다(카우벨 목록, 마스터 인서트, 씬 삭제).

타임라인에서는 블록을 눌러 고르고 아래 줄에서 **여기부터 재생 · 앞뒤로 옮기기 ·
판 수 · 한 벌 더 · 지우기**를 한다. 블록에 **무엇이 들어 있는지 색 띠**로 보여 준다
(드럼·베이스·코드·멜로디) — 이름이 다 「벌스」여도 어느 쪽이 두꺼운지 갈린다.

블록에 `onDoubleTap` 을 안 단다. 같이 걸면 Flutter 가 **한 번 누른 것을 300ms
붙잡아 둔다** — 고를 때마다 굼뜨다. 시험을 쓰다가 알았다(탭이 안 먹었다).

### 구간부터 재생

`buildSong(from:)` 이 앞 구간을 건너뛴다. 목록에서는 **번호 칸이 곧 ▶** 이고,
타임라인에서는 손잡이 줄에 있다. 고치는 중이면 `_refresh` 도 그 자리를 이어 간다 —
뒷부분을 고치는데 매번 처음으로 돌아가면 고칠 수가 없다.

### ② 라이브 플러그인

엔진은 **처음부터 되어 있었다** — `live` 는 붙박이 버스라 인서트를 들고 있다.
없던 것은 **꽂을 자리**뿐이었다. `LiveChannel` 이 `FxChainOwner` 가 되고, 믹서의
라이브 스트립·줄에 「이펙트」가 붙었다. 곡마다 따로 저장된다.

시험을 쓰자 곧바로 진짜가 하나 나왔다 — **새 곡을 만들어도 앞 곡의 이펙트가 남았다.**
`Project.reset()` 은 씬·트랙만 되돌리고 마스터·라이브는 `Store` 가 들고 있어서다.
`_resetChannels()` 를 만들어 마스터 인서트·볼륨까지 같이 비운다(마스터도 같은 병이었다).

### ④ 믹서·사용법이 어디서든

만들기·잇기·라이브 상단바에 **믹서**와 **사용법** 아이콘을 뒀다. 믹싱은 만들면서
하는 일이지 다 만들고 하는 일이 아니다. 헤매는 사람에게 **나가라고 하는 안내**도
안내가 아니다. (첫 화면과 달리 `simple` 이어도 보인다 — 여기까지 들어온 사람이다)

### ① 옛 판에서 가져온 것들

| 가져온 것 | 왜 |
|---|---|
| **줄 이름 탭 = 소리 미리듣기** | 15줄 중 어디에 찍을지 여태 **찍어 봐야만** 알았다. 찍고 듣고 지우는 세 번이 한 번이 된다 |
| **악기 미리듣기(탭=듣기, 다시 탭=적용)** | 서른 개를 소리 한 번 못 듣고 이름만 보고 골랐다 |
| **다시하기(Redo)** | 되돌리기만 있으면 **마음 놓고 못 되돌린다.** 짝이 있어야 산다 |
| **전체 음 길이 ±** | 스타카토 ↔ 레가토를 통째로. 20음이면 20번이던 일 |
| **원탭 프리셋** | 랙을 열면 여덟 개 목록뿐 — 무엇을 왜 꽂는지 모르면 못 꽂는다. 「노래하듯」·「따뜻하게」는 안다 |
| **나가는 순간 저장** | 자동 저장은 0.8초 뒤다. 찍고 바로 홈을 누르면 **그게 사라졌다** |
| **구간에 내용 미리보기** | 재료(`Scene.clips`)는 이미 있었다. 안 보여 주고 있었을 뿐 |
| **구간 바로 뒤에 한 벌 더** | 맨 뒤에 붙였다가 열 칸을 끌어 올릴 필요가 없다 |
| **빈 얼개 내보내기 막기** | 44바이트짜리 WAV 가 공유 시트로 나갔다 |

**안 가져온 것**과 까닭: 12음 크로매틱(라이브 설계와 정면으로 부딪힌다 — 패드는
「틀린 음이 안 나온다」가 약속이다) · 테마 11종(값에 비해 크다) · 이모티콘 서랍(크다,
다음에) · 곡 전체 코드 진행 편집(크다, 다음에) · 꾹 눌러 소리 유지(엔진에 목소리를
잡아 둘 손잡이가 없다 — `noteOn` 이 fire-and-forget 이라 큰 일이다) · 메트로놈(박에
정확히 놓으려면 앞질러 예약해야 한다 — 화면 타이머로 하면 흔들린다).

### 손가락 바닥선이 다시 걸렸다

줄 이름이 **누르는 것**이 되자 `tap_size_test` 가 62×31 을 잡았다(바닥선 32).
자를 무디게 하는 대신 **격자 줄 높이 하한을 28 → 32 로 올렸다** — 격자 칸도 같이
커진다(원래도 제일 자주 빗나가던 자리다). 34 로 올려 보니 15줄이 화면에 안 들어가서
되돌리고, 대신 음 막대가 줄 높이를 다 쓰게 했다(1px 여백은 안쪽 margin 으로).

> 「누를 수 있게 만들었다」는 **누를 것을 하나 늘렸다**는 뜻이다. 자가 그걸 잡았다.

가로 편집기 손잡이 줄은 「음 길이」가 늘면서 1.3배 글자에서 126px 넘쳤다.
Wrap 으로 바꿔 봤더니 「가로는 한 줄」 시험이 깨졌다(그 한 줄이 설계였다) —
가로에서만 글자 라벨을 줄여 한 줄을 지켰다.

`flutter test` **65개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 이어서 넷 더 — 설정·지우개·코러스 (2026-09-05)

### 설정 시트 — 화면 방향 · 소리 품질 · 손잡이

화면이 하나도 없었다. 항목 셋짜리 화면을 만들면 「설정이 어디 있지」를 한 번 더
묻게 되니 시트로 뒀다(첫 화면 톱니).

- **화면 방향** 자동/세로/가로. 눕혀 놓고 라이브를 치거나 쇼를 틀 때 화면이 도는
  것만으로 연주가 끊긴다. `SystemChrome` 은 앱 전체에 걸리므로 **한 곳**(`applyOrient`)
  에 모았다 — 안 그러면 어느 화면이 마지막에 말했는지로 정해진다.
- **소리 품질** 좋게/가볍게. 엔진에는 `setQuality` 가 처음부터 있었는데 **부르는 데가
  한 곳도 없었다.** 무거운 곡에서 끊기면 사용자가 할 수 있는 일이 하나도 없었다.
- **간단히/전부 보기** — 원래 있던 것을 여기로 모았다.

설정은 `index.json` 에 남고 켤 때 다시 건다. **「이번 한 번」이면 그건 설정이 아니다.**
모르는 값이 들어오면 기본으로 떨어진다(파일 하나가 앱을 못 열게 만드는 자리를 이미 겪었다).

### 지우개 — 하나와 전부 사이

지우는 길이 둘뿐이었다: 하나씩 두 번 누르기, 또는 판 전체 날리기.
**16비트 하이햇 한 줄이면 32번이다.** 켜고 격자를 훑으면 지나간 것이 계속 지워진다.
찍기 드래그와 같은 길(꾹 눌러 끌기)을 쓴다 — 세로 스크롤과 안 부딪히려면 그래야 한다.

되돌리기는 **한 번에** 다 돌아온다(`_snapshot` 이 끄는 동안 한 번만 쌓는다).
켜져 있으면 버튼이 노랗다 — 켠 줄 모르고 찍으려다 지우면 그게 제일 나쁘다.

### 코러스 — 모듈레이션이 통째로 없었다

얇은 소리를 두껍게 만드는 수단이 리버브뿐이었는데, **리버브는 멀리 보내는 것이지
두껍게 하는 것이 아니다.** 짧은 딜레이(12ms)의 길이를 느리게 흔든다.

좌우를 **반대 위상**으로 흔드는 것이 핵심이다 — 같이 흔들면 가운데서 뭉쳐 두꺼워지기만
하고 안 넓어진다. 읽을 때 소수 자리까지 보간한다(정수로 끊으면 흔들림이 계단이 되어
지직거린다). 프리셋 「넓게」에 넣고 「두껍게」를 새로 만들었다.

> 시험 3번(「깊이가 실제로 먹는다」)이 처음엔 220Hz 로 쟀다가 **자가 천장에 붙었다** —
> 한 주기가 4.5ms 라 깊이 1ms 와 9ms 가 둘 다 반 주기를 넘어 좌우 차이가 똑같이 꽉 찼다.
> 40Hz(25ms 주기)로 바꾸니 0.074 → 0.451 로 갈렸다.
> **자가 천장에 붙으면 무엇을 넣어도 통과한다.**

### 자가 또 잡았다

- `tap_size_test`: 줄 이름이 눌리게 되자 62×31 이 걸렸다(바닥선 32). 격자 줄 하한을
  28→32 로 올리고 음 막대가 줄 높이를 다 쓰게 했다. **「누를 수 있게 만들었다」는
  「누를 것을 하나 늘렸다」는 뜻이다.**
- `store_check_test` 4-c4: 라이브 이펙트를 저장하는 시험을 쓰자 곧바로 **새 곡에
  앞 곡 이펙트가 남는** 진짜가 나왔다(마스터도 같은 병이었다).

`flutter test` **66개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 꾹 눌러 소리 유지 · 끌어서 음 잇기 (2026-09-05)

옛 판 조사에서 값이 제일 크면서 제일 큰 일로 남겨 뒀던 것. 라이브 패드가 여태
**어떻게 만지든** 「짧게/보통/길게」 중 미리 고른 길이로만 났다 — 라이브인데
표현이 없었다. 손가락을 옆 패드로 끌어도 아무 일이 안 났다.

막고 있던 것은 엔진이었다. `noteOn(voice, freq, dur, vel)` 은 **길이를 미리 받는**
fire-and-forget 이라 목소리를 잡아 둘 손잡이 자체가 없었다.

### 엔진 — 릴리스로 건너뛰기

엔벨로프는 **미리 구워 둔 마디 목록**이다. 그래서 「지금부터 릴리스」는 마디를
건너뛰는 일이 된다:

```dart
void release() {
  if (done || _n.length < 2) return;
  if (_seg >= _n.length - 1) return;   // 이미 릴리스 안이다
  _seg = _n.length - 2;                // `_next` 가 하나 올린다
  _next();                             // 지금 값에서 기울기를 다시 잡는다
}
```

마지막 마디가 늘 「무음까지」라(`buildAdsr`·`buildRing` 둘 다) 거기로 옮기면 된다.
`_next` 가 **현재 값에서** 기울기를 다시 잡으므로 뚝 끊기지 않는다.

`Engine.noteHold(id, …)` / `noteRelease(id)` / `releaseAllHeld()`.
`id` 는 부르는 쪽의 손가락(포인터) 번호다.

**세 가지를 조심해야 했다:**

1. **수명도 같이 줄인다.** 안 줄이면 소리는 멎었는데 목소리를 계속 물고 있어서,
   빨리 여러 번 치면 목소리가 바닥난다.
2. **회수 루프가 손잡이도 떼어야 한다.** `maxSec`(12초)이 지나 저절로 끝난 목소리를
   `_held` 가 계속 가리키면, 그 자리에 들어온 **다른 음**을 손 뗄 때 꺼 버린다 —
   소리가 이유 없이 사라지는 종류다. 시험 7번이 이걸 못 박는다.
3. **손을 안 떼도 언젠가는 놓는다**(`maxSec`). 화면이 죽거나 메시지를 놓쳐도
   목소리가 영영 물려 있으면 그 자리는 영영 못 쓴다.

### 화면 — 격자 전체에 Listener 하나

**패드마다 Listener 를 달면 안 된다.** 손가락이 그 패드를 벗어나는 순간 그 위젯은
move 를 더 못 받는다(포인터는 처음 잡은 위젯에 묶인다). 격자 전체에 하나 달고
**자리로 어느 패드인지 셈한다.**

화음은 음이 여럿이라 번호를 나눠 쓴다(`pointer * 8 + i`) — 엔진은 번호 하나에
목소리 하나만 문다. 아르페지오는 시간차로 흩뿌리는 것이라 **잡아 두면 아르페지오가
아니다** — 그쪽은 예전 길(batch)을 그대로 쓴다.

### 담기는 것도 들린 것과 같아야 한다

처음 판은 **뗄 때만** 담았다. 그러면 C4 → Eb4 로 끌어도 「담기 1」 — 마지막 음만
남는다. 지나간 음도 들렸으니 지나갈 때마다 담는다(「담기 3」). 길이도 **실제로 잡고
있던 만큼** 적는다. 「짧게/보통/길게」는 이제 **바닥**이다(톡 쳤을 때의 길이).

> 라이브에서 「들린 것 ≠ 담긴 것」은 이 저장소가 이미 두 번 겪은 자리다
> (레이턴시 되돌리기, 화음 옥타브). 세 번째는 시험이 먼저 잡았다.

`flutter test` **67개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 백업·음이름·연주해서 곡 만들기 (2026-09-05)

### 곡을 파일로 꺼낸다 — 앱을 지우면 전곡이 사라지고 있었다

곡은 앱 전용 폴더(`documents/songs/*.json`)에만 있었다. **앱을 지우거나 폰을 바꾸면
그걸로 끝이다.** 「만든 걸 잃는 것보다 나쁜 건 없다」가 이 앱의 첫 규칙인데 정작
제일 크게 잃는 길이 열려 있었다.

「내 곡」의 ⋮ → **파일로 꺼내기**. 카톡·드라이브 아무 데나 보내 두면 그게 백업이다.
`Store.exportJson` 은 **지금 열려 있는 곡이면 먼저 저장한다** — 마지막 저장 이후에
만진 것이 빠진 파일은 백업이 아니다. 조용히 실패하지 않고 말해 준다(백업한 줄 알고
지나가는 것이 제일 나쁘다).

**가져오기는 아직 없다.** 파일 고르기가 필요한데 그건 새 dependency(`file_picker`)라
사용자에게 물어볼 일이다. 내보내기만으로도 「폰을 바꿔도 남는다」는 성립한다.

### 편집기 줄 이름에 음이름

`3↑` 옆에 `Eb5`. 도수만 보면 무엇을 찍고 있는지 알 수 없다. **라이브 화면은 이미
음이름을 보여 준다** — 같은 앱에서 한쪽만 알려 주면 사람은 그쪽에서만 배운다.
베이스는 실제 재생 음역(C1 기준)으로 적는다 — 화면에 C4 라 써 놓고 C1 이 나면
그건 거짓말이다.

위아래로 쌓았더니 1.3배 글자에서 줄 높이(32)를 넘겼다 → 한 줄로 붙이고 `FittedBox`
로 자리가 모자라면 통째로 줄어들게 했다.

### 연주해서 곡 만들기

여태 곡을 만드는 길은 하나였다: 곡 화면에서 「＋구간 붙이기」로 하나씩 놓기.
**그건 적는 일이지 만드는 일이 아니다.** 씬을 틀어 놓고 인트로 → 벌스 → 코러스로
넘겨 보는 것이 훨씬 자연스럽고, 그때 이미 사람은 곡을 만들고 있다. 그 손짓을
그냥 받아 적는다(씬 줄 왼쪽 「연주 녹음」).

셈은 `lib/arrange_rec.dart` 로 떼어 놨다 — 「몇 판 머물렀나」를 초에서 판 수로
바꾸는 것이 이 기능의 전부인데, 그게 틀리면 곡 길이가 통째로 어긋난다.

- **같은 씬을 다시 눌러도 구간이 안 늘어난다** — 그건 넘어간 게 아니라 다시 튼 것이다
- **반올림한다** — 손으로 넘기면 판 경계에 딱 맞을 리가 없다. 내림하면 스치듯 지난
  구간이 통째로 사라진다. 누른 것 자체가 「여기 한 판」이라는 뜻이다
- 갈아 끼울 때 **되돌릴 거리**를 준다 — 곡 구성은 한 번에 제일 많이 잃는 자리다
- 벽시계(`Stopwatch`)를 쓴다. 재는 것이 **사람이 언제 눌렀나**라서다 —
  엔진 시계는 판 안의 위치라 판을 넘으면 0 으로 돌아간다

`SceneSequencer.sceneLoopSec` 을 새로 빼서 `songSpans` 도 그걸 쓰게 했다.
두 곳에서 따로 세면 반드시 어긋난다.

### 자가 또 거짓말하고 있었다

`editor_ui_test` 14) 「촘촘히 해도 보던 마디가 그 자리」가 **마디 자의 「3」 을 잰다**
고 적어 놓고, 실제로는 **가로 스크롤 밖**에 있는 줄 이름 「3」(도수 3)을 재고 있었다.
그 x 는 밀어도 안 변하니 **무엇을 하든 통과했다.** 판이 2마디라 자에도 점프 줄에도
「3」 이 없었기 때문이다.

줄 이름에 음이름을 붙이면서 그 글자가 `find.text` 에 안 잡히게 되자 드러났다.
이제 마디를 4로 늘리고 자의 「3」 을 잰다.

> 자가 **엉뚱한 데를 보고 있으면** 통과도 실패도 뜻이 없다.
> 이번 것은 「고치다 보니 드러난」 경우다 — 안 건드렸으면 계속 안 보였다.

`flutter test` **68개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 코드 종류 · 악기 즐겨찾기 · 곡 길이 맞추기 (2026-09-05)

### 코드 종류 고르기 — 엔진은 처음부터 알고 있었다

`theory.dart` 는 스물네 가지 코드를 알고 라이브러리 패턴도 `min7`·`maj7`·`min9` 를
쓰고 있었다. 그런데 **편집기에서 고를 자리가 없어서**, 사용자가 직접 찍는 코드는
무엇을 찍어도 기본 3화음뿐이었다 — 코드 트랙의 색이 하나로 고정돼 있었다.

고른 음 아래 칩 줄에 `기본 · m7 · M7 · 7 · sus4 · sus2 · dim`.
**스물넷을 다 내놓지 않는다** — 이 여섯이면 팝·재즈·로파이가 다 나오고, 이름도
악보에서 보던 그대로다. 첫 항목이 「기본」인 것은 **잘못 골랐을 때 나가는 길**이다.

5번째 칸은 코드 줄에서 「종류」이고 음정 줄에서는 「글라이드」다 — 그래서
`setChordType` 은 코드가 아니면 아무것도 안 한다. 6번째 칸(층)이 있으면 자리를
맞춰 넣는다(안 그러면 층이 밀려 나가 방금 찍은 코드만 한 옥타브 떨어진다).

시험 3번이 **고른 종류가 소리까지 가는지**를 본다(m7 4음 vs 기본 3음) — 화면만
바뀌고 소리가 그대로면 아무 일도 안 한 것이다.

### 악기 즐겨찾기 넷

연주 중에 악기를 바꾸려면 모달 서랍을 열었다 닫아야 했다 — **그 사이 연주가 끊긴다.**
최근에 쓴 넷이 칩 줄에 남아 한 번 탭으로 오간다. 곡에 저장하지 않는다 —
「방금 뭘 썼나」는 이 화면에 있는 동안의 일이다.

### 곡 길이 맞추기

「짧게 2:05 · 보통 3:25 · 길게 4:45」. 구간을 하나씩 더하고 빼서 3분을 맞추는 것은
**셈이지 음악이 아니다.**

- **마디가 아니라 초로 잰다** — 78BPM 로파이와 128BPM 하우스는 같은 마디 수라도
  길이가 배로 다르다
- **인트로·아웃트로는 안 건드린다** — 곡의 처음과 끝은 자리를 지켜야 하는 것이다
- **구간을 지어내지 않는다** — 없던 대목을 만들면 사용자가 만든 곡이 아니게 된다.
  가운데 대목의 판 수로만 맞춘다
- 한 번에 한 판씩, **목표를 지나치기 직전에** 멈춘다(크게 바꾸면 훌쩍 넘어간다)
- 지금 길이에 제일 가까운 칩이 켜져 있다 — 「지금 어디쯤인가」가 보인다

가로에서 줄이 하나 더 늘자 곡 목록이 17px 밀려 났다 → 가로에서는 길이 줄과
보기 줄을 **한 줄로 붙인다**.

`flutter test` **68개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 메트로놈 · 레벨 미터 (2026-09-05)

### 메트로놈 — 앞질러 예약한다

드럼이 없는 씬(패드·코드만)에서 녹음하면 **기댈 박이 아예 없다.** 반주는 흐르는데
어디가 1박인지 안 들리니 박에 맞춰 칠 수가 없다.

**화면 타이머로 「지금」 울리면 안 된다.** 프레임은 16ms 마다 오고 그마저 밀린다 —
박을 ±16ms 로 흔드는 메트로놈은 자가 아니라 소음이다. 그래서 앞질러 예약하고
시각은 엔진이 지킨다(`scheduleDrum` 은 delay 를 받아 샘플 단위로 놓는다).

「무엇을 언제 놓을지」는 `LivePads.beatsToSend` 가 정한다 — 화면에서 떼어 놨다.
이 방식에서 틀릴 수 있는 것이 정확히 셋이라 시험이 그 셋을 잰다:

- **두 번 놓기** — 같은 박을 두 번 예약하면 두 번 들린다(`sent` 로 막는다)
- **건너뛰기** — 8초 판 120BPM 이면 1~15박이 하나도 안 빠져야 한다
- **판 넘어가기** — 판을 넘는 박은 안 낸다(넘어가면 기록을 비우고 다음 바퀴가 잡는다)

마디 첫 박은 세게 친다 — 어디가 1박인지 들려야 자다.

### 레벨 미터 — 값은 처음부터 오고 있었다

믹서에 페이더만 있고 **소리가 얼마나 크게 나가는지 알려 주는 것이 하나도 없었다.**
그러면 「왜 찌그러지지」·「왜 작지」를 귀로만 알아내야 한다.

`AudioStats.peakOut` 은 처음부터 화면까지 오고 있었다 — 개발용 **엔진 시험 화면**에만
숫자로 있었을 뿐이다. 마스터 페이더 **옆에** 막대를 붙였다(따로 떨어뜨리면 눈이 두
군데를 오간다).

**dB 로 그린다.** 배수로 그리면 조용한 곡이 늘 바닥에 붙어 보인다(절반 크기는
−6dB 인데 막대는 절반이 된다). 0dB 자리에 눈금을 긋는다 — `peakOut` 은 리미터 **전**
봉우리라, 눈금을 넘으면 리미터가 붙잡고 있다는 뜻이다.

봉우리 자국은 천천히 내려온다(×0.72). 지표가 250ms 마다 오는데 그때마다 값이 확
떨어지면 **깜빡이는 막대가 되고, 깜빡이는 막대는 안 보는 것과 같다.**

`meterFrac` 만 떼어 시험한다 — 눈금이 틀리면 그림도 틀리기 때문이다.
줄 모양에서는 고정 폭을 안 준다(320dp 폰에서 50px 넘쳤다) — 자리를 나눠 갖게 두면
좁은 화면에서 좁아질 뿐 안 잘린다.

`flutter test` **69개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 트랙별 레벨 미터 (2026-09-05)

마스터 미터에 이어 **트랙마다** 붙였다. 왜 이게 마스터보다 중요한가:

> 폰 스피커로는 저음이 거의 안 들린다. **베이스가 두 배로 커도 귀로는 모른다.**
> 눈이 있으면 그건 바로 보인다.

`TrackMix.peak` 을 `process` 끝(팬·페이더를 **지난 뒤**)에서 잰다 — 실제로 나가는
크기가 그것이다. `takePeak()` 은 **읽고 비운다**: 안 비우면 한 번 커진 값이 영영
안 내려온다. 250ms 마다 오는 지표가 곧 「그 사이의 제일 큰 값」이 된다.

`AudioStats.busPeaks` 로 버스 이름 → 봉우리를 실어 보낸다. 옛 판(짧은 메시지)이
와도 안 죽는다 — 없으면 빈 표다.

**렌더 시간에 안 잡힌다.** 샘플마다 비교 둘씩인데 `render_bench` 로 재 보니
실시간 대비 4.6% 그대로다(재기 전과 같다).

시험은 눈금(`meterFrac`)과 **값이 실제로 오는가**를 나눠서 본다 —
눈금이 맞아도 값이 안 오면 막대는 늘 바닥이고, 그건 없는 것과 같다:
- 소리 난 버스만 값이 올라오는가(멜로디만 쳤는데 베이스가 뛰면 배선이 틀린 것이다)
- 읽으면 비워지는가
- **페이더를 내리면 값도 내려가는가** — 재는 자리가 페이더 앞이면 이게 안 변한다
- 드럼 버스도 잡히는가

`flutter test` **69개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 연주 흔들림 — 타이밍이 죽어 있었다 (2026-09-05)

`Human` 은 셋을 흔든다고 적어 놨다: 음정(c) · 세기(v) · **타이밍(t)**.
그런데 `Human.t` 는 **선언과 `setLevel` 안의 대입 한 줄뿐, 쓰는 데가 없었다.**
`setLevel` 자체도 lib 안에서 부르는 데가 0 이었다. 그래서 박이 자로 잰 듯 딱 맞았고,
사람이 친 것과 기계가 친 것의 차이는 대개 거기서 난다.

### 어디에 걸었나

`Engine.schedule` / `scheduleDrum` — **예약하는 자리**다. 소리를 내는 자리가 아니라
「언제 낼지 정하는」 자리라야 한다. 드럼도 흔든다 — 드러머가 제일 안 기계 같아야 한다.

**0보다 앞으로 안 당긴다.** 앞으로 당겨 봐야 이미 지난 시각이라 곧바로 울리고,
그러면 흔든 게 아니라 **앞으로 튄 것**이 된다.

그루브·스윙(`feel.dart`)과는 다른 일이다 — 그쪽은 **일부러** 미는 것이고(뒷박을
늦추면 그게 스윙이다) 이건 **매번 조금씩 다른** 흔들림이다. 둘 다 있어야 사람 같다.

### 손잡이를 같이 줬다

설정에 「연주 흔들림 — 끔 / 자연스럽게 / 많이」. 딱 맞는 쪽을 좋아하는 사람도 있고,
무엇보다 **끌 수 있어야 시험이 결정적으로 돈다**(기존 시험 여덟 개가 `setLevel(0)`
을 이미 부르고 있었다 — 이제 그게 실제로 타이밍까지 끈다).

기준 곡 지문은 그대로다 — 그 시험이 `setLevel(0)` 으로 도니까.

### 내보내기가 거짓말할 뻔했다

렌더는 **다른 아이솔레이트**에서 돈다. `Human` 은 static 이라 거기서는 늘 기본값(1)
으로 시작한다 — 사용자가 「끔」으로 해 놓아도 **파일에는 흔들림이 실렸을 것이다.**
`ExportJob.human` 으로 지금 값을 실어 보낸다.

> 마스터 페이더가 정확히 이 이유로 한 번 빠졌었다(2026-08-30).
> **들은 것과 파일이 다르면 그건 앱이 거짓말을 한 것이다.**

그리고 그 고침이 곧바로 시험 하나를 깼다 — `renderWav` 가 `Human.setLevel` 을
부르면서 **같은 아이솔레이트에서 뒤에 도는 것의 설정을 바꿔 버렸다**(「재생 =
내보내기」가 2.5초 내내 최대 차이 1.2 로 어긋났다). `Human` 은 static 이라
**빌리는 것**이다 — 렌더가 끝나면 되돌려 놓는다.

> 전역을 건드리는 함수는 **빌린 것을 갚아야 한다.** 안 갚으면 그 함수를 부른
> 사람이 모르는 사이에 남의 설정이 바뀐다.

`flutter test` **70개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 구간 미리보기 — 미니 피아노롤 (2026-09-05)

색 띠(`_TypeStrip`)는 「무엇이 들어 있나」만 말한다. **벌스가 둘이면 둘 다
「드럼·베이스·코드·멜로디」다.** 어떤 곡인지는 하나도 안 알려 준다.

구간마다 그 씬의 가락 한 줄을 뽑아 점으로 찍는다 — 올라가는 가락인지 한 자리를
맴도는지가 그냥 보인다. 목록 줄에도, 타임라인 블록에도(자리가 넉넉할 때만) 그린다.

- **멜로디를 먼저 본다** — 귀에 제일 크게 남는 줄이다. 없으면 코드, 그다음 베이스.
- **도수 범위를 쓰는 만큼만 편다** — 15줄로 고정하면 좁은 가락이 한 줄에 뭉친다.
- 그림 자체는 못 재니 **재료가 성한가**를 시험이 본다: 칸 수가 0 이 아닌가(그리다
  0 으로 나눈다), 음이 판 밖으로 안 나가는가(막대가 칸 밖으로 삐져나온다),
  없는 씬에 물어도 안 죽는가.

> `LayoutBuilder(builder: (context, c))` 가 바깥의 `c`(씬 색)를 **가렸다** —
> 색 자리에 제약이 들어가서 엉뚱한 데서 터졌다. 짧은 이름은 가리기 쉽다.

`flutter test` **70개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 씬 캐릭터 — 옛 앱의 이모티콘 서랍 (2026-09-05)

씬 이름은 「벌스 2」 처럼 다 비슷하게 남는다. 색 띠도 「무엇이 들어 있나」만
말한다. **어느 씬이 어느 씬인지**를 한눈에 갈라 주는 것이 여태 없었다.

옛 앱(뮤직 두들)의 캐릭터 11종을 가져왔다. 두 가지를 바꿨다.

- **자세를 표가 아니라 박으로 뽑는다.** 옛 앱은 8분음표 16칸짜리 포즈 표를
  돌렸다 — 한 판이 2마디냐 4마디냐에 따라 춤이 달라진다. 여기서는 「지금 몇
  박째」 하나만 받는다: `_bump`(박마다) · `_bar`(마디마다) · `_bar2`(두 마디마다).
- **지금 씬만 춤춘다.** 다 움직이면 어느 것이 소리 나는지 오히려 안 보이고,
  씬 수만큼 시계가 돈다. `CritterBeat` 는 `LoopPosBuilder` 를 안 쓴다 —
  그쪽은 멈춰 있어도 초당 60번 다시 그린다.

측정할 수 없는 것(예쁜가)과 측정할 수 있는 것을 갈랐다. 시험이 보는 것:
**두 목록이 맞나**(`kDefaultCritters` ⊂ `kCritters` — 없는 id 를 적으면 화면에
아무것도 안 그려지고 오류도 안 난다) · **정말 그려지나**(픽셀 4%↑) ·
**정말 움직이나**(네 자세 중 셋 이상이 서로 다른 픽셀) · 새 씬은 안 쓴 것을
받나 · 저장 왕복에 살아남나. 그림 자체는 `test/critter_snap.dart` 로 뽑아서 봤다.

> 구간 줄에 **칸을 하나 더 만들었더니** 320dp 폰에서 아래 띠 줄이 8.9px 넘쳤다.
> `Expanded` 가 있어도 그 안의 고정폭(`_TypeStrip`)이 못 줄어든다. 이름 줄
> **안에**(`WidgetSpan`) 넣으니 자리가 모자랄 때 이름과 같이 줄어들 뿐이다.

> 고양이 꼬리·문어 다리는 40×52 판을 잠깐 넘어간다. 옛 앱에서는 브라우저가 SVG 를
> 잘라 줘서 안 보였다 — `clipRect` 를 빠뜨리면 **옆 칩 위에** 그려진다.

`flutter test` **71개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 씬 코드 진행 — 코드를 바꾸면 베이스·멜로디가 따라온다 (2026-09-05)

옛 앱의 `P.tl.prog[마디]` 를 가져왔다. 여태 화성을 바꾸려면 코드 패턴을 열어
고치고, 베이스를 열어 같은 만큼 옮기고, 멜로디도 다시 맞춰야 했다 —
**세 군데를 손으로 맞추는 일**이고 하나만 빠뜨려도 그 구간이 어긋난 채 돈다.

- **단위가 마디가 아니라 코드 음 하나다.** 옛 앱은 마디마다 하나였는데, 반 마디에
  코드가 둘인 패턴(로파이·시티팝)이 흔해서 마디로 묶으면 뒤 코드는 바꿀 길이 없다.
- **가까운 쪽으로 옮긴다**(−3~+3). 1도→7도는 여섯 칸 위가 아니라 한 칸 아래다.
- 따라가는 패턴이 **짧으면 먼저 펼친다** — 안 그러면 한 마디를 두 코드가 나눠 써서
  한쪽을 맞추는 순간 다른 쪽이 어긋난다. **길면** 코드가 되풀이되는 자리마다 옮긴다.
- 코드 타입(m7)과 층은 그대로 둔다 — 화성을 바꾼다고 버릴 뜻이 아니다.

시험(`prog_check_test`)은 데이터가 아니라 **실제로 나가는 소리**를 본다:
`SceneSequencer.build` 가 뽑은 베이스 주파수가 도수 차만큼 옮겨졌는가, 구간
**밖**은 그대로인가, 되돌리면 주파수까지 돌아오는가.

> 되돌리기를 **스낵바로 냈더니 눌러도 아무 일이 없었다.** 서랍이 열려 있는 동안
> 스낵바는 서랍의 막(barrier) **아래**에 깔린다 — 탭이 막에 먹히고 서랍만 닫혔다.
> 닿지 않는 되돌리기는 없는 것과 같다. 되돌리기 줄을 **서랍 안으로** 옮겼다.

> `makeEditable` 은 `scene.clips` 를 **그 자리에서** 갈아 끼운다. 되돌리기 표에
> 옛 이름을 적으려면 부르기 **전에** 적어야 한다 — 뒤에 적으면 새 이름을 옛
> 이름이라고 적는다(되돌려도 그대로).

> 시험에서 「구간 밖은 그대로」를 **앞쪽만** 쟀더니 첫 자리를 고른 판에서
> 「0음이 그대로였다」로 통과했다. 재는 시늉만 하는 검사다 — 앞뒤 둘 다 재고
> **잰 것이 0개가 아닌지**도 같이 본다.

`flutter test` **73개 파일 전부 통과**, `flutter analyze lib` 무결점.

> **캐릭터가 생기기 전에 저장한 곡**에는 하나도 없다 — 폰에서 열어 보고 알았다
> (정작 제일 많이 여는 곡들이다). 불러올 때 **하나도 없을 때만** 채운다:
> 하나라도 있으면 그 파일은 새 형식이고, 빈 씬은 사용자가 **일부러 뗀** 것이다.

폰(SM-A175N) 설치·확인 완료: 씬 칩에 캐릭터가 뜨고, 씬 ⋮ → 「코드 진행 — 8자리」
→ F단조 곡이 `Db Db Eb Eb / Fm Fm Fm Eb` 로 제대로 읽힌다.

## 캐릭터를 끝까지 잇는다 · 코드를 눌러 듣는다 (2026-09-05)

- **곡 화면 눈금 줄**과 **쇼 화면 무대**에 지금 흐르는 구간의 캐릭터가 나와 춤춘다.
  구간 줄마다 춤추게 하면 구간 수만큼 시계가 돌고, 무엇보다 「지금 어디를
  지나는지」가 오히려 안 보인다. 눈금 줄은 **이미 매 프레임 다시 그려지는 자리**라
  시계를 하나도 더 안 만든다.
- 두 화면이 `critterAt()` **하나**를 물어본다. 두 군데에 따로 적으면 언젠가
  말없이 어긋난다 — 이 프로젝트에서 제일 자주 나온 병이다.
- **코드 진행에서 눌러 보면 소리가 난다.** 「4도가 뭐지」를 글자로는 못 안다.
  자리를 누르면 지금 걸린 코드가 나고(견줄 것), 고르면 새 코드가 난다.
  음색은 **코드 트랙 것**을 쓴다(`chordVoiceOf`) — 패드 곡에서 피아노가 나면
  견주려고 눌러 보는 것인데 견줄 수가 없다.
- 첫 안내(사용법) 1단계에 「코드 진행」 한 줄을 넣었다 — 씬 이름 옆 ⋮ 안에 있어서
  **있는 줄 모르면 없는 것**이다(「느낌」을 넣은 것과 같은 이유).

> 무대 캐릭터를 **연주자 줄에 세웠더니** 악기 이름표를 덮었고, 밴드 한 명처럼
> 보였다 — 얘는 연주자가 아니라 「지금 어느 대목인가」다. **관객석**으로 내렸다.

> 크기를 `Positioned` **안의** `LayoutBuilder` 로 쟀다. 왼쪽·아래만 묶었으니 높이가
> 무한대로 온다 — 「무대 높이에 맞춘다」는 주석만 남고 실제로는 늘 최대값이었다.
> **코드가 하지 않는 일을 적어 둔 주석**이다. 무대 높이는 `Stack` 바깥에서 잰다.

폰 확인: 인트로에서는 댄서가, 벌스로 넘어가면 고양이가 관객석에서 춤춘다.

## 곡 파일 가져오기 — 「파일로 꺼내기」의 짝 (2026-09-06)

꺼내기만 있으면 백업이 반쪽이다. 폰을 바꾼 사람이 카톡에 보관해 둔 곡 파일을
**다시 열 길이 없었다.** `file_picker` 를 넣고 `Store.importJson` 을 붙였다.

가져오기는 **잃는 쪽으로 틀리기 쉬운** 기능이라 규칙을 셋 못 박았다:

- **절대 덮어쓰지 않는다.** 이름이 같아도 늘 새로 만든다 — 「가져왔더니 내 곡이
  없어졌다」보다 나쁜 결과는 없다. 이름이 겹치면 뒤에 번호를 붙인다.
- **곡 파일이 아니면 아무것도 안 만든다.** 고르는 창에서는 사진도 고를 수 있다.
  확장자로 거르지 않고(카톡·드라이브를 거치면 확장자가 바뀐다) **내용**을 본다 —
  `scenes` 와 `tracks` 가 목록이어야 곡이다.
- **가져오기 직전에 만지던 것을 흘리지 않는다.** 열기 전에 저장한다.

시험(`import_check_test`)이 그 셋을 그대로 잰다 + 껐다 켜도 남는가(목록에 안
적혔으면 다음에 앱을 켤 때 사라진다) + 목록에 보여 줄 길이(`sec`)가 채워지는가.

버튼은 목록 **아래 한 줄**로 뒀다 — 위 줄(정렬 칩 + 「새 곡」)에 넣으면
320dp 폰에서 자리를 다툰다.

> **빌드가 막혔다.** `file_picker` 는 자기를 `compileSdk 34` 로 고정해 두는데,
> 그것이 끌고 오는 `flutter_plugin_android_lifecycle` 2.0.35 가 **36 이상**을
> 요구한다. 루트 `android/build.gradle.kts` 의 하위 모듈 바닥값을 34 → 36 으로
> 올려 풀었다(주석도 같이 고쳤다 — 「34 로 맞춘다」고 적힌 채 36 을 하면 그게
> 다음 사람을 속인다). `compileSdk` 는 **무엇으로 컴파일하는가**일 뿐이고
> 폰에서 도는 방식은 app 모듈의 `targetSdk` 가 정한다 — 그건 안 건드렸다.

> `file_picker` 는 **12 로 못 올린다** — 12는 win32 ^6 을 요구하는데 share_plus ^10
> 은 win32 ^5 를 쓴다. share_plus 를 13 으로 올리면 풀리지만 거기서
> `Share.shareXFiles` 가 없어져 「파일로 꺼내기」를 다시 써야 한다. 8.3.7 로 둔다.

`flutter test` **74개 파일 전부 통과**, `flutter analyze lib` 무결점.
APK 는 빌드·설치까지 했으나 **폰이 잠겨서 화면 확인은 못 했다**(잠금 해제는 안 한다).

## 타임라인을 여러 줄로 — 씬 줄 + 악기 줄 (2026-09-06)

옛 앱(뮤직 두들)의 타임라인을 가져왔다. 마디 눈금 · 씬 줄 · **악기별 트랙 줄**.
빈 칸을 누르면 그 자리에만 다른 판이 들어가고, 재생할 때 **그 마디에서는 씬 것을
이긴다**(옛 앱 `fireStep` 의 규칙 그대로).

- **자리를 잡는 법이 다르다.** 옛 앱은 「곡 전체 몇 마디째」였다. 여기서 그러면
  구간을 하나 끼우거나 판 수를 늘리는 순간 뒤엣것이 전부 어긋난다 — 씬 번호가
  밀리는 것과 똑같은 병이다. 그래서 **(구간, 구간 안 몇 마디째)** 로 잡는다.
  구간을 옮기면·베끼면·지우면 클립이 따라간다(`_remapLanes`).
- **씬 클립과 트랙 클립이 같은 코드를 쓴다**(`SceneSequencer._emitClip`).
  두 벌을 들고 맞추면 언젠가 한쪽만 고쳐진다.
- 클립이 하나도 없으면 소리가 **한 톨도** 안 바뀐다(시험 1번) — 새 길이 옛 곡을
  건드리면 안 된다. 옛 파일에는 `lanes` 칸이 아예 없다.
- 칸마다 위젯을 만들지 않는다. 120마디 × 7줄이면 840개다 — 옛 앱이 보급형 폰에서
  그걸로 밀렸다. **격자는 그림 하나, 줄마다 손잡이 하나**로 두고 눌린 x 로 마디를 센다.

> `python3` 로 「클래스 시작 ~ 다음 클래스」를 잘라 갈아 끼우다가 그 사이에 있던
> **최상위 함수까지 같이 날렸다**(`pickPatternSheet`·`_Bar`·`_BlockBar`·`_sceneColor`).
> 분석기가 바로 잡아 줬고 세션 기록에서 되살렸지만, 잘라내기 전에 **그 범위에
> 무엇이 들어 있는지 먼저 봐야 한다.**

## 곡 목록이 손가락 밑에서 움직이지 않는다 (2026-09-06)

「최근」 정렬은 곡을 열 때마다 다시 늘어선다 — 곡을 열면 앞 곡이 저장되면서
시각이 갱신되기 때문이다. 두어 번 오가면 누르려던 곡이 딴 자리에 가 있어서
**엉뚱한 곡을 연다.** 자리는 **들어올 때(그리고 정렬을 바꿀 때)만** 정하고,
그 뒤로는 새로 생긴 곡만 앞에 붙이고 없어진 곡만 뺀다.

시험은 「순서가 그대로다」와 **「얼리지 않았다면 정말 바뀌었을 것」**을 같이 본다 —
안 바뀌는 판이면 앞 검사는 늘 통과한다(재는 시늉).

## 악기를 종류가 아니라 이름으로 (2026-09-06)

여태 「＋악기」 줄은 **드럼·베이스·코드·멜로디** 넷을 내놨다. 그건 이 앱의
속사정이지 사람이 아는 말이 아니다 — 기타를 넣고 싶은 사람이 「코드인가
멜로디인가」부터 답해야 했다.

- 줄에 있는 것은 **「악기 추가」 하나**. 누르면 피아노·기타·벨·색소폰… 스무 가지가
  이름으로 나오고, **고르면 종류가 따라온다**(`kInstrumentPicks`).
- 타입 이름도 고쳤다: 코드 → **화음**, 멜로디 → **가락**.
- 편집기의 코드 줄은 이제 **코드 이름**(Am·F)과 **느낌**(활짝·쓸쓸함)을 적는다.
  옛 앱의 `DEG_MOOD` 를 `theory.dart` 로 가져왔다 — 「4도」는 악보를 아는 사람의
  말이고, 「활짝」은 아무나 아는 말이면서 고를 때 실제로 도움이 된다.

`flutter test` **76개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 코드 서랍 · 재생선 · 마무리 (2026-09-06)

**코드 서랍** — 엔진은 처음부터 코드 스물넷을 알고 있었는데 고를 자리가 여섯뿐이라
직접 찍는 코드는 늘 밋밋했다. 편집기 코드 줄에 「더…」를 붙였다.

- 칩에 적히는 것이 `m7` 이 아니라 **그 조에서 무슨 코드가 되는지**다(`Am7`).
  「min7 로 바꾸시겠어요」는 아무 뜻이 없다.
- 표가 셋이 됐다 — `kChordTypeIntervals`(소리)·`kChordSuffix`(이름)·`kChordGroups`(서랍).
  셋이 어긋나면 화면엔 뜨는데 소리가 안 바뀌거나 이름이 빈다. **오류는 안 난다** —
  그래서 `chord_palette_test` 가 셋을 맞춰 본다(숨은 코드 0 · 이름 24 · 소리 23종 바뀜).

> 「고르면 소리가 바뀐다」를 스물넷 **전부**에 걸었더니 `min` 에서 실패했다 —
> C단조 1도는 원래 `min` 이라 안 바뀌는 게 맞다. 시험이 틀렸지 코드가 틀린 게
> 아니었다. **그 도수의 원래 성질은 빼고** 잰다.

**타임라인 재생선** — 타임라인인데 지금 어디를 지나는지 안 보이면 줄만 늘어놓은
표다. 초→마디는 씬마다 빠르기가 달라 딱 비례하지 않으므로 **구간을 찾아 그 안에서만**
비례로 나눈다(`timelineHeadX` — 화면을 안 세우고도 시험이 잰다).

> 격자 그림이 `sectionStarts.length` 만 견주고 있었다. 구간 **길이**를 바꾸면
> 경계 자리는 옮겨지는데 개수는 그대로다 — 진한 줄이 옛 자리에 남는다.
> `setEquals` 로 고쳤다. **개수만 견주는 비교**는 이 저장소의 단골이다.

> 판 수를 줄이면 클립이 구간보다 뒤로 밀린다. 소리는 안 나는데(시험 6번) **화면에는
> 다음 구간 위에 그려지고 있었다.** 구간 안으로 자르고, 아예 밖으로 나간 것은
> 흐리게 「판 밖」으로 남긴다 — 지우는 것은 사용자 몫이다.

`flutter test` **77개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 한 판에 화음과 낱음 · 프로 모드 (2026-09-06)

**한 판에 둘 다** — 옛 앱은 클립 하나가 `lanes.chord` 와 `lanes.rows` 를 같이 들었다.
패드로 화음을 깔면서 그 위에 낱음 몇 개를 얹는 것이 한 판 안에서 됐다.
여기서는 트랙 종류가 본줄을 정하니 **반대쪽을 `NotePatternDef.also` 에 담는다.**

- 씬 클립과 트랙 클립이 그랬듯, 덧줄도 **같은 만드는 이**를 쓴다 —
  `alsoDef` 를 반대쪽(`buildRowsPattern`/`buildChordPattern`)에 그대로 넘긴다.
- 손잡이는 **줄 이름 칸 위 빈 자리**에 넣었다. 위쪽 바에 얹었더니 세로 폰에서
  줄이 하나 늘어 **격자가 그만큼 줄었고**(가로에서는 41px 넘쳤다), 무엇보다
  격자를 아래로 밀어 시험의 칸 좌표가 전부 어긋났다. 그 자리는 원래 비어 있고
  「지금 어느 줄을 보고 있나」가 붙을 자리이기도 하다.
- 반대쪽에 음이 있으면 개수를 적는다(`·3`) — 모르면 「소리가 하나 더 나는데
  어디서 나지」가 된다.

**프로 모드** — 도수 줄은 조에 맞는 음만 낸다. 블루 노트도 지나가는 반음도 못 찍는다.
설정에서 켜면 편집기 줄 손잡이를 **꾹 눌러** 반음 줄(25줄)로 바꾼다.

- 자리를 **절대 음이 아니라 으뜸음에서 몇 반음**으로 잡는다(`semiFreq`).
  옛 앱은 절대 음이라 조를 바꿀 때 찍어 둔 것을 통째로 옮기는 코드가 따로 있었다 —
  여기서는 도수와 똑같이 **조를 따라 저절로 옮겨 간다.**
- 켤 때 찍어 둔 것을 **좌표계째 옮긴다**(`NoteOps.toChromatic`/`toDegrees`).
  깃발만 바꾸면 도수 7(한 옥타브)이 반음 7(5도)이 되어 **가락이 소리 없이 딴
  가락이 된다.** 되돌리기에도 쌓는다.
- 기본은 꺼짐이다. 그 제약이 「아무거나 눌러도 어울린다」를 만들어 준다(§15).

> 프로 모드가 꺼졌을 때 `onLongPress: null` 로 뒀더니 **꾹 누른 것이 탭으로 흘러가
> 줄이 바뀌었다.** Flutter 는 길게 눌러도 탭을 쏜다 — 손잡이는 늘 달고, 꺼져
> 있으면 「설정 ▸ 프로 모드」를 알려 준다. 시험이 잡았고, 그 김에 검사 하나가
> **엉뚱한 이유로 통과하고 있던 것**도 드러났다.

`flutter test` **79개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 텐션 · 슬래시 베이스 (2026-09-06)

엔진은 처음부터 텐션(`ChordSpec.tensions`)과 슬래시 베이스(`bass`)를 낼 수 있었는데
**적을 자리가 없었다.** 자리를 새로 만들지 않고 **이미 있는 칸**(노트의 5번째,
처음부터 글자였다)에 규칙만 얹었다:

    min7 · min7+t9 · maj7/4 · min7+t9+t11/2

칸을 안 늘리니 **저장 형식이 그대로다** — 옛 파일도 옛 앱도 그냥 읽는다.
슬래시 베이스는 절대 음이 아니라 **도수**(0~6)라 조를 바꾸면 같이 옮겨 간다.

서랍 맨 위에 **지금 무슨 코드가 되는지**를 큼직하게 적는다(`Am7(9)/C`) —
무엇을 만들고 있는지 그 자리에서 알아야 고를 수 있다.

> 종류 칩(`m7`·`M7`…)이 **덧음·밑음을 지우고 있었다.** 서랍에서 공들여 붙인
> 텐션이 칩 한 번에 날아간다 — 짝이 하나 없는 그 모양이다. 칩도 지금 붙어 있는
> 것을 들고 다시 쓴다.

**박자(3/4·6/8)는 안 한다.** `kStepsPerBar = 16` 이 패턴 데이터·시퀀서·편집기·
타임라인에 다 박혀 있고, 라이브러리 판이 전부 16칸으로 쓰여 있다 — 박자를 바꾸면
**있는 판이 전부 딴 리듬이 된다.** 얻는 것보다 부수는 것이 크다(§15).

`flutter test` **79개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 새 길이 옛 길을 밟는 자리 훑기 (2026-09-06)

반음 줄과 덧줄이 생기면서 **도수를 전제하던 곳**이 위험해졌다. 훑어서 셋을 고쳤다.

> **코드 진행이 반음 판을 도수만큼 옮기고 있었다.** 단조에서 1도→4도는 도수 3,
> 반음 5다 — 3도 올리려다 3반음만 올라가 화성이 어긋난다. **소리는 나므로 오류로는
> 안 잡힌다.** 옛 앱에 `progSemi` 가 따로 있던 이유가 이것이었다.

> 트랙을 바꿔도 **덧줄이 열린 채로 남았다.** 화음 판의 낱음 줄을 보다가 트랙을
> 바꾸면 딴 악기의 덧줄이 열린다 — 뭘 고치고 있는지 알 수 없다.

> 「멜로디에 맞추기」가 **반음 판을 도수로 읽고 있었다.** 같은 숫자라도 뜻이 다르니
> 엉뚱한 코드를 골라 준다. 도수로 옮겨서 넘긴다.

> 첫 안내에 **줄 하나를 더 적었더니 「시작하기」가 화면 밖으로 밀렸다.** 설명과 같이
> 굴러가고 있었던 것이다. 버튼을 바닥에 붙였다 — 안내는 앞으로도 길어질 텐데
> **누를 것이 안 보이면 안내가 아니다.**

`flutter test` **79개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 타임라인 클립 — 옮기기 · 복사해서 붙이기 (2026-09-06)

놓기와 지우기밖에 없었다. 하나 만들어 두고 **여러 자리에 붙이는 것**이 트랙 줄을
쓰는 제일 흔한 손짓이다(옛 앱의 `tlClip9` 자리).

- 클립을 누르면: 패턴 바꾸기 · **한 칸씩 옮기기** · **복사** · 지우기
- 복사한 뒤 **빈 칸을 꾹** 누르면 붙는다. 복사한 것이 없으면 꾹도 고르기와 같다 —
  빈손으로 꾹 눌렀을 때 아무 일도 안 일어나면 고장으로 보인다.
- **같은 트랙 것만** 붙는다. 종류가 다르면 엉뚱한 판이 실린다.

> 줄에 `onTapDown` 하나로 마디를 세고 있었는데, 꾹 누르기를 같이 달면 **탭이
> 300ms 늦어진다**(Flutter 가 두 번째 손짓을 기다린다). 눌린 자리는 `onTapDown` 이
> 적어 두고, 무엇을 할지는 `onTap`/`onLongPress` 가 정한다.
> — 타임라인 블록에서 `onDoubleTap` 을 뺐던 것과 같은 함정이다.

`flutter test` **79개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 타임라인이 재생을 따라간다 · 띠가 진실을 말한다 (2026-09-06)

- **재생선 따라가기.** 3분짜리 곡은 마디가 100을 넘는다 — 재생선이 화면 밖으로
  나가면 타임라인은 「지금 어디」를 말해 주지 못한다. 편집기와 **같은 규칙**을
  쓴다(`followTarget`): 보이는 폭을 벗어날 때만 왼쪽 15% 자리로 끌어오고,
  **손으로 민 뒤 5초는 가만히 있는다** — 뒷부분을 보려고 민 사람을 계속 앞으로
  끌어오면 아무것도 못 본다.
- **구간 띠가 트랙 줄까지 센다.** 씬에서는 쉬는 악기라도 그 구간에만 넣어 뒀으면
  **소리가 난다.** 안 세면 「드럼뿐」이라 적어 놓고 기타가 나온다.

`flutter test` **79개 파일 전부 통과**, `flutter analyze lib` 무결점.

> 폰에서 보고 잡은 것: 타임라인 클립에 **저장 이름**(`Ballad Chorus`)이 떴다.
> 고를 때 본 이름은 「발라드 코러스」다 — **같은 것인 줄 모른다.**
> 줄·메뉴·알림 셋 다 `patternLabel` 로 맞췄다.
>
> 폰 확인: 마디 눈금 · 씬 줄(미니 피아노롤 포함) · 악기 줄 다섯(드럼·808 베이스·
> 패드·벨·플럭)이 뜨고, 빈 칸 → 패턴 서랍 → 클립 놓기 → 클립 메뉴(바꾸기·한 칸씩
> 옮기기·복사·지우기)까지 다 돈다.

## 폰에서 보고 고친 것 — 이름은 **읽는 이름**으로 (2026-09-06)

폰에 올려 놓고 눌러 보니, 저장에 쓰는 글자가 화면에 그대로 새고 있었다.

- 타임라인 클립: `Ballad Chorus` → **발라드 코러스**(고를 때 본 이름).
  줄·클립 메뉴·알림 셋 다 `patternLabel` 로 맞췄다.
- 편집기 음 막대: `min7` → **Fm7**. `min7` 은 이 앱의 속사정이고, 막대에 필요한
  것은 읽는 이름이다. 텐션·밑음이 붙으면 그것도 같이 — `Fm7(9)/Ab`
  (`chordBarLabel`).

폰 확인(패드 판): 줄 이름이 **Bb 7도·뭉클 / Ab 6도·활짝 / … / Cm 1도·중심** 으로
뜨고, 왼쪽 위 손잡이를 누르면 **낱음 ·12**(반대쪽 음 개수)로 바뀌면서 15줄
음이름 판이 열린다. 다시 누르면 화음 판으로 돌아오고 「멜로디에 맞추기」도 같이
돌아온다. 씬 화면은 **드럼 / 베이스 / 화음 / 가락** 과 「＋ 악기 추가」 하나.

> 시험 하나가 틀려 있었다: `min7/4` 를 `Cm7/F` 로 적어 뒀는데 **도수는 0부터 센다** —
> 4번은 5도(G)다. 코드가 아니라 내가 틀렸다.

`flutter test` **79개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 내보내기도 새 길을 태우는가 (2026-09-06)

트랙 클립·덧줄·반음 줄은 전부 `buildSong` 을 거치므로 내보내기에 **자동으로**
실린다. 「자동으로 실린다」는 **믿을 것이 아니라 재는 것**이다 — 재생과 내보내기가
갈라지면 「폰에서는 나는데 파일에는 없다」가 된다.
`export_check_test` 에 넷을 더했다: 트랙 클립 · 덧줄 · 반음 줄(그 주파수까지) ·
**내보내기 일감이 재생과 같은 목록인가**.

## 타임라인에서 바로 그 씬 고치기 (2026-09-06)

곡을 만들다 보면 「이 대목 패드를 좀 바꾸자」가 계속 나온다. 여태는 **뒤로 →
만들기 → 씬 고르기** 세 걸음이었다 — 세 걸음이면 안 고치게 된다.
구간을 고르면 손잡이 줄에 ✎ 가 생기고, 누르면 **그 씬을 걸고** 만들기로 간다.

화면을 여는 일은 곡 화면이 못 한다(집이 안다) — 그래서 `onEditScene` 으로
부탁만 하고, 집이 `launchScene` 뒤에 연다. **씬을 안 걸면 엉뚱한 씬이 열린다.**

> 손잡이가 아홉이 되니 320dp 폰에서 넘쳤다. 이름이 먼저 줄고, 그래도 모자라면
> 손잡이 줄이 **옆으로 흐른다** — 잘라 버리면 작은 폰에서만 못 쓰는 손잡이가 생긴다.
>
> **넘침 검사가 여태 이 줄을 못 보고 있었다.** 구간을 골라야 나오는 줄이라
> 「고르지 않은 화면」만 재고 있었던 것이다. 고른 채로 320dp 를 재는 검사를 뒀다.

`flutter test` **79개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 막 써 보기가 타임라인 줄을 잡았다 (2026-09-06)

「막 써 보기」(씨앗 고정 무작위 4000걸음)에 **타임라인 클립 놓기·지우기**를 손짓으로
더하고, 불변식 하나를 더했다: **클립이 있는 구간·있는 트랙·있는 판을 가리키는가.**

돌리자마자 **진짜 고장 둘**이 나왔다. 둘 다 「짝이 하나 없는」 그 모양이다.

> **트랙을 지우면 그 줄의 클립이 남았다.** 없는 악기를 가리키는 클립이 되는데,
> 화면에는 그 줄이 아예 없으니 **지울 길도 없다.** 트랙과 같이 걷고, 되돌릴 때
> 같이 돌려놓는다(그 사이에 구간이 줄었으면 버린다).

> **스타일을 바꿔도 남았다.** 스타일 하나에 트랙·씬·구간이 통째로 갈리는데
> 클립만 옛 번호를 들고 있었다. `_seedOrder` 에서 비운다 —
> 「스타일 바꾸기」는 `GenreUndo` 로 되돌릴 수 있으니 잃는 것이 아니다.

무작위가 아니었으면 **둘 다 안 나왔을 것**이다. 씬 지우기·구간 옮기기는 손으로도
짚어 봤지만, 「클립을 놓고 → 스타일을 바꾸는」 순서는 생각해 본 적이 없었다.

`flutter test` **79개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 막 써 보기를 넓혔다 — 덧줄·반음 줄·코드 진행·캐릭터 (2026-09-06)

손짓 넷을 더하고(덧줄 얹기/비우기 · 반음 줄 켜고 끄기 · 코드 진행 바꾸기 ·
캐릭터 바꾸기), 불변식 하나를 더했다: **음이 제 좌표계 범위 안인가**
(도수 0~14 · 반음 0~24 · 화음 0~6, 덧줄은 본줄과 **반대**).

돌리자마자 또 잡혔다 — 이번엔 **시험 제 코드가 저지르고 있던 실수**였다.

> 「음 찍기」가 판을 **네 칸짜리 생성자로 새로 짜고** 있었다. `also` 와
> `chromatic` 을 안 옮기니, 반음 줄로 찍어 둔 판에 음을 하나 더하는 순간
> **깃발이 꺼지고 반음이 도수로 읽혔다**(가락이 소리 없이 딴 가락이 된다).

시험만 고치면 다음에 또 난다. **구조로 막았다** — `NotePatternDef.copyWith` 를
두고, 판을 베끼는 자리는 전부 「바꿀 것만 적는다」로 바꿨다
(`changeChord` 두 곳 · 편집기 `_writeNotes` · 막 써 보기 세 곳).
안 적은 칸은 저절로 따라온다.

`flutter test` **79개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 록 기타는 파워코드를 친다 (2026-09-07)

파워코드는 **3음이 없다**(뿌리 + 5도 + 옥타브). 왜곡을 걸면 3음의 배음이 5도와
맥놀이를 일으켜 지저분해진다 — 그래서 록 기타는 3음을 뺀다. 뺀 자리는 베이스와
보컬이 채운다. 3음을 넣으면 그 자리에서 록이 아니게 된다.

- `kChordTypeIntervals['five'] = [0, 7, 12]` · 이름표 `5`(`C5`) · 서랍 「기본」에 넣었다.
- **스타일이 아니라 악기가 정한다**(`genreChordType`) — 록이어도 피아노가 화음을
  맡으면 3음이 있어야 한다. 파워코드는 「록의 소리」가 아니라 **기타의 손 모양**이다.
- **적어 둔 코드가 이긴다**(`buildChordPattern` 의 `plainType`) — 종류를 직접
  고른 음은 안 건드린다. 고른 것을 스타일이 덮으면 고를 이유가 없다.

> **록의 화음은 패드가 내고 있었다.** 스타일 설명에 「기타가 앞에」라고 적혀 있고
> 화음 판 이름도 `Rock Power` 인데 정작 음색이 패드였다 — **글로만 기타였다.**
> `kGenreTypeVoice` 를 두고 록 화음을 기타로 돌렸다. 사용자가 음색을 고른 뒤에는
> 안 건드리고(`_voiceIsAuto`), 다른 스타일로 가면 그 스타일 것으로 돌아온다.

> 크로마 시험이 「Eb 를 적었는데 안 들린다」고 했다 — **애초에 Eb 를 적은 적이 없다.**
> 3음이 없는 코드다. 시험이 「적어 놓은 것」을 셀 때 **스타일이 정해 주는 종류**를
> 안 보고 있었다. 소리가 아니라 자가 틀린 쪽이다.

시험은 표가 아니라 **나가는 주파수**를 본다: 화음 8개 전부에 3음이 없고 5도는 있다.

`flutter test` **80개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 첫 화면 — 옛 앱처럼 「질문에 답해서 곡 만들기」 (2026-09-07)

옛 앱 첫 화면의 머리 항목을 가져왔다. **음악 용어를 하나도 안 쓰고 열 가지만 묻는다**
— 기분·장소·움직임·꽉참·앞 악기·저음·드럼·공간·길이·거칢.

- **0에서 만들지 않는다.** 답을 모아 **어떤 완성곡을 뼈대로 쓸지** 고르고(팝·프로그
  하우스·트랩·재즈·엠비언트 다섯), 그 위에 조성·빠르기·길이·편성·믹스를 바꾼다.
  검증된 곡을 변형하는 것이라 **「음악이 아닌 것」이 안 나온다** — 이 앱에서 제일
  나쁜 결과가 그것이다.
- 길이는 **초**로 맞춘다(`fitToLength`). 마디로 세면 느린 곡을 「짧게」로 골랐는데
  4분이 나온다.
- 값만 정하는 곳(`ask_song.dart`)과 화면(`ui/ask_sheet.dart`)을 갈랐다 —
  시험이 화면 없이 답만 넣어 결과를 잰다.

시험은 **답이 정말 곡을 바꾸는가**를 잰다: 빠르기 135>116>100>84 · 길이
124/207/281초 · 저음·드럼 크기·잔향이 답 순서대로 · 악기 수 7/6/5 ·
**답 33가지 어느 것을 골라도 소리가 나고 주파수가 성하다.**

> 옛 앱처럼 세 줄(질문·완성곡·빈 프로젝트)을 다 넣었더니 **아래 네 칸 중 둘이
> 화면 밖으로 밀렸다.** 시작할 길을 열어 주려다 **길 자체를 가린** 셈이다.
> 한 줄만 남기고, 그마저 **누운 화면에서는 안 낸다**(800×400 에서는 그 한 줄에도
> 밀린다). 나머지 둘은 이 앱에 이미 길이 있다 — 스타일 고르기 · 내 곡 ▸ 새 곡.

`flutter test` **82개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 두드려 넣기 — 박자는 손으로, 높낮이는 나중에 (2026-09-07)

어려운 박자는 손으로 못 찍는다. 머릿속 리듬을 16칸 격자의 몇 번째 칸으로 옮겨
적으려면 그 리듬을 **이미 셀 줄 알아야** 하는데, 셀 줄 알면 애초에 어렵지 않다.
그래서 순서를 바꿨다 — **박자는 두드려 넣고, 높낮이는 나중에 끌어 올린다.**
사람이 한 번에 하나씩만 하면 되는 일이 된다.

편집기 안에 둔다(따로 화면을 만들지 않았다). 두드린 결과가 **그 자리에 나타나고**
바로 위아래로 끌어 고칠 수 있어야, 두 단계가 한 흐름이 된다.

- 한 마디 미리 세기(4→3→2→1) → **판 한 바퀴만** 돌고 자동으로 끝난다.
  계속 겹쳐 담게 하면 「언제 그만두지」를 사용자가 정해야 한다. 한 바퀴로 끊으면
  **끝이 저절로 온다** — 마음에 안 들면 다시 누르면 되고, 되돌리기가 있다.
- 미리 세기는 **메트로놈을 꺼도 들린다.** 그건 자가 아니라 출발 신호다.
- 두드릴 판: 가락·베이스·화음은 하나, 드럼은 넷(쿵·탁·치·둥). 드럼만 나눈 이유는
  **소리마다 줄이 다르기 때문**이다 — 한 줄에 다 찍으면 나중에 나눌 수가 없다.
- 칸 맞추기 기본은 **8분**이다. 초보가 두드리면 16분으로는 거의 늘 어긋나 옆 칸에
  붙는다 — **친 대로 들리는 쪽**이 정확한 쪽보다 낫다.
- 음 길이는 **누르고 있는 만큼**. 「따— 딴딴」이 그대로 들어간다.
- 처음 높이는 **그 자리에 흐르는 코드의 뿌리음**이다. 한 줄에 다 찍으면 「박자만
  넣었다」가 눈에는 보이지만 들으면 한 음이 반복된다 — 방금 친 리듬이 음악으로
  안 들린다. 코드 위에 놓으면 그 자리에서 이미 어울려서, 높낮이를 고치는 일이
  **「틀린 것 고치기」가 아니라 「더 낫게 만들기」**가 된다.

> **세로 끌기를 세기에서 높낮이로 바꿨다.** 두드려 넣기가 생기면서 「박자는 맞는데
> 아직 제자리가 아닌 음」이 한 판에 열 개씩 생긴다. 그걸 고치는 길이 「지우고 옆
> 줄에 다시 찍기」 두 번이면 두드린 보람이 없다. 옮기는 것이라 **길이·세기가 따라온다**
> (다시 찍으면 둘 다 초기값으로 돌아간다). 세기는 아래 바의 여리게·보통·세게가
> 맡는다 — 거기 이미 있었다. 끄는 방향은 **처음 움직인 쪽으로 잠근다**: 안 잠그면
> 길이를 늘리다 손이 조금 기울 때 음이 딴 줄로 튄다.

> **메트로놈을 라이브 버스로 옮겼다.** 여태 드럼 킷의 'rim' 이었다 — 그러면 믹서에서
> 드럼을 줄일 때 자까지 같이 작아지고, 「반주 끄기」로 트랙을 다 내리면 **자가 통째로
> 사라진다.** 박을 세는 소리는 곡의 일부가 아니라 **곡을 재는 소리**다.
> 라이브 화면의 메트로놈도 같이 옮겼다 — 한 앱에 메트로놈 소리가 둘이면 안 된다.

> **호스트가 없으면 아예 안 열리게 막아 뒀다가 풀었다.** 막다른 길이다. 열되
> 「소리를 아직 못 켰습니다 — 소리가 나야 박을 셉니다」라고 **이유를 말한다.**
> 기다려도 안 오는 것을 기다리게 두면 안 된다.

시험은 표가 아니라 **자리와 주파수**를 본다: 두드린 칸이 재생 막대가 가리키는 칸과
2000자리에서 **한 칸도 안 어긋난다**(`headStep` 과 맞대 본다 — 「말없이 어긋난 두 표」가
제일 나기 쉬운 자리다) · 지연 64ms 를 되돌리면 0칸, 안 되돌리면 1칸 밀린다 ·
8분으로 맞추면 홀수 칸이 0개 · 두드린 음이 **그 자리 코드 안의 음**이다(조 4개 ×
장단 2개, 옥타브를 접어 주파수로 견줌) · 두드린 16개가 시퀀서에서 **소리 16개**로 나온다.

> **막 써 보기에 두드려 넣기·높낮이 옮기기를 넣자마자 딴 것이 잡혔다** — 캐릭터다.
> 저장 전 `critter: null` 이 저장 후 `'@dancer'` 로 **되살아났다.**
> 옛 파일을 채워 주는 규칙이 「하나도 없으면 옛 파일」이었는데, 그러면 **씬이 하나뿐인
> 곡에서 그 하나를 떼면 늘 옛 파일로 보인다.** 주석에는 「일부러 뗀 것은 안 되살린다」고
> 적혀 있었지만 코드는 그걸 못 하고 있었다(**코드가 하지 않는 일을 적어 둔 주석**).
> 값이 아니라 **칸의 유무**(`containsKey('critter')`)로 가른다 — 옛 파일에는 칸 자체가
> 없고, 새 파일에는 값이 null 이어도 칸이 있다.

`flutter test` **85개 파일 전부 통과**, `flutter analyze lib` 무결점.

## 홀수 박자 — 그릇까지 됐고 판이 없다 (2026-09-07, 진행 중)

「홀수박 넣어」로 시작했다. **엔진 쪽 배선은 절반 끝났고, 판 라이브러리는 한 곡도
안 썼다.** 지금 `meter` 를 '3/4' 로 바꿔도 편집기 격자는 16칸이고 고를 판이 없다.

### 된 것

- **`lib/meter.dart`** — 4/4 · 3/4 · 6/8 · 5/4 · 7/8.
  칸은 **어느 박자에서나 16분음표**로 고정이다(`stepSec` 이 안 바뀐다). 마디에
  그 칸이 몇 개인지만 달라진다 — `beats * (16 ~/ unit)`, 옛 앱 `meterSteps()` 와 같은 셈.
  메트로놈은 `clickSteps`(몇 칸마다) + `strongAt`(세게 치는 칸)으로 적는다:
  6/8 은 여섯 번 치고 **1·4 를 세게**(둘로만 치면 초보가 그 사이를 못 채운다),
  7/8 은 **2+2+3**(고르게 세면 마디 머리가 안 들린다 — 홀수 박자에서 제일 먼저 잃는 것).
  시험 19개(`meter_check_test`)가 표를 잰다. 그중 둘이 특히 중요하다:
  **강세가 메트로놈이 실제로 치는 칸 위에 있는가**(아니면 적어만 두고 안 들린다),
  **마디를 넘어도 마디마다 「하나」가 제자리인가**.
- **판이 제 박자를 들고 다닌다** — `NotePatternDef.spb` · `DrumPatternDef.spb`(기본 16).
  타일링(`tileNoteList`·`tileDrumHits`·`buildDrumPattern`)이 **곡이 아니라 판 자신의**
  마디 길이로 민다. `makeEditable` 도 박자를 들고 온다.
  저장은 **4/4 가 아닐 때만** 적는다(`vels` 와 같은 규칙) — 안 적으면 3/4 판이
  저장했다 열 때 4/4 로 되살아난다(캐릭터 버그와 같은 종류라 처음부터 넣었다).
- **`Project.meter` / `Project.spb`** · **`GenreDef.meter`**.
  **곡 하나는 박자 하나다** — 마디 길이가 트랙마다 다르면 마디라는 것이 없어진다
  (씬·구간·타임라인이 전부 마디로 센다). 판은 제 박자를 들되, 곡에 실릴 수 있는 것은
  곡의 박자와 맞는 판뿐이다(고르는 화면이 걸러야 한다 — **아직 안 만들었다**).
  `buildSong`/`buildObjectSong` 은 Project 를 안 받고 장르만 받아서 **스타일이 박자를 정한다.**
- 배선: 시퀀서 6군데 · `song.dart` 9군데. **`flutter test` 86개 파일 전부 통과** —
  4/4 곡이 한 칸도 안 달라졌다는 뜻이다(이게 배선의 검사자다).

> 파이썬으로 치환할 줄을 셀 때 **12칸 들여쓰기 줄이 10칸 문자열을 통째로 품고 있어**
> 4개가 7개로 세어졌다. 그대로 갈았으면 `buildObjectSong` 이 `spb` 선언 전에 그걸 써서
> 컴파일이 깨졌다. **깊은 것부터** 치환한다.

### 남은 것 (다음 세션 순서)

1. **편집기 격자** — `lib/ui/editor_view.dart` 8군데. `_GridRow`·`_Cell`·`_SelBar` 에
   `MeterDef` 를 넘기고, `_posLabel` 이 `meterBeatAt` 을 쓰게 한다.
   `headStep(pos, {steps, loopBars})` 에 `spb` 를 더한다(기본 `kStepsPerBar` — 시험이 이미 쓴다).
2. 나머지 자리: `variation`·`arrange`·`prog_ops`·`harmony`·`ui/live_view`·`tap_rec`·
   `ui/tap_sheet`·`ui/song_view`. `grep -rn kStepsPerBar lib` 로 그대로 나온다.
   두드려 넣기의 미리 세기는 **4박 고정**이다(`TapClock(countBeats: 4, beatsPerBar: 4)`) —
   박자의 `clicksPerBar` 를 받아야 3/4 에서 셋을 센다.
3. **3/4 · 6/8 판 라이브러리** — 드럼·베이스·코드·가락. **이 일의 대부분이다.**
   격자를 12칸으로 만드는 것은 배선이고, **왈츠처럼 들리는 판을 쓰는 것이 진짜다.**
   옛 앱이 딱 여기서 멈춰서 홀수박이 고장 난 채로 남았다(격자만 12칸, 판은 16칸 그대로 →
   3/4 에서 판을 고르면 12~15칸이 다음 마디로 넘쳤다 → 그래서 박자를 바꾸면 곡을 지웠다).
4. 왈츠(3/4) · 6/8 발라드 스타일, 박자 고르는 화면, 판 고르기를 박자로 거르기.

### 함께 미뤄 둔 것

- **악기 품질** — 가설만 있고 측정을 못 했다. `synth.dart:248` 에서 필터 엔벨로프가
  `PLUCKY[voice] == true` 인 악기에만 걸린다. 패드·스트링·브라스·리드·오르간은
  `_cutFrom == _cutTo` 라 **음이 시작해서 끝날 때까지 음색이 한 번도 안 움직인다.**
  곁들여: `VG = {1:0.30, 2:0.60, 3:1.0}` 로 세기가 3단계뿐이라 다이내믹이 계단식이고,
  `bright = 0.66 + 0.34*(vel/3)` 라 세기 1과 3의 밝기 차가 좁다.
  다음 할 일은 **재는 것**이다 — `chroma_check_test` 의 고에젤 방식을 그대로 써서
  지속음 악기의 스펙트럼 중심이 시간에 따라 변하는가, 세기 1→3 에서 음색이 변하는가
  (크기만 변하면 가짜다). 숫자를 보고 나서 무엇을 고칠지 정한다.
  **홀수박과 동시에 열지 않는다** — 어느 쪽 변화가 무엇을 바꿨는지 못 가린다.
- **폰 실기 확인** — 「두드려 넣기」를 아직 폰에서 못 눌러 봤다(USB 가 계속 빠졌다).
  APK 는 설치돼 있다. 잠금 해제된 채로 케이블만 꽂으면 바로 된다.

## 홀수 박자 — 배선 끝, 왈츠 하나 실제로 돈다 (2026-09-08)

지난 세션이 남긴 「남은 것」 네 개 중 1·2 를 끝내고, 3·4 는 **왈츠(3/4) 하나로
끝에서 끝까지** 뚫었다 — 6/8 발라드는 아직이다.

### 1·2. 배선 (끝)

- `lib/ui/editor_view.dart` 8군데 — `_posLabel`·`headStep`·`_GridRow`·`_Cell`·
  `_SelBar` 가 전부 `widget.project.meterDef`(→ `MeterDef`)를 받는다.
  `steps`/`bars`/마디 자·마디 점프도 `kStepsPerBar` 대신 `spb`(그 곡의 박자).
- `grep -rn kStepsPerBar lib` 로 나온 나머지 전부: `arrange.dart`(`applyArrange`
  spb) · `prog_ops.dart`(`ProgSlot.meter`, `readProg` meter) · `harmony.dart`
  (`suggestChords` spb) · `variation.dart`(`varyDrums`/`varyMelodic`/`varyChord`
  spb — `_pushKick`·`_legato`·`_legatoChord` 가 「한 마디 안」을 그 박자로 잰다)
  · `tap_rec.dart`(`TapRecorder.spb`) · `ui/tap_sheet.dart`(`TapClock` 이
  `meter.clicksPerBar` 를 받는다 — 3/4 에서 진짜로 셋을 센다) · `ui/live_view.dart`
  · `ui/song_view.dart`. 전부 `project.spb`/패턴 자신의 `spb` 를 받아 쓰지,
  16 을 새로 박아 넣지 않았다.
- **`fill.dart` 에 숨어 있던 것 하나 더 잡았다** — `applyFill` 안의
  `const stepsPerBar = 16` 이 grep 에 안 걸렸다(리터럴이라 이름이 `kStepsPerBar`
  가 아니었다). 매듭(필인)이 마디를 16칸으로 잘못 세서, 왈츠에서는 매듭이
  **엉뚱한 자리에서 잘렸다** — `feel_reach_test` 의 「필 손잡이가 안 죽었는가」가
  잡았다(왈츠만 0.0%). `spb` 매개변수를 받게 고쳤다.

### 3. 왈츠(3/4) — 판 넷 + 장르 하나, 전 시험 통과

**"장르를 더할 때 표 하나만 빠뜨려도 조용히 반쪽짜리가 된다"**(`genres.dart`
머리 주석)는 이미 겪은 값이라, 왈츠도 그 표들을 하나도 안 빠뜨리고 채웠다:
`patterns.dart`(드럼·베이스·코드·멜로디 4개, 전부 `spb: 12`) · `genres.dart`
(`GenreDef(key:'waltz', meter:'3/4', ...)`) · `song.dart`(`kSongForms['waltz']`)
· `genre_mix.dart`(`kGenreMix`·`kStyleGain`) · `presets.dart`(`kStylePrefixes`)
· `pattern_names.dart`(영어 안 남게 'Waltz'→'왈츠') · `packs.dart`('느긋하게' 팩에).
`genre_check_test`·`pack_check_test`·`melody_check_test`·`pattern_names_test`
전부 통과 — 왈츠도 **30~270초짜리로 실제로 울린다.**

> **`Project.setGenre` 가 `meter` 를 안 옮기고 있었다.** `GenreDef.meter` 는
> 지난 세션에 만들어 뒀는데(「된 것」에 적혀 있었다), **그걸 실제로 읽어서
> `Project.meter` 에 앉히는 줄이 없었다.** 왈츠를 골라도 곡은 여전히 4/4로 셌다
> — 다른 시험은 이걸 안 잡았다(패턴이 소리는 낸다, 자리만 밀린다). 이제
> `setGenre` 가 `meter = genreDef(key).meter;` 를 한다.

> **되살리기(장르를 바꿔도 내가 고친 판은 지키는 길)가 박자를 안 봤다.**
> `_seedArray` 의 「mine」 되살리기는 씬·타입·순번만 맞으면 옛 판을 그대로
> 다시 얹었다 — 4/4 로 만든 내 베이스를 왈츠로 바꾼 뒤에도 그대로 얹으면
> 12칸 격자에 16칸짜리 음이 앉는다. **`rough_use_test`(막 써 보기, 4000걸음
> 무작위 조작)가 잡았다** — 손으로 짠 시나리오로는 절대 안 나올 조합
> (「패턴 만들기 → 스타일을 왈츠로 바꾸기」)이었다. 되살릴 판의 `spb` 가
> 지금 곡의 `spb` 와 다르면 안 되살리고 `parkedMine` 으로 센다.

> **패턴 고르기 목록이 박자를 안 걸렀다.** `SceneSequencer.patternNames`/
> `patternNamesFor` 가 라이브러리 전체를 다 보여줬다 — 4/4 곡에서 목록을 열면
> 왈츠 판도 뜨고, 고르면 12칸짜리 음이 16칸 격자에 얹혀 뒤가 넘친다. 이제
> `spb` 로 거른다(내 패턴도 마찬가지). 이게 아래 4번의 「판 고르기를 박자로
> 거르기」다 — 화면 쪽 손잡이는 없지만 **자료가 이미 걸러져서 나간다.**

`flutter test` **86개 파일 전부 통과**(`rough_use_test` 가 위 되살리기 버그를
잡아 준 뒤로도 다시 통과), `flutter analyze lib` 무결점.

### 남은 것

1. ~~**6/8 발라드**~~ — **끝났다(2026-09-08 이어서).** 아래 참고.
2. **박자 고르는 화면** — 지금은 `GenreDef.meter` 를 화면에서 고를 길이 없다
   (스타일을 고르면 그 스타일의 박자로 따라간다, `setGenre` 가 하는 일).
   「스타일이 아니라 박자를 먼저 고르고 그 박자의 스타일만 보여주기」는
   아직 없다 — `kMeters`(`meter.dart`)로 걸러서 `packedGenres()`/장르 목록을
   추리는 화면 손잡이가 남았다.
3. 위 되살리기·패턴 목록 필터는 **자료만** 고쳤다 — 편집기·씬 화면에
   「이 판은 지금 곡의 박자와 안 맞아 안 보여요」 같은 안내 문구는 없다.
   지금은 그냥 목록에서 조용히 빠진다(고장은 아니지만, 사용자가 「어라 아까
   그 판이 없어졌네」 할 수 있는 자리다).

## 6/8 발라드(흔들발라드) + makeEditable 의 마지막 16 (2026-09-08, 이어서)

같은 날 이어서 6/8 을 끝냈다 — 왈츠와 같은 틀이지만 `strongAt:[0,6]`(셋씩
두 갈래)이라 드럼·베이스 자리를 다시 짰다. 장르 키 `ballad68`(라벨 「흔들발라드」,
패턴 머리말 「Sway」) — `ballad`(4/4)와 이름이 겹치면 안 돼서 새 키를 썼다.

- 판 넷(`Sway Chorus`·`Sway Walk`·`Sway Keys`·`Sway Line`, 전부 `spb:12`) +
  6곳 표(`genres.dart`·`song.dart`·`genre_mix.dart`(믹스·`kStyleGain`)·
  `presets.dart`(`kStylePrefixes`)·`pattern_names.dart`(『Sway』→「흔들발라드」)·
  `packs.dart`('느긋하게' 팩에 추가) — 왈츠 때 세운 순서를 그대로 따랐다.
- `melody_clash_test` 가 **가락 하나가 조에 따라 다르게 부딪친다**는 걸 잡았다.
  왈츠 가락(장조)을 그대로 흔들발라드(단조)에 옮겨 썼더니, iv 코드(F–Ab–C, 단조는
  장3화음이 아니라 단3화음이라 Ab 가 낀다) 위에서 끄는 음(G)이 Ab 와 반음으로
  울었다 — **같은 도수·같은 패턴이라도 장조에서 맞던 자리가 단조에서는 아니다.**
  끝음을 도수4(G)에서 도수3(F, iv 의 뿌리음)으로 바꿔 고쳤다.

> **`makeEditable` 에 `kStepsPerBar`(16) 가 하나 더 숨어 있었다.** 물려받을
> 라이브러리 판이 없을 때(빈 클립을 고치기 시작할 때) 판·낱음 둘 다
> `spb: lib?.spb ?? kStepsPerBar` 로 **무조건 16을 깔았다** — 흔들발라드
> 곡에서 빈 트랙을 고치면 그 자리에서 만들어지는 「내 패턴」이 4/4 로 태어났다.
> `rough_use_test`(막 써 보기, 4000걸음)가 42번째 걸음에서 잡았다:
> 「스타일 ballad68 → (드럼 클립이 빈 채로 남는 경로를 거쳐) → 드럼 찍기」
> 순서라야 나오는 조합이라 손으로 짠 시나리오로는 못 봤을 자리다.
> `kStepsPerBar` 대신 **이 곡의 지금 `spb`**를 쓰게 고쳤다 — 이게 왈츠 세션에
> 고친 `_seedArray`(되살리기)·`patternNamesFor`(목록 거르기)와 **같은 종류의
> 구멍**이다: "라이브러리 판을 베낄 때"는 다 고쳤는데 "베낄 라이브러리 판이
> 아예 없을 때"의 기본값 하나를 빠뜨렸다.

`flutter test` **86개 파일 전부 통과**(이번에도 `rough_use_test` 가 마지막 구멍을
잡아 줬다), `flutter analyze lib` 무결점. 이걸로 홀수 박자 항목 1~3 이 끝났다 —
남은 건 위 2·3(박자 고르는 화면, 안내 문구)뿐이다.

## 박자 고르는 화면 + 안내 문구 (2026-09-08, 이어서)

같은 날 이어서 남은 둘을 끝냈다 — 「남은 것」 2·3 항목.

### 박자 고르는 화면

`lib/ui/create_sheet.dart`(「무슨 음악을 만들까요」/「스타일 바꾸기」 시트) 위에
**칩 한 줄**을 얹었다 — 「전부 · 네 박 · 세 박 · 여섯 박」. 숫자(3/4·6/8)가 아니라
`MeterDef.feel` 의 앞 토막(`'세 박 — 왈츠처럼...'` 에서 `'세 박'`)을 쓴다 —
이 화면은 처음부터 「용어를 안 쓴다」가 원칙이었다(계획 4-1). 고르면
`packedGenres()` 결과를 그 박자로 걸러서 보여준다 — 걸러서 **빈 팩**(예:
「세 박」만 고르면 「비트」 팩엔 하나도 안 남는다)은 소제목째로 숨긴다.

- 박자가 **하나뿐이면**(옛 파일들처럼 4/4 전용이면) 칩 줄 자체가 안 그려진다 —
  고를 게 하나뿐인 손잡이는 손잡이가 아니라 소음이다(`kMeters` 순서로 실제
  쓰이는 것만 추려서 판단한다).
- `tap_size_test`(누를 것 크기)가 **칩이 24px 로 눌린 걸** 잡았다 — 가로 스크롤
  `ListView.padding` 의 위·아래 값이 **cross axis extent를 그대로 깎아 먹는다**
  (세로 스크롤에서 위·아래 패딩이 하는 일과 다르다). `bottom:10` 을 리스트
  안쪽에 뒀다가 34짜리 칩이 24가 됐다 — 바깥 `Padding` 으로 옮겼다.
- `create_flow_test`가 `find.byType(ListView)`/`find.byType(Scrollable).first`
  로 장르 목록을 짚고 있었는데, 이제 그 화면에 가로 스크롤 `ListView`(칩 줄)가
  하나 더 있어서 **타입만으로는 어느 것인지 헷갈렸다.** 장르 목록에
  `key: Key('genreList')` 를 달고 시험 쪽도 그 키로 짚게 고쳤다 — 화면에 새
  스크롤 목록이 생기면 늘 나는 종류의 충돌이라 남겨 둔다.

### 안내 문구

`pickPatternSheet`(패턴 고르기 시트 — `scene_view.dart`·`song_view.dart` 세 자리에서
부른다)에 `meterHint` 를 더했다. 지금 곡이 4/4 가 아니면 부제 아래에
"『세 박』 곡이라 그 박자의 판만 보여요." 한 줄이 뜬다 — `SceneSequencer.
patternNamesFor` 가 목록을 이미 그 박자로 걸러 주고 있었지만(왈츠 세션에서 고침),
**왜 줄었는지 말은 안 하고 있었다.** 4/4 곡에는 지금까지와 똑같이 아무 말도
안 붙는다(`meterHint == null`).

`flutter test` **86개 파일 전부 통과**, `flutter analyze lib` 무결점. 이걸로
5단계 47/N(홀수 박자) 「남은 것」 넷이 전부 끝났다.

## 악기 품질 — 재 봤다, 아직 안 고쳤다 (2026-09-08, 이어서)

미뤄 뒀던 「악기 품질」을 처음으로 **재 봤다**(`tool/` 에 진단 스크립트를 짜서
돌리고 지웠다 — 임시였다). 결론부터: **첫 번째 재는 법이 틀렸었다.**

### 첫 시도 — 고에젤(정확한 배음 자리)로 재니 다 움직이는 것처럼 보였다

`chroma_check_test` 처럼 배음 1~8배 자리를 고에젤로 재서 「초반 vs 후반」·
「vel1 vs vel3」 무게중심을 봤다. 바이올린 −36.5%, 색소폰 −30%, 스트링 −33.5%,
보컬 −27.6%... **극적으로 움직였다.** `_cutFrom==_cutTo` 라 안 움직여야 하는데
왜 이렇게 크게 움직였을까 — 라고 생각했는데, 답은 딴 데 있었다.

> **비브라토가 배음 자리를 살짝 옮겨서, 「정확한 자리」만 재는 고에젤이
> 거기 없는 걸 「사라졌다」로 읽었다.** `VIB` 표(깊이)와 측정된 "어두워짐" 크기가
> 거의 그대로 겹쳤다 — 바이올린(깊이 0.010, 제일 큼) −36.5%(제일 큼),
> 트럼펫(깊이 0.006, 제일 작음) +1.7%(거의 0), 보컬(0.011) −27.6%, 플루트(0.007)
> −5.8%. **필터가 움직인 게 아니라 자가 흔들린 것**이었다.

### 다시 잰 법 — 넓은 대역(고역통과 RMS 비율)

정확한 배음 자리 대신 **500Hz 위/아래 RMS 비율**로 다시 쟀다 — 비브라토 몇 %엔
안 흔들리고, 필터가 실제로 열리고 닫히면 크게 바뀐다. 결과가 확 줄었다:
바이올린 −36.5%→−9.8%, 색소폰 −30%→−5.1%, 스트링 −33.5%→−3.5%, 보컬
−27.6%→−0.1%. **시간에 따른 「밝기가 어두워진다」는 대부분 측정 방식의 착시였다.**

그런데 **세기(vel1→vel3)** 쪽은 이 자로 재도 여전히 작았다 — 대부분 −3%~+10%
사이. `bright = 0.66 + 0.34*(vel/3)` 가 필터 컷오프를 최대 29%밖에 안 움직이니
당연한 결과다. **HANDOFF 가 처음부터 의심했던 그 자리("밝기차가 좁다")는
측정으로 확인됐다** — 여기는 착시가 아니다.

### 안 고쳤다 — 여기서 멈춘 이유

`bright` 공식을 넓히는 건(예: `0.55+0.50*(vel/3)`) 간단하지만, **이 악기들
전부**(패드·스트링·바이올린·첼로·브라스·색소폰·오르간… 24종)의 소리가
**모든 기존 곡·모든 장르에서** 조금씩 달라진다 — 장르마다 귀와 숫자로 맞춘
`kStyleGain`(5단계 48/N)이 전부 다시 어긋날 수 있다. 그리고 **얼마나 넓혀야
"가짜"가 아니게 되는지는 숫자가 안 알려준다** — 그건 듣고 정할 값이다.
`§15`(오디오 엔진은 시험 없이 안 바꾼다)와 「기존 데이터·소리를 이유 없이
바꾸지 않는다」 둘 다에 걸리는 자리라, **사용자 확인 없이 진행하지 않았다.**

### 더 파 봤다 — 방향이 반대인 곳이 있다, 그런데 원인을 못 찾았다

넓은 대역 자로 다시 재니(`test/timbre_check_test.dart` 로 정식으로 남겨 뒀다)
**패드 −9.0%, 스트링 −8.9%, JP스트링 −6.3%, 색소폰 −3.7%** 처럼 **세게 칠수록
오히려 어두워지는** 것들이 나왔다 — "좁다"가 아니라 **방향이 반대**다.
`bright` 공식(`0.66+0.34*(vel/3)`)은 vel 이 오를수록 컷오프가 커지므로 이럴 수
없는데, 왜 그런지 `Biquad`(RBJ 쿡북 로우패스) 를 직접 불러 색소폰의 배음
구조로 확인했다:

> **공진(Q)을 없애도(0.7dB), 배음을 기본음 하나만 남겨도 똑같이 나왔다.**
> 컷오프 1623Hz 로 거른 것이 2100Hz 로 거른 것보다 **에너지가 더 크게** 나왔다
> — 로우패스는 컷오프가 높을수록 에너지가 늘거나 같아야 하는데(사인파 하나로
> 확인: 500→16000Hz 에서 정상상태 진폭이 매끄럽게 늘었다), 배음을 여러 개
> 합쳐 3초짜리로 만들어 재면 그 반대가 나왔다. 필터 계수 자체는 옳다(단일
> 사인파 시험은 기대대로 나온다) — **문제가 필터에 있는지, 여러 배음을 합쳐
> 오래 도는 신호를 재는 내 창(3초짜리, 마지막 1초 재기) 쪽에 있는지를
> 못 갈랐다.**

그래서 **원인 없이 방향만 보고 고치지 않았다.** 원인을 모른 채 24종 악기가
같이 쓰는 필터 경로를 고치면, 지금 눈에 안 보이는 다른 자리를 깨뜨릴 수
있다 — `test/timbre_check_test.dart` 는 그래서 **잘못을 판정하지 않는다**
(무음처럼 확실한 고장만 잡는다). 세기·시간 밝기 변화는 전부 프린트로만
남는다 — 다음에 이 자리를 열 때 「전과 후」를 잴 자다.

### 다음에 할 일

1. **원인을 먼저 확실히 한다.** 배음 여러 개를 섞은 신호를 Biquad 로 걸러
   "정상상태"라고 믿은 구간이 실제로 정상상태인지(위상·에너지가 시간에 안
   변하는지) 짧은 구간으로 잘라 가며 눈으로 확인하는 게 먼저다 — 지금은
   "3초 중 마지막 1초"가 정말 안정됐다고 가정만 했다.
2. 원인이 **잼(측정) 쪽**이면: 자를 고치고 다시 재서, 방향이 여전히 반대인
   악기가 남는지 본다.
3. 원인이 **엔진 쪽**이면(예: 필터 상태가 새 노트마다 안 지워진다, 등):
   그 버그를 고치는 게 "밝기차를 넓히는 것"보다 먼저다 — 버그를 고치면
   밝기차가 저절로 늘 수도 있다.
4. 그러고 나서야 `bright` 공식을 넓힐지 정한다. 넓히면 `kStyleGain`(장르별
   크기 보정)을 다시 재야 한다 — 밝기가 바뀌면 체감 크기도 같이 바뀐다.

## 제품 방향 합의 + 악기 품질 1단계: 표본 재생 뼈대 (2026-09-09)

### 방향 (사용자와 합의)

**수익화**: 광고 + 콘텐츠 팩 구매(지금 `packs.dart` 구조가 이미 이 모양이다).
**투자 순서**: 악기 퀄리티 먼저. **방향**: 신스 튜닝이 아니라 **표본(실제
녹음) 재생**으로 — "숙련자가 놀랄 기능"으로는 ① 장르 송폼을 "나중에 고르는
프리셋"으로 내리고 **새 프로젝트는 빈 캔버스로 시작**(미디 편집·표본 배치를
바로) ② 더 정교한 이펙트 체인, 이 둘을 다음 순서로 잡았다.

### 표본 소스 — 무료·상업용 무제한

- **University of Iowa Electronic Music Studios**(<https://theremin.music.uiowa.edu/>) —
  1997년부터 "제한 없이 사용 가능"(상업적 사용 포함, 저작권료·크레딧 의무 없음).
  현악기·관악기·타악기·피아노, 반음마다 pp/mf/ff 세 세기. **지금 신스 악기
  목록(violin·cello·sax·trumpet·clarinet·flute·brass·piano)과 그대로 겹친다.**
- **VCSL(Versilian Community Sample Library)** — CC0(퍼블릭 도메인급). 5GB
  전체지만 악기별로 20~75MB씩 나뉘어 필요한 것만 쓸 수 있다. 다음에 현악기
  쪽을 넓힐 때는 여기가 더 적합할 수 있다(Iowa 현악기는 음 하나가 아니라
  **글리산도로 이어 녹음**돼 있어 낱음으로 잘라 쓰기가 번거롭다).

### 만든 것 — 피아노 표본 뼈대 (끝에서 끝까지)

`lib/sampler.dart` — 표본 재생 전용 모듈. **신스 경로와 완전히 갈라 둔다**:
`kSampleBanks[voice]` 가 채워져 있으면 `SynthNote.noteOn` 이 오실레이터·필터·
엔벨로프를 하나도 안 만들고 그 자리에서 표본 경로로 빠진다(`_startSample`/
`_nextSample`, `synth.dart`). 없으면(대부분의 악기, 그리고 못 불러온 경우)
지금까지처럼 합성으로 운다 — **한 줄도 안 섞인다.**

- **표본을 자동으로 안 부른다.** `ensureSamplesLoaded()` 를 오디오
  아이솔레이트가 시작할 때 **명시적으로** 부를 때만 채워진다(`audio_isolate.dart`,
  `unawaited` — 급식 루프 시작을 안 늦춘다). 이 앱의 시험은 대부분 위젯 없이
  `SynthNote()` 를 맨 Dart 로 만드는데, 거기서 자동으로 표본을 불러오려 하면
  `rootBundle` 바인딩이 없어 깨진다 — 안 부르면 기존 시험 **86개가 한 줄도
  안 달라진다**(실측: 그대로 전부 통과).
- **피치 시프트**는 root 주파수 대비 배율로 선형보간(`_smRatio =
  targetFreq/rootFreq * (표본SR/48000)`) — 표본 자체의 자연스러운 감쇄를
  그대로 쓴다(별도 엔벨로프를 안 얹는다). **음 길이보다 길게 눌려 있으면**
  80ms 짧게 눌러 끈다(release) — 안 그러면 손을 뗀 지 한참 지나서도 표본이
  계속 운다.
- **세기(vel 1~3)** 는 pp/mf/ff 세 벌의 표본 자체를 갈아 끼운다(신스처럼
  필터로 흉내 내지 않는다 — 진짜 다른 녹음이니 음색까지 같이 바뀐다). 음량은
  신스와 같은 `VG` 표를 그대로 써서 세기 감각이 트랙마다 안 어긋난다.
- **root 4개**(C2·C3·C4·C5) × **세기 3개** = 12개 파일, 22.05kHz 모노로
  낮추고 5초로 잘라 2.4MB(`assets/samples/piano/`, `tool/prep_samples.dart`
  가 다듬는다).

> **원본에 최대 0.58초짜리 무음 프리롤이 있었다.** University of Iowa 원본을
> 그대로 5초 잘라 넣었더니, 새로 만든 시험(`test/sampler_check_test.dart`)의
> 피치 검사가 이상한 값을 냈다 — 알고 보니 **표본 자체가 0.3~0.5초 동안
> 거의 무음이다가 그제서야 망치 소리가 난다.** 안 잘라 내면 귀한 5초 중
> 십분의 1이 넘게 무음으로 날아간다. `tool/prep_samples.dart` 가 이제
> **피크의 3% 를 넘는 첫 자리**(20ms 앞부터 살려서 어택 초반을 안 자른다)를
> 찾아 그 앞을 잘라 낸다.
>
> 이 병을 잡은 것도 시험이었다 — 자기상관으로 피치를 재는데 값이 널을
> 뛰어서(2000Hz 고정값) 원인을 파고들다 프리롤을 발견했다. 그다음엔
> **옥타브 오차**(자기상관이 반박자를 고르는 흔한 함정, 147.7Hz = 293.66Hz의
> 정확히 절반)도 나왔다 — 요청한 주파수 근처(±30%)로만 lag 를 찾게 좁혀서
> 옥타브 오차 자체가 안 나게 고쳤다.

`test/sampler_check_test.dart`: 자산이 실제로 읽히는가 · 소리가 나는가 ·
root 가 아닌 음도 그 높이로 피치 시프트되는가(자기상관, ±3% 이내) · 세기
1·3이 실제로 다른 표본인가 · 짧게 친 음이 release 뒤로 조용해지는가.
`flutter test` **88개 파일 전부 통과**(새 시험 포함), `flutter analyze lib`
무결점.

### 다음에 할 일

1. **더 넓히기** — 지금은 피아노 하나, 반음 4개뿐이다. 같은 방식으로
   root 를 더 촘촘히(반음마다까지는 안 해도 3도마다 정도) 받고, 다른 어쿠스틱
   악기(기타·색소폰·트럼펫 등)로 넓힌다. 현악기는 Iowa 대신 VCSL 이 나을 수
   있다(위 참고).
2. **스테레오/공간감** — 지금은 모노로 양쪽에 그대로 복사한다(`outL=outR=s`).
   신스처럼 유니즌 폭·팬은 아직 없다.
3. **라운드로빈** — 같은 음을 두 번 연달아 치면 완전히 같은 파형이 두 번
   난다(신스는 원래 그렇다 — 표본은 그게 더 눈에 띈다). 표본을 한 자리에
   여러 벌 두고 번갈아 쓰는 것도 다음 단계.
4. **용량 정책** — 지금 2.4MB 는 기본 빌드에 넣어도 되는 크기다. 악기를
   늘리면(현악기·관악기까지) 수십MB 가 될 수 있다 — "기본 몇 개는 내장,
   나머지는 콘텐츠 팩으로 다운로드" 를 이 시점에 정해야 한다(수익화 방향과
   바로 이어진다).
5. **구조 변경(빈 캔버스로 시작)** — 위에서 합의한 두 번째 우선순위. 별도
   계획으로 다뤄야 하는 큰 변경(온보딩·`Project.initial()`·`kSongForms`
   전부 걸린다).

## 악기 품질 1단계 이어서 — 바이올린 (2026-09-09)

같은 틀로 두 번째 표본 악기를 넣었다 — `violin`, root 8개(G3·B3·D4·F4·A4·C5·
E5·G5), 3.3MB.

- **Iowa MIS(예전 페이지)는 못 쓴다** — 위에서 확인했듯 음 하나가 아니라
  **줄 하나를 오르내리는 글리산도**로 녹음돼 있어 낱음으로 못 자른다.
  대신 **MIS Pitches 2014**(`MIS-Pitches-2012/MISViolin2012.html`)를 썼다 —
  여긴 낱음이다(활로 켠 것과 뜯은 것, 줄마다 따로). 관악기·다른 현악기도
  이 2012 계열에 낱음이 있다(트럼펫·클라리넷·첼로·비올라·콘트라베이스·
  플루트·호른·바순·오보에·트롬본 등) — 다음에 넓힐 때 이 계열부터 본다.
- **원본에 세기가 ff 한 벌뿐이다.** pp/mf/ff 세 파일을 다 같은 내용으로
  둬서(`sampler.dart` 의 규칙은 그대로 두고) 음량만 `VG` 표로 갈리게 했다 —
  거짓으로 지어내지 않았다. 시험(`sampler_check_test.dart`)도 그 사실을
  그대로 검증한다("음색은 원본 제약상 아직 동일").
- `tool/prep_samples.dart` 를 **여러 악기 폴더를 한 번에 훑게** 고쳤다
  (`assets/samples/*/*.raw.wav` 를 전부 찾아 그 자리에서 다듬는다) —
  피아노 하나에만 쓰던 걸 재사용 가능하게 만들었다.

> **zsh 의 단어 나누기가 bash 와 다르다.** 파일 이름을 일괄로 바꾸는 스크립트를
> `for pair in $pairs` 식으로 짰다가, zsh 는 공백으로 안 나눠서(`shwordsplit`
> 옵션이 꺼져 있다) 루프가 한두 번만 돌고 조용히 끝나는 바람에 **G3 파일이
> 통째로 사라지고 G5 파일 셋이 엉뚱한 내용으로 덮였다.** 다행히 원본 AIFF가
> 남아 있어 되살렸다. 이 저장소 셸은 zsh 다 — 여러 항목을 훑는 배시 스크립트를
> 짤 때 `for x in $list`(공백 나누기 기대) 대신 배열이나 한 줄씩 명시하는 쪽이
> 더 안전하다.

`flutter test` **89개(파일 수는 88개, 시험 케이스 수)** 전부 통과, `flutter
analyze lib` 무결점. 다음은 위 "더 넓히기"를 이어가거나, 트럼펫·색소폰 같은
관악기로 넘어가면 된다(2012 계열, 세기 한 벌 제약도 같다).

## 악기 품질 1단계 세 번째 — 트럼펫 (2026-09-09, 이어서)

같은 틀로 세 번째: `trumpet`, root 9개(E3·G3·Bb3·D4·F4·A4·C5·E5·G5, 단3도
간격), 5.0MB. 바이올린과 같은 원본 제약(세기 ff 한 벌뿐)이라 파일 셋(pp/mf/ff)
이 다 같은 내용이다.

- `tool/prep_samples.dart` 를 손 안 대고 그대로 썼다(여러 폴더를 훑게 고친
  게 바로 여기서 재사용됐다) — 프리롤도 전부 0.02~0.04초로 짧아 문제없었다.
- **지난번 zsh 사고를 안 되풀이했다** — 이번엔 파일 이름 바꾸기를 8~9번
  **한 줄씩 명시**해서 돌렸다(반복문 없이). 느리지만 확실하다.
- 시험은 바이올린 것을 그대로 안 베꼈다 — 같은 제약이라 같은 검증은 중복이다.
  **이 표본만의 위험**(자산 경로·root 주파수 표)만 짧게 확인한다.

지금까지 표본 셋 합쳐 **11MB**(피아노 2.4 + 바이올린 3.3 + 트럼펫 5.0).
`flutter test` 전부 통과(90개 시험 케이스), `flutter analyze lib` 무결점.

### 다음 (누적)

- 관악기 하나 더(색소폰·클라리넷·플루트 중 하나 — 전부 2012 계열에 낱음 있음)
  또는 저음 현악기(첼로·콘트라베이스)로 넓히면 4중주/재즈 편성이 표본으로
  갈린다.
- 위에서 미룬 것들 여전히 남아 있다: 스테레오/공간감, 라운드로빈, 용량 정책
  (11MB 도 아직은 기본 빌드에 넣을 만하지만 다음 악기 몇 개면 임계점),
  빈 캔버스 구조 변경.

## 악기 품질 1단계 네 번째 — 첼로 (2026-09-09, 이어서)

저음 현악기로 넓혔다: `cello`, root 9개(C2·Eb2·Gb2·A2·C3·Eb3·Gb3·A3·C4,
단3도 간격, C2~C4 두 옥타브), 5.4MB. University of Iowa MIS Pitches 2014
Cello 페이지에서 **전부 C 현(sulC) 하나로만** 받았다 — 현을 바꾸면 그 현
특유의 음색이 끼어드는데, 다른 표본 악기처럼 한 벌짜리 음색을 지키려면
한 현으로 통일하는 쪽이 맞다. 바이올린·트럼펫과 같은 원본 제약(세기 ff
한 벌뿐)이라 pp/mf/ff 세 파일이 다 같은 내용이다.

- 이번엔 원하는 URL을 몰라서(첼로 페이지 구조가 처음) 실제 목록 페이지를
  긁어(`curl` + `grep href`) 어떤 파일이 있는지 먼저 확인했다 — 짚어서
  없는 파일을 요청하는 실수를 피했다.
- `tool/prep_samples.dart` 그대로 재사용 — 프리롤 0.15~0.37초, 문제없이
  잘렸다.
- 파일명 바꾸기도 트럼펫과 같은 방식(반복문 없이 한 줄씩 명시)으로 27개
  (9음×3세기) 처리 — 안전하게 끝났다.
- 시험은 트럼펫과 같이 최소로(자산 로드 + 피치 시프트) — 다만 **첫 저음
  악기**라 자기상관 lag 검색 창(±30%)이 저음(D2, 73Hz)에서도 통하는지를
  같이 확인했다(잰 값 74.8Hz, 통과 — 창이 저음에서도 안전하다는 뜻).

지금까지 표본 넷 합쳐 **16MB**(피아노 2.4 + 바이올린 3.3 + 트럼펫 5.0 +
첼로 5.4). `flutter test` 전부 통과(91개), `flutter analyze lib test`
그대로 30건(기존 것과 동일, 새로 는 것 없음).

### 다음 (누적)

- 16MB 면 다음 악기 한둘로 임계점(대략 20MB 안팎)에 닿는다 — 이제부터는
  용량 정책(기본 빌드 vs. 다운로드형 콘텐츠 팩)을 실제로 정해야 한다.
  수익화 방향(광고+콘텐츠 팩)과 바로 이어지는 결정이다.
- 관악기 하나 더(색소폰·클라리넷·플루트) 또는 콘트라베이스로 편성을
  넓힐 수 있으나, 용량 정책을 정하기 전엔 또 늘리는 게 맞는지부터
  봐야 한다.
- 여전히 남음: 스테레오/공간감, 라운드로빈, 빈 캔버스 구조 변경.

## 악기 콘텐츠팩 분리 — 기본팩/무료 추가팩 (2026-09-09)

사용자 결정: 밴드 악기·오실레이터 악기는 **기본팩**(늘 들어있음), 표본
악기(피아노·바이올린·트럼펫·첼로)는 **무료 추가팩**으로 나눈다. 지금은
서버가 없어서 **구조만** 먼저 나눴다 — 물리적으로는 여전히 앱에 같이
들어있고, 나중에 진짜 다운로드형으로 바꾼다(사용자도 이 순서에 동의).

- `lib/instrument_packs.dart` 새로 만들어 `INSTRUMENTS`(합성 악기 34개)를
  기본팩 30개·추가팩 4개(표본 있는 것)로 코드로 갈랐다. 두 목록을 손으로
  안 적고 `sampler.dart` 의 `kSampleInstrumentKeys` 로 나머지를 자동 계산
  하게 해서, 나중에 표본 악기가 늘어나도 이 파일은 안 고쳐도 된다.
- **표본 로딩을 "전부 한꺼번에" 에서 "악기별로 따로"로 바꿨다** —
  `sampler.dart` 의 `ensureSamplesLoaded()`(넷 다 로드) 를
  `ensureInstrumentLoaded(voice)`(하나만 로드) 로 대체했다.
  `synth.dart` 의 `SynthNote.noteOn` 이 표본 악기를 **처음 만났을 때**
  스스로 이걸 부른다(`unawaited`) — 곡이 안 쓰는 표본 악기는 아예 안
  읽는다. `audio_isolate.dart` 시작할 때 걸던 전체 로드 호출은 없앴다.
- 왜 이게 "끊김 없이"로 이어지는가: 로드가 끝나기 전에 친 음은 여전히
  합성으로 대신 나간다(원래 있던 안전장치, `bank == null` 이면 합성
  경로) — 넷을 한꺼번에 기다리던 예전보다 **악기 하나만** 기다리니
  로드가 더 빨리 끝나고, 안 쓰는 악기 때문에 앱 시작이 느려지지도
  않는다.
- `test/instrument_packs_check_test.dart`(팩 분리가 겹침·빠짐 없이
  맞는지) 와 `test/sampler_lazy_check_test.dart`(표본을 하나도 안 읽은
  상태에서 바로 쳐도 예외 없이 소리가 나고, 백그라운드 로드가 실제로
  끝나는지) 를 새로 추가했다. `flutter test` 전부 통과(93개),
  `flutter analyze lib test` 그대로 30건(기존 것과 동일).

### 다음 (누적)

- 진짜 다운로드형으로 바꿀 차례가 오면 `ensureInstrumentLoaded` 안의
  `rootBundle.load` 를 네트워크 다운로드+로컬 캐시로 바꾸면 된다 —
  `synth.dart`/`audio_isolate.dart` 쪽은 그대로 둬도 된다(이번에 그렇게
  분리해 뒀다). 호스팅(Firebase/Supabase Storage 등) 을 어디로 할지는
  그때 다시 정한다.
- 여전히 남음: 스테레오/공간감, 라운드로빈, 빈 캔버스 구조 변경.

## 표본에 스테레오 자리 주기 + 기본팩 밴드 악기(기타·베이스) 표본화 (2026-09-10)

두 가지를 같이 했다.

**1) 표본 스테레오 패닝** — 지금까지 표본은 원본이 모노라 좌우가 완전히
같았다(`outL = outR = s`). `sampler.dart` 에 `kSamplePan`(악기별 고정
-1~1 값)을 추가하고, `synth.dart` 의 `_startSample` 이 오실레이터 부분음과
같은 등파워 패닝 식(`(pan+1)*pi/4` → cos/sin)을 표본에도 적용하게 했다.
바이올린·기타는 오른쪽, 첼로는 왼쪽, 피아노·베이스는 가운데 — 실내악
자리처럼 살짝 갈랐다. 라운드로빈처럼 원본이 더 필요한 개선이 아니라
지금 표본 그대로 값싸게 되는 것부터 했다.

**2) 기본팩 밴드 악기 표본화** — 사용자 요청: 드럼·일렉기타·베이스기타
표본을 좋은 걸로. 이번엔 **일렉 기타(클린)·핑거 베이스**를 표본으로
바꿨다(드럼은 `lib/drums.dart` 가 `SynthNote`/`INSTRUMENTS` 와 완전히
다른 별도 타격 합성기라 표본을 붙이려면 그 파일 안에 새 서브시스템을
만들어야 한다 — 이번엔 손 안 댔다, 다음 항목).

- 출처: **FreePats**(freepats.zenvoid.org) — "EGuitar FSBS Clean"과
  "Finger Bass YR", 둘 다 **CC0(퍼블릭 도메인)**, 크레딧 의무 없음,
  상업적 사용 제한 없음.
- 기타는 원본이 6현 전체·세기 두 벌·라운드로빈 4벌까지 있는 477MB짜리
  정식 멀티샘플이었다 — 라운드로빈 첫 벌만, root 18개(C2~C#6)만 골라
  추렸다. **세기가 진짜 두 벌**이다(soft/normal 실제로 다른 녹음) —
  바이올린류보다 다이내믹이 낫다.
- 베이스는 원본이 한 옥타브 낱음·세기 한 벌(`electric-bass-YR`)이라
  12개를 그대로 다 썼다.
- **기본팩 전용으로 `tool/prep_samples.dart` 에 악기별 길이 캡을 추가**
  (`maxSecOverride`) — 기타·베이스는 3초, 다른 표본 악기는 그대로 5초.
  뜯는 소리라 3초면 자연 감쇄가 거의 끝나는데, 5초를 다 담으면 「기본팩」
  (늘 번들) 용량이 아깝다.
- **악기팩 분리 로직을 손봤다** — `guitar`·`fingerbass` 도 표본이 있지만
  밴드 악기라 추가팩이 아니라 **기본팩**에 남아야 한다. `sampler.dart` 에
  `kAddonPackSampleKeys`(피아노·바이올린·트럼펫·첼로만)를 새로 둬서,
  "표본이 있다" 와 "추가팩이다" 를 갈랐다 — `instrument_packs.dart` 는
  이제 이 목록만 보고 나머지는 다 기본팩으로 계산한다.
- 다운로드는 `.7z` 압축이라 시스템에 `7z` 가 없었다 — `pip3 install
  --user py7zr` 로 사용자 계정에만 설치해서 풀었다(시스템 전체를 안
  건드림).
- `test/sampler_check_test.dart` 에 기타·핑거베이스 확인을 추가(기타는
  세기 두 벌이 진짜 다른지도 확인). `flutter test` 전부 통과(95개),
  `flutter analyze lib test` 그대로 30건.

표본 용량: 피아노 2.4 + 바이올린 3.3 + 트럼펫 5.0 + 첼로 5.4 + **기타
7.0 + 핑거베이스 4.6** = **28MB**. 이 중 기타·베이스(11.6MB)는 기본팩이라
항상 들어간다 — 추가팩(피아노·바이올린·트럼펫·첼로, 16MB)은 나중에
다운로드형으로 뺄 수 있다.

### 다음 (누적)

- **드럼** — `lib/drums.dart` 의 `DrumVoice`/`DrumKit` 은 피치가 있는
  `SynthNote` 와 다른 구조다(타격 하나하나가 악기 하나가 아니라 "손 하나"
  처럼 여러 소리를 냄). 표본을 붙이려면 `sampler.dart` 와 비슷한 표본
  뱅크를 드럼 조각(킥·스네어·하이햇 등) 이름으로 찾게 새로 만들어야
  한다 — 피치 시프트가 없는 대신(대개 원음 그대로 튼다) 구조가 다르다.
  FreePats 의 "MuldjordKit"(CC BY 4.0, 크레딧 필요)이 후보다.
- 기본팩 용량이 28MB 안의 11.6MB까지 늘었다 — 드럼까지 더하면 기본팩만
  으로도 앱 설치 용량이 꽤 커진다. 기본팩도 나중엔 압축(OGG/Opus 등)을
  고민할 때가 올 수 있다(지금은 WAV 그대로).
- 여전히 남음: 빈 캔버스 구조 변경, 진짜 다운로드형 추가팩.

## 드럼 표본화 — 어쿠스틱/록 킷 (2026-09-10)

`lib/drums.dart` 는 `SynthNote`/`INSTRUMENTS` 와 완전히 다른 구조(타격
하나가 톤·노이즈·클러스터 레이어 여러 개를 동시에 냄)라 지난 회차에
"다음"으로 미뤄 뒀던 것. 이번에 새 서브시스템(`lib/drum_sampler.dart`)을
만들어 붙였다.

- **왜 어쿠스틱·록만인가** — 808·909·로파이 킷은 **의도적으로 전자음**
  이다. 표본(실제 드럼 녹음)을 거기에 켜면 오히려 그 킷의 정체성이
  사라진다. `DrumKit` 에 `sampled: bool` 필드를 추가해서 어쿠스틱·록만
  `true` — 나머지는 그대로 합성이다(사용자 취향에 따라 계속 그렇게
  들려야 할 킷들).
- 출처: **FreePats "MuldjordKit"**(원작 Lars Muldjord, DrumGizmo용) —
  **CC BY 4.0(크레딧 필요)**. 원본 233MB(조각당 라운드로빈 9~56벌짜리
  정식 멀티샘플)에서 조각마다 세기 3벌(pp/mf/ff, 원본 파일 번호가
  조용함→큼 순서인 걸 활용) × 라운드로빈 1~2벌만 추려서 **1.8MB**로
  줄였다. `assets/samples/drum_kick/SOURCE.md`(일곱 폴더에 같은 내용)에
  정확한 출처·고른 파일 번호 표가 있다.
- **하이햇은 조각 자체가 갈린다** — `drums.dart` 의 `hat()` 이 원래
  `open = vel >= 3` 로 닫힘/열림을 나누길래, 표본도 그 경계를 그대로
  따라 `hatClosed`/`hatOpen` 두 표본 뱅크로 나눴다(세기가 "더 큰 소리"가
  아니라 "다른 조각"인 경우).
- **탐만 피치가 있다** — 곡마다 다른 `tomFreq` 로 틀어야 해서
  `kDrumRootFreq['tom'] = 180.0` 기준으로 피치 시프트한다. 나머지
  조각(킥·스네어·크래시·라이드·하이햇)은 항상 원음 그대로 — 애초에
  피치가 없는 소리다.
- **라운드로빈이 진짜로 생겼다** — 여태 다른 표본 악기는 원본이
  세기 한 벌뿐이라 못 넣었는데, 드럼은 조각당 2벌까지 있어서 매번
  무작위로 고른다(`DrumSampleBank.pick`, 씨앗 고정 난수라 곡을 다시
  내보내도 같은 결과). `drums.dart` 가 이미 갖고 있던 "머신건 방지"
  흔들림(`_Hit`)과 같은 목적인데, 표본은 진짜 다른 녹음이라 더 낫다.
- **초킹(하이햇 닫기)도 표본에 통한다** — `DrumVoice.next()` 맨 앞에
  표본 분기를 넣고, 기존 초킹·팬 코드(`_chokeLeft`/`_panL`/`_panR`)를
  그대로 재사용했다(따로 안 만듦 — 로직이 하나뿐이라야 나중에 안 갈린다).
  손 안 댄 다른 시험(`drum_check_test.dart` 등)이 여전히 통과하는 이유도
  이거다 — `kDrumSampleBanks` 를 안 채우면 `_startDrumSample` 이 바로
  `false` 를 돌려주고 예전 합성 경로로 그대로 간다.
- **크레딧 표기**(CC BY 4.0 조건) — `lib/ui/settings_sheet.dart` 맨
  아래에 "소리 표본 출처" 칸을 새로 넣어 MuldjordKit·Iowa·FreePats를
  적었다. 이게 없으면 라이선스 위반이라, 다른 항목보다 먼저 처리했다.
- `test/drum_sampler_check_test.dart` 새로 추가(어쿠스틱/록만 켜지는지,
  하이햇이 세기로 다른 조각을 고르는지, 초킹이 결국 멎는지). `flutter
  test` 전부 통과(97개), `flutter analyze lib test` 그대로 30건.

표본 총 용량: **29MB**(기본팩 13.4MB — 기타 7.0+베이스 4.6+드럼 1.8 /
추가팩 16MB — 피아노+바이올린+트럼펫+첼로).

### 다음 (누적)

- 기본팩이 이제 29MB 중 13.4MB — 드럼 조각을 더 추가(림샷·박수 등)하기
  전에 용량/압축 정책을 한 번 더 볼 때가 됐다.
- 여전히 남음: 진짜 다운로드형 추가팩. (라운드로빈은 드럼에서 됐다 —
  다른 표본 악기는 원본 제약상 아직 안 됨.)

## 빈 캔버스 구조 변경 (2026-09-10)

사용자의 원래 요청("장르별 송폼은 나중에 고를 수 있는 프리셋으로 남기고
프로젝트 시작시 아무것도 없는것부터 시작")을 처리했다. 계획대로:
`Project.initial()` 은 **손 안 댔다**(기존 시험 수십 개가 "새 프로젝트 =
장르가 이미 씌워짐"을 전제로 만들어져 있어서) — 대신 `Project.blank()` 를
새로 만들어 **앱이 실제로 띄우는 진짜 프로젝트**(`main.dart`)만 갈아탔다.

- `Project.blank()` — 트랙 4개는 그대로, 씬 하나(전부 쉼)·구간 하나뿐.
  **씬을 0개로는 안 뒀다** — `Project.scene` 게터가 `scenes[currentScene
  .clamp(0, scenes.length-1)]` 라서, 0개면 `clamp(0,-1)` 에서 바로 죽는다
  (Dart 의 `clamp` 는 하한>상한이면 예외). 씬 하나 + 구간 하나가 "진짜
  빈 캔버스"와 "죽지 않는 코드"를 같이 만족하는 최소 형태다.
- `genre` 를 빈 문자열로 뒀다 — `genreDef`/`songGenreOf` 는 모르는 키를
  **첫 장르로 떨어뜨려 안 죽지만**, 그 값을 화면에 그대로 보이면 "패턴이
  하나도 없는데 '로파이'라고 써 있다"는 모순이 생긴다. 그래서
  `home_view.dart`(지금 곡 카드)·`scene_view.dart`(스타일 칩) 두 곳에
  `genre.isEmpty` 분기를 넣어 "빈 프로젝트"/"스타일 고르기"로 보이게
  했다. (부수 효과: `scene_view.dart` 의 `_pickStyle` 이 갖고 있던
  `if (picked == p.genre) return` 가드가, genre 가 우연히 'lofi' 였다면
  "로파이"를 골라도 조용히 무시되는 버그가 될 뻔했다 — 빈 문자열이라 그
  버그가 안 생긴다.)
- `main.dart` 의 앱 시작 프로젝트만 `Project.blank()` 로 바꿨다. 저장된
  곡이 있으면 `Store.start()` 의 `project.loadJson(j)` 가 이 값을 덮어써서
  — **기존 사용자의 저장된 곡에는 영향이 없다.**
- `home_view.dart` 의 `_maybeGuide()`(첫 실행에 저절로 뜨던 곳)에서
  장르 시트 자동 표시를 없앴다 — `s.markGuideSeen()` 만 하고 끝. 장르
  프리셋은 없어진 게 아니라 자리를 옮겼다: 씬 화면의 "스타일 바꾸기" 칩
  (이미 있던 기능, `scene_view.dart` 의 `_pickStyle`)이 그대로 그 시트를
  연다 — 사용자는 빈 캔버스로 들어가서 **직접 미디를 찍거나, 원하면
  그 안에서 프리셋을 고른다.** 이제 안 쓰는 `home_view.dart` 의
  `_startWith`(장르 적용+이동을 합친 헬퍼)는 지웠다 — `_pickStyle` 이
  이미 더 나은 버전(되돌리기 포함)을 갖고 있었다.
- `test/guide_ui_test.dart` 를 새 흐름에 맞게 다시 썼다(장르 시트가 아니라
  **아무것도** 안 뜨는지 확인) — 나머지 8개 체크(사용법·네 단계·되돌아와도
  안 뜸·재시작해도 남음·가로 안 넘침)는 그대로다. `test/
  blank_project_check_test.dart` 새로 추가(씬 1개·구간 1개·genre 빈 문자열·
  `p.scene` 게터가 안 죽는지).
- `flutter test` 전부 통과(98개) — **`Project.initial()` 을 쓰는 시험
  70여 개는 한 줄도 안 고쳤는데 그대로 통과했다**(정확히 계획한 대로 —
  건드린 건 `main.dart` 가 쓰는 진짜 프로젝트뿐). `flutter analyze lib
  test` 그대로 30건.
- **시뮬레이터로 직접 눈으로는 못 봤다** — 이 맥에 Xcode 커맨드라인
  도구만 있고 전체 Xcode 가 없어서 `Claude_Code_iOS_Simulator` 가 안
  붙었다(에러: "Install Xcode... `sudo xcode-select -s /Applications/
  Xcode.app/Contents/Developer`" — 비밀번호가 필요해 내가 대신 못 한다).
  위젯 시험(`guide_ui_test.dart` 의 레이아웃·넘침 체크 포함)으로 대신
  확인했다 — 실기기/시뮬레이터 확인은 아직 안 한 상태라는 걸 밝혀 둔다.

### 다음 (누적)

- **Xcode 전체 설치가 필요하다** — App Store 에서 Xcode 설치 후
  `sudo xcode-select -s /Applications/Xcode.app/Contents/Developer` 를
  직접 실행해야 시뮬레이터로 실제 화면을 볼 수 있다(비밀번호 필요).
- 빈 캔버스에 들어갔을 때 "여기서 뭘 하지"에 대한 안내가 아직 약하다 —
  「사용법」 안내 문구가 "처음엔 패턴이 없어 조용하다"는 옛 상황(장르
  시드 이후 특정 구간만 비는 경우)을 말하고 있는데, 이제는 **정말
  통째로 빈** 경우가 기본이 됐다. 안내 문구를 한 번 더 다듬을 만하다.
- 여전히 남음: 진짜 다운로드형 추가팩.

## 이펙트 체인 — 비켜주기(사이드체인 덕킹)를 사용자 손잡이로 (2026-09-10)

사용자의 원래 요청 중 남아 있던 "더 정교한 이펙트 체인"을 처리했다.
기존 인서트 체인(앰프·딜레이·리버브·컴프·그래픽 EQ·리미터·드라이브·
베이스앰프·코러스, 9종 · 트랙별 리오더 가능 · 되돌리기 · 저장)은 이미
꽤 깊었다 — 그중 눈에 띄던 빈틈은 **사이드체인 덕킹**이었다: `Engine`
DSP와 아이솔레이트 메시지(`AudioClient.setDuck`)까지 배선이 다 돼
있는데, 장르가 정하는 고정값(`kGenreDuck`)일 뿐 사용자가 만질 손잡이가
하나도 없었다.

- `Project` 에 `duckAmountOverride`(null=장르 기본값 따름)와
  `duckRelSec`(복귀 시간, 기본 0.16초)을 추가 — 저장 파일에는 **만졌을
  때만** 적힌다(다른 필드와 같은 "옛 앱 호환" 규칙).
- `Engine.duckRelSec` 를 새로 만들어 고정 상수였던 복귀 시간(160ms)을
  런타임에 바꿀 수 있게 했다 — `_duckRel = exp(-1/(sec*kSampleRate))` 를
  세터에서 다시 계산한다.
- `AudioClient.setDuck(amount, {relSec})` — 기존 시그니처에 옵션 인자만
  더해서(`relSec` 생략하면 지금 값 유지) 호출하는 쪽을 안 깨뜨렸다.
  `_cDuck` 메시지가 `[amount, relSec]` 두 값을 실어 나른다.
- `lib/ui/mixer_view.dart` 의 마스터링 시트(`showMasterSheet`)에 "비켜주기
  (사이드체인)" 칸을 새로 넣었다 — 세기·복귀 시간 슬라이더 둘, 되돌리기
  아이콘(세기는 "장르 기본으로", 복귀 시간은 "0.16초로"). 트랙 인서트
  (`FxRack`)와 다른 자리에 둔 이유: 이건 트랙 하나에 꽂는 플러그인이
  아니라 **마스터 버스에서 드럼만 빼고 나머지에 거는 사이드체인
  라우팅**이라 성격이 다르다.
- `sequencer.dart` 의 두 재생 시작 지점(`playLoop`/`playSong`)이 이제
  `p.duckAmountOverride ?? kGenreDuck[p.genre] ?? 0` 을 보낸다 — 사용자가
  만진 세기는 스타일을 바꿔도(장르 기본값이 달라져도) 그대로 이긴다.
- `test/duck_check_test.dart` 새로 추가 — 복귀 시간을 늘리면 실제로
  더 오래 걸려 돌아오는지(`Engine.render` 로 직접 재생), `Project` 의
  저장·불러오기 왕복(만졌을 때만 파일에 남는지, 옛 파일도 안전한지).
  `flutter test` 전부 통과(100개), `flutter analyze lib test` 그대로
  30건.

### 다음 (누적)

- 여전히 남음: 진짜 다운로드형 추가팩. (이펙트 체인은 사이드체인 하나로
  일단 마무리 — 센드 버스(공유 리버브)·플랜저/페이저 같은 모듈레이션계는
  또 다른 회차의 몫으로 남긴다.)

## 사용법 안내 문구 — 빈 캔버스에 맞게 다듬기 (2026-09-10)

사용자 결정: 다운로드형 추가팩은 **지금은 구조만**(호스팅 미정, 나중에
다시 논의) — 그래서 남은 소소한 항목을 처리했다. 빈 캔버스가 기본이
되면서 「사용법」 안내(`showFirstGuide`)의 1단계 문구가 옛 상황(장르가
이미 채워진 뒤 일부 구간만 비는 경우)을 말하고 있었다.

- 1단계 맨 앞에 "처음엔 아무 소리도 안 납니다 — 정상입니다"와 "「스타일
  고르기」로 한 번에 채우거나" 를 추가했다 — 빈 캔버스에서 제일 먼저
  드는 의문("고장인가?")과 제일 쉬운 탈출구(프리셋 불러오기)를 맨 위에
  둔다. 기존 문구(패턴 하나씩 고르기·느낌·코드 진행)는 그대로 뒤에 남았다.
- 무음 대처 문구도 "패턴이 전부 「없음」이라 그렇습니다"로 원인을 먼저
  밝히고 "스타일 고르기로 채우거나 하나씩" 두 갈래를 다 알려주게 바꿨다.
- `test/guide_ui_test.dart` 가 찾는 문자열(`'소리가 안 나면'` 등)은 그대로
  살렸다 — 시험 수정 없이 통과. `flutter test` 전부 통과, `flutter
  analyze lib` 무결점.

## 표본 악기 음량 회귀 수정 — 패닝이 몰래 조용하게 만들고 있었다 (2026-09-10)

사용자 지적: "악기들 퀄리티가 말도안되는데" — 특히 밴드(재즈·일렉기타·
베이스)가 안 좋고 전체가 다 별로. 원인을 찾았다: **몇 회차 전에 표본에
스테레오 패닝을 넣으면서(`kSamplePan`) 보정 없는 등파워 패닝 식**
(`cos(p)`/`sin(p)`, `p=(pan+1)*pi/4`)을 썼다 — 이 식은 **가운데(pan=0)
에서도 두 채널이 0.707배로 조용해지고**, 조금만 옆으로 틀어도(바이올린
0.35, 기타 0.2) 한쪽 채널이 -6dB 까지 떨어진다. 표본의 세기 표(`VG`)는
이 패닝이 없던 시절 소리로 맞춰 둔 값이라, 패닝을 넣은 뒤로 **표본
악기 전체가 합성 악기보다 조용하게 들렸다** — 최근에 표본화한
기타·베이스(밴드 악기, 기본팩)가 바로 이 무보정 패닝을 처음 탄 것들이라
제일 크게 느껴졌을 것이다.

- `drums.dart` 의 `_setPan` 은 처음부터 `math.sqrt2` 로 보정해서 "가운데서
  모노였을 때와 같은 크기"를 지키고 있었다 — 그런데 표본(`synth.dart` 의
  `_startSample`)에 패닝을 넣을 때 그 보정을 안 가져왔다. `_smPanL/R` 에
  `* math.sqrt2` 를 더해서 드럼과 같은 식으로 맞췄다.
- 확인: 시험에서 피아노(가운데, pan 0) RMS 가 패닝 넣기 전 값(0.0068)
  으로 정확히 돌아왔고, 바이올린도 옛 값(0.0319)에 훨씬 가까워졌다
  (등파워 손실은 여전히 있다 — pan≠0 이면 한쪽은 원래보다 세지고 반대쪽은
  약해지는 게 패닝의 정의다. 다만 **가운데였을 때 억울하게 조용해지던
  것과, 그 위에 또 압축을 안 한 6dB 손실**은 없앴다).
- `flutter test` 전부 통과(100개), `flutter analyze` 그대로 30건.

### 다음 (누적)

- "전체가 다 별로"라는 지적은 이 회귀만으론 다 설명 안 될 수 있다 —
  더 들어 보고 구체적으로 어디가 이상한지(악기별로) 계속 짚어 주면
  다음 걸 잡을 수 있다.
- 여전히 남음: 진짜 다운로드형 추가팩.

## 내보내기가 표본을 아예 안 쓰고 있었다 — 진짜 원인 (2026-09-10)

사용자 질문 "샘플 쓰고 있는거 맞아?"가 정확했다. **재생은 표본을 제대로
쓰는데, 내보내기(WAV 파일로 뽑기)는 표본 악기 전체가 합성으로 나가고
있었다** — 패닝 음량 회귀보다 훨씬 큰 문제였다.

원인: `lib/export.dart` 의 렌더는 UI/오디오와 **다른 세 번째 아이솔레이트**
(`Isolate.spawn(_renderEntry, ...)`)에서 돈다. `rootBundle.load` (표본
자산을 읽는 통로)는 플랫폼 채널이 있어야 하는데, 이 렌더 아이솔레이트는
`BackgroundIsolateBinaryMessenger.ensureInitialized(token)` 를 **한 번도
안 불렀다** — `audio_isolate.dart` 는 처음부터 이걸 하고 있어서 재생은
멀쩡했다. 채널이 없는 상태에서 `rootBundle.load` 는 조용히 실패하고,
`ensureInstrumentLoaded`/`ensureDrumPieceLoaded` 의 try/catch 가 그 실패를
삼켜서(표본 하나 못 읽었다고 무음이 되면 안 되니까) **에러 한 줄 없이
그냥 합성으로 대신 났다.** 몇 회차에 걸쳐 표본 악기를 늘려 온 게 실제
재생에서는 다 들렸는데, 내보낸 파일·공유한 파일에는 하나도 안 실리고
있었던 것이다.

- `renderWavInIsolate` 가 `RootIsolateToken.instance` 를 잡아서 렌더
  아이솔레이트로 같이 넘긴다.
- `_renderEntry` 가 그 토큰으로 `BackgroundIsolateBinaryMessenger.
  ensureInitialized` 를 부른 **뒤에**, 새로 뺀 `preloadExportSamples(job)`
  로 그 곡이 실제로 쓰는 표본 악기·표본 드럼(어쿠스틱/록 킷일 때만)을
  **다 읽고 기다린 다음** `renderWav` 를 부른다.
- **`await` 순서가 핵심이다** — `renderWav` 자체는 중간에 한 번도 안
  쉬는 동기 루프(내보내기를 빠르게 하려고 일부러 그렇게 짰다)라서, 표본
  로딩을 렌더 **도중에** 비동기로 걸면(재생 쪽처럼) 이벤트 루프에 넘어갈
  틈이 없어 영영 안 끝난다. 그래서 재생과 다르게 **렌더 시작 전에 완전히
  끝내 놓는다.**
- `test/export_sample_check_test.dart` 새로 추가 — `preloadExportSamples`
  가 ExportJob 이 실제로 쓰는 표본 악기만 골라 읽는지(안 쓰는 건
  안 읽는지), 어쿠스틱/록 킷일 때만 드럼 표본을 읽는지(808 은 안 읽는지)
  확인한다. 아이솔레이트 경계 너머는 이 프로세스에서 직접 못 보므로,
  그 로직 자체를 함수로 떼어(`preloadExportSamples`) 여기서 직접 부른다
  — 토큰 배선(`_renderEntry` 안의 `ensureInitialized` 호출)은 타입으로
  강제되는 단순 연결이라 상대적으로 덜 위험하다고 보고 따로 시험을
  안 만들었다.
- `flutter test` 전부 통과(102개), `flutter analyze lib test` 그대로
  30건.

### 다음 (누적)

- **직접 들어서 확인이 필요하다** — 지금까지 "내보낸 파일이 이상하게
  들렸다"는 게 사실이었을 가능성이 크다. 다시 내보내서 들어 보고, 그래도
  이상하면 이제는 진짜 표본이 실렸다는 전제로 다음 문제를 짚을 수 있다.
- 여전히 남음: 진짜 다운로드형 추가팩.

## 록 베이스 기본 음색 + 업라이트 베이스·색소폰 표본 추가 (2026-09-10)

사용자가 "악기들 퀄리티가 말도안되는데"라며 밴드(재즈·기타·베이스)를
짚었다. 실제로 뜯어보니 **원인이 예상과 달랐다** — 재즈는 우리가 표본화한
기타·핑거베이스를 **아예 안 쓰고 있었다**(재즈는 업라이트 베이스·
일렉피아노·나일론 기타·색소폰 편성이라 전부 남남이다). 진짜 문제는
따로 둘이었다:

**1) 록의 베이스가 표본을 놓치고 있었다** — `kGenreTypeVoice['rock']` 이
화음·가락은 `guitar`(표본)로 바꿔 주면서 **베이스는 안 건드려서** 여전히
기본값 `bass`(톱니파 신스)였다. 기타 두 대가 표본인데 베이스만 신스면
밴드가 안 어울린다 — `'bass': 'fingerbass'` 를 추가했다(pop·gospel 은
이미 하고 있던 것과 같은 조합).

**2) 재즈가 표본을 하나도 안 쓰고 있었다** — 그래서 재즈가 실제로 쓰는
악기 중 가장 "신스 티"가 잘 나는 둘, **업라이트 베이스**(피치카토)와
**알토 색소폰**을 University of Iowa 에서 새로 받았다(제한 없이 공개,
다른 Iowa 표본과 같은 조건). 업라이트는 활이 아니라 **피치카토**로
받았다 — 재즈 워킹 베이스는 뜯는 연주라서 아르코를 쓰면 성격이 안
맞는다. 둘 다 세기 한 벌(다른 Iowa 표본과 같은 제약).

- `lib/sampler.dart` 의 `_kSampleSets` 에 `'upright'`·`'sax'` 추가,
  `kAddonPackSampleKeys` 에도 넣어서 무료 추가팩으로 분류(피아노·바이올린
  등과 같은 자리 — 밴드 기본팩이 아니라 특수 악기 취급).
- **재즈 쪽 코드는 한 줄도 안 고쳤다** — `song.dart` 의 재즈 편성이 이미
  `voice: 'upright'`/`voice: 'sax'` 로 정확히 그 이름을 쓰고 있어서,
  표본을 등록하는 순간 기존 표본 구조(`SynthNote.noteOn` 의 `kSampleBanks[voice]`
  분기)가 자동으로 그 소리를 대신 낸다 — 이게 이 표본 시스템을 애초에
  "악기 이름 하나로 갈아 끼우기"로 설계해 둔 이유다.
- `test/sampler_check_test.dart` 에 업라이트·색소폰 확인 추가. `flutter
  test` 전부 통과, `flutter analyze` 그대로 30건.

표본 총 용량: 기존 29MB + 업라이트 4.4MB + 색소폰 4.1MB = **37.5MB**
(기본팩 13.4MB·추가팩 24.1MB).

### 다음 (누적)

- 재즈의 나머지(`epiano`·`nylon`·`marimba`)는 아직 합성이다 — 더 좋게
  하려면 다음 후보: FreePats "ElectricPiano"(합성 로즈 재현이라 실물
  녹음은 아님), 나일론 기타는 Iowa 에 없다(클래식 기타 자체가 카탈로그에
  없음, VCSL 등 다른 출처를 봐야 한다).
- 용량이 37.5MB 로 늘었다 — 다음에 더 추가하기 전에 압축/다운로드형
  정책을 한 번 더 짚어야 할 때다.
- 여전히 남음: 진짜 다운로드형 추가팩.

## 일렉 피아노(FM epiano) 표본 추가 (2026-09-10)

재즈에 이어 남은 목록 중 영향 범위가 제일 넓은 걸 먼저 했다 — `epiano`
는 재즈뿐 아니라 **디스코·R&B 세 장르**가 같이 쓴다.

출처: **FreePats "FM Piano 1"**(CC0, 퍼블릭 도메인) — 진짜 1980년대
DX7(야마하 FM 신스)의 "E. Piano 1" 패치를, DX7 을 정확히 모델링한 무료
소프트웨어 신스(Hexter)로 재생해 녹음한 표본이다. 다른 Iowa 표본과
달리 **세기가 진짜 세 벌**(v60/v80/v100 → pp/mf/ff)이라 다이내믹이
제대로 갈린다. root 12개(F#1~C7, 반음 6개 간격).

- `lib/sampler.dart` 의 `_kSampleSets`·`kAddonPackSampleKeys`·
  `kSamplePan` 에 추가 — 다른 표본과 같은 패턴. `song.dart` 는 **또
  한 줄도 안 고쳤다**(재즈 때와 같은 이유 — voice 이름이 이미
  `'epiano'` 로 정확히 일치해서 표본 등록만으로 세 장르 전부에 자동
  적용된다).
- `test/sampler_check_test.dart` 에 확인 추가(자산 로드·피치 시프트·
  세기 두 벌이 실제로 다른지). `flutter test` 전부 통과, `flutter
  analyze` 그대로 30건.

표본 총 용량: 37.5MB → **46MB**(추가팩만 24.1→32.6MB).

### 다음 (누적)

- 재즈의 나일론 기타(`nylon`)·마림바(`marimba`)는 아직 합성이다 —
  나일론 기타는 Iowa·FreePats 카탈로그에 없어서(둘 다 스틸현 어쿠스틱만
  있음) 다른 출처(VCSL 등)를 봐야 한다.
- **용량이 46MB 까지 늘었다 — 다음 추가 전에 반드시 용량/압축·다운로드형
  정책을 짚어야 한다.** 계속 미루고 있다.
- 여전히 남음: 진짜 다운로드형 추가팩.

## 첫 화면 — 프로젝트 고르기로 다시 짰다 (2026-09-10)

사용자 요청: 첫 화면을 "전에 만들었던 뮤직두들 화면처럼" — 확인해 보니
원래 웹 버전(`~/AI Company/music_doodle/assets/app.html`)은 홈 화면 없이
바로 만들기 화면으로 열리는 구조였다. 대화로 방향을 구체화한 결과: DAW
테마 목업(로직·큐베이스·프로툴·스튜디오원, 4개를 Artifact로 만들어
비교) 중 **로직 프로의 "프로젝트 초이저"** 를 골랐고, 이어서 "프로젝트를
선택해서 들어가면 씬·타임라인·라이브·쇼로 구성"이라는 요구까지 반영했다.

**바뀐 구조:**
- 예전: 첫 화면 = "지금 곡" 카드 + 할 일 네 버튼(만들기·곡·라이브·쇼),
  버튼마다 **다른 화면**으로 push. 씬에서 라이브로 가려면 뒤로 → 첫 화면
  → 라이브, 세 걸음.
- 지금: 첫 화면 = **프로젝트 고르기**(로직 프로젝트 초이저 참고 — 왼쪽
  필터(최근/스타일별, 넓은 화면만) + 오른쪽 카드 그리드, 카드는
  `Store.songs` 그대로 사용). 카드를 고르면 `ProjectWorkspace`
  (`lib/ui/workspace_view.dart`, 새 파일) 하나가 열리고, 그 안에서
  씬·타임라인·라이브·쇼를 **위쪽 탭**으로 오간다 — 뒤로가기 없이.
  "뒤로"는 이제 프로젝트 자체를 나가 첫 화면으로 돌아가는 뜻이다.
- 타임라인의 "이 씬 고치기"는 이제 새 화면을 열지 않고 **탭만 씬으로
  바꾼다**(`_editScene`).
- 쇼 탭도 검게·꽉 차게 보여 주되(발표용, 예전 `bare:true` 와 같은 뜻)
  탭 줄은 남겨서 거기서 다른 모드로 바로 넘어갈 수 있게 했다(예전엔
  뒤로가기 하나뿐이라 쇼 → 라이브로 가려면 프로젝트를 통째로 나가야
  했다 — 오히려 개선).

**만들다가 걸린 것 — 탭 줄이 좁은 폰에서 넘쳤다.** 처음엔 탭 넷을 가로
스크롤 줄로 짰는데, 400dp 폰에서 뒤로가기·곡 이름·탭 넷·사용법을 다
넣으니 "쇼" 탭이 화면 밖으로 밀렸다. 시험에서 `tester.tap(find.text
('라이브'))` 가 조용히 다른 곳을 눌러(경고만 뜨고 실패는 나중에) 처음엔
원인을 못 찾았다 — `ensureVisible` 로 스크롤해서 눌러 보니 진짜 원인이
"탭이 화면 밖에 있다"는 실제 UI 버그였다는 게 드러났다. 스크롤로
때우지 않고 **탭 넷을 `Expanded` 로 똑같이 나눠 늘 다 보이게** 고쳤고
(좁은 화면에서는 곡 이름도 감춘다), 시험도 스크롤 없이 그대로 통과한다.

- 새 파일 `lib/ui/workspace_view.dart`(`ProjectWorkspace`). `home_view.dart`
  는 프로젝트 고르기로 다시 썼다(옛 `_Big`·`_SongHost` 는 지웠다 —
  `_pickStyle`·`SongView` 의 기존 로직을 그대로 재사용해서 새로 만든
  코드가 아니다).
- `main.dart` 는 **한 줄도 안 고쳤다** — `HomeView` 생성자 인터페이스를
  그대로 유지해서(project·transport·live·master·host·store·ready·
  audioError·onOpenLab·onSongOpened) 앱 진입점이 안 흔들렸다.
- `test/home_ui_test.dart`·`test/guide_ui_test.dart`·
  `test/simple_mode_test.dart` 를 새 구조에 맞게 고쳤다(첫 화면엔 이제
  믹서 버튼이 없다 — 믹서는 프로젝트 안에서만 연다). `flutter test`
  전부 통과(105개), `flutter analyze lib test` 그대로 30건.
- **시뮬레이터로 못 봤다** — 지난 회차와 같은 이유(이 맥에 전체 Xcode가
  없음). 웹 타깃도 이 프로젝트엔 없고, 새로 켜도 오디오 아이솔레이트가
  `Isolate.spawn` 을 쓰는데 그게 **웹에서 아예 안 된다**(다트 웹 컴파일은
  진짜 아이솔레이트를 지원 안 함) — 켜 봐야 소리 쪽은 어차피 못 본다.
  위젯 시험(레이아웃·탭 전환·넘침 체크 포함)으로 대신 확인했다.

### 2026-09-11 — 첫 화면 실기기 확인 → 버그 둘 찾아 고침

"폰에서 진행해봐" 요청으로 안드로이드 실기기(삼성, `RFKL505TRAZ`)에
APK 빌드·설치해서 직접 봤다. 위젯 시험은 다 통과했었는데도, 실기기
화면에서만 보이는 버그가 둘 나왔다 — 시험만으로는 못 잡는 것들이었다.

1. **빈 프로젝트 카드가 "로파이 · 78BPM" 으로 나왔다.** `_ProjectCard`
   가 `songGenreOf(meta.genre)` 를 곧장 불러서, `genre==''`(빈 프로젝트)
   일 때 `genreDef('')` 가 첫 장르(로파이)로 조용히 폴백하는 걸 그대로
   보여준 것. `home_view.dart` 에 `_genreLabel(key)` 헬퍼를 새로 만들어
   (`key.isEmpty` 면 "빈 프로젝트") 이 파일 안의 장르 라벨 자리 5곳
   (카드·사이드바·필터 칩·정렬·헤더)을 전부 이걸 쓰게 고쳤다.
2. **워크스페이스 탭 줄에서 "타임라인"·"라이브"가 "타…"/"라…" 로
   잘려 안 읽혔다.** 지난 회차에 가로 스크롤을 없애고 `Expanded` 로
   넷을 고르게 나눴는데도, 실기기는(`host != null`이라 믹서 아이콘까지
   붙어서) 폭이 더 좁아 글자가 못 버텼다. `workspace_view.dart`
   의 `_ModeTab` 을 고쳐 좁은 화면(`compact`)에서는 **아이콘만**
   보여주고 이름은 `Tooltip` 으로만 가지게 했다. `home_ui_test.dart`
   도 `find.text` 대신 `find.byTooltip` 으로 찾게 바꿨다(좁든 넓든
   똑같이 통함).

두 수정 다 `flutter analyze lib test` 그대로(30건 베이스라인),
`flutter test` 전부 통과 확인 뒤, APK 다시 빌드해서 실기기에
재설치 → 스크린샷으로 둘 다 직접 확인함(빈 프로젝트 라벨 정상,
탭 줄 잘림 없음).

**추가로 찾은 더 큰 버그 — 탭 줄이 실기기에서 아예 안 눌렸다.**
아이콘만 보이게 고친 뒤 다시 실기기로 확인하다가, `adb shell input
tap` 으로 탭·뒤로가기·믹서·사용법을 눌러도 화면이 하나도 안 바뀌는
걸 발견했다. 처음엔 탭 위젯(`_ModeTab`)의 문제로 의심했는데, 같은
줄의 `IconButton`(뒤로가기·믹서·사용법)까지 전부 안 눌려서 위젯
코드 문제가 아니라 **그 줄 전체가 시스템에 먹히고 있다**는 뜻이었다.
실제로 탭을 몇 번 반복하다 보니 알림창이 열려 있는 게 찍혔다 —
삼성 One UI가 화면 맨 위(상태 표시줄) 근처 탭을 알림창 당기기로
가로채고 있던 것.

원인: `workspace_view.dart` 의 `ProjectWorkspace` 가 세로 공간을
아끼려고 `SafeArea(top: false)` 로 위쪽 여백을 없앴는데, 그러면서
탭 줄이 **상태 표시줄과 같은 자리**에 그려졌다. 이 기종은(노치 대응
때문인지) 상태 표시줄의 터치 인식 영역이 눈에 보이는 것보다 넓어서,
탭 줄에 닿는 손짓이 앱까지 못 오고 시스템이 먼저 가져갔다. 첫 화면
(`home_view.dart`)은 처음부터 `SafeArea()` 를 그대로 썼기 때문에
이 문제가 없었다 — 워크스페이스만 위쪽을 깎은 게 화근.

고침: `SafeArea(top: false, ...)` 를 그냥 `SafeArea(...)` 로 되돌려
탭 줄을 상태 표시줄 아래로 내렸다(한 줄 수정). `flutter analyze`
그대로, 다시 빌드·설치해서 씬·타임라인·라이브·쇼 네 탭 전부 실제
손짓(`adb input tap`)으로 눌러 전환되는 걸 확인했고, 가로 모드도
같은 폰에서 돌려 봤다(`adb shell settings put system user_rotation`) —
가로에서도 탭 줄이 눌리고 레이아웃이 안 깨진다. 다만 가로에서
"타임라인" 글자가 "타임…" 으로 살짝 잘리는 건 남아 있다(사소함,
다음에 손볼 것).

**이 회차의 교훈:** 위젯 시험은 `tester.tap` 이 위젯 트리를 직접
찾아 누르기 때문에, 시스템이 그 좌표를 가로채는 이런 종류의 버그는
**절대 못 잡는다** — 진짜 손가락(또는 `adb input tap`)으로 실기기를
눌러 봐야만 나온다. `SafeArea(top: false)` 처럼 "여백을 줄인다"는
선택은 화면 최상단에 인터랙션 가능한 요소를 놓을 때 특히 조심해야
한다(시스템 제스처 영역과 겹칠 수 있다).

### 2026-09-11 — 라이브 반음 건반(실제 피아노) + 초보 모드

"라이브 건반 배치가 구리다, 12음계 다 쓰게 하자, 초보 모드에선 틀린
음은 흐리게·소리 안 나게" 요청으로 라이브 화면에 **반음 건반**을
새로 얹었다.

- 기본은 그대로 **다이아토닉 패드**(도수 7개 — 틀린 음이 안 남). 새
  「반음」 칩을 누르면 **실제 피아노 모양**(흰 건반 7 + 검은 건반 5,
  `lib/ui/live_view.dart` 의 `_PianoKeyboard`)으로 바뀐다. 자리는
  **음이름**(C·C#·D…)으로 고정해 조가 바뀌어도 피아노 모양 자체는
  안 흔들린다 — 으뜸음이 어느 건반인지만 색으로 옮겨 다닌다.
- 계산은 기존 관례대로 `live_ops.dart` 에 새 `ChromaticPad` 로
  떼어 놨다(패드가 도수를 쓰던 `LivePads` 와 짝). `semiFreq`(이미
  편집기 프로 모드가 쓰던 함수)를 그대로 써서 반주·편집기와 어긋나지
  않는다. 화음·아르페지오는 반음에 없다 — 다이아토닉 코드 계산이
  도수를 전제하기 때문에 일부러 뺐다(주석에 이유 적어 둠).
- **초보 모드**(`Store.beginner`, 기본 켜짐, 설정 시트에 새 항목) —
  켜져 있으면 지금 조에 안 맞는 반음 건반이 흐려지고 눌러도 소리가
  안 난다(`_muted`). 다이아토닉 패드가 주던 「틀린 음이 안 나온다」
  약속을 반음 건반에서도 지키되, 원하는 사람은 설정에서 끌 수 있다.
- 녹음(`LiveRecorder`)도 반음을 그대로 받는다 — `NotePatternDef`
  에 이미 있던 `chromatic` 플래그(편집기 프로 모드가 쓰던 것과 같은
  자리)를 얹었다. **녹음 도중에는 반음↔도수 전환을 막는다**(한 판
  안에 두 가지 뜻이 섞이는 걸 막으려고 `_recChromatic` 으로 시작할
  때 잠근다).
- 실기기(`RFKL505TRAZ`)에서 직접 확인: 피아노가 정확한 흰/검 건반
  모양으로 뜨고, 으뜸음이 색으로 보이고, 초보 모드에서 스케일 밖
  건반(D·E·A·B 등, 조에 따라 다름)이 흐리게 나오며 눌러도 색이 안
  바뀐다(소리·녹음도 안 탄다) — 스크린샷으로 확인.
- `test/live_chromatic_check_test.dart`(새 파일) — `ChromaticPad`
  주파수·음계 판정 계산을 숫자로 확인. `test/live_ui_test.dart` 에
  「반음 건반 전환」시험 새로 추가(큰 시험과 분리 — 같은 파일 안
  여러 `pumpWidget` 뒤에 놓으면 `ensureVisible`+`tap` 이 이상하게
  씹히는 문제가 있어[원인 못 찾음], 별도 `testWidgets` 로 뺐더니
  깨끗하게 통과했다 — 나중에 비슷한 증상 보이면 참고).
- `flutter analyze lib test` 그대로(30건), `flutter test` 107개
  전부 통과.

### 2026-09-12 — 첫 화면 카드에 미리보기(씬·타임라인·쇼)

"프로젝트 선택할 때 미리보기(타임라인·씬·쇼모드 설정에서 선택 가능)를
넣자" 요청으로 홈 화면 카드 위쪽의 단색 아이콘 배지를 **작은 그림
미리보기**로 바꿨다.

- `Store.previewMode`(설정 새 항목 "카드 미리보기" — 씬/타임라인/쇼,
  기본 씬) 하나로 **카드 전체가 한꺼번에** 무엇을 그릴지 정한다(카드
  마다 따로 고르는 방식은 일부러 안 함 — 사용자가 그렇게 정했다).
  - 씬: 악기 넷(드럼·베이스·코드·멜로디)을 색 블록으로 — 씬 화면
    악기 줄 색(초록·파랑·보라·호박)과 맞췄다. 패턴이 없으면 테두리만.
  - 타임라인: 구간을 순서대로 색 막대로 늘어놓는다(이름을 해시해
    막대 색을 고른다 — 값이 늘어도 표를 안 고쳐도 된다).
  - 쇼: 같은 「켜진 악기」 값을 어두운 무대 배경 위 점들로.
- **곡 파일을 열지 않고 그린다** — `sec`(길이)를 저장할 때 같이
  적어 두던 기존 관례를 그대로 따라, `SongMeta` 에 `sceneActive`
  (악기별 켜짐)·`timelineScenes`(구간 이름 순서)를 새로 얹어 저장할
  때마다 채운다(`Store.saveNow`/`_createFrom`). 옛 곡은 `null` 로
  읽히고, 기존에 있던 "길이 채우기" 한 번짜리 배치(`_fillMissingLengths`,
  여는 김에 곡 파일을 한 번 열어야 하는 자리)에 얹어 같이 채운다 —
  새 배치를 따로 만들지 않았다.
- `test/card_preview_check_test.dart`(새 파일) — 계산(`sceneActiveOf`/
  `timelineScenesOf`) 과 저장·재로딩 왕복을 확인. `test/home_ui_test.dart`
  에 세 모드 다 렌더 예외 없는지 보는 시험 추가 — **`tester.runAsync`
  로 감싸야 했다**(실제 파일을 쓰는 `Store` 를 위젯 시험의 가짜 시계
  안에서 바로 부르면 끝나지 않고 걸린다 — `guide_ui_test.dart` 가
  이미 쓰던 패턴인데 처음엔 빠뜨려서 시험이 무한정 멈췄다).
- 실기기(`RFKL505TRAZ`)에서 세 모드 다 설정에서 바꿔 가며 스크린샷
  으로 확인 — 씬(색 블록 4개) · 타임라인(구간 수만큼 막대) · 쇼(무대
  점) 전부 정상.
- `flutter analyze lib test` 그대로(30건), `flutter test` 109개
  전부 통과.

### 2026-09-12 — 타임라인 구간 끌기(순서·길이)

"편집 제스처를 구체화하자"는 요청에 "타임라인 구간 순서 바꾸기·길이
조절 먼저"로 범위를 좁혀 받아, `song_view.dart` 의 타임라인(마디 비율
그림) 구간 카드에 **손잡이 둘**을 얹었다 — 왼쪽은 순서, 오른쪽은
길이(판 수). 밑에 있던 계산(`Arrangement.move`/`setReps`)은 이미
버튼(◀▶ · −/+)으로 쓰던 것 그대로다 — **새 계산 없이 손짓만 얹었다.**

- 오른쪽 모서리를 끌면 씬 한 판(반복 하나) 너비만큼마다 판 수가
  ±1(`_resizeUpdate` — 끈 거리를 `SceneSequencer.sceneLoopBars` 로
  나눠 몇 판인지 셈한다). 왼쪽 모서리를 끌면 옆 구간의 절반을 넘는
  순간 자리를 바꾼다(`_dragUpdate`) — `ReorderableListView` 의 "넘으면
  바로 바뀐다" 느낌을 마디 비율로 늘어선 이 줄에서도 냈다.
- **막힌 데 하나, 원인 찾는 데 제일 오래 걸렸다.** 처음엔 카드 몸통
  전체에 `GestureDetector.onHorizontalDrag*` 를 달았는데, 실기기는
  고사하고 시험에서부터 **아무리 끌어도 순서도 길이도 안 바뀌었다.**
  까닭: 이 줄 자체가 `_LaneScroll` 안에서 가로로 긴 `SingleChildScrollView`
  다 — 카드 위의 가로 끌기가 그 스크롤과 **같은 손짓**이라 다투게 되고,
  둘 다 평범한 드래그 인식기라 스크롤이 이겼다(카드는 한 번도 안
  움직였다). `Draggable` 이 `ListView` 안에서도 잘 끌리는 것과 같은
  길로 고쳤다 — `RawGestureDetector` + `ImmediateMultiDragGestureRecognizer`
  (새 위젯 `_ClaimDrag`)를 **좁은 손잡이에만** 달아서, 손을 대는 즉시
  그 손짓을 채가게 했다. 카드 몸통의 탭(고르기)·꾹 누르기(여기서부터
  재생)는 그대로 둬서 이 문제와 안 얽힌다.
- 손잡이는 아이콘이 아니라 세로선 둘을 직접 그렸다(`_ResizeGrip`,
  `Semantics` 라벨 "구간 순서 손잡이"/"구간 길이 손잡이") —
  `Icons.drag_indicator`(점 여섯)를 그대로 쓰면 목록 모드의 순서
  손잡이와 모양이 겹쳐 헷갈린다.
- `test/song_timeline_drag_check_test.dart`(새 파일) — 순서 손잡이를
  끌면 실제로 `song.sections` 순서가 바뀌는지, 길이 손잡이를 양쪽으로
  끌면 판 수가 늘고 줄고 1판 밑으로는 안 내려가는지를 시험이 직접
  손짓(`tester.startGesture`+`moveBy`)으로 확인한다. 처음엔 손잡이를
  `find.bySemanticsLabel` 로 찾으려 했는데 그 라벨이 카드 전체의
  시맨틱 노드에 다른 글자와 뭉쳐 못 찾았다(`Semantics(container: true)`
  로 한 번 고쳤지만, 결국 더 단순한 `ValueKey` 로 찾는 쪽을 최종으로
  택함 — 시맨틱 핸들을 테스트마다 열고 닫는 부담이 없다).
- `flutter analyze lib test` 그대로(30건), `flutter test` 110개
  전부 통과. **실기기 확인은 아직** — 빌드까지는 끝냈는데 폰이
  USB에서 잠깐 빠져 다음에 이어서 스크린샷으로 봐야 한다.

### 2026-09-12 — 타임라인 끌기 실기기 확인 결과 (부분 성공)

**모서리 손잡이(길이 조절)는 실기기에서 시험 못 함 — 진짜 손가락으로
직접 확인 필요.** `adb shell input swipe` 로 여러 번 시도했는데(정확한
좌표는 `uiautomator dump` 로 잡아 확인함) 손잡이까지는 닿는데(raw
`Listener.onPointerDown` 로그가 찍힘) `ImmediateMultiDragGestureRecognizer.
onStart` 가 실기기에서는 한 번도 안 불렸다 — 위젯 시험(`song_timeline_
drag_check_test.dart`)에서는 `tester.startGesture`/`moveBy` 로 멀쩡히
통과하는데, 이게 실제 손짓과 `adb` 로 흉내 낸 손짓의 차이인지, 이
기종만의 문제인지, 아니면 아직 못 찾은 진짜 버그인지 **원인을 못
밝혔다.** 저수준 방식(`recognizer.addPointer` 직접 호출, `Draggable` 이
쓰는 것과 같은 길)으로 한 번 고쳐 봤지만 실기기 결과는 같았다.

**다음에 이어서 볼 사람에게:** 코드는 `lib/ui/song_view.dart` 의
`_ClaimDrag`/`_ClaimDragState` — 왼쪽 모서리(순서)·오른쪽 모서리(길이)
둘 다 이 길을 쓴다. 실제 손가락으로 폰을 눌러 봐서 되면 `adb` 흉내
손짓만의 문제였던 것이고, 안 되면 진짜 버그니 원인을 더 파야 한다.
버튼(◀▶·−/+, `_BlockBar`)은 그대로 남아 있어 손짓이 안 되더라도 순서·
길이를 바꾸는 길 자체는 막히지 않는다.

### 2026-09-12 — 조작 화면 정리(장르·조를 「프로젝트 설정」으로)

"쓸데없는 기능 빼고 장르선택 빼고 키·박자 선택은 설정에서 하도록 조작
화면을 깨끗하게" 요청으로, 씬 화면 위쪽에 늘 떠 있던 스타일·조 칩과
타임라인 위쪽의 스타일 줄을 **전부 걷어내고** 새 시트(`lib/ui/
project_settings_sheet.dart`, "프로젝트 설정")로 모았다. 박자는 따로
손잡이를 안 뒀다 — 애초에 독립된 값이 아니라 스타일이 정하는 값이라
(`create_sheet.dart`의 박자 필터가 그 자리), 스타일 고르기 안에 이미
있다.

- 작업 화면 위쪽 모드줄(`_ModeBar`)에 톱니바퀴 아이콘을 새로 놓아
  프로젝트 설정을 연다 — 씬·타임라인·라이브·쇼 어느 모드에서도 한
  자리에서 열린다(사용자가 직접 고른 위치).
- 씬 화면(`scene_view.dart`)의 `_Transport`에서 `genre`/`onPickStyle`/
  `simple`(조 감춤용) 필드와 `styleChip`/`keyChip`, 그리고 `_pickStyle`/
  `_pickKey` 함수를 통째로 걷어냈다 — 남은 것은 재생·빠르기·느낌뿐이다.
- 타임라인(`song_view.dart`)의 `_GenreBar`(스타일 줄, "시티팝106
  발라드68…" 칩들)도 통째로 지웠다.
- 새 시트는 기존 로직을 **그대로 재사용**한다 — 스타일 고르기는
  `showCreateSheet`(박자 필터 포함) 그대로, 조 고르기는 씬 화면에
  있던 12키×장/단조 칩 그대로. 스타일을 바꿨을 때 "손으로 고친 판이
  어떻게 됐는지" 알려주는 문구(`genreChangeNote`, 원래 타임라인에만
  있던 것)를 여기로 옮기면서 **씬 화면에서 바꿀 때도** 그 안내를
  받게 됐다(전에는 씬 화면에서 바꾸면 그 안내가 없었다 — 조용한 개선).
- 시험: `test/project_settings_ui_test.dart`(새 파일, 시트 자체 확인) ·
  `test/create_flow_test.dart`·`test/scene_ui_test.dart`·`test/
  simple_mode_test.dart`(칩이 이제 화면에 없다는 것으로 고쳐 씀 — 옛
  칩 존재를 확인하던 5개 남짓한 항목을 정리).
- 실기기 확인: 씬·타임라인 둘 다 칩이 사라지고 톱니바퀴로 새 시트가
  열리는 것, 시트 안에서 스타일 이름·12키가 바로 보이는 것 스크린샷
  으로 확인함.
- `flutter analyze lib test` 그대로(30건), `flutter test` 111개 전부
  통과.

### 2026-09-12 — 타임라인 끌기, 원인 찾음: `adb input swipe` 만의 문제였다

바로 위 "부분 성공" 항목의 후속. `adb shell input swipe`(시작·끝만 주는
빠른 흉내 손짓)로는 실기기에서 한 번도 안 먹혔는데, **`adb shell input
draganddrop`**(눌러서 잠깐 있다가 서서히 옮기는, 진짜 손짓에 훨씬 가까운
흉내)으로 다시 해 보니 **순서 손잡이도 길이 손잡이도 둘 다 바로 됐다**
(스크린샷으로 직접 확인 — 판 수가 늘고, 순서도 바뀌었다). 그러니 코드
버그가 아니라 **`input swipe` 가 `ImmediateMultiDragGestureRecognizer`
가 반응할 만큼 촘촘한 손짓을 안 만든 것**이었다 — 실제 손가락(또는
`draganddrop`)이면 문제없다. 다만 **진짜 손가락으로 마지막 확인은
아직 안 함** — 사용자가 폰으로 직접 눌러 보면 좋다.

### 2026-09-12 — 느낌도 「프로젝트 설정」으로

"분위기 조절 손잡이도 설정으로 넣고 설정창도 정리해" 요청으로 씬
화면에 마지막까지 남아 있던 「느낌」 칩도 걷어냈다 — 이제 씬 화면
위쪽은 **재생·빠르기 둘뿐**이다. `project_settings_sheet.dart` 에
"느낌" 항목을 스타일 다음(조보다 앞)에 추가했다 — 자주 만지는 순서로
뒀다. 느낌은 다섯 손잡이짜리 자기 시트(`feel_sheet.dart`)가 이미
있어서, 새로 만들지 않고 한 줄 손잡이(지금 느낌 이름)를 누르면 그
시트가 프로젝트 설정 위에 겹쳐 열리게만 했다.

- `scene_view.dart` 의 `_Transport` 에서 `feel`/`onPickFeel` 필드와
  `feelChip`, `_pickFeel` 함수, 그리고 그걸 담던 `Wrap`/조건부 레이아웃
  (`wide` 분기)까지 통째로 걷어냈다 — 남은 건 재생 버튼 + 빠르기
  슬라이더 + "▶ 눌러서…" 안내 한 줄. `_Chip` 위젯도 이 파일에서
  아무도 안 써서 같이 지웠다(시트 쪽 `_Chip` 은 별개 파일이라 안 건드림).
- 시험: `test/project_settings_ui_test.dart` 에 느낌 손잡이 확인(5·6번)
  추가, `test/simple_mode_test.dart` 의 "느낌은 남는다" 체크는 대상이
  없어져서 지움(주석으로 이유 남김).
- 실기기 확인: 프로젝트 설정 시트에 스타일→느낌→조 순서로 다 뜨는 것,
  씬 화면이 재생·빠르기만 남아 훨씬 헐렁해진 것 스크린샷으로 봄.
- `flutter analyze lib test` 그대로(30건), `flutter test` 111개 전부
  통과.

### 2026-09-12 — 빠르기(BPM)도 「프로젝트 설정」으로, 되돌리기 알림 짧게

"되돌리기 알림이 너무 오래 떠 BPM설정도 설정탭에서 해야지" 요청.
스타일·느낌·조에 이어 BPM 슬라이더도 씬 화면에서 걷어내
`project_settings_sheet.dart` 로 옮겼다(스타일 다음, 느낌보다 앞 —
자주 만지는 순서). 씬 화면 위쪽은 이제 "빠르기 104 BPM" 처럼 **읽기
전용 글자**만 남았다(만지는 손잡이는 프로젝트 설정에만 있다).

- `scene_view.dart` 의 `_Transport` 에서 `onBpm` 콜백·`Slider`·
  `SliderTheme` 를 통째로 걷어내고, `transport.bpm` 을 보여만 주는
  `Row` 로 바꿨다. `_SceneViewState.build()` 에서 `_Transport` 로
  넘기던 `onBpm` 인자도 같이 지웠다(더는 안 쓴다).
- `project_settings_sheet.dart` 에 "빠르기" 섹션을 스타일과 느낌
  사이에 추가 — 슬라이더를 만지면 `project.setBpm(transport, v)` 호출
  + `onChanged()`(재생 중이면 다음 판부터 새 빠르기, 다른 항목과 같은
  경로).
- 스타일 바꿀 때 뜨는 "되돌리기" 스낵바 지속 시간을 6000/4000ms →
  4000/2500ms 로 줄였다(`project_settings_sheet.dart`) — "너무 오래
  뜬다"는 지적 반영. 다른 화면(씬 지우기 등)의 되돌리기 알림은 원래
  3~7초 그대로 두었다(그쪽은 별 불만 없었다).
- `flutter analyze lib test` 그대로(30건, 새 이슈 없음), `flutter test`
  111개 전부 통과.
- 실기기 확인: 씬 화면에 빠르기 글자만 남고 슬라이더가 사라진 것,
  프로젝트 설정 시트에 스타일→빠르기→느낌→조 순서로 다 뜨는 것,
  스타일을 바꾸면 빠르기 값도 같이 바뀌는 것 스크린샷으로 확인함.

### 2026-09-12 — 샘플곡 서랍 + 프로젝트 삭제 + 장르별 자동 마스터링

큰 묶음 요청 하나를 한 번에 처리했다.

**샘플곡 서랍** (`lib/song_samples.dart` 새 파일) — 홈 화면 맨 위 "최근
프로젝트" 아래에 "샘플곡" 서랍을 새로 넣었다. 대표 장르 5개(로파이·
하우스·힙합·발라드·팝) + 연주곡 1개(시티팝, 멜로디 전혀 없음), 총 6개.
씬이 아니라 **타임라인**(인트로→벌스→코러스→브레이크→아웃트로, 장르
프리셋의 기존 송폼 그대로)으로 만들었고, 멜로디는 간주(인트로·브레이크·
아웃트로)에만 있고 벌스·코러스엔 없다(`Project.userNote` 에 손으로 쓴
새 패턴을 얹고, 그 씬의 `clips` 만 골라 null 처리 — 라이브러리 패턴은
안 건드림). 멜로디도 코드톤(0·2·4도)만 쓰지 않고 지나가는음(1·3·5·6도)
을 섞어 새로 썼다. 팝은 멜로디 트랙이 둘(목소리 `voc`·아르페지오
`plk`)이라 목소리만 간주 규칙을 태우고 아르페지오는 장르가 짜 준 대로
뒀다.

앱을 처음 켤 때(`Store.start()`, `seedSamples: true` — `main.dart` 만
켠다, 시험은 기본 꺼짐) 있던 프로젝트를 전부 지우고 샘플곡 6개 + 빈
테스트 프로젝트 1개로 다시 시작한다. `index.json` 에 `samplesSeeded`
깃발을 남겨 **한 번만** 한다.

**버그 하나 잡음(실기기에서 직접 열어 보고서야 발견)**: 샘플 `Project`
객체의 `name` 필드를 안 채워서(`Project.initial()` 기본값 '새 곡'이
그대로 파일에 저장됐다) 샘플곡을 한 번 열었다 저장하면(`saveNow` 가
`meta.name = project.name` 으로 되돌린다) 목록 이름이 "새 곡"으로
덮어써졌다. `song_samples.dart` 를 실제로 여닫아 보고서야 잡은 것 —
`p.name = def.name;` 한 줄 추가로 고쳤고, `test/sample_seed_check_test.dart`
에 "열었다 저장해도 이름이 그대로"를 못박는 시험을 추가했다.

**프로젝트 삭제 기능** (`home_view.dart`) — 카드 오른쪽 위 ✕ 아이콘.
확인 창 없이 바로 지우고 되돌리기 스낵바를 준다(이 앱의 삭제는 늘 이
규칙). 내 프로젝트가 하나만 남으면 막는다(샘플곡은 안 셈).

**프로젝트 설정 「스타일 바꾸기」가 편성을 안 건드리게 바뀜**
(`project_settings_sheet.dart`) — 여태는 `setGenre` 를 그대로 불러
트랙·씬·패턴까지 통째로 갈아치웠는데, 이제는 **박자(빠르기·시간표기)와
마스터링만** 바꾸고 트랙·씬·패턴(편성)·조는 그대로 둔다. 되돌리기도
그 넷(장르명·박자·빠르기·마스터 인서트)만 담아서 가볍다.

**장르별 자동 마스터링(신설)** — `fx.dart` 에 `kGenreMasterPreset`(장르
→ 기존 `kMasterFxPresets` 이름)을 추가하고, `MasterChannel` 에
`applyGenrePreset`/`fxAuto` 를 얹었다(트랙의 `fxAuto` 와 완전히 같은
규칙 — 사용자가 마스터 랙을 한 번이라도 만지면 그 뒤로는 장르가 안
건드린다). 스타일 바꾸기·새 곡·프로젝트 초기화·샘플곡 만들기 네
자리에서 전부 이 프리셋이 자동으로 걸린다.

**BPM 입력 방식도 바뀜**(`project_settings_sheet.dart`) — 슬라이더 대신
-/+ 버튼과 숫자 직접 입력 칸.

- 시험: `test/song_samples_check_test.dart`(새 파일 — 간주에만 멜로디·
  지나가는음 확인) · `test/sample_seed_check_test.dart`(새 파일 — 심기
  1회성·이름 버그 회귀) · `test/project_style_change_test.dart`(새 파일
  — 스타일 바꾸기가 편성을 안 건드리는지) · `test/project_settings_ui_test.dart`
  (master 인자 추가로 수정).
- `flutter analyze lib test` 그대로(30건, 새 이슈 없음), `flutter test`
  114개 전부 통과.
- 실기기 확인: 샘플곡 6개 서랍 스크린샷, 로파이 샘플의 인트로/코러스
  패턴 차이 확인, 스타일을 팝으로 바꿔도 트랙·패턴이 그대로인 것과
  스낵바 문구, 되돌리기 동작, BPM +/- 버튼 동작, 앱을 지웠다 다시 깔아
  이름 버그가 고쳐진 것까지 전부 스크린샷으로 봄.

### 2026-09-13 — 기타 코드 보이싱 · 악기 설정(EQ) · 타임라인 트랙탭→믹서 ·
음표 제스처 손끝 확인 · 샘플곡을 씬 대신 타임라인 레인으로 재구성

큰 묶음 하나를 더 처리했다.

**기타 코드 보이싱이 실제 바레 코드 손 모양으로 바뀜** (`theory.dart` 의
`guitarChordMidi`/`kGuitarChordShape`) — 여태는 기타도 피아노·패드와 똑같이
한 옥타브 띠 안에 촘촘히 코드톤을 쌓았다. 이제 기타/나일론 음색이면 오픈 E
장조/단조 모양(6음, 두 옥타브 가까이 퍼짐, 근음·5도가 겹침)을 그대로 프렛만
옮겨 짚는다 — 자리바꿈(`voiceLead`)은 안 쓴다(기타는 코드마다 같은 손 모양을
옮겨 짚지, 건반처럼 자리를 바꾸지 않는다). `buildChordPattern(voice:)` 로
스며든다. 목록에 없는 종류(9화음 등)는 기존 길로 되돌아간다.

**악기 설정(장르적 악기 세팅)** (`instrument_tone.dart`, 새 파일) — 기타는
본체(스트랫/레스폴)·픽업(넥/미들/브릿지), 피아노는 모델(그랜드/업라이트)·
에이징(0~100%). 표본을 새로 못 구하니 **트랙의 3밴드 EQ**(이미 재생에
실리는 자리)에 그 성격을 얹는 방식이다. 믹서의 채널 시트(`mixer_view.dart`
`showChannelSheet`)에 "악기 설정" 구간으로 들어갔다 — 만지면 그 트랙
`mixAuto` 가 꺼져(장르 자동 EQ가 안 덮음) 트랙의 다른 손잡이와 같은 규칙.

**타임라인에서 악기 트랙 이름을 누르면 믹서가 바로 뜬다** (`song_view.dart`)
— `showChannelSheet` 를 그대로 재사용. 씬 화면까지 안 가도 타임라인에서
바로 볼륨·팬·EQ·악기 설정·인서트를 만질 수 있다.

**음표 편집 제스처 — 이미 있던 것 확인 + 손끝 다듬기** — 화음/음표 편집기의
"꾹 눌러 옆=길이·위아래=높낮이" 드래그는 이미 있었고 실기기에서 재확인함
(길이 2→7칸, 도수 이동 둘 다 동작). 다듬은 것: (1) 고른 음의 오른쪽 끝에
작은 손잡이 표시(타임라인 리사이즈 그립과 같은 생김새)를 그려 어디를 밀면
길이가 느는지 눈으로 보이게 함, (2) 음을 잡을 때·자리바꿈이 일어날 때·
길이/높낮이로 방향이 잠길 때 햅틱(진동) 피드백 추가 — GarageBand 류가 흔히
쓰는 손끝 확인.

**악기(음색) 고르기를 계열별 서랍으로 정리** — `instruments.dart` 의
`kVoiceFamily`(건반·뜯고 치는·관악기·현악기·신스·베이스·목소리)가 이미 있었는데
안 쓰이고 있었다. 트랙의 "악기" 고르기(`scene_view.dart` `pickFromSheet`)와
믹서의 "라이브 악기" 고르기(`mixer_view.dart`)에 이 표를 얹어 33개를 한 줄에
쭉 늘어놓던 것을 계열별 묶음으로 갈랐다. 드럼 키트는 계열이 없어 여태처럼
한 줄.

**샘플곡을 "씬 프리셋" 대신 "타임라인 레인"으로 다시 짬** (`song_samples.dart`
전면 재작성) — 여태는 `setGenre` 가 짜 준 씬(구간 프리셋)에 멜로디만 손보는
식이었다. 사용자가 "씬으로 만들지 말고 타임라인에서 악기 트랙 하나하나
만들어서 짜라"고 요청 — 이제 각 샘플은:
- 씬을 전부 **이름·마디수만 있는 빈 자리**로 두고(클립 없음),
- `Arrangement.lanes`(`putLane`)에 트랙마다 패턴을 직접 놓는다. 레인은
  **되풀이하지 않으므로**(씬 클립과 다른 점, `sequencer.dart`) 구간이 판
  길이보다 길면(8마디 벌스에 4마디 판) 그 길이 간격으로 여러 번 이어
  놓는다(`_lane` 헬퍼) — 사람이 같은 클립을 복사해 붙인 것과 같은 자료
  모양이다.
- 팝 샘플은 밴드 편성(7트랙: 드럼·베이스·피아노·기타·보컬·아르페지오·현)이라
  `Project.initial()` 기본 4트랙에 `addTrack` 으로 셋을 더 만든다.

**버그 하나 더 잡음(실기기에서 직접 재생해 보고서야 발견)**: 씬이 비어 있으면
`loopBarsOf` 가 항상 4(기본값)로 떨어지는데, 새 `Section` 을 전부
`reps: 1` 로 고정해 둬서 8·16마디짜리 벌스·코러스가 전부 4마디로 잘려
나왔다(곡 길이가 2:15 대신 1:26으로 나와서 알아챔). `reps` 를
`(원하는 마디 수 / loopBarsOf).round()` 로 고쳤다.
`test/song_samples_check_test.dart` 에 "구간이 잘리지 않고 온전한 마디
수" 회귀 시험을 추가했다.

- 시험: `test/guitar_voicing_check_test.dart`(새 파일)·
  `test/instrument_tone_check_test.dart`(새 파일)·
  `test/song_samples_check_test.dart`(레인 구조로 전면 재작성 + 마디 수
  회귀 시험 추가).
- `flutter analyze lib test` 그대로(30건, 새 이슈 없음), `flutter test`
  116개 전부 통과.
- 실기기 확인: 샘플곡을 새로 심어(앱 데이터 지우고 재설치) 씬 화면이
  전부 "패턴 없음"인 것, 타임라인 화면에 트랙별 클립 블록이 실제로 놓여
  있는 것(8마디 벌스에 4마디 판 두 개), 곡 길이 2:15 로 돌아온 것,
  트랙 이름을 눌러 믹서가 뜨는 것, 음표를 꾹 눌러 길이·높낮이를 바꾸는
  것, 화음이 기타 음색일 때 실제 바레 코드 모양으로 나는 것까지 전부
  스크린샷/시험으로 확인.

## 2026-09-13 — 가로모드·세로모드 전체 화면 확인

**한 일**: "모든 화면 가로모드·세로모드 확인해" 요청 처리. 실기기(갤럭시,
`RFKL505TRAZ`)를 `adb shell settings put system user_rotation 1` 로 강제
가로 회전시켜 첫 화면·씬·타임라인·노트 편집기를 스크린샷으로 직접 확인 —
전부 반응형으로 잘 접힌다(첫 화면은 가로에서 왼쪽에 필터 사이드바까지
따로 생긴다). 손으로 화면마다 다 돌려 보기엔 화면 수가 많고(씬·타임라인·
라이브·쇼·노트 편집기·믹서 시트·프로젝트 설정 시트·첫 화면) 빠뜨리기
쉬워서, `test/orientation_check_test.dart`(새 파일)를 추가해 자동으로
같이 돌게 했다 — 이미 있던 `feel_ui_test.dart` 의 "세로/가로 안 넘침"
확인 패턴을 그대로 따랐다. 폰 세로(411×890)·가로(890×411) 크기로 작업
화면 4탭 전부, 노트 편집기, 첫 화면을 띄워 `RenderFlex` 오버플로 같은
예외(`tester.takeException()`)가 나는지 잡고, 믹서 채널 시트·프로젝트
설정 시트는 실기기에서 제일 낮은 축에 해당하는 가로 높이(340px)로 한
번 더 확인 — 이 둘은 시트 안에서 `maxHeight: 화면높이×0.85` +
`SingleChildScrollView` 로 이미 짜여 있어서 낮은 화면에서도 스크롤만
될 뿐 잘리지 않는다.

**결과**: 오버플로 버그 없음 — 이 앱은 처음부터 `Expanded`/`Column`
위주로 짜여 있어서 가로에서도 별문제 없이 접혔다. 추가로 고칠 코드는
없었고, 재확인이 다음에도 자동으로 돌도록 시험만 추가.

- 시험: `test/orientation_check_test.dart`(새 파일, 4개 케이스).
- `flutter analyze lib test` 그대로(30건), `flutter test` 120개 전부
  통과(116 → 120).
- 실기기 확인: 첫 화면·씬 화면·라이브 화면을 가로로 스크린샷 찍어 직접
  봄(포함: BPM이 이미 설정탭 전용인 것, 되돌리기 알림이 3초인 것도
  이번에 코드로 재확인 — 지난 요청 (a)가 이미 반영돼 있었다).

### 다음 (누적)

- **"직접 곡을 써 보면서 불편한 점 찾아 UI/UX 개선" 요청도 들어옴** —
  아직 못 함. 다음에 이어서.
- **타임라인 손짓, 진짜 손가락으로 마지막 확인 한 번 남음** — 코드는
  `draganddrop` 흉내로 이미 됨 확인했다(위 항목). 급한 건 아니다.
- 이어서 "폰에서 UI/UX 확인하고 개선"을 계속 진행 중 — 더 나오면 이
  파일에 계속 追加.
- 남은 요청(2026-09-11 한 번에 몰아서 받은 것 중 아직): 씬 화면·미디
  (음표) 편집 제스처도 구체화(타임라인 다음 순서로 사용자가 정함) —
  **부분 완료**: 있던 제스처 확인 + 손끝(햅틱·시각 손잡이) 다듬음. 씬
  화면 자체의 새 제스처는 아직.
- **iOS 시뮬레이터 확인이 아직 안 됐다** — Xcode 설치가 먼저다(사용자
  비밀번호 필요, 이전 회차에도 같은 벽).

## 2026-09-13 — 직접 곡을 써 보며 UX 점검

**한 일**: "직접 곡을 써 보면서 불편한 점 찾아 UI/UX 개선해" 요청 처리.
실기기에서 새 프로젝트를 만들어 처음 쓰는 사람처럼 끝까지 따라가 봄 —
프로젝트 설정(스타일·빠르기·조), 씬 화면에서 트랙별 음색·패턴 고르기,
가락 패턴 고르기 시트(전부 보기로 스타일별 40개 넘는 패턴 확인),
"연주 녹음" 칩(현재 재생을 그대로 녹음해 두는 기능 — 트랙 하나만 따는
게 아니라 전체 믹스를 뜨는 것이었다, 확인만 하고 지움), 마지막으로
설정에서 "손잡이"를 「전부 보기」로 바꿔 노트 편집기 진입점(연필 아이콘)을
열고 리드 트랙에 직접 음 3개를 찍어 재생까지 확인.

**결과**: 노트 편집기 자체(탭으로 음 찍기 → 바로 선택되어 길이·세기·
글라이드 조절 패널이 뜸 → 재생하면 재생 막대가 따라감)는 매끄럽고
군더더기 없었다 — 지난 회차에 다듬은 햅틱·시각 손잡이가 잘 작동한다.
따로 고칠 코드는 없었음.

**찾은 것 하나(버그 아님, 설계 결정)**: 새 프로젝트는 기본이 "간단히"
모드(`Store.simpleMode = true`)라, 가락·화음 트랙의 패턴 고르기 시트를
열어도 "전부 보기"까지 눌러 봐도 미리 만든 패턴들만 보이고 **빈 판에
직접 그리는 길이 그 시트 안에는 없다** — 노트 편집기 진입점(연필
아이콘)이 「간단히」 모드에서는 아예 안 보이게 숨어 있어서다(의도된
설계 — `scene_view.dart` 주석: "간단히 보기에서는 감춘다 — 음을 하나씩
찍는 건 한 단계 깊은 일이다"). 꺼내려면 설정 ▸ 손잡이 ▸ 전부 보기로
바꿔야 한다. "음악 낙서장"이라는 이름값(자유롭게 낙서하듯 그리기)을
생각하면, 패턴 고르기 시트 안에 "간단히" 모드에서도 보이는 "직접
그리기" shortcut을 하나 넣을지는 제품 방향을 정하는 사람(사용자)의
판단이 필요해 보여 코드는 건드리지 않고 여기 기록만 남긴다.

- 테스트용으로 만든 "새 곡 8" 프로젝트는 확인 후 지웠고, 설정의
  "손잡이"도 원래 있던 "간단히"로 되돌려 놓았다 — 실제 앱 상태는
  이번 점검 전과 같다.
- 코드 변경 없음(찾은 것이 버그가 아니라 의도된 설계라서). `flutter
  analyze`·`flutter test` 는 이 회차에서 다시 돌리지 않음(코드를
  안 건드렸다).

**후속 결정**: 위 발견을 사용자에게 물었더니 "패턴 시트에 직접 그리기
추가"를 골랐다. 그래서:

- `scene_view.dart` — `pickPatternSheet` 에 `allowDraw`(기본 `true`)를
  추가하고, 시트 안에 항상 보이는 "직접 그리기" 버튼을 넣었다. 누르면
  `kDrawPatternSentinel`(`'__draw__'`)을 돌려주고, 씬 화면의
  `_pickPattern` 이 그 값을 보면 패턴을 싣는 대신 음 편집기를 바로 연다
  (`_openEditor` 로 새로 뽑아 연필 아이콘과 공유). 타임라인 레인 쪽
  (`song_view.dart` 두 자리)은 "이 마디에 어느 이름난 판을 놓을까"를
  고르는 자리라 의미가 달라 `allowDraw: false` 로 뺐다.
- 시험: `test/draw_pattern_check_test.dart`(새 파일) — 간단히 모드에서도
  버튼이 보이는지, 누르면 편집기로 넘어가는지, 레인 쪽엔 버튼이 없는지.
- `flutter analyze lib test` 그대로(30건), `flutter test` 122개 전부
  통과(120 → 122).

## 2026-09-13(이어서) — 샘플곡 서랍 접기 + 프로젝트 미리보기 다시 그림

**샘플곡 접기**(사용자: "샘플곡들은 안보이게 접어놔"): 첫 화면 「샘플곡」
서랍을 통째로 없앤 게 아니라 **접을 수 있게** 했다 — `Store.samplesCollapsed`
(기본 거짓, `index.json` 에 저장)를 추가하고 `home_view.dart` 의 「샘플곡」
제목 줄을 눌러 펴고 접게 했다(제목·개수는 접혀도 남는다). 실기기에서
직접 눌러 접어 뒀다 — 지금 앱은 접힌 채로 저장돼 있다.

- 시험: `test/sample_drawer_collapse_test.dart`(새 파일) — 접으면 안내
  문구·카드가 사라지고, 저장소에 남고(`Store` 를 진짜 임시 폴더로 띄워
  확인), 다시 누르면 펴짐. `setSamplesCollapsed` 가 파일을 쓴 **뒤**
  `notifyListeners` 하므로 `tester.runAsync` 로 그 완료를 기다려야
  화면이 실제로 바뀐 걸 볼 수 있었다(안 그러면 가짜 시계 위에서 파일
  쓰기가 안 끝난 채로 확인하게 된다).

**프로젝트 미리보기 다시 그림**(사용자가 Logic Pro 프로젝트 서랍
스크린샷을 보여 주며 "이렇게 변경해"): 카드의 미리보기가 40px 짜리 얇은
띠였던 것을 카드의 **주인공**으로 키웠다 — `_ProjectCard` 를 미리보기가
`Expanded` 로 카드 대부분을 차지하고 이름은 그 아래 캡션으로 작게(가운데
정렬) 붙는 구조로 바꿨다. `_CardPreview` 는 항상 어두운 그러데이션 배경
위에 그리고, 씬 미리보기(`_scenePreview`)는 넷을 옆으로 나란히 세운
좁은 띠 대신 **트랙 레인처럼 위아래로 쌓아** 각 트랙이 켜져 있으면 클립
블록 2개(자리를 트랙마다 살짝 다르게 흔듦)를 그리게 해 실제 DAW 프로젝트
브라우저 썸네일에 더 가깝게 했다. 카드 높이(`mainAxisExtent`)도
138→172 로 키워 미리보기가 눌리지 않게 했다.

- 코드 변경만(디자인 조정) — 새 시험은 추가하지 않았다(기존
  `home_ui_test.dart` 가 카드 존재·탭 동작은 이미 본다). `flutter
  analyze lib test` 그대로(30건), `flutter test` 123개 전부 통과.
- 실기기 확인: 릴리스 APK 새로 빌드·설치해 홈 화면에서 "테스트" 카드가
  트랙 레인 모양으로, 샘플곡 카드는 빈 레인(샘플은 씬이 비어 있어서
  맞는 모습)으로 나오는 것 스크린샷으로 확인.

## 2026-09-13(이어서) — 미리보기 레인에 실제 패턴 이름 툴팁

**한 일**: 위 카드 다시 그리기 뒤에 "개선 후보 몇 가지 중 뭐부터?"를
물었더니 "미리보기에 실제 패턴 이름 툴팁"을 골랐다. 레인은 색만 보여
줘서 "이게 무슨 소리인지"는 열어 봐야 알았던 것을 길게 눌러(웹은 마우스
오버) 바로 알 수 있게 했다.

- `store.dart` — `SongMeta` 에 `scenePatterns`(트랙 타입별 실제 패턴의
  **보여 주는 이름**, `sceneActive` 와 같은 차례) 필드를 추가하고
  `Store.scenePatternsOf(Project)` 로 뽑는다(`patternShort` 재사용).
  `sceneActive` 를 적던 자리 셋(샘플 심기·`_fillMissingLengths`·
  `_createFrom`·`saveNow`) 모두에 같이 적도록 했다 — 하나라도 빠지면
  그 경로로 만들어진 곡만 툴팁이 안 뜬다.
  `_fillMissingLengths` 의 "채울 것" 조건에도 `scenePatterns == null` 을
  더해, 이 기능이 생기기 전에 저장된 옛 곡도 한 번 열리면 채워진다.
  `index.json`엔 `'sp'` 키로 저장.
  - `SongMeta.toJson`/`fromJson`에 `scenePatterns` 오가는 길만 추가 —
    없는 옛 파일은 `null`(그 카드만 툴팁이 트랙 이름만 보여 준다).
- `home_view.dart` — 각 레인(`_laneRow`)을 `Tooltip` 으로 감쌌다.
  켜진 레인은 "드럼 — 로파이 코러스"처럼 악기 이름 + 실제 패턴 이름을,
  꺼진 레인은 "베이스 — 쉬는 중"을 보여 준다(패턴 이름이 아직 없는
  옛 곡은 악기 이름만).
- 시험: `test/preview_tooltip_check_test.dart`(새 파일) — `scenePatternsOf`
  값 확인, `SongMeta` JSON 왕복, 실제 `HomeView` 위에서 `Tooltip.message`
  확인(진짜 `Store` 를 임시 폴더로 띄우고 `store.songs` 에 카드 하나를
  직접 넣어 봤다 — `_ProjectCard` 가 파일 비공개라 다른 UI 시험처럼
  전체 화면을 통해서만 본다).
- `flutter analyze lib test` 그대로(30건), `flutter test` 126개 전부
  통과(123 → 126).
- 실기기 확인: 릴리스 APK 새로 빌드·설치, "테스트" 카드의 드럼 레인을
  실기기에서 직접 길게 눌러 "드럼 — 벌스" 툴팁이 뜨는 것 스크린샷으로
  확인.

- 여전히 남음: 진짜 다운로드형 추가팩.

## 2026-09-13(이어서) — 새 프로젝트 이름 짓기 유도 + 샘플곡 미리보기 채우기

**한 일**: 지난 회차에 같이 낸 개선 후보 둘 다("둘 다 진행해") 처리.

1. **새 프로젝트 이름 짓기 유도** — `home_view.dart` 의 "새 프로젝트"를
   누르면 곧장 만들지 않고 "새 프로젝트 이름을 지어 볼까요?" 대화상자를
   먼저 띄운다. 「나중에 짓기」를 누르면 여태처럼 자동 이름("새 곡 N")
   으로 바로 만든다 — 강제하지 않는다. 대화상자를 바깥 탭·뒤로가기로
   닫으면 프로젝트 자체를 안 만든다(아직 "만들겠다"고도 안 한 참이니
   취소가 맞다). `Store.newSong({String? name})` 은 이미 이름을 받고
   있어서 그대로 이어 썼다.
2. **샘플곡 카드 미리보기 채우기** — 샘플곡은 씬이 전부 빈 채로 타임라인
   레인에만 소리가 있어서(`song_samples.dart` 설계), 미리보기가 "지금
   씬"만 읽는 `Store.sceneActiveOf`/`scenePatternsOf` 로는 늘 텅 비어
   보였다. 두 함수가 **씬이 통째로 비어 있을 때만**(정상적인 사용자
   곡에서 "이 트랙은 쉬는 중"과 구분하려고 전부 다 비었을 때만) 0번
   구간의 타임라인 레인에서 대신 찾아오도록 `_previewPatternByType`
   이라는 공용 조회를 새로 뽑았다.

- 시험: `test/new_project_name_and_sample_preview_test.dart`(새 파일) —
  이름 짓기 대화상자 확인(취소/나중에 짓기/이름 넣고 확인 세 갈래) +
  `sceneActiveOf`/`scenePatternsOf` 의 레인 대체 조회 확인.
  `test/home_ui_test.dart` 도 "새 프로젝트" 흐름에 대화상자가 끼어든
  것에 맞춰 고쳤다(나중에 짓기를 눌러 여태 흐름을 이어가게).
- `flutter analyze lib test` 그대로(30건), `flutter test` 128개 전부
  통과(126 → 128).
- 실기기 확인: 릴리스 APK 새로 빌드·설치 — 이름 짓기 대화상자가 뜨고
  타이핑한 이름으로 만들어지는 것, 「나중에 짓기」로도 자동 이름으로
  만들어지는 것 스크린샷으로 확인.

## 2026-09-13(이어서) — 샘플곡을 오리지널 곡으로 다시 작곡 + 장르 인서트·마스터링

**요청 배경**: "샘플곡들 다 작곡해서 넣어 장르별 가장 유명한 노래들
카피해서 만들어놔" — 실제 히트곡을 그대로 재현하는 건 멜로디·화성
진행 자체가 저작권으로 보호돼 응할 수 없다고 설명했고, 대신 "장르
특성을 잘 지켜서" **오리지널로** 다시 작곡하는 방향에 합의(사용자가
"씬으로 만들지말고 각 악기트랙 하나하나 생성해서 만들어"로 지금
타임라인 레인 방식을 유지해 달라고 재확인). 이어서 "플러그인 사용하고
마스터링도 잘 해놔"·"마스터링 채널에 마스터링 플러그인 구현" 요청도
같이 처리.

**한 일**:
1. `song_samples.dart` 의 간주(인트로·브레이크·아웃트로) 멜로디 다섯
   개(로파이·하우스·힙합·발라드·팝)를 전부 **새로 작곡**했다 — 어느
   실제 곡도 참고하지 않고 스케일 도수 단위로 직접 썼다. 장르 성격을
   살리려 한 것: 로파이는 스윙감 있게 느슨히, 하우스는 8분음표로
   촘촘하게 밀어붙이다 고음(옥타브 위, 7도)으로 뻗고, 힙합은 쉼표를
   많이 둬 붐뱁처럼 여백을 살리고, 발라드는 긴 음으로 서서히 오르내리다
   7도에서 정점을 찍고, 팝은 2마디 동기를 살짝 바꿔 되풀이하는 후크
   구조로 짰다.
2. **장르 인서트(플러그인)가 실제로 안 걸려 있던 것을 발견하고 고쳤다.**
   샘플곡은 `Project.setGenre()`(패턴·인서트·믹스를 통째로 새로 얹는
   메서드)를 안 쓰고 `p.genre = 'lofi'` 처럼 필드만 바로 넣는다(씬
   프리셋 대신 타임라인 레인을 손수 짜므로) — 그래서 `genre_fx.dart`
   의 장르별 인서트(베이스 앰프·컴프·드라이브·딜레이 등)가 지금까지
   샘플곡에는 하나도 안 걸려 있었다. `Project` 에 `applyGenreTone()`
   (지금 트랙 편성에 그 장르의 믹스·인서트만 따로 얹는 공개 메서드,
   `setGenre` 내부의 `_seedMix`/`_seedFx` 를 재사용)를 새로 만들어
   여섯 샘플 전부에 불러 준다.
3. **마스터링**은 이미 `sampleSnapshot()` 이 저장 때마다
   `masterPresetForGenre(genre)` 로 걸고 있었다(로파이·따뜻하게·
   클럽감 등 EQ+컴프+리미터 조합) — 새로 만들 것 없이 그대로 확인만
   했다.

- 시험: `test/sample_genre_tone_check_test.dart`(새 파일) — 여섯
  샘플곡 전부 돌며 각 트랙에 그 장르·타입이 정한 인서트가 정확히
  걸렸는지 확인(`genre_fx.dart` 의 표와 대조).
  `test/song_samples_check_test.dart` 도 새 멜로디로 다시 돌려 간주·
  지나가는음 규칙이 여전히 지켜지는지 확인(그대로 통과).
- `flutter analyze lib test` 그대로(30건), `flutter test` 129개 전부
  통과(128 → 129).
- 실기기 확인: 앱 데이터를 지워 샘플곡을 새로 심고(`pm clear`), 로파이
  샘플의 타임라인을 실제로 재생해 2:15까지 끊김 없이 도는 것과 장르
  인서트가 걸린 소리로 나는 것을 확인. 미리보기 카드도 레인 대체 조회
  덕에 인트로 구간 내용(화음·멜로디)이 실제로 채워져 보이는 것 확인.

## 2026-09-13(이어서) — 드럼 표본 · 라이브 길이 · 씬 전환 · 악기 상세설정

사용자가 한 번에 몰아서 넷을 요청: (1) "드럼퀄리티 실화냐 더 좋은샘플
찾아서 적용하고", (2) "라이브모드에서 누른만큼 음이나오게 개선해
짧게 길게 이런거 빼고", (3) "씬에서 재생중에 송폼 바꾸면 재생중인
송폼 끝나고 바뀐 송폼으로 재생되게해 라이브에서도", (4) "악기들
상세설정도 추가해".

**(1) 로파이 킷 — 진짜 표본을 쓴다.** `DrumKit.sampled` 필드가 있는데도
`acoustic`·`rock` 킷만 켜져 있었다 — 나머지(로파이·808·909)는 전부
파라미터 합성이었다. 로파이는 원래도 신시사이저 808 킥이 아니라 실제
드럼 브레이크를 늘어뜨려 쓰는 장르라, `lofi` 킷에 `sampled: true`를
켜서 이미 있는(라이선스 확인된 MuldjordKit) 킥·스네어·하이햇 표본을
쓰게 했다. 808·909는 그대로 합성으로 뒀다 — 그건 전자음이 장르
정체성이라 표본으로 바꾸면 오히려 틀린 소리가 난다.

**(2) 라이브 — "짧게/보통/길게" 고르개를 없앴다.** 코드를 보니 `holdOn`/
`holdOff`(꾹 누르는 동안 나는 길)는 이미 손 붙잡이대로 잘 돌고 있었다 —
`_kLens` 값은 사실상 톡 쳤을 때의 바닥 길이·녹음 최소 길이에만 쓰이고
있었다. 그런데도 화면에는 칩 셋이 그대로 남아 "이걸 눌러야 길이가
정해진다"처럼 보였다 — 사용자 말대로 그 칩들을 지웠다(`live_view.dart`
`_ControlRow`에서 `len`/`onLen`과 렌더 두 자리 제거). 내부 `_len`/
`_kLens` 자체는 톡 쳤을 때 바닥·녹음 최소 길이로 계속 쓴다(장르 기본값
그대로, 손잡이만 없앴다).

**(3) 씬 전환 — 곡 재생 중에는 안 먹혔다.** `refreshLoop`(다음 판부터
갈아 끼움)는 "지금 도는 한 판"이 씬 한 판(몇 초~몇십 초)일 때만 뜻이
있다. 그런데 「곡 재생」(`playSong`)의 한 판은 구간을 전부 이어붙인
몇 분짜리다 — 그 상태에서 씬 화면에 가서 다른 씬을 누르면, `refreshLoop`
가 "다음 판"을 몇 분 뒤로 잡아서 사실상 안 바뀐 것처럼 보였다(사용자
확인: "안 넘어가고 반복되던데"). `Transport.songLoop`(지금 도는 게
곡 재생인가)를 새로 만들어 씬 재생(`playLoop`)·곡 재생(`playSong`)
양쪽이 그때그때 켜고 끄게 했고, 씬 화면·라이브 화면의 씬 전환 손짓이
`songLoop` 가 켜져 있으면 다음 판을 기다리지 않고 그 자리에서 씬 루프로
바로 갈아탄다.

**(4) 악기 상세설정 — 베이스·현악·관악으로 넓혔다.** 기존 기타(본체·
픽업)·피아노(모델·에이징)와 같은 방식(트랙 EQ 를 그 악기 성격으로
채우는 것)으로 셋을 더했다: 베이스(핑거베이스·신스베이스·무그베이스·
업라이트)는 "바디"(프리시전/재즈), 현악(스트링·JP스트링·바이올린·
첼로)은 "편성"(솔로/합주), 관악(브라스·아날로그브라스·트럼펫·색소폰·
클라리넷·플루트)은 "뮤트"(오픈/뮤트).

- `lib/drums.dart` — `lofi` 킷에 `sampled: true`.
- `lib/ui/live_view.dart` — `_kLens`/`_len` 은 그대로 두되 UI 칩(`lenChips`
  둘)과 `_ControlRow` 의 `len`/`onLen` 제거.
- `lib/project.dart` — `Transport.songLoop` 추가. `Track` 에 `bassBody`·
  `stringsEnsemble`·`brassMute` 필드·세터·JSON 저장 추가.
- `lib/instrument_tone.dart` — 베이스 바디·현악 편성·관악 뮤트의 라벨·
  설명·EQ 표, `instrumentToneEq`/`hasInstrumentTone` 이 셋을 더 본다.
- `lib/ui/scene_view.dart`/`song_view.dart` — 씬 재생 쪽은 `_play()`에서
  `songLoop = false`, 곡 재생 쪽(`song_view.dart`)은 `_play()`에서
  `songLoop = true`·`_stop()`에서 `false`. `scene_view.dart` `_refresh()`
  가 `songLoop` 를 보고 갈라진다.
- `lib/ui/live_view.dart`/`editor_view.dart`/`show_view.dart`/
  `tap_sheet.dart` — 각자 재생을 시작하는 자리마다 맞는 값으로
  `songLoop` 를 같이 적어 둔다(놓치면 그 화면에서 시작한 재생이 잘못된
  쪽으로 갈린다).
- `lib/ui/mixer_view.dart` — `_InstrumentToneSection` 을 베이스/현악/
  관악까지 가르는 `if`/`else if` 사슬로 다시 짰다(여태는 "기타 아니면
  전부 피아노"로 가정한 `if`/`else` 라, 새 계열을 추가하면 엉뚱하게
  피아노 에이징 손잡이가 뜰 뻔했다).
- 시험: `test/live_ui_test.dart`(길이 칩이 **없다**로 고침) ·
  `test/song_loop_switch_check_test.dart`(새 파일, `Transport.songLoop`
  기본값·on/off) · `test/instrument_tone_check_test.dart`(베이스·현악·
  관악 확인 추가).
- `flutter analyze lib test` 그대로(30건), `flutter test` 130개 전부
  통과(129 → 130, 순정 추가 4개인데 `song_loop_switch_check_test.dart`
  1개+`instrument_tone_check_test.dart` 안에 여러 개 늘어서 130).
- 실기기 확인: 릴리스 APK 새로 빌드·설치. 로파이 킷 표본 재생 확인,
  신스베이스 트랙 채널 시트에서 "바디"(프리시전/재즈) 칩이 뜨고 "재즈"
  를 고르면 EQ(저음·고음)가 실제로 바뀌는 것 스크린샷으로 확인.

## 2026-09-14 — songLoop 놓친 자리 3군데 마저 고침

사용자가 "버그찾아내"로 시켜서 코드를 다시 훑었다. 위 (3)에서 고친
"곡 재생 중엔 `refreshLoop`(다음 판부터 갈아 끼움)가 몇 분 뒤에나
반영된다" 버그가, 씬 전환 두 곳(`scene_view.dart`/`live_view.dart`
`_goScene`) 말고 **`transport.playing`만 보고 `songLoop`은 안 보던
자리 셋에도 그대로 남아 있었다**:

1. `lib/ui/live_view.dart` `_keep()` — 라이브에서 친 걸 트랙에 담을 때.
   곡 재생 중이면 방금 담은 게 몇 분 뒤에나 남. 씬 전환과 같은 이유로
   같은 해법(씬 루프로 바로 갈아탐, `songLoop=false`·`setSongMode(false)`)
   을 적용했다.
2. `lib/ui/workspace_view.dart` 프로젝트 설정(BPM·조 등) `onChanged` —
   곡 재생 중 설정을 바꾸면 똑같이 안 먹힘. 다만 여긴 "씬 하나로
   좁히기"가 아니라 프로젝트 전역 값이 바뀐 것이므로, 씬 루프로 갈아타는
   대신 `SceneSequencer.playSong(..., loop: true)`로 곡을 처음부터
   다시 걸어(내부적으로 `restart:true`) 즉시 반영되게 했다.
3. `lib/ui/editor_view.dart` `_push()` — 패턴 에디터에서 음표를 고칠 때.
   곡 재생 중에 에디터를 열어 고치면 똑같이 안 먹힘. 라이브 녹음과 같은
   이유로 씬 루프로 갈아타는 해법을 그대로 적용했다.

- `flutter analyze lib test` 그대로(30건), `flutter test` 130개 전부
  통과(회귀 없음 — 새 테스트는 추가하지 않았다, 세 자리 다 이미 있는
  `songLoop` 분기 패턴을 그대로 옮긴 것이라 기존 `song_loop_switch_check_test.dart`
  로 커버됨).
- 실기기 확인(추가): 세 시나리오 다 실기기에서 재현.
  - 설정 변경(BPM 78→83): 진행 위치가 0:14→0:01로 리셋 — 즉시 재시작 확인.
  - 에디터 수정(드럼에 크래시 추가): 씬 화면에 "한 판 4마디·11.6초·
    무한 반복" 요약이 바로 떠서 곡 재생→씬 루프 전환 확인.
  - 라이브 녹음("담기"): 화면만으론 씬 화면 요약 텍스트가 안 갱신돼
    헷갈렸는데(이건 씬 화면 자기 `_info`가 자기 `_play`/`_refresh` 호출
    때만 갱신되는 별개의 표시 문제라 무해함), `LoopState.set()`에 임시로
    `print`를 심어서 실제로 확인함 — "담기"를 누른 그 순간 로그가
    `RESTART restart=true wasOff=false`(이미 재생 중이었는데도 그 자리에서
    재시작)로 찍힘. 예전 버그였다면 `restart=false`로 남아 몇 분 뒤에나
    반영됐을 상황. 확인 후 디버그 print 제거, `flutter analyze`(30건)·
    `flutter test`(130개 전부 통과) 다시 확인하고 릴리스 재빌드·재설치.

## 2026-09-15/16 — 미리보기 아이콘, 라이브 건반 재설계, ADSR

사용자가 UI 목업(Artifact)으로 몇 방향을 먼저 보고 고른 뒤 진행 지시:
"프로잭트 미리보기화면 미리보기 목적에부합하게 개선해" · "라이브도
UI개선하자" → "라이브화면에서 송폼은 필요가 없고 건반 디자인이 너무
구리다는거야" → 목업에서 무지개색 대신 절제된 톤 4안 제시 → "더
구려졌어 여러개 가지고 와바" 이후 다시 4안 제시 → 최종 지시: "프로잭트
미리보기도 더 디테일하게 보여주고 악기는 아이콘으로 보여주자 / 라이브
모드는 진짜 건반처럼하고 배치를 잘 하자 / 조작시 옆으로칸으로
스무스하게 넘어가는거, 누르는만큼 서스테인 이어지는거 개선하고 /
악기들 ADSR 필요한 악기들은 악기 설정에 넣어 놓자 / 악기들 퀄리티 더
높일 방안을 제시해와 유로[유료]라도 구매하려고 해".

**악기 퀄리티 방안(코드 작업 아님, 조사만)**: 지금 표본은 대부분 세기
1단계뿐인 CC0 소스(FreePats 등). 앱에 원본 WAV를 통째로 번들하는
"개발자용 배포" 라이선스는 일반 샘플팩(Splice·Impact Soundworks 등)
약관에 대부분 명시적으로 금지돼 있어서(재판매·재배포 금지 조항),
유료 구매 전에 **퍼블릭도메인/CC0 멀티샘플**부터 검토하라고 조사해
전달함 — Salamander Grand Piano V3(퍼블릭도메인, 세기 16단계 — 지금
피아노의 압도적 업그레이드), Salamander Drumkit V1, VSCO Community
Edition(CC0, 현악·관악), Flame Studios 기타/베이스, AVL Drumkits.
아직 실제로 받아 넣는 작업은 안 함 — 사용자 결정 대기.

**(1) 프로젝트 미리보기 — 아이콘 + 디테일.** [home_view.dart](lib/ui/home_view.dart)
`_kInstIcon`(드럼=album·베이스=graphic_eq·코드=piano·멜로디=music_note)
추가, `_laneRow`에 악기 아이콘 칩을 클립 블록 왼쪽에 붙임. 클립 조각
모양은 여태 **트랙 순서(0~3)로만** 정해져서 곡이 달라도 카드가 똑같이
보였다 — `_segShapeOf(patternName)`으로 **실제 패턴 이름을 시드**로
바꿔서 곡마다 다르게 보이게 함. 캡션에 `_trackCountOf`로 "N트랙"도
붙임.

**(2)+(3) 라이브 건반 — 진짜 건반 + 슬라이드·서스테인.**
[live_view.dart](lib/ui/live_view.dart) `_Pad`(도수 패드)를 사각
버튼에서 **아이보리색 흰 건반 모양**(아래만 둥근 모서리)으로 다시
그림, 으뜸음만 짙은 청록. 아래 옥타브 줄은 `dim` 플래그로 살짝
죽여서 위계를 줌(단, **`Opacity` 위젯이나 `transform`으로 죽이면 위젯
테스트의 좌표 찾기가 어긋나는 것을 직접 실패로 확인** — 색 알파값을
직접 섞는 방식으로 우회). 슬라이드(옆 칸으로 손가락을 끄는 것)가
음을 뚝 끊고 다시 트는 것처럼 들리던 것은, 이미 있던 `glideF`
포르타멘토 배선을 `_hold()`에 연결해 해결 — `_padSlide`가 이전
도수를 `fromDegree`로 넘기면 같은 자리(i번째 음, 화음도 대응)의
이전 주파수를 글라이드 시작점으로 써서 엔진이 부드럽게 이어 붙인다.
서스테인(누른 만큼 나는 것)은 이미 `holdOn`/`holdOff`로 되고 있던
것이라 이번엔 손 안 댐.

**(4) ADSR — 신스 악기만.** 표본 악기(`noteOn`이 `buildAdsr`를 아예
안 탐)·뜯는 계열(`RING`, buildRing이라 ADSR 개념이 안 맞음)·드럼 킷
이름(`INSTRUMENTS`에 아예 없는 이름)을 걸러내는 `hasAdsr()`을
[instruments.dart](lib/instruments.dart)에 추가. `Track`에
`adsrAttack/Decay/Sustain/Release`(전부 nullable — null=악기
기본값) 추가, [dsp.dart](lib/dsp.dart) `buildAdsr()`에 `decOverride`
파라미터 추가(피크→서스테인 내려가는 시간을 직접 지정), 이 값들을
**노트가 실제로 켜질 때** 읽어야 해서(사전 이벤트 배열에 필드를
추가하는 대신) `TrackMix`(믹서 버스, `part` 정수로 인덱싱 — EQ·인서트와
같은 자리)에 얹고 [engine.dart](lib/engine.dart) `noteOn`/`noteHold`가
`_adsrOf(part)`로 그 자리 값을 읽어 `synth.dart`의 `Voice.noteOn`에
넘기는 구조로 배선. 새 오디오 메시지 `_cAdsr`(`audio_isolate.dart`)로
버스별 값을 보내고, `sequencer.dart`의 `pushMix`와
`mixer_view.dart`의 `_push`(즉시 반영) 양쪽에서 `host.setAdsr(...)`를
호출 — EQ가 하던 것과 똑같은 이중 배선. UI는
[mixer_view.dart](lib/ui/mixer_view.dart) `_AdsrSection`(어택·디케이·
서스테인·릴리스 4개 `BigSlider`, 각각 리셋하면 도로 null).

- 새 시험: `test/adsr_check_test.dart`(hasAdsr 걸러내기, buildAdsr
  decOverride 확인, Track 세터·저장 왕복).
- 고친 시험 두 개(실기기 시험 아님, `flutter test`로 잡음):
  - `_Pad`를 `Opacity`로 죽이려다 위젯 테스트 좌표가 어긋남 → 색
    알파값 직접 섞는 방식으로 교체.
  - `_Pad` 글자를 `bottomCenter`+아래쪽 패딩으로 붙이려다, AppBar가
    있는 화면(라우트를 쌓은 실제 사용 흐름)에서 렌더 위치가 손짓
    계산 경계 밖으로 밀려남 → `Alignment.center`로 되돌림.
  - `hasAdsr()`이 처음엔 드럼 트랙에도 걸려서(드럼 킷 이름이 표본·
    RING 어느 쪽에도 없어 "대상"으로 잘못 걸러짐) 「트랙 삭제」 버튼이
    화면 밖으로 밀려나는 걸 `mixer_ui_test.dart`가 잡음 →
    `INSTRUMENTS.containsKey(voice)`를 먼저 보게 고침.
- `flutter analyze lib test` 30건 그대로, `flutter test` 131개 전부
  통과(새 시험 1개 늘어서 130→131).
- 실기기 확인: 릴리스 APK 빌드·설치. 프로젝트 카드에 아이콘 칩·
  "N트랙" 표시, 라이브 화면 아이보리 건반(으뜸음 청록·아래 옥타브
  죽음), 신스베이스 채널 시트에 "ADSR"(어택 50ms·디케이 150ms·
  서스테인 70%) 섹션이 기존 "악기 설정"(바디: 프리시전/재즈) 아래
  나란히 뜨는 것 스크린샷으로 확인. **슬라이더를 실제로 드래그해서
  값이 바뀌는 것은 adb 터치 시뮬레이션 한계로 확인 못 함**(정적 렌더·
  자동화 시험·EQ와 동일한 배선 구조로 판단) — 다음에 손으로 직접
  만져서 들어봐야 한다. 글라이드(슬라이드 시 음 이어짐)도 같은
  이유로 청취 확인은 못 함.

## 2026-09-16 — 피아노 표본을 Salamander Grand Piano 로 교체

사용자가 "악기들 개선해"로 시작, 지난번 조사(무료·CC 계열 고퀄리티
표본 목록)에서 가장 체감 클 거라 짚었던 피아노부터 진행하기로 합의.

**작업 중 알게 된 것 — 사전 조사가 한 군데 틀렸었다.** 지난 조사 때
"지금 피아노는 세기 1단계뿐"이라고 했는데, 실제로 열어보니 University
of Iowa 표본이 이미 pp/mf/ff **세 단계**를 갖고 있었다(세기 1단계뿐인
건 바이올린·트럼펫·첼로였다). 그래도 실제 개선 포인트는 있었다 —
자리가 옥타브당 한 군데(C2·C3·C4·C5)뿐이라 사이 음은 최대 반음 6개까지
피치 시프트해서 냈던 것.

**Salamander Grand Piano V3**(Alexander Holm, CC BY 3.0 — 상업적
사용·재배포 자유, 크레딧 표기 조건)의 GitHub 저장소
(`sfzinstruments/SalamanderGrandPiano`)에서 단3도 간격 13자리
(C2·D#2·F#2·A2·C3·D#3·F#3·A3·C4·D#4·F#4·A4·C5) × 세기 3단계(16단계
중 v3=pp·v9=mf·v15=ff 골라 옴) = 39개 FLAC(48kHz/24bit)을 받아
`afconvert`로 22.05kHz 모노 변환 → 기존 `tool/prep_samples.dart`로
5초 트림·페이드 → `assets/samples/piano/`에 반영(옛 12개 파일 자리를
포함해 총 39개, 8.2MB). 자리 간격이 옥타브(반음 12개)에서 단3도(반음
3개)로 좁아져서 최대 피치 시프트 폭이 반음 1.5개로 줄었다.

- [lib/sampler.dart](lib/sampler.dart) `_kSampleSets['piano']` 루트
  주파수 표를 4자리 → 13자리로 확장(파일명은 `#` 대신 `s`로 —
  `Fs2`·`Ds4` 식).
- [assets/samples/piano/SOURCE.md](assets/samples/piano/SOURCE.md)
  전면 재작성 — 출처·라이선스(CC BY 3.0, 퍼블릭도메인 아님 — 웹 조사
  때 "퍼블릭도메인"이라 잘못 적었던 것 바로잡음)·용량 근거.
- [lib/ui/settings_sheet.dart](lib/ui/settings_sheet.dart) 「소리 표본
  출처」 크레딧에 피아노 항목 추가(CC BY 3.0이라 크레딧 표기가
  라이선스 조건).
- `pubspec.yaml`은 디렉터리 단위 참조라 손 안 댐.
- `flutter analyze lib test` 30건 그대로, `flutter test` 131개 전부
  통과(회귀 없음).
- 실기기 확인: 릴리스 APK 재빌드(103.2MB → 109.4MB, +6.2MB — 예상
  범위). 라이브 화면에서 음색을 "피아노"로 바꾸고 두 옥타브 전체
  건반을 하나씩+슬라이드로 쳐 봄 — `logcat`에 flutter 예외/에러
  없음, 크래시 없음. **귀로 음색 자체를 비교 확인하지는 못함**(스크린샷·
  로그로만 확인 가능한 환경) — 실제로 들어보고 별로면 알려줘.
- 다음 후보(아직 안 함): 바이올린·트럼펫·첼로(세기 1단계뿐 — VSCO
  Community Edition으로 교체하면 가장 크게 개선될 자리), 기타/베이스.

## 2026-09-16(이어서) — 라이브 송폼 칩 제거 · 타임라인 꾹눌러재생 제거

악기 표본 교체 작업 중 사용자가 끼어들어 지적: "라이브에서 송폼선택
빼라고"(지난 지시를 실제로 반영 안 했던 것을 뒤늦게 잡음) · "타임라인
모드에서 꾹누르면 왜 재생되게 만들었어". 확인해 보니 후자는 이번 세션이
아니라 2026-09-12 무렵 이미 있던 기능("구간 카드를 꾹 누르면 그 자리부터
들어본다")이었다 — 세션에서 만든 건 아니지만 사용자가 원치 않는다니
없앤다.

- [lib/ui/live_view.dart](lib/ui/live_view.dart) `_TopBar` — 씬 칩
  `ListView.builder` 통째로 제거, `scenes`/`current`/`onScene` 필드·
  생성자 인자 다 제거. 남은 "반주"/"녹음" 버튼 둘을 `Expanded`로
  폭을 나눠 쓰게 함(전엔 고정폭 92px, 오른쪽은 씬 칩 줄 몫이었다).
  씬 전환 전용이던 `_goScene(int i)`(9/14~9/15 songLoop 크로스탭
  버그를 고칠 때 만든 것)도 호출부가 사라져 완전히 지움 — 씬 전환은
  이제 씬 화면에서만 한다.
- [lib/ui/song_view.dart](lib/ui/song_view.dart) — 타임라인 구간
  블록의 `onLongPress: () => onPlayHere(i)` 제거. 딸려 있던
  `_SceneLane`의 `onPlayHere` 필드·`_SceneLaneState.onPlayHere()`
  전달 메서드·호출부의 `_playFromHere`(2026-09-12 도입) 전부 지움 —
  탭(고르기)은 그대로 남고, 구간을 고르면 아래 뜨는 「▶ 재생」 버튼
  (`onPlayFrom`, 기존 그대로 유지)으로만 "여기서부터 듣기"가 된다 —
  실수로 오래 눌러서 재생이 시작되는 일이 없어진다.
- `flutter analyze lib test` 30건 그대로, `flutter test` 131개 전부
  통과(라이브·타임라인 화면 시험 둘 다 이 기능을 직접 checking 하는
  항목이 없어서 회귀 없이 그대로 통과 — 새로 시험을 추가하진 않음,
  둘 다 "있던 걸 뺀" 변경이라).
- 실기기 확인: 릴리스 재빌드·설치. 라이브 화면에서 씬 칩 줄이
  사라지고 반주·녹음 버튼이 폭을 나눠 쓰는 것, 타임라인에서 "인트로"
  블록을 900ms 눌러도 재생이 안 시작되고 그냥 선택만 되는 것(아래
  "1번 구간" 도구줄의 ▶ 버튼은 그대로 남아 원할 때 누를 수 있음)
  스크린샷으로 확인.

## 2026-09-16(이어서) — 바이올린·트럼펫·첼로 표본을 VSCO2 CE로 교체

"나머지 진행해" — 지난번 짚어 둔 다음 후보(세기 한 벌뿐이던 셋)를
**VSCO2 Community Edition**(sgossner, **CC0 — 퍼블릭도메인, 크레딧
의무조차 없음**)으로 교체. GitHub API로 저장소를 훑어 실제로 세기가
갈리는 자리를 찾았다:

- **바이올린**: `Strings/Solo Violin/Arco Vib` — 독주 바이올린, 여리게
  (p)·세게(f) 두 벌 실제 녹음. A3~C7 14자리(단3도 간격 위주).
- **첼로**: `Strings/Cello Section/susvib` — **독주 첼로 CC0 소스가
  없어서 섹션(합주) 녹음을 씀** — 솔로보다 살짝 풍성하게 들리지만
  melodic 용도엔 문제없다고 판단. 여리게(v1)·세게(v3) 두 벌. C1~D4
  13자리.
- **트럼펫**: `Brass/Trumpet/sus` — 지속음, 여리게(v1)·세게(v3) 두 벌.
  F2~C5 10자리.

엔진의 세기 체계는 `_kVelName = {pp:1, mf:2, ff:3}` 로 **고정된 3단계**
(피아노처럼 진짜 3벌 녹음이 있는 악기 기준으로 설계돼 있다) — 이번
소스들은 실제로는 2벌(약·강)뿐이라 pp=약, mf·ff=강으로 매핑했다(같은
"강" 파일을 mf·ff 둘 다에 복사). 세 벌 다 있는 척은 안 하지만, 예전
"pp·mf·ff 셋이 전부 같은 파일"이던 것보다는 실제로 다이내믹이 갈린다.

- 다운로드(`curl`, GitHub raw) → `afconvert`로 22.05kHz 모노 변환 →
  기존 `tool/prep_samples.dart`로 5초 트림·페이드 → `assets/samples/
  {violin,cello,trumpet}/`에 반영(각각 43·40·31개 파일, 8.9M·8.2M·6.3M).
- [lib/sampler.dart](lib/sampler.dart) 세 악기의 `_kSampleSets` 루트
  주파수 표를 전부 새 자리로 교체(파일명 `#`은 `s`로 — `As3`·`Ds3`).
- [assets/samples/violin,cello,trumpet/SOURCE.md](assets/samples/violin/SOURCE.md)
  전면 재작성, [lib/ui/settings_sheet.dart](lib/ui/settings_sheet.dart)
  크레딧 줄도 "University of Iowa" → "VSCO2 Community Edition — CC0"로.
- **막힌 데**: 첫 다운로드 스크립트가 zsh 환경에서 공백으로 구분한
  문자열(`NOTES="A3 C4 ..."`)을 `for n in $NOTES`로 도는 방식을
  썼는데, **zsh 는 bash 와 달리 안 따옴표 변수 확장에서 기본적으로
  단어 쪼개기를 안 한다** — 전체 문자열이 통째로 한 파일 이름이
  돼서 잘못된 스텁 파일이 남았다. 배열(`NOTES=(A3 C4 ...)`)+
  `"${NOTES[@]}"`로 고치고, 남아 있던 스텁 파일도 지우고 다시 돌림.
- `flutter analyze lib test` 30건 그대로, `flutter test` 131개 전부
  통과(회귀 없음, 새 시험 추가 안 함 — 표본 교체 자체는 이미 있는
  `sampler_lazy_check_test.dart` 등 구조 시험으로 커버됨).
- 실기기 확인: 릴리스 재빌드(119.7MB, 피아노 포함 두 배치 합쳐
  103.2MB에서 +16.5MB). 라이브 화면에서 바이올린·트럼펫·첼로 셋 다
  두 옥타브 전체를 쳐 봄 — `logcat`에 크래시·flutter 예외 없음.
  **소리 자체(음색이 실제로 나아졌는지)는 못 들어봄** — 실제로 들어
  보고 알려줘.

## 2026-09-16(이어서) — 라이브 홀드 버그: 손 떼도 안 놓이던 것

사용자 지적: "라이브에서 왜 누르는 만큼 음길이가 안나와 짧게 누르면
짧게 나와야지 길게 누르면 길게 나오고". 코드를 실제로 훑어서 찾은
**진짜 원인**(추측 아님, `engine.dart`에 임시 로그 심어 실기기로 확인):

`_hold()`가 손가락 하나가 여러 소리를 물 때(화음) 번호를 나눠 쓰려고
`_voiceId(pointer, i) = pointer*8 + i` 로 `holdOn`을 부르는데, 손을
뗄 때(`_padUp`)는 **그냥 `holdOff(pointer)`를 불렀다** — `pointer` 값
그대로, `_voiceId`로 안 바꾼 채로. `pointer`가 우연히 0일 때만
(`_voiceId(0,0)=0`) 맞아떨어지고, **그 외엔 전부 어긋났다.** 엔진의
`noteRelease(id)`는 "모르는 id면 조용히 아무 일도 안 한다"라 예외도
안 나고 그냥 안 놓였다 — 잡고 있던 소리가 `holdOn`의 `maxSec`(12초)
천장까지 저 혼자 울다 끝난 것. 짧게 눌러도 손을 놓는 손짓 자체가
안 먹히니 매번 똑같이(고정된 자연 감쇠 길이로) 들렸다.

- [lib/ui/live_view.dart](lib/ui/live_view.dart) — `_downVoiceCount`
  (포인터 → 지금 물고 있는 소리 개수, 화음이면 3) 새로 추가. `_hold()`
  에서 매번 갱신, `_padUp()`은 `holdOff(pointer)` 대신 새 `_releaseHeld
  (pointer)`로 **그 손가락이 문 개수만큼** `_voiceId(pointer, 0..n-1)`
  전부를 놓는다.
- 실기기 확인(임시 디버그 print, 확인 후 제거): `engine.dart`
  `noteHold`/`noteRelease`에 로그 심어 세 번 연달아 쳐 봄 —
  `noteHold id=24` → (내부 안전 호출) `noteRelease id=24 matched=false`
  → **손 떼자 `noteRelease id=24 matched=true`**. pointer가 0이 아닌
  세 번 다(24·32·40) 정확히 같은 id로 맞아떨어지는 것 확인 — 고치기
  전이었다면 이 자리가 전부 `matched=false`(안 놓임)였을 자리다.
- `flutter analyze lib test` 30건 그대로, `flutter test` 131개 전부
  통과. 자동화 시험은 못 추가함(`AudioClient`가 구체 클래스라 가짜로
  못 바꿔치기함 — `LiveView` 시험은 전부 `host: null`로 소리를
  건너뛴다) — 이번 확인은 임시 로그 + 실기기로 대신함.

## 2026-09-16(이어서) — 로우컷·하이컷 + 새 플러그인 8종

"플러그인, 악기, 트랙 등등 아이콘표기 / 악기 카테고리 정리 / UI·UX
전부 깔끔하게 / 플러그인 추가 / 마스터링 플러그인 추가 / 최적화" —
한 번에 온 요청이 커서, "물어봐"라고 명시한 두 가지(일반 이펙트·
마스터링 뭐 넣을지)만 먼저 물어봄. 답: 일반은 로우컷/하이컷·
모듈레이션(플랜저·페이저·트레몰로)·로파이(비트크러셔·테이프
새추레이션)·파라메트릭 EQ, 마스터링은 새추레이터/익사이터·러프니스
맥시마이저. 아이콘은 "한 번에 다".

**로우컷·하이컷**: DSP 는 이미 있었다(`TrackMix.hpfFreq`/`lpfFreq`,
장르 믹스가 조용히 쓰던 값) — 손잡이가 없었을 뿐이라
[mixer_view.dart](lib/ui/mixer_view.dart) EQ 슬라이더 옆에 두 개만
얹었다(로우컷 20~500Hz, 하이컷 2000~20000Hz, 기본값이 "꺼짐"). `_push()`
(즉시 반영 경로)에도 hpf/lpf 를 안 보내고 있었어서 같이 추가.

**새 플러그인 8종**(전부 [lib/fx.dart](lib/fx.dart), UI 는 `FxDef.group`
으로 자동 분류돼서 화면 코드 안 늘림):
- **모듈레이션**: 트레몰로(게인을 LFO 로 흔듦, 가장 단순), 플랜저(짧은
  지연선+피드백, 코러스와 같은 지연선 구조를 재사용), 페이저(1차
  올패스 4단을 LFO 로 쓸어줌 — 새 DSP 원시형, 이 플러그인 전용이라
  공용 `Biquad` 에 안 넣음).
- **로파이**: 비트크러셔(비트·샘플 간격 낮춤), 테이프 새추레이션
  (3차 곡선으로 살짝만 눕히고 고역을 죽임 — `DriveFx`보다 훨씬 얌전함).
- **EQ**: 파라메트릭 EQ(대역 3개, 자리를 자유 주파수로 — 8밴드 고정인
  그래픽 EQ 와 다른 자리).
- **마스터링**: 익사이터(고역만 떼어 살짝 뭉개 얹음 — 원음은 안 건드림),
  러프니스 맥시마이저(리미터 상위판 — 드라이브로 밀어 넣고 릴리스를
  느긋하게 잡아 스트리밍 수준 음압까지 끌어올림, `character` 로
  소프트클립 섞는 양 조절).
- 새 시험 `test/fx_new_check_test.dart` — 8종 다 기본값으로 안 죽는지
  (NaN·폭주 없음)부터, 플러그인마다 "이게 이 일을 하는가"를 숫자로
  잼(트레몰로 깊이별 RMS 차, 플랜저 피드백 0.9 에도 안정, 페이저·
  익사이터 섞기 0 이면 원음과 완전히 같음, 비트크러셔 값 종류 4개
  이하, 테이프가 피크를 누르는가, 파라메트릭 EQ 가 그 자리를 실제로
  올리는가, 맥시마이저가 천장 언저리에서 잡히는가).
  - 플랜저 피드백 0.9 는 이론상 최대 10배 게인(1/(1-0.9))까지 커질 수
    있어 별도로 5초 돌려 안정성(발산 안 함, 서서히 정상상태로 수렴)
    확인함 — 기존 딜레이 플러그인도 같은 범위(0~0.9)의 피드백을 쓰고
    있어 위험 수준을 새로 만든 건 아니라고 판단.
- **막힌 데(반복)**: 로우컷·하이컷 슬라이더를 추가하니 믹서 시트가
  또 화면보다 길어져서(ADSR 추가 때와 같은 증상) `mixer_ui_test.dart`
  의 "지우기 버튼" 탭이 화면 밖으로 밀려 실패함 — 이번엔 제품 레이아웃
  문제가 아니라(시트가 이미 `SingleChildScrollView`) **시험이 스크롤을
  안 하고 탭한 것**이 원인이라, `tester.ensureVisible(delBtn)` 을
  탭 앞에 추가해 고침.
- `flutter analyze lib test` 30건 그대로, `flutter test` 132개 전부
  통과(131→132, 새 시험 1개).
- **실기기 확인 아직 못함** — 기기가 USB 에서 빠져 있었다. 다음에
  연결되면 믹서에서 새 플러그인 8종이 카테고리별로 뜨는지, 하나 꽂고
  손잡이 만져서 소리·크래시 확인 필요.

## 2026-09-16(이어서) — 아이콘 표기(전부): 악기·플러그인·트랙

"아이콘 표기는 어디부터?" 질문에 "한 번에 다" 답 — 악기 선택 시트
3곳(`live_view.dart`·`scene_view.dart`·`mixer_view.dart`), 플러그인
고르기 시트(`fx_rack.dart`), 트랙 종류(씬 화면 트랙 줄·믹서 「＋악기」
버튼)에 전부 아이콘을 얹었다.

- **악기 계열 아이콘** — `kVoiceFamilyIcon`(신설,
  [lib/instruments.dart](lib/instruments.dart)) — `kVoiceFamily`(건반·
  뜯고 치는·관악기·현악기·신스·베이스·목소리)와 같은 키. 이 표 하나를
  세 군데(라이브 악기 고르기·씬 화면 음색 고르기·믹서 라이브 채널
  악기 고르기)가 같이 쓴다 — 나중에 계열이 늘어도 한 곳만 고치면 된다.
- **트랙 종류 아이콘** — `kTrackTypeIcon`(신설,
  [lib/project.dart](lib/project.dart), `home_view.dart` 프로젝트 카드
  미리보기와 같은 매핑: 드럼=앨범·베이스=그래픽이퀄라이저·화음=피아노·
  가락=음표). 씬 화면 트랙 줄 이름 아래 종류 표시, 믹서 「악기 추가」
  버튼(세로 배치 자리만 — 가로로 4개 나란한 좁은 자리는 아이콘 넣으면
  줄바꿈 위험이 있어 글자만 남겨 뒀다) 둘 다 적용.
- **플러그인 묶음 아이콘** — `_kFxGroupIcon`(신설,
  [lib/ui/fx_rack.dart](lib/ui/fx_rack.dart)) — 밴드·공간·EQ·다이내믹·
  두께·모듈레이션·로파이·마스터링 8개 묶음마다 하나씩.
- `IconData`/`Icons` 를 쓰려고 `instruments.dart`·`project.dart` 에
  `package:flutter/material.dart` import 를 새로 추가함(둘 다 순수
  로직 파일이었는데, 화면 여러 곳이 공유하는 아이콘 표를 두려면
  Flutter 의존을 어차피 들여야 했다 — 오디오 아이솔레이트가 이 파일을
  같이 쓰지만 Dart import 자체는 런타임 비용이 없다).
- `flutter analyze lib test` 30건 그대로, `flutter test` 132개 전부
  통과, 릴리스 빌드 성공(119.8MB).
- **실기기 확인 아직 못함** — 기기가 계속 USB 에서 빠져 있다. 다음에
  연결되면: (1) 홀드 버그 고친 뒤 실제로 손 뗀 만큼 소리가 끊기는지
  귀로, (2) 새 플러그인 8종을 하나씩 꽂아서 소리·크래시 확인, (3)
  로우컷·하이컷 슬라이더 동작, (4) 아이콘이 화면 다섯 곳에 다 제대로
  뜨는지 스크린샷으로 봐야 한다.

## 남은 일(다음 배치)
- 악기 선택 UX 를 카테고리 아이콘보다 더 다듬을지(예: 탭 전환식으로
  바꾸기 등) — 지금은 "아이콘 얹은 기존 목록"까지만 함.
- UI/UX 전반 정리 — 범위가 넓어 사용자가 구체적으로 "여기가 거슬린다"
  짚어주는 대로 다음에 이어가는 게 나을 듯.
- 최적화(렉 없게) — 실기기로 실제 버벅임을 재현/측정한 뒤 손대는 게
  맞다(추측성 최적화는 오히려 코드만 복잡해질 위험).

## 2026-09-16(이어서) — 실기기 확인 완료

기기 재연결. 릴리스 APK(119.8MB) 설치해서 이번 배치 전부 확인:

- **트랙 종류 아이콘**: 씬 화면 트랙 줄에 드럼=앨범·베이스=이퀄라이저·
  화음=피아노·가락=음표 아이콘 정상 표시.
- **악기 카테고리 아이콘**: 라이브 「악기」 시트에 건반·뜯고 치는·
  관악기·현악기·신스·베이스·목소리 7개 계열 아이콘 전부 정상.
- **플러그인 묶음 아이콘**: 믹서 「무엇을 꽂을까요」 시트에 밴드·공간·
  EQ·다이내믹·두께·모듈레이션·로파이·마스터링 8개 아이콘 전부 정상.
- **새 플러그인 동작**: 비트크러셔(로파이)·익사이터(마스터링) 실제로
  꽂아서 손잡이(자리·양·섞기 등)가 `FxDef` 그대로 자동 생성되는 것,
  값 단위 표시(Hz→"3.0k" 축약 등)까지 확인. 인서트 지우기도 정상.
  크래시·flutter 예외 로그 없음.
- **로우컷·하이컷**: 채널 시트에 EQ 밑에 정상 표시("꺼짐" 기본값),
  슬라이더를 움직이면 값이 즉시 반영되고 리셋 버튼도 정상 동작.
- 확인 중 실수로 건드린 "신스 베이스" 트랙 볼륨(0%)·로우컷 값은
  테스트 뒤 원래대로(100%·꺼짐) 되돌려 둠 — 실제 곡에는 영향 없음.
- **아직 못한 것**: 홀드 버그가 실제로 "누른 만큼만 소리 남"을 귀로
  듣는 것, 새 플러그인 8종의 소리 자체(음색이 실제로 의도한 대로
  들리는지)는 스크린샷·로그로는 확인 불가 — 직접 쳐 보고 이상하면
  알려줘.

## 2026-09-16(이어서) — 업라이트 베이스 표본 교체

University of Iowa 세기 한 벌(9자리, C1~C3)에서 VSCO2 Community
Edition(CC0)의 Solo Contrabass Pizz로 교체(`lib/sampler.dart` `upright`
항목). 14자리(E0~B2)로 넓혔고, 그중 v1/v3 두 벌 녹음이 있는 6자리
(E0·F#0·G0·A#0·C1·E1)는 pp=여리게·mf/ff=세게로 실제 세기가 갈린다.
나머지 8자리는 원본이 한 벌뿐이라 예전처럼 세 파일이 같다.
`flutter analyze`(36개, 기존과 동일) · `flutter test`(132개) 통과.
CC0라 크레딧 표기 의무는 없음(`assets/samples/upright/SOURCE.md`).

## 2026-09-16(이어서) — 쇼 모드 진입 시 곡이 처음부터 재시작되던 버그

**증상**: 타임라인에서 곡을 전체 재생해 놓고 "쇼" 탭으로 가면 곡이
처음부터 다시 시작됨. 처음엔 "라이브 모드"로 잘못 알고 `live_view.dart`
쪽을 이 잡듯 뒤졌는데(코드상 탭 전환만으로 재생을 다시 거는 곳이 없어
실기기에서 임시 로그(`playSong`/`playLoop` 호출 스택 출력)까지 찍어
확인) 재현이 안 됐다 — 사용자가 다시 확인해 준 덕에 실제로는 "쇼"
화면이었다는 걸 알아냄.

**원인**: `lib/ui/show_view.dart` `initState()` 가
`if (h != null && widget.transport.playing) { playSong(..., loop:true); }`
로, **곡 전체가 이미 재생 중이었어도** 무조건 `playSong`(=처음부터
다시 걸기)을 불렀다. 원래 의도는 "씬 루프(한 판이 수 초)가 돌고 있는
채로 들어오면 쇼 화면의 시간축(한 칸 = 곡 전체)과 어긋나니 곡 모드로
바꿔 건다"는 것이었는데, 조건에 `!songLoop`이 빠져서 **이미 곡 전체
모드였던 경우까지 재시작시켰다.**

**수정**: 조건에 `&& !widget.transport.songLoop` 추가 — 이미 곡 전체가
돌고 있으면 그대로 두고, 씬 루프였을 때만 곡 모드로 다시 건다.

**확인**: 릴리스 APK 재설치 후 실기기에서 타임라인 "곡 재생" → 6초
대기 → "쇼" 탭 전환 → 진입 직후 0:08/2:12, 3초 뒤 0:27/2:12로 이어서
올라가는 것 확인(재시작이었다면 0:00부터 다시 시작했을 것). `flutter
analyze`(36개)·`flutter test`(132개) 통과.

## 2026-09-16(이어서) — 색소폰 표본 교체

University of Iowa 알토 색소폰(비브라토 세기 한 벌, 9자리)에서
FreePats "Tenor Saxophone"(CC0, Versilian Community Sample Library)로
교체(`lib/sampler.dart` `sax` 항목). 13자리(E2~E6)로 넓혔고, 그중
v2/v3 두 벌 녹음이 있는 5자리(C3·C4·E3·G#2·G#3)는 pp·mf=여리게,
ff=세게로 실제 세기가 갈린다. 테너라 알토보다 낮은 음역이지만 어차피
피치 시프트로 맞추므로 실사용엔 문제없음. CC0라 크레딧 표기 의무 없음
(`assets/samples/sax/SOURCE.md`). `flutter analyze`(36개)·`flutter
test`(132개) 통과.

## 2026-09-17 — 믹서 서랍 정리 + 신스 질감(유니즌·필터 움직임)

사용자 요청: "신스 악기 음색 개선 — 유니즌/디투리즈로 두꺼워지게,
필터/움직임 추가. 악기별로 설정 가능하게. 모든 설정들 서랍형식으로
정리."

**믹서 채널 시트를 서랍(접었다 펴는 구획)으로 재구성**
(`lib/ui/mixer_view.dart` `_Drawer`). 볼륨·좌우·울림·EQ·로우컷·
하이컷은 "기본" 서랍 하나에 묶어 늘 펴 둔다(자주 만지는 것들 — 여기를
접으면 `mixer_ui_test.dart`의 "5-c) 채널 시트" 검사가 깨진다). 악기
설정·질감·ADSR·이펙트는 각각 서랍으로 접어서 시작한다 — 손잡이가 많은
악기(신스류)는 시트가 한 화면을 넘길 만큼 길어서 계속 스크롤만
하다 끝났다.

**유니즌 — 새 손잡이.** `Track.uniCents`(null=악기 기본값, `Inst.uni`)
→ `TrackMix.uniCents`(신설, `mixer.dart`) → 새 엔진 메시지 `_cUni`/
`AudioClient.setUni()`(`audio_isolate.dart`) → `Engine.noteOn`/
`noteHold`가 `_adsrOf(part)`에서 같이 읽어 `Synth.noteOn`의 새 인자
`uniOverride`로 전달(`synth.dart`) — ADSR 손잡이와 완전히 같은 배선
패턴을 그대로 따랐다. `noteOn` 안에서 `I.uni`를 쓰던 세 자리(유니즌
오실레이터 수 계산, 위/아래 디튠 오실레이터 생성)를 전부 `uni`(오버
라이드 적용값)로 바꿨다.

**필터 움직임 — 새로 만들 필요가 없었다.** `Track.lfoHz`/`lfoDepth`가
이미 있었다(엠비언트 장르가 패드에 조용히 걸던 "숨 쉬는 필터" —
`genre_mix.dart`) — 화면에 손잡이만 없었다. `pushMix`/`mixer_view.
_push`의 `setBus` 호출에 `lfoHz`/`lfoDepth`가 빠져 있던 것도 로우컷·
하이컷과 같은 종류의 누락이라 같이 채웠다. 하이컷이 열려 있으면
(기본 20000Hz) 필터가 움직여도 안 들리므로 안내 문구를 넣었다.

새 "질감" 서랍은 ADSR과 같은 대상(`hasAdsr()` — 신스 계열만)에 뜬다.
`flutter analyze`(36개, 기존과 동일)·`flutter test`(132개, `mixer_ui_
test.dart` 포함) 통과. 릴리스 APK 실기기 확인: 서랍 펼침/접힘 정상,
유니즌 5cent·필터 속도 0.14Hz 슬라이더 조작 시 크래시 없이 값 반영,
리셋 버튼으로 셋 다 기본값 복귀 확인. **못 한 것**: 유니즌 두께감·
필터 움직임이 실제로 귀에 들리는지는 직접 들어봐야 한다.

## 2026-09-17(이어서) — Doodle Play Phase 1: 드럼(킥·스네어·하이햇) 직접 연주해서 만들기

사용자가 준 별도 지시서("Music Doodle Engine — Doodle Play 신규 기능
구현 지시서")를 분석해서 착수. 지시서 원칙("기존 오디오 엔진·Clip·
Scene·Editor를 새로 만들지 않는다", "한 화면 = 한 악기", "장르가 순서를
정한다")을 그대로 따랐고, 사용자가 확정한 세 가지 결정을 반영했다:
1. 드럼 구조 — 킥/스네어/하이햇을 별도 트랙으로 쪼개지 않고, **드럼
   패턴 하나에 레인만 순서대로 채워 넣는 방식**(엔진 구조 그대로 유지).
2. 진입 지점 — 홈 화면의 새 "두드려서 만들기" 카드(기존 "질문에 답해서
   곡 만들기" 바로 아래).
3. 이번 배치 범위 — 킥 하나만이 아니라 **드럼 전체(킥+스네어+하이햇)**.

**조사 결과, 재사용 부품이 거의 다 있었다**(새로 만든 건 진행 순서를
강제하는 화면 자체뿐):
- 소리: `AudioClient.drumOn` — 라이브 화면과 같은 경로.
- 박 세기(미리 세기 4→REC→끝): `TapClock`(`lib/tap_rec.dart`) — 편집기의
  "두드려 넣기" 화면이 이미 쓰던 상태 기계 그대로.
- 손끝 위치→칸 변환: `TapRecorder`(같은 파일) — pad 인덱스 기반이라
  드럼 레인에 원래도 맞게 설계돼 있었다(`kTapDrumPads`).
- Clip 생성: `DrumOps`(`edit_ops.dart`) + `Project.putUserPattern`/
  `setClip` — 편집기 "두드려 넣기"가 쓰는 것과 완전히 같은 파이프라인.
- 다듬기: `EditorView`를 그대로 열었다(`Project.makeEditable`이 사용자
  패턴은 그대로 열어 주므로 손댈 필요 없음).

**새로 만든 파일**: `lib/ui/doodle_play_view.dart` (`DoodlePlayView` +
`openDoodlePlay()` + `kDoodleDrumStages` 데이터 — 나중에 베이스·코드·
멜로디를 더해도 이 목록만 늘리면 된다, 화면 로직은 안 바뀜).

**흐름**: 홈 "두드려서 만들기" → 장르 선택(바텀시트, `kGenres` 그대로
재사용, BPM·드럼킷 결정) → `store.newSong()`으로 새 빈 프로젝트 →
`project.setGenre()` → `DoodlePlayView` 전체 화면 → 킥 단계(빈 드럼
패턴을 만들어 즉시 그 씬에 배정, 씬 루프 재생 시작) → 두드리면
`drumOn`으로 즉시 소리 + `TapRecorder`가 칸에 기록 → 2마디(지시서 10번
기본값) 지나면 자동 종료 → 리뷰 화면(다시 녹음/다듬기/사용하기) →
사용하기 누르면 그 레인만 `DrumOps`에 반영해 패턴에 쓰고
`SceneSequencer.refreshLoop`로 바로 들리게 함 → 스네어 → 하이햇 순으로
반복 → 완성 화면 → 워크스페이스(씬 화면)로 복귀.

마지막 4박은 화면 도트 색과 `HapticFeedback.lightImpact()`로 알린다
(촉각 없는 기기에서도 조용히 무시되니 따로 안 감쌌다). 실수로 뒤로가기
나가는 것은 `PopScope(canPop: _allDone)`로 막고 닫기(X) 버튼으로만
나가게 했다(지시서 32번).

**의도적으로 안 한 것(범위 밖)**: 짧은/빠른 연속 탭 같은 제스처 확장
(지시서 14번 "향후 확장"), 베이스·코드·멜로디 단계, "▶ 들어보기"
버튼(연주하면서 바로 들리므로 1차는 생략), Undo 연동(기존 편집기
되돌리기와의 관계는 다음 배치에서 검토).

**확인 상태**: `flutter analyze`(36개, 기존과 동일)·`flutter test`
(132개) 통과, 회귀 없음. **실기기 확인은 못 했다** — 폰이 USB에서
끊겨서(`adb devices`에 안 잡힘) 새 화면을 실제로 두드려 보는 것까지는
다음에 폰을 다시 연결한 뒤 해야 한다. 특히 확인이 필요한 것: 실제
2마디 타이밍이 맞게 도는지, `TapClock`의 마디 시작 대기가 새 프로젝트
막 만든 직후에도 정상 동작하는지, 완성 후 워크스페이스에서 드럼 소리가
실제로 들리는지.

## 2026-09-19 — Doodle Play 실기기 확인 + 진짜 버그 하나 발견·수정

폰 재연결. `store.newSong()` 뒤 BPM 안 옮겨지던 것부터 잡았다:
`Project.setGenre()`는 `Project` 안의 장르 이름만 바꾸고 `Transport.bpm`
은 안 건드린다("질문에 답해서 곡 만들기"의 `applyAsk`도 같은 이유로
`tr.bpm = r.bpm`을 따로 한다) — `_doodlePlay()`에도
`transport.bpm = genreDef(genre).bpm` 을 추가했다. 화면 위쪽 장르
표시도 `project.genre`(키, 'hiphop')를 그대로 찍고 있어서
`genreDef(genre).label`('힙합')로 고쳤다.

**진짜 버그(엔진 공용 코드, `tap_rec.dart`)**: 실기기에서 KICK 화면을
열어 두면 미리 세기 뒤 녹음이 **몇 백 ms 만에** "끝"으로 넘어가고
아무것도 안 찍혔다(반복 재현). 디버그 로그(`TapClock.update` 앞뒤로
`pos`·`beatF` 찍기)로 잡았다: `LoopClock.pos()`가 아주 가끔 살짝
**뒤로** 흔들린다(0.25초마다 오는 실측값과 그 사이 스톱워치 보간이
만나는 자리에서 나는 정상적인 미세한 어긋남 — 실제로 잡힌 값은
0.0677→0.0564, 약 −0.18박어치). `TapClock.update()`의 "판이 넘어갔다"
판정(`d<0 이면 d+=beatsPerLoop`)이 이 작은 뒤흔들림도 "판을 통째로
한 바퀴 돌았다"로 잘못 읽어서 그 자리에서 `_run`에 `beatsPerLoop`
(이번 경우 16박)를 통째로 얹어 버렸다 — 그러면 미리 세기(4박)·녹음
(8박) 문턱을 한 틱 만에 넘어 버린다.

**수정**: `d`가 `-beatsPerLoop/2`보다 작을 때만(진짜 한 바퀴 — 이론상
`-beatsPerLoop`에 가깝다) 되접고, 그보다 작은 음수(흔들림)는 0으로
무시하게 `lib/tap_rec.dart`의 `TapClock.update()`를 고쳤다. 이 파일은
편집기의 "두드려 넣기" 화면(`tap_sheet.dart`)도 같이 쓰는 공용
코드라 — 이 버그는 사실 그 화면에도 이미 있었을 잠재 결함이었다
(드물게만 터져서 안 걸렸을 뿐). `test/tap_check_test.dart`(진짜 한
바퀴 도는 시뮬레이션 포함)가 여전히 통과하는 것으로 정상적인 되접기는
안 깨졌는지 확인했다.

**실기기로 드럼 전체(킥→스네어→하이햇) 끝까지 확인**: 힙합(88BPM)
새 프로젝트 → KICK 두드리기(7번 침, "다시 녹음"으로 재시도도 확인) →
사용하기 → SNARE(7번) → 사용하기 → HI-HAT(7번) → "사용하기 · 완성" →
"드럼 완성!" 화면 → "내 곡으로 가기" → 워크스페이스 씬 화면에 진입,
드럼 트랙 패턴이 `doodle_1789814807834`로 정상 배정되어 곡이 실제로
재생 중인 것 확인(4트랙 다 소리 남, 크래시 없음). 되돌아간
"다듬기"(편집기)에서도 2마디 드럼 격자가 정상적으로 열렸다.

`flutter analyze`(36개, 기존과 동일)·`flutter test`(132개,
`tap_check_test.dart` 포함) 통과. **못 한 것**: 킥/스네어/하이햇 소리
자체의 타이밍이 귀로 듣기에 정확한지, 여러 장르를 돌며 반복 사용해도
안정적인지는 좀 더 오래 써 봐야 안다.

## 2026-09-19(이어서) — Doodle Play Phase 2: BASS 단계 추가

드럼 셋 뒤에 BASS를 이어 붙였다. 지시서 28번("TAP → 현재 추천 음")대로
TAP만 구현 — UP/DOWN(스케일 이동)·HOLD(길게 연주)는 지시서 자체가
"향후 확장"으로 남긴 부분이라 이번에도 안 했다.

**드럼과 다른 점, 재사용한 것**: 드럼은 레인(같은 패턴 안의 한 줄)만
채웠지만 베이스는 **새 note 패턴**이 필요하다. 이것도 새로 안 만들고
편집기의 "두드려 넣기"가 쓰는 것을 그대로 가져왔다 — `tapDegree`로 그
칸에 흐르는 코드의 뿌리음을 구하고(`project.readSceneProg()`),
`tapToNotes`로 `NotePatternDef`용 행을 만든다. 소리도 새 경로가
아니라 `AudioClient.noteOn`(기존 라이브 재생 경로) 그대로.

**구조 변경**: `(String lane, String label)` 튜플 목록이던
`kDoodleDrumStages`를 `DoodleKind{drum,bass}`를 가진 `DoodleStage`
클래스 + `kDoodleStages` 목록으로 일반화했다. `_tap()`/`_keep()`이
`stageDef.kind`로 갈라져 드럼은 `DrumOps`, 베이스는 `tapToNotes`+
`NotePatternDef` 경로를 탄다 — 코드·멜로디를 더할 때도 이 분기만
늘리면 된다(지시서 30번, 화면 로직은 안 바뀜).

베이스 트랙·패턴도 드럼과 같은 이유로 **Doodle Play 진입 시점에 미리
비워 둔다**(`p.tracks.firstWhere(type=='bass')` 찾아 빈
`NotePatternDef` 배정) — BASS 단계에 오기 전까지 조용하다가 사용자가
친 것만 남는다.

**실기기 확인**: 로파이(78BPM)로 킥(9)→스네어(8)→하이햇(8)→
베이스(8) 전부 침 → "기본 비트 완성!"(문구도 "베이스까지" 반영해
고침) → 씬 화면에서 드럼(`doodle_drum_...`)·신스 베이스
(`doodle_bass_...`) 패턴이 둘 다 정상 배정되어 4트랙이 함께 재생 중인
것 확인, 크래시 없음. `flutter analyze`(36개, 그대로)·`flutter
test`(132개) 통과.

**남은 것(다음 배치)**: 코드·멜로디. 코드는 `chord: true`로
`tapToNotes` 호출을 그대로 재사용할 수 있을 것 — 지시서 29번대로
"TAP → 현재 추천 코드"만 먼저.

## 2026-09-19(이어서) — Doodle Play Phase 3: CHORD 단계 추가

지시서 29번("TAP → 현재 추천 코드")대로 CHORD를 이어 붙였다. 방금
남긴 계획 그대로 `tapToNotes(..., chord: true)`를 재사용했고, 소리는
`tap_sheet.dart`의 `_sound()`가 코드 줄에서 하던 것과 똑같이
`diatonicChords(key)[degree % 7]` → `chordFreqsOf()` → `host.batch()`
로 화음 전체(여러 음)를 한 번에 낸다 — 한 음만 나면 코드인지 안
들려서다.

**구조**: `DoodleKind`에 `chord` 하나만 늘리고 `_tap()`/`_keep()`/
`_openEditor()`의 switch에 케이스 하나씩 추가 — 지난 배치에서 정리해
둔 대로 화면 로직은 안 바뀌고 목록·분기만 늘었다. 코드 트랙·패턴도
드럼·베이스와 같은 방식으로 진입 시점에 미리 비워 둔다.

**실기기 확인**: 로파이(78BPM)로 킥(8)→스네어→하이햇→베이스(7)→
코드(7) 전부 침(중간에 실제 전화가 걸려와 화면을 덮었는데도 크래시
없이 버티고 이어서 진행됨) → "기본 비트 완성!"(문구도 "코드까지"
반영) → 씬 화면에서 드럼·신스 베이스·패드(코드) 세 트랙 모두
`doodle_*` 패턴이 정상 배정되어 재생 중인 것 확인. `flutter
analyze`(36개, 그대로)·`flutter test`(132개) 통과.

**남은 것**: 멜로디 하나만 남았다. 지시서 28번 흐름과 비슷하게
스케일 안전음 탭 방식이 될 것.

## 2026-09-19(이어서 2) — Doodle Play Phase 4: MELODY 단계 추가 (드럼·베이스·코드·멜로디 전부 완성)

지시서 28번("피아노 건반처럼 모든 음을 선택하게 하지 않는다 — 현재
Scale 기준 안전한 음만")대로 MELODY를 마지막으로 이어 붙였다. 알고
보니 이 "스케일 안전음"은 **이미 있는 계산**이었다 — `tap_rec.dart`의
`tapDegree()`가 `type != 'bass'`이고 `chord`가 아닐 때 이미
`d + 7`(코드 뿌리음을 가운데 옥타브로 옮긴 자리)을 돌려주는데, 이게
정확히 "지금 코드에 어울리는 스케일 음"이다. 그래서 새 계산은 하나도
안 만들고, 베이스 케이스와 똑같은 파이프라인에 `type: 'melody'`만
넘기는 것으로 끝났다.

**구조**: `_degreeAt()`을 `chord: true/false`만 받던 것에서
`type`(String)도 받게 살짝 넓혔다(베이스는 `type:'bass'`, 코드는
`type:'chord'`, 멜로디는 `type:'melody'` — `tapDegree`가 이 문자열로
베이스인지 아닌지만 구분한다). `DoodleKind`에 `melody` 하나 늘리고
`_tap()`/`_keep()`/`_openEditor()` switch에 케이스 하나씩 추가 — 이번
배치도 화면 진행 로직은 그대로, 목록·분기만 늘었다(지시서 30번
그대로). 멜로디 트랙·패턴도 나머지 셋과 같은 방식으로 진입 시점에
미리 비워 둔다("리드" 음색 트랙, 없으면 4번째 트랙).

**완성 화면 문구**도 이제 다섯 악기(킥·스네어·하이햇·베이스·코드·
멜로디) 전부 언급하도록 고쳤다 — 더 이상 "멜로디는 씬 화면에서"가
아니라 "다 만들었다"로 바뀌었다(지금까지 매 배치 문구를 그때그때
고쳐 온 것과 같은 이유 — 다음 배치가 없으니 안내가 아니라 완료
선언).

**실기기 확인**: 로파이(78BPM)로 킥(8)→스네어→하이햇→베이스→
코드(3)→멜로디(11) 전부 침 → 진행 표시에 MELODY까지 6칸이 정확히
뜨고 마지막 버튼 문구가 "사용하기 · 완성"으로 바뀌는 것 확인 →
"기본 비트 완성!" 화면 문구가 다섯 악기 다 언급하도록 나온 것 확인
→ 씬 화면에서 드럼·신스 베이스·패드·**리드(신스 베이스·화음·가락 셋
다 이번에 친 `doodle_*` 패턴)** 네 트랙 모두 정상 배정되어 재생 중인
것 확인, 크래시 없음. (이번 회차는 `adb shell input tap`의 좌표
스케일 실수로 재시도가 여러 번 있었지만, 화면 자체의 문제는 아니었다.)
`flutter analyze`(36개, 그대로)·`flutter test`(132개) 통과.

**남은 것**: 없음 — 지시서가 요구한 드럼·베이스·코드·멜로디 MVP
범위를 다 채웠다. 다음은 사용자가 실제로 써 보면서 나오는 피드백
(UP/DOWN·HOLD 같은 "향후 확장" 제스처, 미리 듣기 버튼, 실행취소
연동 등 이미 적어 둔 미룬 항목들) 대응이 될 것.

## 2026-09-19(이어서 3) — 핑거 베이스 표본을 Karoryfer Fashionbass 로 교체

Doodle Play를 끝낸 뒤 사용자가 고른 다음 작업 둘 중 하나("악기 퀄리티
계속" / "가로모드 개선") — 악기 쪽부터. 남아 있던 마지막 "가짜
다이내믹" 악기가 핑거 베이스였다(나머지는 이번 세션 안에 업라이트·
색소폰까지 다 갈았다) — FreePats "Finger Bass YR"는 세기 한 벌뿐이라
pp/mf/ff 세 파일이 전부 같은 녹음이었다.

**대체 표본 찾기**: VSCO2 CE(업라이트가 이미 쓰는 소스)는 더블베이스만
있고 일렉 베이스가 없다. VCSL(sgossner)도 마찬가지로 없다. FreePats도
핑거 베이스는 이 한 벌뿐이었다. 웹 검색으로 **Karoryfer Samples**
(sfzinstruments GitHub 조직에 CC0로 풀린 여러 베이스 기타 표본)를
찾았고, 그중 "Fashionbass"(Killer KB-Simmony 베이스, D. Smolken 연주·
매핑)가 **진짜 pp/p/mf/f/ff 다섯 벌** 세기 + 단3도 간격 19자리
(F#0~A4)로 다른 후보(growlybass는 낮은 음이 안 남아서 탈락, meatbass·
sneakybass는 더블베이스라 upright 자리)보다 이번 목적에 제일 맞았다.

**작업**: GitHub raw로 57개(19자리 × pp/mf/ff, rr1만) IEEE float
44.1kHz 원본을 받아 `afconvert`로 22.05kHz 16비트로 낮춘 뒤 기존
`tool/prep_samples.dart`로 3초 트림·페이드(기본팩 기준 그대로 유지).
원본 파일명이 플랫 표기(gb0·ab1)라 프로젝트 관례(샵 표기, Fs0·Gs1)로
바꿔 넣었다.

- [lib/sampler.dart](lib/sampler.dart) `_kSampleSets['fingerbass']`
  루트 주파수 표를 12자리(E1~D#2, 반음 간격)에서 19자리(F#0~A4, 단3도
  간격)로 전면 교체 — 자리 수는 줄었지만 실제 다이내믹이 진짜라 훨씬
  낫다.
- [assets/samples/fingerbass/SOURCE.md](assets/samples/fingerbass/SOURCE.md)
  전면 재작성(출처·라이선스 CC0·자리 목록).
- [lib/ui/settings_sheet.dart](lib/ui/settings_sheet.dart) 「소리 표본
  출처」 크레딧 정리 — 핑거 베이스 항목을 새 출처로 바꾸면서, 실은
  이번 세션 초반에 업라이트·색소폰·일렉피아노를 바꿀 때 이 크레딧
  목록에 반영을 빠뜨렸던 것도 같이 발견해 채워 넣었다(업라이트→
  VSCO2 CE 줄에 합침, 색소폰→기타 줄에 합침, 일렉피아노 새 줄).
- `pubspec.yaml`은 폴더 단위 참조라 손 안 댐.
- `flutter analyze`(36개, 그대로)·`flutter test`(132개) 통과.
- 실기기 확인: 디버그 APK. 록(138BPM, 베이스 악기가 `fingerbass`로
  고정되는 유일한 장르) Doodle Play로 킥→스네어→하이햇→**베이스(5번
  치기, `logcat`에 flutter 예외·`bad wav` 에러 없음 확인)**→코드→
  멜로디까지 끝까지 진행 → 씬 화면에서 "핑거 베이스" 트랙이
  `doodle_bass_...` 패턴으로 정상 배정되어 재생 중인 것 확인, 크래시
  없음. **귀로 음색 자체를 비교 확인하지는 못함**(이전 표본 교체들과
  같은 환경 제약) — 실제로 들어보고 별로면 알려줘.
- 다음 후보(사용자가 같이 고른 나머지 하나): 가로모드에서 타임라인·씬
  화면이 너무 꽉 차 보인다는 지적 — 레이아웃 손보기, 아직 시작 안 함.

## 2026-09-20 — 가로모드에서 타임라인이 꽉 차던 것 고침 + 홈 화면 사이드바 넘침 버그 발견·수정

실기기를 가로로 눕혀 직접 봤다("타임라인·씬 등등 너무 꽉 차고
답답하다"는 지적을 말로만 짐작하지 않고 재현부터). 두 가지를 찾았다.

**진짜 버그(디버그 배너로 확인) — 홈 화면 왼쪽 사이드바가 가로에서
넘쳤다.** [lib/ui/home_view.dart](lib/ui/home_view.dart)의 `_Sidebar`가
스크롤 없는 `Column`이었다 — 장르가 지금 17개까지 늘었는데(세션 내내
로파이·록·발라드 등 계속 추가돼 왔다), 가로에서는 세로 높이가 짧아
목록 하나만으로도 "BOTTOM OVERFLOWED BY 38 PIXELS" 배너가 떴다. 목록을
`SingleChildScrollView`로 감싸 스크롤되게 고쳤다 — 세로에서는 애초에
넘칠 일이 없어 화면이 그대로다.

**타임라인이 꽉 차 보이던 진짜 이유 — "+구간 붙이기" 줄이 화면 아래
계속 펼쳐져 있었다.** [lib/ui/song_view.dart](lib/ui/song_view.dart)의
`_AddSection`은 씬 칩 목록을 최대 92px 높이로 늘 펼쳐 둔다(스크롤 없이
바로 닿게 하려고 5단계에서 일부러 그렇게 만든 것 — `song_ui_test.dart`
"6) 붙이기 줄 고정"). 세로에서는 42px짜리 두 줄(길이·보기)을 한 줄로
합치는 기존 가로 대응(`height < 520`)이 이미 있었는데, 이 칩 목록은
그 대응에서 빠져 있었다 — 실제로 가로에서 재 보니 헤더·재생 버튼·
길이·보기 줄을 다 빼고 나면 타임라인 자체가 겨우 한 줄 반밖에 안
남았다(구간 블록 안의 멜로디 미리보기 점선도 거의 안 보임).

**고침**: `_AddSection`에 `compact` 옵션을 추가 — 가로(`height < 520`,
길이·보기 줄 합치기와 같은 문턱)에서는 92px짜리 칩 목록 대신 40px짜리
한 줄 버튼("＋구간 붙이기 ›")만 두고, 누르면 `showModalBottomSheet`로
같은 칩 목록을 띄운다. 세로에서는 손 안 댐 — `find.text('＋구간
붙이기')`로 찾는 기존 시험(`song_ui_test.dart`)이 그대로 통과하는
이유이기도 하다(글자는 압축 버튼에도 그대로 있다).

- [lib/ui/song_view.dart](lib/ui/song_view.dart) `_AddSection`에
  `compact` 파라미터 추가, 칩 목록 빌드를 `_chips()` 메서드로 빼서
  인라인·시트 두 군데서 재사용. 호출부에서
  `compact: MediaQuery.of(context).size.height < 520` 전달.
- [lib/ui/home_view.dart](lib/ui/home_view.dart) `_Sidebar`의 `Column`을
  `SingleChildScrollView`로 감쌈.
- `flutter analyze`(36개, 그대로)·`flutter test`(132개, "2) 가로 안
  넘침" 포함) 통과.
- 실기기 확인: 디버그 APK, 가로로 눕혀서 — (1) 홈 화면 사이드바가
  더는 안 넘치고 로파이~힙합까지 스크롤로 다 닿는 것 확인. (2) 씬 화면
  → 타임라인 진입 → "+구간 붙이기"가 한 줄 버튼으로 줄어들고, 그 자리
  만큼 씬 트랙 행이 넉넉해져 구간 블록의 멜로디 미리보기까지 온전히
  보이는 것 확인. (3) 버튼을 눌러 시트가 뜨고 씬 칩을 그대로 고를 수
  있는 것 확인. (4) 세로로 되돌려 기존 인라인 칩 줄이 그대로인 것도
  같이 확인 — 회귀 없음.
- **남겨 둔 것**: 씬 화면 자체(트랙 행 높이)는 이번엔 안 건드렸다 —
  타임라인만큼 명백히 망가진 자리가 아니라 "행이 넉넉해서 스크롤이
  필요하다" 정도라, 다음에 사용자가 직접 써 보고 더 손볼 곳을 짚으면
  그때 이어가는 게 나을 것 같다.

## 2026-09-20(이어서) — 재생 중 "이상한 노이즈" 진짜 원인 찾아 고침(PCM 플러그인 락 경합)

사용자가 "소리가 안 나오고 이상한 노이즈가 나온다"고 신고. 홈 화면
"0개" 표시 버그(위 항목)를 고친 김에 같은 세션에서 이어서 조사 —
실기기로 곡을 재생하며 `logcat`을 보니 몇 초 만에 이게 반복됐다:

```
Long monitor contention with owner PCMPlaybackThread ... for 438ms
```

**원인**: `packages/flutter_pcm_sound`(이 프로젝트 안으로 포크해 둔
오디오 재생 플러그인, `pubspec.yaml`에 `path: packages/flutter_pcm_sound`
로 물려 있다)의 Java 쪽 재생 스레드가, 큐에 **몇 바이트 남았는지**를
버퍼 하나(약 4ms치) 쓸 때마다 `mSamples`(재생 대기열) **전체를 처음부터
다시 훑어서** 더하고 있었다. Dart 쪽이 미리 여러 마디를 한꺼번에
먹여 두면(지연 대비 버퍼링) 큐에 수백 개가 쌓이는데, 그 수백 개를
매번 다 더하는 동안 `synchronized (mSamples)` 락을 쥐고 있었다 —
그런데 Dart의 "feed"(새 소리 밀어넣기) 호출도 **같은 락**을 기다려야
해서, 그 순간마다 최대 438ms씩 막혔다. 그만큼 오디오 트랙에 새
데이터가 제때 안 들어가면 끊기거나 지지직거리는 소리(신고된 "노이즈")
로 들린다.

**고침**: 큐를 훑어서 다시 세는 대신, 넣을 때(+)·뺄 때(−) 그때그때
갱신되는 누적 바이트 수(`mQueuedBytes`) 하나만 들고 있게 바꿨다 —
이러면 락을 쥐는 구간이 큐 길이와 무관하게 항상 O(1)이다.

- [packages/flutter_pcm_sound/android/src/main/java/com/lib/flutter_pcm_sound/FlutterPcmSoundPlugin.java](packages/flutter_pcm_sound/android/src/main/java/com/lib/flutter_pcm_sound/FlutterPcmSoundPlugin.java) —
  `mQueuedBytes` 필드 추가. `"feed"`에서 청크 넣을 때 더함, `"setup"`
  (재시작)에서 0으로 리셋, `"remainingFrames"`는 훑지 않고 이 값만
  읽음, `playbackThreadLoop()`는 방금 쓴 만큼만 뺌(`data.remaining()`을
  쓰기 전에 미리 저장 — 쓰고 나면 0이 된다).
- Dart 쪽은 손 안 댐(순수 네이티브 쪽 문제였다) — `flutter analyze`
  (36개, 그대로)·`flutter test`(132개) 통과.
- 실기기 확인: 디버그 APK 다시 빌드·설치. 고치기 전엔 재생 시작
  5초 안에 "Long monitor contention" 경고가 4번(최대 438ms) 찍혔는데,
  고친 뒤엔 같은 곡을 30초 넘게 틀어도(3마디대까지 진행, 크래시 없음)
  `logcat`에 경고·예외가 **한 번도** 안 뜸. **귀로 "노이즈가 사라졌다"
  자체를 확인하지는 못함**(이 세션 환경 제약 — 스크린샷·로그로만 확인
  가능) — 실제로 들어보고 여전히 이상하면 알려 달라고 해야 한다.

## 2026-09-20(이어서 2) — Doodle Play 제스처 확장: HOLD · UP/DOWN · 탭 이펙트 (+ 「다듬기」 빈 판 버그)

사용자가 "제스처 개선할 것들 보고하고 질문해서 적용해" → 셋(HOLD ·
탭 화면 이펙트 · UP/DOWN) 중 **"모두"**를 골랐다. 지시서가 "향후 확장"
으로 미뤄 뒀던 것을 전부 붙였다.

**손짓을 `GestureDetector` 대신 `Listener` 로 받는다.** 탭 인식기와 끌기
인식기가 같이 있으면 "누가 이겼나"가 정해질 때까지 `onTapDown` 이
미뤄진다 — 박자를 치는 화면에서 그 지연은 그대로 틀린 박이 된다. 날
포인터 이벤트(`onPointerDown/Move/Up`)로 받고 쓸기 판정을 직접 한다
(세로 36px 넘게 움직이면 음이 아니라 쓸기).

- **HOLD**: 누르고 있는 동안 소리가 계속 난다 — 베이스·멜로디는
  `host.holdOn`, 코드는 `batch(hold:true)`, 뗄 때 `holdOff(_kHoldId)`.
  드럼만 예외로 여전히 톡 치는 한 방이다(타악은 잡고 있을 것이 없다).
  `TapRecorder` 는 원래부터 누른 시간으로 길이를 재고 있었다
  (`lenOf(from,to)`) — 여태 `down` 직후 바로 `up` 을 불러서 그 기능을
  안 쓰고 있었을 뿐이라, **엔진 쪽은 고칠 게 없었다.**
- **UP/DOWN**: 위/아래로 쓸면 한 옥타브 위/아래(−1~+1). 쓸기로 판정되면
  방금 낸 소리(`holdOff`)와 적으려던 음(`TapRecorder.cancel` — 이번에
  추가한 작은 메서드)을 **둘 다 거둔다**. 안 거두면 쓸 때마다 의도 없던
  짧은 음이 한 칸씩 남는다.
  - **주의(빠지기 쉬운 함정)**: `tapToNotes` 의 `oct:` 인자는 **코드 줄
    에만** 먹는다 — `patterns.dart` 의 `buildChordPattern` 만 `n[5]` 를
    읽고, 낱음 줄(베이스·가락)을 그리는 `buildRowsPattern` 은 그 칸을
    아예 안 본다. 그래서 낱음 줄은 `oct` 대신 **도수 자체를 ±7**
    (한 옥타브 = 도수 7칸) 옮겨 적는다(`_shiftRows`). 처음엔 `oct:` 로
    넘겼다가 조용히 무시되는 것을 코드 읽다 발견했다.
- **탭 이펙트**: 친 순간 원 바깥으로 물결이 한 번 퍼지고, 누르고 있는
  동안 원이 악기 색(드럼 초록·베이스 파랑·코드 보라·가락 주황)으로
  차면서 글자가 `TAP`→`HOLD` 로 바뀐다. 시계가 이미 30ms마다
  `setState` 를 부르므로(`_tick`) 애니메이션 장치를 따로 안 둔다 —
  친 시각과 지금의 차로 크기·진하기를 셈한다.

**덤으로 찾은 진짜 버그 — 「다듬기」가 빈 판으로 열렸다.** 방금 친 것은
`_pending` 에만 있고 판에 적히는 것은 「사용하기」(`_keep`)뿐이었다 —
그래서 치자마자 「다듬기」를 누르면 편집기가 **빈 격자**로 떴다(실기기
에서 "빈 판입니다" 안내가 뜬 걸 보고 잡음). 치고 나서 바로 고치고 싶은
게 사람 마음인데 거기서 빈 판이 뜨면 방금 친 게 날아간 줄 안다.
적는 부분을 `_commit()` 으로 떼어내 「사용하기」와 「다듬기」가 같이
쓰게 고쳤다.

- [lib/ui/doodle_play_view.dart](lib/ui/doodle_play_view.dart) — `_tap()`
  을 `_pressDown/_pressMove/_pressUp` 셋으로 쪼갬, `_shiftRows`·
  `_stageColor`·`_hitRipples`·`_octBadge` 추가, `_keep()` 에서 `_commit()`
  분리.
- [lib/tap_rec.dart](lib/tap_rec.dart) — `TapRecorder.cancel(pad)` 추가
  (공용 코드지만 더하기만 했다 — 두드려 넣기 화면은 안 쓴다).
- `flutter analyze`(36개, 그대로)·`flutter test`(132개) 통과.
- 실기기 확인(로파이 78BPM, BASS 단계): (1) 길게 누르기 10번 → 6번
  기록, 「다듬기」로 열어 보니 **한 칸짜리 점이 아니라 서너 칸짜리 긴
  마디**로 찍혀 있음(HOLD 가 길이로 들어간 증거). (2) 위로 쓸기 →
  "한 옥타브 위" 이름표가 뜨고 음이 안 찍힘. (3) 누르고 있는 동안 원이
  파랗게 차고 글자가 `HOLD` 로 바뀌는 것 스크린샷으로 확인. (4)
  「다듬기」에 더는 "빈 판입니다" 안내가 안 뜸.
- **못 한 것**: 이펙트·옥타브 이동이 **귀로** 자연스러운지는 확인 못 함
  (스크린샷·로그로만 가능한 환경). 이름표가 화면 폭을 다 먹던 것은
  실기기에서 보고 `Center` 로 감싸 고쳤다.

## 2026-09-21 — Doodle Play "눌러도 바로 반응 안 한다" 고침 (무반응 구간 + 버퍼 지연)

사용자: "두들플레이 딜레이있어 누르자마자 동작이 안돼". 원인이 둘이었다.

**① 진짜 원인 — 녹음 전에는 눌러도 아무 일도 안 났다.** `_pressDown` 이
`cs.phase != TapPhase.rec` 이면 그대로 `return` 했다. 그런데 이 화면은
단계를 열면 **대기(다음 마디까지, 최대 약 3초) → 미리 세기(4박, 약 3초)**
를 지나야 녹음이 시작된다 — 그 6초 동안 큰 동그라미를 눌러도 소리도
화면도 반응이 없었다. 사용자 눈에는 "눌러도 안 되는 화면"이다.

고침: **소리는 어느 대목이든 바로 낸다. 담는 것만 녹음 중에 한다.**
치기 전에 무슨 소리가 날지 미리 만져 볼 수 있는 이득도 같이 생겼다.
녹음 전에도 "지금 몇 번째 칸인가"를 알아야 미리 들려주는 소리가 그 자리
코드에 맞으므로, `TapRecorder` 를 `_armStage` 에서 미리 만들어 둔다
(실제로 담기는 것은 `_startRec` 이 새로 끼우는 깨끗한 녹음기다 — 미리
만져 본 것은 판에 안 남는다).

**② 버퍼가 제일 안전한 값이었다.** 앞질러 만들어 두는 양이 곧 손가락
지연인데(`AudioClient.aheadFrames`), 기본이 3072프레임 = 64ms 였다.
엔진 시험 화면이 스스로 「권장」이라 적어 둔 값은 2048(43ms)이다. 박자를
손으로 치는 화면이라 이 차이가 그대로 "늦게 난다"로 느껴진다 — 이
화면에 있는 동안만 2048로 낮추고 나갈 때 사용자가 고른 값으로 되돌린다.
(어제 고친 PCM 락 경합 때문에 예전엔 작은 버퍼가 위험했는데, 그게
없어져서 이제 안전하다 — 실기기에서 14연타에도 경합·언더런 로그 0.)

**덤**: 큰 동그라미의 터치 영역을 상자 전체로 넓혔다
(`HitTestBehavior.opaque`) — 기본값이면 동그라미 밖 모서리는 눌러도
아무 일이 안 났다.

- [lib/ui/doodle_play_view.dart](lib/ui/doodle_play_view.dart) —
  `_pressDown/_pressUp` 의 대목 검사를 "담기"에만 적용, `_newRecorder()`
  로 분리해 `_armStage` 에서도 만들기, `_kDoodleAhead`(2048) 진입 시 적용·
  퇴장 시 복구, `Listener` 에 `behavior: opaque`.
- `flutter analyze`(36개, 그대로)·`flutter test`(132개) 통과.
- 실기기 확인: 대기("마디가 시작되면 시작할게요") 중에 눌렀을 때 원이
  악기 색으로 차는 것 스크린샷으로 확인(예전엔 무반응) → 이어서 녹음
  구간에서 16번 눌러 9번 정상 기록 → 미리 만져 본 소리는 안 담기는 것
  확인. 버퍼 43ms 상태로 연타해도 `logcat` 에 경합·언더런·예외 없음.
- **못 한 것**: 줄어든 지연(64→43ms)이 귀로 얼마나 다른지는 확인 못 함
  (이 환경에서는 소리를 들을 수 없다) — 여전히 늦게 느껴지면 말해 달라.

## 2026-09-21(이어서) — 지연의 진짜 범인은 **디버그 빌드**였다 + 두들플레이 범위·제스처 개편

사용자: "아직도 지연이 있어 너가 직접 확인해봐 너무너무 느려".
추측을 멈추고 **재 봤다** — `_pressDown`(UI 아이솔레이트)과 오디오
아이솔레이트가 그 음을 받는 순간에 각각 시각을 찍어 `logcat` 으로 비교.

| | 디버그 | 릴리스 |
|---|---|---|
| 보통 | 1~5ms | 0.2~2ms |
| 최악 | **299ms** | **4ms** |

이 앱의 오디오 엔진은 전부 Dart 라서 디버그(JIT·최적화 없음)에서는 렌더가
밀리고, 그 밀림이 그대로 손가락 지연으로 나온다. **그동안 실기기 확인을
줄곧 `--debug` 로 설치해 왔다** — 사용자는 그걸 만지고 "너무 느리다"고
한 것이다. **앞으로 연주감·지연을 확인할 때는 반드시 `--release` 로
설치한다.** (기능·크래시 확인은 디버그로 해도 된다.)

### 두들플레이 범위 (사용자 결정)
- **멜로디 단계를 뺐다.** 두드려 만드는 것은 곡의 바닥(드럼·베이스·화음)
  까지고, 가락은 씬 화면에서 트랙으로 쌓는다. 멜로디 트랙을 비우던
  `initState` 코드도 같이 지웠다 — 안 지우면 이미 골라 둔 가락이 날아간다.
- **씬마다 쓴다.** 씬 화면 아래에 「두드려서 채우기」 버튼을 달았다
  (`scene_view.dart` `_DoodleFillButton`). 홈 화면 입구는 첫 씬용으로 유지.

### 제스처 (사용자 선택 + 추천)
- **치는 자리 = 화면 전체**(위아래 안내줄 제외). 190px 동그라미 안만 받던
  것을 `Expanded` + `LayoutBuilder` + `Listener(opaque)` 로 넓혔다 —
  동그라미는 과녁이 아니라 표시가 됐다.
- **여러 손가락**(최대 4). 손가락마다 `TapRecorder` 판(pad)과 홀드 번호를
  따로 준다(`_Finger`). 한 손가락만 받으면 빠른 연타를 번갈아 못 친다.
- **세게 치면 세게**(사용자 결정). 화면이 주는 압력/접촉면적으로 세기를
  1~3 으로 센다. 기기마다 주는 범위가 딴판이라 **그 사람이 이 폰에서 내는
  범위를 그때그때 배워서**(`_pLo`/`_pHi`) 그 안에서 나눈다. 값이 안 변하는
  기기면 조용히 「누른 자리 높낮이」로 되돌아간다. 튜닝용으로 `[VEL]` 로그를
  남겨 뒀다 — 실제로 쳐 보고 이상하면 이 값을 보고 고친다.
- **좌우로 음 고르기**(추천안, 사용자 "선호 없음"이라 채택). 왼쪽부터
  뿌리음·3도·5도·7도. 코드 안의 음만 고르므로 무엇을 눌러도 안 틀린다 —
  같은 리듬을 쳐도 선율이 생긴다. 화면에 1·3·5·7 구역을 옅게 표시.
- 세기·고른 음은 `TapHit` 에 자리가 없어서(박자만 담는 그릇) 칸 번호로
  따로 들고 있다가(`_velOf`/`_toneOf`) 판에 적을 때 얹는다.

**아직 안 한 것(다음 후보)**: 두 손가락=화음 넓이, 놓는 순간 꾸미기
(릴리스), 흔들기(비브라토). 사용자가 "완전 구체화될 때까지 계속 제시"
하라고 했으므로 이어서 제안할 것.

## 2026-09-21 — Doodle Play: 연주감 손짓 6종 + 「친 소리 ≠ 적힌 음」 3건

사용자 지시: "제스처의 목적은 연주하는 느낌을 내는 거야. 한 옥타브 올리고
끝이 아니라." 네 관점(드러머·악기설계·모바일UX·엔진)으로 34개를 뽑아
추린 뒤, 사용자가 고른 것을 넣었다.

### 먼저 — 손짓을 얹기 전에 나온 버그 3건 (전부 시험으로 재현했다)

이 화면은 **들은 것과 판에 적힌 것이 다른** 상태였다. 귀로는 「좀 이상한데」
로만 느껴져서 여태 안 잡혔다.

1. **베이스 단계가 통째로 한 음짜리.** 베이스 음높이는 `tapDegree` →
   `_rootAt` 으로 **그 칸에 흐르는 코드**를 읽어 정하는데, `initState` 가
   코드 판을 비우고 시작하므로(안 친 악기가 울리면 안 되니까) 진행이 빈
   목록이었다 → 32칸 전부 도수 0. 시험: 빈 진행 `{0}` / 진행 있음 `{0, 5}`.
   - 처방(둘 다): ① `kDoodleStages` 순서를 드럼→**코드**→베이스로 바꿨다.
     코드를 먼저 치면 그 판이 곧 진행이 되어 베이스가 따라 걷는다.
     ② 비우기 **전에** 씬에 있던 진행을 챙겨 둔다(`_priorProg`) — 코드 단계를
     대충 넘겨도 원래 화성을 따라간다. `_prog()` 가 둘을 갈라 준다.
2. **코드 단계에서 오른쪽을 치면 다른 코드가 저장됨.** 코드 줄의 첫 칸은
   도수가 아니라 **코드 번호**(`buildChordPattern` 이 `% chords.length` 로
   읽는다)인데 거기 좌우 도수(+2/+4/+6)를 더하고 있었다. 시험: 들린 Am
   `{262,330,440}` vs 적힌 것 `{247,294,392}`.
   - 처방: 좌우는 **베이스 전용**(`_hasTone`)으로 못박았다. 코드의 표현은
     대신 두께(손가락 수)가 맡는다.
3. **베이스에서 아래로 쓸면 친 음이 전부 뭉개짐.** 베이스 도수는 0~6(이미
   제일 낮은 옥타브)이라 −7 하면 전부 0. 시험: `{0,3,4,5}` → `{0}`.
   - 처방: 베이스 옥타브는 0/+1 두 칸. 더해서 **소리 낼 때와 적을 때가 같은
     함수**(`_writeDegree`)를 쓰게 묶었다 — 전에는 clamp 순서가 달라 갈렸다.

### 넣은 손짓

- **A4 세기 = 누른 세로 위치**(위 22% 세게 / 가운데 56% 보통 / 아래 22%
  여리게). **압력 경로는 걷어냈다** — 이 폰은 진짜 손가락으로도 압력이 안
  갈린다(사용자 확인). 압력을 흉내만 내는 기기가 흔한데 세기는 드러머가
  제일 먼저 쓰는 표현이라 **기기 운**에 맡길 수 없다. 가운데를 일부러 넓게
  둔 이유: 리듬을 탈 때는 화면을 안 본다.
- **A2 꾹 눌러 롤**(드럼). 220ms 넘게 붙이고 있으면 16분 연타. 화면 시계
  (30ms)로 내면 덜컹거리므로 `drumBatch` 의 `delaySec` 로 **엔진에 시각까지
  맡긴다**(`_tickRoll`, 예약 창 0.12초). 격자에 맞춰 돌고 첫 한 방보다 한
  단계 여리다. 기계가 낸 시각이라 `TapRecorder`(사람 반응 지연 보정)를
  안 거치고 `_rollSteps` 에 따로 모은다.
- **A3 하이햇만 16분 격자.** 여태 `_newRecorder()` 가 `snap` 을 안 넘겨
  전부 8분이었다 → 16분 하이햇을 치면 두 방이 같은 칸으로 반올림되고
  `TapRecorder._hits` 가 '판:칸' 키라 **뒤엣것이 앞엣것을 덮어써서** 친 것이
  조용히 사라졌다. 8분이 기본인 데는 이유가 있으므로(`tap_rec.dart` 머리말)
  잘게 치는 것이 당연한 하이햇만 올렸다.
- **B1 미끄러뜨리기**(베이스). 잡은 채 좌우로 구역을 넘으면 다음 음으로
  이어진다. 소리는 `holdOn(glideF: 앞 주파수)`, 판에는 다섯째 칸에 1 —
  재생 쪽 `buildRowsPattern` 이 `n[4]==1` 을 같은 뜻으로 읽으므로 **친 소리와
  재생이 일치한다.** 엔진·patterns·tap_rec 는 손 안 댔다(출구가 이미 열려
  있었고 이 화면만 안 쓰고 있었다). 같은 칸 안에서 미끄러지면 쪼개지 않고
  높이만 갈아 끼운다 — 쪼개면 '판:칸' 키 충돌로 앞 토막이 사라진다.
- **B5 톡 치면 짧게.** 90ms 안에 떼고 8px 안에서 끝났으면 길이 1칸.
  `lenOf` 의 바닥값이 격자 한 단위라 이게 없으면 아무리 톡 쳐도 전부 8분 —
  스타카토 베이스(펑크·디스코의 뼈대)를 **아예 못 만들었다**.
- **B3 코드 두께.** 같은 칸에서 120ms 안에 닿은 손가락 수가 3화음/7화음/
  9화음을 정한다. 벌린 거리는 안 잰다(손·화면 크기마다 달라 문턱을 못 잡음).
  종류는 스케일에서 뽑으므로 조 밖 음이 안 나오고, 9도가 14반음이 아닌
  자리에서는 7화음에서 멈춘다. `theory.dart` 에 표를 새로 쓰지 않았다.
- **D1 쓸기 문턱**(판정 개선). `_pressMove` 가 **시간 제한 없이** `dy>=36`만
  보던 탓에, 긴 음을 1~2초 잡고 있는 동안 손가락이 천천히 흘러내리기만 해도
  쓸기로 읽혀 `holdOff`+`cancel` 이 돌았다 — **그 음이 소리도 기록도 없이
  사라지고 옥타브까지 바뀌었다.** 28px + 닿은 뒤 160ms 안 + 세로가 가로의
  1.6배일 때만. 이 규칙이 B1(가로 글라이드)의 축 우선도 같이 정한다.

### 덤으로 고친 것
- 코드 소리를 `batch`(8초짜리 한 방, 번호가 없어 `holdOff` 로 못 끊는다)에서
  **`holdOn` + 전용 번호**(`_kChordId`)로 바꿨다. 이제 떼면 멎고, 두께가
  바뀌어도 앞 화음이 겹쳐 지저분해지지 않는다. 주석은 원래 "놓을 때
  `holdOff` 로 끊는다"고 적혀 있었는데 실제로는 안 끊겼다.
- 검토 화면의 "N번 쳤어요"가 롤로 들어온 칸을 안 세서 **실제로 적히는 것과
  다른 숫자**였다. `_playedText()` 로 합쳐 세고, 롤이 섞였으면 "(굴린 것
  포함)"을 붙인다.

### 확인
- `flutter analyze` 36건(기준선 유지), `flutter test` **140개 전부 통과**
  (132 → 140). 새 시험은 `test/doodle_gesture_test.dart` — 위 세 버그의
  근거와 글라이드·스타카토·16분 격자가 **재생까지 이어지는지**를 잰다.
- 실기기(RFKL505TRAZ, **릴리스 빌드**): 단계 순서 KICK·SNARE·HI-HAT·
  CHORD·BASS 확인. 세기 띠·음 띠·단계별 안내 문구 확인. 꾹 눌러 롤 →
  "23번 쳤어요 (굴린 것 포함)", 편집기 격자에 **붙어 있는 16분 칸**과
  밝기가 다른(세기가 갈린) 칸 확인. 베이스 2마디에 **Eb1·G1·Bb1·C1 네
  가지 높이** 확인 — 고치기 전이라면 전부 한 줄에 붙었을 자리다.
- **아직 귀로 못 들어 본 것**: 미끄러뜨리기(B1)의 실제 글라이드 소리.
  데이터·재생 경로는 시험으로, 소리 경로는 코드로 확인했으나 실기기에서
  손으로 끌어 본 것은 아니다.

**다음 후보**(이번엔 뺀 것): A1 치는 자리(드럼 가로 = 림/클랩/라이드 —
넣으면 `_commit` 이 레인 하나만 비우는 것도 같이 고쳐야 유령 타격이 안
남는다), B4 코드 자리바꿈, C1 판 통째 밀기(`TapRecorder` 에 원본 위치
보관이 선행), C3 방금 것 지우기.

## 2026-09-21 (2) — 씬과 박자가 안 맞던 진짜 이유 + 판 길이를 씬에 맞춤 + 손짓을 보이게

사용자 신고: "씬 재생이랑 두들플레이 박자가 안맞아 그리고 왜 두마디만하는거야
씬따라서 2,4,8 마디 해야지" / "제스처 너무 다들 직관적이지 못 해".

### A. 「박자가 안 맞아」 — 자가 **버퍼 한 통만큼** 늦게 울리고 있었다

두 경로가 **서로 다른 시계**로 시각을 쟀다. 이건 추측이 아니라 코드로 확인했다.

- 씬 루프: `delay = (nextAt - engine.nowFrames)/SR` → 엔진이 `_now + delay` 를
  더하면 정확히 `nextAt`. **렌더 커서 기준이고, 맞다.**
- 자(메트로놈): `nowSec = pos * _loopSec` 로 쟀는데, 그 `pos` 는
  `loop.posOf(engine.nowFrames - buffered)`(`audio_isolate.dart:847`) =
  **귀에 들리는 자리**다. 그런데 그 delay 도 결국 같은 렌더 커서에 얹힌다
  (`engine.dart:358`). 그래서 자만 정확히 `buffered` 만큼 늦게 났다.

크기: 3072프레임이면 **64ms**(78BPM 16분음표의 33%). 시험으로 잰 값 —
보정 전 0.5640s / 보정 후 0.5000s(`test/doodle_timing_test.dart`).

처방(전부 이 화면 안):
- `_sendMet` 이 넘기는 기준을 렌더 시계로 올린다: `pos*_loopSec + _buffered/48000`.
  `_buffered` 는 **실측값**을 쓴다 — `AudioStats` 가 `aheadFrames` 라는 이름으로
  보내지만 실은 `buffered` 다(보내는 쪽 10번 칸). 목표치와 달라질 수 있다.
- 롤(`_tickRoll`)도 같은 함정에 빠져 있었다. 소리는 늦고 칸은 격자에 정확히
  찍혀서 **연주 중 귀와 「사용하기」 뒤 재생이 달랐다.** delay 에서만 빼고
  칸 번호는 그대로 둔다(그쪽이 맞다). 이미 지난 격자는 0으로 밀지 않고 건너뛴다.
- `_metSent` 를 **진짜 한 바퀴일 때만** 비운다. 위치 보간은 실측값과 만나는
  자리에서 조금씩 뒤로 흔들리는데(`TapClock` 도 같은 이유로 전용 가드가 있다),
  그 미세 역행까지 랩으로 읽어 같은 박을 두 번 보내 플램으로 들렸다.
- `_armStage` 가 `_metSent`·`_metLastNow` 를 안 비워서 새 단계 카운트인의
  첫 박이 막힐 수 있었다.
- `_beatSec` 를 박자표에서 끌어온다(`_stepSec * _meter.clickSteps`). `60/bpm`
  고정이라 6/8·7/8 에서 자가 **절반 속도**로 울렸다. 4/4 에서는 값이 같아
  회귀가 아니다(시험이 둘 다 잰다).
- **버퍼를 내리지 않는다.** 2048로 낮췄던 것은 "늦게 난다"를 버퍼로 때우던
  대증요법이었는데, 이 화면은 씬 루프를 **틀어 놓은 채** 돈다. 곡 재생에
  `kAheadSong` 을 쓰는 근거가 "급식 루프가 상시 20~36ms 밀린다"(1728프레임)이고
  장치 버퍼 1024를 더하면 2752 — 2048로는 평소에도 모자랐다. 이제 바닥을
  3072로 두고 **내리지만 않는다**(`_kDoodleAheadFloor`).
- `TapRecorder.latencySec` 를 버퍼 손잡이에서 떼어 상수로 고정
  (`_kTouchLatencySec = 0.03`). `loopPos` 가 이미 DAC 출력 지점이라 여기서
  보정할 것은 **터치 입력 지연뿐**인데, 그 값이 `aheadFrames` 를 따라다녔다.
  자를 고치면서 **같이** 고쳐야 한다 — 전에는 늦은 클릭에 맞춰 늦게 치고
  그만큼 되돌려 담아 **우연히** 맞던 상태였다.

### B. 「왜 두마디만」 — 2마디 고정이 씬 루프까지 잘라 먹고 있었다

`_kDoodleBars = 2` 가 11군데에 박혀 있었다. 그런데 더 나쁜 것은 **순서**였다:
`initState` 가 `playLoop` **보다 먼저** 드럼·코드·베이스 클립을 2마디 빈
패턴으로 갈아 끼우는데, 씬 루프 길이는 `SceneSequencer.build` 가 "그 씬에서
제일 긴 패턴"으로 정한다. 즉 **이 화면에 들어오는 것만으로 씬이 2마디로 잘렸다.**

처방: `late final int _bars` 로 바꾸고, **클립을 비우기 전에**
`SceneSequencer.sceneLoopBars(p, p.currentScene)` 로 재서 2/4/8 로 갈무리한다.
`Project.loopBarsOf` 는 쓰지 않는다 — `audible(t)` 를 안 봐서 실제 루프를
만드는 `build()` 와 규칙이 다르다(둘이 어긋나면 반드시 사고가 난다).
`NotePatternDef` 에 `spb: project.spb` 도 같이 넘긴다(기본값 16이 박혀 나가
홀수 박자에서 판 길이가 갈렸다).

`_bars == _loopBars` 가 되면 `TapRecorder.stepOf` 의 되접기(`% steps`)가
항등이 되어, 루프 뒷마디에 친 것이 앞마디 위에 겹쳐 적히던 것도 사라진다.

### C. 손짓을 「외우는 규칙」에서 「보이는 과녁」으로

사용자 지적: "제스처 너무 다들 직관적이지 못 해". 맞는 말이라 원칙을 바꿨다 —
**보이는 것을 친다.**

- **세기: 위가 아니라 가운데.** 「위를 치면 세게」는 실제 악기에 없는 규칙이라
  순수 암기였다. 진짜 드럼은 한가운데가 꽉 차고 가장자리가 여리다. 그래서
  화면 한가운데 동그라미를 **과녁 그 자체**로 만들었다(여태 아무 기능 없는
  장식이었다). 패드를 화면 정중앙으로 옮겨 바깥 테두리와 **동심원**이 되게
  했다 — 실기기에서 두 원이 따로 놀던 것을 보고 고쳤다. 보이는 동그라미와
  실제 과녁이 어긋나면 "가운데가 세게"가 그냥 거짓말이 된다.
- **베이스: 좌우 1·3·5·7도 → 세로 사다리 5칸.** 「3도」는 악보를 아는 사람의
  말이고 이 앱의 첫 규칙은 악보를 몰라도 되는 것이다. 「높은 음은 위」는
  아무도 안 배워도 안다. 아래 「낮게」 ~ 위 「높게」, 칸마다 띠로 보이고
  지금 잡은 칸은 밝게 켜진다. 고르는 것은 여전히 그 칸 코드 안의 음뿐이라
  무엇을 눌러도 안 틀린다.
- **옥타브 쓸기는 베이스에서 없앴다.** 사다리가 그 범위를 덮고, 무엇보다
  세로 위치(세기)와 세로 이동(옥타브)이 **같은 축에서 싸우고 있었다.**
  미끄러뜨리기도 가로 → **세로**로 옮겼다(사다리를 타고 미끄러진다).
  베이스 세기는 자동(2) — 베이스는 세기보다 음과 길이가 크게 들린다.
- 코드 단계는 그대로 둔다(손가락 개수 = 두께, 위아래 쓸기 = 옥타브).
  느낌 카드 안은 사용자가 뺐다.

### 확인
- `flutter analyze` 36건(기준선), `flutter test` **148개 통과**(140 → 148).
  새 시험 `test/doodle_timing_test.dart` 가 자 지연·역행 판정·판 길이·박자표를 잰다.
- 실기기(RFKL505TRAZ, 릴리스): 박 도트가 **16개**(4마디×4박)로 늘었고,
  편집기 머리글이 **「4마디」**, 마디 탭이 1·2·3·4 로 나온다(전에는 2마디).
  베이스 사다리에서 아래→위로 쓸어 친 것이 **G1 → Bb1 → C2** 로 올라가 적혔다.
  드럼 패드가 화면 정중앙이고 바깥 테두리와 동심원이다.
- **아직 귀로 못 확인한 것**: 자와 씬이 실제로 딱 붙는지는 숫자로만 쟀다
  (시험 0.5640 → 0.5000). 사람 귀로도 맞는지는 사용자 확인이 필요하다.

**남은 것**: 8마디 씬이면 한 바퀴가 23.5초(92BPM)다 — 길다. 카운트인·진행
표시를 그대로 둘지 손볼지 정해야 한다. `tap_sheet.dart`·`live_view.dart` 도
같은 자 지연 함정을 갖고 있다(이번엔 두들플레이만 고쳤다).

## 2026-09-22 — 두들플레이는 **빈 씬 + 메트로놈**으로 시작한다

사용자 지시: "두들플레이 시작할 때 멜로디 넣지마 씬 아무것도 없는상태에서
해야지 메트로놈만 넣어."

- 들어올 때 **두드려 채울 세 트랙(드럼·베이스·코드) 말고 전부 재운다.**
  나갈 때 원래 음소거 상태로 되돌린다(`_priorMute`).
- **지우지 않고 음소거다.** `setClip(t, null)` 로 비우면 이미 골라 둔 가락이
  씬에서 떨어져 나가고, 0.8초 뒤 자동 저장(`store.touch`)이 그 상태를 파일에
  그대로 쓴다 — 중간에 앱이 죽으면 영영 못 되찾는다. 음소거는 못 되돌려도
  사용자가 믹서에서 바로 켠다.
- **자(메트로놈)를 대기 구간부터 울린다.** 여태는 미리 세기(count)부터였는데,
  그때는 씬의 가락이 깔려 있어 화면이 조용하지 않았다. 이제 씬을 통째로
  재우므로 대기가 **완전한 침묵**이 된다 — 4마디면 길게는 12초다. 그동안
  아무 소리도 안 나면 고장 난 화면으로 보이고 박을 미리 타 볼 수도 없다.
  (`_sendMet` 의 갈림을 `phase == done` 일 때만 빠지게 바꿨다)

### 순서가 전부다 — 시험으로 박아 뒀다
`sceneLoopBars` 는 `audible(t)` 를 본다. 그래서 **재우면 씬 길이가 줄어든다.**
마디 수(`_bars`)는 반드시 **재우기 전에** 재야 한다. 순서가 뒤집히면 4·8마디
씬이 조용히 짧은 판으로 줄어든다(이 저장소가 이미 같은 모양으로 당했다 —
클립을 먼저 비우는 바람에 씬 루프가 2마디로 잘렸던 건).
`test/doodle_timing_test.dart` 가 가락을 8마디로 늘려 두고 잰다: **8 → 4**.

### 확인
- `flutter analyze` 36건(기준선), `flutter test` **150개 통과**(148 → 150).
- **실기기 확인은 아직 못 했다** — 폰(RFKL505TRAZ)이 분리된 상태다.
  릴리스 APK 는 빌드돼 있다(`build/app/outputs/flutter-apk/app-release.apk`).
  붙는 대로 확인할 것: (1) KICK 단계에서 가락이 안 들리고 자만 들리는지,
  (2) 대기 구간에도 자가 도는지, (3) 나간 뒤 씬에서 가락이 되살아나는지.

## 2026-09-22 (2) — 표본 악기가 손을 떼도 안 멎고 있었다

사용자 요청: "UI, UX 실험하고 개선점 보고해 / 두들플레이 제스처도 개선점 보고해 /
악기들 퀄리티도 개선하고". 아래는 **악기** 쪽 실행분이다.

### 무엇이 틀렸나

`SynthNote.release()` 는 `_env` 와 `_life` 만 건드린다. 그런데 표본 경로
(`_nextSample`)는 **둘 다 안 읽는다** — 페이드를 `_age >= _smRelStart` 로만
건다. 게다가 `_life` 를 깎는 줄(synth.dart 의 합성 경로)은 `next()` 가 표본일 때
먼저 `return` 해서 **아예 안 탄다.**

`Engine.noteHold` 는 `dur` 자리에 `maxSec = 12.0` 을 넘긴다. 그래서
`_smRelStart = 12초` 가 되고, 손을 떼도 표본이 **끝까지 그대로 울었다.**
`_startSample` 머리말이 "손을 늦게 뗀 라이브 연주는 짧게 눌러 끈다"고 적어 둔
바로 그 동작이 **배선만 빠져 있었다.**

시험으로 잰 값 — 손을 뗀 **1.2초 뒤**에 남아 있던 소리:

| 악기 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 트럼펫 | **126%** (더 커진다) | 0.00% |
| 색소폰 | **101%** | 0.00% |
| 첼로 / 바이올린 | 83% / 81% | 0.00% |
| 전자피아노 | 63% | 0.00% |
| 기타 / 핑거베이스 | 24% / 21% | 0.00% |
| 피아노 | 11% | 0.00% |

9개 표본 악기 중 **8개**가 안 멎고 있었다(업라이트만 소리가 워낙 작아 해당 없음).
빠른 연타에서는 안 꺼진 음이 겹겹이 쌓여 뭉갰다.

### 고친 방법
- `release()` 에 표본 분기를 넣어 `_smRelStart` 를 지금 자리로 당긴다.
  `math.min` 이어야 한다 — 그냥 대입하면 이미 잦아들던 음에 release 가 또 왔을 때
  페이드가 처음으로 **되감긴다**(시험이 이것도 잰다).
- 재우는 길이는 악기마다 다르다(`_smRelHold`). 뜯는·치는 악기는 `RING` 의 첫 값
  (피아노 0.34s, 기타 0.40s)만큼 더 울리고, 표에 없는 켜는·부는 악기는 80ms 에
  바로 멎는다 — 활·숨이 멎으면 소리도 멎는 게 맞다.
- `_startSample` 에 `voice` 인자 하나 추가(호출부는 한 곳).

### 확인
- 새 시험 `test/sample_release_test.dart` — 표본 악기 **전부**를 돌며 잰다.
  `git stash` 로 고치기 전 상태를 만들어 **정말로 실패하는지도 확인**했다(8건 실패).
  기존 `sampler_check_test.dart` 는 `dur` 를 짧게 준 경우만 재서 이걸 못 잡았다 —
  `release()` 를 한 번도 안 부른다.
- `flutter analyze` 36건(기준선), `flutter test` **153개 통과**(150 → 153).
- 실기기 릴리스 설치 완료. **귀로 확인은 아직** — 사용자 차례.

### 아직 안 한 것 (감사에서 나온 나머지)
- **악기별 레벨 트림** — 자산 RMS 최대 **27.4dB** 차(업라이트가 색소폰보다 그만큼
  작다). 이번 시험에서도 C4 기준 업라이트 0.00019 vs 색소폰 0.39392 로 그대로
  보인다. 다만 고치면 `kStyleGain` 을 17개 스타일 전부 다시 재야 한다
  (jazz 1.72 는 **안 들리는 업라이트를 메꾸려고** 올려 둔 값이다). 귀로 확인이 필요.
- **표본 경로 세기 로우패스** — 기타·색소폰·업라이트는 세기 층 절반이 md5 기준
  같은 파일이라 세게 쳐도 밝아지지 않고 커지기만 한다.
- **시작 위상 무작위화** — 패드가 칠 때마다 똑같이 웅웅거린다. 3줄이면 되지만
  렌더 결과에 수치를 박아 둔 시험 3개의 기준값을 다시 잡아야 한다.

## 2026-09-22 (3) — 악기 레벨 맞추기 + 두들플레이 탈출 + 이름 노출

사용자 지시: "개선해"(= 앞서 제안한 ①탈출 ②레벨 ③UI 순서대로).

### ① 두들플레이에서 나갈 수 있게

`canPop: _allDone` 만 있고 `onPopInvoked` 가 없어서 **X 도 뒤로가기도 아무 일을
안 했다** — 다섯 단계를 다 끝내기 전엔 나갈 길이 아예 없었다(실기기 확인).
이 화면은 씬의 다른 트랙을 재워 두므로, 갇힌 사람이 앱을 강제 종료하면
`dispose` 의 복구가 안 돌고 자동 저장이 **음소거된 채로** 파일에 쓴다.

이제 X·뒤로가기가 확인 대화상자를 띄운다. 이미 「사용하기」로 넘긴 단계는
씬에 남고 지금 치던 판만 버려지는데, 그걸 그대로 문장으로 적어 준다.
실기기에서 X → 「그만하기」 → 나가짐 → 믹서에서 음소거 복구까지 확인했다.

### ② 표본 악기 레벨 — 33.3dB 차를 1.0dB 로

`test/sample_level_meter.dart` 를 새로 만들어 **각 악기를 제 음역대에서** 쟀다
(베이스를 C4 로 재면 원래 안 나는 소리를 늘려 재는 셈이라 값이 엉뚱해진다).
실측 vel3: sax −7.7 · epiano −12.4 · guitar −13.9 · piano −15.3 · trumpet −17.1
· cello −17.7 · fingerbass −21.1 · violin −25.7 · **upright −41.0** dBFS.

**함정 두 개를 연달아 밟았다. 둘 다 시험이 잡았다:**

1. **RMS 로 맞추면 안 된다.** 뜯는 악기는 어택만 뾰족하고 뒤가 조용해서 RMS 가
   낮다. 지속음(색소폰)에 RMS 로 맞추자 **핑거베이스 피크가 +11.0dB** 로 천장을
   뚫었다. → **피크**를 −6dBFS 에 맞춘다(−6dB 는 화음이 겹칠 자리).
2. **왼쪽 채널만 재면 안 된다.** 오른쪽으로 패닝된 악기(바이올린 +0.35 ·
   기타 +0.2 · 전자피아노 +0.1)는 오른쪽이 더 크다 — 바이올린이 −0.9dB 까지
   올라가 있었다. → 양쪽 채널의 **최대**로 잰다.

결과: 9개 악기 전부 피크 −6dB, **폭 1.0dB**(33.3dB → 1.0dB).

세기도 같이 고쳤다. 첼로·바이올린·피아노·트럼펫·핑거베이스는 **녹음 자체에**
세기가 들어 있는데(실측 ff−pp 20~25dB) 거기에 신스용 `VG`(−10.5dB)를 또
곱해서 여리게가 **−43dB** 까지 내려가 안 들렸다. 그 악기들만 완만한 표
(`kSampleVG`, −1.9dB)를 쓴다. 나머지(기타·전자피아노·색소폰·업라이트)는 세기
층 절반이 사실 같은 파일이라 기존 `VG` 를 그대로 써야 세기 차이가 남는다.

**업라이트는 원본이 문제다** — 파일 피크가 −20~−46dB 다(다른 팩은 0dB 로
정규화돼 있다). 이 팩만 정규화가 안 됐다. 런타임에서 14배 올려 맞췄지만,
원본을 다시 만드는 게 맞다.

**`test/mix_meter.dart` 는 안 돌렸다** — `ensureSamplesLoaded()` 를 안 불러서
표본을 **합성 대체음으로** 잰다(`voice_meter.dart` 와 같은 함정). 10분을 돌려도
이 변경은 안 잡힌다. 덤으로 알게 된 것: `kStyleGain` 은 애초에 합성 대체음
기준으로 맞춰진 값이고, 앱은 표본으로 운다 — **이 둘이 어긋나 있다.**

### ③ 이름·글자
- 패턴 이름이 `doodle_drum_1790002094977` 로 **그대로 보였다**. 판 이름 자체는
  못 바꾸지만(저장된 곡들이 그 이름으로 서로를 가리킨다) 보여 줄 때 바꾼다 —
  `doodleLabel()` → 「두드린 드럼 · 두드린 베이스 · 두드린 코드」.
- 홈 화면 맨 아래 안내문이 두 줄이 되며 둘째 줄이 잘렸다. 한 줄에 들어가게
  줄이고 아래 여백을 24 로 늘렸다.

### 확인
- `flutter analyze` 36건(기준선), `flutter test` **156개 통과**(153 → 156).
- 새 시험 `test/sample_level_test.dart` — 천장 넘김 0건, 악기간 폭 3dB 이내,
  세기 방향이 맞는지. 위의 함정 두 개를 다시 밟으면 여기서 걸린다.
- 실기기 확인: 홈 안내문 · 패턴 이름 · 탈출 + 음소거 복구.
- **귀로는 아직** — 레벨이 맞았는지, 표본이 손을 떼면 멎는지는 사용자 차례.

### 남은 것
- 표본 경로 세기 로우패스(세게 쳐도 안 밝아진다) · 시작 위상 무작위화.
- `kStyleGain` 을 **표본을 읽은 상태로** 다시 재기(위 함정 때문에 지금 값은
  표본과 안 맞는다). `mix_meter.dart` 에 `ensureSamplesLoaded()` 를 붙이는 게 먼저.
- 업라이트 표본 원본 재정규화.

## 2026-09-22 (4) — 긴 음의 가운데를 만질 수 없던 것

사용자 신고: "미디 편집할때 긴 블럭의 중간부분을 길게누르면 그 블럭을 잡아야지
왜 새로 생성되니 이런식으로 사용감 하나하나 분석해서 개선해".

### 원인
손끝 판정이 음의 **시작 칸**만 봤다 — `_noteAt(degree, step)` 은
`n[1] == step` 이다. 그런데 음은 `[도수, 시작칸, 길이, …]` 라 여러 칸에 걸쳐
있다. 6칸짜리 음의 5번 칸을 누르면 **빈 칸으로 읽혀 그 위에 새 음이 깔렸다.**
눈에는 한 덩어리로 보이는데 **만질 수 있는 곳은 왼쪽 끝 한 칸뿐**이었다.

재현(위젯 시험, 고치기 전):
```
[[10, 2, 6, 2]]                       ← 6칸짜리 음 하나
가운데 꾹 누름 → [[10,2,6,2], [10,5,2,2]]        ← 둘이 됐다
끌기까지   → [[10,2,6,2], [10,5,2,2], [11,5,2,2]] ← 셋
```

### 고친 곳
`_noteCovering(degree, step)` 을 넣고 **세 곳**을 바꿨다 —
꾹 누르기(`_dragStart`) · 탭(`_tapNote`) · 지우개(`_eraseAt`).
잡은 뒤에 쓰는 칸은 **그 음의 시작 칸**이어야 한다(`found[1]`) — 길이 고치기
(`NoteOps.setLen`)도 줄 옮기기도 시작 칸으로 음을 찾기 때문이다. 누른 칸을
그대로 쓰면 엉뚱한 자리를 고치거나 아무 일도 안 일어난다.

### `_paintAt` 은 **일부러 안 바꿨다**
처음엔 거기도 `_noteCovering` 으로 바꿨는데 `editor_ui_test.dart` 10번이
깨졌다 — 「빈 칸을 꾹 눌러 옆으로 끌면 **쫘르륵 깔린다**」는 지나간 칸마다
하나씩 찍는 동작이고, 기본 길이가 2칸이라 덮인 칸을 건너뛰게 하면 한 칸 걸러
하나만 찍혀 줄이 성겨진다. 잡기·지우기와 달리 여기만 규칙이 다른 이유를
코드에 적어 뒀다. (시험이 잡아 준 과잉 수정이다)

### 확인
- `test/editor_hit_test.dart` — 판정 **규칙**을 잰다(덮는 칸 전부에서 잡히는가,
  경계가 안 새는가, 시작 칸을 돌려주는가).
- `test/editor_longnote_ui_test.dart` — 실제 편집기 위젯을 띄워 **배선**을 잰다.
  규칙만 맞고 배선이 빠지면 시험은 통과하는데 손은 헛돈다.
  `git stash` 로 고치기 전 상태에서 **정말 실패하는지도 확인**했다.
- `flutter analyze` 36건(기준선), `flutter test` **167개 통과**(164 → 167).
- 실기기 확인은 **결론이 안 났다** — 격자가 스크롤돼 행 좌표가 어긋난다.
  그래서 위젯 시험으로 고정했다(이 화면은 앞으로도 그게 맞다).

### 이어서
편집기 사용감 전반을 세 관점(손끝 판정·고치는 동작·피드백)으로 감사 중.

## 2026-09-22 (5) — 편집기 사용감 감사 1·2순위

세 관점(손끝 판정·고치는 동작·피드백)으로 감사하고, **확인된 것만** 고쳤다.

### ⚠️ 1순위 — 앞 회차 수정이 만든 새 어긋남

`_noteCovering` 을 넣으면서 목록을 **앞에서부터** 찾게 했는데, 그리는 쪽
(`_GridRow` 의 `Stack`)은 목록 **뒤쪽 음을 위에** 그린다.

「쫘르륵 깔기」는 칸마다 기본 길이 2칸으로 찍으므로 이웃끼리 **1칸씩 겹친다**
(의도된 동작 — `editor_ui_test.dart` 10번이 못 박아 둔 것). 그 겹친 칸을
누르면 **눈에 보이는 막대가 아니라 그 밑에 깔린 왼쪽 음이 잡혔다.**

재현(고치기 전):
```
[[10,2,2,2], [10,3,2,2]]   ← 3번 칸이 겹친다. 위에 보이는 건 3번 시작 음
3번 칸 두 번 탭 → [[10,3,2,2]] 남음   ← 안 보이던 2번 음이 지워졌다
```
→ **뒤에서부터** 찾게 고쳤다. 고친 뒤 `[[10,2,2,2]]` 가 남는다(보이던 것이 지워짐).

### 2순위 — 칸 경계를 누르면 아무 일도 안 일어났다

`_Cell`·`_NoteBar` 의 `GestureDetector` 가 기본값(`deferToChild`)인데 자식
`Container` 에 1px 여백이 있어 **그 띠는 손끝을 안 받았다.** 30px 칸에서 양옆
1px 씩이다. 폰에서 손가락은 칸보다 굵으므로 경계 가까이를 누르면 조용히
무시됐다 — 같은 자리를 꾹 누르면 되는데 그래서 더 헷갈린다.
→ `behavior: HitTestBehavior.opaque` 두 줄. 꾹 누르기는 그대로다(격자 부모가
`translucent` 라 끌기가 계속 통한다 — 시험 10~13번이 안전망).

재현(고치기 전): 칸 왼쪽 끝 0.5px 탭 → **음 0개**. 고친 뒤 1개.

### 확인
- `flutter analyze` 36건(기준선), `flutter test` **171개 통과**(167 → 171).
- 두 건 모두 `git stash` 로 고치기 전에 **정말 실패하는지** 확인했다.

### 감사가 짚은 나머지 (아직 안 고침 — 확인은 됨)
- ▶ 로 못 움직였는데 **선택만 옆 음으로 넘어가** 휴지통이 남의 음을 지운다.
- 길이를 늘리면 **뒷 음 위를 덮어** 그 음을 못 만지게 된다(`NoteOps.setLen`).
- 음을 꾹 **잡았다 그냥 떼기만 해도** 되돌리기가 헛돌고 다시하기가 사라진다.
- 음 하나를 고르면 아래 바가 커지며 격자가 **30px 출렁여** 다음 손가락이
  다른 줄에 떨어진다.
- 3/4·6/8 곡에서 ▶ 가 재생 막대 없는 자리로 화면을 끌고 간다(`spb` 누락).
- 막혀서 못 하는 동작에 **아무 반응이 없다**(진동 없음).

### 정리
감사가 검증용으로 만든 `test/tmp_scratch/*` 4개가 앞 커밋에 딸려 들어갔다.
이번 커밋에서 지운다.

## 2026-09-22 (6) — 편집기 사용감 3·5번 + 4번은 되돌림

### ▶ 로 못 옮겼는데 휴지통이 **남의 음**을 지웠다

`NoteOps.move` 는 옮기려는 자리에 이미 음이 있으면 **아무것도 안 하고**
원래 목록을 그대로 돌려준다. 그런데 `_moveNote` 의 성공 판정이 「목표 자리에
음이 있나」였다 — 그러면 거기 있던 **남의 음**을 보고 성공으로 읽는다.
판은 그대로인데 노란 테두리만 옆 음으로 넘어가고, 그 뒤에 휴지통을 누르면
**엉뚱한 음이 사라졌다.**

재현(고치기 전): 4번·5번 음이 있을 때 4번을 고르고 ▶ → 휴지통 →
**5번이 지워지고 4번이 남았다.** 고친 뒤엔 4번이 지워진다.
→ 판정을 「내 음이 제자리를 떠났나」로 바꿨다(한 줄).

### 3/4·6/8 에서 ▶ 가 막대 없는 자리로 화면을 끌고 갔다

그리는 쪽은 `headStep(..., spb: spb)` 인데 따라가기 쪽(`_followPlayhead`)이
`spb` 를 안 넘겨 **기본값 16**으로 쟀다. 6/8(한 마디 12칸) pos=0.5 에서
맞는 값 12.0 vs 빠뜨린 값 16.0 — 4칸 어긋난다. 4/4 에서는 값이 같아
여태 안 드러났다. → `spb: spb` 한 낱말.

### 격자 출렁임은 **되돌렸다** — 고치려던 것보다 나빴다

음 하나를 고르면 아래 바가 42→86dp 로 커지며 **격자가 44px 줄어든다**
(실측 528 → 484). 15줄이면 줄 높이가 35.2 → 32.3 으로 바뀌어 아래쪽 줄이
한 줄 가까이 밀린다 — 다음 손가락이 다른 줄에 떨어진다.

86dp 로 **고정해 봤더니 더 나빴다**: 격자가 448px 이 되어 15줄(최소 줄 높이
32 × 15 = 480px)이 안 들어가고 **맨 아랫줄이 화면 밖**으로 나갔다. 기존 시험
8-b·8-c·8-d 가 맨 아랫줄을 눌러서 한꺼번에 깨졌다(시험이 잡아 줬다).

제대로 고치려면 아래 바를 격자 **위에 띄워야** 한다(레이아웃 자리를 안
뺏게) — 설계 변경이라 따로 다룬다. 코드에 그 경위를 적어 뒀다.

### 확인
- `flutter analyze` 36건(기준선), `flutter test` **177개 통과**(171 → 177).
- 두 수정 모두 고치기 전에 **정말 실패하는지** 확인했다.

### 남은 것 (감사에서 확인됨)
- 길이를 늘리면 **뒷 음 위를 덮어** 그 음을 못 만지게 된다(`NoteOps.setLen`).
- 음을 꾹 **잡았다 그냥 떼기만 해도** 되돌리기가 헛돌고 다시하기가 사라진다.
- 막혀서 못 하는 동작에 **아무 반응이 없다**(진동 없음).
- 아래 바를 띄워서 격자 출렁임 없애기(위 참고).
