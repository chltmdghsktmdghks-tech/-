// P0 — **곡을 끝까지 돌려 본다.** (개선 계획 3-1·3-6 · §19 P0)
//   flutter test test/long_run_test.dart
//
// 골든 시험은 **4초**만 렌더한다. 짧게 보면 못 잡는 것들이 있다:
// 새는 목소리 · 뒤로 갈수록 뒤집히는 크기 · 서서히 밀리는 DC ·
// **특정 구간에서만 넘치는 것**(드롭은 3분짜리 곡의 2분 30초에 온다).
//
// 여기서는 **대표 셋만** 본다 — 열세 곡을 다 돌리면 35분 분량이라 매번 돌리기엔 무겁다.
// 셋은 성격이 갈리는 것으로 골랐다: 성긴 곡(로파이) · 빽빽한 곡(트랩) · 긴 음이 겹치는
// 곡(엠비언트, 동시 발음이 제일 많다). 열세 곡 전부는 `long_run.dart`(수동)가 본다.
import 'long_run.dart';

void main() => runLongCheck(only: const ['lofi', 'trap', 'ambient']);
